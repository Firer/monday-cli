# v0.9 implementation plan

> **KICKOFF 2026-05-22 — v0.9 = the multi-level board cluster, on the
> `2026-01` pin.** Picked at v0.9 candidate-selection per R-NEW-75
> after v0.8.0 shipped (npm `latest`, 2026-05-21T23:45:48Z; annotated
> tag `v0.8.0` at `090fb76`). SDK probe at candidate-selection:
> `@mondaydotcomorg/api` still at 14.0.0 baking `2026-01`; no 15.x
> (`2026-04`) or 16.x (`2026-07`) release. So — third consecutive
> pivot (v0.7, v0.8, v0.9) — the API-version-bump milestones DEFER and
> v0.9 ships the next actionable bundle: the multi-level board surface
> the 2026-05-22 dev-board probe sweep opened up (see user-memory
> `project_multilevel_dev_board_capabilities` + `docs/v0.8-plan.md`
> §22 R-v0.8-NEW-23/24/25, the candidates this plan promotes to
> milestones).
>
> **Why now:** the sweep proved multi-level boards support deep
> subitem nesting + a real `subtasks`-column data model, ALL reachable
> at the CLI's pinned `2026-01` (no SDK bump needed) — and it surfaced
> a SHIPPED-CORRECTNESS bug: v0.8.0's `item create --parent` rejects
> multi-level boards with a now-false error. v0.9 fixes that and builds
> out the surface.
>
> **DEFERRED (retain numbers; reopen when the gating SDK publishes):**
> M39 (API `2026-04` pin) + M40 (`item set-description` — proven to
> work on `multi_level` boards but the mutation is `2026-04`-only) +
> M41 (`doc block-create-bulk`) need SDK 15.x; M44 (user-entity
> migration) + M45 (`user activity`) need SDK 16.x.

## 1. Milestones

> **Scope: full multi-level cluster** (chosen at candidate-selection;
> the shipped `--parent` fix folds into M50, no interim v0.8.1 patch).
> Per-milestone decisions land at each milestone's pre-flight contract
> diff.

| M | Surface | Wire op | Status |
|---|---------|---------|--------|
| **M50** | **Multi-level subitem nesting.** Lift the `item create --parent` `multi_level` rejection; support creating nested subitems on multi-level boards (same-board model — subitems live on the host board with `parent_item` links + the board's self-referencing `subtasks` column, NOT a separate `sub_items_board`). Corrects the shipped v0.8.0 false-claim error. Closes the M28 deferral (slipped v0.4→v0.9). | `create_subitem(parent_item_id)` — existing wire, unified dispatch (no `hierarchy_type` branch) | **PRE-FLIGHT DONE 2026-05-22** (docs-only contract diff + D1–D5 closed; decision: MINIMAL LIFT — delete the inverted `multi_level` gate, no classic-board rejection added; see §3). IMPL next: delete the rejection block + flip the existing test + close-docs. |
| **M51** | **Multi-level board awareness.** Surface `Board.hierarchy_type` (`classic`\|`multi_level`) in `board get`/`describe`/`list` projections (currently read internally for the M28 rejection but not emitted — needs raw GraphQL per SDK drift). Document `board duplicate` as the multi-level board creation path (verified: `duplicate_board_with_pulses` preserves `multi_level`; `create_board` cannot — no hierarchy arg). | read-side projection extension; no new mutation | SKELETON |
| **M52** | **Board views read.** Add a read-only `views` slot to the board projection (and/or a `board views <bid>` verb) — `board.views { id name type settings_str view_specific_data_str settings sort filter access_level }`. Kanban DATA ops already work via existing verbs (status-label create = Kanban columns; `item set <status>` / `item move --to-group` = move card); this closes only the view-metadata read gap. | `board.views` read | SKELETON |
| ~~M39 / M40 / M41~~ | API `2026-04` pin + `item set-description` + `doc block-create-bulk` | — | DEFERRED — SDK 15.x not published |
| ~~M44 / M45~~ | user-entity migration + `user activity` | — | DEFERRED — SDK 16.x not published |

**Rough build order:** M50 (headline; shipped-correctness fix; pre-probed) → M51 (board awareness; small read-side) → M52 (views read) → release-prep (version bump 0.8.0 → 0.9.0 + CHANGELOG + close-docs, per the R-NEW-82/84 baseline).

## 2. Empirical probe basis (M50 — already done)

The 2026-05-22 dev-board sweep IS M50's pre-flight wire probe. Pinned:
- `create_subitem(parent_item_id)` nests to depth-3+ on `multi_level`
  boards at API `2026-01` (the CLI pin) — `parent_item` chains, all on
  the host board, the board carries a `subtasks`-type column
  self-referencing `boardId: <self>`.
- `Board.hierarchy_type` ∈ {`classic`, `multi_level`}; readable at
  `2026-01` via raw GraphQL (SDK-drift field, like `is_leaf`).
- Classic boards genuinely lack the nesting home at `2026-01` — so M50
  must KEEP a (corrected) classic-board path: reject classic-board
  `--parent` nesting with an accurate message, allow it on multi-level.

Probe scripts: `scripts/probe/m40-multilevel-*` + `kanban-columns.ts`
(local/gitignored). Full map: user-memory
`project_multilevel_dev_board_capabilities`.

## 3. Per-milestone decisions

> Land at each milestone's pre-flight contract diff. Expected M50
> dimensions: (D1) classic-vs-multi_level dispatch — how `item create
> --parent` branches; (D2) the corrected classic-board rejection
> message + whether it keeps a `deferred_to` slot or becomes a flat
> "not supported on classic boards"; (D3) nested-create dry-run
> envelope; (D4) `item subitems` read parity on multi-level (same-board
> model); (D5) ERROR_CODES delta (expected zero).

### M50 — multi-level subitem nesting (pre-flight CLOSED 2026-05-22)

**Probe basis:** the 2026-05-22 dev-board sweep (§2) IS the wire
probe — no new probe run. All facts verified at API `2026-01` (the
CLI's pin, NOT SDK-gated).

**Headline finding (drove the scope).** The current runtime gate is
*inverted*. `src/commands/item/create.ts` rejects ONLY
`hierarchy_type === 'multi_level'` — the case the probe proved WORKS
(depth-3+) — while classic / `top_level` / `null` boards already
create subitems (shipped M9, v0.2). The genuinely-unsupported case
(classic *deep* nesting, subitem-of-a-subitem) is what the M28 premise
was really about, and it is already handled accurately by
`deriveSubitemsBoardId` ("no subtasks column") when `--set` is present.
So M50 IMPL is a *deletion* of the false-claim gate, not a
re-implementation. `item subitems` already reads multi-level
hierarchies (hierarchy-agnostic `subitems` connection); no read-side
gap.

**Decision: MINIMAL LIFT** (user-confirmed 2026-05-22 — the unified
single-path option over an explicit classic-board guard).

- **D1 — Dispatch.** Delete the `hierarchy_type === 'multi_level'`
  rejection block (`create.ts:735-752`). Classic AND multi-level flow
  through ONE `create_subitem` + `deriveSubitemsBoardId` path; no
  `hierarchy_type` branch. The `subtasks` column's
  `settings_str.boardIds[0]` transparently yields the correct
  column-resolution target — a separate sub_items_board on classic
  boards, the self-referenced host board on multi-level boards
  (the column self-references `boardId: <self>`). Dry-run and live
  share `resolveCreateMode`, so both lift together.
- **D2 — Classic-board treatment.** The false-claim `multi_level`
  rejection is DELETED outright (no message relocation). NO
  `hierarchy_type`-keyed rejection survives, and NO `deferred_to` slot
  anywhere (keeping `deferred_to: 'v0.9'` while shipping AT v0.9 is the
  R-NEW-82 "wait for the version you're already running" anti-pattern).
  The only board-capability gate is the existing data-model
  `deriveSubitemsBoardId` `usage_error` ("no subtasks column / no
  settings / no linked board"), which fires when `--set` targets a
  board with no subitems lane — accurate, no false claim. Classic
  deep-nesting WITHOUT `--set` falls through to Monday's server-side
  response (the user accepted this over a friendly pre-mutation guard,
  which would have cost an extra parent-board lookup on every subitem
  create).
- **D3 — Dry-run envelope.** No shape change. The §6.4 "Subitem
  variant" (`operation: "create_subitem"`, `parent_item_id`,
  `board_id` omitted, same `resolved_ids` / `diff`) applies to
  multi-level boards identically; M50 only lifts the gate that
  currently blocks the dry-run from reaching it. cli-design §6.4
  "Classic boards only" qualifier corrected to "Both hierarchy types".
- **D4 — `item subitems` read parity.** Already holds — no change.
  `item subitems` reads Monday's hierarchy-agnostic
  `items(ids) { subitems { ... } }` connection, which returns direct
  children regardless of the classic/multi-level model. Deep reads
  compose via recursive `monday item subitems` calls (documented at
  the command; same on multi-level).
- **D5 — ERROR_CODES delta.** ZERO. Registry stays 29. M50 REMOVES a
  `usage_error` path (the multi_level rejection) and adds none; the
  surviving `deriveSubitemsBoardId` rejections are existing
  `usage_error`s. 117 commands unchanged (no new verb).

**Pre-flight stub: NOT APPLICABLE.** The R-NEW-76 parseArgv-before-
c8-ignore stub convention anchors NEW deferred wire legs; M50 adds
none — `create_subitem` is the existing M9 mutation, live for classic
boards today. So this pre-flight is docs-only (cli-design contract +
this decision list); no `m50_preflight_stub` literal, no new PIN test.
The existing `multi_level board → usage_error` integration test
(`item-create.test.ts:1121`) stays GREEN at pre-flight (runtime
unchanged) and FLIPS to a success assertion at IMPL.

**IMPL hand-off — contract-term checklist (R-v0.7-NEW-4).** The IMPL
session runs a cross-doc grep and flips every site below.

*Should DISAPPEAR from current (non-historical, non-regression-guard)
prose post-IMPL:*
  - the `multi_level` rejection throw + message (`create.ts:735-752`)
  - `details.deferred_to: 'v0.9'` (the slot)
  - "Monday's sub_items_board carries no subtasks column at API
    2026-01" / "depth-2 subitems have no data-model home" (false claim)
  - "Classic boards only" / "classic-board-only" / "classic only" /
    "M9 supports classic only" / "classic-only gate" / "classic-only
    subitem gate"
  - "v0.3 M28 Decision 11 closure" (in the rejection message)

*Source sites needing the flip (enumerated):*
  - `src/commands/item/create.ts`: file docstring (~18-24),
    `resolveCreateMode` docstring step 2 (~716), the rejection block
    (735-752, DELETE), `deriveSubitemsBoardId` docstring (~540, the
    "classic-board model" framing → "classic + multi-level" framing).
  - `src/api/dry-run.ts`: `CreateMode` docstrings (~568-577, ~593-601;
    "classic-board-only", "the hierarchy_type gate (multi_level
    rejection)").
  - `src/api/item-board-lookup.ts`: docstrings (~19 "classic-only
    gate", ~140 "M9's classic-only subitem gate").
  - `tests/integration/commands/item-create.test.ts`: file docstring
    (~20) + FLIP the `multi_level board → usage_error` test
    (1121-1152) to assert a successful `create_subitem` against a
    `multi_level` fixture; ADD a regression guard that the false-claim
    phrase + `deferred_to: 'v0.9'` never reappear in emitted output.

*Should APPEAR post-IMPL:* `v0.9-M50`, "unified subitem dispatch",
"multi-level subitem nesting", "host board", "self-referencing
subtasks column", "depth-3+", "closes M28".

**Codex pre-flight review:** _(filled after the run)_

## 7. v0.9 release exit checklist

> Filled at v0.9 release-prep close. Mirrors v0.8-plan §7.

## 22. R-class register

> Carries forward the v0.8 §22 watch-items (R-v0.8-NEW-22 noun-desc
> dedup, R-v0.8-NEW-21 help-text hygiene, R-v0.8-NEW-19/20, etc.) +
> the promoted candidates (R-v0.8-NEW-23 → M50, R-v0.8-NEW-24 → M51,
> R-v0.8-NEW-25 → M52). Populated at the first M50 close-docs.
