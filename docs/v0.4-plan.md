# v0.4 implementation plan

> **Status:** plan, not contract. The binding contract is
> [`cli-design.md`](./cli-design.md). This document sequences the
> work needed to ship v0.4 of that contract; it doesn't redefine it.
> If a conflict arises, `cli-design.md` wins and this plan is updated.

## 1. Scope summary

v0.4 is "**polish + nice-to-haves** — agents *react* and *attach*,
not just *narrate* and *write*." v0.3 closed the v0.1/v0.2 narration
gap (cross-board reads, item history, partial-success bulk) and the
v0.1/v0.2 outbound-write gap (notifications, webhooks). v0.4 picks
up the remaining cli-design §13 v0.4 candidates: a polling-based
reactivity primitive (`item watch`) so agents can wait for state
changes without scripting their own polling loop, bulk throughput
via `--concurrency` against Monday's per-account concurrency cap,
asset upload (`add_file_to_column` / `add_file_to_update`) so an
agent can attach evidence to its writes, read-only `doc list/get`,
`team` create/manage to close the noun gap, and shell completion
(bash / zsh / fish) for the rare human user. None of these are
load-bearing for the agent-narration story v0.3 already shipped —
v0.4 is what an agent reaches for when the v0.3 surface isn't
enough, not what the agent needs to do its day-one job.

**In scope (from `cli-design.md` §13 v0.4 + Appendix B verb-noun
matrix):**

| Group | Commands |
|-------|----------|
| **reactivity** | `item watch <iid>` (polling-based event stream over the M24 `item history` projection; emits NDJSON event records as state changes; see cli-design §14.4 for the cadence + circuit-breaker pre-flight question). |
| **bulk throughput** | `--concurrency <N>` extension to the v0.2 fail-fast bulk path + the v0.3-M25 partial-success bulk path. Probed against Monday's per-account concurrency cap (was surfaced as `concurrency_exceeded` in earlier API versions; verify still canonical at API `2026-01`). |
| **assets** | `monday item upload <iid> --column <col> <file>` (or similar shape; final argv pinned at the asset-upload milestone's pre-flight) wrapping Monday's `add_file_to_column` mutation. Separate `monday update upload <uid> <file>` for `add_file_to_update`. Multipart wire shape — first v0.4 verb touching `src/api/transport.ts`'s multipart seam. |
| **read-only docs** | `monday doc list` (account-scoped doc directory); `monday doc get <doc-id>` (doc body + metadata). Read-only; full docs CRUD deferred further per cli-design §13 v0.4 entry. |
| **team CRUD** | `monday team create` / `monday team update` / `monday team delete` (Appendix B already lists list/get/create/delete; v0.4 lands the writer surface). |
| **shell completion** | `monday completion <bash|zsh|fish>` emitting completion scripts via commander's built-in machinery. Out-of-band from the GraphQL surface — pure CLI ergonomics for the rare human user. |
| **polish** | Refreshed envelope-snapshot suite for v0.4 surfaces; `output-shapes.md` ToC audit; README quickstart updates; CHANGELOG `0.4.0` entry; version bump. |

**Universal partial-success rule (cli-design §6.1 compliant — same
shape as v0.2/v0.3).** Multi-target verbs in v0.4 (the
`--concurrency` extension to bulk emits the same per-target
`data.results: [...]` envelope v0.3-M25 pinned; `item watch` is
**single-target streaming**, NOT multi-target — it emits one event
record per emitted NDJSON line, with the top-level envelope
covering the watch session itself). The shared per-target dispatch
helper (`dispatchSequential` in `src/api/partial-success-mutation.ts`,
lifted in v0.2) extends with a `dispatchConcurrent` sibling sharing
the same per-target id-field + `foldAndRemap` context — no §6.1
revision. v0.4 introduces no contract revision to §6.1.

**Out of scope (deferred per `cli-design.md` §13 + carried over from
v0.3 deferrals):**

- Full `doc` CRUD (`create`/`update`/`delete`) — v0.4 lands read-only
  per §13 v0.4 entry; full CRUD deferred to v0.5.
- Multi-level subitem creation — v0.3-M28 closed Decision 11 on
  empirical grounds (Monday's `sub_items_board` carries no
  `subtasks` column at API `2026-01`). Re-probe before any v0.4
  reopening; if the data model still doesn't surface deeper nesting,
  slips to v0.5 contingent on Monday's API evolution.
- OAuth app registration (`monday auth login`) — v0.3.0 ships with
  the M28 placeholder guard. Reopens only on clear user demand for
  browser-based login over `MONDAY_API_TOKEN`; not a v0.4 deliverable
  by default.
- Boolean filter DSL — v0.4 keeps the v0.1/v0.2 `--where` operator
  allowlist + `--filter-json`. Pencilled at v0.4 stretch / v0.5
  firm; only ships when fixture-driven adoption metrics show
  concrete demand.
- Deterministic pagination resume (`--resume <token>`) — cli-design
  §14.6 open question. Not a v0.4 candidate without explicit
  contract-change PR.
- `monday item recreate-from-archive` — still no misleading
  `restore` command (cli-design §5.4 stands as a permanent
  non-goal-adjacent decision).

**Permanent non-goals (per cli-design §13 — reminded here so plan
readers don't propose them as v0.4 candidates):**

- Hosting webhooks (CLIs can't expose public HTTP — out of model);
  v0.3's `webhook create` is the *registration* shape against
  Monday, not a receive endpoint. `item watch` is **agent-driven
  polling**, NOT a hosted webhook receiver.
- App framework / installable monday apps (different surface area).
- Real-time GraphQL subscriptions (Monday's endpoint doesn't
  support them) — `item watch` polls; the cadence pre-flight pins
  the polling burn vs freshness trade-off explicitly.
- Telemetry, update-notifier, analytics — ever.
- Forms (Monday's public-submission feature) — same hosted-
  endpoint shape as webhooks.
- Saved queries / aliases — would silently change behavior across
  machines; shell aliases / shell functions are the established
  UNIX answer.
- `monday undo` (replay-based reversal of recent mutations) —
  Monday's state model is authoritative and concurrent; honest
  substitutes are `--dry-run` (v0.1) and `item history` (v0.3-M24).

## 2. Pre-flight: what's already in place

Working at HEAD (`main`, post-`6554e0c`). v0.3.0 published to npm
2026-05-13; **3249 tests passing + 1 skipped** across 130 files;
coverage 99.26 / 96.40 / 99.37 / 99.51 (statements / branches /
functions / lines) above the 95 / 95.45 / 95 / 95 floor (branches
margin 0.95pp); ERROR_CODES count 29; command count 95.

What v0.4 inherits from M0–M28:

- **Foundations (M0–M2.5)** — runner / signal handling / typed
  errors / redaction / envelope builders / commander wiring /
  transport seam / retry layer / `MondayClient` / `account`
  commands all live and stable. v0.4 inherits unchanged. **`item
  watch` leans on the signal handling + abort plumbing** — the
  watch session terminates on SIGINT/SIGTERM with a clean drain
  per cli-design §3.1 #8.
- **Read surface (M3–M4 + M23/M24/M25)** — `workspace`, `board`,
  `user`, `update`, `item` reads (incl. filter DSL, cursor
  pagination, NDJSON streaming, projection layer) plus the v0.3
  cross-board layer (M23 cross-board `item search`, M23 board
  favorites, M24 `item history` projection). **`item watch` reuses
  the M24 `item-history-projection.ts` projector** verbatim — the
  watch event stream is the same event taxonomy the history walker
  emits, just streamed-as-emitted rather than range-fetched.
- **Write surface (M5a–M5b + M8–M18 + M19/M20 + M25/M27)** — every
  mutation verb v0.3 ships, plus the §5.3 writable allowlist (13
  friendly types post-M19) and the `--set-raw` escape hatch.
  **`--concurrency` plugs into the same dispatchers** —
  `dispatchSequential` extends with a `dispatchConcurrent` sibling
  in `src/api/partial-success-mutation.ts`. **Asset upload is a
  new write seam** — multipart wire shape, no existing dispatcher
  applies; first v0.4 transport extension.
- **v0.3 layers** — Monday Dev convention (M26a setup + M26b
  workflow verbs), multi-profile auth (M21 + the M28 OAuth
  placeholder guard), diagnostics cluster (M22 status + usage),
  cross-board reads (M23/M24/M25), outbound writes (M27
  notifications + webhooks). v0.4 inherits all of this; `item
  watch` reuses M24's projector; `--concurrency` extends M25's
  bulk dispatcher; asset upload doesn't touch any of these but
  reuses M21's multi-profile credential resolution.
- **R-class generalised helpers** —
  `executeItemMutation` (R-NEW-29, M25 IMPL kickoff — shared
  per-item mutation dispatcher), `parseJsonArg` (R-NEW-42, M27
  IMPL close — argv-JSON-parse-boundary), `requireDevBoard`
  (R-NEW-35, M26b — slot-check helper), `walkDevBoardItems` /
  `hydrateDevBoardColumns` / etc. (R-NEW-36 cluster, M26b). v0.4
  consumes these where applicable: `--concurrency`'s
  `dispatchConcurrent` sits next to `dispatchSequential` and
  shares `executeItemMutation`; asset upload's argv side uses
  `parseJsonArg` if it carries JSON-shaped flags.
- **Cache invalidation discipline (cli-design §8)** —
  `invalidateBoard(boardId)` plus the §8 leg-count split
  (single-leg vs fan-out). **`item watch` is live-only** —
  polling against Monday is the source of truth; the local cache
  is irrelevant during a watch session. Asset upload invalidates
  the parent item's board (single-leg).
- **Partial-success envelope** (cli-design §6.1 with v0.3-M25's
  `data.results: [...]` per-target shape) — `--concurrency`
  emits the same shape but with parallel-dispatch ordering. **The
  results array preserves input order**, NOT completion order,
  so agents can index back to the input batch deterministically;
  this is the v0.3-M25 contract that `--concurrency` inherits.
- **Streaming helpers (R52 + R53)** —
  `startNdjsonStream` / `walkPages.onItem` / `paginate.onItem`
  with proper `'drain'` backpressure. **`item watch` is the first
  v0.4 streaming consumer** — one event per emitted NDJSON line;
  the trailer-meta builder emits the session summary on graceful
  exit. The trailer's `meta.events_emitted` + `meta.polls_made`
  + `meta.watch_duration_seconds` are the v0.4-M29 envelope
  additions.
- **Dry-run engine** (M5a, cli-design §9.2) — asset upload ships
  dry-run from day one (uploads a 0-byte stub; reports the
  planned `add_file_to_column` call's column-resolution + parent
  item info). `item watch` is non-mutating so dry-run is N/A.
- **Output-shapes contract** (`docs/output-shapes.md`) — ~110
  envelope snapshots after v0.3.0; v0.4's release-prep adds new
  snapshots covering `item watch` (event-record shape +
  session-summary trailer), `--concurrency` (per-target results
  with parallel-dispatch ordering preserved), asset upload (file
  metadata + parent item linkback), `doc` reads, `team` writers,
  and `completion`'s emitted-script envelope shape.
- **Test infrastructure** — fixture transport, fixture server
  (E2E), redaction-hardening suite, agent-flow E2E, NDJSON
  streaming integration tests, the v0.3 `__test_oauth_helper`
  seam pattern, R-NEW-20's `MondayClient` seam-injection stub
  factory (4 consumers at v0.3 close; v0.4 likely brings a 5th
  consumer that finally triggers the lift). **3249 tests
  passing + 1 skipped**.
- **Tooling** — ESM, NodeNext, strictest TS, ESLint
  `strictTypeChecked + stylisticTypeChecked`, vitest 4. CI runs
  typecheck + lint + test:coverage + build smoke on Node 22 and
  24. Two-AI review (Codex `gpt-5.5`, read-only) per design
  change and per implementation milestone.

What's **missing** that v0.4 needs (each filled by a named
milestone — the first is M29 below; subsequent v0.4 milestones get
their entries added as their pre-flights run):

- A **polling event loop** (M29) wrapping the M24 `item-history-
  projection.ts` projector with a cadence-aware fetcher + circuit
  breaker. New module `src/api/item-watch.ts` (or similar — pinned
  at M29 pre-flight). The polling loop's state lives in-memory only
  (no cache hooks); the last-seen-event-id watermark resets on each
  watch invocation, so two concurrent watchers against the same
  item don't share state.
- A **concurrency-bounded dispatcher** (later v0.4 milestone —
  TBD) sibling to `dispatchSequential` in
  `src/api/partial-success-mutation.ts`. Probe Monday's per-account
  concurrency cap (was surfaced as `concurrency_exceeded` in earlier
  API versions) at pre-flight to pin the default `--concurrency`
  value + the rejection envelope shape.
- A **multipart upload path** (later v0.4 milestone — TBD) in
  `src/api/transport.ts`. Monday's file mutations use
  `multipart/form-data`, NOT GraphQL JSON. New seam alongside
  existing `client.request`; first v0.4 transport extension. The
  R-NEW-41 watch-item (wire-vs-CLI asymmetry documentation
  pattern) likely fires here — multipart wire shape doesn't fit
  the column-value translator's JSON-payload conventions.
- A **doc resolver** (later v0.4 milestone — TBD) — Monday's
  `docs(...)` query at API `2026-01` (was unstable in earlier API
  versions; re-probe at pre-flight). Account-scoped, similar
  pattern to M19's `account_tags`.
- A **team writer module** (later v0.4 milestone — TBD) — Monday's
  `create_team` / `update_team` / `delete_team` mutations. Probe
  permission requirements at pre-flight (was admin-only at earlier
  API versions). New noun-namespace if not already in §4.3.
- A **completion-script emitter** (later v0.4 milestone — TBD) —
  commander has built-in completion machinery; the v0.4 verb
  shells out to it + emits the script per shell flavour.

## 3. Milestone sequencing

v0.4 opens with **M29 — `item watch`** as the first milestone. The
remaining v0.4 milestones (a `--concurrency` extension, asset
upload, `doc` read-only, `team` writers, shell completion, and a
release-prep milestone closing 0.4.0) are sequenced incrementally as
each pre-flight runs. The first session opens the doc with §1 + §2 +
§3 opener + the M29 entry only; subsequent v0.4 pre-flight sessions
add M30, M31, etc. with their own entries here.

Order at a glance:

```
       M29                                — item watch (polling + circuit breaker)
       M30 ─ ...                          — TBD as later milestones get pre-flighted
```

Each milestone is a coherent commit cluster (3–10 commits) ending on
a green tree with the listed exit criteria met. Earlier milestones
unblock later ones; do not start M(N) until M(N-1)'s exit criteria
are satisfied. Each milestone updates `docs/output-shapes.md` for any
new command shapes it lands and adds envelope snapshots for any new
emitted shape.

### M29 — `item watch <iid>` (polling-based event stream)

**The opening v0.4 milestone.** Lands the polling-based reactivity
primitive deferred from v0.1's stable contract per cli-design §13
v0.4 entry — agents wait for state changes on a single item via a
single CLI invocation that emits NDJSON event records as Monday's
activity log accrues new entries. Closes the cli-design §14.4 open
question on cadence + circuit-breaker shape (the question has been
open since v0.1; v0.4-M29 is where it converges).

**Status: pre-flight contract diff landed; IMPL not yet shipped.**
cli-design §14.4 closure landed at `31713fb` (§4.3 row + §13 v0.4
entry + §14.4 open-question converted to a closed-decision block).
The M29 pre-flight contract diff lands the stub modules
(`src/api/item-watch.ts` + `src/commands/item/watch.ts` under
`/* c8 ignore start */` block-wraps per testing.md preferred form),
the argv-parser zod schema with `--interval` / `--since` / `--once`
/ `--max-events` / `--max-duration` / `--include` slots, the
output-shapes.md M29 entry (event-record + trailer-meta shapes), and
this plan-doc §8 + §9 sub-sections. Decisions D1–D5 all closed at
this commit per the empirical-probe-pinned shape
(`scripts/probe/m29-polling-burn.ts`, 2026-05-13, API `2026-01`).
ERROR_CODES count stays at **29** — Monday's
`complexity_exceeded` / `concurrency_exceeded` / `rate_limited`
codes already cover the circuit-breaker exit per D1 closure. Command
count: **95 → 96** (one new verb).

**Why this goes first.** Three reasons: (1) **Smallest v0.4
surface.** `item watch` adds one verb + one new module + one
streaming envelope shape; the multipart upload path (assets) and
the per-account concurrency probe (`--concurrency`) both touch
more existing infrastructure with more pre-flight risk.
(2) **Pre-trodden ground.** The M24 `item-history-projection.ts`
projector already emits the event shape `item watch` streams;
M29's runtime is "loop + project + emit" against an existing
projector, not a from-scratch event taxonomy. v0.3-M22's NDJSON
streaming + signal-handling discipline applies directly. (3) **No
dependency on later v0.4 surfaces.** `item watch` doesn't need the
`--concurrency` bulk extension (single-item watch), doesn't need
the multipart upload path (read-only stream), doesn't need `doc`
or `team` writers, doesn't need shell completion. Any other v0.4
candidate going first would either depend on an empirical probe
(asset multipart shape, `concurrency_exceeded` envelope at API
`2026-01`, `docs` query stability, `team` writer permissions) that
hasn't run yet OR would strand the v0.1-deferred reactivity
primitive indefinitely. M29 mirrors v0.3-M22's diagnostics-cluster
sizing — well-bounded, clear pre-flight question, single dominant
design decision.

**Pre-flight design clearance — closed at pre-flight contract
diff.** All eight clearance items closed at the M29 pre-flight
contract diff per the §8 D1–D5 closures below + the cli-design
§14.4 closure at `31713fb`. Verbatim closures:

1. **Polling cadence default + override — CLOSED.** Fixed 30s
   default (`DEFAULT_WATCH_INTERVAL_MS`) with `--interval <ms>`
   override; range 1000ms–3600000ms. Empirical-probe-pinned:
   per-poll cost is 10 complexity points against Monday's
   1,000,000/min budget — 30s burns 0.002%, so `daily_limit`
   (writes-only at API `2026-01`; doesn't apply to reads) is
   irrelevant + politeness + Monday's documented >30s
   `activity_logs` propagation lag (M24 finding 2026-05-11) are
   the binding constraints. The "10% of daily budget" framing in
   the original clearance was misframed against `daily_limit`;
   the closure replaces it with the per-minute complexity-budget
   math. Options (b) exponential-backoff-by-default + (c)
   Monday-signal-driven adaptive cadence rejected — adaptive
   shapes belong in v0.5+ if a v0.4-M29 user surfaces a real-
   world friction point.
2. **Circuit-breaker shape — CLOSED.** Reactive on Monday's
   `complexity_exceeded` / `concurrency_exceeded` /
   `rate_limited`. Respect `reset_in_x_seconds` (60s default
   when absent; 300s ceiling — `MAX_BACKOFF_SECONDS`). Trip
   after 5 consecutive failed polls
   (`CIRCUIT_BREAKER_CONSECUTIVE_FAILS`). Per-failure
   `WatchSessionWarning` records accumulate in-session and emit
   in the trailer's `_meta.warnings` slot (NOT interleaved with
   event lines per §6.3 streaming contract).
3. **Output envelope shape — CLOSED.** Each emitted line is a
   single JSON event record matching v0.3-M24's `item-history-
   projection.ts` `historyEvent` shape verbatim — NO additional
   `meta.emitted_at` slot (the M24 projector's `created_at` is
   the chronological key + the trailer-meta's `retrieved_at`
   carries session-end wall-clock). The session-level envelope
   wraps the stream: success at session end (on `--max-events`
   reach, on `--max-duration` reach, on `--once` drain, on
   SIGINT graceful drain); failure on circuit-breaker trip or
   non-recoverable Monday error. Trailer-meta carries the seven
   M29-specific slots per D3 below.
4. **Concurrency cap interaction — CLOSED (D4).** Each
   invocation independent (no shared registry); aligns with
   cli-design §3.1 #5.
5. **Exit semantics on signal — CLOSED.** SIGINT/SIGTERM
   triggers a graceful drain: in-flight poll completes or
   aborts cleanly via the AbortController seam (`ctx.signal`
   threaded into the polling loop, mirroring v0.3-M22's
   `monday status` probe's signal-handling pattern); trailer-
   meta emits as a final NDJSON line; exit 130 per §7. No
   special shutdown delay.
6. **`--once` flag — CLOSED (D2).** Distinct from
   `--max-events 1`. `--once` drains the backlog from `--since`
   (or the most-recent {@link DEFAULT_ONCE_BACKLOG_LIMIT}=100
   events if `--since` absent) and exits without polling.
   `--max-events 1` waits for the NEXT event.
7. **`--since <event-id>` for restartability — CLOSED.** Accepts
   the `last_seen_event_id` value from a prior session's
   trailer-meta. Runtime looks up the event-id → `created_at`
   once at startup, sets the initial poll-from timestamp. NOT a
   full `--resume <token>` state-machine resume (cli-design
   §14.6 stays open).
8. **`--include <kind1>,<kind2>` filter — CLOSED (D5).** Accepts
   the M24 closed 9-kind enum verbatim (`update_column_value` /
   `create_column` / `create_group` / `update_board_name` /
   `update_board_nickname` / `board_workspace_id_changed` /
   `update_posted` / `update_replied` / `unknown` — NOT the
   `item.created` / `item.column_changed` / `update.created`
   prefixes the original clearance speculated about; the M24
   probe's actual taxonomy is verb-shaped, not dotted). Applied
   at projection time; default = all kinds emitted. v0.4-M29
   polls `activity_logs` only, so `update_posted` /
   `update_replied` are valid argv values but emit no events at
   v0.4 (forward-compat for v0.5+ comment-polling).

**Decisions deferred to the pre-flight contract diff session**
(named so a future session can close them by name; not in any
preferred priority — each closes when the M29 pre-flight session
runs):

- **D1 — Polling cadence default + circuit-breaker shape.** The
  load-bearing pair from the §14.4 open question (clearance 1 +
  2 above). Empirical-probe-first per the v0.3-M22/M23/M24/M26a
  convention; the probe runs as part of M29 pre-flight, not at
  some later session.
- **D2 — `--once` vs `--max-events 1` semantics.** Clearance 6
  above. Confirm the two shapes are distinct enough to ship both;
  if they collapse, ship only `--max-events 1` and document the
  one-shot recipe.
- **D3 — Session-summary trailer schema.** Clearance 3 above.
  Final field list + envelope-shape pin; output-shapes.md entry
  pre-written at the pre-flight contract diff.
- **D4 — Multi-watcher concurrency policy.** Clearance 4 above.
  Confirm the "each invocation independent" decision against
  cli-design §3.1 #5 ("agents do their own parallelism"); the
  alternative (shared in-process watcher registry via a cache
  file) is rejected at planning, but worth one more think at
  pre-flight before pinning.
- **D5 — `--include` enum closure.** Clearance 8 above. The M24
  event-kind taxonomy is the closed enum; confirm at pre-flight
  that `item watch`'s `--include` accepts the same kinds (NOT a
  subset, NOT a superset). If the M24 taxonomy needs extending
  for watch (e.g., a new `item.subscription` kind for the
  "someone is now watching this item" signal Monday surfaces),
  that's a cli-design §13 v0.4 amendment landing before M29
  pre-flight.

**In-scope command list (M29):**

- `monday item watch <iid>` — single new verb under the existing
  `item` namespace. Argv: `<iid>` required; `--interval <ms>`
  (default per D1), `--since <event-id>` (restartability),
  `--once` (one-shot check; per D2), `--max-events <N>`,
  `--max-duration <seconds>`, `--include <kind1>,<kind2>` (per
  D5). Output: NDJSON event records + session-summary trailer per
  D3.

**Deliverables (skeletal — pinned at the M29 pre-flight contract
diff session, not at planning kickoff):**

- `src/api/item-watch.ts` — new module wrapping the M24 projector
  with a polling loop + circuit breaker. Signatures TBD at
  pre-flight.
- `src/commands/item/watch.ts` — new command file under
  `src/commands/item/`. Wires the api module to the NDJSON
  streaming envelope; threads the SIGINT AbortController seam
  per clearance 5.
- `src/utils/errors.ts` — possibly a new ERROR_CODE for the
  circuit-breaker-trip exit (`watch_circuit_broken`?) — closes at
  M29 pre-flight per D1. If the circuit-breaker exit reuses an
  existing code (`complexity_exceeded` / `rate_limited` /
  `concurrency_exceeded`), no widening needed.
- Output-shapes.md — new entries: `item watch` event record
  shape + session-summary trailer shape.

**Exit criteria (M29):**

- `monday item watch <iid>` ships with full unit + integration
  test coverage (happy single-event path; multi-event path;
  `--once`; `--since <event-id>`; `--max-events` + `--max-
  duration` limit-driven exits; SIGINT graceful drain;
  circuit-breaker trip; every kind in the M24 taxonomy via
  `--include`).
- Coverage holds at or above 95.45% branches.
- ERROR_CODES count 29 → ≤30 (depends on D1's circuit-breaker
  decision per the deliverables list).
- Command count 95 → 96.
- Test count 3249 → ~3290 (single new verb with NDJSON streaming
  + multi-kind event-record assertions typically lands ~30–50
  tests; calibrated against v0.3-M22's diagnostics-cluster size).
- Codex pre-flight pass before first feat commit; Codex
  implementation pass before M29 close.
- `docs/output-shapes.md` ToC updated; envelope snapshots
  refreshed.
- This plan doc's §11 M29 post-mortem section gets written at
  M29 close (mirrors v0.3-plan §11 M19 post-mortem).
- cli-design.md §14.4 open question closed (the "watch-via-
  polling cadence and circuit breaker (v0.4)" entry); separate
  pre-M29 contract-change PR with its own Codex review per
  CLAUDE.md workflow rules.

**Refactor backlog candidates surfaced** (will resolve at M29
close per the §22 R-class register):

- A potential `dispatchPollingLoop` helper if a second polling
  surface lands in v0.4+ (asset upload async-job poll? webhook
  health-check poll?). LOW priority watch-item — 1 consumer at
  M29; fires at 2nd + 3rd.
- R-NEW-20 `MondayClient` seam-injection stub factory — `item
  watch` integration tests will need a poll-sequenced stub
  variant similar to v0.3-M25's `buildSequenceClientStub` but
  with cadence-aware return shape. If the test-side shape
  converges with M25's, the R-NEW-20 trigger fires at the 5th
  consumer (M25 was the 4th). If divergent, stays deferred.

## 4. Cross-cutting "definition of done" per command

v0.4 inherits v0.3-plan §4 verbatim — the DoD applies to every
verb v0.4 ships. See [v0.3-plan §4](./v0.3-plan.md#4-cross-cutting-definition-of-done-per-command)
for the canonical list (one file per subcommand; zod schema for
argv; one GraphQL operation per file; universal envelope; stable
error codes from §6.5; no interactive prompts; exit codes per §7;
dry-run for destructive verbs; cache invalidation per §8; stdin
support; NDJSON streaming for unbounded outputs; coverage floor;
envelope snapshots; output-shapes.md ToC; cli-design.md updates
via Codex-reviewed contract-change PRs; Codex pre-flight + impl
reviews per milestone).

**v0.4-specific additions:**

- **Streaming verbs (`item watch`, M29)** ship with the
  signal-handling discipline pinned in M29 clearance 5 — SIGINT
  triggers a graceful drain that emits a valid NDJSON trailer-
  meta record before exit `130`. The AbortController seam
  threading is the same pattern v0.3-M22's `monday status`
  probe used, NOT a new pattern.
- **Multipart-upload verbs (asset upload milestone)** ship with
  a 0-byte stub dry-run mode (clearance pending at the asset-
  upload pre-flight). The dry-run reports the planned
  `add_file_to_column` mutation without uploading bytes.
- **Concurrency-bounded verbs (`--concurrency` extension
  milestone)** preserve input-order in `data.results: [...]`,
  NOT completion order — the v0.3-M25 invariant carries over.

## 5. Testing strategy — the pyramid, made explicit

v0.4 inherits v0.3's pyramid (unit / integration / E2E). See
[v0.3-plan §5](./v0.3-plan.md#5-testing-strategy--the-pyramid-made-explicit)
for the canonical cassette categories and per-layer rules.

New cassette categories v0.4 introduces (filled in at each
milestone's pre-flight):

- **Item-watch polling cassettes (M29)** — N-poll cassettes with
  event-emission timing per cadence; circuit-breaker-trip
  cassettes (Monday returns `complexity_exceeded` /
  `concurrency_exceeded` for K consecutive polls); empty-poll
  cassettes for sustained-quiet items; SIGINT-during-poll
  cassettes for graceful-drain coverage.
- **Concurrency-bounded dispatch cassettes (later milestone)** —
  N-target fan-out with parallel-dispatch ordering preserved in
  results; per-account concurrency cap rejection envelope.
- **Multipart-upload cassettes (later milestone)** — multipart
  request shape captured at the transport seam; happy upload +
  too-large + wrong-content-type + auth failure variants.
- **Doc directory cassettes (later milestone)** — `docs(...)`
  query at API `2026-01`; happy + empty + paginated.
- **Team writer cassettes (later milestone)** — `create_team` /
  `update_team` / `delete_team` mutations; admin-required
  rejection variants.

## 6. Risk register

v0.4 inherits v0.3-plan §6 unchanged where applicable. See
[v0.3-plan §6](./v0.3-plan.md#6-risk-register) for the canonical
register. v0.4-specific risk additions (filled in at each
milestone's pre-flight):

- **R-v0.4-1 — Polling burn against Monday's daily budget (M29).**
  A single 30-day watch session against a moderately-active item
  emits ~2880 polls/day at 30s cadence. If Monday charges
  ~complexity per poll (the M22 probe pinned operations as the
  daily budget unit), 30s cadence stays under 10% of the 10k
  daily-operation default. **Mitigation:** circuit-breaker per
  M29 clearance 2 + adaptive backoff option (D1 at pre-flight).
  The cadence default is the load-bearing decision; pre-flight
  probe pins the numeric default + the agent-facing hint about
  long-running session budget.
- **R-v0.4-2 — Multipart wire-shape asymmetry (asset upload
  milestone).** Monday's file mutations use `multipart/form-data`
  NOT GraphQL JSON. The transport seam extension is a contract-
  shaping decision; the R-NEW-41 wire-vs-CLI asymmetry watch-item
  fires here (3rd consumer; lift candidate for a shared
  `## Wire-vs-CLI semantics documentation conventions` section in
  `docs/architecture.md` per the M27 finding).
- **R-v0.4-3 — Per-account concurrency cap drift
  (`--concurrency` milestone).** Monday's concurrency cap was
  surfaced as `concurrency_exceeded` in earlier API versions; at
  API `2026-01` the exact wire shape needs re-probing.
  **Mitigation:** empirical probe at pre-flight pins the cap +
  the rejection envelope shape.

## 7. v0.4 release exit checklist

To close 0.4.0 (mirrors v0.3-plan §7's structure; see
[v0.3-plan §7](./v0.3-plan.md#7-v03-release-exit-checklist) for
the canonical checklist):

- All v0.4 milestones (M29–M(N), where N is pinned as later
  milestones get sequenced) closed end-to-end with Codex pre-
  flight + impl reviews.
- Coverage holds at or above 95 / 95.45 / 95 / 95.
- Test count grows by the per-milestone deltas with no flake or
  regression.
- ERROR_CODES count widened only as needed (each new code via
  pre-M(N) contract-change PR).
- CHANGELOG `0.4.0` entry written; semver bump to `0.4.0`;
  annotated git tag; GitHub release; npm publish.

## 8. Decisions (open questions to close before each milestone)

The §8 register accumulates per-milestone decisions as each pre-
flight session opens. M29's decisions (D1–D5) are listed at the
M29 entry above; they close at the M29 pre-flight contract diff
session. Later milestones add their own D-series here.

### M29

All five M29 decisions closed at the M29 pre-flight contract diff
session per the empirical probe
(`scripts/probe/m29-polling-burn.ts`, 2026-05-13, API `2026-01`)
+ cli-design §14.4 closure at `31713fb`.

- **D1 — Polling cadence default + circuit-breaker shape.**
  **Closed:** default cadence 30s with `--interval <ms>` override
  (1000ms–3600000ms range). Per-poll cost is stable at 10
  complexity points against Monday's 1,000,000/min budget — 30s
  burns 0.002% of the per-minute budget; politeness + Monday's
  documented >30s `activity_logs` propagation lag (M24 probe)
  are the binding constraints, NOT budget. Circuit breaker is
  REACTIVE on Monday's `complexity_exceeded` /
  `concurrency_exceeded` / `rate_limited` wire errors;
  backoff respects `reset_in_x_seconds` (60s default cap; 300s
  ceiling); trip to failure envelope after 5 consecutive failed
  polls (`CIRCUIT_BREAKER_CONSECUTIVE_FAILS`); per-failure
  warnings emit to the NDJSON stream before trip. **No new
  ERROR_CODE** — the three existing rate-limit codes cover the
  circuit-breaker exit; trailer-meta `circuit_broken_at` slot
  + the underlying Monday error discriminate the trip mode.
- **D2 — `--once` vs `--max-events 1` semantics.** **Closed:**
  ship both as DISTINCT shapes. `--once` drains backlog from
  `--since` (or recent N events) and exits without polling;
  `--max-events 1` waits for the NEXT event. Collapsing them
  would surprise — `--once` is "what changed since I last
  looked?"; `--max-events 1` is "wake me on the next change".
- **D3 — Session-summary trailer schema.** **Closed:** seven
  M29-specific slots (`events_emitted` + `polls_made` +
  `failed_polls` + `watch_duration_seconds` +
  `last_seen_event_id` + `circuit_broken_at` + `exit_reason`)
  on top of the standard §6.3 streaming trailer shape.
  `exit_reason` discriminates trailer interpretation
  (`max_events` / `max_duration` / `once_complete` / `signal` /
  `circuit_broken`); see output-shapes.md `item watch` entry +
  cli-design §4.3 row.
- **D4 — Multi-watcher concurrency policy.** **Closed:** each
  invocation independent; no shared registry; last-seen-event-id
  watermark lives in-memory only. Aligns with cli-design §3.1
  #5 ("agents do their own parallelism, CLI is per-invocation").
  Two concurrent watchers double the poll volume against Monday;
  agents that need N watchers spawn N processes.
- **D5 — `--include` enum closure.** **Closed:** accepts the M24
  closed 9-kind enum verbatim (`update_column_value` /
  `create_column` / `create_group` / `update_board_name` /
  `update_board_nickname` / `board_workspace_id_changed` /
  `update_posted` / `update_replied` / `unknown`). No M24
  taxonomy extension at M29. v0.4-M29 polls `activity_logs`
  ONLY, so `update_posted` / `update_replied` are valid argv
  values but emit no events at v0.4 (those synthesized kinds
  come from the M24 `updates` source which v0.4-M29 doesn't
  poll). Forward-compat for v0.4-stretch / v0.5 comment-polling
  is the design intent — extending to `--include-comments`
  later doesn't require an argv-surface revision.

## 9. Preconditions

Mirrors v0.3-plan §9 — checklist of preconditions per milestone
+ status tick at IMPL close. See
[v0.3-plan §9](./v0.3-plan.md#9-preconditions) for the canonical
shape.

### M29 — `item watch <iid>`

Pre-flight preconditions (all ticked at the M29 pre-flight
contract diff commit):

- [x] **cli-design §14.4 closure landed.** Open-question converted
  to closed-decision block at `31713fb`. §4.3 row updated with the
  final M29 argv shape; §13 v0.4 entry updated to cite the
  closure rather than the open question.
- [x] **Empirical probe ran against a live workspace.**
  `scripts/probe/m29-polling-burn.ts` measured per-poll cost (10
  complexity points; stable across `limit:` values + 5 sequential
  samples), the merged (activity_logs + updates) shape cost (20
  points; 2× activity-logs-only), the per-minute complexity budget
  ceiling (1,000,000), and the `platform_api.daily_limit` shape
  (writes-only at API `2026-01`; doesn't apply to reads). Report
  at `scripts/probe/m29-polling-burn.report.txt` (gitignored;
  local-only per the standing `scripts/probe/` convention).
- [x] **Stub modules land under c8 ignore.**
  `src/api/item-watch.ts` exports the load-bearing types
  (`WatchItemInputs` + `WatchItemResult` + `WatchSessionWarning` +
  `WatchExitReason` + `WATCH_POLL_QUERY` + cadence/circuit-breaker
  constants) and the `watchItem` stub wrapped in
  `/* c8 ignore start */ ... /* c8 ignore stop */` block-wraps per
  testing.md preferred form. `src/commands/item/watch.ts` exports
  the `itemWatchCommand` CommandModule with a real zod argv
  schema (parse-boundary surface; tested) and a c8-ignored stub
  action body.
- [x] **Argv parser unit tests landed.**
  `tests/unit/commands/item-watch-argv.test.ts` covers the
  parse-boundary surface: positional `<iid>`, all six optional
  flags, `--interval` range enforcement, `--since` numeric-ID
  validator, `--include` comma-split + per-kind validation +
  empty-after-trim rejection, `--once` mutual-exclusion with
  `--max-events` / `--max-duration`, strict-object rejection of
  unknown flags. Action body is c8-ignored at pre-flight; runtime
  tests land at M29 IMPL.
- [x] **Output-shapes.md entry landed.** New section under `item
  (reads)` documenting the per-event NDJSON record shape (mirrors
  M24's `historyEvent` verbatim), the trailer-meta shape with
  M29-specific session counters (`events_emitted` + `polls_made`
  + `failed_polls` + `watch_duration_seconds` +
  `last_seen_event_id` + `circuit_broken_at` + `exit_reason`),
  the circuit-breaker progression semantics, the cadence range,
  the restartability mechanism (`--since <event-id>`), the
  event-kind filter forward-compat, the multi-watcher policy,
  the SIGINT graceful-drain mechanism, the `meta.source: "live"`
  invariant.
- [x] **ERROR_CODES count: 29 (unchanged).** Per D1 closure: no
  new code at M29; Monday's `complexity_exceeded` /
  `concurrency_exceeded` / `rate_limited` cover the
  circuit-breaker exit envelope. The trailer-meta
  `circuit_broken_at` + the underlying Monday error code in the
  failure envelope discriminate trip mode.
- [x] **Command count: 95 → 96.** `monday item watch <iid>`
  registered in `src/commands/index.ts`; envelope-snapshot
  refreshed (`tests/integration/__snapshots__/envelope-
  snapshots.test.ts.snap` updated from 95 → 96).
- [x] **R-NEW-37 W2 audit-point honored safely-by-construction.**
  `WATCH_POLL_QUERY`'s named operation `ItemWatchPoll` is pinned
  as a literal at the fetcher boundary; the M29 IMPL session
  threads it via `client.raw(..., { operationName:
  'ItemWatchPoll' })`. No caller-overridable `operationName`
  slot on `WatchItemInputs` (mirrors the M27 round-1 P2-1 fix
  pattern).
- [x] **Coverage floor holds.** Pre-flight stubs land under c8
  ignore start/stop block-wraps so they DON'T move coverage.
  Coverage at pre-flight: 99.27 / 96.40 / 99.38 / 99.51 against
  the 95 / 95.45 / 95 / 95 floor (branches margin 0.95pp).
- [x] **Codex pre-flight review ran (cli-design + pre-flight).**
  See §11 M29 post-mortem for round summary (fills in at M29
  IMPL close).

IMPL preconditions (tick at M29 IMPL close):

- [ ] Runtime body of `watchItem` shipped.
- [ ] Integration tests covering every M29 clearance path
  (happy single-event, multi-event, `--once`, `--since`,
  `--max-events`, `--max-duration`, SIGINT graceful drain,
  circuit-breaker trip, per-kind `--include`).
- [ ] Codex impl review ran (1–2 rounds expected per the
  v0.3-M19-onward cadence).
- [ ] Output-shapes.md M29 entry refreshed with concrete sample
  envelopes once IMPL is live.
- [ ] `docs/v0.4-plan.md` §11 M29 post-mortem written.
- [ ] CLAUDE.md status flipped to "M29 IMPL landed; next
  milestone TBD".

## 10. Post-mortem template

Each milestone's post-mortem (§11+ below) follows the v0.3-plan
§10 template verbatim:

- **Codex round summary** — number of pre-flight rounds +
  implementation rounds; per-round finding count by priority
  (P1 / P2 / P3); links to the .review-output.md artifacts
  (gitignored locally).
- **Contract drift findings** — any cli-design §X decision that
  drifted during implementation; the fix + the lesson learned.
- **Exit ratchets** — coverage floor changes (raised only; never
  lowered without documented rationale per the M18 → R42
  precedent), test-count delta, snapshot delta, surface count
  delta.
- **Refactor backlog candidates surfaced** — entries added to §22
  R-class register at milestone close.
- **Lessons for the next milestone** — what to do differently;
  what worked well; what to retain.

Per-milestone post-mortems land at §11 (M29), §12 (M30 — TBD), etc.
— monotonically incrementing per-milestone, matching v0.3-plan's
cadence (NOT v0.2-plan's odd-numbered cadence; per the v0.3-plan
§10 fix landed at post-M24 close-docs).

## 11. M29 post-mortem — `item watch` (polling-based event stream)

**To be written at M29 close.** Empty placeholder; fills in once
M29 pre-flight + IMPL ships and the close-docs sweep runs.

## 22. Refactor backlog — v0.4 R-class register

(Empty at planning kickoff; R-class candidates accumulate here as
v0.4 milestones surface them. Mirrors v0.3-plan §22's structure.)

v0.3 R-class entries (R-NEW-1 through R-NEW-43) stay tracked in
v0.3-plan §22 for historical context. Open candidates carried
forward from v0.3 that v0.4 may trigger:

- **R-NEW-20** (`MondayClient` seam-injection stub factory) — 4
  consumers at v0.3 close; v0.4 may bring a 5th (likely M29's
  poll-sequenced stub or the `--concurrency` milestone's
  parallel-dispatch stub). Lift evaluation at 5th consumer.
- **R-NEW-31** (discriminated-union per-status detail schema
  pattern) — 1 consumer at v0.3 close; v0.4 may bring a 2nd +
  3rd via the multipart-upload milestone's success/failure
  detail discrimination or `team` writer status discrimination.
- **R-NEW-41** (asymmetric wire-vs-CLI semantics documentation
  pattern) — 2 consumers at v0.3 close; **3rd consumer expected
  at the asset-upload milestone** (multipart wire-shape
  asymmetry). Fires the documentation-pattern lift trigger.
- **R-NEW-43** (deferred-feature surface pattern — placeholder
  guard + sentinel constant + production-mode-no-seam
  integration test) — 1 consumer at v0.3 close (M28 OAuth
  placeholder); v0.4 may bring a 2nd if any v0.4 verb is gated
  on external registration (e.g., a webhook-delivery verifier
  needing an HMAC secret).

v0.4-specific R-class candidates seeded at planning kickoff:

- **R-v0.4-W1 — `dispatchPollingLoop` helper.** Surfaces at M29
  IMPL if a 2nd polling surface lands in v0.4+ (asset upload
  async-job poll? webhook health-check poll?). LOW priority
  watch-item — 1 consumer at M29; fires at 2nd + 3rd consumer.
