# v0.8 implementation plan

> **PIVOT 2026-05-20 — v0.8 drops all API-`2026-07`-dependent
> milestones; re-scopes to ship the v0.6-M38 D2/D7 file `--set`
> carve-out folds on the `2026-01` pin.** Picked at v0.8 kickoff
> candidate-selection per R-NEW-75 framework after v0.7.0 shipped
> (2026-05-20T15:48:07Z, npm; annotated tag at `3e46f59`). SDK
> probe at the candidate-selection session: `@mondaydotcomorg/api`
> still at 14.0.0 baking `2026-01` natively; no 15.x release
> (which would bake `2026-04`) or 16.x release (which would bake
> `2026-07`, the v0.8 SKELETON's gating dependency). Mirrors the
> v0.7-pivot precedent verbatim: when SDK doesn't publish in time,
> the version's API-bump milestones DEFER and the version ships
> the next carry-forward bundle from cli-design §13.
>
> **DEFERRED v0.8 SKELETON milestones** (retain their numbers
> pending SDK 16.x publication; reopen at the v0.9-or-later
> kickoff that finds SDK 16.x ready):
>
> - **M44 — user-entity contract migration.** Three `User` field
>   type changes (`created_at` / `birthday` / `utc_hours_diff`) +
>   three deprecated `Query.users` args (`kind` / `newest_first` /
>   `non_active`) + 1000-row pagination cap. Wire surface only
>   exists at API `2026-07`.
> - **M45 — `monday user activity <uid>`.** Paginated cursor-based
>   activity-log read mirroring `item history`. Wire surface
>   `users.activity_logs` only exists at API `2026-07`.
> - **API pin bump `2026-01` → `2026-07`** — DEFERRED with M44/M45.
>
> The v0.7-deferred M39 / M40 / M41 cluster (API `2026-04` pin +
> `set_item_description_content` + `create_doc_blocks`) remains
> DEFERRED under the same SDK-version gating; reopens when SDK
> 15.x publishes with `CURRENT_VERSION = '2026-04'` natively AND
> a paid-tier sandbox is available for the M40 wire probe.
>
> **What ships in v0.8** (effective scope post-pivot):
>
> - **M49 — 🚨 P1 file-upload multipart wire-format fix. SHIPPED
>   `2ec67ad` (2026-05-21).** The CLI shipped the Apollo multipart
>   spec; live Monday rejected it — **every file-upload command was
>   broken in published v0.7.0** (M31/M38/M42/M43/M46). Switched
>   `multipart-transport.ts` to Monday's native format (probe-pinned,
>   live-verified). Unblocks M47; M46's shipped feature now works live.
> - **M46 — multi-file `--set` per call** (v0.6-M38 D2 deferral
>   `multi_file_set_unsupported`). Extends v0.7-M42's per-item
>   file-dispatch envelope to multiple file entries per item.
>   Zero new wire surface, zero new transport seam, zero new
>   ERROR_CODES expected (registry stays at 29). **(SHIPPED; now
>   works live since M49 fixed the shared transport.)**
> - **M47 — stdin file `--set` `<file-col>=-`** (v0.6-M38 D7
>   deferral). **PRE-FLIGHT DONE** (`f5353e4..a424293`, Codex CONVERGED
>   R2); **IMPL next.** Unblocked by M49's file-upload fix. Probe DONE:
>   `add_file_to_column` accepts any non-empty filename (default
>   `"blob"`; only empty → 500), so `--filename` is OPTIONAL. Scoped
>   single-file / single-target (stdin is one non-replayable stream).
> - **M48 — writable `board_relation` / `dependency` column
>   settings** (`monday board column-create --type board_relation
>   --settings '{"boardIds":[...]}'`). **ELEVATED to committed v0.8
>   scope on user demand (2026-05-20)** from the §1 optional-
>   candidate list — surfaced by a live agent run during the v0.7.1
>   hotfix (the M19 empty per-type create-time settings schema meant
>   the CLI couldn't point a Connect-Boards column at a target board at
>   create time). **PRE-FLIGHT DONE 2026-05-21** (`fbd60a6..0ca0d81`;
>   Codex CONVERGED): the probe REFUTED the M19 "no documented shape"
>   assumption (wire wraps the relation config under `defaults.
>   settings`, single-leg), and D1 closed as **option (c) — dependency
>   is same-board** (accepts `allowMultipleItems`; rejects cross-board
>   `boardIds`/`boardId`). **IMPL next**; the wire-wrap leg is a
>   `m48_preflight_stub` until then. See §3 M48.
> - **v0.8 refactor cluster** (**✅ SHIPPED 2026-05-21**; not a feature
>   milestone — parallels the release-prep cluster shape). Landed the
>   two crystallised code lifts from M46 IMPL: **R-v0.7-NEW-5** (the
>   fail-fast error-decoration split-and-rebuild scaffold —
>   `reThrowDecorated`, 4 consumers: clear / JSON-bulk / M42
>   file-bulk / M46 file-bulk-multi) + **R-v0.8-NEW-6** (the
>   orphan-warn `causeProjection` builder, 3 consumers), both now in
>   `src/api/error-decoration.ts`. Got Codex review (CONVERGED R4) +
>   the 4-path coverage ratchet (branches 95.47% → 95.88%).
>   R-v0.8-NEW-2 (`enforceSingleFileColumnSet`
>   rename) folds into M47 (shared `file-column-set.ts` touch);
>   R-v0.8-NEW-4 (M42/M43 schema parse-test backfill) folds into the
>   release-prep envelope-snapshot probe; R-v0.8-NEW-7/8 (process /
>   review-template) adopt at close-docs / next review.
> - **Release-prep cluster** mirroring v0.7's 6-commit baseline
>   (envelope-snapshot refresh probe + ToC audit + deferral slip
>   per R-NEW-82 + README quickstart refresh + version bump
>   `0.7.0` → `0.8.0` + audit-fix if `npm audit` flags +
>   CHANGELOG `[0.8.0]` entry + close-docs sweep).
>
> The v0.8 identity collapses from "API `2026-07` + user-entity
> migration" to "v0.6-M38 file `--set` polish carve-out folds +
> the `board_relation` writable-settings feature + a refactor
> cluster" — same shape as v0.7's "API `2026-04` + carry-forward
> enrichments" → "v0.6.x carve-out folds only" collapse.
>
> §1's milestone table and §3's milestone sequencing entries
> below describe the **original** v0.8 SKELETON (M44/M45/API pin
> bump/polish) — read with this PIVOT block as the override.
> M46/M47/M48 entries land in §3 at each milestone's pre-flight
> contract diff commit per R-NEW-75 step 6 (M46 SHIPPED; M48
> committed pending its `create_column` probe).

> **Status:** SKELETON. Filed forward at the post-v0.6.0 publish
> session (2026-05-18). PIVOTED at v0.8 kickoff candidate-selection
> session (2026-05-20, post-v0.7.0 publish) per R-NEW-75 framework
> — see PIVOT block above. Decisions per milestone (§8) +
> empirical-probe results per milestone (§9) populate at pre-flight
> kickoffs for the shipped milestones (M46 + M47).
>
> The binding contract is [`cli-design.md`](./cli-design.md). This
> document sequences the work needed to ship v0.8 of that contract;
> it doesn't redefine it. If a conflict arises, `cli-design.md` wins
> and this plan is updated.

## 1. Scope summary

v0.8 is "**Monday API 2026-07 + user-entity migration**" — the
second of two API-version-bump releases pinning the CLI to API
`2026-07`. Unlike v0.7's broadly-additive 2026-04 surface, **2026-07
ships a focused user-entity restructure with multiple breaking
changes** (field type changes, deprecated `Query.users` args, a
1000-row hard cap on user pagination). v0.8's load-bearing work is
the user-entity contract migration, NOT new feature surface area —
the new wire surface (`users.activity_logs` + `user_configs`) is
secondary scope.

**In scope (v0.8 milestones — full per-milestone scope filled at
each milestone's pre-flight kickoff):**

| M | Surface | Wire op | Status |
|---|---------|---------|--------|
| **M46** | v0.6.x **multi-file `--set` per call carve-out fold** — `monday item update <iid> --set f1=p1 --set f2=p2 ...` / `monday item update --where ... --set f1=p1 --set f2=p2 ...` / `monday item create --set f1=p1 --set f2=p2 ...` per-item multi-leg fan-out (sequential within an item × parallel across items for bulk per M42's `dispatchParallel`). Closes D2 deferral from v0.6-M38. | `add_file_to_column` (v0.4-M31 wire, N legs per item) | SHIPPED at `3e6bdc1..1289133` (R0 IMPL + 2 Codex IMPL fix-up rounds; CONVERGED at R3). Pre-flight CONVERGED at `89a86ea..d9b035b` (R0 + 3 Codex rounds). |
| **M49** | **🚨 P1 BUG — file-upload multipart wire-format fix.** The CLI sent the Apollo multipart spec (`operations`/`map`/`"0"`); live Monday rejected it ("query not found" / "Unsupported query"). Rewritten to Monday's native format (`query` + sibling `variables` + string-`map` + named part). Was breaking ALL file uploads in published v0.7.0 (M31/M38/M42/M43/M46). | `add_file_to_column` (native multipart at `/v2/file`) | **SHIPPED `2ec67ad`** (Codex R1 CONVERGED, 0 findings; live-verified) |
| **M47** | v0.6.x **stdin file `--set` `<file-col>=-` carve-out fold** — closes D7 deferral from v0.6-M38. Probe DONE: `--filename` is OPTIONAL (any non-empty name works; only empty filename → 500); default `"blob"`. Scoped to single-file, single-target (stdin is one non-replayable stream — bulk/multi-file/2nd-stdin reject `usage_error`). | `add_file_to_column` (multipart, M49-fixed) | ✅ **SHIPPED at v0.8-M47 IMPL** (`0d99ea7`). Pre-flight diff `f5353e4..a424293` (Codex CONVERGED R2) landed argv `<file-col>=-` + `--filename` + `routeFileColumnDispatch` stdin-scope enforcement as REAL; the IMPL (`0d99ea7`) swapped the `m47_preflight_stub` for the live stdin → `Blob` → `add_file_to_column` leg + the four dispatch sites + the D4 size-less dry-run echo. Empty pipe → `usage_error` `stdin_file_empty`; `item create` reads stdin before leg-1 (no orphan). R-v0.8-NEW-2 rename folded at pre-flight; R-v0.8-NEW-15 not-tipped, -16 graduated (`da62add`), -17 done (`d686177`). +11 tests (4237 pass + 3 skip); branches 95.9%; 29 ERROR_CODES / 117 commands unchanged. §3 M47 entry. **Codex IMPL CONVERGED R1** (0 P1/P2/P3; 11 watch-items clear). |
| **M48** | **writable `board_relation` / `dependency` column settings** — `monday board column-create --type board_relation --settings '{"boardIds":[...]}'` points a Connect-Boards column at a target board at create time (M19 shipped an empty per-type create-time settings schema). ELEVATED to committed v0.8 scope on user demand (2026-05-20). | `create_column` `defaults: {settings:{boardIds:[int],...}}` (per `get_column_type_schema`) | **✅ SHIPPED** (pre-flight `fbd60a6..0ca0d81` + IMPL `4803cbf`/`f79bb16`; Codex CONVERGED pre-flight R3 + IMPL R2; D1-D4 closed — D1 option (c), dependency same-board) |
| **v0.8 refactor cluster** | Committed refactor work (parallels release-prep). R-v0.7-NEW-5 `reThrowDecorated` fail-fast-scaffold lift (4 consumers) + R-v0.8-NEW-6 `projectCauseForEnvelope` builder (3 consumers), both crystallised at M46 IMPL. R-v0.8-NEW-2 rename folds into M47; R-v0.8-NEW-4 into release-prep. | (n/a — internal lifts) | **✅ SHIPPED 2026-05-21** (`refactor(api): lift reThrowDecorated + projectCauseForEnvelope`). Pre-flight at `ca615e7` (Codex R1 CONVERGED). IMPL: stubs → runtime bodies, 7 sites delegated, 6 c8-ignores removed, 4-path ratchet (`tests/unit/api/error-decoration.test.ts`); Codex IMPL CONVERGED R4 (0 P1/P2, R1-R3 W9 prose only). Branches 95.47% → 95.88%; `item/update.ts` 79.42% → 87.27%. §3 narrative + §22 R-class register |
| **M44** | User-entity contract migration — handle the three `User` field type changes (`User.created_at`, `User.birthday`, `User.utc_hours_diff`), the three deprecated `Query.users` args (`kind`, `newest_first`, `non_active`), the 1000-row hard cap on `Query.users` pagination. Update `monday user list` / `monday user get` / `monday user me` projections in lockstep. Surface a warning when `--all` would exceed the 1000-row cap. | (n/a — contract surface migration) | SKELETON (DEFERRED — see PIVOT block; pending SDK 16.x) |
| **M45** | `monday user activity <uid>` — paginated, cursor-based, filterable activity-log read for users (mirrors `monday item history` shape). Closes the v0.3-M24 item-activity-only gap that's been carried since the activity-log surface first landed. | `users.activity_logs` (new query, paginated with cursor + board/event filters) | SKELETON (DEFERRED — see PIVOT block; pending SDK 16.x) |
| **API pin bump** | `2026-04` → `2026-07` (single-place runtime pin flip + 475+ literal sites across docs/probes/cassettes). Lands at the close-docs sweep step OR as a dedicated commit before release-prep (decision at v0.8 kickoff). | (n/a) | SKELETON (DEFERRED — see PIVOT block; pending SDK 16.x) |
| **polish** | Refreshed envelope-snapshot suite for v0.8 surfaces; `output-shapes.md` ToC audit; README quickstart updates; CHANGELOG `0.8.0` entry; version bump 0.7.0 → 0.8.0; close-docs sweep. Mirrors v0.7 release-prep cluster shape verbatim. | (n/a) | SKELETON |

**Optional v0.8.x candidates (filed but NOT in v0.8.0 scope by
default — elevated to in-scope only on demonstrated user demand):**

- `monday user configs <uid>` — new `user_configs` query (2026-07
  additive). Reads per-user configuration slots Monday exposes.
  **Rationale for deferral:** unclear agent fit until we know what
  config slots Monday surfaces (probe at v0.8 kickoff).
- New `User` fields projection (10 new fields: `account_id`,
  `status`, `invitation_method`, `serial_number`, `is_deleted`,
  `photo_url`, `became_active_at`, `bb_visitor_id`,
  `is_email_confirmed`, `user_config`). M44 projects whichever
  subset is agent-useful (likely `account_id`, `status`,
  `photo_url`, `is_deleted`, `is_email_confirmed`); skip the rest
  until demand surfaces. Decision lands at M44 pre-flight.

**Out of scope (deferred to v0.8.x / v0.9 / permanently):**

### 2026-07 wire surfaces NOT exposed at v0.8.0

- **`UserStatus` / `InvitationMethod` / `UserKindFilter` enums** —
  Project to typed string literals at the boundary (M44 D2 closure);
  no dedicated CLI verb surfaces these.

### Carry-forward backlog (continues from v0.6 / v0.7)

- **Multi-file `--set` per call** (v0.6-M38 D2 deferral). **Status:**
  ✅ CARVED OUT at v0.8-M46 (SHIPPED) — per-item multi-leg fan-out for
  the 3 reachable callShapes. See §3 M46.
- **`<file-col>=-` stdin support** (v0.6-M38 D7 deferral). **Status:**
  ✅ SHIPPED at v0.8-M47 IMPL (`0d99ea7`; Codex CONVERGED R1) —
  `--filename` companion pinned (OPTIONAL, default `"blob"`), scoped
  single-file / single-target; live stdin → Blob → `add_file_to_column`
  on `item set` / single-item `item update` / `item create`. See §3 M47.
- **Multi-level subitem creation** — slipped v0.3 → v0.4 → v0.5 →
  v0.6 → v0.7 → v0.8. **Status:** STAYS deferred unless re-probe at
  v0.8 kickoff confirms Monday's `sub_items_board` carries the
  `subtasks` column at API `2026-07` (unchanged at agent's
  2026-05-18 changelog research; data-model gap unchanged through
  2026-04 + 2026-07 entries).
- **Cross-board `item move` value-overrides** — slipped same chain.
  **Status:** STAYS deferred unless Monday's `ColumnMappingInput`
  picks up a value slot at API `2026-07` (no entry in the
  changelog research; gap unchanged).
- **Resumable cross-board cursor** — design-work-gated; no API
  changes unlock it. **Status:** STAYS deferred to v0.8.x or later
  if the per-board cursor-lifetime under aggregation design issue
  finds a resolution.
- **Profile-scoped argument defaults** — filed at v0.6 kickoff.
  **Status:** STAYS at v0.8.x candidate-selection backlog;
  prerequisite §13 carve-out Decision unchanged.
- **OAuth app registration** — placeholder guard from v0.3-M28.
  **Status:** STAYS deferred.
- **Writable `board_relation` / `dependency` column settings** —
  **ELEVATED 2026-05-20 to committed v0.8 scope as M48 on user
  demand** (per this list's "elevated to in-scope only on
  demonstrated user demand" criterion). See §1 table + §3 M48 entry;
  the detail below is the candidate's ORIGINAL filing record (the
  pre-probe snapshot 2026-05-20) — **§3 M48 is authoritative for the
  resolved contract; several assumptions here were since REFUTED, see
  the resolution note.** Surfaced 2026-05-20 by a live agent run
  during the v0.7.1 hotfix. As filed: `monday board column-create
  --type board_relation --settings '{"boardIds":[...]}'` was rejected
  because the per-type settings schema for `board_relation` /
  `dependency` was empty (`emptySettingsSchema`, M19), so the CLI
  could not create a Connect-Boards column pointed at a target board —
  the M19 comment asserted Monday had no documented
  `create_column.defaults` shape for linked boards and that only
  post-create UI config worked. **NOT a v0.7.1 regression** — the
  empty schema was the deliberate M19 design. The original plan was
  probe-first: confirm whether `create_column.defaults` accepts the
  JSON at create time, then land a settings schema + Codex pass.
  **→ RESOLVED at the M48 probe + pre-flight (2026-05-21):** the M19
  "UI-only / no documented shape" assumption was REFUTED — the wire
  WRAPS the relation config under `defaults.settings` (read back
  UNWRAPPED on `settings_str`), single-leg, `boardIds` are int. The
  presumed `dependency` mirror was ALSO refuted: `dependency` diverges
  (same-board — Monday coerces an arbitrary target to the host board,
  not a `dependencyBoards`-style cross-board mirror), so M48 ships
  dependency same-board (accepts `allowMultipleItems`; rejects
  `boardIds`/`boardId`). Full closed contract + D1-D4 at §3 M48.
- **Proactive API-budget guard** — surfaced 2026-05-20 by the same
  run. `monday usage` reports the daily op budget (200/day on the
  probed account) but no command warns as a batch approaches the cap,
  so a large bulk / `--where` fan-out can silently exhaust it.
  **Status:** v0.8.x candidate — a `--require-budget <N>` pre-flight
  guard (fail fast with `usage_error` before the run) and/or a
  warning when remaining ops drop below a threshold on bulk commands.

**Permanent non-goals (per cli-design §13 — unchanged):**

See [`docs/v0.6-plan.md`](./v0.6-plan.md) §1 for the full list.
v0.8 inherits unchanged.

## 2. Pre-flight: what's already in place

> **Filled in at v0.8 kickoff candidate-selection session, AFTER
> v0.7.0 ships.**

Placeholder. At kickoff, this section mirrors v0.6-plan §2's
structure verbatim: working HEAD pin (post-v0.7.0 release), live
numbers post-v0.7 + 5 milestones (M39-M43), foundations carrying
forward into v0.8.

**Foundations expected to carry forward from v0.7 (preview):**

- **API version pin** at v0.7 close: `2026-04`. v0.8 bumps to
  `2026-07`.
- **SDK version pin** at v0.7 close: 15.x baking `2026-04` (or
  14.0.0 with string-literal override if v0.7-M39 D1 closed
  override-side). v0.8 lifts SDK to 16.x baking `2026-07` (if
  published) OR carries the override forward to `2026-07`.
- **Existing user-namespace verbs** — `monday user list`,
  `monday user get`, `monday user me` (v0.1 base + v0.5-M34 team
  writers). M44 migrates their projections + flag schemas.
- **Cursor + filter pattern from v0.3-M24** (`item history`) +
  v0.3-M23 (cross-board search) + v0.3-M27 (webhook + notification
  writes) — M45's `user activity` reuses the cursor-pagination
  helpers + filter-decoder patterns established here.

## 3. Milestone sequencing

> **Per-milestone detail filled in at each milestone's pre-flight
> kickoff.** Skeleton entries below sketch the wire surface +
> known design dimensions; D1-D{N} decisions land at pre-flight
> contract diff commits.

### M44 — User-entity contract migration

**Wire surface:** Existing `Query.users` + `User` shape — same wire
op, modified contract surface at the SDK + projection layer.

**Pre-IMPL cross-doc grep (R-NEW-72 graduated discipline):** Run
at kickoff to enumerate every consumer of the affected User
shape + Query.users args:

```bash
grep -rn '\bQuery\.users\b\|users\\b.*kind\\b\|newest_first\|non_active' src/ tests/ docs/
grep -rn '\bUser\.created_at\b\|\bUser\.birthday\b\|\bUser\.utc_hours_diff\b' src/ tests/ docs/
grep -rn 'user list --all\|user.*pagination' src/ docs/
```

**Empirical probe required at pre-flight:**

- Type-change probe — what TypeScript types does SDK 16.x ship
  for the three changed `User` fields (`created_at`, `birthday`,
  `utc_hours_diff`)? Was the change ISO-string ↔ Date object,
  string ↔ number, or something else? Drives the projection
  re-shape design.
- 1000-row cap probe — does Monday's `Query.users(limit: ...)`
  reject `limit > 1000` with `validation_failed`, or silently
  cap? Drives the `user list --all` warning shape.
- Deprecated-args behavior probe — do the three deprecated
  `Query.users` args (`kind`, `newest_first`, `non_active`)
  still function at API `2026-07`, or do they return
  `validation_failed`? Drives migration timing (graceful
  degradation with deprecation warning vs immediate removal).

**Open decisions at pre-flight:**

- **D1 — Type-change projection design.** Three sub-decisions
  (one per changed field) — each driven by the type-change probe
  results. Likely D1a / D1b / D1c at pre-flight.
- **D2 — Deprecated `Query.users` args handling.** Options:
  (a) Remove the CLI flags that consumed the deprecated args
      (clean break — agents update at v0.8 cutover).
  (b) Keep CLI flags but surface a deprecation warning + map to
      the non-deprecated equivalents at the wire boundary
      (graceful — agents migrate over the v0.8.x window).
  (c) Mark CLI flags as deprecated in `--help` but keep
      forwarding the args verbatim (Monday's deprecation warning
      surfaces); transition once Monday drops the args.
- **D3 — 1000-row cap warning shape.** `monday user list --all`
  needs to surface a warning when the requested page size +
  cumulative pagination would exceed 1000 rows. Warning shape:
  envelope-level `warnings: [{code: 'user_pagination_capped',
  ...}]` with `details.cap: 1000` + `details.hint` pointing at
  narrowing filters. Mirrors v0.3-M23's `cross_board_truncated`
  warning shape.
- **D4 — New `User` fields projection subset.** Which of the 10
  new 2026-07 `User` fields land in `monday user get` /
  `monday user list` projections? Likely: `account_id`,
  `status`, `photo_url`, `is_deleted`, `is_email_confirmed`
  (agent-useful). Skip: `serial_number`, `bb_visitor_id`,
  `became_active_at`, `invitation_method` (niche / internal) +
  `user_config` (deferred to v0.8.x candidate). Decision at
  M44 pre-flight.
- **D5 — ERROR_CODES delta.** Likely zero (registry stays at 29).
  Existing `validation_failed` covers cap violations + deprecated-
  arg rejections.

**IMPL Codex review estimate:** 3-5 fix-up rounds (contract surface
migrations are typically more invasive than feature adds — every
test cassette pinning the old User shape needs refreshing;
deprecation-warning shape design surfaces edge cases).

**R-class triggers anticipated:**

- Per-version contract-migration discipline — first major instance
  in the project's lifetime where an existing surface migrates
  (vs additive only). May surface a new R-NEW-X "wire-shape
  migration cluster pattern" at IMPL close.
- Pagination-cap warning helper potentially shared with v0.3-M23's
  `cross_board_truncated`. R-NEW-58 ahead-of-feat trigger fires
  only if the helper sees 3+ consumers (likely just 1 at M44 +
  the existing M23 consumer = 2; below threshold).

### M45 — `monday user activity <uid>`

**Wire surface:** `users.activity_logs` query — paginated
(cursor-based) with filters for board + event type. New query, no
prior CLI surface.

**Empirical probe required at pre-flight:**

- Wire-shape probe — what fields does `users.activity_logs[]`
  carry? Likely mirrors `boards.activity_logs` shape from v0.3-M24
  (`item history`); verify exact field set + per-event projection
  variants.
- Cursor lifetime probe — does Monday's standard 60-minute cursor
  lifetime apply, or does this surface ship a different lifetime?
  Drives the `stale_cursor` shape pin.
- Filter-decoder probe — what filter values does the wire accept
  (event type enums, board IDs, date ranges)?

**Open decisions at pre-flight:**

- **D1 — Reuse `--filter-json` DSL?** v0.1 / v0.3 ships the
  `--where <col>=<value>` + `--filter-json` shape; M45 likely
  reuses it for board / event-type / date filters. Decision at
  pre-flight after the filter-decoder probe.
- **D2 — Per-event projection (typed-discriminated-union pattern
  from v0.3-M24).** Reuse M24's per-event-type projection
  (`update_column_value` / `update_posted` / `update_replied`)
  + extend for any user-activity-specific event types (likely
  `user_login` / `user_status_changed` / `user_invitation_*`).
- **D3 — NDJSON output mode?** v0.4-M29 `item watch` ships NDJSON
  for long-poll streaming; `user activity` is paginated read, not
  long-poll. Decision: ship JSON envelope with cursor pagination
  (mirrors `item history`), NOT NDJSON. Confirmed at pre-flight.
- **D4 — ERROR_CODES delta.** Likely zero. `stale_cursor`
  (existing) covers the 60-minute cursor lifetime; `not_found`
  covers invalid `uid`; `usage_error` covers bad filter shapes.

**IMPL Codex review estimate:** 3-4 fix-up rounds (median IMPL
cadence; per-event projection design is the main risk).

**R-class triggers anticipated:**

- M24's per-event projection helper sees a 2nd consumer at M45;
  R-NEW-58 ahead-of-feat trigger fires only if a 3rd consumer is
  anticipated within v0.8 scope (none expected).

### M46 — Multi-file `--set` per call carve-out fold (D2 closure from v0.6-M38)

**Status:** SHIPPED at `3e6bdc1..1289133` (R0 IMPL + R1-R2 Codex
IMPL fix-up commits; CONVERGED at R3 with zero findings). The
v0.6-M38 → v0.8-M46 D2 fold lands multi-file `--set` per call for
the 3 reachable callShapes (single-item update / bulk update /
create), each firing N sequential `add_file_to_column` legs per
item. Pre-flight contract diff cluster CONVERGED at
`89a86ea..d9b035b` (R0 + 3 Codex pre-flight rounds: R1 behavioral
fix-up at `d14d2ed` + R2 prose-drift sweep at `84ff1dc` + R3 W9
ratification at `d9b035b`).

**IMPL cluster:** `3e6bdc1..1289133` (R0 IMPL + 2 Codex IMPL fix-up
rounds; CONVERGED at R3). Behavioural findings: **0 P1 across all 3
IMPL rounds** — R1 surfaced 2 P3 (W9 stub-prose drift), R2 surfaced
1 P2 (the command-registry `outputSchema` union under-advertised the
new M46 emit shapes — runtime was already correct, the gap was the
declared/advertised contract) + 1 P3 (W9 prose), R3 zero. The
R-v0.7-NEW-4 inherited contract-term checklist + a pre-emptive
cross-doc sweep held the W9 cycle to 2 prose rounds (R1+R2),
markedly shorter than M42's 8-round escalation — a PARTIAL
short-circuit (R2 still caught adjacent routing/test prose the
narrower R1 sweep missed). Logged as R-v0.8-NEW-5's 2nd supporting
instance (see §22).

IMPL summary:
- **D1-D6 closures landed as pinned at pre-flight.** Sequential
  within-item dispatch (D1); single-item
  `multi_file_update_partial_failure` / bulk per-item-record +
  fail-fast `applied_file_columns_per_item` / create-time
  `create_then_file_upload_partial_failure` extended with an
  always-present `applied_file_columns` slot (D2); N upfront
  pre-checks before any wire leg (D3); registry stays at 29 (D4);
  mixed-rule unchanged (D5); the 3 new `operation` literals
  `add_files_to_columns` / `item_update_bulk_file_set_multi` /
  `item_create_with_files` discriminate the multi-file envelopes
  (D6).
- **R-v0.8-NEW-1 RESOLVED as LIFT** — the inner sequential N-leg
  loop + partial-failure accumulator lifted to
  `dispatchFileLegsSequentially` (`src/api/file-column-set.ts`),
  3 consumers; only the per-callShape envelope decoration +
  `foldAndRemap` placement stayed inlined (see §22).
- **`multi_file_set_unsupported` + `m46_preflight_stub` literals
  stay RESERVED** post-IMPL (regression-guarded across success /
  dry-run / partial-failure for all 3 callShapes; the `'item_set'`
  argv-unreachable defensive throw is the only remaining
  `multi_file_set_unsupported` runtime site).

**Wire surface:** N `add_file_to_column` legs per item, sequenced
within each item, dispatched across the (single-item / bulk
matched-set / create-time freshly-created) target via the
respective dispatch helper. No new wire op; reuses v0.4-M31's
multipart wire + v0.6-M38's `executeFileColumnSet` runtime body
verbatim, called N times per item.

**Empirical probe NOT required at pre-flight** (wire surface is
known + cassette-pinned at v0.4-M31; novelty is the per-item
multi-leg fan-out + atomicity envelope shape, which is
implementation work, not wire-shape work).

**Open decisions at pre-flight — CLOSED at M46 pre-flight contract
diff (this commit):**

- **D1 — Per-item multi-leg ordering.** ✅ **Closed at v0.8-M46
  pre-flight contract diff: sequential within an item; cross-item
  parallelism reuses M42's `dispatchParallel` (parallel ACROSS
  items, sequential WITHIN each item's file legs).** Sequential
  within-item preserves the partial-failure surface accuracy —
  `details.applied_file_columns` echoes the file columns that
  succeeded before the failure in the order they fired. Parallel
  within-item would surface a non-deterministic `applied_file_
  columns` slot; the trade-off (slight latency win) is not worth
  the partial-failure-debugging cost. Cross-item parallelism stays
  unchanged from M42 D1 (`--concurrency` controls cross-item
  parallel workers; each worker's per-item multi-leg fan-out is
  sequential). No new probe required.

- **D2 — Multi-file partial-failure envelope shape.** ✅ **Closed
  at v0.8-M46 pre-flight contract diff: orphan-warn-style for
  single-item update; per-item shape extends M25's partial-success
  for bulk; extended `create_then_file_upload_partial_failure`
  discriminator for create-time.** Concrete shapes:
  - **Single-item `item update` multi-file partial failure:**
    `internal_error` with `details.reason:
    'multi_file_update_partial_failure'` carrying
    `details.item_id` + `details.applied_file_columns: [<col_ids>]`
    (file columns that landed before the failure, in dispatch
    order) + `details.failed_file_column: <col_id>` +
    `details.cause` (underlying error from M31's pinned surface —
    `column_archived` / `validation_failed` / `not_found` /
    `file_too_large`) + `details.hint` directing the agent to
    retry the unfailed file columns OR reissue all N legs.
  - **Bulk multi-file per-item failure under `--continue-on-error`:**
    Per-item `data.results[i].error: {code, message}` (mirrors
    M42's partial-success shape) extended with
    `data.results[i].applied_file_columns: [<col_ids>]` +
    `data.results[i].failed_file_column: <col_id>` when the
    per-item failure happened mid-multi-leg. The aggregate
    `data.summary` extends M42's `bulkFileSetDataSchema` with
    new `file_count` slot reflecting the N files per item that
    the call attempted.
  - **Bulk multi-file fail-fast (default):** Top-level error with
    `details.applied_to: [<item_ids>]` (items with ALL file legs
    succeeded) + `details.applied_file_columns_per_item: {[item_id]:
    [<col_ids>]}` (per-item partial-leg-success map for the item
    where the failure fired) + `details.failed_at_item: <item_id>` +
    `details.failed_file_column: <col_id>`. Mirrors v0.1 fail-fast
    bulk-update decoration extended with the multi-leg slot.
  - **Create-time multi-file:** Existing M43 discriminator
    `create_then_file_upload_partial_failure` EXTENDED — slot
    `details.applied_file_columns: [<col_ids>]` becomes always-
    present (length 0..N-1 reflecting file columns that landed
    after `create_item` leg-1 succeeded but before the failing
    file leg). Length 0 corresponds to M43's single-file failure
    case (leg-2 fails immediately); length k>0 corresponds to M46
    multi-file partial failure after k file legs succeeded. The
    literal `create_then_file_upload_partial_failure` stays stable
    across single + multi cases (extending an existing payload is
    schema-additive per §6.1).

- **D3 — Pre-check ordering.** ✅ **Closed at v0.8-M46 pre-flight
  contract diff: single upfront pass over ALL file paths before
  any wire call.** Mirrors M42 D3 + M43 D-equivalent. For single-
  item N-file: N paths × 1 pre-check upfront. For bulk N-file: N
  paths × 1 pre-check upfront (shared across the matched-item
  set). For create-time N-file: N paths × 1 pre-check upfront.
  Any pre-check failure aborts the whole call regardless of
  `--continue-on-error` per cli-design §5.8 "pre-checks MUST fire
  BEFORE any wire round-trip". The `--continue-on-error` flag
  partitions ONLY wire-dispatch failures (per-leg
  `add_file_to_column` rejections from Monday), never local file
  pre-check failures.

- **D4 — ERROR_CODES delta.** ✅ **Closed at v0.8-M46 pre-flight
  contract diff: ZERO net change. Registry stays at 29.** All
  M46-specific rejections route through existing codes:
  - Local file pre-check failures → `usage_error` with
    `details.reason: 'file_not_readable'` / `'file_empty'`
    (existing M31 discriminators reused).
  - Mixed mutex violations → `usage_error` with `details.reason:
    'mixed_file_and_value_sets'` (unchanged — universal rule on
    `'item_update_single'` / `'item_update_bulk'`; SUPPRESSED on
    `'item_create'` per M43 D6 asymmetry; multi-file allowance
    does NOT lift mixed-rule).
  - Single-item multi-file partial failure → `internal_error` with
    `details.reason: 'multi_file_update_partial_failure'` (NEW
    discriminator extending R-v0.6-NEW-2 graduated pattern; 6th
    instance — graduated helper absorbs cleanly).
  - Bulk multi-file per-item failure under fail-fast → existing
    v0.1-shaped top-level error with `details.applied_to` etc.
    extended (slot-additive, schema-additive — non-breaking).
  - Create-time multi-file partial failure → existing
    `create_then_file_upload_partial_failure` (extended with
    `details.applied_file_columns` always-present slot — schema-
    additive).

  The `'multi_file_set_unsupported'` discriminator literal at
  `src/api/file-column-set.ts:583` no longer surfaces from the
  runtime path for the 3 reachable callShapes (`'item_update_single'`
  / `'item_update_bulk'` / `'item_create'`) post-M46 carve-out
  fold; the literal stays RESERVED across the codebase (do not
  reuse for a different rejection reason; mirrors M42/M43 reserved-
  literal discipline). The throw at the `'item_set'` callShape is
  argv-unreachable (single-positional `<col>=<value>` shape; 2+
  file `--set` cannot be expressed in argv); kept as defensive
  type-system ceiling, not as a live runtime rejection.

- **D5 — Mixed file + value `--set` interaction.** ✅ **Closed at
  v0.8-M46 pre-flight contract diff: NO CHANGE to mixed-rule
  semantics.** Keeps `'mixed_file_and_value_sets'` rejecting on
  `'item_update_single'` / `'item_update_bulk'` for 1+ file `--set`
  + any value `--set` / `--set-raw` / `--name`. Stays SUPPRESSED
  on `'item_create'` (M43 D6 asymmetry preserved — `create_item`
  natively bundles non-file `column_values` into leg-1 atomically).
  M46 scope is "lift the multi-file leg only"; mixed-rule lift is
  a separate future milestone if demand surfaces (would require
  extending the partial-failure envelope to cover mixed JSON + N
  file partial-failures, a different shape).

- **D6 — Per-item envelope shape for multi-file dispatch.** ✅
  **Closed at v0.8-M46 pre-flight contract diff: new envelope
  schemas isolated to multi-file paths; single-file paths
  (M38/M42/M43) UNCHANGED.** Discriminate via new
  `operation` literals so agents reading `data.operation` can
  branch uniformly:
  - **Single-item update multi-file:** new
    `fileColumnSetMultiOutputSchema` with `operation:
    'add_files_to_columns'` + `item_id` + `assets: [{column_id,
    asset}, ...]` (length N) + `applied_file_columns: [<col_ids>]`
    (length N on success). The M38 single-file
    `fileColumnSetOutputSchema` stays UNCHANGED for N=1 (literal
    `operation: 'add_file_to_column'`).
  - **Bulk multi-file:** new `bulkFileSetMultiDataSchema` with
    `operation: 'item_update_bulk_file_set_multi'` + per-item
    `data.results[i].{item_id, assets: [{column_id, asset}, ...],
    applied_file_columns: [<col_ids>]}` (with `error` slot on
    per-item failure as in M42). Aggregate `data.summary` extends
    M42's `bulkFileSetDataSchema` shape with `file_count: number`
    slot (N files per item attempted). M42's single-file
    `bulkFileSetDataSchema` stays UNCHANGED for N=1.
  - **Create-time multi-file:** new
    `itemCreateWithFilesOutputSchema` with `operation:
    'item_create_with_files'` literal + `item:
    {id, name, board_id, group_id, parent_id?}` (inlines M43's
    `ItemCreateOutput` shape — leg-1's full item projection,
    NOT a scalar `item_id`) + `assets: [{column_id, filename,
    file_size_bytes, asset}, ...]` (length N) +
    `applied_file_columns: [<col_ids>]` (length N). M43's
    single-file envelope stays UNCHANGED for N=1.

  Two separate envelope shapes per callShape preserves backward
  compatibility with M38/M42/M43 callers; agents distinguish via
  the `operation` literal. Schema-additive overall (non-breaking
  per §6.1).

**Pre-flight stub structure (this commit).** Per R-NEW-76:

- argv parse (commander → `parseArgv` → zod) — shipped contract.
- `validateInputShape` (single vs bulk discriminator) — shipped
  contract.
- `setEntries` / `rawEntries` parse — shipped contract.
- `loadBoardMetadata` (board-metadata fetch) — shipped contract.
- `preCheckM38FileDispatch({callShape: ...})` — shipped contract;
  now returns `kind: 'file_multi'` / `'file_bulk_multi'` /
  `'file_create_multi'` on the clean multi-file dispatch paths
  (was throwing `'multi_file_set_unsupported'` at v0.6-M38 for
  every callShape). Single-file paths still return `kind: 'file'`
  / `'file_bulk'` / `'file_create'` unchanged.
- Per-item multi-leg dispatch helpers — at pre-flight these are
  **c8-ignored stubs** throwing `internal_error` with
  `details.reason: 'm46_preflight_stub'`. Three stub helpers:
  `runItemUpdateSingleFileMultiDispatch` (single-item update),
  `runItemUpdateBulkFileMultiDispatch` (bulk), and
  `runItemCreateFileMultiDispatch` (create-time). **Lifted at M46
  IMPL** in a separate session; the c8-ignore wrapper drops and
  the runtime body emits the per-callShape envelope per D6. The
  `'m46_preflight_stub'` discriminator literal stays RESERVED
  across the codebase post-IMPL (regression-guarded by integration
  tests mirroring M42 / M43 reserved-literal discipline). The
  pre-flight stub `internal_error` code (rather than `usage_error`)
  is deliberate: the argv was accepted at pre-flight (parse
  succeeded), the issue is that the multi-leg dispatch isn't
  shipped yet — that's a server-side / internal gap, not a
  user-input problem.

**IMPL Codex review estimate:** 3-4 fix-up rounds (median IMPL
cadence). The per-item multi-leg partial-failure envelope shape
is the main risk (3 callShape variants × dry-run + live × fail-
fast + continue-on-error matrix = 12 emit paths to land + test).
Estimate may revise after Codex pre-flight surfaces P-class
findings.

**R-class triggers anticipated:**

- **R-v0.8-NEW-1 (filed at M46 pre-flight).** Per-item multi-leg
  file fan-out helper. M46 inlines the fan-out across 3 dispatch
  helpers (single-item update / bulk per-item / create-time);
  R-NEW-58 3-consumer trigger fires at pre-flight. Resolution at
  M46 IMPL: lift to a shared helper IF the per-callShape envelope
  emit shapes (D6) are structurally similar enough; defer if the
  3 sites' partial-failure envelope decoration diverges (single-
  item orphan-warn vs bulk M25 partial-success vs create-time
  `create_then_file_upload_partial_failure` extension are
  different shapes — likely deferred to a follow-up cluster).
- **R-v0.6-NEW-1** (file-source.ts `precheckLocalFile`) — already
  graduated at 5 consumers (v0.7-M43 close-docs). M46 calls
  `precheckLocalFile` N times per item (one per file path) — same
  helper, no new lift work.
- **R-v0.6-NEW-2** (`details.reason` discriminator pattern) —
  graduated at 5 instances (v0.7-M43 close-docs). M46 adds 1 new
  discriminator (`multi_file_update_partial_failure`) + extends 1
  existing (`create_then_file_upload_partial_failure` with
  `applied_file_columns` slot). The graduated helper absorbs
  cleanly; no lift work triggered.
- **R-v0.7-NEW-4** (Pre-IMPL contract-term checklist) — graduated
  into `.claude/rules/workflow.md` at v0.7-M42 IMPL R7 + refined
  at R8. M46 inherits the checklist (see below).
- **R-NEW-76** (parseArgv-BEFORE-c8) — wire-dispatch-anchored at
  v0.7-M43 IMPL. M46 pre-flight stubs apply the discipline:
  `parseArgv` + sibling parse-boundary helpers run BEFORE the
  c8-ignore start block so argv-level failures surface as
  `usage_error` not `internal_error`.
- **R-v0.7-NEW-5** (fail-fast error-decoration block lift) —
  considered + likely NOT tipped at M46. The fail-fast bulk path
  + M43 leg-1 catch + M46 per-item multi-leg catch are
  structurally distinct (different remap targets + different
  decoration slot shapes); 3rd consumer pattern doesn't crystallise.
  Filed as 2nd "considered but not tipped" event under R-NEW-58
  consumer-threshold gating.

**Pre-IMPL contract-term checklist (R-v0.7-NEW-4 inherited from
v0.7-M42 IMPL + v0.7-M43 IMPL).**

State-current terms (should appear post-IMPL):

- `v0.8-M46`
- `multi-file --set` / `multi-file dispatch` / `multi-leg per item`
- `file_multi` / `file_bulk_multi` / `file_create_multi` (the
  `enforceSingleFileColumnSet` / `preCheckM38FileDispatch`
  enforcement-kind variants)
- `runItemUpdateSingleFileMultiDispatch` / `runItemUpdateBulkFileMultiDispatch`
  / `runItemCreateFileMultiDispatch` (the per-callShape helpers)
- `multi_file_update_partial_failure` (single-item partial-failure
  `details.reason` discriminator)
- `applied_file_columns` (orphan-warn / partial-success echo slot,
  always present on multi-file partial failure surfaces)
- `add_files_to_columns` / `item_update_bulk_file_set_multi` /
  `item_create_with_files` (the new `operation` literals from D6)
- `m46_preflight_stub` (transient — disappears at IMPL; the
  literal stays RESERVED post-IMPL like M42's `m42_preflight_stub`
  / M43's `m43_preflight_stub`)

Pre-IMPL framing terms (should NOT appear in current prose post-
IMPL except in allowed contexts):

- `'multi_file_set_unsupported'` runtime throw on the 3 reachable
  callShapes. Allowed contexts post-fold:
  - (a) historical: `docs/v0.6-plan.md` M38 D2 prose entries +
    `docs/v0.7-plan.md` historical references.
  - (b) reserved-literal docstrings: the M38
    `file-column-set.ts` module docstring + the M46 helper prose
    naming the literal as RESERVED.
  - (c) regression-guard tests asserting the literal does NOT
    appear in runtime output post-IMPL (mirror M42's
    `'file_set_on_bulk_unsupported'` + M43's
    `'file_set_on_create_unsupported'` reserved-literal tests).
  - (d) the `'item_set'` callShape's argv-unreachable defensive
    throw (kept as type-system ceiling; argv-impossible to express
    2+ file `--set` on the single-positional `monday item set`
    verb).
- `deferred_to: 'v0.7.x'` / `defer to v0.7.x` framing for the
  multi-file leg — disappears post-fold. Allowed (a) historical
  only.
- `Multi-file --set is not supported` / `single-file only` /
  `Exactly ONE file --set entry allowed per call` prose — becomes
  historical post-fold; current prose should describe N file legs
  per item with partial-failure envelope.
- `D2 multi leg` framing as forward-looking — becomes historical
  post-fold; post-IMPL prose should say "v0.6-M38 → v0.8-M46 D2
  fold" (past tense, mirrors M42's "v0.6-M38 → v0.7-M42 D5 fold"
  + M43's "v0.6-M38 → v0.7-M43 D6 fold").
- `pending at v0.8-M46` / `lifts at v0.8-M46`.

After every Codex fix-up round, grep ALL "should not appear"
terms across `src/`, `docs/`, `tests/`. Each hit in a non-allowed
context is a contract-term drift bug; fix before the next Codex
round.

**Pre-flight cluster:** `89a86ea..d9b035b` (R0 + R1/R2/R3 Codex
fix-ups; CONVERGED at R3 with 0 P1 / 3 P2 / 7 P3 across the
three review rounds — R1 found 2 P2 behavioural + 2 P3 W9; R2
found 1 P2 prose-shape mismatch + 3 P3 W9; R3 found 0 P2 + 2 P3
W9 inline-absorbed).

### M47 — stdin file `--set` `<file-col>=-` carve-out fold (v0.6-M38 → v0.8-M47 D7 fold)

**Status:** ✅ **SHIPPED at v0.8-M47 IMPL** (`0d99ea7`, subject:
`feat(item): live stdin file --set <file-col>=- (v0.8-M47 IMPL)`). The
IMPL swapped the one `readStdinFileSource` `m47_preflight_stub` for the
live runtime body + wired the four dispatch sites + the size-less
dry-run echo. **Codex IMPL review CONVERGED at R1** (0 P1 / 0 P2 / 0 P3;
all 11 watch-items cleared — the tightest possible outcome, beating the
M43 4-round / M46 partial-short-circuit trend; the inherited
contract-term checklist + a fully-pinned pre-flight earned the
single-round convergence). R-v0.8-NEW-17's verb-shaped upload hint
refresh rode the close at `d686177`; R-v0.8-NEW-16's workflow.md
graduation at `da62add`.

Pre-flight contract diff cluster was at `f5353e4..a424293` — R0
`f5353e4` (subject:
`feat(item): pre-flight stubs for stdin file --set <col>=- (v0.8-M47)`)
+ R1 fix-up `a424293` (W9 prose flip). Codex pre-flight review:
**CONVERGED at R2** (R1: 0 P1 / 0 P2 + 2 P3 W9 prose drift → fixed at
`a424293`; R2: 0 findings, all audit points cleared). Selected as the
next pre-flight at the
2026-05-21 post-refactor-cluster candidate-selection (R-NEW-75) over
M48 + release-prep — picked for file-path continuity (the just-shipped
refactor cluster + M46 touched the same `file-column-set.ts` surface),
zero open design decisions, and the R-v0.8-NEW-2 rename fold. Closes
the **last v0.6-M38 file `--set` carve-out (D7)**.

**Problem.** v0.6-M38 deferred stdin (`<file-col>=-`) on the friendly
file `--set` path per D7: no `--filename` companion shape was pinned,
and stdin reads byte-anonymously (the filename is the load-bearing
handle for Monday's wire `Asset.name`). M49's file-upload wire fix
unblocked it (the shared `add_file_to_column` multipart path now works
live), and the M49 probe pinned the `--filename` shape.

**Empirical probe — DONE (folded into the M49 pre-flight probe,
2026-05-21).** `add_file_to_column` accepts **any non-empty filename**
(`filename="probe.txt"` ✓, `filename="stdin"` ✓, default `"blob"` ✓);
only an **empty filename → `500`**. So `--filename` is OPTIONAL (a
nicety, not a hard requirement); the CLI defaults to a non-empty
placeholder and rejects an empty `--filename ""` at the parse boundary.
**No new transport seam, no new probe required** beyond M49 — M47
reuses the M49-fixed native multipart path verbatim; the only new
surface is an in-process stdin→Blob source in `file-source.ts`.

**Decision: `--filename` default = `"blob"`** (binding, 2026-05-21).
Source-agnostic (stdin carries no intrinsic name) and matches the
M49 probe's tested default. Agents that want a meaningful `Asset.name`
pass `--filename`.

**Decision: single-file, single-target scope** (binding, 2026-05-21,
`AskUserQuestion`). stdin is a single non-replayable stream, and the
existing bulk/multi-file dispatch path **re-reads the file per leg**
(`buildBlobFromPath` → `readFile` each time) — which stdin can't do.
Rather than introduce a divergent buffer-once-and-reuse mechanism,
M47 scopes stdin to single-file, single-target dispatch (cleanest
surface, fewest Codex rounds; bulk-stdin deferred as a possible future
increment). Trade-off considered + rejected: allowing bulk + multi-file
mix (more consistent with M42/M46 but larger surface + a memory cost
for large piped inputs + the buffer-once divergence).

**Wire surface:** zero net change. Reuses v0.4-M31's (M49-fixed)
`add_file_to_column` multipart wire via `executeFileColumnSet`. The
stdin source (`readStdinFileSource` in `src/utils/file-source.ts`)
buffers stdin once → Web `Blob` (mime sniffed from `--filename`), then
the same single-file dispatch path runs. Single-target only, so no
fan-out / replay concern.

**Closed decisions (D1-D5):**

- **D1 — stdin sentinel = `-`.** A file-column `--set` value of the
  bare `-` (or the `monday item set <iid> <file-col>=-` positional)
  sources the file body from stdin. The conventional CLI stdin token
  (mirrors `--body-file -` on `update create` / `update reply`).
  `precheckLocalFile` is bypassed for the stdin source.
- **D2 — single-file, single-target scope.** Exactly one
  `<file-col>=-` per call, as the SOLE file `--set` entry, on
  `item set` / single-item `item update` / `item create`. Rejections
  (all `usage_error`; literals reserved going forward):
  - `'multiple_stdin_file_sets'` — 2+ `<file-col>=-` entries.
  - `'stdin_file_set_not_sole_file'` — stdin `=-` + ≥1 other file
    `--set` (path or stdin); stdin can't combine with the M46
    multi-file path.
  - `'stdin_file_set_on_bulk_unsupported'` — `=-` on the
    `'item_update_bulk'` callShape.
  Enforced in `routeFileColumnDispatch` (the renamed
  `enforceSingleFileColumnSet`) BEFORE any wire/stdin activity. The
  mixed-rule + duplicate-column mutexes still apply to a stdin entry
  (stdin changes only the file SOURCE, not the surrounding mutex
  surface): `--set f=- --set status=Done` still rejects
  `'mixed_file_and_value_sets'`.
- **D3 — `--filename` companion.** OPTIONAL; default `"blob"`
  (probe-pinned; empty `500`s). Sets Monday's wire `Asset.name`. The
  command schemas carry `filename: z.string().min(1).optional()`, so
  an empty `--filename ""` rejects at the parse boundary (`usage_
  error`) and the wire never sends the empty name that `500`s.
  Consulted ONLY on a `<file-col>=-` dispatch; a no-stdin `--filename`
  is a harmless no-op rather than a resolution-coupled reject (whether
  a stdin source exists is only knowable after column resolution; the
  loud-error cost wasn't worth coupling `--filename` validation to the
  dispatch routing). Resolved via the pure `resolveStdinFilename`
  helper (unit-tested for both arms; avoids replicating the `??`
  default branch across the four dispatch sites).
- **D4 — dry-run envelope.** A stream can't be `fs.stat`'d without
  consuming it, so the stdin dry-run `planned_changes` echo carries
  `filename` + `file_path: '-'` but OMITS `file_size_bytes` (the field
  is additive per §6.4, so omission is non-breaking). The dry-run path
  must NOT consume stdin (it's a preview). **At pre-flight the stdin
  dispatch leg — dry-run echo AND live read — was a single stub** (per
  the M43 precedent that stubbed the whole `runItemCreateFileDispatch`
  at its pre-flight); the M47 IMPL (`0d99ea7`) implemented the size-less
  dry-run echo alongside the live read — all three dry-run branches emit
  the echo without calling `readStdinFileSource`, and the `item create`
  dry-run echoes a two-entry `planned_changes` with entry-2 omitting
  `item_id` + `file_size_bytes`.
- **D5 — ERROR_CODES delta: zero.** Registry stays at **29**. All new
  rejections route through existing `usage_error` with new
  `details.reason` discriminators. No new command (`-` is a new argv
  *value*, not a new verb) — command count stays **117**.

**Invariant preserved.** `precheckLocalFile` / `buildBlobFromPath`
stay path-only — `readStdinFileSource` is a NEW sibling in
`file-source.ts`, not a retrofit. The path helpers' fire-point
ordering (pre-check BEFORE `resolveClient`; Blob build AFTER
column-type validation) is load-bearing and doesn't transfer to a
single non-replayable stream (no pre-check size leg).

**Deferred sibling (follow-up, was NOT in M47 pre-flight scope; DONE at
the IMPL close — see below).** At pre-flight the verb-shaped
`monday item upload` / `monday update upload` argv hints (in
`src/commands/item/upload.ts` + `src/commands/update/upload.ts`) still
said stdin "is not supported in v0.4-M31 (a future contract extension
may add stdin support once a `--filename` companion flag is pinned)".
That precondition is now met for the friendly `--set` path, but M47
deliberately carves out only `--set` (the canonical cli-design §6.4
note was updated to say the verb-shaped uploads stay path-only). The
runtime hint prose stays as-is to keep the M47 pre-flight scoped to the
`--set` surface Codex reviewed; refresh the verb-shaped hints (point
`item upload` at `monday item set <iid> <file-col>=-`; `update upload`
has no `--set` equivalent — it attaches to an Update, not a file
column) at the next `upload.ts` touch or the M47 IMPL. **DONE at the
M47 IMPL close** (`d686177`, R-v0.8-NEW-17): both verbs' `<file>`
schema messages + help notes refreshed — `item upload` points at the
`--set` stdin path; `update upload` drops the now-met "once `--filename`
is pinned" promise.

**R-class outcomes at M47 pre-flight:**

- **R-v0.8-NEW-2 — RESOLVED (rename shipped).** `enforceSingleFileColumnSet`
  → `routeFileColumnDispatch` (the name no longer claims "single" —
  it routes a file `--set` to its dispatch kind + enforces the mutex
  gates). The coupled types renamed for coherence:
  `EnforceSingleFileColumnSetInputs` → `RouteFileColumnDispatchInputs`,
  `FileColumnSetEnforcementResult` → `FileColumnDispatchRoute`.
  Mechanical rename across `src/` + `tests/` (the ~37-ref estimate held)
  + cli-design.md §5.8 (current contract) + this plan's current refs;
  the historical v0.6/v0.7 plan-doc references keep their original
  framing per the historical-post-mortem convention. Bundled into the
  M47 pre-flight commit per its named trigger (shares the
  `file-column-set.ts` touch).
- **R-v0.6-NEW-1** (file pre-check + Blob helper) — graduated at the
  5-consumer threshold (v0.7-M43). M47's stdin source is a *sibling*
  (`readStdinFileSource`), not a 6th consumer of `precheckLocalFile`
  /`buildBlobFromPath` (those stay path-only per the invariant), so no
  graduation-count movement.
- **R-NEW-76** (parseArgv-BEFORE-c8) — honoured: the argv
  `<file-col>=-` + `--filename` parse + the `routeFileColumnDispatch`
  stdin-scope enforcement were REAL at pre-flight (invalid argv / bad
  stdin scope surface `usage_error`, not `internal_error`); only the
  live stdin-read + Blob leg lived behind the stub. At IMPL the stub is
  gone but the ordering stays load-bearing — the stdin sites are reached
  only AFTER the scope gate, so the contract is preserved (Codex IMPL W7
  clear).

**R-class outcomes at M47 IMPL (`0d99ea7`):**

- **R-v0.8-NEW-15 — CONSIDERED, NOT TIPPED (no lift).** The "shared
  stdin live-dispatch helper" watch-item resolved at IMPL kickoff once
  the runtime bodies existed to compare: the stdin sites compose the
  already-extracted `readStdinFileSource` (`file-source.ts`) + the
  existing `addFileToColumn` (`assets.ts`) fetcher directly, with
  per-site emit inline. The post-source dispatch+emit DIVERGES (set /
  update emit `FileColumnSetOutput`; create is a two-leg
  `create_item` → `add_file_to_column` under the §5.8 orphan-warn
  envelope, emitting `ItemCreateOutput`), exactly mirroring the
  PATH-based file dispatch — which is itself inline per-site, not
  shared — and [R-v0.8-NEW-1]'s M46 outcome (inner dispatch lifted,
  per-callShape envelope inline). A combined read+dispatch helper would
  also force the wrong ordering on `item create` (stdin must be read
  BEFORE leg-1 to avoid orphaning on empty stdin, but path-blob build
  stays AFTER leg-1 via `executeFileColumnSet`). The 1st "considered but
  not tipped" event for the M47 surface; logged as a non-event.
- **R-v0.8-NEW-16 — GRADUATED into `.claude/rules/workflow.md`**
  (`da62add`). Four supporting instances
  (`m42`/`m43`/`m46`/`m47_preflight_stub`) tipped the 3-5 graduation
  threshold; the literal-naming convention + the RESERVED-literal
  regression-guard rule now live in the "Pre-flight contract diff
  discipline" section.
- **R-v0.8-NEW-17 — DONE** (`d686177`; see the Deferred-sibling note
  above). The verb-shaped `item upload` / `update upload` stdin hints
  refreshed.

**Pre-flight anchoring (shipped at `f5353e4`, W9 fix-up `a424293`;
runtime body shipped at M47 IMPL `0d99ea7`):**

- `src/utils/file-source.ts` — new `STDIN_FILE_SENTINEL` (`-`),
  `DEFAULT_STDIN_FILENAME` (`"blob"`), `isStdinFileSetSource` +
  `resolveStdinFilename` (pure, real, unit-tested), `StdinFileSource`
  interface, and `readStdinFileSource(filename)` (c8-ignored stub
  throwing `internal_error` with `details.reason: 'm47_preflight_stub'`).
- `src/api/file-column-set.ts:routeFileColumnDispatch` — stdin scope
  gate (the 3 rejections above) added as mutex priority 0 (fires
  before the callShape / multi-file / mixed gates). A clean stdin
  source falls through to the existing single-file clean leg and
  returns `kind: 'file'` / `'file_create'` (rawValue carries `-`).
- `src/commands/item/{set,update,create}.ts` — `--filename` flag +
  `filename` schema field; the file-dispatch sites detect the sentinel
  (`isStdinFileSetSource`) and route to the stub (`readStdinFileSource`)
  instead of `precheckLocalFile`. The `m47_preflight_stub` literal is
  RESERVED post-IMPL (regression-guarded), mirroring M42's
  `m42_preflight_stub` / M43's `m43_preflight_stub`.
- Test surface: unit tests for the `routeFileColumnDispatch` stdin
  gate (clean stdin → `kind: 'file'` / `'file_create'`; the 3
  rejections; mixed-still-applies) + the `file-source.ts` helpers
  (`isStdinFileSetSource` / `resolveStdinFilename` / `readStdinFileSource`
  stub envelope) + integration stub-envelope assertions for `item set`
  (live + dry-run), single-item `item update`, and `item create`. **+15
  tests (4211 → 4226 pass + 3 skipped)**; branches **95.88% → 95.89%**
  (the stdin gate's defensive narrowing + the c8-ignored stub leg keep
  the margin); functions **99.11%**; 29 ERROR_CODES, 117 commands.

**Pre-IMPL contract-term checklist (R-v0.7-NEW-4 inherited).**

State-current terms (should appear post-IMPL):
- `v0.8-M47`
- `routeFileColumnDispatch` (the renamed enforcement/routing fn)
- `readStdinFileSource` / `StdinFileSource` / `isStdinFileSetSource` /
  `resolveStdinFilename` / `STDIN_FILE_SENTINEL` / `DEFAULT_STDIN_FILENAME`
- `<file-col>=-` / stdin file `--set` / `--filename`
- `multiple_stdin_file_sets` / `stdin_file_set_not_sole_file` /
  `stdin_file_set_on_bulk_unsupported` (reserved `usage_error`
  discriminators)
- `m47_preflight_stub` (transient — disappears at IMPL; the literal
  stays RESERVED post-IMPL like M42/M43)

Pre-IMPL framing terms (should NOT appear post-IMPL except in allowed
contexts — historical post-mortems / reserved-literal docstrings /
regression-guard assertions):
- `enforceSingleFileColumnSet` (renamed; allowed only in historical
  v0.6/v0.7 plan-doc post-mortems)
- `<file-col>=-` / stdin file `--set` framed as "deferred" / "D7
  candidate" / "NOT supported" (flip to "carved out at v0.8-M47")
- `--filename` framed as "not yet pinned" / "no companion shape"

**Codex pre-flight review — CONVERGED at R2 (estimate 1-2 held).**
- **R1** (`a424293`): **0 P1 / 0 P2**, 2 P3 — both W9 prose drift.
  P3-1: §1 carry-forward backlog still listed M46 multi-file `--set`
  (SHIPPED) + M47 stdin (`<file-col>=-`, pre-flight) as "STAYS
  deferred" → flipped to CARVED-OUT. P3-2: the `file-column-set.ts`
  module docstring still framed the D7 stdin closure as "OUT OF SCOPE"
  with no `--filename` pinned → rewritten to the M47 carve-out state.
  All behavioural audit points (W1 redactor / W2 op-name / W3
  parseArgv-before-c8 / W4 path-helper invariant / W5 rename
  completeness / W6 stdin-gate ordering / W7 coverage shape / W8
  no-stdin-consumption-on-dry-run / W10 `--filename`-no-op decision)
  clean in R1.
- **R2** (this close): **CONVERGED** — 0 P1 / 0 P2 / 0 P3. The fix-up
  reopened nothing; W1'/W5'/W9'/W-deferred all cleared. Behavioural
  surface confirmed aligned for pre-flight.

The R-v0.7-NEW-4 inherited contract-term checklist held the W9 cycle
to a single fix-up round (R1 → R2 converged), markedly shorter than
M42's 8-round escalation — consistent with the M43 (4-round) + M46
(partial short-circuit) trend. Logs as another supporting instance of
the checklist's short-circuit value.

### M48 — Writable `board_relation` / `dependency` column settings

**Status:** **SHIPPED 2026-05-21.** Pre-flight DONE (`fbd60a6` feat +
`6ab2caa` R1 fix-up + `0ca0d81` R2 fix-up; Codex pre-flight CONVERGED
at R3). **IMPL DONE** (`4803cbf` feat + `f79bb16` R1 P3 prose fix-up;
Codex IMPL CONVERGED at R2 — see the round logs below): the
`m48_preflight_stub` wire-wrap leg swapped for the live
`variables.defaults = {settings}` wrap + the mocked-wire assertion test
+ the RESERVED-literal regression guard. The `m48_preflight_stub`
literal has now DISAPPEARED from runtime output and stays RESERVED
across the codebase (regression-guarded). Gating `create_column` probe
COMPLETE 2026-05-20 — outcome (b) confirmed read-only AND empirically
via a live create+readback; the M19 "no documented shape" assumption is
REFUTED; residual CLOSED. D1-D4 closed at the pre-flight (see the
Decisions block below).

**Problem.** `monday board column-create --type board_relation
--settings '{"boardIds":[...]}'` cannot point a Connect-Boards column
at a target board at create time: M19 shipped an empty per-type
create-time settings schema (`emptySettingsSchema`) for
`board_relation` / `dependency` because no documented
`create_column.defaults` shape was known. Surfaced by a live agent
run during the v0.7.1 hotfix (filed at `f947098`).

**Gating empirical probe — RESULT (2026-05-20, token in
`.env.probe.local`; raw output local at
`scripts/probe/m48-board-relation-settings.report.txt`,
gitignored).** The probe could NOT run the live create+readback —
the probe token returns `USER_UNAUTHORIZED` (403) on `create_board`
(same permission wall as the v0.7 M40 findings; the token reads +
introspects but can't create boards). BUT the question is answered
**read-only** by two findings:

1. `create_column.defaults` is the create-time settings channel —
   its introspected description: *"Type-specific configuration object
   containing column defaults and settings. Accepts both JSON objects
   (recommended) and JSON strings... use `get_column_type_schema`
   query to see available properties and validation rules."*
2. `get_column_type_schema(type: board_relation)` returns the
   authoritative create-time `defaults` JSON-schema — **the
   documented shape M19 assumed didn't exist**:

   ```json
   { "settings": {
       "boardIds": [<integer>, ...],   // array of related board IDs
       "boardId": <integer>,           // default board ID for the relation
       "allowMultipleItems": <boolean>,
       "allowCreateReflectionColumn": <boolean>
   } }
   ```

   `dependency` parallels it: `settings.{boardIds:[int], boardId:int,
   dependencyNewInfra:bool, allowMultipleItems:bool}`.

**Outcome (b) — the friendly `--settings` adapts to a documented
create-time shape; single-leg dispatch.** Three load-bearing details
the schema pins:
- `boardIds` are **integers**, not strings (the translator coerces /
  validates the agent-supplied IDs to int).
- the relation config nests under a **`settings`** key inside
  `defaults` (`{settings: {boardIds: [...]}}`), NOT at the top level.
- `defaults` accepts a JSON object (recommended) or a JSON string.

So a single `create_column(... defaults: {settings:{boardIds:[...]}})`
wires the target at create time — **no two-leg degradation needed**
(outcome (c) ruled out).

**Residual CLOSED — live create+readback CONFIRMED 2026-05-20**
(write-scoped token under the `nwebster-company` profile; the first
three probe tokens were read-only — see the M40-style permission
note in the report). The empirical leg created two throwaway boards,
then `create_column(board_id: A, column_type: board_relation,
defaults: {settings: {boardIds: [<int B>], allowMultipleItems:
true}})` and read the result back:
- create_column **ACCEPTED** the `defaults` at create time.
- the new column's `settings_str` read back as
  `{"boardIds":[<B>],"allowMultipleItems":true}` — **target board
  WIRED at create time** (not merely stored-but-inert).
- both throwaway boards were deleted (clean, no orphans).

**IMPL shape pin (load-bearing for the translator):** the create-time
arg is `defaults: {settings: {...}}` (relation config WRAPPED under a
`settings` key); the column's read-side `settings_str` is the
**UNWRAPPED** inner object (`{boardIds, allowMultipleItems}`). So the
M48 translator: accept the agent's `--settings
'{"boardIds":[...],"allowMultipleItems":true}'` → wrap into
`defaults.settings` for the wire → done (single-leg). `boardIds` are
integers on the wire.

**M47-vs-M48 sequencing — RESOLVED 2026-05-21 (now historical).** The
probe showed M48 is cleanly buildable as a **single-leg** feature
(outcome (b)), and neither M47 nor M48 was blocked. At the 2026-05-21
candidate-selection the user picked the **v0.8 refactor cluster**
first; M48 + M47 went STANDBY. The refactor cluster then SHIPPED, and
the post-cluster candidate-selection picked M47 over M48 + release-prep
(file-path continuity on `file-column-set.ts`, zero open decisions, the
R-v0.8-NEW-2 rename fold). **M47 has since SHIPPED (`0d99ea7`), M48
came off standby, and its D1 `dependency`-divergence decision is now
CLOSED at this pre-flight** (option (c) — dependency same-board; see
the Decisions block). **M48 IMPL has since SHIPPED (`4803cbf`),
leaving release-prep as the LAST v0.8 unit.**

**Design-matrix probe findings (2026-05-20, write scope — these
refine the decisions below):**
- **`board_relation` honours both `boardIds` (array) AND `boardId`
  (scalar)** at create; empty `{boardIds:[]}` is accepted (degenerate
  unconfigured column); **self-reference** (`boardIds:[<host>]`) is
  accepted; `allowMultipleItems:false` is honoured.
- **Monday VALIDATES board existence at create** — `{boardIds:
  [999999999]}` → `"Resource not found"`. So the CLI does NOT need a
  pre-flight board-existence round-trip; the wire rejection surfaces
  (maps to existing `not_found`).
- **`dependency` DIVERGES — not a clean board_relation parallel.**
  Passing `{boardIds:[<other board>]}` to a `dependency` column got
  COERCED to the host board + auto-added
  `dependencyNewInfra:true` / `dependency_mode:null` (read-back:
  `{"boardIds":[<host>],"dependencyNewInfra":true,
  "allowMultipleItems":true,"dependency_mode":null}`). Dependency
  columns are effectively same-board; an arbitrary target boardId is
  NOT honoured at create the way board_relation honours it.

**Decisions (CLOSED at M48 pre-flight 2026-05-21):**
- **D1 — friendly `--settings` surface + `dependency` framing →
  RESOLVED via `AskUserQuestion` (option (c) + user follow-up "I want
  dependencies, but they are fine if they need to be same board
  only").** `board_relation`: full schema —
  `{boardIds?: int[], boardId?: int, allowMultipleItems?: bool,
  allowCreateReflectionColumn?: bool}`, all optional, `.strict()`,
  passed at the agent TOP level + WRAPPED into `defaults.settings` on
  the wire (`boardRelationSettingsSchema`). `boardIds` / `boardId`
  accept JSON integers OR numeric strings → coerced to wire int
  (`relationBoardIdSchema`, `.transform().pipe()` idiom). `dependency`:
  **same-board** — accepts only `{allowMultipleItems?: bool}`
  (`dependencySettingsSchema`); the cross-board target keys
  (`boardIds` / `boardId`) reject with a TARGETED `usage_error`
  (`rejectDependencyBoardTargets` → `details: {column_type,
  rejected_keys, hint}`) pointing at `--type board_relation`, BEFORE
  the schema's `.strict()` runs. Chosen over (a) defer-dependency and
  (b) silent host-coercion because the user wants dependency shipped
  AND the CLI's no-silent-surprises philosophy forbids coercing an
  agent's target away invisibly. `allowCreateReflectionColumn` /
  Monday-auto-managed keys (`dependencyNewInfra`, `dependency_mode`)
  are NOT exposed for `dependency`.
- **D2 — single-leg dispatch.** Probe outcome (b); live-confirmed. No
  two-leg. The existing `create_column` single-leg call carries the
  wrapped `defaults`.
- **D3 — ERROR_CODES delta: ZERO (registry stays 29).** Bad
  `--settings` JSON / non-integer board IDs / dependency board-target
  keys → `usage_error` at the parse boundary; non-existent target
  board → Monday's server-side `"Resource not found"` maps to existing
  `not_found` (no CLI pre-flight board-existence read — confirmed). No
  new `details.reason` discriminator for the live rejections (they
  mirror M16's `{column_type, expected_keys, actual_keys}` /
  `{column_type, rejected_keys, hint}` shapes). The transient
  `m48_preflight_stub` literal is the only new `details.reason`, and it
  disappears at IMPL (RESERVED thereafter).
- **D4 — scope guard.** M48 ships `board_relation` + `dependency`
  (same-board) create-time settings ONLY. `get_column_type_schema`
  exists for ALL types, but generalising create-time `--settings` to
  status / dropdown / timeline / etc. is explicitly OUT of M48 scope —
  `CREATE_TIME_SETTINGS_WRAP_TYPES` is the two-type set; file a
  follow-up candidate if demand surfaces.

**Pre-flight → IMPL shape.** Per the pre-flight stub discipline
(`workflow.md`): the pre-flight shipped the argv surface LIVE — the
per-type schemas, the int coercion, the `dependency` same-board
rejection, and the dry-run echo (unwrapped agent shape, mirroring
read-back) — and gated the LIVE (non-dry-run) `variables.defaults =
{settings}` wrap behind a `c8-ignore`d stub throwing `internal_error`
`details.reason: 'm48_preflight_stub'` (on
`CREATE_TIME_SETTINGS_WRAP_TYPES.has(type)` AND `settings !==
undefined`). Stubbed (rather than fused) because the wire shape
(`defaults: {settings: {…}}`) is the M19-bug class — a wrong wrap =
silently-inert settings, tests green — so isolating the wire-shape
assertion into a focused IMPL Codex review against a transport mock
that mirrors the probe-confirmed wire was worth the split. `parseArgv`
+ `parseSettingsFlag` run BEFORE the stub (R-NEW-76), so invalid argv
always surfaced as `usage_error` not `internal_error`. **M48 IMPL
(`4803cbf`)** replaced the stub with a single dispatch-site ternary —
`variables.defaults = CREATE_TIME_SETTINGS_WRAP_TYPES.has(parsed.type)
? {settings} : settings` — and converted the 2 stub PIN tests to live
wire-shape assertions (`match_variables: {defaults: {settings: {…}}}`,
whole-`defaults` JSON.stringify-compared per
`tests/fixtures/load.ts:variablesMatch`, so a bare pass-through
regression FAILS them) + a RESERVED-literal regression-guard
(`m48_preflight_stub` absent from live-wrap success / dry-run /
dependency-reject surfaces). The `ApiError` import was dropped (the
stub was its only use; `UsageError` stays for the schema-reject
paths). No `RUN_LIVE_TESTS` gate (the wire is plain JSON +
probe-confirmed, unlike M49's multipart).

**Tests:** the pre-flight added +16 (board.test.ts M48 describe block);
the IMPL is net +1 on top (−2 stub PIN tests → +2 live wire-shape
assertions + 1 RESERVED-literal regression guard), landing at **4254
pass + 3 skip**. Branch coverage holds at **95.91% (4484/4675)** vs the
95.45% floor (the removed c8-ignored stub block contributed no counted
branches; the new ternary's two arms are both covered — board_relation
/ dependency hit the `{settings}` arm, status/dropdown/numbers hit the
bare `settings` arm); functions **98.97% (1349/1363)**. ZERO new
commands / ERROR_CODES (117 / 29 unchanged).

**Codex PRE-FLIGHT review — CONVERGED (est. 1-2 held to 3 by the R2
P2-1 code fix).**
- **R1** (`fbd60a6`): 0 P1 / 0 P2 / 2 P3 — both W9 prose. P3-1: a
  cli-design §6.4 inline example soft-wrapped inside
  `allowMultipleItems` (rendered the invalid key `allowMultiple
  Items`). P3-2: the §1 candidate filing-record still asserted the
  pre-probe M19 assumptions. Both fixed at `6ab2caa`. W1-W8 + W10
  clear (incl. an independent confirmation of the int-coercion
  accept/reject matrix at W4).
- **R2** (`6ab2caa`): 0 P1 / **1 P2** / 1 P3. P2-1 (W4): the int
  coercion accepted unsafe board IDs — `Number("99…20 digits")`
  silently rounds and passes `.int().positive()`, so a corrupted ID
  could reach the wire. Fixed at `0ca0d81` with a `Number.isSafeInteger`
  refine + 2 pin tests (mirrors `deriveAllowedBoards` /
  `parseRelationItemIds`). P3-1 (W9): the §3 header + §1 table/bullet +
  sequencing paragraph still said pre-flight "opens next" / "STANDBY" /
  D1 "open" — flipped to PRE-FLIGHT DONE / IMPL next.
- **R3** (`0ca0d81`): **CONVERGED** — 0 P1 / 0 P2 / 1 P3. W4 now
  clear ("the safe-integer guard covers both numeric-string and
  JSON-number unsafe values" — the P2-1 fix confirmed correct +
  complete); all behavioural W-points clear. The lone P3 was the
  self-referential close-docs SHA backfill (the round log can't cite
  its own R2-fix commit `0ca0d81` / the R3 verdict until written) —
  the known terminal state per the round-agnostic-framing rule, closed
  by this commit's backfill, NOT a 4th substantive round. Codex
  verdict: "code/contract behavior looks ready for M48 IMPL hand-off."

The R2 P2-1 catch is the load-bearing one: a behavioural corruption
risk the R1 W4 audit-point read as clean (R1 confirmed accept/reject of
*malformed* inputs but not the *silently-rounded* unsafe-but-valid
path). It re-confirms the testing.md "mock must reject what the real
wire rejects" lesson at the parse boundary — the safe-integer guard is
the same one the existing relation-item parsers already carry.

**Codex IMPL review — CONVERGED at R2 (0 P1 / 0 P2 across both
rounds).**
- **R1** (`4803cbf`): 0 P1 / 0 P2 / **1 P3** (out-of-band). W1-W8 all
  clear — the W3 wrap-shape check (the M19-bug-class load-bearing one)
  confirmed the live path wraps ONLY `board_relation` / `dependency` as
  `defaults: {settings: …}` (status/dropdown/numbers stay bare) and the
  tests pin the whole `defaults` value, so a bare pass-through
  regression fails; W4 confirmed the wrap is wire-only (dry-run +
  read-side `settings_str` stay unwrapped); W5 confirmed `boardIds`
  stays numeric; W6 confirmed `m48_preflight_stub` is gone from `src/` +
  the guard covers all three surfaces; W7 confirmed the `ApiError`
  drop is safe. P3-1: stale stub prose — the "WITHOUT --settings" test
  name + comment still said "does NOT hit the stub" / "the settings-wrap
  leg is stubbed", contradicting the now-live wrap. Fixed at `f79bb16`
  (renamed to "fires create_column with no defaults (the wrap is
  settings-gated, not type-gated)"; no logic change).
- **R2** (`f79bb16`): **CONVERGED** — 0 P1 / 0 P2 / 0 P3; W1-W8 all
  clear. Codex verdict: "M48 IMPL R2 is clean; the R1 P3 prose fix
  landed without introducing new drift."

The IMPL converged a round faster than the pre-flight (R2 vs R3): the
pre-flight had already absorbed the behavioural risk (the R2 P2-1
safe-integer fix), so the IMPL surface was a mechanical stub→ternary
swap + assertion-test conversion with only one prose-drift P3 to clear.
The single P3 was the same stub-literal staleness class the
contract-term checklist (R-v0.7-NEW-4) targets — a "WITHOUT --settings"
test that still narrated the now-removed stub.

**R-class triggers (FILED at the pre-flight post-flight
refactor-audit; unchanged by IMPL):** the M19 per-type settings-schema
registry gained its first two non-empty entries (`board_relation` /
`dependency`), and `CREATE_TIME_SETTINGS_WRAP_TYPES` is a NEW 2-member
set that parallels the wrap behaviour — three parallel per-type
structures keyed by `WritableColumnType` (schema map + expected-keys
map + wrap Set). **R-v0.8-NEW-19** (§22) captures the LOW-priority lift
to collapse them into one record-per-type when a 3rd wrap-type arrives
(the "add the schema but forget the wrap → silently-inert settings" =
M19-bug class). The IMPL added no 3rd wrap-type, so the trigger stays
unmet; the post-IMPL refactor-audit re-confirmed the 2-member count.
The separate **R-v0.8-NEW-20** (§22) — suspected `dependency`
item-VALUE `--set` live-drift (`deriveAllowedBoards` reads
`dependencyBoards`; the probe read `boardIds`) — is a DIFFERENT surface
(item-value, not column-config) and was deliberately untouched by M48.

### M49 — File-upload multipart wire-format fix (P1 BUG — broken in published v0.7.0)

**Status:** **SHIPPED at `2ec67ad`** (R0 IMPL; Codex IMPL review
CONVERGED at round 1 with **zero P1/P2/P3 findings** — all 8
watch-points cleared, header lockdown + native contract verified).
Filed 2026-05-20 from a live probe; fixed 2026-05-21. This was a
production bug, not a feature: **every file-upload command was broken
against live Monday** and had been since the file surface first
shipped. Masked because all file-upload tests use a *mocked* multipart
transport that never validated against Monday's real multipart parser
(R-v0.8-NEW-9). **Live-verified:** a real `add_file_to_column` upload
through the production transport returns an Asset
(`tests/e2e/live-multipart-upload.test.ts`, RUN_LIVE_TESTS-gated).

**D1/D2/D3 closures (settled at the M49 pre-flight probe
`scripts/probe/m49-native-variables.ts`, live, write-scoped token,
2026-05-21):**
- **D1 — endpoint: `/v2/file`** (the documented file endpoint),
  derived from the configured GraphQL base via `deriveFileEndpoint`
  so a custom `MONDAY_API_URL` / proxy still routes uploads. (Native
  multipart was *also* accepted at bare `/v2` in the probe, but
  `/v2/file` is the documented surface.)
- **D2 — keep the sibling `variables` field.** Monday accepts a
  top-level `query` + a sibling `variables` JSON field, so the
  parameterised `$itemId/$columnId/$file` mutation + the
  `addFileToColumn` caller in `src/api/assets.ts` stay UNCHANGED —
  one transport-layer fix covers all five consumers
  (M31/M38/M42/M43/M46). (Inlined-query also worked, but the
  parameterised form is less churn.) `operationName` is NOT sent on
  the wire — the documents carry a single named operation, so Monday
  resolves it without the hint.
- **D3 — live smoke-test + native-mirroring fixture.** The unit
  wire-shape assertions now match the native form and guard against a
  revert (the Apollo `operations`/`'0'`/`operationName` parts must be
  absent); a RUN_LIVE_TESTS-gated e2e drives the real transport so the
  default mocked suite stays but can't lie about the wire again.
  ERROR_CODES: zero delta (registry stays at 29).

**The bug.** `src/api/multipart-transport.ts` builds the multipart
body in the **Apollo GraphQL-multipart spec** —
`operations` (JSON, query inside) + `map: {"0":["variables.file"]}`
+ file part named `"0"` — and POSTs it to `config.apiUrl` (`/v2`).
Live Monday **rejects this at every endpoint** (confirmed via the
CLI's *own* `createMultipartFetchTransport`):
- `/v2`: `400 "query not found in multipart form"`
- `/v2/file`: `400 "Unsupported query"`

**The fix.** Monday's **native** file-upload format works (probed
2026-05-20 with a write-scoped token):
- top-level **`query`** form field (the mutation string; a sibling
  **`variables`** JSON field carries `itemId` / `columnId`),
- **`map`** whose value is a **string**: `{"image":"variables.file"}`
  (NOT an array),
- the file appended as a part **named to match the map key**
  (`image`), with a **non-empty** filename.

Confirmed working: `POST /v2/file` (and `/v2`) returns `200` with the
asset — `filename="probe.txt"` ✓, `filename="stdin"` (no extension)
✓, default `"blob"` ✓; only **empty filename → `500`**. So the fix
also needs a **non-empty-filename guard** (the existing
`filename: z.string().min(1)` covers verb-shaped uploads; confirm it
holds on every path).

**Blast radius (all share the `multipart-transport.ts` seam +
`executeFileColumnSet` / `addFileToColumn`):**
- v0.4-M31 `item upload` / `update upload`
- v0.6-M38 single-item file `--set`
- v0.7-M42 bulk file `--set`
- v0.7-M43 create-time file `--set`
- **v0.8-M46 multi-file file `--set` (just shipped — live-broken
  until M49)**

**Dependency note:** M47 (stdin file `--set`) builds directly on this
path — **M49 is M47's hard prerequisite**. M48 (board_relation) is
independent (it's `create_column`, JSON not multipart).

**IMPL plan.** Rewrite the `multipart-transport.ts` FormData
construction to the native shape; decide endpoint (`/v2/file` is the
documented one, though `/v2` also accepted the native format in the
probe — pin at pre-flight). The fix is **live-verifiable** now (write
token in `.env.probe.local`) — add a `RUN_LIVE_TESTS`-gated live
upload smoke test so this can't silently regress again, since the
mocked transport demonstrably didn't catch it. Codex review (transport
seam = security-sensitive header lockdown lives here; confirm the
rewrite preserves the reserved-header strip). ERROR_CODES: zero delta
(empty-filename 500 maps to existing `internal_error` / a clear
`usage_error` guard before dispatch).

**Open decisions (close at M49 pre-flight):**
- D1 — endpoint: `/v2/file` vs `/v2` (both worked native; pick the
  documented `/v2/file` unless a reason surfaces).
- D2 — keep `variables` (for `itemId`/`columnId`) in a sibling field
  vs inline into the `query` string. Probe inlined; the parameterised
  form is cleaner if Monday accepts a `variables` field alongside
  `query` (confirm at pre-flight).
- D3 — live smoke-test gating (`RUN_LIVE_TESTS=1`) so CI optionally
  exercises the real wire; the default mocked suite stays, but the
  mock fixture should be updated to the native form so it can't lie
  about the wire shape again.

**R-class trigger:** filed as **R-v0.8-NEW-9 — mocked-transport
fixtures must mirror the real wire shape** (the mock validated the
WRONG multipart format for 5 milestones; a fixture that structurally
matched Monday's real parser would have caught this). See §22.

### v0.8 refactor cluster — committed code lifts (R-v0.7-NEW-5 + R-v0.8-NEW-6)

**Status: ✅ SHIPPED 2026-05-21** (IMPL SHIPPED block below).
Originally COMMITTED to v0.8 scope (prioritised 2026-05-20 on user
demand alongside M48); SELECTED 2026-05-21 at candidate-selection as
the next pre-flight target (binding `AskUserQuestion`; M47 + M48
weighed and dropped to standby — see CLAUDE.md "Next session"). Picked
ahead of M47/M48 to consolidate the fail-fast scaffold across the
M42/M46 file-set mutation paths *before* M47 would add a 5th touch,
and to widen the thin global branch margin (95.47% vs 95.45% floor)
via the 4-path ratchet. Not a feature milestone — a refactor cluster
parallel to release-prep, landing the two code lifts that
crystallised at M46 IMPL. Pre-flight is an internal-lift diff (no
wire surface, no probe): stub the lift signatures + the 4-path
delegation, then Codex pre-flight review. **PRE-FLIGHT STUB LANDED
2026-05-21; Codex R1 CONVERGED (0 P1).** `src/api/error-decoration.ts`
ships both helper signatures as c8-ignored, unwired stub throws (the 7
inline call sites stay byte-for-byte; IMPL swaps the stub for the
runtime body + delegates each site). Gates green at the stub commit
(4203 pass + 3 skip; branches 95.47% (4452/4663) unchanged vs the
pre-session baseline + functions 99.03% (1337/1350) — the stub module
is fully c8-ignored, FNF:0, so neither the branch nor function
denominator grows). See "Pre-flight (stub commit)" below.

**✅ IMPL SHIPPED 2026-05-21** at `refactor(api): lift reThrowDecorated
+ projectCauseForEnvelope to error-decoration.ts` (commit-subject ref
per R-v0.8-NEW-7 — adopted at this close-docs). Both stub throws swapped
for runtime bodies; the 7 inline sites delegated (4 `reThrowDecorated`:
JSON-bulk `update.ts` / clear bulk `clear.ts` / M42 file-bulk / M46
file-bulk-multi; 3 `projectCauseForEnvelope`: M43 create / M46 create /
M46 single-item update); the 6 call-site c8-ignore directives removed
(PD5 — clear's usage_error + ApiError-arm wraps; the two create
`details`-arm directives). New `tests/unit/api/error-decoration.test.ts`
(8 tests) drives the 4-path ratchet: both typed arms, each of the 5
ApiError-arm conditional spreads present-AND-absent (incl. the ApiError
arm's OWN `cause` spread per pre-flight P3-1), `retryable` passthrough,
both `projectCauseForEnvelope` details arms. **Codex IMPL CONVERGED at
R4** (R1-R3 each surfaced a single P3 W9 prose-drift finding — stale
"future lift candidate" / removed-block line-number refs / pre-IMPL
tense — fixed iteratively + swept for siblings per R-NEW-72; **0 P1 / 0
P2 across all 4 rounds**, byte-equivalence confirmed R2). **Measured
delta:** global branches **95.47% (4452/4663) → 95.88% (4444/4635)**
(+0.41pp; the conditional-spread arms left `item/update.ts` and arrived
COVERED in the helper); functions **99.03% (1337/1350) → 99.11%
(1340/1352)** (the 2 helpers now wired + counted + hit);
**`item/update.ts` branches 79.42% → 87.27%**; tests 4203 → 4211 (+8).
**Branch floor HELD at 95.45** — the lift's stated goal was to *widen*
the thin margin (0.02pp → 0.43pp headroom), so raising the floor would
defeat it; repo precedent (post-M21, R-v0.8-NEW-10/11) holds the floor
and lets gains provide headroom rather than chasing every gain upward.

- **R-v0.7-NEW-5 — `reThrowDecorated` fail-fast-scaffold lift**
  (headline). The `foldAndRemap` → typed `if (remapped.code ===
  'usage_error') { UsageError } else { ApiError }` split-and-rebuild
  with the 5 conditional-spread metadata arms is byte-identical
  across **4 consumers**: JSON-bulk (`update.ts:1437`),
  item-clear-bulk (`clear.ts:839`), M42 file-bulk (`update.ts:2159`),
  M46 file-bulk-multi (`update.ts:2854`). Lifted to
  `reThrowDecorated(remapped: MondayCliError, details:
  Record<string, unknown>): never` (`src/api/error-decoration.ts`).
  Each site builds its own `details` decoration then delegates the
  ~25-line scaffold. The lift consolidates the conditional-spread c8-ignore
  arms — run a lock-step coverage ratchet across all 4 paths
  (recovers ~0.5pp branches margin per v0.7-plan §22). Full detail:
  `docs/v0.7-plan.md` §22 R-v0.7-NEW-5.
- **R-v0.8-NEW-6 — `projectCauseForEnvelope` builder** (adjacent;
  bundle with the above — both live in mutation-path catch arms).
  The ~4-line `causeProjection = {code, message}; + details?` builder
  recurs across 3 orphan-warn sites (M43 create + M46 create + M46
  single-item). Lift the builder only (the surrounding decoration
  diverges). Full detail: §22 R-v0.8-NEW-6.

**Why a dedicated cluster, not folded into a feature milestone:** the
lift spans 4 mutation paths (clear / JSON-bulk / M42 / M46) — too
broad to ride a single feature's IMPL, and the M42 / JSON-bulk / clear
sites are unrelated to M47/M48's feature work. Needs its own Codex
review pass + coverage ratchet. R-v0.8-NEW-2 (`enforceSingleFileColumnSet`
rename) + R-v0.8-NEW-4 (schema parse-test backfill) stay folded into
M47 / release-prep respectively (they ride a touch those clusters
already make). R-v0.8-NEW-7 (SHA-citation fragility) + R-v0.8-NEW-8
(emit-shape `outputSchema` review audit-point) are process / template
adoptions, not code lifts — adopt at the next close-docs / review.

#### Pre-flight (stub commit) — `src/api/error-decoration.ts`

The pre-flight diff is lighter than a feature's: no probe, no new
wire surface, no new ERROR_CODES (registry stays 29), no new command
(stays 117), no cli-design section. It pins the two helper signatures
as c8-ignored stub throws and documents the inline-vs-lift boundary +
the IMPL coverage-ratchet plan. The stub is **unwired** — the 7 call
sites stay byte-for-byte; nothing imports the module yet (so the whole
module is c8-ignored to keep it out of the coverage denominator, the
same reason `src/cli/index.ts` is excluded). R-NEW-76
(parseArgv-BEFORE-c8) does NOT apply — the helpers have no argv surface
to ship ahead of the c8 block. R-v0.7-NEW-3's stub-body conventions DO:
both throws surface `internal_error` with `details.reason:
'refactor_cluster_preflight_stub'` + `details.milestone:
'v0.8-refactor-cluster'`.

**Pinned signatures:**

- `reThrowDecorated(remapped: MondayCliError, details: Record<string, unknown>): never`
- `projectCauseForEnvelope(err: MondayCliError): Record<string, unknown>`

**Inline-vs-lift boundary (the over-fit watch).** Verified against the
7 sites 2026-05-21:

| Surface | `reThrowDecorated` | `projectCauseForEnvelope` |
|---------|--------------------|---------------------------|
| **LIFTED** | the typed split (`usage_error`→`UsageError` / else→`ApiError`) + the 5 conditional-spread metadata arms (`cause` on both arms; `httpStatus` / `mondayCode` / `requestId` / `retryAfterSeconds` + unconditional `retryable` on the ApiError arm) | ONLY the ~4-line projection builder: seed `{code, message}` + conditional `details` passthrough |
| **STAYS INLINE** | the `foldAndRemap` call, `existing = remapped.details ?? {}`, and the per-site decoration object (shared slots `applied_count` / `applied_to` / `failed_at_item` / `matched_count`; plus M46-multi's `applied_file_columns_per_item` / `failed_file_column` / `file_count` / `file_column_ids`) | the always-`internal_error` outer wrap, the `details.reason` literal, the per-site decoration slots (`created_item_id` vs `item_id`; `applied_file_columns`), the hint text |

The caller assembles its own `details` decoration (it diverges per
site — pushing assembly into the helper would re-introduce per-site
branching inside the helper) and passes the finished record; the helper
owns only the typed split. The two helpers stay SEPARATE (not merged):
`projectCauseForEnvelope`'s wrap pins `internal_error` + projects the
cause, vs `reThrowDecorated` preserving the remapped typed code + applied-
count decoration — the structural distinctness argued at
`docs/v0.7-plan.md` §22 R-v0.7-NEW-5 "M43 IMPL outcome".

**The 7 call sites (line numbers verified 2026-05-21):**

- `reThrowDecorated` (4): item-clear-bulk `clear.ts:839` (c8-ignored
  arms); JSON-bulk `update.ts:1437`; M42 file-bulk `update.ts:2159`;
  M46 file-bulk-multi `update.ts:2854`.
- `projectCauseForEnvelope` (3): M43 create `create.ts:1976`
  (c8-ignored `details` arm `:1984`); M46 create `create.ts:2373`
  (c8-ignored `details` arm `:2377`); M46 single-item `update.ts:2602`.

**Pre-flight decisions (closed at this commit):**

- **PD1 — two separate helpers, not one.** RESOLVED: separate. The
  cause-projection builder is structurally distinct from the fail-fast
  split (see R-v0.7-NEW-5 M43-IMPL distinctness argument). Bundled in
  one session only because both live in mutation-path catch arms.
- **PD2 — module location.** RESOLVED: new `src/api/error-decoration.ts`.
  `errors.ts` is the typed-class + code-table registry; `file-column-set.ts`
  is file-dispatch-specific. The decoration helpers are generic to any
  mutation-path catch arm → own module.
- **PD3 — `reThrowDecorated` parameter shape.** RESOLVED: caller passes
  the fully-assembled `details` record (NOT raw progress slots the helper
  re-assembles). The decoration slots diverge per site (M46-multi adds 4);
  an internal builder would force an optional-slot union that re-creates
  per-site branching inside the helper, defeating the lift.
- **PD4 — `projectCauseForEnvelope` scope.** RESOLVED: builder only. The
  surrounding orphan-warn wrap diverges per site (outer code, reason
  literal, decoration slots, hint) and stays inline.
- **PD5 — call-site c8-ignore directives.** RESOLVED (plan): the IMPL
  REMOVES `clear.ts`'s conditional-spread arm directives (`:832`–`:873`)
  and `create.ts`'s `:1984` / `:2377` `details`-arm directives — the
  consolidated helper's focused unit test covers those arms genuinely,
  so they no longer need ignoring.

**Coverage-ratchet plan (executed at IMPL — the acceptance criterion).**
New `tests/unit/api/error-decoration.test.ts`:

- `reThrowDecorated`: drive `usage_error` → `UsageError` (`cause`
  present + absent — the UsageError arm's only conditional spread); a
  non-`usage_error` code (e.g. `column_archived`, `validation_failed`)
  → `ApiError` with the code preserved. The ApiError arm has **5**
  conditional spreads, not 4 — drive each of `cause` / `httpStatus` /
  `mondayCode` / `requestId` / `retryAfterSeconds` present-AND-absent on
  the ApiError arm specifically (the ApiError arm carries its OWN `cause`
  spread distinct from the UsageError arm's, so testing `cause` only on
  the UsageError side leaves the relocated ApiError-arm `cause` branch
  uncovered — Codex pre-flight R1 P3-1). Plus `retryable` passthrough;
  `details` attached verbatim on both arms. Inputs are
  real `MondayCliError` instances (a typed error class, not a
  wire-derived shape) so the testing.md "no hand-shaped wire inputs"
  rule is satisfied.
- `projectCauseForEnvelope`: details-present → projection carries
  `.details`; details-absent → projection omits it; `code` + `message`
  always copied.

**Mechanism / expected delta.** The previously-uncovered
conditional-spread arms move OUT of `item/update.ts` (denominator drops
there) and arrive in the helper as COVERED (the focused test) → global
branch numerator rises. `item/update.ts` branches lift from ~80%; the
`create.ts` projection-arm c8-ignores convert to genuinely-covered
branches. Net ~+0.5pp global branches margin per the v0.7-plan §22
estimate, widening the thin 95.45% floor. Whether to RAISE the floor is
a watch-item at IMPL close — measure first, don't pre-commit a number
(the post-M21 ratchet precedent: raise only after the margin is real).

**Codex pre-flight review — R1 CONVERGED (0 P1).** Contract verdict:
"signatures and lift boundaries look contract-compatible." W1 / W3 /
W4 / W5 / W6 all returned nothing-flagged (the signatures fit all 7
sites byte-for-byte; the two helpers correctly stay separate; the
`details`-collapse at clear / JSON-bulk is behaviour-preserving; the
create-site `details`-arm c8-ignores are genuinely droppable at IMPL).
Two non-P1 findings, both applied + gate-verified this commit (no R2
needed — mechanical, not contract-design):

- **P2-1 (W8) — fixed.** The c8-ignore block started INSIDE each
  function body, so the function DECLARATIONS were still counted
  (`FNF:2 / FNH:0` for the module). Moved the `/* c8 ignore start */`
  ABOVE each `export function` so the whole declaration is wrapped;
  the lcov now records `FNF:0` for `error-decoration.ts` and the
  global functions denominator drops 1352 → 1350.
- **P3-1 (W7) — fixed.** The ratchet plan listed `cause` present/absent
  only for the `UsageError` arm; the `ApiError` arm carries its OWN
  conditional `cause` spread (5 spreads, not 4). The IMPL test-matrix
  bullet above now drives `cause` present-AND-absent on the ApiError
  arm specifically, so the relocated branch can't stay uncovered.

### v0.8 release-prep (placeholder — opens after M45 IMPL closes)

Mirrors v0.7 release-prep cluster shape verbatim: ToC audit +
`deferred_to: "v0.8"` slip (R-NEW-82 graduated discipline; 6th-
consecutive consumer expected) + README v0.8 quickstart + scope
refresh + version bump 0.7.0 → 0.8.0 + audit-fix if `npm audit`
flags + CHANGELOG `[0.8.0]` entry + close-docs sweep + API pin
flip 2026-04 → 2026-07 in the close-docs commit (OR a dedicated
commit before — decision at release-prep kickoff). R-NEW-84
graduated discipline applies.

## 4. Cross-cutting "definition of done" per command

Identical to v0.5-plan §4 / v0.6-plan §4 / v0.7-plan §4.

See [`docs/v0.6-plan.md`](./v0.6-plan.md) §4 for full detail.

## 5. Testing strategy

Identical to v0.6-plan §5 / v0.7-plan §5.

See [`docs/v0.6-plan.md`](./v0.6-plan.md) §5 for full detail.

## 6. Risk register

> **Per-milestone risks land at each milestone's pre-flight
> kickoff.** Skeleton entries:

- **R1 — SDK 16.x publication timing.** Same shape as v0.7 R1 —
  if SDK 16.x baking `2026-07` is not published at v0.8 kickoff,
  the pin-bump decision tracks v0.7's. Mitigation:
  string-literal override on whatever SDK ships
  `2026-04` as a fallback.
- **R2 — User-entity migration blast radius.** The user surface
  is one of the project's oldest (v0.1) + most-used (referenced
  across `account whoami` / `user list` / `user get` / `user me`
  / team writers M34 / dev convention layer M26 + others).
  Cross-doc grep at kickoff enumerates the exact blast radius;
  IMPL fix-up cadence likely 4-5 rounds vs the 3-round median.
  Mitigation: aggressive pre-flight cross-doc grep + cassette
  refresh sweep + Codex-pre-flight-template W{N} audit-point
  for migration test parity.
- **R3 — Pagination-cap user-facing semantics.** `user list --all`
  is a v0.1 behavior; introducing a 1000-row cap warning is a
  contract-extending change (the envelope gains a new
  warning code). Mitigation: M44 D3's
  `user_pagination_capped` warning shape pins the contract at
  the cli-design §6.5 warning-shape registry; agents key off the
  warning code, never message text.
- **R4 — Deprecated-args graceful degradation timing.** Monday's
  deprecated-args timeline (do they reject at `2026-07` immediately
  or after a deprecation window) drives M44 D2's graceful-vs-clean-
  break decision. Mitigation: pre-flight probe + a "ship clean
  break, document migration in CHANGELOG upgrade-notes" default
  unless probe surfaces graceful path is materially better.

## 7. v0.8 release exit checklist

> **Filled at v0.8 release-prep close.** Mirrors v0.6-plan §7 +
> v0.7-plan §7 verbatim — every cluster-item ticked + post-publish
> flip commit.

## 8. Decisions (open questions to close before each milestone)

> **Per-milestone D-list lands at each milestone's pre-flight
> contract diff commit.** Skeleton above (§3) sketches the
> expected decision dimensions per milestone.

## 9. Preconditions

> **Per-milestone preconditions lands at each milestone's
> pre-flight kickoff.** Skeleton above (§3) sketches the
> expected empirical-probe targets per milestone.

## 10. Post-mortem template

Mirrors v0.5-plan §10 / v0.6-plan §10 / v0.7-plan §10 verbatim.

See [`docs/v0.6-plan.md`](./v0.6-plan.md) §10 for full detail.

## 22. R-class register

> **Status:** placeholder — populated at the first M44/M45 close-docs
> commit with R-class candidates surfaced during pre-flight + IMPL
> Codex reviews. Mirrors v0.6-plan §22 structure.

### Watch-items carried forward from v0.7

> Detail filled at v0.8 kickoff after v0.7 closes; placeholder set
> matches v0.7-plan §22's carry-forward list (R-NEW-58 / R-NEW-72 /
> R-NEW-75 / R-NEW-76 / R-NEW-82 / R-NEW-84 / R-v0.6-NEW-1 / R-v0.6-
> NEW-2) plus any v0.7-graduated rules + v0.7-filed candidates.

### New R-class candidates surfaced at v0.8 (placeholder)

> Populated at each milestone's close-docs commit per the
> v0.6-plan §22 structure.

- **R-v0.8-NEW-1 — Per-item multi-leg file fan-out helper.** Filed
  at v0.8-M46 pre-flight contract diff cluster
  `89a86ea..d9b035b`. M46 inlines the
  per-item multi-leg fan-out across 3 dispatch helpers (single-item
  update `runItemUpdateSingleFileMultiDispatch` / bulk per-item
  `runItemUpdateBulkFileMultiDispatch` / create-time
  `runItemCreateFileMultiDispatch`). R-NEW-58 3-consumer trigger
  fires at pre-flight — the lift candidate is a shared
  `dispatchFileLegsSequentially({entries, itemId, ...}) →
  {appliedColumns, failedColumn?, cause?}` helper consumed by all 3
  callShape helpers, encapsulating the sequential N-leg
  `executeFileColumnSet` loop + partial-failure accumulator pattern.

  **RESOLVED as LIFT at M46 IMPL (`3e6bdc1`).** The inner-loop
  hypothesis held: the sequential N-leg `executeFileColumnSet` loop
  + partial-failure accumulator (appliedColumns / assets /
  lastComplexity / optional `failure: {failedColumn, cause}`) is
  byte-identical across all 3 callShapes. It lifted to
  `dispatchFileLegsSequentially({client, multipart, itemId,
  entries, signal, retries}) → DispatchFileLegsResult` in
  `src/api/file-column-set.ts` (alongside `executeFileColumnSet`),
  consumed by all 3 helpers.

  **What stayed inlined (the divergence the filing anticipated):**
  the per-callShape envelope DECORATION diverges and is NOT shared —
  single-item `internal_error.details.reason:
  'multi_file_update_partial_failure'` whole-call throw / bulk
  per-item-record (`--continue-on-error`) + fail-fast
  `applied_file_columns_per_item` decoration / create-time
  orphan-warn `create_then_file_upload_partial_failure` extension.
  `foldAndRemap` placement is also caller-side: bulk + create remap
  the failing leg's cause (stable-code rule, cli-design §6.5);
  single-item deliberately does not (mirrors the M38 single-item
  precedent). The helper itself stays pure (no remap, no
  board/env/noCache knowledge) — it returns the raw `MondayCliError`
  cause + re-throws non-`MondayCliError` (programmer bug → whole-call
  `internal_error`). So the lift captured the loop; the decoration's
  genuine divergence justified keeping it per-caller, exactly as the
  filing's "defer the decoration" branch predicted — but the inner
  loop was shareable, so this is a LIFT (of the loop), not a defer.

  **Priority:** RESOLVED (was MEDIUM).

  **Consumers post-lift:** `dispatchFileLegsSequentially` — 3
  (`runItemUpdateSingleFileMultiDispatch` /
  `runItemUpdateBulkFileMultiDispatch` per-item worker /
  `runItemCreateFileMultiDispatch`). The "3-consumer at filing time"
  was unusual (all 3 introduced at the same pre-flight commit); the
  lift landed at the IMPL commit that gave the 3 sites runtime
  bodies, which is when the shared-vs-inlined call could finally be
  made on real emit shapes rather than stubs.

- **R-v0.8-NEW-2 — Rename `enforceSingleFileColumnSet`. ✅ RESOLVED
  at v0.8-M47 pre-flight (`f5353e4`).** Renamed to
  `routeFileColumnDispatch` (the name no longer claims "single" — it
  routes a file `--set` to its dispatch kind + enforces the mutex
  gates). Coupled types renamed for coherence:
  `EnforceSingleFileColumnSetInputs` → `RouteFileColumnDispatchInputs`,
  `FileColumnSetEnforcementResult` → `FileColumnDispatchRoute`.
  Mechanical rename across `src/` + `tests/` (the ~37-ref estimate
  held) + cli-design.md §5.8 (current contract) + this plan's current
  refs; historical v0.6/v0.7 plan-doc references keep their original
  framing per the historical-post-mortem convention. Bundled into the
  M47 pre-flight commit per its named trigger (shares the
  `file-column-set.ts` touch — M47 adds the stdin scope gate to the
  same function). Gates verified green (typecheck + lint + 4226 pass).

  Original filing (v0.8-M46 post-cluster refactor-audit): the function
  name became a misnomer post-M46 D2 carve-out fold — it no longer
  enforces "single file" (multi-file dispatch returns clean kinds for
  the 3 reachable callShapes). Its responsibility is "apply file-column
  mutex rules + route to the appropriate dispatch kind" — candidates
  were `applyFileColumnSetMutex` / `routeFileColumnDispatch` /
  `classifyFileColumnSet`; `routeFileColumnDispatch` chosen
  (returns the dispatch route; throws when no valid route exists).

  **Cost:** ~37 references across `src/` + `tests/` (the function
  is called from 3 action bodies + tested at the unit + integration
  layers). Mechanical rename via IDE refactor; gates verify. Carries
  a small docstring update at every callsite that mentions the
  function name.

  **Counter-argument (deferred-not-rejected rationale):** The name
  is wrong but agents reading the source see the function's
  docstring + mutex-priority block above the body, which describes
  the post-M46 behaviour accurately. The misnomer is a low-grade
  drift, not a load-bearing bug. Fits the project's "lift when the
  cost pays for itself" cadence — wait for an unrelated reason to
  touch the file (e.g. M47 stdin file `--set` carve-out) and bundle
  the rename then.

  **Priority:** LOW (cosmetic; no contract or correctness impact;
  defer to opportunistic timing).

  **Trigger criteria:** Bundle with the next milestone that touches
  `src/api/file-column-set.ts` (M47 stdin file `--set` carve-out is
  the most likely next consumer per v0.8-plan PIVOT block).

- **R-v0.8-NEW-3 — `'item_set'` callShape's defensive multi-file
  throw is argv-unreachable.** Filed at v0.8-M46 post-cluster
  refactor-audit. The throw at `src/api/file-column-set.ts:631-657`
  fires only on `callShape: 'item_set'` with 2+ file `--set`
  entries — but `monday item set <iid> <col>=<value>` is single-
  positional, so production argv cannot reach this path. The throw
  is dead code on production paths + reachable only from synthetic
  unit tests that bypass argv parse.

  **Cost:** Removing the throw drops one defensive arm + the
  defensive unit test. The `FileColumnDispatchRoute` type union (was
  `FileColumnSetEnforcementResult` pre-M47 rename, R-v0.8-NEW-2) still
  accepts `'item_set'` callShape via the type-system arm; defensive
  narrowing via `callShape === 'item_set' && fileSetEntries.length > 1`
  becomes an unreachable code branch that the type system can prove
  (with sufficient narrowing in the argv schema).

  **Counter-argument:** Defense-in-depth has value when the
  callShape's argv schema could ever change. The defensive throw is
  cheap insurance. **v0.8-M47 did NOT invalidate the assumption** —
  it added a stdin `<file-col>=-` *value* on the single positional, NOT
  repeatable `--set` to `item set`; `item set` stays single-positional,
  so the defensive multi-file throw on `'item_set'` remains
  argv-unreachable + untouched by M47.

  **Priority:** LOW (cheap to keep; counter-argument is non-trivial;
  watch-item only). STILL OPEN post-M47.

  **Trigger criteria:** Re-evaluate IF either (a) the argv schema
  for `item set` formally constrains positional count via
  type-system invariants that make the runtime gate redundant, OR
  (b) a future milestone extends `item set` argv in a way that
  invalidates the defensive assumption.

- **R-v0.8-NEW-4 — Schema parse-test backfill for v0.7-M42 +
  v0.7-M43 envelope schemas.** Filed at v0.8-M46 post-cluster
  refactor-audit (surfaced by Codex R3 P3-3 finding scope). M46
  shipped direct parse tests for all 3 new multi-file envelope
  schemas (`fileColumnSetMultiOutputSchema` /
  `bulkFileSetMultiDataSchema` / `itemCreateWithFilesOutputSchema`)
  per Codex R2 P2-2's "pin schemas at pre-flight" discipline. But
  the corresponding v0.7-M42 schema (`bulkFileSetDataSchema`) +
  v0.7-M43 schemas (`itemCreateOutputSchema` for the single-file
  path) lack direct parse-test coverage — they're exercised
  end-to-end via integration tests but not via standalone
  `safeParse` tests.

  **Cost:** Adding ~4 parse tests per schema (valid full envelope +
  wrong-operation rejection + key-shape regression-guards). ~12
  tests total. Co-locate in `tests/unit/api/file-column-set.test.ts`
  or migrate to dedicated `tests/unit/commands/item/` files if the
  schema-test-co-location convention warrants restructuring.

  **Counter-argument:** Integration tests already cover the runtime
  emit shape via `parseEnvelope` + JSON-projection assertions; the
  parse-test backfill is belt-and-suspenders. Test count would
  grow ~12 tests for marginal regression-guard value.

  **Priority:** LOW (gap-closing; backfill candidate; act if a
  schema-shape regression surfaces post-IMPL OR at v0.8 release-
  prep's envelope-snapshot refresh probe step).

  **Trigger criteria:** Bundle with v0.8 release-prep's envelope-
  snapshot refresh probe IF the probe surfaces a schema-shape
  inconsistency between M42/M43's integration-only coverage and the
  M46 schema-parse-test discipline.

- **R-v0.8-NEW-5 — Pre-emptive W6 drift sweep during Codex
  round.** Filed at v0.8-M46 post-cluster refactor-audit. Observed
  empirically during the M46 R1 Codex run: while Codex was still
  reading files mid-round, the agent noticed a contract-term drift
  at `src/api/column-types.ts:240` ("multi-file `--set` rejects
  universally") and pre-emptively fixed it. Codex R2 then did NOT
  re-flag column-types.ts (the fix landed before R2 read the file).

  **Hypothesis:** Pre-emptively grepping for state-historical
  framing terms (per the R-v0.7-NEW-4 contract-term checklist)
  WHILE Codex round-N is generating its findings — and applying
  obvious drift fixes inline — short-circuits the W9 prose-drift
  escalation pattern by one round per checklist hit. M46 followed
  the M43 4-round track despite landing 13 drift-site fixes (vs
  M42's 8-round cadence absorbing similar drift count) — the
  pre-emptive sweep MAY be a load-bearing accelerant.

  **Counter-argument:** Single empirical observation; the
  improvement could be (a) confounded by M46's smaller contract
  surface vs M42's, (b) confounded by R-v0.7-NEW-4 itself
  graduating between M42 and M46, or (c) coincidental. Pre-emptive
  sweep also has a cost — agent context juggling between the
  Codex output stream + a grep sweep + drift fixes mid-round.

  **Priority:** LOW (process-discipline candidate; **3 supporting
  instances** as of the v0.8 refactor-cluster IMPL; NOT ready for
  workflow.md graduation — the 3rd instance refuted the short-circuit
  prediction and surfaced a new grep dimension the discipline must
  absorb first; see below).

  **2nd supporting instance (v0.8-M46 IMPL Codex cluster) —
  PARTIAL.** IMPL ran 3 Codex rounds (R0 IMPL + R1/R2 fix-ups;
  CONVERGED R3). The W9 prose cycle was held to 2 rounds (R1's 2 P3
  + R2's 1 P3) converging at R3 — markedly shorter than M42's
  8-round W9 escalation. After R1, the agent ran the R-NEW-72
  post-fix-up cross-doc grep against the contract-term checklist
  and caught + fixed a residual drift site (`create.ts:967`) BEFORE
  R2 read it; R2 then flagged ZERO drift in the swept helper bodies
  + schema docstrings. **BUT the short-circuit was partial**: R2
  still surfaced a P3 in ADJACENT prose (the bulk/create routing
  comments at `update.ts:856` + `create.ts:922` + a stale test
  comment) that the R1 grep's narrower scope (the 3 helper-body
  files' bodies + docstrings) didn't sweep. Lesson refining the
  hypothesis: the pre-emptive sweep accelerates convergence only as
  far as its grep scope reaches — to fully short-circuit, the
  checklist sweep must cover routing call-sites + test prose, not
  just the helper bodies + their docstrings. Still net-positive
  (2 W9 rounds vs M42's 8), but not a total short-circuit. Counter-
  arguments from the filing (smaller contract surface, R-v0.7-NEW-4
  graduation confound) still apply.

  **Trigger criteria:** At the next multi-round Codex cluster with
  cross-doc contract surface, widen the pre-emptive grep scope to
  routing call-sites + test prose and observe whether the W9 cycle
  drops to ≤1 prose round. **Sequencing note (2026-05-21
  candidate-selection):** the immediately-next Codex cluster is the
  v0.8 refactor cluster's pre-flight + IMPL, but it's an internal
  lift with minimal contract-term prose — so M47 pre-flight/IMPL
  (now standby) remains the better *test* of this discipline. Watch
  the refactor cluster's W9 cycle anyway; the strong observation
  lands at M47. If the cycle drops to ≤1 prose round, file a 3rd
  supporting instance + draft the workflow.md sub-rule under
  R-NEW-72 / R-v0.7-NEW-4's umbrella.

  **3rd supporting instance (v0.8 refactor-cluster IMPL) — REFUTED
  the short-circuit prediction; surfaced a NEW grep dimension.** The
  cluster's Codex IMPL ran **4 rounds** (R1: 1 P3 + a proactively-
  found sibling; R2: 1 P3; R3: 2 P3; R4 CONVERGED) — i.e. **3 prose
  rounds**, NOT the ≤1 the sequencing note predicted for an "internal
  lift with minimal contract-term prose." The agent DID run a
  pre-emptive sweep each round (fixed a sibling proactively after R1,
  swept after R2, enumerated comprehensively after R3), yet each round
  still surfaced a fresh stale site. **Root cause: a distinct drift
  sub-class the contract-term checklist doesn't enumerate — "lift-status
  comments."** When a refactor LIFT ships, the in-code prose that
  described it as pending goes stale in shapes the flag-name/error-code
  checklist never names: (a) "future lift candidate" / "stays deferred
  at N consumers" notes (`update.ts:1693`, `create.ts:1559`); (b)
  removed-inline-block line-number references (`update.ts:2136` "lines
  ~1334-1361 above"); (c) pre-IMPL tense in the lifted helper's own
  docstring ("scheduled ahead of M47", "spreads ARE … that DRAG …
  RECOVERS"); (d) a sibling comment cross-referencing a now-flipped
  note ("per the future-lift-candidate note in the module docstring",
  `update.ts:2110`). These scatter across the touched files + the new
  module's docstring — NOT at the delegating call sites the kickoff
  grep covers. **Refinement (the new grep dimension):** a refactor-lift
  IMPL's pre-IMPL / pre-Codex sweep must add — alongside the
  R-v0.7-NEW-4 contract-term terms — a **lift-status pass**: grep the
  touched files for the lift's R-id + `future|candidate|deferred|lands
  at|scheduled|will land|not yet tip` framing + removed-block
  line-number refs (`lines? ~?\d+(-\d+)?`), and flip every hit to
  shipped/past tense BEFORE round 1. **Graduation:** still LOW, still
  NOT ready — the 3 instances now span contract-term prose (M46) +
  lift-status comments (refactor cluster), two grep dimensions that the
  workflow.md sub-rule must BOTH carry. Trigger unchanged: prove the
  combined-dimension sweep drops a cluster's W9 cycle to ≤1 prose round
  (the next genuine test is **M47** pre-flight/IMPL, which touches both
  contract-term prose AND `file-column-set.ts` lift-status comments via
  the R-v0.8-NEW-2 rename) before drafting the workflow.md rule under
  R-NEW-72 / R-v0.7-NEW-4's umbrella.

- **R-v0.8-NEW-6 — Partial-failure `internal_error`-wrap with
  cause JSON projection (orphan-warn family).** Filed at v0.8-M46
  post-IMPL refactor-audit. The "always-`internal_error` wrap +
  `causeProjection = {code, message, details?}` + throw
  `ApiError('internal_error', ..., {cause, details: {reason, ...,
  cause: causeProjection, hint}})`" block — the pattern
  [[R-v0.7-NEW-5]] explicitly named as a separate, structurally-
  distinct candidate "file-able if a 2nd consumer surfaces" — now
  has **3 consumers** post-M46 IMPL:

  1. **M43 create-time leg-2** (`src/commands/item/create.ts`
     ~1976) — `foldAndRemap` → `causeProjection(remapped)` →
     `create_then_file_upload_partial_failure`.
  2. **M46 create-time multi** (`create.ts` ~2373) — same, with the
     always-present `applied_file_columns` slot (shipped `3e6bdc1`).
  3. **M46 single-item multi** (`src/commands/item/update.ts`
     ~2602) — `causeProjection(cause)` →
     `multi_file_update_partial_failure` (shipped `3e6bdc1`).

  The shared sub-shape is the `causeProjection` construction
  (4 lines: seed `{code, message}`, conditionally attach
  `details`) + the outer `ApiError('internal_error', ...)` wrap
  carrying `cause` (Error.cause) + `details.cause` (JSON
  projection). R-NEW-58 3-consumer trigger fires.

  **Over-fitting caveat (why this is MEDIUM-LOW, not a "lift
  now").** The 3 sites diverge on (a) `foldAndRemap`-vs-raw cause
  (M43 + M46-create remap; M46-single-item deliberately does NOT,
  per the M38 single-item precedent), (b) `details.reason` literal
  + decoration slots (`created_item_id` for create vs `item_id`
  for update; `applied_file_columns` present on both M46 sites),
  (c) the hint text. The genuinely-shared surface is just the
  ~4-line `causeProjection` builder — small enough that a
  `projectCauseForEnvelope(err: MondayCliError):
  Record<string, unknown>` helper is the realistic lift scope, NOT
  the whole wrap. **Priority:** MEDIUM-LOW → **COMMITTED to the
  v0.8 refactor cluster** (prioritised 2026-05-20 on user demand;
  bundled with [[R-v0.7-NEW-5]] since both live in mutation-path
  catch arms — see `§3 "v0.8 refactor cluster"`). Lift the
  `projectCauseForEnvelope` builder only (the surrounding orphan-warn
  decoration diverges per site). Cross-references
  [[R-v0.7-NEW-5]] (the sibling fail-fast-scaffold lift; distinct
  because that one preserves the remapped code + applied-count
  decoration, this one pins `internal_error` + projects the cause).

  **✅ LIFT SHIPPED 2026-05-21** (v0.8 refactor cluster IMPL,
  `refactor(api): lift reThrowDecorated + projectCauseForEnvelope`).
  `projectCauseForEnvelope(err: MondayCliError): Record<string,
  unknown>` lives in `src/api/error-decoration.ts`; all 3 orphan-warn
  sites delegate (create `:1976` / `:2373` seed from `remapped`; update
  `:2602` seeds from `cause`). The over-fitting caveat held: ONLY the
  ~4-line builder lifted; the per-site `internal_error` wrap +
  `details.reason` literal + decoration slots + hint stayed inline. The
  two create-site `details`-arm c8-ignores dropped — the helper's
  details-present/absent unit tests cover both arms genuinely.
  Bundled-but-separate from [[R-v0.7-NEW-5]] per PD1; Codex IMPL R1
  confirmed they correctly stay separate (no merge flagged).

- **R-v0.8-NEW-7 — Bare-SHA doc citations are fragile under
  rebase / force-push.** Filed at v0.8-M46 post-IMPL refactor-audit
  (process / tooling). The project cites bare 7-char commit SHAs
  extensively in plan-doc cluster ranges (`§1` status column, `§3`
  milestone entries, `§22` R-class register, CLAUDE.md "Current
  state"). The v0.8-M46 close-docs baked ~12 such citations
  (pre-flight `89a86ea..d9b035b`, IMPL `3e6bdc1..1289133`, plus
  individuals) — and the concurrent `is_leaf` bug-fix
  (`0ed2f91`) was then rebased in BEFORE the M46 chain and the
  result force-pushed, rewriting every M46 SHA and staling all
  ~12 citations in one stroke (re-pointed at `804ee3c`). The
  rewritten commits' own MESSAGES still carry the pre-rebase SHA
  text (unfixable without another history rewrite).

  **Candidate fixes (pick at adoption, not now):**
  - (a) **Prefer commit-subject-line references over bare SHAs**
    in NEW plan-doc prose (e.g. "the M46 IMPL R0 commit" / "the
    pre-flight R3 fix-up") — stable across rebases; the SHA range
    can ride alongside as a convenience that's understood to be
    rebase-volatile.
  - (b) **A post-rebase SHA-re-point checklist step** — when a
    rebase/force-push rewrites a cited range, sweep
    `docs/ + CLAUDE.md` for the old SHAs and re-point (the manual
    process this audit-session ran; could graduate to a
    workflow.md rule).
  - (c) **A pre-commit / CI check** that every backtick-wrapped
    7-40-char hex token in `docs/*.md` + `CLAUDE.md` resolves via
    `git cat-file -e` on the current branch (catches stale
    citations mechanically; false-positive risk on intentional
    reflog-only refs like the M39 `bb7c2cc..2e501b5` — would need
    an allowlist).

  **Priority:** MEDIUM (low-effort, real recurrence risk — the
  project rebases/force-pushes around concurrent-agent work + at
  release-prep). **Trigger:** adopt (a) proactively at the next
  close-docs that writes new SHA citations; escalate to (b)/(c) if
  a 2nd rebase-staling episode occurs. Cross-references the
  release-prep cross-doc grep discipline (R-NEW-82) — the same
  "sweep docs for stale tokens" muscle.

- **R-v0.8-NEW-8 — Codex-review audit-point: new emit shape →
  command-registry `outputSchema` advertisement check.** Filed at
  v0.8-M46 post-IMPL refactor-audit (template extension). M46 IMPL
  added 3 new `operation`-discriminated emit shapes
  (`add_files_to_columns` / `item_update_bulk_file_set_multi` /
  `item_create_with_files`) emitted via schemas passed directly to
  `emitMutation` — so runtime was correct, but the command-registry
  `outputSchema` unions (`itemUpdateOutputSchema` /
  `item.create`'s `outputSchema`) under-advertised them, and
  `monday schema item.update` / `item.create` would not describe
  the new shapes. Codex IMPL caught this as **P2-1 at R2, not R1**
  — i.e. the confirmation round, after the W9-prose round. The
  recurring class: M38 added its single-item file shape to the
  union; M42 deliberately EXCLUDED its bulk shape (per-item-results
  payload, structurally distinct); M46 had to add the single-item
  multi shape + create-with-files shape. Each emit-shape milestone
  re-litigates "does this go in the advertised union?".

  **Candidate:** fold a W-point into
  `.claude/templates/codex-pre-flight-review.md` (and the IMPL
  review prompt): *"For every NEW `operation`-discriminated emit
  schema this milestone adds, confirm the command's registry
  `outputSchema` either advertises it (single-resource `data`
  shapes) OR documents why it's excluded (bulk per-item-results
  payloads, emitted via own schema + `operation`-discriminated, per
  the M42 precedent)."* Catches the gap at R0/R1 instead of R2.
  **Priority:** LOW-MEDIUM (single occurrence so far, but a clean
  mechanical audit-point; fold at the 2nd occurrence — likely M47
  if its stdin path introduces a new emit shape, or any future
  emit-shape milestone). Cross-references R-NEW-6 (the review-
  template structure) + the M42 union-exclusion precedent
  (`itemUpdateOutputSchema` docstring).

- **R-v0.8-NEW-9 — Mocked-transport fixtures must mirror the REAL
  wire shape.** Filed at v0.8-M48 probing (2026-05-20), surfaced by
  the M49 P1 bug. The multipart-upload mock fixture
  (`tests/fixtures/multipart-load.ts`) validated `operationName` +
  `match_filename` + `match_variables` but **never checked the
  multipart FORM STRUCTURE** (Apollo `operations`/`map`/`"0"` vs
  Monday's native `query`/`map`-string/named-part). So 5 milestones
  of file-upload tests (M31/M38/M42/M43/M46) passed green against a
  fixture asserting the **wrong** wire format — the bug only
  surfaced when a live probe finally hit Monday's real parser. The
  general lesson: **a mock that doesn't structurally match the real
  wire can keep a whole feature-family green while it's broken in
  production.**

  **Candidate responses (decide at M49 / a later hardening pass):**
  - (a) **Update the multipart fixture to assert the native form**
    (the M49 fix must do this anyway — the fixture should reject a
    request that wouldn't parse live).
  - (b) **A `RUN_LIVE_TESTS`-gated live smoke test** per wire seam
    (file upload, and audit other mocked seams) so CI can optionally
    exercise the real wire — the JSON `client.request` mock has the
    same theoretical risk, though GraphQL JSON is far less
    format-fragile than multipart.
  - (c) **Audit the other mocked seams** (JSON transport cassettes,
    OAuth) for the same "mock validates a shape the wire wouldn't
    accept" gap — likely low-risk for JSON but worth one pass.

  **Priority:** HIGH (this class of gap shipped a P1). The narrow
  fix (a) rides M49; the broader live-smoke discipline (b) + seam
  audit (c) are a hardening candidate. **Trigger:** land (a) with
  M49; file (b)/(c) as a follow-up if the M49 pre-flight surfaces
  more mocked-vs-wire divergence. Cross-references the testing.md
  "mock at the network boundary" rule — which this refines: *mock at
  the boundary, but the mock must reject what the wire would reject.*

  **RESOLVED at M49 (`2ec67ad`, 2026-05-21).** Refinement of the
  filing: the load-bearing wire-shape assertion was NOT in
  `tests/fixtures/multipart-load.ts` (a higher-level `MultipartTransport`
  mock that operates ABOVE FormData assembly — it never saw the form
  structure, so it can't lie about it) but in
  `tests/unit/api/multipart-transport.test.ts`, which drives the REAL
  `createMultipartFetchTransport` over a `fetchImpl` stub and inspects
  the assembled `FormData`. That test asserted the *wrong* (Apollo)
  shape. Closures: **(a)** rewrote those assertions to the native form
  + added a regression guard (`operations` / `'0'` / `operationName`
  parts MUST be absent — a revert to Apollo fails the test);
  `multipart-load.ts` needed no wire change. **(b)** adopted a
  `RUN_LIVE_TESTS`-gated live smoke test for the multipart seam
  (`tests/e2e/live-multipart-upload.test.ts`) modelled on
  `live-schema-drift.test.ts`. **(c)** the broader audit of the JSON
  `client.request` cassette + OAuth mocks for the same
  "mock-accepts-what-the-wire-rejects" gap stays a DEFERRED follow-up
  (low risk for JSON — far less format-fragile than multipart).
  testing.md's "mock at the boundary" rule now carries the refinement:
  *where the mock structurally can't match the wire, a live smoke test
  backstops it.*

- **R-v0.8-NEW-10 — M46 multi-file `--set` coverage shortfall (CI
  red).** Filed 2026-05-21 during the M49 / CI-fix session.
  Global branch coverage is **95.14% vs the 95.45% floor** (15
  branches short of the gate), failing CI's `test:coverage` step.
  Root cause is PRE-EXISTING and NOT M49: the parent commit
  (`f2dd552`) measures the identical 95.14% (verified via a clean
  worktree coverage run). The deficit concentrates in M46's
  multi-file `--set` additions — `src/commands/item/update.ts`
  (**43** uncovered branches, 79.42%) + `src/commands/item/create.ts`
  (**29** uncovered, 82.31%) — exactly the surface CLAUDE.md flagged
  as *"precise post-IMPL coverage PENDING a clean run, blocked by the
  R-v0.7-NEW-8 worker-pool EAGAIN flake."* The clean run never
  happened, so M46 shipped under-covered. **Decision (user,
  2026-05-21): DEFER** — land the M49 + table-colour fixes now; the
  coverage step stays red until a dedicated M46-coverage pass adds
  tests for the uncovered dispatch arms (partial-failure 0..N-1 /
  dry-run / precheck-abort across the 3 callShapes). **Priority:**
  HIGH (blocks CI green). **Trigger:** a focused coverage session on
  `item/update.ts` + `item/create.ts`; recover ≥15 branches to clear
  the floor (full recovery to ~95%/file is the stretch target). Do
  NOT lower the threshold — the gap is a real test deficit, not a
  mis-set floor.

  **RESOLVED 2026-05-21** (tests `91b458f` + lift `4dcec94`). Global
  branch coverage cleared the 95.45% floor — **95.14% → 95.47%
  (4452/4663)**, CI's
  `test:coverage` step now passes. Two coordinated pieces did it:
  (i) the **R-v0.8-NEW-11** transport-helper lift recovered the
  duplicated defensive arms in `transport.ts` + `multipart-transport.ts`
  (both transports + the new `fetch-transport-helpers.ts` now report
  ZERO uncovered branch arms — net the lift removed the duplicated
  copies AND covered the previously-uncovered TLS `UNABLE_TO_*` arm +
  `combineSignals` empty-input guard once); (ii) targeted M46
  multi-file `--set` dispatch-arm tests on `item/create.ts` (82.31% →
  86.58%) + `item/update.ts` bulk multi (the first-item/first-leg
  fail-fast skip-invalidate arm). Test-only additions (no Codex pass;
  gates carry verification) EXCEPT the R-v0.8-NEW-11 production lift,
  which got its own Codex round (1 P2, applied inline — see that
  entry). The conditional-spread metadata arms in the bulk dispatch
  catch (`update.ts:2159` M42 + `:2854` M46 + create leg-1) were left
  UNTOUCHED — they are the **R-v0.7-NEW-5** `reThrowDecorated` lift's
  lock-step-ratchet target, not this session's scope. Per-file
  `~95%/file` stretch NOT pursued (the floor-clear is the stop
  condition); `item/update.ts` stays at 79.42% (its remaining
  uncovered arms are mostly the R-v0.7-NEW-5 conditional-spreads).

- **R-v0.8-NEW-11 — shared fetch-transport helpers (code lift).**
  Surfaced at M49 IMPL (2026-05-21). `src/api/multipart-transport.ts`
  duplicates FOUR helpers verbatim-or-near-verbatim from
  `src/api/transport.ts`: `describeFetchError` (~30 LOC of
  ENOTFOUND/ECONNREFUSED/CERT/UNABLE_TO_ message mapping),
  `isAbortError`, `headersToRecord`, and `combineSignals` — the
  multipart module's own docstrings literally say "Mirrors
  `transport.ts:describeFetchError`" / "Mirrors
  `transport.ts:combineSignals`". **Two consumers, but a large
  (~60 LOC) surface that already threatens coverage + correctness:**
  (i) the SAME defensive branch is uncovered in BOTH copies —
  `transport.ts:249` + `multipart-transport.ts:409` — so lifting once
  + one test recovers branches in both (directly helps
  **R-v0.8-NEW-10**); (ii) the two `combineSignals` have already
  DRIFTED — `transport.ts` hedges with `typeof AbortSignal.any ===
  'function'` + a legacy Node-<19 fallback, while
  `multipart-transport.ts` assumes `AbortSignal.any` exists and
  c8-ignores the same guards. A unified
  `src/api/fetch-transport-helpers.ts` would converge the message
  vocabulary (already an explicit goal in the docstrings), the signal
  combination, and the c8 treatment. **Priority:** MEDIUM-HIGH —
  "lift now"-leaning per the large-surface-at-2-consumers exception
  (not the usual rule-of-three wait). **Trigger:** ride the
  R-v0.8-NEW-10 coverage session (the lift + a shared-helper test set
  recovers the duplicated uncovered branches once) OR the next
  transport touch; production code, so it needs its own Codex pass.
  **Over-fit watch:** the divergence above is real, not cosmetic —
  the lifted `combineSignals` must keep `transport.ts`'s legacy
  fallback (the safer superset), not multipart's narrower form.
  Cross-references R-v0.8-NEW-10 (coverage) + R-NEW-41 (per-wire-shape
  module conventions — the lift is helper-sharing, NOT merging the two
  transport seams, which stay separate per their different bodies).

  **SHIPPED 2026-05-21** (`4dcec94`). All four helpers
  (`describeFetchError` / `isAbortError` / `headersToRecord` /
  `combineSignals`) lifted to NEW `src/api/fetch-transport-helpers.ts`
  (exported); both transports import them; `assets.ts`'s docstring
  cross-reference repointed at the shared module. A direct unit test
  (`tests/unit/api/fetch-transport-helpers.test.ts`) exercises every
  arm; the existing transport tests still drive them indirectly. The
  over-fit watch held: the lifted `combineSignals` KEEPS `transport.ts`'s
  legacy `AbortSignal.any` feature-detect + Node-<19 controller fallback
  (the safer superset), NOT multipart's narrower form. All three files
  now report ZERO uncovered branch arms. **Codex IMPL R1** (gpt-5.5):
  1 P2 (P2-1/W3 — the initial block-wrap c8-ignored the reachable
  `AbortSignal.any(real)` path; restructured so only the unreachable
  feature-detect guard + legacy fallback are ignored and the platform
  path stays coverage-counted), applied inline; W2 behavioural-
  equivalence + W4 import-cycle/boundary clean; the coverage gate is
  the authoritative confirmation of the c8 fix. Two-consumer
  large-surface lift per the R-NEW-58 exception (not the rule-of-three
  wait).

- **R-v0.8-NEW-12 — live-smoke-test harness (test-infra lift,
  watch-item).** Surfaced at M49 (2026-05-21). The new
  `tests/e2e/live-multipart-upload.test.ts` was modelled on
  `tests/e2e/live-schema-drift.test.ts` and duplicates the
  `LIVE = RUN_LIVE_TESTS === '1' && TOKEN` gate, the `post()`
  JSON-over-fetch helper, the `ENDPOINT` / `API_VERSION` constants,
  and the `describe.skipIf(!LIVE)` shape (multipart adds a
  create-board/column/item + delete-board scaffold on top).
  **Two consumers** today. **Priority:** LOW-MEDIUM. **Trigger:**
  lift the gate + `post` + (optional) throwaway-board scaffold to a
  shared `tests/e2e/live-helpers.ts` at the **3rd** live smoke test —
  likely when R-v0.8-NEW-9(c)'s "audit other mocked seams" lands more
  live backstops. Cross-references R-v0.8-NEW-9 (the live-smoke
  discipline this harness implements).

- **R-v0.8-NEW-13 — Codex template "wire-mock parity" audit-point
  (template extension, watch-item).** Surfaced at M49 IMPL review
  (2026-05-21). M49's Codex prompt added **W8 — "the mock must mirror
  the real wire; the test must reject what the wire would reject"** as
  a milestone-specific watch-point; it's broadly applicable to ANY
  wire-shape-changing milestone (the exact class of bug R-v0.8-NEW-9
  encodes). **One occurrence.** **Priority:** LOW-MEDIUM. **Trigger:**
  fold W8 into `.claude/templates/codex-pre-flight-review.md` as a
  template-stable audit-point at the **2nd** wire-shape milestone —
  mirrors R-v0.8-NEW-8's "fold the emit-shape audit-point at the 2nd
  occurrence" cadence. Cross-references R-v0.8-NEW-8 (sibling
  audit-point candidate) + R-v0.8-NEW-9 (the testing rule W8 enforces).

- **R-v0.8-NEW-14 — Unwired pre-flight stub MODULES need a
  whole-declaration c8-ignore, not body-only (testing discipline +
  template audit-point, watch-item).** Surfaced at the v0.8
  refactor-cluster pre-flight (2026-05-21); Codex R1 P2-1 caught it.
  A standalone-module pre-flight stub that is UNWIRED this session
  (nothing imports it yet — the lift's call sites stay inline until
  IMPL) is still statically instrumented by v8 coverage (`all:true` +
  `include: ['src/**/*.ts']`). c8-ignoring only the function BODY
  leaves the function DECLARATION counted — the lcov records
  `FN:<name>` / `FNF:n` / `FNH:0`, polluting the `functions` metric
  with unreachable stubs. The fix is to place `/* c8 ignore start */`
  ABOVE the `export function` line (wrap the whole declaration), so
  the function is excluded from FNF/FNH too. This is the wrinkle
  R-v0.7-NEW-3 does NOT cover: a WIRED command stub is imported +
  exercised via argv, so its declaration IS hit (FNH:1) and body-only
  c8-ignore suffices; the unwired-helper-module case is new and
  recurs for every standalone-module R-class lift pre-flight (the
  backlog has more). **One occurrence** (this pre-flight; the global
  `functions` floor was not breached — 99.03%/1350 — so it's a hygiene
  signal, not a gate break). **Priority:** LOW. **Trigger:** (a) at the
  **2nd** unwired-stub-module pre-flight, fold a one-line note into
  `.claude/rules/testing.md`'s "c8 ignore start/stop block-wrap"
  subsection ("for an UNWIRED module, wrap the declaration not just the
  body — else `functions` counts the stub as FNH:0"); (b) fold a
  sibling "unwired-stub coverage hygiene" audit-point into
  `.claude/templates/codex-pre-flight-review.md` at the 2nd occurrence
  (mirrors R-v0.8-NEW-13's W8 cadence — Codex already backstops it
  here). Cross-references R-v0.7-NEW-3 (parent — pre-flight stub-body
  conventions, this extends them to unwired modules) + testing.md
  c8-ignore block-wrap subsection + R-v0.8-NEW-13 (sibling
  template-audit-point graduation cadence).

  **IMPL-close note (2026-05-21).** The error-decoration.ts instance is
  now RESOLVED: the refactor-cluster IMPL wired both helpers (7 sites
  delegate) and removed the whole-declaration c8-ignore wraps, so the
  functions are genuinely exercised (FNF:2 / FNH:2; global functions
  99.03%/1350 → 99.11%/1352 as the 2 helpers rejoined the denominator
  COVERED). The watch-item's graduation **trigger did NOT fire** — this
  was the 1st occurrence and it self-closed at IMPL; the testing.md /
  template folds still wait on a **2nd** unwired-stub-module pre-flight
  (the backlog's remaining standalone-module lifts).

- **R-v0.8-NEW-15 — Shared stdin live-dispatch helper (code lift).
  RESOLVED at M47 IMPL (`0d99ea7`) — CONSIDERED, NOT TIPPED.** The
  decision landed exactly as the counter-argument below predicted: the
  blob-source seam (`readStdinFileSource`) was already extracted, the
  wire fetcher (`addFileToColumn`) already existed, and per-site emit
  diverges (set/update inline `FileColumnSetOutput`; create two-leg
  under §5.8 orphan-warn) — so the stdin sites compose the two existing
  pieces directly with inline emit, mirroring the PATH dispatch's own
  inline-per-site shape + [R-v0.8-NEW-1]'s M46 outcome. The create
  ordering constraint (read stdin BEFORE leg-1; path-blob build AFTER
  leg-1) additionally rules out a combined helper. 1st "considered but
  not tipped" event for the M47 surface (R-NEW-58 gating). Original
  filing below. Filed at the v0.8-M47
  post-pre-flight refactor-audit (2026-05-21). The M47 pre-flight wired
  the stdin `<file-col>=-` source as a thin branch at FOUR dispatch
  sites — `item set` live + `item set` dry-run catch
  (`commands/item/set.ts`), single-item `runItemUpdateSingleFileDispatch`
  (`commands/item/update.ts`), `runItemCreateFileDispatch`
  (`commands/item/create.ts`). Today the SHARED logic is already
  lifted (`isStdinFileSetSource` + `resolveStdinFilename` +
  `readStdinFileSource` in `file-source.ts`); each site's branch is a
  3-line stub (`if (isStdinFileSetSource(...)) { resolveStdinFilename;
  await readStdinFileSource; return }`). At M47 IMPL the stub becomes a
  live read + Blob + `add_file_to_column` dispatch + envelope emit —
  and THAT may crystallise a shared helper (parallels
  [R-v0.8-NEW-1]'s `dispatchFileLegsSequentially`, which lifted at M46
  IMPL once the 3 multi-file callShapes had runtime bodies to compare).

  **Cost:** N/A at pre-flight (the shared part is already lifted). At
  IMPL: a `dispatchStdinFileSet({client, multipart, itemId?, columnId,
  filename, signal, retries}) → emit-shaped result` helper IF the
  post-source dispatch+emit is byte-similar across the sites.

  **Counter-argument (likely "considered but not tipped"):** the
  per-site emit DIVERGES — `item set` emits `FileColumnSetOutput`
  inline; `item create` is a two-leg `create_item` then file under the
  §5.8 orphan-warn envelope; the dry-run echo (D4, size-less) differs
  from live. The shared seam may be just the Blob source
  (`readStdinFileSource`, already extracted) with per-site emit staying
  inline — mirroring R-v0.8-NEW-1's outcome (inner loop lifted, the
  per-callShape envelope decoration stayed inlined). Decide on real
  emit shapes at IMPL, not on the pre-flight stubs.

  **Priority:** LOW (decision deferred to M47 IMPL when the runtime
  bodies exist). **Trigger:** evaluate at M47 IMPL kickoff per R-NEW-58
  (lift AHEAD of the feat commit if a 3rd byte-similar consumer
  crystallises). Cross-references [R-v0.8-NEW-1] (the directly-parallel
  M46-IMPL crystallisation precedent) + R-v0.6-NEW-1
  (`file-source.ts` source-helper family).

- **R-v0.8-NEW-16 — GRADUATED into `.claude/rules/workflow.md` at the
  M47 IMPL close (`da62add`).** Four supporting instances
  (`m42`/`m43`/`m46`/`m47_preflight_stub`) cleared the 3-5 threshold;
  the literal-naming convention + the RESERVED-literal regression-guard
  ("asserts the literal is absent from runtime output post-IMPL") fold
  into the "Pre-flight contract diff discipline" section. Kept out of
  the M47 close-docs sweep commit per the "don't graduate a rule inside
  a housekeeping sweep" cadence. Original filing below. Graduate the
  `*_preflight_stub` literal +
  RESERVED-literal regression-guard idiom into workflow.md (process
  discipline, graduation-ready).** Filed at the v0.8-M47
  post-pre-flight refactor-audit (2026-05-21). The pre-flight-stub
  idiom now has **4 supporting instances**: `m42_preflight_stub`
  (v0.7-M42) / `m43_preflight_stub` (v0.7-M43) / `m46_preflight_stub`
  (v0.8-M46) / `m47_preflight_stub` (v0.8-M47). Each uses the SAME
  two-part discipline: (a) a `mNN_preflight_stub` `details.reason`
  literal on the c8-ignored stub throw, surfaced verbatim by an
  integration test that pins the stubbed surface; (b) the literal stays
  **RESERVED** across the codebase post-IMPL (regression-guarded by a
  test asserting it never appears in runtime output). `.claude/rules/
  workflow.md`'s "Pre-flight contract diff discipline" documents the
  c8-stub step but NOT the literal-naming convention NOR the
  RESERVED-literal regression-guard.

  **Cost:** ~6-10 lines folded into workflow.md's "Pre-flight contract
  diff discipline" (the literal-naming convention + the
  RESERVED-literal regression-guard rule + the "asserts the literal is
  absent from runtime output post-IMPL" test pattern).

  **Counter-argument:** the per-milestone plan-doc entries already
  carry the pattern with full context; the workflow.md fold strips the
  evidence. But 4 instances is past the project's typical 3-5 graduation
  threshold (cf. R-NEW-76 graduated at M34 pre-flight, R-v0.7-NEW-4 at
  M42 IMPL R7), and the idiom is now stable enough to state as a rule.

  **Priority:** MEDIUM (graduation-ready; clear recurring pattern).
  **Trigger:** fold into `.claude/rules/workflow.md` "Pre-flight
  contract diff discipline" at the next workflow.md touch OR the M47
  IMPL close (whichever lands first) — kept as a watch-item here rather
  than graduated in a doc-audit commit (per the "don't graduate a rule
  inside a housekeeping sweep" cadence). Cross-references R-NEW-76
  (parseArgv-BEFORE-c8, the sibling pre-flight-stub workflow rule it
  would sit beside) + R-v0.7-NEW-4 (contract-term checklist,
  same-section neighbour).

- **R-v0.8-NEW-17 — DONE at the M47 IMPL close (`d686177`).** Both
  verbs' `<file>` schema messages + help notes refreshed: `item upload`
  now points at `cat file | monday item set <iid> <file-col>=-`;
  `update upload` drops the now-met "once `--filename` is pinned"
  promise (it attaches to an Update via `Update.assets`, not a file
  column — no `--set` equivalent). String-only; no test asserted the
  old strings (probed at the audit), so no test cascade. Both verbs stay
  path-only. Original filing below. Refresh the verb-shaped `item
  upload` / `update upload` stdin hints (prose/UX follow-up). Filed
  at the v0.8-M47 post-pre-flight refactor-audit (2026-05-21);
  documented inline as a "Deferred sibling" in §3 M47 and elevated here
  for discoverability. The `<file>` schema messages in
  `src/commands/item/upload.ts` + `src/commands/update/upload.ts` still
  say stdin "is not supported in v0.4-M31 (a future contract extension
  may add stdin support once a `--filename` companion flag is pinned)".
  M47 MET that precondition for the friendly `--set` path — so the hint
  now points an agent at a future that already arrived elsewhere.

  **Cost:** 4 string edits (2 per file: the `.min(1)` message + the
  `.refine(p !== '-')` message); no test asserts these strings (probed
  at the audit), so no test cascade.

  **Counter-argument:** the strings are still technically TRUE (stdin
  IS unsupported on the verb-shaped uploads — M47 carved out only
  `--set`), and the canonical cli-design §6.4 note was already updated
  to clarify the split, so this is a UX nicety, not a correctness bug.
  Left out of the M47 pre-flight to keep the diff scoped to the `--set`
  surface Codex reviewed.

  **Priority:** LOW (UX-only). **Trigger:** at the next `upload.ts`
  touch OR the M47 IMPL — point `item upload` at `monday item set <iid>
  <file-col>=-`; `update upload` has NO `--set` equivalent (attaches to
  an Update via `Update.assets`, not a file column), so its hint just
  drops the now-met "once --filename is pinned" promise. Cross-references
  §3 M47 "Deferred sibling" note.

- **R-v0.8-NEW-18 — Single-item `FileColumnSetOutput` emit block (code
  lift, watch-item).** Filed at the v0.8-M47 post-IMPL refactor-audit
  (2026-05-21). The single-item file-column success emit —
  `const data: FileColumnSetOutput = { operation: 'add_file_to_column',
  item_id, column_id, filename, file_size_bytes, asset }` then
  `emitMutation({ ..., schema: fileColumnSetOutputSchema, warnings:
  <warns>.map(...), ...toEmit({ data: asset, complexity, stats }),
  source: 'live', cacheAgeSeconds: null, complexity, resolvedIds })` —
  now appears at **FOUR near-identical sites**: `item set` path + `item
  set` stdin (`commands/item/set.ts`), and `runItemUpdateSingleFileDispatch`
  path + stdin (`commands/item/update.ts`). M47 doubled it (the two
  path sites predate M47; M47 added the two stdin sites). Past
  rule-of-three, so a shared `emitFileColumnSet({ ctx, programOpts,
  itemId, columnId, filename, fileSizeBytes, asset, complexity,
  warnings, token, toEmit })` helper is nameable.

  **Cost:** a ~10-param helper in `file-column-set.ts` or a
  command-level shared emit; the 4 sites collapse to one call each.

  **Counter-argument (likely "considered but not tipped"):** the
  per-site context DIVERGES — the warning SOURCE differs (`item set`
  threads `resolverWarnings`; `item update` threads `m38.warnings`),
  and `ctx` / `programOpts` / `toEmit` / `token` are accessed
  differently (closure-local in `set.ts` vs `inputs.*` in `update.ts`).
  A 10-param helper might obscure more than it consolidates, and the
  `item create` emit is structurally OUT (it emits `ItemCreateOutput`
  on `data`, not the file asset — per the M43 contract). This is the
  emit-block sibling of [R-v0.8-NEW-15] (which covered the
  read+dispatch seam and resolved not-tipped); -15 noted set/update
  "emit `FileColumnSetOutput` inline" without forward-tracking the
  block itself — this entry does.

  **Priority:** LOW. **Trigger:** lift if a 5th single-item file-emit
  site appears (e.g. a new single-target file callShape) OR if the
  `<warns>.map(...)` callback's recurring coverage gap (the W9 known
  shape — uncovered at all 4 sites when fixtures carry no resolver
  warnings) becomes a maintenance irritant worth a single covered
  helper. Cross-references [R-v0.8-NEW-15] (read+dispatch seam,
  not-tipped) + [R-v0.8-NEW-1] (the per-callShape-envelope-stays-inline
  precedent).

- **R-v0.8-NEW-19 — Fold the create-time `settings`-wrap flag INTO the
  per-type settings-schema registry (code lift, watch-item).** Filed
  at the v0.8-M48 pre-flight post-flight refactor-audit (2026-05-21).
  M48 gave the M19 `WRITABLE_SETTINGS_SCHEMAS` registry
  (`commands/board/column-create.ts`) its first TWO non-empty entries
  (`board_relation` → `boardRelationSettingsSchema`, `dependency` →
  `dependencySettingsSchema`) AND introduced a NEW sibling set
  `CREATE_TIME_SETTINGS_WRAP_TYPES = {board_relation, dependency}` that
  encodes "which writable types wrap their validated `--settings`
  under `defaults.settings` on the wire". The wrap-membership is a
  per-type property that today lives in a Set parallel to the schema
  registry + the `WRITABLE_SETTINGS_EXPECTED_KEYS` registry — three
  parallel per-type structures keyed by `WritableColumnType`.

  **The lift:** if a 3rd wrap-type arrives, collapse the parallel
  structures into one record per type — e.g.
  `WRITABLE_SETTINGS: Record<WritableColumnType, {schema, expectedKeys,
  wrapUnderSettings: boolean}>` — so a new type's settings contract is
  declared in ONE place rather than threaded across a schema map + an
  expected-keys map + a wrap Set (easy to add the schema but forget
  the wrap, shipping silently-inert settings — the exact M19-bug
  class).

  **Counter-argument (why it stays a watch-item, not a lift now):**
  only 2 types wrap today, and they were added together, so the
  parallel-structure drift hasn't bitten yet. The R-NEW-58 schema-
  registry consumer-threshold framing applies: this is the registry's
  1st create-time-wrap consumer-pair; lift at the 3rd wrap-type (e.g.
  if a future milestone generalises create-time `--settings` to
  status/dropdown/timeline per the D4 scope-guard, those would need
  per-type wrap decisions). **Priority:** LOW. **Trigger:** lift when a
  3rd `CREATE_TIME_SETTINGS_WRAP_TYPES` member is added OR when the D4
  scope-guard is lifted (create-time `--settings` generalised beyond
  board_relation/dependency). **Post-M48-IMPL re-confirmed (2026-05-21,
  `4803cbf`): still 2 members — the IMPL was a mechanical stub→ternary
  swap that added no wrap-type, so the trigger stays UNMET.**
  Cross-references [R-NEW-58] (schema-registry consumer-threshold) + the
  M48 §3 entry's R-class trigger note.

- **R-v0.8-NEW-20 — Suspected `dependency` settings live-drift:
  `deriveAllowedBoards` reads `dependencyBoards`, but the M48 probe
  read `boardIds` (bug watch-item — NEEDS A PROBE, do NOT fix blind).**
  Filed at the v0.8-M48 pre-flight post-flight refactor-audit
  (2026-05-21). **Out of M48 scope** (M48 is create-time COLUMN
  settings; this is the item-VALUE `--set` path). The M48 probe
  created a `dependency` column and read its `settings_str` back as
  `{"boardIds":[<host>],"dependencyNewInfra":true,"allowMultipleItems":
  true,"dependency_mode":null}` — i.e. the allowed-board list is under
  **`boardIds`**, NOT `dependencyBoards`. But M19's
  `deriveAllowedBoards` (`src/api/column-values.ts:1139`) reads
  `obj.dependencyBoards` for `context === 'dependency'`. If live
  dependency columns really store `boardIds` (not `dependencyBoards`),
  then `monday item set <iid> <dependency-col>={"item_ids":[...]}`
  would derive an EMPTY allowed-board list and surface the
  no-allowed-boards `usage_error` even for a correctly-configured
  dependency column — a live-vs-fixture drift analogous to the
  documented `is_leaf` drift (frozen fixtures encode `dependencyBoards`,
  so the M19 unit/integration tests stay green while the live path
  diverges).

  **Why a watch-item, not a fix:** the evidence is one API-created
  column. M19 had SOME basis for `dependencyBoards` ("per Monday's
  distinct settings shape"), and a UI-created dependency column, or a
  different API-version shape, might differ. Confirm with a dedicated
  probe (create a dependency column via the UI + the API, read both
  `settings_str` shapes at API `2026-01`) BEFORE changing
  `deriveAllowedBoards` — flipping it blind could break the path that
  M19's basis was correct for. **Priority:** MEDIUM (a real live break
  if confirmed, but unconfirmed + scoped to dependency item-value
  `--set`, a narrow surface). **Trigger:** run the confirming probe at
  the next session that touches `column-values.ts` / dependency
  item-value writes, or sooner if an agent reports a dependency `--set`
  failing live. Cross-references the `is_leaf` live-drift
  (user-memory `project_is_leaf_live_drift.md`) as the precedent
  pattern. Also saved to user-memory so it survives across sessions.

> Likely future first-candidate-at-skeleton if PIVOT reverses: a
> "wire-shape contract migration cluster" pattern derived from
> M44's user-entity migration — the first non-additive cluster in
> the project's lifetime.
