# v0.1 implementation plan

> **Status:** plan, not contract. The binding contract is
> [`cli-design.md`](./cli-design.md). This document sequences the work
> needed to ship v0.1 of that contract; it doesn't redefine it. If a
> conflict arises, `cli-design.md` wins and this plan is updated.

## 1. Scope summary

v0.1 is "**the read-only core + safe mutations**" — an agent can read
everything the CLI surfaces, make small scoped idempotent changes,
and post comments narrating its work.

**In scope (from `cli-design.md` §13):**

| Group | Commands |
|-------|----------|
| `account` | `whoami`, `info`, `version`, `complexity` |
| `workspace` | `list`, `get`, `folders` |
| `board` | `list`, `get`, `find`, `describe`, `doctor`, `subscribers`, `columns`, `groups` |
| `item` | `list`, `get`, `find`, `search`, `set`, `clear`, `update`, `subitems` |
| `update` | `list`, `get`, `create` |
| `user` | `list`, `get`, `me` |
| `cache` | `list`, `clear`, `stats` |
| `config` | `show`, `path` |
| `schema` | full + per-command (JSON Schema 2020-12) |
| `raw` | positional `<query>`, `--query-file`, `--vars`, `--vars-file` |
| globals | every flag in §4.4, every error code in §6.5 (25 codes — see §9 for the `column_archived` drift to resolve before M0), exit codes 0/1/2/3/130 |

**Out of scope (deferred per §13 "Explicitly deferred"):**

- Any `create`/`move`/`archive`/`delete`/`duplicate`/`upsert` on
  items, boards, columns, groups, workspaces (v0.2).
- `update reply/edit/delete/like/pin/clear-all` (v0.2).
- Boolean filter DSL — only `--where` (operator allowlist) +
  `--filter-json` in v0.1 (v0.2).
- Broad column-type writes — v0.1 allowlist is **only** `status`,
  `text`, `long_text`, `numbers`, `dropdown`, `date`, `people`. All
  other types route through `--set-raw` or return
  `unsupported_column_type`.
- `monday dev …` (v0.3), `monday auth login` / OAuth profiles (v0.3),
  config files (v0.2 stretch / v0.3 firm).
- `monday item watch`, `--concurrency`, asset uploads, shell
  completion (v0.4).
- `monday item recreate-from-archive` (TBD; explicitly **not**
  shipping a misleading `restore`).

## 2. Pre-flight: what's already in place

Working at HEAD (`master`):

- `src/config/load.ts` — env-based config (token + version + URL +
  timeout) with a strict zod schema. Branch-comprehensive unit
  tests in `tests/unit/config.test.ts`. **Bug discovered during
  review:** does not call `dotenv.config()` despite docs saying
  `.env` is loaded. Fix in M0.
- `src/cli/index.ts` — bare commander entry. Wires `--version` and
  `--help`. Catch-all logs to stderr, exits `1`. **Will need
  rework** before v0.1 ships: it doesn't emit the §6 envelope and
  uses exit `1` for everything (should be `3` for config, `2` for
  API, etc.). Also coverage-excluded today — M0 splits the
  testable runner into `src/cli/run.ts` so coverage applies.
- `src/index.ts` — library re-exports (`loadConfig`, `Config`).
- CI runs `typecheck + lint + test:coverage` on Node 22 and 24,
  plus a `--version` / `--help` smoke-test against the compiled
  binary. Coverage threshold: 90% lines/branches/functions/
  statements.
- Tooling: ESM, NodeNext, strictest TS, ESLint
  `strictTypeChecked + stylisticTypeChecked`, vitest 4.

What's **missing** that v0.1 needs:

- `src/api/`, `src/commands/`, `src/utils/`, `src/types/`
  directories (skeleton folders, no files).
- Output-formatting layer (envelope, JSON/table/text/ndjson
  renderers, truncation logic).
- API client wrapper around `@mondaydotcomorg/api`, the error
  mapper, retry/backoff, complexity logging, raw-GraphQL escape.
- Local cache (boards, users) at `$XDG_CACHE_HOME/monday-cli/`.
- Branded ID schemas, request-ID generator, redaction helper.
- A runtime dependency for table rendering (`cli-table3` —
  decision closed in §8) and a UUID source (`crypto.randomUUID`
  from stdlib).
- Integration / E2E test scaffolding (`tests/integration/`,
  `tests/e2e/`, `tests/fixtures/load.ts`, plus a tiny in-process
  HTTP fixture server for E2E — see §5.3).
- A `Transport` interface (`src/api/transport.ts`) so tests can
  swap out `fetch` without bypassing header injection / retry
  layers (see §5.2).
- Executable zod output schemas per command — JSON Schema
  emission via `z.toJSONSchema` (zod v4 native) rather than
  separate static descriptors (decision closed in §8).

## 3. Milestone sequencing

Nine milestones (M5 split per Codex review §3 — column resolution
must precede `--where` filtering, and the original M5 was too big
for one cluster). Each milestone is a coherent commit cluster
(3–10 commits) ending on a green tree with the listed exit
criteria met. Earlier milestones unblock later ones; do not start
M(N) until M(N-1)'s exit criteria are satisfied. Each milestone
updates `docs/output-shapes.md` for any new command shapes it
lands.

Order at a glance: foundations → local commands → API client +
account → broad reads + `board describe` core + column
read-resolver → item reads + filtering + pagination → column
writers + dry-run → item mutations + `update create` →
`board doctor` + `raw` + agent-flow E2E → release prep.

### M0 — Foundations & utilities

Pure infrastructure. No new commands, but everything below
imports from here.

**Deliverables**
- Fix `src/config/load.ts` — actually call `dotenv.config()` so
  `.env` loading matches the documented behaviour. Add a unit
  test that confirms `.env` values are read and that
  `process.env` overrides them.
- `src/cli/run.ts` — testable CLI runner: `run({ argv, env,
  stdout, stderr, stdin, isTTY, clock, transport })`. Catch-all
  converts any error into the §6 envelope and returns the right
  exit code (1 / 2 / 3 / 130). Signal handlers (`SIGINT` →
  abort + flush + 130). Coverage applies here.
- `src/cli/index.ts` shrinks to a 5-line shebang entrypoint that
  calls `run({ argv: process.argv, env: process.env, ... })`.
  Stays coverage-excluded.
- `src/utils/errors.ts` — `ConfigError`, `ApiError`, `UsageError`,
  `CacheError`, all inheriting a base with a typed `code` from the
  v0.1 stable error code set (25 codes — §6.5; pending the
  `column_archived` resolution noted in §9). Each error carries
  `code`, `message`, `httpStatus?`, `mondayCode?`, `requestId?`,
  `retryable`, `retryAfterSeconds?`, `details?`. Distinguishes
  abort *reason*: `timeout` vs `SIGINT` vs explicit cancel — both
  must not collapse onto exit 130.
- `src/utils/redact.ts` — deep-clone-and-redact helper with a
  configurable known-sensitive key list (`apiToken`,
  `Authorization`, `MONDAY_API_TOKEN`, plus a generic
  `*token*`/`*secret*` regex).
- `src/utils/request-id.ts` — `crypto.randomUUID()` wrapper for
  deterministic injection in tests.
- `src/utils/logger.ts` — stderr-only structured logger.
  Levels: `error`, `warn`, `info` (TTY only), `debug` (verbose
  only). Pipes every payload through `redact()` before write.
- `src/utils/output/envelope.ts` — envelope builders:
  - `buildSuccess<T>(data, meta, warnings?)` →
    `{ ok: true, data, meta, warnings }`
  - `buildError(error, meta)` →
    `{ ok: false, error, meta }`
  - `buildDryRun(plannedChanges, meta, warnings?)` →
    `{ ok: true, data: null, meta: { ...meta, dry_run: true },
       planned_changes, warnings }`
  - `buildMutation<T>(data, meta, sideEffects?, warnings?)` adds
    `side_effects` per §6.4.
  - Stable field ordering for snapshot tests.
- `src/utils/output/select.ts` — picks renderer:
  honours `--json` / `--table` / `--output <fmt>`,
  `MONDAY_OUTPUT`, `process.stdout.isTTY`. Returns `'json' |
  'table' | 'text' | 'ndjson'`. Mutually exclusive flag pairs
  raise `usage_error`.
- `src/utils/output/json.ts`, `…/table.ts`, `…/text.ts`,
  `…/ndjson.ts` — renderers. `table` honours `--full`,
  `--width`, `--columns`. JSON / NDJSON never truncate. NDJSON
  collections emit one resource per line; trailer is
  `{"_meta":{...}}` (§6.3).
- `src/types/ids.ts` — branded zod schemas: `BoardIdSchema`,
  `ItemIdSchema`, `ColumnIdSchema`, `GroupIdSchema`, `UserIdSchema`,
  `WorkspaceIdSchema`, `UpdateIdSchema`. Plus `inferred` types.
- `src/types/global-flags.ts` — global-flag zod schema; one
  source of truth shared by every command. Wires runtime
  behaviour for **every** §4.4 flag including `--minimal`,
  `--quiet`, `--verbose`, `--no-color`, `--no-cache`,
  `--timeout`, `--retry`, `--yes`, `--profile`. Each flag's
  effect has a unit test.
- `src/api/transport.ts` — `Transport` interface
  (`request(query, variables, headers, signal): Response`)
  with a `FetchTransport` implementation that owns header
  injection (`Authorization`, `API-Version`) and timeout. The
  `Transport` instance is injected through `run({ transport })`
  so tests swap in fixtures without bypassing headers/retry.
- Per-command exports use **executable zod output schemas**
  (`outputSchema: z.ZodType`), not separate static descriptors.
  `monday schema` (M1+) emits via `z.toJSONSchema(schema)`
  (zod v4 native). Tests validate emitted command output
  against `outputSchema` to catch drift.

**Tests (pyramid)**
- Unit (the bulk): every error class instantiates with the right
  shape; `redact()` strips known-sensitive keys recursively and
  on arrays / nested objects; envelope builders produce stable
  field order; renderer selection covers TTY/non-TTY/explicit-flag
  matrix; table truncation hits the floor (12 chars) and
  ellipsis; NDJSON trailer well-formed.
- No integration / E2E in M0 (no commands yet).

**Exit**
- Coverage ≥ 90% on `src/utils/**` and `src/types/**`.
- `monday --version` / `monday --help` still pass smoke-test.
- A deliberately thrown `ConfigError` from inside `loadConfig`
  produces the §6 error envelope on stderr and exits `3`.

---

### M1 — Local-only commands (`config`, `schema`, `cache`)

The smallest user-visible surface. None of these touch Monday's
API, which makes them perfect for shaking out the envelope, the
schema introspection, and the cache layer before the network
joins the picture.

**Deliverables**
- `src/api/cache.ts` — XDG-aware path resolution, 5-minute TTL,
  mode-`0600` writes, `fs.fstat`-on-read mode check, atomic
  rename for safe replacement. Used in M3+ for board metadata
  and the user directory.
- `src/commands/config/show.ts` — emits resolved config with the
  token redacted (`MONDAY_API_TOKEN: <set>` / `<unset>`,
  **never** the value).
- `src/commands/config/path.ts` — emits the `.env` file path(s)
  considered.
- `src/commands/cache/list.ts`, `…/clear.ts`, `…/stats.ts`.
  `clear --board <bid>` deletes one board entry; without flag,
  clears everything.
- `src/commands/schema/index.ts` — emits the full CLI schema as
  JSON Schema 2020-12. `monday schema <command>` narrows.
  Schema source is a single `commandRegistry` keyed by command
  name; each command exports its zod input schema and a static
  output-shape descriptor.
- `cli/index.ts` registers all of the above.

**Tests (pyramid)**
- Unit: schema emitter produces valid JSON Schema 2020-12 for a
  representative command (use a JSON Schema validator); cache
  read rejects mode-`0640`; cache TTL boundary; config redaction
  retains structure.
- Integration: invoke each command via the in-process command
  invoker, assert envelope shape and stable error codes for
  failure modes (cache directory unwritable → `cache_error`,
  unknown command for `monday schema xyz` → `usage_error`).
- E2E: spawn `dist/cli/index.js`:
  - `monday schema --json` produces valid JSON.
  - `monday config show --json` redacts the token.
  - `monday cache list --json` works on an empty cache.

**Exit**
- All M1 commands listed in `--help`.
- `monday schema` describes itself (introspection round-trip).
- Coverage ≥ 90% on `src/api/cache.ts` and
  `src/commands/{config,cache,schema}/**`.

---

### M2 — API client + `account` commands

First network calls. Validates the entire I/O stack — error
mapping, retries, request_id propagation, complexity surfacing —
against the simplest possible commands.

**Deliverables**
- `src/api/client.ts` — wraps `@mondaydotcomorg/api`'s
  `ApiClient`. Constructor takes `Config + AbortSignal`. Sets
  `Authorization` (no `Bearer ` prefix) and `API-Version`
  (default `2026-01`, override via `--api-version` /
  `MONDAY_API_VERSION`). Exposes typed wrappers for the SDK
  operations v0.1 needs **plus** a `raw<T>(query, vars)`
  escape that goes through `client.request<T>()`. Internal
  callers see `Record<string, unknown>` for vars; the `any`
  in the SDK's `QueryVariables` is contained here.
- `src/api/errors.ts` — maps SDK / GraphQL / network failures
  to the 24 error codes:
  - GraphQL `errors[0].extensions.code` →
    `unauthorized` / `forbidden` / `validation_failed` /
    `complexity_exceeded` / `rate_limited` /
    `daily_limit_exceeded` / `concurrency_exceeded` /
    `ip_rate_limited` / `resource_locked` / `not_found`.
  - HTTP 423 → `resource_locked` (with `Retry-After`).
  - `AbortError` → exit code `130`.
  - `TimeoutError` → `timeout`.
  - `fetch failed` / `EAI_*` → `network_error`.
  - Anything unmapped → `internal_error` with the original
    error as `cause` and a "report this" hint.
- `src/api/retry.ts` — exponential backoff + jitter for codes
  marked `retryable: true` (max retries from `--retry`,
  default 3). Honours `retry_after_seconds` when present.
  **Disables the SDK's own retry** (or asserts it's off) to
  avoid double counting.
- `src/api/complexity.ts` — opt-in `complexity { used,
  remaining, reset_in_seconds }` selection on every query when
  `--verbose`. Surfaces in `meta.complexity`.
- `src/commands/account/whoami.ts` — `me { id, name, email,
  account: { id, name } }`.
- `src/commands/account/info.ts` — `account { id, name, slug,
  plan, country_code, ... }`.
- `src/commands/account/version.ts` — emits
  `{ pinned, available }` (latest from API + the SDK's
  `CURRENT_VERSION`).
- `src/commands/account/complexity.ts` — uses the
  `complexity` query without a payload (cheapest possible).
- `src/utils/output/table.ts` gains an account-specific
  rendering branch (or stays generic).

**Tests (pyramid)**
- Unit: error mapper maps every Monday error code to the right
  CLI code; retry layer respects `retry_after_seconds`; client
  injects `API-Version` + `Authorization` headers; redaction
  test with token in `error.cause`.
- Integration: each `account` command via the injected
  `Transport` with a fixture cassette; asserts request body,
  headers (`Authorization`, `API-Version` injected by
  `FetchTransport`), response → envelope.
- E2E: ships a tiny in-process HTTP fixture server
  (`tests/e2e/fixture-server.ts`) that loads cassettes from
  `tests/fixtures/`. Spawn binary with
  `MONDAY_API_TOKEN=fixture` + `MONDAY_API_URL` pointing at
  the local server; assert exit codes 0 / 2 across
  success / failure paths.

**Exit**
- All four account commands ship with envelope + tests.
- HTTP fixture server runs in CI as part of the E2E suite.
- Every entry in §6.5's error code table has at least one test
  that produces it (most via injected mock errors). See §5.6
  for the exhaustive code-by-code map.
- `--verbose` adds `meta.complexity` to a real query.
- Token never appears in any error message, log, or stdout/stderr
  output. Verified by a hardened redaction regression suite:
  asserts the literal token (`"tok-leakcheck-xxxx"`) is absent
  from every emitted byte across the integration suite **and**
  exercises adversarial shapes — token in `Error.cause`,
  non-enumerable `Error.message`/`stack`, mixed-case header
  keys, arrays, circular references, debug/`--verbose` output,
  redacted log payloads.
- `npm pack` smoke-test runs in CI starting from M2 (lifted
  from M7 per Codex review §7) — the published tarball must
  contain a working bin entry.
- Coverage ≥ 90% on `src/api/**` and `src/commands/account/**`.

---

### M3 — `workspace` + `board` (incl. `describe` core) + `user` + `update` reads

Broadens the read surface. Lands `board describe` core and
the **column read-resolver** here (rather than M5) because
the M4 filter DSL needs to resolve `--where status=Done`
against real column metadata. `board doctor` waits for M6 — it's
a separate diagnostics layer on top of the same metadata.

**Deliverables**
- `src/api/board-metadata.ts` — fetches and caches
  `board describe`-style data: columns (id, type, title,
  archived, description, parsed `settings_str`, per-type
  detail), groups, `hierarchy_type` + `is_leaf` (raw GraphQL
  for the latter two — see §6 risk row). Cached via M1's
  cache layer with the auto-refresh-on-failure semantics
  from `cli-design.md` §8.
- `src/api/columns.ts` — the **read** half of §5.3 column
  resolution: token → column ID. ID-exact → NFC-case-fold
  title → ambiguous error → not-found error. Auto-refreshes
  the board cache once before surfacing `column_not_found`.
  Same module is reused by M4's filter parser and M5a's
  value translator.
- `src/api/resolvers.ts` — name-resolution shared logic:
  - `findOne(scope, name)` — exactly-one match → return;
    multiple → `ambiguous_name` with `details.candidates`;
    zero → `not_found`. `--first` opts into "lowest ID wins".
  - `userByEmail(email)` — checked against the user-directory
    cache (per M1 cache layer); falls back to
    `users(emails: […])`; unknown → `user_not_found`.
- `src/commands/workspace/{list,get,folders}.ts`.
- `src/commands/board/{list,get,find,describe,subscribers,columns,groups}.ts`.
- `src/commands/user/{list,get,me}.ts`. `user me` aliases
  `account whoami`.
- `src/commands/update/{list,get}.ts` — read comments. (The
  `update create` mutation lands in M5b.)

**Tests (pyramid)**
- Unit: resolver branches (one / many / none), `--first`
  semantics; sorted-by-ID stability for collections;
  `board-metadata.ts` cache hit / miss / refresh-on-failure;
  `columns.ts` resolution cases incl. NFC and case-folded
  collisions.
- Integration: each command against fixture cassettes (per
  §5.2 format — ordered, headers, retries); assert
  pagination cursor surfaces in `meta.next_cursor` for
  collections; assert `board describe` returns the full
  shape including raw-GraphQL fields (`hierarchy_type`,
  `is_leaf`).
- E2E: spawn binary for `monday board describe <bid> --json`,
  `monday board list --json`, `monday user me --json`,
  `monday update list <iid> --json`.

**Exit**
- 14 commands shipped, each with unit + integration; at
  least one E2E per noun.
- `find` works for boards (the only `find` in M3); errors
  cleanly on ambiguous matches.
- Cross-noun output consistency check: `monday workspace get`,
  `monday board get`, `monday user get` use the same `meta`
  skeleton.
- `monday board describe <bid>` already includes
  `example_set` per writable column — agents can use this
  alone to write to every M5b-writable column on the board.
- Coverage ≥ 90% on touched files.

---

### M4 — `item` reads + filtering + pagination

The heaviest read wave. Filter DSL leans on M3's column
read-resolver; pagination contract gets exercised here.

**Deliverables**
- `src/api/filters.ts` — parses `--where <token><op><val>`.
  Operator allowlist: `=`, `!=`, `~=`, `<`, `<=`, `>`, `>=`,
  `is_empty`, `is_not_empty`. Token resolves via
  `src/api/columns.ts` (M3) — supports column ID, title,
  `id:`/`title:` prefixes, and the `me` sugar for people
  columns. Emits a `query_params.rules` object suitable for
  `items_page`. `--filter-json` is passed through verbatim.
  Multiple `--where` flags are AND'd. Edge cases the unit
  tests must cover: column titles containing operators
  (`Plan A=B`), Unicode normalisation collisions,
  `due:is_empty` (no value), `me` in people-column `--where`,
  column titles with leading/trailing whitespace.
- `src/api/pagination.ts` — `items_page` initial call →
  `next_items_page` walker. `--all` exhausts. `--limit N`
  caps total. NDJSON streaming emits per-item, trailer with
  cursor + has_more. **Stale cursor (60 min) → fail-fast
  with `stale_cursor` error**, no silent re-issue. Cursor
  age tracked via the injected clock so tests can replay
  expiry without wall-clock waits.
- `src/api/sort.ts` — per-page sort by ID asc (deterministic
  per §3.1 #8). Documents that cross-page order is Monday's
  unless `--filter-json` includes an `order_by`.
- `src/commands/item/list.ts` — `--board <bid>` required,
  optional `--group`, `--where` (repeatable), `--filter-json`,
  `--state active|archived|all`, `--all`, `--limit`.
- `src/commands/item/get.ts` — `items(ids: [<iid>])`; full
  column values per §6.2.
- `src/commands/item/find.ts` — `--board <bid>` required;
  uses `findOne` resolver against item names.
- `src/commands/item/search.ts` —
  `items_page_by_column_values` with the same `--where`
  parser.
- `src/commands/item/subitems.ts` — list children.

**Tests (pyramid)**
- Unit: filter parser covers every operator × every column
  type's value coercion; adversarial token cases (operators
  in titles, Unicode, etc.); pagination walker handles
  `next_cursor: null` (terminate), `has_more: true`
  (continue), `--limit` short-circuit; stale-cursor
  detection at the boundary; per-page sort stability with
  Unicode names.
- Integration: end-to-end
  `monday item list --all --output ndjson` against a
  multi-page cassette; assertion on item count and trailer;
  stale-cursor cassette produces exit `2` and the documented
  error envelope.
- E2E: pipe `monday item list --json | jq '.data | length'`
  and verify the count matches the cassette.

**Exit**
- All 5 item-read commands ship.
- NDJSON streaming verified (no envelope-on-stdout, trailer
  on last line).
- Stale-cursor cassette in `tests/fixtures/`.
- Filter parser regression tests cover every cli-design.md
  §5.5 v0.1 operator + the adversarial cases above.
- Coverage ≥ 90% on `src/api/{filters,pagination,sort}.ts` and
  `src/commands/item/{list,get,find,search,subitems}.ts`.

---

### M5a — Column-value writers + dry-run engine

The hardest pure-logic surface. No new commands ship in M5a;
this milestone produces the modules M5b consumes. Splitting
M5 (Codex review §3) keeps M5b a small command-only commit
cluster.

**Deliverables**
- `src/api/column-values.ts` — the **write** half of §5.3:
  - **Type-specific translation** for the 7 allowlisted
    types (per cli-design §5.3.3):
    - `text` / `long_text` / `numbers` → simple-string
      payload.
    - `status` — label first, integer index fallback; emits
      `{"label":...}` or `{"index":...}`.
    - `dropdown` — comma-split labels → `{"labels":[...]}`;
      numeric IDs → `{"ids":[...]}`.
    - `date` — ISO date / ISO datetime / relative tokens
      (`today`, `tomorrow`, `+3d`, `-1w`, `+2h`) resolved
      against the active **profile timezone**
      (`MONDAY_TIMEZONE` or system tz). Resolution uses the
      injected clock from M0.
    - `people` — comma-split emails / `me` → user-directory
      lookup → `{"personsAndTeams":[...]}`. Unknown →
      `user_not_found`.
  - **Monday `JSON` scalar discipline.** Settled wrapper
    rule: every payload is a plain object passed as a
    GraphQL `JSON` scalar; the SDK / fetch layer is
    responsible for stringification. One unit test fixture
    captures the exact wire shape per type so v0.2's bulk
    surface inherits the rule.
  - **Mutation selection.** Single value of a "simple" type
    (`text`/`long_text`/`numbers`) →
    `change_simple_column_value`. Single value of a "rich"
    type → `change_column_value`. Multi-`--set` against the
    same item → `change_multiple_column_values` (atomic).
  - **`unsupported_column_type` error** carries
    `column_id`, `type`, the literal Monday-shape JSON the
    type expects, and a `--set-raw` example.
  - **`--set-raw <col>=<json>`** escape hatch using
    `change_column_value`.
- `src/api/dry-run.ts` — runs the read-side resolution
  (board lookup via M3's metadata, column resolution via
  M3's `columns.ts`, value translation via the new
  `column-values.ts`, user-email lookup), assembles
  `planned_changes` per §6.4. Bulk via `--where` produces
  an N-element `planned_changes`. **Always all-or-nothing**:
  if read-side resolution fails for any planned change, the
  whole command fails with `data: null` and an error.

**Tests (pyramid)**
- Unit: type translation per allowlisted column with
  adversarial inputs — Unicode in titles, NFC-distinct
  identical visual characters, ambiguous-title error path,
  `me` resolution, relative-date resolution against a
  frozen clock and injected timezone (London + Tokyo +
  UTC); date-DST boundary; the `JSON` scalar wire shape per
  type captured as a fixture.
- Integration: dry-run engine fed each combination of
  resolution failure (`ambiguous_column`,
  `column_not_found`, `user_not_found`,
  `unsupported_column_type`); for each case, **zero**
  mutation requests reach the mock transport.

**Exit**
- All 7 v0.1-allowlisted column types translate correctly
  (covered by unit + the wire-shape snapshots).
- Dry-run engine produces `planned_changes` matching §6.4
  byte-for-byte (snapshot test).
- Coverage ≥ 90% on `src/api/{column-values,dry-run}.ts`.

---

### M5b — Item mutations + `update create`

Lands every v0.1 mutation. Small commit cluster — the work
is in M5a. Includes the bulk `item update --where ...` path
because that's where `confirmation_required` (without
`--yes`) gets exercised.

**Deliverables**
- `src/commands/item/set.ts` — single-column write.
- `src/commands/item/clear.ts` — clear via the type's
  documented "clear" payload.
- `src/commands/item/update.ts` — multi-column atomic update;
  also accepts `--name <n>` to rename. Supports the bulk
  shape `--where <token><op><val>` (no positional `<iid>`,
  resolves to multiple items via `items_page`); bulk
  without `--yes` returns `confirmation_required`. Bulk
  with `--dry-run` returns the full N-element
  `planned_changes`.
- `src/commands/update/create.ts` — accepts `--body <md>`
  or `--body-file <path>` (or `-` for stdin). Markdown
  rendered to HTML by Monday (CLI passes verbatim — the
  HTML mismatch risk is documented in §6). **Supports
  `--dry-run`** even though it's a non-idempotent
  comment-create — the dry-run envelope reports "would
  post comment X to item Y" without sending.

**Tests (pyramid)**
- Unit: option-parsing branches per command; bulk path
  filters out items unmatched.
- Integration: `monday item set` happy path + every error in
  the resolution chain (`column_not_found` after refresh,
  `ambiguous_column`, `user_not_found`,
  `unsupported_column_type`, `validation_failed` from
  Monday); `--dry-run` returns `planned_changes` and emits
  **zero** mutation requests; multi-`--set` bundles into
  one `change_multiple_column_values`; cache-stale path
  emits the `stale_cache_refreshed` warning;
  `update create --body-file -` reads from stdin;
  `item update --where status=Backlog` without `--yes`
  returns `confirmation_required`.
- E2E: spawn binary; assert
  `monday item set 5001 status=Done --board 12345 --dry-run --json`
  envelope matches the contract sample byte-for-byte
  (snapshot).

**Exit**
- All M5b commands ship.
- Auto-refresh-on-failure path covered (§8) — verified by a
  cassette that returns `column_not_found` then succeeds on
  refresh.
- Token never leaks in mutation error envelopes (regression
  test from M2 still green with mutation paths now exercised).
- `confirmation_required` test for bulk `item update`
  without `--yes`.
- `update create --dry-run` snapshot test.
- Coverage ≥ 90% on
  `src/commands/{item/{set,clear,update},update/create}.ts`.

---

### M6 — `board doctor` + `raw` + agent-flow E2E

Ships the diagnostics layer and the GraphQL escape hatch.
Validates the whole stack with the v0.1 agent flow.

**Deliverables**
- `src/commands/board/doctor.ts` — diagnostics per §11.2:
  duplicate column titles → would cause `ambiguous_column`;
  columns of types not in v0.1 allowlist → flag for
  `--set-raw`; stale cache vs live; broken
  `board_relation` targets.
- `src/commands/raw/index.ts` — accepts `<query>` positional,
  `--query-file <path>` (or `-`), `--vars <json>`,
  `--vars-file <path>` (or `-`). Result is wrapped in an
  envelope; raw GraphQL errors map per `api/errors.ts`.
- `tests/e2e/agent-flow.test.ts` — replays the **v0.1
  fallback path** from `examples.md` §1's callout (since
  `monday dev …` is v0.3): `monday item list --board <bid>
  --where status=Backlog --where owner=me` →
  `monday item set <iid> status='Working on it'` →
  `monday item set <iid> status=Done` →
  `monday update create <iid> --body "..."`. Asserts the
  contract holds end-to-end across four binary spawns.

**Tests (pyramid)**
- Unit: `doctor` rule firing per category.
- Integration: `monday board doctor` flags each diagnostic
  class with a cassette; `monday raw '{ me { id } }'`.
- E2E: agent flow above.

**Exit**
- Agent-flow E2E green using v0.1 commands only.
- `monday raw` round-trips the example query against the
  fixture server.
- Coverage ≥ 90% on touched files.

---

### M7 — Hardening, polish, release

The mile from "all features land" to "ready to publish".

**Deliverables**
- `docs/output-shapes.md` — per-command output shape
  reference, generated where possible from the
  `commandRegistry` exports + handwritten where the schema
  is too dynamic (column values).
- README usage section — auth quickstart, agent quickstart,
  one example per noun pointing at `examples.md`.
- CHANGELOG.md — `0.1.0` entry summarising the contract.
- Version bump `package.json` → `0.1.0`.
- `npm audit` clean — no high/critical advisories.
- `monday --version` reports `0.1.0`.
- CI green on Node 22 + 24.
- Final coverage report attached as artefact; numbers
  documented in CHANGELOG.

**Tests (pyramid)**
- Add the E2E suite that runs **on the published tarball**
  via `npm pack` + `node` against the extracted `dist/`. This
  catches `package.json` `files` / `bin` field regressions.
- Add a snapshot-test sweep over every command's
  `--json` envelope on a representative fixture so v0.2
  changes that drift the schema fail loudly.

**Exit (also the v0.1-as-a-whole exit checklist below)**

## 4. Cross-cutting "definition of done" per command

Every command, regardless of milestone, must satisfy:

1. **Source.** One file in `src/commands/<group>/<verb>.ts` for
   noun-verb commands; `src/commands/<noun>/index.ts` for
   noun-only commands (`schema`, `raw`). Imports `api/`, never
   the SDK directly. Top-of-file comment names the GraphQL
   operation(s) it calls (per `cli.md`).
2. **Schemas.** Exports an `inputSchema` (zod) for argv parsing
   **and** an `outputSchema` (zod) describing the `data`
   payload. The same `outputSchema` powers `monday schema`
   (via `z.toJSONSchema`) and the envelope contract test —
   no separate static descriptor.
3. **Args parsed via zod before any API call.** Commander
   delivers `string`/`string[]`/`boolean`; the zod schema
   coerces and refines at the command's edge. Internal code
   sees the validated type.
4. **Help text.** `description()` + at least one usage example
   in `addHelpText('after', ...)` (per `cli.md`).
5. **Stdin contract** documented if the command accepts stdin
   (e.g. `--body-file -`, ID-from-stdin via `xargs` pattern).
6. **Idempotency.** Documented: yes/no in the command
   description. **Every mutation supports `--dry-run`** —
   including non-idempotent ones like `update create`, where
   the dry-run reports "would post comment X to item Y".
7. **Tests.**
   - Unit covering every branch of the command's pure logic.
   - Integration covering happy path + every error code the
     command can produce + format variations + envelope
     contract test (per §5.2).
   - E2E if the command is non-trivial (any mutation, any
     paginated read, any command with stdin or file inputs).
8. **Output.** Every applicable format produces sensible
   output: `json` and `table` always; `text` for
   single-resource commands; `ndjson` for collections that
   benefit from streaming (`item list --all`). Non-applicable
   formats fail with `usage_error` (e.g. `--ndjson` against a
   single-resource command). Table truncation is honoured;
   JSON / NDJSON never truncate. Snapshot-tested.
9. **Stdout/stderr discipline.** Result on stdout (or
   nothing-on-stdout for an error). Errors, warnings (in TTY
   mode), debug logs all go to stderr. The envelope contract
   test asserts no stdout pollution on failure.
10. **Errors.** Every thrown error inherits a typed class with
    a `code` from §6.5. No plain `throw new Error(...)` in
    `commands/`.
11. **No token leakage.** Verified by the redaction regression
    suite (M2).

## 5. Testing strategy — the pyramid, made explicit

The project's testing rule is "every branch covered" (`testing.md`),
not just "hit the 90% threshold". The pyramid below is how we
distribute that effort across layers.

```
              ┌────────────┐
              │    E2E     │   ~5–10% of tests
              │ (binary)   │   broad smoke + 1 agent flow
              └────────────┘
           ┌──────────────────┐
           │   Integration    │   ~20–30%
           │ (mock transport) │   one per command, all error codes
           └──────────────────┘
        ┌────────────────────────┐
        │         Unit           │   ~60–75%
        │   (pure functions)     │   every branch, every error
        └────────────────────────┘
```

### 5.1 Unit (`tests/unit/**`)

- **Scope.** Pure logic only. `src/utils/**`, `src/types/**`,
  `src/api/{errors,filters,column-values,dry-run,...}.ts`,
  per-command argument parsers and formatters. No network. No
  fs writes outside `os.tmpdir`.
- **Coverage rule.** Every branch named in `testing.md` —
  happy / edge / error / format variations. The 90% v8
  threshold is a floor; the goal is "every named branch hit
  by an `it` block".
- **Mocking.** Vitest `vi.fn()` for collaborators *only* when
  the test's purpose is to verify the call. Prefer real
  inputs and assert real outputs.
- **Naming.** `<file-under-test>.test.ts`, colocated under
  `tests/unit/<mirror-of-src-path>/`. `describe` blocks name
  the function/class; `it` blocks complete "it ___".

### 5.2 Integration (`tests/integration/**`)

- **Scope.** A whole command, end-to-end through the
  `commands/` → `api/` → injected `Transport` stack. Every
  command in v0.1 has at least one integration test.
- **Mocking strategy.** Inject a `FixtureTransport` via
  `run({ transport })` (M0). The transport implements the
  same interface as `FetchTransport` so header injection,
  retry, abort handling, and error mapping all run unchanged
  — the only difference is where the bytes come from. **Do
  not** spy on internal SDK methods or monkey-patch
  `commands/*` helpers; that bypasses the layers we want to
  exercise.
- **Cassette format** (per Codex review §2 — single
  request/response pairs aren't enough). Each fixture is an
  ordered list of interactions:

  ```ts
  interface Cassette {
    interactions: Interaction[];
  }

  interface Interaction {
    operation_name?: string;        // fast match key
    match_query?: RegExp | string;  // optional substring/regex
    match_variables?: Partial<Vars>;
    expect_headers?: Record<string, string | RegExp>;
    response?: { data?: unknown; errors?: GraphQLError[] };
    http_status?: number;           // for non-200 (e.g. 423)
    delay_ms?: number;              // for timeout tests
    repeat?: number;                // for paginated walks
  }
  ```

  Stored at `tests/fixtures/<group>/<verb>/<case>.json`.
  Loader in `tests/fixtures/load.ts`. Multi-page walks and
  retry sequences are first-class.
- **Required cases per command.** Happy path + every error
  code reachable from the command + each `--output`
  variation that affects rendering + each significant flag
  combination (e.g. `--all`, `--limit`, `--filter-json`,
  `--dry-run`, `--no-cache`).
- **Envelope contract tests.** A shared test helper
  validates *every* emitted JSON envelope across the suite:
  `meta` skeleton present, `meta.schema_version === "1"`,
  errors only on stderr, no stdout pollution on failure,
  emitted output validates against the command's
  `outputSchema`. Every command's integration test runs
  through this helper before its case-specific assertions.
- **Forbidden.** Real network. Spawning the compiled binary.

### 5.3 E2E (`tests/e2e/**`)

- **Scope.** Spawn `node dist/cli/index.js …` via
  `child_process.spawn`. Network mocked by an in-process
  HTTP fixture server (`tests/e2e/fixture-server.ts`,
  shipped in M2) listening on a random port; spawn with
  `MONDAY_API_URL=http://127.0.0.1:<port>`. The server
  loads the same cassettes as the integration suite, so
  fixtures are not duplicated. Assert on `stdout`,
  `stderr`, exit code.
- **Required cases.**
  - Per noun: at least one happy-path command.
  - Per exit code: 0 (success), 1 (usage — bad flag),
    2 (API — fixture rejection), 3 (config — missing token),
    130 (SIGINT, via spawn-and-kill).
  - Per output format: `--json`, `--table` (force TTY),
    `--ndjson` for `monday item list --all`.
  - The agent flow from `examples.md` §1 (M6).
- **`RUN_LIVE_TESTS=1`** opt-in tier hits the real Monday
  API against a dedicated test workspace. Not run in CI by
  default. Each live test must clean up everything it creates
  (`try { ... } finally { cleanup() }`).

### 5.4 Coverage targets

- `vitest run --coverage` includes unit + integration. E2E
  spawns a separate process so v8 doesn't see it; we accept
  that.
- Threshold floor (configured): **90% lines, branches,
  functions, statements**. Raise as the codebase grows; do
  not lower.
- The threshold is necessary but not sufficient. Per-command
  PRs include a manual branch-coverage checklist (happy /
  edge / error / format) that reviewers can audit.
- `tests/fixtures/` is excluded from coverage.

### 5.5 Anti-patterns to avoid

- **Internal-call snapshots.** Don't snapshot the mocked
  transport call argument unless the test's whole purpose is
  to verify the GraphQL request body — and then do it
  explicitly with `toMatchObject`, not a freeform snapshot.
- **Shared state.** Each `it` is independent. Setup in
  `beforeEach` (not `beforeAll` unless mutation-free).
- **Skipping the unhappy path.** Every command's integration
  test covers at least one error path. PRs that skip
  unhappy-path tests get sent back.

### 5.6 Error-code coverage map (§6.5 → milestone)

Every code in `cli-design.md` §6.5 must be producible **and**
have a test that produces it. Codex review §4 grouped these by
how they're exercised; the table below assigns each to its
milestone so M0–M7 collectively own the full set.

| Code | Milestone | How produced |
|------|-----------|--------------|
| `usage_error` | M0 | Bad-flag unit test on `cli/run.ts` |
| `confirmation_required` | M5b | Bulk `item update --where ...` without `--yes` |
| `not_found` | M3 | `monday board get 0` cassette returning `{boards: []}` |
| `ambiguous_name` | M3 | `monday board find <name>` with 2-board cassette |
| `ambiguous_column` | M5a | Two columns with NFC-equal titles in `board describe` cassette |
| `column_not_found` | M5a | `--set` against unknown token + cache-refresh cassette returning unchanged metadata |
| `user_not_found` | M5a | `monday item set …person=ghost@example.com` with `users(emails:)` returning empty |
| `unsupported_column_type` | M5a | `monday item set <iid> file_field=... --board <bid>` with `file` column metadata |
| `unauthorized` | M2 | Cassette responding `401` |
| `forbidden` | M2 | Cassette responding `403` / Monday `Permission denied` |
| `rate_limited` | M2 | Cassette with Monday rate-limit error + `retry_in_seconds` |
| `complexity_exceeded` | M2 | Cassette with `ComplexityException` |
| `daily_limit_exceeded` | M2 | Cassette with `DailyLimitExceeded` |
| `concurrency_exceeded` | M2 | Cassette with concurrency error |
| `ip_rate_limited` | M2 | Cassette with `IP_RATE_LIMIT_EXCEEDED` |
| `resource_locked` | M2 | Cassette `http_status: 423` + `Retry-After` |
| `validation_failed` | M5b | Cassette with bad-status-label rejection |
| `stale_cursor` | M4 | Multi-page cassette where page N replies with cursor-expired error; injected clock advances `cursor_age_seconds > 3600` |
| `config_error` | M0 | Missing `MONDAY_API_TOKEN` fed to `run({ env: {} })` |
| `cache_error` | M1 | `EACCES`-injected cache write |
| `network_error` | M2 | Fixture server refuses connection |
| `timeout` | M2 | Cassette with `delay_ms: 9999` against `--timeout 100` |
| `internal_error` | M0 | Top-level mapper unit test with an unrecognised error type |
| `dev_not_configured` | **deferred** | `dev` namespace is v0.3 — covered there |
| `dev_board_misconfigured` | **deferred** | Same as above |
| `column_archived` (drift) | **see §9** | Pending resolution before M0 |

## 6. Risk register

Risks ranked by impact × likelihood (top = most pressing).
Codex review §3 prompted the re-rank — token leakage to the
top, SDK retry downgraded, three new risks added.

| Risk | Impact | Mitigation |
|------|--------|------------|
| **Token leakage** — token in `Error.cause`, non-enumerable `Error.message`/`stack`, header casing variants, debug logs, retried-request URL. | **High** | All output paths funnel through `redact()`. Hardened regression suite (M2 exit) walks adversarial shapes: `cause` chain, mixed-case header keys, arrays, circular refs, `--verbose` debug output. Asserts literal token string (`"tok-leakcheck-xxxx"`) absent from every emitted byte across integration + E2E suites. |
| **SDK ↔ API drift + tiny typed surface** — SDK 14.0.0's generated operations cover only a handful of queries/mutations; `BatteryValue`, `hierarchy_type`, `is_leaf`, `capabilities` are unmodelled, and most v0.1 commands need raw GraphQL with zod response parsers. | **High** | Use `client.raw<T>(query, vars)` for unmodelled fields; **every raw response goes through a zod parser** before crossing into `commands/`; per-command response schemas live alongside the command's `outputSchema`. If Monday changes shape mid-version, the parser fails closed → `internal_error` with the raw payload in `details`. |
| **Output schema drift** — command authors update returned data and forget to update the descriptor; agents key off a contract that no longer matches. | **High** | Output descriptors are **executable zod schemas**, not separate static metadata. Envelope contract test (§5.2) validates emitted output against `outputSchema` for every integration test. JSON Schema emission via `z.toJSONSchema(schema)` is automatic — no hand-maintained second source. |
| **Abort handling — timeout vs SIGINT vs explicit cancel** — all three can collapse onto exit 130 unless distinguished. | Medium | The wrapper `AbortController` carries a `reason: 'timeout' \| 'sigint' \| 'cancel'`; `api/errors.ts` maps each to a distinct error code (`timeout` → exit 2; `sigint` → exit 130; `cancel` → caller-defined). Tests cover all three signal paths. |
| **Pagination cursor races in tests** — `--all` flows can't replay wall-clock cursor expiry. | Medium | Cassette format supports multi-page `repeat` and an explicit `stale-cursor` error response. Cursor age uses the **injected clock** from M0 — tests advance it past 3600s without sleeping. |
| **Cache file permissions** — group/world-readable cache betrays workspace structure. | Medium | All cache writes mode `0600`; reads `fstat`-check and refuse non-`0600`; coverage test for the refusal path. |
| **Cursor 60-min lifetime in long agent runs** — agents pause and resume, hitting `stale_cursor` regularly. | Medium | Document fail-fast in `cli-design.md` §5.6 (already done); `examples.md` §5 shows recovery patterns; revisit a deterministic resume token in v0.2. |
| **`--where` parser fragility** — column titles containing operators (`=`, `<`, `~`), Unicode normalisation edge cases, the `me` sugar interacting with title-to-ID resolution, `:is_empty` no-value form. | Medium | M4's filter parser has a dedicated unit-test sweep covering each adversarial case; the parser is intentionally restrictive (the boolean DSL waits for v0.2 with real fixtures). |
| **TTY detection in tests** — `process.stdout.isTTY` varies by environment. | Low | Inject the writer/TTY-flag through `run()` rather than reading `process.stdout` directly; tests force the value. |
| **Date timezone edge cases** — `+3d` at a DST boundary in `Europe/London`, integer overflow on `+10000d`, leap day. | Low | Frozen clock + injected timezone in tests; explicit DST-boundary case; reject input where the resolved date doesn't round-trip; cap relative offsets at a documented maximum (e.g. ±5 years). |
| **Monday `JSON` scalar payload shape** — object vs JSON-stringified string varies across mutations; getting it wrong ships silent corruption. | Low | One settled wrapper rule in `api/column-values.ts`: payloads are plain objects passed as a `JSON` scalar; the SDK / fetch layer stringifies. Wire-shape snapshot test per type (M5a). |
| **SDK retry double-counting** — *Originally* flagged as Medium; downgraded after Codex review §3 — `graphql-request@6.1.0` exposes no built-in retry layer. | Low | No "disable" needed. Regression test asserts exactly N transport attempts for a `retryable: true` failure (catches future SDK changes that introduce a retry layer). |
| **Complexity-budget rate-limiting in CI** — fixture tests don't hit it, but `RUN_LIVE_TESTS` could. | Low | Live tests gated; default CI never makes a real call. |
| **Markdown body in `update create`** — Monday's HTML renderer differs from common markdown flavours. | Low | Pass through verbatim; document caveats in `update create --help`; defer richer body handling to v0.2. |
| **Branded ID misuse** — accidentally passing a `BoardId` where an `ItemId` is wanted. | Low | Brands enforce at compile time; `noUncheckedIndexedAccess` and `verbatimModuleSyntax` already on. |

## 7. v0.1 release exit checklist

The plan is "done" when **all** of the following pass:

- [ ] Every command in §1's in-scope table is implemented and
      registered in `cli/run.ts`.
- [ ] Every command satisfies the §4 "definition of done"
      checklist.
- [ ] Every v0.1-active error code in `cli-design.md` §6.5 is
      producible and has a test (per §5.6 map). The two `dev_*`
      codes are deferred with the `dev` namespace; the
      `column_archived` drift is resolved before M0 (§9).
- [ ] `npm run typecheck && npm run lint && npm test` is green.
- [ ] Coverage ≥ 90% across lines/branches/functions/
      statements (vitest threshold passes).
- [ ] CI green on Node 22 and 24.
- [ ] `npm audit` reports no high/critical advisories.
- [ ] `monday --version` reports `0.1.0`.
- [ ] `monday schema --json` validates as JSON Schema 2020-12.
- [ ] `monday schema --json` describes every shipped command,
      including its input flags and output shape.
- [ ] `monday config show --json` redacts the token.
- [ ] No emitted byte (across the integration suite) contains
      the literal `MONDAY_API_TOKEN` value.
- [ ] Agent-flow E2E (the v0.1 fallback path from
      `examples.md` §1's callout: `monday item list … --where …`
      → `monday item set … status='Working on it'` →
      `monday item set … status=Done` →
      `monday update create …`) green against fixtures.
- [ ] Envelope contract test (§5.2) green for every command —
      every emitted JSON validates against its `outputSchema`,
      every `meta` skeleton present, no stdout pollution on
      failure.
- [ ] `tests/e2e/published-tarball.test.ts` passes (M7) — i.e.
      `npm pack` produces a working artefact.
- [ ] `docs/cli-design.md` "Contract at a glance" section in
      CLAUDE.md still matches shipped behaviour.
- [ ] `docs/output-shapes.md` documents every command's `data`
      shape.
- [ ] CHANGELOG.md `0.1.0` entry written.
- [ ] README's quickstart works for a new user with only a
      Monday API token.

## 8. Decisions (open questions closed)

These were open in the first draft of this plan; the Codex
review §7 surfaced enough evidence to close all five. The
decisions are recorded here so M0 doesn't relitigate them.

1. **Table renderer: `cli-table3`.** Small, well-tested
   dependency; less renderer code to own than the
   alternative. Adding it in M0.
2. **JSON Schema emission: zod v4 native `z.toJSONSchema()`.**
   The installed `zod` version exposes it. Tests verify
   brands, refinements, and coercions round-trip via the
   emitter (a known sharp edge with zod-to-JSON Schema
   libraries).
3. **Cassette format: hand-written, ordered multi-interaction.**
   See §5.2 for the schema. Single request/response pairs are
   too thin for paginated walks and retry sequences; the
   ordered shape is first-class from the start.
4. **Bin entry: keep `dist/cli/index.js` as `bin`.** TypeScript
   preserves the shebang. Move the `npm pack` round-trip
   smoke-test from M7 to M2 so the bundling surface gets
   exercised early — catches `package.json` `files`/`bin`
   regressions before they pile up.
5. **`--profile` in v0.1: accept absent or `default`.** Anything
   else returns `usage_error` with a structured `details.hint`
   pointing at v0.3. `MONDAY_PROFILE` is treated identically
   (env / flag must agree).

## 9. Preconditions for M0 — `cli-design.md` drift to resolve

The Codex review surfaced one contract gap that predates this
plan: `cli-design.md` §5.3.6 mentions a `column_archived`
error returned for mutations against archived columns, but
the §6.5 stable-error-code table doesn't list it. The plan
inherited the inconsistency.

There are two clean fixes. Either is acceptable; the plan
doesn't preempt the call:

- **(a) Add `column_archived` to §6.5** as a 26th code with
  `Origin: --set against an archived column on a board`,
  `Retryable: No`. This is the smaller change and makes the
  case a first-class part of the contract.
- **(b) Drop `column_archived` from §5.3.6** and route the
  case through `validation_failed` (Monday already rejects
  writes to archived columns at the API). This consolidates
  but loses the structured signal an agent could key off.

**Lean: (a).** Archived-column writes are a recoverable
agent error (re-resolve via `board describe` after listing
archived columns), distinct from a generic validation
failure. A dedicated code is the right ergonomic.

**Action.** Resolve before M0 starts. The fix is a small
PR against `cli-design.md` (§6.5 table + the "Stable error
codes (v0.1)" count below it); after merge, this plan's §1
scope and §5.6 coverage map are updated to match. Until
resolved, the plan refers to "25 stable codes" with the
26th flagged here.

## 10. Codex review log

The first draft of this plan (commit pre-applying review)
was reviewed by Codex (`gpt-5.5`, read-only). All ten
evaluation asks were answered; the substantive findings are
folded into the sections above. For traceability:

| Codex finding | Where applied |
|---------------|---------------|
| M4 filtering depends on M5 column resolution | §3 milestones — column read-resolver moved to M3 |
| `board describe` core needed before mutations | §3 milestones — `board describe` lands in M3 |
| M5 too big — split | §3 milestones — split into M5a (writers + dry-run) + M5b (mutation commands) |
| Error-code count drift (24 vs 25) + `column_archived` plan-only | §1, §5.6, §9 |
| Transport interface, not `vi.spyOn` | §2 pre-flight, §5.2 |
| HTTP fixture server for E2E | §2 pre-flight, §5.3 (lifted to M2) |
| Cassette format too thin | §5.2 — ordered interactions schema |
| Envelope contract tests | §5.2, §4 DoD #7 |
| Token leakage to top of risks | §6 |
| SDK retry double-counting overstated | §6 — downgraded |
| Missing risks: tiny SDK / abort handling / output drift | §6 |
| Output schemas should be executable zod | §2, §4 DoD #2, §6 |
| `src/cli/index.ts` coverage-excluded | §2 pre-flight, M0 |
| `loadConfig` doesn't call dotenv | §2 pre-flight, M0 |
| Global flag plumbing missing | M0 deliverables |
| Cache fallback semantics | M1 (cache_error non-fatal) |
| Bulk mutation confirmation | M5b — bulk `item update --where` |
| `raw` `--query` typo | §1 — fixed to positional `<query>` |
| Agent-flow E2E uses v0.3 `dev` | M6 — switched to v0.1 fallback path |

**Findings not applied** (with reasoning):

- *Property-test framework*. Codex flagged it as optional;
  table-driven adversarial cases give most of the benefit
  for v0.1's surface. Revisit in v0.2 if the parser surface
  grows.
- *Move `board describe` into M2*. Codex suggested; the plan
  puts it in M3 alongside the rest of the board read
  surface, which keeps M2 focused on validating the API
  client + envelope on the simplest possible commands. M3
  is still earlier than the original M5/M6 placement.
