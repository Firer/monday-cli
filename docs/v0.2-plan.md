# v0.2 implementation plan

> **Status:** plan, not contract. The binding contract is
> [`cli-design.md`](./cli-design.md). This document sequences the work
> needed to ship v0.2 of that contract; it doesn't redefine it. If a
> conflict arises, `cli-design.md` wins and this plan is updated.

## 1. Scope summary

v0.2 is "**the mutating core — agents can drive a backlog**". v0.1
gave agents the read surface plus four narrow mutation verbs (`item
set/clear/update`, `update create`); v0.2 lifts the write surface to
parity with the read surface so an agent can create items, move them
between groups and boards, archive/delete them, post and edit
comment threads, and shape the boards themselves. The friendly
column-value translator gains six more types, plus the long-deferred
`--set-raw` escape hatch.

**In scope (from `cli-design.md` §13 v0.2):**

| Group | Commands |
|-------|----------|
| **column writers** | `--set-raw <col>=<json>` escape hatch + friendly translators for `link`, `email`, `phone`, `tags`†, `board_relation`†, `dependency`† (†tentative — may slip to v0.3 if fixture work surfaces blocking design cost; see §6 risk register) |
| `item` | `create`, `archive`, `delete`, `duplicate`, `move`, `upsert`, bulk `clear --where ... <col>` |
| `update` | `reply`, `edit`, `delete`, `like`, `unlike`, `pin`, `unpin`, `clear-all`; `list --board <bid>`; `list <iid> --with-replies` |
| `workspace` | `create`, `update`, `delete`, `add-users`, `remove-users` |
| `board` | `create`, `update`, `archive`, `delete`, `duplicate`, `add-users` |
| `board` (structure) | `column-create`, `column-update`, `column-delete`, `group-create`, `group-update`, `group-archive`, `group-duplicate`, `group-delete` |
| polish | NDJSON streaming for `item search` and `update list`; refreshed envelope-snapshot suite; `0.2.0` release artefacts |

**Universal partial-success rule (cli-design §6.1 compliant).** Multi-
target verbs (`workspace add-users` / `remove-users`,
`board add-users`, `update clear-all`) emit one **success envelope**
(`ok: true`) with per-target results in `data.results: [...]`. Each
record carries its own `{<target_id_field>, ok: true | false,
error?}` where the field name is verb-specific — `user_id` for
add-users / remove-users, `update_id` for `clear-all`, `<verb>_id`
generally. The shared envelope-builder (`buildPartialSuccessMutation`)
takes the field name as a parameter; the per-record shape is
otherwise uniform (same `ok` and optional `error: { code, message }`).
The envelope's top-level `error` slot is reserved for whole-call
failure (couldn't reach the API, couldn't resolve any target before
the loop began, an unrecoverable validation error). All-failed-but-
each-call-attempted is still `ok: true` — the agent reads
`data.results` to determine outcomes. This avoids a §6.1 contract
revision (Codex round-1 P1).

**Out of scope (deferred per §13 "Explicitly deferred"):**

- `monday dev …` — Monday Dev workflow shortcuts (v0.3).
- `monday auth login` / OAuth profiles / config files / `~/.monday-cli/config.toml` (v0.3).
- `monday status` / `monday usage` diagnostics (v0.3).
- `notification send` / `webhook list/create/delete` (v0.3).
- `item search` cross-board (omit `--board`); `item history`; `board favorites`; `item update --continue-on-error` partial-success bulk (v0.3).
- `item watch`; `--concurrency`; asset upload (`add_file_to_column`); shell completion; `doc list/get`; `team` create/manage (v0.4).
- Boolean filter DSL — v0.2 keeps the v0.1 `--where` operator allowlist + `--filter-json`. The boolean DSL is fixture-driven; only ships when adoption metrics show concrete demand. Pencilled at v0.2 stretch / v0.3 firm.
- `monday item recreate-from-archive` — still no misleading `restore` command (cli-design §5.4 stands).

## 2. Pre-flight: what's already in place

Working at HEAD (`main`, post-`638d49a`):

- **Foundations (M0–M2.5)** — the runner / signal handling / typed errors / redaction / envelope builders / commander wiring / transport seam / retry layer / `MondayClient` / `account` commands all live. v0.2 inherits these unchanged.
- **Read surface (M3–M4)** — `workspace`, `board`, `user`, `update`, `item` reads (incl. filter DSL, cursor pagination, NDJSON streaming for `item list`, projection layer). Every write verb in v0.2 leans on these for resolution: `--board <bid>` lookup, column resolution, `me` token, board metadata cache.
- **Write half of §5.3 (M5a)** — `column-values.ts` (translators for `text`/`long_text`/`numbers`/`status`/`dropdown`/`date`/`people`), `dates.ts`, `people.ts`, `column-types.ts` writable allowlist, `dry-run.ts` engine. **M8's writer expansion plugs into this** — six new translator slots in the same dispatcher, plus the long-deferred `--set-raw` path.
- **Mutation surface (M5b)** — `item set`/`clear`/`update` (single + bulk via `--where`) + `update create`. Shared helpers: `item-helpers.ts`, `resolver-error-fold.ts`, `resolveColumnsAcrossClauses`, `parse-boundary.ts`. **M9–M13 reuse these directly**: bulk `item clear --where` is the per-item leg already plus a cursor walk; `item create`/`upsert`/`move` reuse the resolver fold; `update reply`/`edit`/`delete` reuse `update create`'s body-source plumbing (`--body` / `--body-file` / stdin).
- **M9.5 cleanup helpers (post-M9, pre-M10)** — six new shared modules + one drift fix collapse ~1900 LOC of duplication: `set-expression.ts splitSetExpression` (R22), `resolution-context.ts buildResolutionContexts` (R24), `item-board-lookup.ts` (R23 — `lookupItemBoard` + `lookupItemBoardWithHierarchy` + `resolveBoardId`), `source-aggregator.ts mergeSource/mergeSourceWithPreflight/mergeCacheAge` (R21), `resolver-error-fold.ts foldAndRemap` extension (R26 — mutation catch-arm wrapper), `resolution-pass.ts resolveAndTranslate` (R20 — three-pass column resolution helper). **M10–M18 inherit these on day one**: each new mutation command (archive / delete / duplicate / move / upsert) starts with `resolveBoardId` + `foldAndRemap`; column-write surfaces inherit `resolveAndTranslate` + `buildResolutionContexts`. The duplication-trigger threshold is "lift on the third consumer" per v0.1-plan §17 R-timing rule; ALL future write surfaces are now consumer N+1, never N=1, of these patterns.
- **Diagnostics + escape hatch (M6)** — `board doctor`, `raw`, agent-flow E2E. `raw` already gives agents a write escape; v0.2's `--set-raw` is the typed alternative for the column-value path specifically.
- **Release scaffolding (M7)** — `published-tarball.test.ts`, `envelope-snapshots.test.ts` (40 snapshots), `output-shapes.md`, `CHANGELOG.md`. **M18's release prep extends each**: ~30 new envelope snapshots, ~30 new `data` shape entries, `0.2.0` changelog cluster.
- **Test infrastructure** — fixture transport, fixture server (E2E), redaction-hardening suite, agent-flow E2E. Coverage floor sits at **94% branches / 95% lines / functions / statements**; v0.2 holds or raises it (never lowers).
- **Tooling** — ESM, NodeNext, strictest TS, ESLint `strictTypeChecked + stylisticTypeChecked`, vitest 4. CI runs typecheck + lint + test:coverage + build smoke on Node 22 and 24. Two-AI review (Codex `gpt-5.5`, read-only) per design change and per implementation milestone.

What's **missing** that v0.2 needs (each filled by the named milestone):

- A **cache-invalidation discipline** for board-metadata-mutating commands (M16). Today, every `--set` consults the cached board metadata; mutations that change board shape (`column-create`/`column-delete`/`group-rename`) need to invalidate the cache so subsequent reads/writes see the new shape. v0.1 had no schema-mutating commands so the question never arose.
- A **partial-success envelope** for multi-target add-users / remove-users (M14, M15). Single-target failure is the v0.1 mutation envelope; for these multi-user ops, per-user `{ok, error?}` records keep the agent informed without abandoning the whole call. Fail-fast bulk update (cli-design §6.5 `details.applied_to`) is the working precedent — `--continue-on-error` for `item update` waits until v0.3.
- A **`--set-raw <col>=<json>`** path through the existing translator (M8). The translator currently dispatches on type; raw bypasses dispatch but still resolves the column ID and validates the JSON shape against zod.
- A **column-mapping JSON shape** for cross-board `item move` (M11). Simple `{<source_col_id>: <target_col_id>}` (string-to-string) mapping that translates one-for-one onto Monday's `columns_mapping: [ColumnMappingInput!]` parameter. The richer `{id, value?}` value-translation form is deferred to v0.3 (Monday's wire shape carries no value slot — supporting it requires a non-atomic post-move mutation; see §15 M11 post-mortem).
- An **idempotency layer** for `item upsert` (M12). Match-by lookup + create-or-update branching; the branch decision is captured in the mutation envelope (`operation: "create_item" | "update_item"`) so agents can reason about which path fired.
- A **board-structure mutation cluster** (M16). Column/group create/update/archive/delete each have one Monday mutation; what's new is the cache invalidation fan-out + the column-type allowlist for `column-create`.

## 3. Milestone sequencing

Eleven milestones (M8–M18; M16 split into columns-then-groups per
Codex P2). Each milestone is a coherent commit cluster (3–10 commits)
ending on a green tree with the listed exit criteria met. Earlier
milestones unblock later ones; do not start M(N) until M(N-1)'s exit
criteria are satisfied. Each milestone updates `docs/output-shapes.md`
for any new command shapes it lands and adds envelope snapshots for
any new emitted shape.

Order at a glance: **writer expansion → item lifecycle → bulk symmetry
+ idempotency → updates → workspace + board CRUD → board columns
(introduces eager invalidation contract) → board groups (reuses it)
→ polish + release**.

```
       M8                                 — writer expansion (foundations)
       M9 ─ M10 ─ M11                     — item lifecycle (create / archive+delete+duplicate / move)
       M12                                — item upsert + bulk item clear
       M13                                — update mutations + update list expansions
       M14 ─ M15                          — workspace + board lifecycle
       M16                                — board columns (cache-invalidating; also pins the eager-invalidation contract)
       M17                                — board groups (cache-invalidating, smaller surface)
       M18                                — NDJSON streaming + 0.2.0 release prep
```

### M8 — Writer expansion + `--set-raw` escape hatch

The foundational v0.2 milestone. No new commands ship — instead,
the column-value translator gains the firm-row translator slots and
the `--set-raw` escape hatch, so every later milestone (M9 `item
create`, M11 `item move --columns-mapping`, M12 `item upsert`,
M16 `column-create`) can lean on the expanded writable type surface
without re-touching tests. **Foundations-first** is the load-bearing
ordering decision (§8 decision 1).

**As shipped (M8 close).** Three firm-row friendly translators
landed (`link` / `email` / `phone`). `WRITABLE_COLUMN_TYPES` grew
from 7 to 10. The three tentative-row types (`tags` /
`board_relation` / `dependency`) deferred to a follow-up; the
runtime keeps them in `V0_2_WRITER_EXPANSION_TYPES` so
`unsupported_column_type` still carries `deferred_to: "v0.2"` and
points agents at `--set-raw` as the working escape hatch in the
meantime. The §16 M8 close-pass Codex review caught six P2 items
(see §10 post-mortem); fixes for cumulative warning preservation,
resolution-before-translation ordering, bulk cache-age aggregation,
and argv-parse-before-board-lookup landed in the same commit
sweep.

**Deliverables**
- `src/api/column-values.ts` — three new firm translator slots:
  - `link` — `<url>` (one segment) → `{url:<url>, text:<url>}`; `<url>|<text>` (pipe-split, max 1 split, both segments trimmed) → `{url, text}`. `text` defaults to the URL when the pipe form is absent. URL validated via `z.string().url()`; failure → `usage_error`.
  - `email` — single email → `{email:<value>, text:<value>}`; `<email>|<text>` → `{email, text}`. Email validated via `z.string().email()`; failure → `usage_error`.
  - `phone` — `<phone>|<country>` (pipe form mandatory) → `{phone:<phone>, countryShortName:<country>}` where `<country>` is a 2-letter ISO 3166-1 alpha-2 code (uppercase). E.164-loose validation (`+?\d{6,15}`); ISO code validated against a frozen allowlist (~250 codes from `Intl` / a vendored list). Single-segment `<phone>` (no `|<country>`) rejected with `usage_error` — Monday's phone-column validation requires both number and country code AND verifies they match (per Monday's phone-validation changelog), so the friendly translator can't safely default `countryShortName: ""`. Agents who need empty-country phones use `--set-raw`.
  - **Deferred (still tentative; `V0_2_WRITER_EXPANSION_TYPES`):** `tags` (account-tag lookup; adds `tag-directory.ts` mirroring `user-directory` from M5a; new error code `tag_not_found` only if it ships firm — §8 decision 2), `board_relation` (comma-split item IDs → `{item_ids:[N1,N2]}`; allowed-boards check against `settings.boardIds ?? [settings.boardId]`), `dependency` (same shape as `board_relation` against Monday's separate `dependency` payload). All three can be unblocked by `--set-raw` today; landing the friendly translators is a follow-up M8x or rolls into v0.3.
- `src/api/raw-write.ts` — the `--set-raw <col>=<json>` path. Resolves `<col>` through the same `resolveColumnWithRefresh` flow as `--set`. Two reject lists fire after resolution but before mutation (matching cli-design §5.3 escape-hatch contract):
  - **Read-only-forever** (`mirror`, `formula`, `auto_number`, `creation_log`, `last_updated`, `item_id`) → `unsupported_column_type` with `read_only: true`.
  - **`files`-shaped** (`file`, anything else where Monday's write path is `add_file_to_column` rather than `change_column_value`) → `unsupported_column_type` with `deferred_to: "v0.4"`.
  Otherwise validates `<json>` parses as a `JsonObject`, then constructs a `TranslatedColumnValue` with `payload: { format: 'rich', value: <parsed> }` so the existing `selectMutation` dispatcher handles it uniformly. **No type-shape validation** beyond the JSON-object boundary — the user took the escape hatch and owns wire-shape correctness; Monday's server-side rejection surfaces as `validation_failed`. Mutually exclusive with `--set` for the same `<col>` (resolution-time enforced — the duplicate-ID check fires after both flags' tokens resolve to column IDs, since argv-parse can't tell whether two distinct tokens alias to the same column without board metadata; cli-design round-2 P2 correction). **Pass ordering**: M8 close-pass refactor (§10 post-mortem item #2) split each command's resolution path into resolve-all → cross-token-duplicate-check → translate, so a friendly+raw pair targeting the same resolved column surfaces the mutual-exclusion `usage_error` even when the friendly value would have produced a translator error.
- `src/api/column-types.ts` — `WRITABLE_COLUMN_TYPES` grows from 7 to 10 (the three firm rows). The frozen `as const` array's order is part of the contract; tests iterate it. The three tentative types stay outside the writable allowlist via `V0_2_WRITER_EXPANSION_TYPES` so the `unsupported_column_type` branch still fires with `deferred_to: "v0.2"` for them. Update `commands/board/describe.ts` so `example_set` covers the firm new types.
- `unsupported_column_type` — for v0.1's `link`/`email`/`phone` row, `deferred_to: "v0.2"` disappears (now writable). The `tags`/`board_relation`/`dependency` row keeps `deferred_to: "v0.2"` (a follow-up promotes them or rolls them to v0.3). Read-only-forever and `deferred_to: "future"` rows unchanged.
- `monday item set` and `monday item update` learn `--set-raw <col>=<json>`. Bulk `item update --where` learns `--set-raw` too. `--set` and `--set-raw` against the same column are mutually exclusive (resolution-time enforced; `usage_error`). Help text gets an example block.
- `cli-design.md` §5.3 step 4 + writer-expansion roadmap table + §4.3 command-tree (adds `--set-raw` to the relevant signatures) updated to reflect the v0.2 firm rows. M8 ships its own design diff for the design doc — submitted alongside the M8 PR for two-AI review per CLAUDE.md.

**Tests (pyramid)**
- Unit: each new translator's grammar — happy path, every error path, Unicode in the human-readable text segment, pipe-split edge cases (empty trailer, multiple pipes, leading/trailing whitespace), idempotent translation (re-running translation on the output reproduces the wire shape). Wire-shape fixture pinned per (count × type) cell so v0.3 inherits the rule unchanged. `--set-raw` coverage: column resolution succeeds, JSON parse fails (`usage_error`), JSON parse succeeds and round-trips through `selectMutation`, `--set` and `--set-raw` against the same column collide (`usage_error`).
- Integration: `item set --board <bid> <link-col>=https://example.com|Site` produces the expected wire payload (cassette pinned). Repeat for each new type. `--set-raw` against an `unsupported` / `future` column type that accepts `change_column_value` *succeeds* at the CLI layer (the escape hatch's whole point) and surfaces Monday's rejection as `validation_failed`. `--set-raw` against read-only-forever (`mirror` / `formula` / etc.) or `files`-shaped (`file`) → `unsupported_column_type` at the CLI layer, no API call fires.
- E2E: one happy-path spawn per new firm type (`link`, `email`, `phone`); the tentative types ship with E2E too if they make the cut.

**Exit**
- `monday item set <iid> --board <bid> link_col=https://x` succeeds against fixtures.
- `monday item set <iid> --board <bid> --set-raw status='{"label":"Done"}'` succeeds against fixtures and equals the friendly `--set status=Done` payload byte-for-byte.
- `unsupported_column_type` `deferred_to: "v0.2"` no longer fires for any of the firm v0.2 row types.
- `board describe`'s `example_set` covers all newly-writable types.
- Two-AI review pass cleared.
- Coverage floor held at 94/95/95/95.

---

### M9 — `item create` (incl. subitem creation + position)

The first net-new item-mutation verb. Non-idempotent (creating an
item with the same name twice yields two items — cli-design §5.8);
ships with `--dry-run`.

**Deliverables**
- `src/commands/item/create.ts` — `monday item create --board <bid> --name <n> [--group <gid>] [--set <col>=<val>]... [--set-raw <col>=<json>]... [--parent <iid>] [--position before|after --relative-to <iid>] [--dry-run]`. Calls `create_item` (or `create_subitem` when `--parent` is present) with the full `--set` payload bundled into `column_values`. **Single round-trip is a hard exit gate** (Codex P1): if Monday's `create_item.column_values` proves not to accept any v0.2 type's wire shape, M9 halts; we don't fall back to `create_item` + `change_multiple_column_values` because a partial-state failure between the two calls leaves an item with the requested name but missing column values. The v0.1 fixture pass against `column_values` covers all seven v0.1 types; M8's three firm new types (`link`/`email`/`phone`) each get a fixture-pinned wire-shape test on the create path before M9 implementation begins. The three deferred types (`tags`/`board_relation`/`dependency`) only need `create_item` fixtures once their friendly translators ship. `--position` resolves to Monday's `position_relative_method` + `relative_to`.
- `--parent <iid>` triggers `create_subitem`. Subitems live on Monday's auto-generated subitems board; column resolution targets *that* board, not the parent's. M9 plumbs the resolved subitem-board ID through the same column-resolution path as the top-level path. **Classic boards only.** Multi-level boards (`hierarchy_type: "multi_level"` per cli-design §2.8 — where subitems live on the parent's board rather than an auto-generated subitems board) are rejected pre-mutation with `usage_error` carrying `details.hierarchy_type` and a hint pointing at v0.3. Per Codex round-1 P1 on the design diff: M9's column-resolution path assumes the auto-generated-subitems-board model and would resolve against the wrong metadata on multi-level boards. Multi-level subitem support is a v0.3 candidate that needs its own design pass + cli-design §2.8 widening + a hierarchy-type-aware column-resolution branch. The pre-mutation hierarchy_type check happens after parent-item lookup (the CLI needs the parent's `board.hierarchy_type` to gate) but before any column resolution / value translation / mutation.
- `--position before|after --relative-to <iid>` — both flags required together; one without the other is `usage_error`. `relative_to` must reference an item on the same board (verified by item lookup, mirrors M5b's wrong-board check).
- `idempotent: false` in the CommandModule. Help text calls out `item upsert` (M12) for the idempotent variant.
- Mutation envelope: `data: { id: <new_item_id>, name, board_id, group_id, parent_id? }` plus the v0.1 top-level `resolved_ids: { ... }` slot per cli-design §6.4 (Codex round-2 P2: v0.1 mutation contract uses top-level `resolved_ids`, not nested in `data`). Snapshot pinned.
- Resolver-warning fold: every `--set`-resolution warning that surfaces during the create flow folds into `details.resolver_warnings` on failure (mirrors M5b discipline).

**Tests (pyramid)**
- Unit: argv parser — `--position` requires `--relative-to`; `--parent` and `--group` are mutually exclusive (subitems live on the subitems board, not in groups); `--name` empty after trim → `usage_error`; multiple `--set` against the same column → `usage_error`.
- Integration: happy path (top-level item, default group, multiple `--set`); subitem path (`--parent <iid>`); position path (before / after); each error path (`not_found` on board, `not_found` on parent, `not_found` on relative-to, `validation_failed` on Monday rejection); dry-run path **for both branches** — top-level `operation: "create_item"` AND subitem `operation: "create_subitem"` envelopes pinned (Codex round-4 P2 — both planned-change operations are §9 preconditions; tests must assert both shapes).
- E2E: one happy-path spawn (top-level), one subitem spawn.

**Exit**
- `monday item create --board <bid> --name "Test" --set status=Done` returns a new item ID against fixtures.
- Subitem path verified (top-level create + subitem create round-trip).
- `--dry-run` echoes the planned create without firing the mutation.

---

### M10 — `item archive` + `item delete` + `item duplicate`

Lifecycle siblings. Share the confirmation-gate pattern from M5b's
bulk paths (`--yes` required for destructive verbs).

**Deliverables**
- `src/commands/item/archive.ts` — `monday item archive <iid> --yes [--dry-run]`. Calls `archive_item`. `--yes` required for live archive (destructive — archived items hide from default queries and Monday only retains them for a 30-day recovery window). The pre-M10 cli-design §4.3 surface showed `item archive <iid>` without `--yes`; M10's pre-flight precondition added the `--yes` (post-pre-flight cli-design §4.3 carries the full annotation). `--dry-run` reports the would-archive item state and bypasses the `--yes` gate per §3.1 #7. Idempotent on the wire (re-archiving an archived item is a no-op on Monday's side); CLI marks `idempotent: true`.
- `src/commands/item/delete.ts` — `monday item delete <iid> --yes [--dry-run]`. Calls `delete_item`. `--yes` mandatory (truly destructive — Monday's delete is recoverable for 30 days but the CLI doesn't promise that). Not idempotent (re-deleting an already-deleted item is `not_found`).
- `src/commands/item/duplicate.ts` — `monday item duplicate <iid> [--with-updates] [--dry-run]`. Calls `duplicate_item` with `with_updates` boolean. Returns the new item's ID + the source item's ID. **Not idempotent** — every call creates a new item.
- All three share `ITEM_FIELDS_FRAGMENT` / `parseRawItem` / `projectItem` (the v0.1 R9 + R18 boundary) so every envelope's `data` (live) or `planned_changes[0].item` (dry-run) matches `monday item get` byte-for-byte. Dry-runs do a single dedicated read (`ItemArchiveRead` / `ItemDeleteRead` / `ItemDuplicateRead`) — `runByIdLookup` would be the wrong helper here because the cli-design §6.4 dry-run shape needs `data: null` + `planned_changes`, not the get-shaped envelope `runByIdLookup` emits. **`meta.source` is always `"live"`** for both paths: archive's + delete's live mutations are single-leg wire calls; duplicate's live is two-leg (`ItemBoardLookup` + `duplicate_item` — Monday requires `board_id`, derived from the source item's board); duplicate's dry-run is single-leg (`ItemDuplicateRead`). No leg consults the cache — the wire calls are unconditional — so `mergeSource` doesn't appear and `cache_age_seconds: null`.

**Tests (pyramid)**
- Unit: `--yes` enforcement on `archive`/`delete`; `--with-updates` boolean parser; idempotency-flag in CommandModule shape.
- Integration: each verb happy path; each error path (`not_found`, `forbidden`, `validation_failed`); `confirmation_required` without `--yes` for `delete`; dry-run for each.
- E2E: one happy-path spawn for `delete --yes` (it's the highest-blast-radius verb); the tarball test learns the new commands.

**Exit**
- All three verbs return correct envelopes against fixtures.
- `monday item delete <iid>` without `--yes` exits 1 with `confirmation_required`.

---

### M11 — `item move` (group + cross-board) — **shipped (on main)**

The most design-loaded item-lifecycle verb. Cross-board moves
re-resolve columns on the target and may need a `--columns-mapping
<json>` to translate values that don't share IDs.

> **Status — 2026-05-02.** Shipped on `main` across five atomic
> commits: `daa9d22` (feat — initial implementation) → `338a5ea`
> (docs sweep — cli-design §4.3 + §6.4 + §9.1 + this §15 + 4
> envelope snapshots) → `4385aba` (Codex round-1 fix — F1 empty-
> cells filter + F2/F3 doc consistency) → `747f300` (Codex round-2
> fix — semantic-empty cellHasData + target-id validation +
> dry/live wire parity + doc sweep) → `19d5a1e` (test gap-closers
> — value-string branch, invalid+unmatched precedence, empty-
> source live shape). Two transports under one verb: same-board
> (`--to-group`) via `move_item_to_group`, cross-board
> (`--to-group --to-board`) via `move_item_to_board` with the
> strict-default unmatched-column check + `--columns-mapping`
> payload. Net new: `src/api/column-mapping.ts` (the JSON parse
> boundary) + `src/commands/item/move.ts` (the verb). **56 new
> tests** across all three layers (13 mapping unit + 20 planner
> unit + 22 integration + 1 e2e + 4 envelope snapshots).
> **Coverage 98.98 / 95.35 / 99.37 / 99.07.** Two scope adjustments
> from the original deliverables — captured in §15 M11 post-mortem:
> (1) the rich `{id, value?}` form for cross-board value-overrides
> was deferred to v0.3 because Monday's `ColumnMappingInput` is
> strictly ID-to-ID (no value slot) so supporting it requires a
> non-atomic post-move `change_multiple_column_values` mutation;
> (2) the `--to-group <gid>` flag is now required for both
> same-board AND cross-board (Monday's
> `move_item_to_board(group_id: ID!)` is mandatory; cli-design §4.3
> line 530's pre-M11 mutual-exclusion drafting was misleading and
> the M11 docs sweep updated it). M11 surfaced an R-class refactor
> backlog (§16): R30 `SourceAggregator` accumulator helper (5 sites
> across `create.ts` / `update.ts` / `move.ts`; **shipped 1454116**
> in the M11 → M12 cleanup window), R31 `cellHasData` lift to
> item-projection (defer to second consumer), R32 JSON-object argv
> parse helper (defer to third consumer).

> **Note — pre/post-implementation diff.** The deliverables /
> tests / exit bullets below were updated post-implementation to
> match the shipped surface (Codex round-1 F2). Two scope
> adjustments versus the original draft, both surfaced from
> reading the SDK 14.0.0 typings during pre-implementation and
> captured in §15: (1) `--to-group <gid>` is required for both
> forms (Monday's `move_item_to_board(group_id: ID!)` is
> mandatory) — pre-M11 the line showed `--to-group | --to-board`
> as mutually exclusive; (2) `--columns-mapping` ships only the
> simple `{<src>: <target>}` form — the rich `{id, value?}` form
> was deferred to v0.3 because Monday's `ColumnMappingInput`
> carries no value slot and supporting it requires a non-atomic
> post-move `change_multiple_column_values` mutation with
> cross-leg partial-failure semantics that have no precedent.

**Deliverables**
- `src/commands/item/move.ts` — `monday item move <iid> --to-group <gid> [--to-board <bid>] [--columns-mapping <json>] [--dry-run]`. `--to-group` is required for both forms; `--to-board` alone (no `--to-group`) → `usage_error`. The mutex now lives between `--columns-mapping` and `--to-board`'s absence (`--columns-mapping` without `--to-board` → `usage_error` because mapping is cross-board-only).
  - **Same-board (group move):** calls `move_item_to_group`. No metadata loads, no column re-resolution.
  - **Cross-board move:** calls `move_item_to_board(item_id, board_id, group_id, columns_mapping)`. Loads source + target board metadata to compute the unmatched-column check. `--columns-mapping <json>` accepts the simple `{<source_col_id>: <target_col_id>}` form (string-to-string) mapping directly to Monday's `columns_mapping: [ColumnMappingInput!]` parameter. Strict default per §8 decision 5: source columns whose IDs don't exist on target AND aren't bridged by the mapping → `usage_error` with `details.unmatched: [{source_col_id, source_title, source_type}]` + `details.example_mapping: {<src>: "<target_col_id>"}`. `--columns-mapping {}` (empty object) is the explicit "drop everything (Monday's permissive default)" opt-in. Verbatim ID matches surface explicitly in the planned `column_mappings` so the dry-run echo + wire payload fully describe what Monday receives.
- `src/api/column-mapping.ts` — zod parse boundary for `--columns-mapping`. `Record<string, string>` shape; rejects non-object roots, non-string values (the rich `{id, value?}` form is rejected with `details.hint` pointing at the v0.3 deferral and the `monday item set` post-move workaround), empty-string keys/values. R18 `unwrapOrThrow` wrap so failures surface as `usage_error` (exit 1) with `details.issues`.
- `--dry-run` reports planned column mappings + still raises `usage_error` on unmatched (agents shouldn't have to interpret a "would-fail" dry-run shape). Cross-board dry-run shape includes `column_mappings: [{source, target}]` echo enumerating every populated source column.
- **Empty source cells excluded from the unmatched check** (Codex round-1 F1). The projection includes every wire `column_values` entry — including empty cells (`text: null` / `value: null`). The verb's `cellHasData` filter restricts the strict-default check to cells with actual content (non-null wire `value` OR non-empty `text`); empty cells drop out because Monday wouldn't carry their values across the move anyway.

**Tests (pyramid)**
- Unit: mapping-JSON parse boundary (R18 — every malformed-shape branch + the rich-form deferral hint); `planColumnMappings` planner (verbatim match, mapping override, unmatched aggregation, empty-opt-in, defensive fallback); `cellHasData` + `collectSourceColumnIds` filter (Codex round-1 F1 regression).
- Integration: same-board happy path + dry-run + not_found; cross-board happy path with explicit mapping + verbatim + empty opt-in + unmatched usage_error + dry-run + parse-boundary + redaction + idempotent pin; the F1 regression (cross-board with empty source cells doesn't raise unmatched).
- E2E: one cross-board happy path with mapping (the four-leg flow proven through the compiled binary).

**Exit**
- Same-board and cross-board paths both verified against fixtures.
- `--columns-mapping` JSON parses through `parseColumnMappingJson` and round-trips into Monday's `columns_mapping` wire payload.
- `--dry-run` reports the mapping plan + still raises `usage_error` on unmatched.

---

### M12 — `item upsert` + bulk `item clear --where` — **shipped (on main)**

Idempotency cluster. Both verbs deliberately ship together: `item
upsert`'s match-then-update branch reuses M5b's `item update` logic
verbatim, and bulk `item clear --where` reuses M5b's bulk `item
update --where` cursor walk. Two coherent additions in one
milestone.

> **Status — 2026-05-03.** Shipped on `main` across nine atomic
> commits: `5b5a5b1` (pre-flight cli-design contract) → `747771c`
> (feat — upsert) → `c3ee917` (feat — bulk clear `--where`) →
> `1f081a4` (Codex round-1 fix — me-resolution in lookup +
> dry-run source folding + empty-page-with-cursor + clear schema
> union + surface line) → `59e6d09` (Codex round-2 fix — replaced
> the misleading `owner=alice@example.com` example) → `db18fb7`
> (Codex round-3 fix — round-2's "pass numeric user IDs"
> workaround was unattainable; people grammar rejects raw
> numeric IDs per §5.3 step 3) → `f45d0e7` (Codex round-4 fix —
> the date round-trip claim was false because items_page filter
> requires `["EXACT", "YYYY-MM-DD"]`; the rich-grammar columns
> `link` / `email` / `phone` also don't round-trip; reframed as
> a per-kind safe-list) → `89d5cf3` (Codex round-5 P3 polish —
> help-text wrap + `item search` shared-pipeline accuracy). 1826
> tests passing, branches 95.01%, all gates green.

> **Note — five-round review.** M12's post-implementation review
> needed five rounds (vs M11's two, M9's four), with each round
> narrowing the v0.2 contract for `--match-by` further. The
> root cause is captured in §17's "lessons" — wire-shape claims
> need empirical proof (existing-surface tests or new tests),
> not "translator accepts X" reasoning. The shipped contract is
> a strict per-kind safe-list (always-safe / via-label-text /
> restricted / not-v0.2-safe) with stable hidden text /
> `external_id` columns as the recommended canonical key.

**Deliverables**
- `src/commands/item/upsert.ts` — `monday item upsert --board <bid> --name <n> --match-by <col>[,<col>...] [--set <col>=<val>]... [--set-raw <col>=<json>]... [--dry-run]`. **Sequential-retry idempotent** (cli-design §5.8) — re-running with the same args from the same agent is safe; concurrent agents are *not* a uniqueness guarantee (Codex round-1 P1: two agents observing zero matches at the same instant both create). Match-by lookup → if 0 matches, branch to `create_item` (M9 surface reused); if 1 match, branch to the v0.1 `item update` logic (M5b's existing single-item path: synthetic `name` key bundled into `change_multiple_column_values` when `--name` accompanies `--set`, or `change_simple_column_value(column_id: "name")` when `--name` is the only diff — Codex round-4 P2 alignment with the v0.1 contract; M12 reuses `commands/item/update.ts`'s entry point rather than re-implementing rename); if 2+ matches, fail with `ambiguous_match` (28th stable error code if both v0.2 codes ship, 27th if `tags` slips; see §8 decision 2). The mutation envelope's `data.operation: "create_item" | "update_item"` exposes the branch (`data.operation` not `meta.operation` — Codex round-2 P2; `meta` has no `operation` slot, and v0.1's mutation envelope already keeps operation-shape signals in `data`). Race mitigation guidance lives in `--help`: agents should use a stable hidden key column for `--match-by` and tighten the predicate so a duplicate from a race surfaces as `ambiguous_match` on the next call. Concurrent-write protection is a v0.4 candidate (cli-design §9.3 lock-resource semantics).
- `--match-by` accepts column tokens (resolved via `resolveColumnsAcrossClauses`) plus the literal `name` token, which matches against item name. Multiple match-by columns AND-combine (item must equal all named values). Match-by values come from the corresponding `--set` flag's value (which is required for every match-by column).
- `src/commands/item/clear.ts` — bulk `monday item clear --board <bid> <col> --where <c>=<v>... | --filter-json <json> --yes [--dry-run]`. Per-item leg already shipped (M5b's `translateColumnClear`). Cursor walk + `confirmation_required` + per-item failure decoration mirror bulk `item update --where`. Bulk dry-run aggregates per-item `planChanges` results into one N-element `planned_changes` array, dedupes warnings.
- `cli-design.md` §6.5 grows from 26 codes to 28 if both `tag_not_found` (M8, conditional on `tags` shipping firm) and `ambiguous_match` (M12) land; 27 if `tags` slips. §13's command tree updated.

**Tests (pyramid)**
- Unit: upsert match-by parser (multi-column AND); branch-decision logic given 0 / 1 / 2+ match-row counts; bulk-clear parser reuses bulk-update test scaffolding.
- Integration: upsert happy path (create branch + update branch); upsert ambiguous-match (`ambiguous_match`); bulk clear happy path with `--where`; bulk clear `confirmation_required` without `--yes`; bulk clear per-item failure decoration (matching the §6.5 `applied_to` shape).
- E2E: one upsert happy path (each branch); one bulk-clear happy path.

**Exit**
- Upsert idempotent — running twice with the same args yields one item, second call hits the update branch.
- Bulk clear walks every matched item and decorates failures.
- Two new error codes (`tag_not_found`, `ambiguous_match`) producible with tests.

---

### M13 — Update mutations + `update list` expansions — **shipped (on main)**

The full comment-thread surface. Eight mutation verbs (most thin
wrappers) plus two `update list` shape expansions.

> **Status — 2026-05-07.** Shipped on `main` across **twelve**
> atomic commits: `bb7ee17` (docs pre-flight — cli-design §4.3
> + §6.4 partial-success envelope shape pinned) → `7837d0c`
> (feat reply + body-source helper lift) → `b867c96` (feat edit)
> → `b45578d` (feat delete — destructive, --yes-gated) →
> `86a5fe0` (feat like / unlike + shared toggle helper) →
> `feaf8e8` (feat pin / unpin) → `62466ce` (feat clear-all +
> partial-success mutation builder; first consumer of the §6.4
> partial-success envelope) → `f84a7b3` (feat list audit —
> `--board <bid>` + `--with-replies` opt-in default flip; the
> one breaking change in v0.2) → `10a50da` (docs close — §19
> post-mortem + status block + output-shapes + CLAUDE.md) →
> `9c16d43` (Codex round-1 fix — pagination cap warning on
> clear-all + `--limit-pages` flag + GraphQL query-shape pins
> on `update list`) → `451737e` (Codex round-2 fix — board-
> variant query-shape pin + cli-design surface drift + post-
> mortem honesty) → round-3 docs-cleanup commit (this section).
> **1900 tests passing**, branches 95.39%, all gates green;
> coverage floor (94/95/95/95) held. The §19 M13 post-mortem
> captures the SDK-discovery scope adjustment (clear-all atomic
> vs page-walk), the toggle-helper lift, the body-source helper
> R-class trigger, and the round-1 / round-2 / round-3 review
> arc.

**Deliverables**
- `src/commands/update/reply.ts` — `monday update reply <uid> --body <md> | --body-file <path> [--dry-run]` (stdin via `--body-file -`). Calls `create_update` with `parent_id: <uid>`. Reuses `update create`'s body-source parser verbatim. `--dry-run` shape mirrors v0.1's `update create` dry-run.
- `src/commands/update/edit.ts` — `monday update edit <uid> --body <md> | --body-file <path> [--dry-run]`. Calls `edit_update`. Idempotent. Dry-run shape: `operation: "edit_update"`, `update_id`, `body`, `body_length`.
- `src/commands/update/delete.ts` — `monday update delete <uid> --yes [--dry-run]`. Calls `delete_update`. Destructive. Dry-run shape: `operation: "delete_update"`, `update_id`.
- `src/commands/update/like.ts`, `…/unlike.ts` — `monday update like <uid> [--dry-run]` / `unlike <uid> [--dry-run]`. Calls `like_update` / `unlike_update`. Idempotent. Dry-run shape: `operation: "like_update" | "unlike_update"`, `update_id`.
- `src/commands/update/pin.ts`, `…/unpin.ts` — same shape with `pin_to_top` [`--dry-run`]. Idempotent.
- `src/commands/update/clear-all.ts` — `monday update clear-all <iid> --yes [--limit-pages <n>] [--dry-run]`. Page-walks `updates(item_id)` via `walkPages` (Monday's updates query is page-based, not cursor-based — Codex P2) to collect IDs, then sequential `delete_update` per ID. **Partial-success envelope** per the §1 universal rule: `data.results: [{update_id, ok, error?}]`. The envelope is `ok: true` even when every per-update delete fails (whole-call success means the page-walk and dispatch ran; per-update success is inside `results`). Dry-run reports the would-delete IDs. `--limit-pages <n>` extends the page-walk cap (1-500, default 50, 100 updates/page); on threads bigger than the cap the walker truncates and the CLI surfaces `pagination_cap_reached` in `warnings` so the agent knows the live + dry-run output covers only the collected prefix (Codex round-1 F1 fix).
- `src/commands/update/list.ts` audit:
  - `--board <bid>` — new: board-wide updates (no `<iid>`); `--board` and `<iid>` mutually exclusive (one or the other).
  - `--with-replies` — **clarification, not new shape** (Codex P2): the existing `update list` already returns `replies: [...]` per top-level update (verified in `src/commands/update/list.ts:42` + `docs/output-shapes.md:343`). M13 makes `--with-replies` an explicit opt-in for replies to be populated; without the flag, `replies: []`. This is a v0.1→v0.2 default change for `update list` — pinned as a §9 cli-design precondition + a CHANGELOG breaking-changes line. Alternative considered + rejected: keep replies-always-on and make `--with-replies` a no-op (more agent-friendly default but doubles every `update list` query's complexity score; Monday's complexity budget is the binding constraint).
- NDJSON streaming for `update list` (both per-item and per-board) deferred to M18; cross-referenced here because the `data` shape M13 ships defines what M18's NDJSON serialises.

**Tests (pyramid)**
- Unit: each command's argv parser; `--with-replies` shape transformation; `--board` / `<iid>` mutual exclusion.
- Integration: happy path per command; error per command (`not_found` on update / item / board, `forbidden`); `--with-replies` round-trip (top-level + reply integrated); `clear-all` per-update failure decoration.
- E2E: one `update reply` happy path; one `update edit` happy path; one `update delete --yes` happy path.

**Exit**
- All eight mutation verbs ship with envelopes pinned.
- `update list --board` and `update list --with-replies` shapes pinned via output-shapes.md + envelope snapshot.
- The agent-flow E2E (M6) gains a comment-edit step (optional, gated on whether the test workspace's permissions allow it).

---

### M14 — Workspace lifecycle — **shipped (on main)**

Smaller surface than the board cluster, but admin-permission-
sensitive. Ships first among container-CRUD because boards reference
workspaces (`board create --workspace <wid>`).

> **Status — 2026-05-07.** Shipped on `main` across **eleven**
> atomic commits: `3fe1d58` (docs pre-flight — cli-design §4.3
> + §6.4 workspace verb annotations + workspace-create / -update /
> -delete / -add-users / -remove-users operation shapes pinned)
> → `57fa844` + `a1904f8` + `2086953` (three pre-flight Codex
> rounds — 6 + 3 + 1 findings, all P1/P2 contract bugs caught
> before any code shipped) → `ccd7f1e` (feat workspace create
> + scaffolds the cluster) → `c361057` (feat workspace update —
> preflight diff + partial-attributes wire) → `2b2a2cc` (feat
> workspace delete — destructive verb with inline gate;
> 5th destructive-verb consumer that triggered R29) →
> `91ccae6` (feat workspace add-users — first M14 partial-
> success consumer; per-user fan-out via dispatchSequential
> + `data.operation` slot + numeric-vs-email split) →
> `6e77cc9` (feat workspace remove-users — completes the M14
> verb cluster) → `cbd0ab4` + `d37eae2` + `4c6b807` (three
> implementation Codex rounds — 2 + 2 + 1 findings, narrowing
> the missing-root-key + null-payload + meta.source aggregation
> contracts) → `2b74321` (refactor R29 — destructive-verb gate
> consolidation lifted into `src/api/destructive-gate.ts`;
> byte-identical post-lift) → this close commit (docs sweep +
> §21 post-mortem). Project coverage: 98.92 / 95.04 / 99.46 /
> 99.08 — branches dropped from 95.44 (M13 close) to 95.04 net
> of M14's new partial-success surface area, but stays above
> the 94/95/95/95 floor; the per-file workspace dip (78-92%
> branches across the partial-success verbs) is absorbed by
> sibling files at 99+%. Tests 1900 (M13 close) → 1957 (M14
> close).


**Deliverables**
- `src/commands/workspace/create.ts` — `monday workspace create --name <n> [--kind open|closed] [--description <d>] [--dry-run]`. Calls `create_workspace`. Dry-run shape: `operation: "create_workspace"`, `name`, `kind`, `description?`.
- `src/commands/workspace/update.ts` — `monday workspace update <wid> [--name <n>] [--kind open|closed] [--description <d>] [--dry-run]`. Calls `update_workspace`. Idempotent. Dry-run reports the field-level diff (cli-design §6.4 multi-column shape applied to workspace fields).
- `src/commands/workspace/delete.ts` — `monday workspace delete <wid> --yes [--dry-run]`. Calls `delete_workspace`. Destructive. Dry-run shape: `operation: "delete_workspace"`, `workspace_id`.
- `src/commands/workspace/add-users.ts`, `…/remove-users.ts` — `--users <id|email>,... [--dry-run]`. Calls `add_users_to_workspace` / `delete_users_from_workspace`. Each email user resolves through M5a's `userByEmail`; numeric IDs are argv-derived and skip the resolver. **Partial-success envelope** per §1 universal rule: per-user `{user_id, ok, error?}` records in `data.results: [...]`; envelope is `ok: true` regardless of per-user outcomes (whole-call success means dispatch ran). Top-level `error` only when the whole call failed — couldn't reach API, OR **no dispatchable `user_id` remains after parsing / resolution** (every `--users` token was an email AND every email failed lookup → `user_not_found`, exit 2; `details.failed_tokens: [...]`). Mixed calls (numeric + ghost email; email-resolves + email-fails) stay partial-success. Mutation envelope's `data.operation` reflects the verb (`add_users_to_workspace` / `delete_users_from_workspace`) — `data.operation` not `meta.operation` per the upsert decision in M12. Dry-run lists per-user `{user_id, would_apply}` — same shape minus the network call. **`meta.source` aggregation rule** splits dry-run vs live (cli-design §6.4 partial-success-envelope rule): dry-run sees only resolver legs (all-numeric → `none`; cache → `cache`; live → `live`; combinations → `mixed`); live folds in every per-target mutation dispatch leg too (always `live`), so all-numeric live aggregates to `live` (not `none`).
- `MutationEnvelope` is reused (no §6.4 schema bump): the `data` slot widens to accept `{ results: [...] }` for partial-success consumers. Decision 3 closed accordingly.

**Tests (pyramid)**
- Unit: argv parsers; `--users` parser (comma-split, mixed IDs and emails); destructive-verb `--yes` enforcement.
- Integration: each verb happy path; each error path (`forbidden` on non-admin, `not_found` on workspace, `user_not_found` on resolution); `add-users` partial success (one valid, one ghost email).
- E2E: one happy path per verb.

**Exit**
- All five verbs ship with envelopes pinned.
- Partial-success envelope shape pinned via output-shapes.md + snapshot.
- `forbidden` paths exercise correctly (workspace admin verbs typically need account admin).

---

### M15 — Board lifecycle — **shipped (on main)**

Board CRUD on the board itself (not its column/group structure —
those are M16/M17). Reuses the workspace milestone's partial-success
shape for `add-users`.

> **Status — 2026-05-07.** Shipped on `main` across **fifteen**
> atomic commits: `a3b03c3` (docs pre-flight — cli-design §4.3 +
> §6.4 board verb annotations + six new operation shapes pinned)
> → `62934e3` + `27c66f3` + `b222d23` + `080946b` (four pre-flight
> Codex rounds — 4 + 4 + 6 + 1 findings = 15 total, 1 P1 + 9 P2 +
> 5 P3, all caught before any code shipped) → `d7c342a` (feat
> board create + scaffolds the cluster; bundles the
> `BOARD_FIELDS_FRAGMENT` + `boardProjectionSchema` projection
> helper from day one per the §22 R39 pattern; migrates `board
> get` to share the helper byte-identical) → `669ceea` (feat
> board update — preflight diff + per-attribute fan-out wire;
> per-field calls fan out N sequential `update_board(board_id,
> board_attribute, new_value)` mutations + force-live final read
> for the success envelope) → `b511f6c` (feat board archive —
> destructive verb with shared gate; R29 helper count 5 → 6) →
> `0fc526f` (feat board delete — destructive verb with shared
> gate; R29 helper count 6 → 7) → `e072d57` (feat board
> duplicate — wrapped data envelope with `is_async`; the M15-
> unique verb whose `data` slot wraps because Monday's
> `BoardDuplication { board, is_async }` SDK type carries a
> readiness-check field the projection schema doesn't model) →
> `0088283` (feat board add-users — third partial-success-fan-
> out consumer; mirrors workspace add-users / remove-users
> verbatim modulo the wire mutation name + `board_id` detail
> key) → `ba88ea8` + `28e44bd` (two implementation Codex rounds
> — 3 + 3 findings, 0 P1 + 2 P2 + 4 P3, narrowing the null-
> payload-on-`board update` + dry-run-snapshot-completeness +
> stable-error-code-pin contracts; round-2 returned closure with
> P3 only) → `4756269` (refactor R41 — partial-success null-
> payload guard lifted into `src/api/response-root.ts`; three
> consumers; byte-identical post-lift) → this docs close commit
> (§3 M15 status flip + §22 R41 annotation + §23 post-mortem +
> §9.2 tick + CLAUDE.md status flip + output-shapes.md entries
> for the six new verbs). Project coverage: 98.96 / 95.04 /
> 99.48 / 99.14 — branches held at the M14-close baseline
> despite M15's larger surface (six verbs + wrapped envelope
> + per-attribute fan-out wire), and the 94/95/95/95 floor
> still clears with margin. Tests 1957 (M14 close) → 2023
> (M15 close).
>
> **R40 stays deferred to the M15 → M16 cleanup window** per
> v0.2-plan §22 R40's "M15 close OR M15 → M16 cleanup window"
> schedule. R40's surface is larger (~250 LOC consolidation
> across three files including the parseUsersArg + resolveTokens
> loops) and benefits from a focused session rather than
> bundling here. **R42** (retroactive missing-root-key sweep
> across pre-M14 mutation verbs, ~17 sites) stays scheduled for
> a post-M15 dedicated session.

**Deliverables**
- `src/commands/board/create.ts` — `monday board create --name <n> [--workspace <wid>] [--kind public|private|share] [--template <bid>] [--description <d>] [--dry-run]`. Calls `create_board`. `--template <bid>` passes Monday's `template_id` arg — `<bid>` is the template board's ID, managed via Monday's UI. `BoardKind` has no `template` value (only `public` / `private` / `share`); the CLI doesn't pre-validate template-ness ahead of the wire call. Non-template IDs surface a wire error re-mapped per cli-design §6.5. cli-design §4.3 line 460 (pre-M15) showed `board create` without `--template`; the M15 pre-flight `a3b03c3` adds it. Dry-run shape: `operation: "create_board"`, `name`, `workspace_id?`, `kind`, `description?`, `template_id?`.
- `src/commands/board/update.ts` — `monday board update <bid> [--name <n>] [--description <d>] [--dry-run]`. Calls `update_board`. Idempotent. Dry-run reports the field-level diff.
- `src/commands/board/archive.ts` — `monday board archive <bid> --yes [--dry-run]`. Destructive (archive is consistently `--yes`-gated across nouns per §8 decision 9).
- `src/commands/board/delete.ts` — `monday board delete <bid> --yes [--dry-run]`. Destructive (Monday's recoverability window the same 30-day caveat as items).
- `src/commands/board/duplicate.ts` — `monday board duplicate <bid> [--name <n>] [--workspace <wid>] [--with-updates] [--dry-run]`. Calls `duplicate_board`.
- `src/commands/board/add-users.ts` — same shape as M14's workspace `add-users` (`--dry-run` + partial-success envelope per §1 universal rule).

**Tests (pyramid)**
- Unit: argv parsers; `--kind` enum; `--template` argv ID-shape validation only (no template-ness check — that's Monday-side).
- Integration: each verb happy path; each error path; `add-users` partial success.
- E2E: one happy path per verb (six spawns); the tarball test learns them all.

**Exit**
- All six verbs ship with envelopes pinned.
- `monday board duplicate <bid> --with-updates` round-trips correctly (item count + update count match expectations).

---

### M16 — Board columns (cache-invalidating concentrate) — **shipped (on main)**

The eager-invalidation-introducing half of board structure (Codex
P2 split). Three verbs that mutate column shape; each invalidates
the v0.1 board-metadata cache. Splitting columns from groups keeps
the cache-design work reviewable in one milestone — M16 introduces
the eager-invalidation contract + the `invalidateBoard(boardId)`
helper; M17 reuses both for its own group mutations.

> **Status — 2026-05-07.** Shipped on `main` across **ten** atomic
> commits: `ea74d7a` + `c42b751` (docs prelude — pre-flight SHA
> backfill + §22 R45/R46 candidate-log) + `c0efab5` (the seven-
> round pre-flight contract referenced from this status as the
> single landmark — see §9.2 line 824 for the round-by-round
> finding tally) → `26aa821` (feat column-create — bundles R45
> projection helper + adopts it; first M16 verb; introduces the
> `invalidateBoard(boardId)` helper in `src/api/cache.ts`,
> extends `READ_ONLY_FOREVER_TYPES` with `item_assignees` per
> the M16 cli-design pin, ships the per-type `--settings` zod
> schemas + the `noncanonical_column_type` warning categoriser)
> → `08ef580` (feat column-update — per-attribute fan-out across
> `change_column_title` + `change_column_metadata({column_property:
> description})`; the trailing wire-call's `Maybe<Column>` is
> authoritative for both fields, so no force-live final read leg
> fires; pins the §8 fan-out call-site contract via three round-
> trip cassettes covering happy-path + partial-application +
> zero-legs-succeeded shapes) → `565d5d3` (feat column-delete —
> destructive verb with shared R29 gate; first two-tuple consumer
> of `enforceDestructiveGate` via the new `extraDetails` slot
> carrying `board_id` alongside the canonical `column_id`
> detailKey per cli-design §6.5 single-target shape) → `4580af7`
> (M15 retrofit board update — `invalidateBoard` per §8 fan-out
> call-site contract; high-water-mark counter on `succeededLegs`
> gates whole-call-success vs partial-application-failure vs
> zero-legs-succeeded paths) → `15a353b` (M15 retrofit board
> archive — single-leg invalidation per §8; required because the
> mutation flips cached `state: 'active'` → `'archived'`) →
> `20bfeea` (M15 retrofit board delete — single-leg invalidation
> per §8; required so a same-process describe sees `not_found`
> rather than a phantom board) → `f94c2fa` (Codex round-1 P2 fix-
> ups — board-update double-invalidate restructure to match
> column-update's try/catch boundary; partial-application + final-
> read-failure round-trip cassettes; board-delete §8 envelope-pin
> assertion fix) → `95009ae` (Codex round-2 P3 — board-delete
> test-name diagnostic fix-up). Implementation closed at Codex
> round 2 with 0 P1 + 0 P2 + 0 P3 outstanding (round 1: 0 P1 +
> 3 P2 + 0 P3, all addressed; round 2: closure-ready). Project
> coverage: 99.01 / 95.43 / 99.49 / 99.14 — branches HELD at the
> M15-close baseline (95.04) and TICKED UP slightly (95.43)
> despite M16's larger surface (three new verbs + three
> retrofitted M15 verbs + the R45 helper + the per-type settings
> schemas + the categoriser); the 94/95/95/95 floor still clears
> with margin. Tests 2023 (M15 close) → 2127 (M16 close).
>
> **R45 shipped at the first M16 implementation commit** in
> `26aa821 feat(m16): add board column-create — R45 column
> projection helper + first column verb`. New file
> `src/api/column-mutation-result.ts` exporting `projectMutation
> Column` + `COLUMN_FIELDS_FRAGMENT` + `columnProjectionSchema`;
> all three M16 verbs adopt the helper from day one (mirrors
> R39's M15-implementation timing). Per-noun divergences pinned
> via the `columnIdKey: 'column_id' | 'title'` parameter (column-
> create uses `title` for the pre-id throw shape; column-update +
> column-delete use `column_id`); the `boardId` parameter is
> always paired with the column id since the wire signature is
> two-tuple. Net +50 LOC in the new helper file + ~−30 LOC across
> the three column verbs (vs the inline-implementation
> alternative).
>
> **`invalidateBoard(boardId, env?)` shipped in `src/api/cache.ts`**
> as the M16 §8 contract's exported primitive — a thin wrapper
> over `clearEntry(root, { kind: 'board', boardId })` that owns
> cache-root resolution. Idempotent (no-op on missing entry).
> Coexists with the pre-existing `evictBoardMetadata` in
> `src/api/board-metadata.ts` (a parallel re-export rather than a
> rename, so the M3-era helper's existing test surface stays
> unchanged). Six call sites adopt the wrapper — three M16
> column verbs (single-leg + fan-out) plus three M15 retrofitted
> verbs (board update fan-out, board archive + delete single-leg).
>
> **R29 destructive-gate helper grew an `extraDetails` slot** in
> `565d5d3` to support two-tuple destructive verbs (column-delete
> echoes `{board_id, column_id, hint}` per cli-design §6.5
> single-target shape; the wire signature is two-tuple). The
> existing seven single-id consumers stay byte-identical post-
> extension (the canonical `[detailKey]: target` + `hint` always
> win on key collision; `extraDetails` merges FIRST). M17's
> group-archive / group-delete will reuse the same slot.
>
> **`READ_ONLY_FOREVER_TYPES` reclassification.** M16 pre-flight
> pinned `item_assignees` as read-only-forever (cli-design §4.3
> column-create) — Monday computes it server-side from people
> columns; never writable via the API. Extended the set in
> `src/api/column-types.ts`; `unsupportedColumnTypeError`'s
> `getColumnRoadmapCategory` now categorises it as
> `'read_only_forever'` (was `'future'`); `--set-raw
> item_assignees=<json>` now rejects at column-resolution time
> rather than waiting for Monday's wire `validation_failed`.
> Updated the existing column-values + column-types unit tests
> to reflect the reclassification.
>
> **R46 shipped at the M16 → M17 cleanup window** in `5b06d53
> refactor(r46): lift withBoardInvalidation post-success
> projection wrapper into api/board-mutation-invalidation` (per
> v0.2-plan §22 R46's "post-M16 cleanup window" schedule —
> mirrors R40 + R43's M15 → M16 cleanup-window timing). New
> `src/api/board-mutation-invalidation.ts` exports
> `withBoardInvalidationSingleLeg` (4 consumers: column-create
> / column-delete + M15-retrofit board archive / board delete)
> and `withBoardInvalidationFanOut` (2 consumers: column-update
> + M15-retrofit board update); both pin the §8 leg-count split
> in the type system. The fan-out variant exposes a
> `BoardFanOutTracker.recordLegSuccess()` callback so the high-
> water-mark counter survives a thrown closure without forcing
> consumers to wrap their own try/catch. Twelve focused unit
> tests pin the contract end-to-end through a real `mkdtemp`
> cache; project coverage held at 99.01 / 95.43 / 99.49 / 99.14
> (M16-close baseline, identical) — the six round-trip
> integration cassettes in `tests/integration/commands/board.
> test.ts` pass byte-identical pre-lift vs post-lift. Tests
> 2127 → 2139 (+12 helper unit tests). M17's five group verbs
> (single-leg + fan-out mix) adopt the helpers from day one
> (mirrors R29's M14-close-then-M15-adopts pattern).
> **R42** (retroactive missing-root-key sweep across pre-M14
> mutation verbs) + **R44** (generic `projectMutationResource`
> over R28/R37/R43/R45) stay deferred per §22 R42 / §22 R44.

**M15 retrofit cluster:** `invalidateBoard(boardId)` is also
called post-success from **three** M15 verbs — `board update`,
`board archive`, and `board delete`. Per-verb rationale:

- `board update` — M15's pre-flight pinned the success-path final
  read as force-live to avoid emitting stale `data`, but force-
  live only protects the immediate envelope — later commands
  reading the same board's metadata still hit the stale cache
  entry. The retrofit fires invalidation alongside the force-
  live read.
- `board archive` — the wire mutation flips `state` from
  `active` to `archived`; the cached `boardMetadataSchema.state`
  field would lag until TTL eviction. Without retrofit, a same-
  process `board describe` / `board list` after the archive
  reads `state: "active"` until the cache expires.
- `board delete` — the cache entry would otherwise serve a
  phantom board until TTL eviction; the retrofit's invalidation
  deletes the cache file so the next read cleanly cache-misses
  to the live `not_found`.

Other M15 verbs are excluded with explicit reasoning: `board
create` / `board duplicate` (no pre-existing cache entry to
invalidate); `board add-users` (cached `boardMetadataSchema`
doesn't surface `subscribers`, deferred to v0.3 if subscribers
join the projection). Same retrofit applies to M14 if
`workspaces` joins the cache layer in v0.3 (no current cache
pressure there). cli-design §8 + §4.3 board-archive / board-
delete annotations carry the contract pin; M16 implementation
lands the call sites.

**Deliverables**
- `src/commands/board/column-create.ts` — `monday board column-create <bid> --type <type> --title <t> [--description <d>] [--settings <json>] [--dry-run]`. Calls `create_column`. **`--type` validated against known Monday column types** (the full ~40-type set, parsed at argv-parse-time); a warning fires when the requested type isn't in `WRITABLE_COLUMN_TYPES`, branched by category per cli-design §5.3 escape-hatch contract:
  - Raw-writable types (anything that accepts `change_column_value` — e.g. `country`, `hour`, `timeline` in v0.2) → warning suggests `--set-raw <col>=<json>`.
  - Read-only-forever (`mirror`, `formula`, `auto_number`, etc.) → warning notes there's no write path; agents should pick a different `--type`.
  - `files`-shaped (`file`) → warning notes write path is `add_file_to_column`, pinned to v0.4 (asset upload).
  The command still proceeds in all cases — Monday accepts non-writable types and agents may legitimately want them. cli-design §4.3 carries the full per-verb annotation post-pre-flight (`--settings <json>` + warning category + `details: {column_type, category, suggested_write_path}` shape). `--settings <json>` for type-specific config (status labels, dropdown options, etc.); zod-validated per type for types in `WRITABLE_COLUMN_TYPES`; raw-writable / read-only-forever / files-shaped types accept any well-formed JSON (Monday validates server-side). Dry-run shape: `operation: "create_column"`, `board_id`, `type`, `title`, `description?`, `settings?`.
- `src/commands/board/column-update.ts` — `monday board column-update <bid> <cid> [--title <t>] [--description <d>] [--dry-run]`. Calls `change_column_title` / `change_column_metadata` as needed. Idempotent. Dry-run reports the field-level diff.
- `src/commands/board/column-delete.ts` — `monday board column-delete <bid> <cid> --yes [--dry-run]`. Calls `delete_column`. Destructive.
- `src/api/cache.ts` — `invalidateBoard(boardId, env?)` exported. Each M16 column mutation + the M15 retrofit cluster (`board update` / `board archive` / `board delete`) invokes it per the cli-design §8 call-site contract (single-leg verbs invalidate AFTER success-envelope `data` projection on success path / skip on error path; fan-out verbs invalidate ONCE after the per-attribute loop settles, conditional on at least one leg succeeding — handles whole-call success AND whole-call partial-application failure with one rule, see §8 for the full timing rule including the partial-application "data projection or error projection" carve-out). Subsequent `--set` / `item list` reads see fresh metadata. Existing TTL-based eviction + cache-miss-refresh (cli-design §8) stays as a backstop.
- Per-verb integration carries the `invalidateBoard` call site in each command's success path. The call is documented in each command's source header so M17 group mutations + future board-shape mutations follow the same pattern. Implementation owns the "which file, exactly which line" detail — the contract pins the timing (see cli-design §8 call-site contract), not the module placement.

**Tests (pyramid)**
- Unit: each argv parser; cache `invalidateBoard` semantics (call → read returns live, not cache); per-type `--settings <json>` zod schema; the writable-allowlist warning policy.
- Integration: each verb happy path; each error path; cache-invalidation round-trip **per verb** — six round-trip cassettes covering the three M16 column verbs (`column-create` → `--set` against new column; `column-update` → `board describe` showing renamed column; `column-delete` → `--set` against removed column → `column_not_found`) PLUS the three M15 retrofit verbs (`board update` → `board describe` showing new name; `board archive` → `board describe` showing archived state; `board delete` → `board describe` → `not_found`). Each test asserts (a) the post-mutation read sees the live state (not the pre-mutation cache); (b) `meta.source: "live"` on the post-mutation read (not `"cache"` or `"mixed"`); (c) NO `stale_cache_refreshed` warning appears (otherwise the test passes for the wrong reason — the cache-miss-refresh path is the backstop, not the path under test).
- E2E: one happy path per verb (three spawns).

**Exit**
- All three verbs ship with envelopes pinned.
- Cache invalidation verified per verb — round-trip cassettes for column-create / column-update / column-delete + the M15 retrofit cluster (board update / archive / delete) all hit `meta.source: "live"` on the post-mutation read with no `stale_cache_refreshed` warning. The flagship test (`monday board column-create --type text --title "New"` followed immediately by `monday item set <iid> --board <bid> "New"=hello`) succeeds against fixtures without an intervening `monday cache clear` AND without the cache-miss-refresh path firing.
- New column types created through `column-create` round-trip through `board describe` and the appropriate write path: `--set` when `--type` is in `WRITABLE_COLUMN_TYPES`; `--set-raw` when the type is raw-writable but outside the friendly-translator allowlist; no write at all for read-only-forever or `files`-shaped types (the `column-create` warning reflects this).
- cli-design §8 eager-invalidation diff merged.

---

### M17 — Board groups

The smaller-but-still-cache-touching half of board structure (Codex
P1 round 2 caught my "no cache pressure" misclaim — the v0.1
board-metadata cache includes groups too, see `src/api/board-
metadata.ts:4` + `cli-design.md §8` cache layout). Five verbs that
mutate group shape; each invalidates the same board-metadata cache
M16 introduces `invalidateBoard(boardId)` for. Smaller-than-M16
because there's no per-type `--settings` schema work and no
column-shape implications, but the cache discipline carries over.

**Deliverables**
- `src/commands/board/group-create.ts` — `monday board group-create <bid> --name <n> [--position top|bottom] [--color <c>] [--dry-run]`. Calls `create_group`. **Calls `invalidateBoard(boardId)` post-success** (the cached `groups: [...]` list is now stale). Dry-run shape: `operation: "create_group"`, `board_id`, `name`, `position?`, `color?`.
- `src/commands/board/group-update.ts` — `monday board group-update <bid> <gid> [--name <n>] [--color <c>] [--position top|bottom] [--dry-run]`. Calls `update_group`. Idempotent. Cache-invalidating.
- `src/commands/board/group-archive.ts` — `monday board group-archive <bid> <gid> --yes [--dry-run]`. Calls `archive_group`. Destructive — `--yes` consistent with `item archive` / `board archive` (§8 decision 9). Cache-invalidating.
- `src/commands/board/group-duplicate.ts` — `monday board group-duplicate <bid> <gid> [--with-updates] [--dry-run]`. Calls `duplicate_group`. Cache-invalidating.
- `src/commands/board/group-delete.ts` — `monday board group-delete <bid> <gid> --yes [--dry-run]`. Destructive. Cache-invalidating.

**Tests (pyramid)**
- Unit: each argv parser.
- Integration: each verb happy path; each error path; `group-archive` / `group-delete` `confirmation_required` without `--yes`. **Cache-invalidation round-trip per verb** — `group-create` followed by `monday board groups <bid>` returns the new group without manual `cache clear` and without the cache-miss-refresh path firing. Same shape as M16's column-mutation round-trip test, asserting the eager-invalidation contract.
- E2E: one happy path per verb (five spawns).

**Exit**
- All five verbs ship with envelopes pinned.
- Archive consistency verified across `item` / `board` / `group` archive verbs (`--yes` mandatory; `confirmation_required` without).
- Cache-invalidation round-trip verified per verb (no stale `groups: [...]` after mutation).

---

### M18 — NDJSON streaming + 0.2.0 release prep

The polish + ship milestone. Mirrors M7's shape: streaming parity,
envelope-snapshot refresh, output-shapes update, README + CHANGELOG,
version bump.

**Deliverables**
- NDJSON streaming for `item search` (cursor-based via `paginate.onItem`) and `update list` (page-based via `walkPages` — Codex P2 found `update list` uses `walkPages`, not `paginate`; this milestone extends `walkPages` with an `onItem` streaming hook to reach parity with `paginate`'s contract). Both per-item and per-board `update list` variants. Trailer matches the §6.3 shape (`{"_meta":{...}}`).
- `tests/integration/envelope-snapshots.test.ts` — extends from 40 snapshots (M7) to ~70+. One per shipped command (every M8–M17 command adds one).
- `docs/output-shapes.md` — adds a `data` shape entry per shipped command. Snapshot-backed.
- `tests/e2e/published-tarball.test.ts` — assertion on noun count grows from "≥10" to "≥10" still (no new nouns; ~30 new verbs spread across the existing 5 mutation-receiving nouns + workspace/board/update/item).
- `tests/e2e/agent-flow.test.ts` — extended to exercise one v0.2 verb (likely `item create`) on the way through, so the smoke spans a v0.1 + v0.2 mix.
- `CHANGELOG.md` — `0.2.0` entry. Per-milestone what-shipped lines, **breaking changes section** (the M13 `--with-replies` default change for `update list` is the one breaking change; everything else is purely additive), upgrade notes (`unsupported_column_type` `deferred_to: "v0.2"` resolves for the firm row, slips for tentative).
- `README.md` — agent quickstart updated with `item create` + `item upsert` examples (the two verbs that change the "drive a backlog" story most).
- `package.json` — version bump to `0.2.0`.
- `cli-design.md` "Contract at a glance" section in CLAUDE.md updated; status block flips to "M0–M18 shipped"; new milestones table rows.

**Tests (pyramid)**
- Unit: NDJSON serialiser handles the new shapes (board-wide updates trailer, M13's `--with-replies`-gated nested shape).
- Integration: NDJSON streaming for `item search` (cursor) + `update list` (page-based with `walkPages.onItem`) — line count, trailer presence, no truncation.
- E2E: published-tarball test with the full v0.2 surface present.

**Exit**
- Every M8–M17 command has an envelope snapshot.
- `docs/output-shapes.md` covers every shipped command.
- `CHANGELOG.md` 0.2.0 entry written and reviewed.
- `npm pack` produces a working artefact with the full v0.2 surface; the published-tarball test passes.
- README quickstarts work end-to-end.
- Two-AI review pass cleared.
- Coverage floor held or raised (target: 96/95 branches/lines after M18).

---

## 4. Cross-cutting "definition of done" per command

v0.2 inherits v0.1's §4 checklist verbatim with four additions:

12. **Cache invalidation discipline.** Any command that mutates
    board metadata — column or group shape (M16, M17) — calls
    `invalidateBoard(boardId)` after a successful response. The
    v0.1 board-metadata cache includes both `columns: [...]` and
    `groups: [...]`; either kind of mutation invalidates. Documented
    in the command's source header. The integration test exercises
    a `mutate → read → see fresh state` round-trip AND asserts the
    cache-miss-refresh path did not fire (eager invalidation, not
    lazy refresh).
13. **Partial-success envelope shape pinned.** Multi-target commands
    that emit per-target results (`workspace add-users` /
    `remove-users`, `board add-users`, `update clear-all`) document
    the `data.results: [...]` slot in `output-shapes.md` and pin
    one envelope snapshot per verb. Envelope is always `ok: true`
    when dispatch ran (per §1 universal rule); top-level `error`
    only on whole-call failure.
14. **Every mutation verb supports `--dry-run`.** cli-design §9.2
    is binding — there are no exceptions. Verbs whose underlying
    Monday call isn't itself a column-value mutation (e.g.
    `archive_item`, `delete_workspace`, `like_update`) get a
    minimal dry-run shape: `operation: "<mutation_name>"` plus the
    primary IDs, no diff section. cli-design §6.4 receives a
    design diff per milestone listing the new `planned_changes.
    operation` values (§9 preconditions).
15. **Idempotency reasoning written down.** Every M9–M17 command's
    source header has a one-line idempotency rationale (yes / no /
    "yes when X branch fires") so a future agent reading the file
    doesn't have to reverse-engineer it from cli-design.md.

## 5. Testing strategy — the pyramid, made explicit

v0.2 inherits v0.1's §5 strategy verbatim. Three deltas:

### 5.1 New cassette categories

- **Cache-invalidation cassettes.** New shape: a sequence where the
  first interaction is a board-structure mutation, the second is a
  metadata read returning the post-mutation shape. The fixture
  transport's `interactions[]` ordering is sufficient — no new
  cassette schema field needed.
- **Partial-success cassettes.** For `add-users` / `remove-users`,
  the cassette returns Monday's per-user response array (some
  succeeded, some failed); the integration test asserts the
  `data.results: [...]` shape matches.
- **Cross-board move cassettes.** Source-board metadata + target-board
  metadata + the `move_item_to_board` response. Two metadata reads
  per cassette.

### 5.2 Error-code coverage map (§6.5 deltas)

| Code | Milestone | How produced |
|------|-----------|--------------|
| `tag_not_found` | M8 (if `tags` ships) | `--set tags=ghost` against an account-tag list cassette returning empty |
| `ambiguous_match` | M12 | `item upsert --match-by name` with a 2-row match cassette |

Existing v0.1 codes get new producers (deeper coverage):
- `usage_error` — M16 `column-create --settings <json>` with malformed JSON (caught at argv-parse, before any network call) AND with type-mismatched settings (e.g. `--type text --settings '{"labels":[]}'` — caught by the per-type zod schema for types in `WRITABLE_COLUMN_TYPES`); both fail fast at the argv boundary per the contract pin.
- `validation_failed` — M16 `column-create` with a well-formed but Monday-rejected `--settings` payload (e.g. exceeding Monday's per-type limits, server-side schema mismatches the CLI doesn't model). Surfaces only after the wire call fires, not at argv-parse.
- `forbidden` — M14/M15/M16/M17 admin-only verbs against a non-admin token.
- `confirmation_required` — every live destructive M10/M14/M15/M16/M17 verb without `--yes` AND without `--dry-run` (the `--dry-run` carve-out per §3.1 #7).

### 5.3 Coverage targets

Floor stays at **94% branches / 95% lines / functions / statements**.
M18's exit checklist targets **95% branches / 96% lines / functions
/ statements** — same shape as v0.1's late-milestone ratchets.

## 6. Risk register

Risks ranked by impact × likelihood (top = most pressing).
v0.2-specific risks added; v0.1 risks (token leakage, SDK drift,
abort handling, etc.) inherit unchanged.

| Risk | Impact | Mitigation |
|------|--------|------------|
| **`tags` / `board_relation` / `dependency` slip** — the three tentative M8 types may surface design cost (per-account tag-list complexity, cross-board ID resolution) that warrants a v0.3 slip. | High | M8 ships the three "trivial" types (`link`, `email`, `phone`) firm; the three tentative types each get a fixture-driven feasibility pass at M8 open. Slip decision is per-type and recorded in M8's post-mortem. The unsupported_column_type `deferred_to` row's value flips to `"v0.3"` for any slipped type. |
| **Cache-invalidation gaps** — a board-structure mutation in M16 forgets to call `invalidateBoard`, leaving stale metadata in the cache; subsequent `--set` against the new column fails with `column_not_found` despite the cache-miss-refresh M5a baked in. | High | `invalidateBoard` is a top-of-success-path call site for every M16 command; documented in the module header. Integration test exercises the round-trip per verb. The cache-miss-refresh path is the second line of defence — the test must assert no refresh fires when invalidation worked correctly (otherwise the test passes for the wrong reason). |
| **Partial-success envelope drift** — `data.results: [...]` shape wanders between `add-users` (M14), `add-users` (M15 board), `clear-all` (M13), and any future multi-target verb. | High | One shared envelope-builder (`buildPartialSuccessMutation`) reusing the existing `MutationEnvelope` (§1 universal rule: always `ok: true` when dispatch ran). One snapshot test per verb, one `docs/output-shapes.md` entry per verb. Decision 3 in §8 closes the question pre-M13; the builder ships in M13 and is reused unchanged in M14/M15. |
| **`item upsert` ambiguous-match design** — multiple match rows with conflicting column values force a "which to update?" question the CLI can't answer. | Medium | `ambiguous_match` returns deterministic — never silently update one of N. `details.candidates: [{id, name, ...}]` gives the agent the data to retry with a tighter `--match-by`. Match-by columns AND-combine, so adding a column narrows. |
| **`item upsert` race conditions across concurrent agents** — two agents observing zero matches at the same instant both branch to `create_item`; the agent's "idempotent" mental model is broken. | Medium | The CLI is sequential within one process (cli-design §9.3); concurrent agents are an inter-process question the v0.2 contract doesn't fully solve. `--match-by` should target a stable hidden key column (Monday's `item_id`-by-content hash, or an external-ID column the agent owns). The next call surfaces the duplicate as `ambiguous_match`, giving the agent recovery info. Concurrent-write protection through Monday's resource-locking mutations is a v0.4 candidate. Documented in `--help` and §6.5. |
| **Cross-board `item move` data loss** — Monday silently drops column values for unmatched columns on cross-board moves; the agent assumes preservation. | Medium | The CLI rejects unmatched columns with `usage_error` rather than letting Monday silently drop. `--columns-mapping <json>` is mandatory when source and target differ; without it, source-column-IDs-must-match-target-column-IDs is enforced pre-call. The dry-run output enumerates every column's planned mapping. |
| **`column-create --type` writable-allowlist drift** — agents create columns of types the CLI can't write to (e.g. `time_tracking` in v0.2), then immediately try to `--set` them. | Medium | `column-create` lints with a category-branched warning when `--type` is outside the v0.2 writable allowlist: raw-writable types point at `--set-raw`; read-only-forever flag no write path; `files`-shaped flag the v0.4 asset-upload deferral. Doesn't block — Monday accepts non-writable types — but warns explicitly per category. |
| **Permission-error verbosity** — Monday's `Permission denied` is a single string; the CLI surfaces `forbidden` but doesn't decompose into "which permission" (admin / owner / member). | Low | `forbidden` carries `details.required_permission: 'admin' | 'owner' | 'member'` when the message body parses; otherwise omitted. Best-effort. |
| **Update list `--with-replies` complexity** — Monday's reply tree can nest arbitrarily deep; the CLI must decide a depth cap. | Low | Cap at depth 1 (top-level + immediate replies). Deeper nesting → flatten as siblings of the root reply. Documented in cli-design §6.2; agents wanting full trees use `raw`. |
| **NDJSON streaming for `update list --board <bid>`** — board-wide updates may be very large; per-line emission needs back-pressure. | Low | The same `onItem` callback as M4's `item list --all`; vitest E2E covers a 1000-update fixture without OOM. |
| **`--set-raw` JSON injection** — agent passes a malformed JSON literal that contains a closing brace mid-string; the parser must not be susceptible to argv-shell quoting attacks. | Low | `JSON.parse` happens inside the CLI process after argv parsing; no shell expansion. zod boundary still applies (must parse to a `JsonObject`). Adversarial test fixtures: nested object, escaped quotes, Unicode in keys. |
| **Boolean filter DSL temptation** — agents start asking for OR / NOT in `--where` even before fixture demand justifies the design pass. | Low | Re-affirm the v0.2 stretch / v0.3 firm slot in `cli-design.md` §13. `--filter-json` is the v0.2 escape hatch and remains so. |
| **v0.2 surface size growth — coverage drift** — adding ~30 commands while holding 94% branch coverage requires every milestone to add tests at the same density v0.1 did. | Low | Coverage threshold gate in CI is the floor; M18's exit criteria explicitly raise the floor. Every per-milestone PR checks coverage delta in the review. |

## 7. v0.2 release exit checklist

The plan is "done" when **all** of the following pass:

- [ ] Every command in §1's in-scope table is implemented and registered. Target: ~30 new commands across `item` (6 new), `update` (8 new + 2 list shape expansions), `workspace` (5 new), `board` (6 lifecycle + 3 column structure + 5 group structure = 14 new), plus the `--set-raw` flag on existing `item set` / `item update`.
- [ ] Every command satisfies the §4 "definition of done" checklist (v0.1's plus v0.2's four additions).
- [ ] Every v0.2-active error code in `cli-design.md` §6.5 is producible and has a test (per §5.2 map). New codes: `tag_not_found` (if `tags` ships), `ambiguous_match`. Existing codes: deeper coverage per §5.2.
- [ ] `npm run typecheck && npm run lint && npm test` is green.
- [ ] Coverage ≥ 95% lines / functions / statements + 94% branches; M18 raises the floor to 96/95 if achievable.
- [ ] CI green on Node 22, 24, **and 26** (LTS lifecycle — check support windows at M18).
- [ ] `npm audit` reports no high/critical advisories.
- [ ] `monday --version` reports `0.2.0`. Verified via build + the M18 published-tarball E2E `--version` cross-check.
- [ ] `monday schema --json` validates as JSON Schema 2020-12. Pinned by `tests/e2e/schema.test.ts` (ajv compile of every shipped command's input + output schema).
- [ ] `monday schema --json` describes every shipped command. Pinned by `tests/e2e/published-tarball.test.ts` + `schema.test.ts` per-command.
- [ ] `monday config show --json` redacts the token. (Inherits v0.1 pin.)
- [ ] No emitted byte (across the integration suite) contains the literal `MONDAY_API_TOKEN` value. (Inherits v0.1 pin; redaction-hardening suite extended for new commands.)
- [ ] Agent-flow E2E green against fixtures, including one v0.2 verb (`item create`).
- [ ] Envelope contract test green for every command — every emitted JSON validates against its `outputSchema`, every `meta` skeleton present, no stdout pollution on failure. M18's `tests/integration/envelope-snapshots.test.ts` adds ~30 snapshots on top of the per-command checks.
- [ ] `tests/e2e/published-tarball.test.ts` passes.
- [ ] `docs/cli-design.md` "Contract at a glance" section in CLAUDE.md still matches shipped behaviour. Status block flipped to "M0–M18 shipped" + M8–M18 rows in milestones table.
- [ ] `docs/output-shapes.md` documents every command's `data` shape. New shapes: per M8–M18 command.
- [ ] `CHANGELOG.md` `0.2.0` entry written.
- [ ] `README.md`'s quickstart works for a new user — including the v0.2 "drive a backlog" example (`item create` + `item upsert` + `update reply`).

## 8. Decisions (open questions to close before M8)

These questions are open at v0.2-plan draft time. Each gets a decision
recorded here before M8 starts so implementation doesn't relitigate.
The two-AI review on this plan should land each one.

1. **Foundations-first vs agent-value-first ordering.** Closed: foundations-first (M8 before any new mutation). Re-touching M9–M17 when M8 lands later is roughly 5 milestones × test-suite extension cost; foundations-first eats one milestone of "no visible progress" but saves the rework. The visible-progress alternative (M9 first, M8 inserted later) is rejected.
2. **Stable error code count grows from 26 to 27 or 28.** Closed: `tag_not_found` (M8, only if `tags` ships firm — adds 1 to v0.1's 26), `ambiguous_match` (M12 — adds 1 unconditionally). Total: 27 if `tags` slips, 28 if both ship. A 29th slot reserved for a possible `cross_board_columns_unmatched` if M11's `usage_error` decoration proves insufficient — pencilled, not committed.
3. **Partial-success envelope shape — `data.results: [...]` vs new top-level `results`.** Closed: `data.results: [...]` reuses the existing mutation envelope. **The envelope is always `ok: true` when dispatch ran**, even when every per-target call inside the loop failed — the call's contract is "I ran the dispatch and here are the per-target outcomes." Top-level `error` (`ok: false`) is reserved for whole-call failure (couldn't reach API; OR — for resolver-fronted verbs like M14 `--users` add/remove — **no dispatchable target remains after parsing / resolution**, e.g. every `--users` token was an email AND every email failed lookup; refined from the M14 pre-flight Codex round-2 P1 — earlier shorthand was "all `--users` tokens failed resolution" which read as too-strict and would have aborted mixed numeric + ghost-email calls before dispatching the resolvable numerics). This avoids a §6.1 contract revision (Codex round-1 P1: my draft incorrectly said `ok: false` when all targets fail). Per-target record shape: `{<verb_target_id>: string, ok: true | false, error?: { code, message }}` where the id field is `user_id` for add-users / remove-users and `update_id` for `clear-all`. The shared `dispatchSequential` helper (`src/api/partial-success-mutation.ts`) accepts the field name as a parameter so the per-verb shape is self-documenting in JSON without forcing every consumer to adopt a generic `target_id`.
4. **`item upsert --match-by` accepts `name` or only column tokens?** Closed: accepts `name` as a special token (matches `item.name`). Justification: `name` is the most common match-by case for "is this item already there?" and forcing agents to first map name to a column ID feels wrong. Documented in cli-design §5.8 update.
5. **Cross-board `item move` default — strict (require mapping) or permissive (drop unmatched)?** Closed: strict. Monday's permissive default silently drops column values; the CLI rejects with `usage_error` and a `--columns-mapping` example. `--columns-mapping {}` (empty object) is the explicit "drop everything" opt-in.
6. **Cache invalidation — eager or lazy?** Closed: eager. Each M16 column-mutation + the M15 retrofit cluster (`board update` / `board archive` / `board delete`) calls `invalidateBoard(boardId)` post-success. The cache-miss-refresh path (cli-design §8) stays as a backstop. The on-disk cache file is **shared** between concurrent CLI processes (same `$XDG_CACHE_HOME/monday-cli/boards/<bid>.json`); `invalidateBoard` deletes the shared file via `clearEntry`'s `unlink`, so process A's invalidation does remove process B's cache entry. The missing primitive is **inter-process coordination** — without advisory locking or per-entry version stamping, concurrent processes race in three documented ways (stale-after-unlink reads, stale-write-after-invalidate, concurrent-mutation reorder; see cli-design §8). Inter-process coordination is a v0.3 candidate; the M16 contract is single-process-correct and best-effort cross-process. Requires a cli-design §8 design diff (§9 precondition).
7. **`column-create --type` allowlist enforcement.** Closed: validate against known Monday column types (any of the ~40-type set), warn (don't block) when outside `WRITABLE_COLUMN_TYPES`. Warning is **category-branched** per cli-design §5.3 escape-hatch contract — raw-writable types suggest `--set-raw`; read-only-forever flag that there's no write path at all; `files`-shaped types flag the v0.4 deferral. Blocking would be paternalistic — agents may legitimately create read-only columns.
8. **`update clear-all` — sequential delete or parallel?** Closed: sequential (mirrors M5b bulk update). Parallel waits for the v0.4 `--concurrency` flag.
9. **Archive `--yes` consistency across nouns.** Closed: every live archive verb requires `--yes` (with `--dry-run` carve-out per §3.1 #7). cli-design §3.1 says live destructive ops without `--yes` AND without `--dry-run` return `confirmation_required`; archive hides items/boards/groups and only Monday's UI can recover them within ~30 days, so it counts as destructive. The pre-M10 cli-design §4.3 surface showed `item archive` without `--yes`; M10's pre-flight precondition added it. Same for `group-archive` (M17 pre-flight). Codex P1 caught the inconsistency in my v0.2-plan first draft.
10. **M16 split — columns first, then groups.** Closed: M16 ships columns (`column-create/update/delete`) and **introduces the eager-invalidation contract** + `invalidateBoard(boardId)` helper. M17 ships groups (`group-create/update/archive/duplicate/delete`) and **reuses** the M16 invalidation contract — both column and group mutations invalidate the v0.1 board-metadata cache (the cache holds both `columns: [...]` and `groups: [...]`; an earlier draft of this decision claimed groups had no cache pressure — Codex round-2 P1 caught the misclaim). M18 absorbs what was the old M17 (NDJSON + release prep). Justification: cache invalidation is the highest-risk design surface in board-structure mutations; concentrating the *contract introduction* in M16 with three verbs keeps the round-trip tests reviewable. Codex round-1 P2 caught the over-stuffed M16 in the first draft.
11. **Every mutation supports `--dry-run` — no exceptions.** Closed: cli-design §9.2 is binding. Verbs whose underlying Monday call isn't a column-value mutation (e.g. `archive_item`, `delete_workspace`, `like_update`) get a minimal dry-run shape: `operation: "<mutation_name>"` plus the primary IDs, no diff section. cli-design §6.4 receives a per-milestone design diff listing the new `planned_changes.operation` values (§9 preconditions). Codex P1 caught the omission in my first draft.

## 9. Preconditions

Codex round-2 P2 split this section into hard M8 gates vs. per-
milestone gates. Each `cli-design.md` design diff goes through the
established two-AI review pattern (CLAUDE.md "Two-AI review for
non-trivial design decisions") before merge.

### 9.1 Before M8 starts (hard gates)

- [ ] Decisions 1–11 in §8 closed via the v0.2-plan two-AI review pass (no implementation until cleared).
- [ ] `cli-design.md` §5.3 step 4 + writer-expansion roadmap table receive a design diff for the v0.2 firm row (firm = `link`/`email`/`phone`; tentative = `tags`/`board_relation`/`dependency`). Required because M8's column-value translator additions are direct extensions of cli-design §5.3.
- [ ] `cli-design.md` §4.3 command-tree receives a design diff adding the M8 flags: `--set-raw <col>=<json>` on `item set` / `item update` / bulk `item update --where`. Required because the §4.3 surface is the binding command tree.

### 9.2 Before consuming milestone

Each is a hard gate before the named milestone ships, but doesn't
block M8 starting.

- [ ] **Before M9** —
  - cli-design §4.3 adds `--set-raw <col>=<json>` on `item create` (M8 reuse — M8's gate covers `item set/update`, but `item create` is M9's surface and needs its own §4.3 listing).
  - cli-design §6.4 adds `planned_changes.operation` values: `create_item`, `create_subitem` (decision 11). Codex round-3 P1 caught these missing from an earlier draft of §9.
- [ ] **Before M10** — cli-design §4.3 adds `--yes` on `item archive` (decision 9); cli-design §6.4 adds `planned_changes.operation` values: `archive_item`, `delete_item`, `duplicate_item` (decision 11).
- [ ] **Before M11** — cli-design §6.4 adds `planned_changes.operation` values: `move_item_to_group`, `move_item_to_board` (decision 11).
- [ ] **Before M12** —
  - cli-design §4.3 adds `--set-raw` on `item upsert` (M8/M9 reuse).
  - cli-design §5.8 (item upsert idempotency) updated to pin `name` as a valid `--match-by` token (decision 4) AND document the concurrent-agent race caveat (sequential-retry idempotent only).
  - cli-design §6.5 adds `tag_not_found` + `ambiguous_match` (26 → 27 if `tags` slips, 28 if both ship; reserve 29 for a possible `cross_board_columns_unmatched`).
- [x] **Before M13** — **shipped: `bb7ee17 docs(m13): pre-flight cli-design contract for update mutations + partial-success envelope`**
  - cli-design §4.3 adds `--with-replies` on `update list` AND notes the default-behaviour change (current default populates `replies`; new default is empty unless flag set). Tagged as breaking in §7 / CHANGELOG (M18).
  - cli-design §6.4 adds the partial-success envelope shape: `data.results: [...]` slot pinned, always-`ok: true` when dispatch ran, top-level `error` reserved for whole-call failure, per-record id-field name parameterised by the calling verb (decision 3).
  - cli-design §6.4 adds `planned_changes.operation` values: `edit_update`, `delete_update`, `like_update`, `unlike_update`, `pin_to_top`, `unpin_from_top`, `clear_all_updates`.
- [x] **Before M14** — **shipped: `3fe1d58 docs(m14): pre-flight cli-design contract for workspace lifecycle`** + three pre-flight Codex rounds (`57fa844` / `a1904f8` / `2086953`) before implementation began. M14 itself shipped across `ccd7f1e` … `2b74321` (R29 lift); see §3 M14 status block + §21 post-mortem.
  - cli-design §4.3 adds `workspace create/update/delete/add-users/remove-users`; cli-design §6.4 adds `planned_changes.operation` values: `create_workspace`, `update_workspace`, `delete_workspace`, `add_users_to_workspace`, `delete_users_from_workspace`.
  - **R37 + R38 shipped in the M13 → M14 cleanup window** — `2182ded refactor(r37+r38): lift Update projection helper + UPDATE_FIELDS_FRAGMENT`. One consolidated commit lifted (a) `projectMutationUpdate` (R28 sibling for `Update`; 5 consumers, two-export seam — `projectMutationUpdate` for the four full-projection sites + `assertUpdateMutationPresent` for clear-all's `{ id }`-only wire shape) and (b) `UPDATE_FIELDS_FRAGMENT` (mirror of `ITEM_FIELDS_FRAGMENT`; 6 consumers). 1900 tests passed byte-identical pre-lift vs post-lift; helper file at 100/100/100; project branches at 95.43% (above the 94/95/95/95 floor).
  - **R29 shipped at M14 close** — `2b74321 refactor(r29): lift destructive-verb confirmation gate into api/destructive-gate`. Workspace delete arrived as the 5th consumer; the helper accepts already-parsed `globalFlags` to preserve the M10 round-1 P2 gate-before-`resolveClient` ordering invariant in the type signature itself. All 5 sites (item archive / item delete / update delete / update clear-all / workspace delete) migrate; 1957 tests pass byte-identical post-lift.
- [x] **Before M15** — **shipped: `a3b03c3 docs(m15): pre-flight cli-design contract for board lifecycle`** + four pre-flight Codex rounds (`62934e3` / `27c66f3` / `b222d23` / `080946b`) before implementation began. M15 itself shipped across `d7c342a` … `4756269` (R41 lift); see §3 M15 status block + §23 post-mortem.
  - cli-design §4.3 adds `--template <bid>` on `board create` plus full block-comment annotations for all six board verbs (`create` / `update` / `archive` / `delete` / `duplicate` / `add-users`) covering wire shapes, idempotency, dry-run, destructive gates.
  - cli-design §6.4 adds `planned_changes.operation` values: `create_board`, `update_board`, `archive_board`, `delete_board`, `duplicate_board`, `add_users_to_board`.
  - **Cross-cutting contract amendments** introduced by the M15 pre-flight (round-1 through round-3 fixes) — call them out so M15 close docs them as shipped:
    - cli-design §6.1 universal envelope widens `data` to `<resource | array | verb-specific JSON>` with `board duplicate`'s `{ board, is_async }` wrap + partial-success consumers as the documented examples.
    - cli-design §6.4 generic mutation paragraph mirrors the §6.1 widening — single-target verbs typically use `<resource>` projection but verb-specific wrappers are allowed when wire shape carries fields the projection doesn't model.
    - cli-design §6.4 board-duplicate live envelope pinned as `data: { board: <projection>, is_async }` per Monday's `BoardDuplication` SDK type, with readiness-check guidance for `is_async: true`.
    - cli-design §9.1 `delete_*` row split into wire-level (re-deleting converges) vs CLI-level (re-run surfaces `not_found`); per-verb prose stays the source of truth for retry semantics.
    - cli-design §4.3 + §6.4 `board update` post-mutation read pinned as force-live (cache-bypass) so the success envelope can't emit stale `data`; the dry-run preflight preview is the only cache-allowed leg.
    - cli-design §6.4 board-archive / board-update / board-duplicate dry-run shapes carry a cache-staleness caveat — cache-sourced previews may lag up to TTL; `--no-cache` forces a live preflight when freshness is critical.
- [x] **Before M16** — **shipped: `c0efab5 docs(m16): pre-flight cli-design contract for board columns + eager invalidation`** — seven pre-flight Codex rounds folded into the single pre-flight commit (rounds 1-7 surfaced 0 P1 + 16 P2 + 6 P3 cumulative; closed at round 7 with 0 P1 + 0 P2 + 0 P3). Diverges from M14/M15's "one commit per Codex round" pattern by rolling all round fixes into the pre-flight commit; the trade-off is fewer git landmarks for the pre-flight arc but a cleaner working-tree review for the implementation milestone that follows.
  - cli-design §4.3 adds `--settings <json>` + `[--dry-run]` on `board column-create` plus full block-comment annotations for all three column verbs (`column-create` / `column-update` / `column-delete`) covering wire shapes, idempotency, dry-run, destructive gates, and post-success invalidation. Three load-bearing wire-shape clarifications surfaced during the pre-flight:
    - `create_column`'s wire argument for type-specific config is **`defaults: JSON`**, NOT `settings_str: String!` (`settings_str` is the READ-side serialisation on `Column`; `defaults` is the WRITE-side input on `create_column`). The CLI flag stays `--settings <json>` for agent ergonomics; the dry-run / live wire payload echoes the input as `defaults`.
    - `column-update` is per-attribute fan-out across **two** wire surfaces: `change_column_title(board_id, column_id, title)` for `--title`, and `change_column_metadata(board_id, column_id, column_property: ColumnProperty, value: String)` with `column_property: description` for `--description`. The `ColumnProperty` enum (SDK 14.0.0) carries only `title` / `description`; `change_column_metadata` could equivalently set the title, but the CLI routes `--title` to the more specific `change_column_title` surface. Multi-flag invocations fan out N sequential wire calls — same pattern as `update_board`.
    - `column-update`'s success-envelope `data` projects from the trailing per-attribute wire-call's response (Monday's column mutations return `Maybe<Column>` post-mutation, so the trailing call's response is authoritative for both fields). Distinguishes column-update from `board update`, which forces-live a final whole-board read because its per-attribute wire calls return only the slice that changed. Both verbs share the partial-application caveat (no transaction across per-attribute calls; per-field calls earlier in the sequence have already committed when a later call fails).
  - cli-design §6.4 adds `planned_changes.operation` values: `create_column`, `update_column`, `delete_column`. Column-create's `settings` slot echoes the agent's `--settings <json>` argv input (which becomes the wire `defaults: JSON` argument). Column-update's preflight read sources from the v0.1 board-metadata cache (`meta.source: "live" | "cache"`) and pins the same partial-application caveat as board-update. Column-delete is destructive-no-read (mirrors `item delete` / `update delete` / `workspace delete` / `board delete`); there is no `column-archive` shape because Monday has no `archive_column` mutation.
  - cli-design §8 (Caching) receives a design diff for **eager invalidation on board-structure mutations** (decision 6). Current §8 only documents TTL eviction + cache-miss-refresh; the new contract adds: every M16 column mutation + the M15 retrofit cluster (`board update` / `board archive` / `board delete`) calls `invalidateBoard(boardId)` post-success, with TTL + refresh paths remaining as backstops. The helper is exported from `src/api/cache.ts` (the existing `evictBoardMetadata` in `src/api/board-metadata.ts` is the precedent — `invalidateBoard` lifts the export under the contract-aligned name). Two ordering invariants pinned, split by leg-count: single-leg verbs invalidate AFTER success-envelope `data` projection (skip on error path); fan-out verbs (column-update; M15 `board update`) invalidate ONCE after the full per-attribute loop settles, conditional on at least one per-call leg having succeeded — handles whole-call success AND whole-call partial-application failure with one rule. The on-disk cache file is **shared** across processes (eager invalidation deletes the shared file via `clearEntry`'s `unlink`); the missing primitive is inter-process coordination — three named race shapes documented (stale-after-unlink reads, stale-write-after-invalidate, concurrent-mutation reorder). Inter-process coordination explicitly deferred to v0.3. Monday's server-side cache is out-of-scope. Covers both M16 (columns) AND M17 (groups) since the v0.1 cache layout includes `groups: [...]` too (Codex round-2 P1).
  - **Cross-cutting contract amendments** introduced by the M16 pre-flight (round-1 through round-4 fixes across cli-design §3.1, §6, §8, §9.1, and v0.2-plan §3 / §5.2 / §8 decision 6) — call them out so M16 close docs them as shipped:
    - cli-design §9.1 idempotency table adds a row for `change_column_title` / `change_column_metadata` (yes — same input leaves same column metadata; pinned via the per-verb `column-update` annotation).
    - cli-design §8 backstop-signal clarification: the `meta.source: "mixed"` + `stale_cache_refreshed` warning is ONLY the cache-miss-refresh backstop signal — when eager invalidation worked correctly, the next read sees a clean cache miss → live fetch with `meta.source: "live"` and no warning. Integration tests assert the "clean miss" shape on the eager-invalidation happy path so a silent regression to "backstop saved us" doesn't pass for the wrong reason (Codex round-1 F4).
    - cli-design §8 retrofit cluster widens to **three** M15 verbs: `board update` (force-live + invalidation, two layers), `board archive` (cached `state` flips active → archived; needs invalidation), `board delete` (cache must drop; otherwise a same-process `board describe` reads a phantom board until TTL). Other M15 verbs explicitly excluded with reasoning: `board create` / `board duplicate` (no pre-existing cache to invalidate); `board add-users` (cached `boardMetadataSchema` doesn't surface subscribers, deferred to v0.3 if subscribers join the projection). Codex round-1 F2 caught the gap.
    - cli-design §8 fan-out invalidation ordering split between single-leg verbs (invalidate AFTER success-envelope `data` projection) and fan-out verbs (invalidate ONCE after the full per-attribute loop ends, gated on at least one successful per-call leg — generalises cleanly to N-leg fan-out). Codex round-1 F3 caught the original prose's contradiction between "after data committed" + "after FIRST successful per-attribute call".
    - cli-design §8 cross-process race-model rewrite: the on-disk cache file IS shared between processes (eager invalidation deletes the shared file); the missing primitive is inter-process coordination, not file-ownership. Three named race shapes documented: stale-after-unlink reads, stale-write-after-invalidate, concurrent-mutation reorder. Cross-process coordination explicitly deferred to v0.3. Codex round-1 F5 caught the original "process-local" framing.
    - cli-design §4.3 column-create `noncanonical_column_type` warning gets a stable `details: {column_type, category, suggested_write_path}` shape with `category` ∈ `"raw_writable" | "read_only_forever" | "files_shaped"` (stable enum, SemVer-minor to add, SemVer-major to remove). `--settings` per-type schema validation pinned at the contract level (per-type zod schemas exist; argv-parse fails with `usage_error` carrying `details: {column_type, expected_keys?, actual_keys?, hint}`); the per-type schema field sets are owned by M16 implementation rather than enumerated in the contract. Codex round-1 F6 surfaced both gaps.
    - cli-design §4.3 column-delete `--dry-run` carve-out: dry-run bypasses the `--yes` confirmation gate (mirrors `item archive` / `item delete` / `board archive` / `board delete` precedent — dry-run is non-executing). Codex round-1 F7.
    - cli-design §4.3 column-update wire-shape clarification: `change_column_metadata`'s `column_property` and `value` arguments are both optional at the wire (SDK 14.0.0 `MutationChange_Column_MetadataArgs`); the CLI always supplies both for `--description`. Codex round-1 F1.
- [ ] **Before M17** — cli-design §4.3 adds `--yes` on `group-archive` (decision 9); cli-design §6.4 adds `planned_changes.operation` values: `create_group`, `update_group`, `archive_group`, `duplicate_group`, `delete_group`. cli-design §8 invalidation diff already covers M17 from M16's gate.
- [ ] **Before M18** — release-prep updates only (CLAUDE.md status, README quickstart, CHANGELOG). No new design diffs.

The earlier draft of §9 conflated all of these as "M8 preconditions"
(Codex round-2 P2 caught the mis-scoping). Splitting them lets M8
start as soon as 9.1 clears, rather than blocking on every milestone's
design diff up front. The "Before M9" sub-section was a round-3
correction (Codex round-3 P1) — an earlier draft of the split had
M9's `create_item` / `create_subitem` operations in a closing-
paragraph note rather than an actual list item.

---

> **Post-mortem sections** (per-milestone, mirroring v0.1-plan.md
> §11–§21) are appended below as each milestone ships. The first
> entry is **§10 — M8 post-mortem**.

---

## 10. M8 post-mortem — what Codex caught after implementation

M8 shipped writer-expansion + `--set-raw` across three atomic
commits on `main` (`ad2aba7` link/email/phone friendly translators,
`d0a123d` raw-write helpers + dry-run plumbing, `6f5162f` command
wire-up). The milestone-close Codex pass returned **0 P1, 6 P2, 2
P3** in a single round — a clean review compared to M6's six-round
close arc. Verdict: **Y on round 1**, fix-up commits land in the
M8 close sweep alongside this post-mortem.

### Findings table

| Code | Severity | Area | Finding |
|------|----------|------|---------|
| F1 | P2 | dry-run engine | `--set-raw` translator catch folded only the current resolution's warnings, dropping cumulative warnings from prior `--set` legs. M5b R19 contract violation. |
| F2 | P2 | resolution ordering | Translation ran inline with resolution across dry-run + live single + live bulk. `--set X=bad-value --set-raw X={...}` surfaced the translator `usage_error` instead of the cli-design §5.3 line 961-972 mutual-exclusion `usage_error`. |
| F3 | P2 | bulk source aggregation | On a cold-cache bulk run, `meta.source` correctly promoted to `mixed` (live metadata fetch + cache-hit per-token resolution) but `meta.cache_age_seconds` reported `meta.cacheAgeSeconds` (`null` for live fetches), contradicting the `mixed` source. |
| F4 | P2 | argv-parse ordering | `item update` single resolved board ID via `ItemBoardLookup` BEFORE parsing `--set-raw`. Without `--board`, malformed JSON paid a network round-trip — diverged from `item set` and the bulk path's argv-boundary contract. |
| F5 | P2 | docs drift | `docs/v0.2-plan.md` §3 M8 still described the pre-slip scope (six translators, `WRITABLE_COLUMN_TYPES` → 13, tentatives slipping to v0.3). Reality: 3 firm types shipped, total 10 writable, tentatives still `v0.2`. |
| F6 | P2 | tests | Friendly link/email/phone translators landed without `match_variables`-pinned cassette tests through `item set`. Wire-shape regressions wouldn't fail loudly — only the link envelope had a snapshot pin (without wire-shape pin). |
| P3-1 | P3 | comments | `column-values.ts` module header still said v0.1 had no `--set-raw` and listed link/email/phone in the unsupported v0.2 branch. Stale post-M8. |
| P3-2 | P3 | comments | `board doctor`'s `v0_2_writer_expansion` message said v0.2 "will add it (plus --set-raw)" — `--set-raw` already exists post-M8. |

### What pattern explains the findings

All six P2s cluster around a single failure mode: **multiple legs
that should compose coherently were composed by inlining the
translation step into the resolution loop.** The clean fix in F2
(separate resolve / cross-token-check / translate passes) makes
F1 fall out for free (each catch site folds the cumulative
`warnings` array, not the per-token slice) and reshapes the
update.ts paths into three parallel implementations of the same
contract.

F3 is the same shape one layer up: meta-resolution ran live, then
per-token resolution ran cache, and the aggregate logic only
tracked source — not age. The fix mirrors the single-item path's
`aggregateCacheAge` running max.

F4 and F5 are scope-of-fail-fast bugs: the argv-boundary contract
("malformed input fails before any network call") wasn't applied
uniformly across `item update`'s single path; the doc drift was
the v0.2-plan being written for the pre-slip scope and never
revised when the M8 implementation slipped tags / board_relation
/ dependency to a follow-up.

### Single load-bearing lesson

**Resolve everything before translating anything.** A multi-token
mutation surface has three implicit phases: (1) resolve every
column token to a real column ID (cache, refresh, archived check,
warnings), (2) detect duplicate-target collisions across tokens
(same-token, same-resolved-id), (3) translate each input value
into a wire payload. M5b's single-token surfaces conflated
phases 1 and 3 because there was nothing to compare against;
M8's two-flag surface (`--set` + `--set-raw`) made the
conflation visible because the same column can be addressed two
ways and the cross-token check needs both legs resolved before
it can fire.

This is the same lesson v0.1's M3 logged about cache + live
provenance ("aggregate source after every leg, never before"),
generalised to "aggregate every reusable property after every
leg, then act on the aggregate." For M9 (`item create`) and
later mutation surfaces with column payloads: keep resolution
and translation as separate passes by default; if a future
optimisation collapses them, fence the collapse with explicit
asserts that the cross-token duplicate check still fires
pre-translation.

### Fix-up commits

- Resolution-before-translation refactor across `src/api/dry-run.ts` + `src/commands/item/update.ts` (single + bulk paths). Cumulative warning fold across every typed throw.
- Bulk envelope's `cacheAgeSeconds` switches from `meta.cacheAgeSeconds` to a tracked `aggregateCacheAge` (max across resolution legs).
- `item update` single moves `splitSetExpression` + `parseSetRawExpression` before `resolveBoardId`.
- `docs/v0.2-plan.md` §3 M8 rewritten for the post-slip scope.
- New tests: 1 dry-run unit (cumulative-warning preservation), 5 integration (3 mutual-exclusion variants, 1 cold-cache bulk source-aggregation, 5 wire-shape pins for `link`/`email`/`phone` through `item set`).
- Stale comments updated in `src/api/column-values.ts` + `src/commands/board/doctor.ts`.

Coverage held at 98.04 / 94.34 / 97.95 / 98.25 (statements / branches / functions / lines), well above the 95 / 94 / 95 / 95 floor. Test count: 1558 → 1568.

## 11. M9 post-mortem — what Codex caught after implementation

M9 shipped `monday item create` (top-level + classic-only subitem
creation, single round-trip exit gate per cli-design §5.8).
Implementation took three commits (refactor + engine + command)
plus two fix-up rounds. Codex review: round 1 N (1 P1 + 3 P2),
round 2 partial Y (P1/P2-#1/P2-#3 fixed, P2-#2 partial), round 3
**Y, zero findings**.

### Findings table

| # | Severity | Finding | File |
|---|----------|---------|------|
| 1 | P1 | Cache-stale archived columns surface as `validation_failed`, not `column_archived` (F4 remap missing on create catch arm) | `src/commands/item/create.ts` |
| 2 | P2 | Subitem dry-run/live source aggregation ignored pre-planner legs (parent lookup, parent-board metadata) — `meta.source: "none"` lied about wire calls | `src/commands/item/create.ts` |
| 3 | P2 | Live mutation fixtures didn't pin `match_variables` — regressions that drop `columnValues`, mis-map position enum, or send top-level columns to `create_subitem` would slip through | `tests/integration/commands/item-create.test.ts` |
| 4 | P2 | `executeCreateSubitem` silently dropped `parent_id` from the envelope when Monday returned `parent_item: null`, even though argv carried the parent ID | `src/commands/item/create.ts` |
| 5 | P2 (round 2) | Live `--set <long_text>=value` re-wrap not pinned by any fixture — friendly path's `{text: <value>}` bundling could regress without test failure | `tests/integration/commands/item-create.test.ts` |
| 6 | P3 (round 2) | F4 remap test was a placeholder (live resolution + permissive expectation), not a real cache-sourced regression pin | `tests/integration/commands/item-create.test.ts` |

### What pattern explains the findings

Two patterns, both familiar from M0 / M5b / M8 post-mortems:

**Pattern 1 — "the argument that the gate is unnecessary."** P1 (#1)
came from a deliberate decision to skip the `validation_failed` →
`column_archived` remap on create. The reasoning seemed solid:
"the resolver already gates on archived (`includeArchived: true` +
explicit throw), so a Monday `validation_failed` here is a true
value-shape failure, not a stale-cache miss." But the cache *can*
say active when Monday has since archived — that's the exact
scenario the F4 helper exists for. M5b ran into a cousin of this
when bulk failures dropped the remap (M5b finding #3, fixed in
M5b cleanup re-review #2). The lesson: when a contract helper
exists across multiple call sites (item set, item update single +
bulk, and now item create), the default is to wire it through;
the burden is on the caller to argue why it shouldn't apply, and
"the gate above caught everything I thought of" is rarely a
strong enough argument.

**Pattern 2 — "match_operation_name covers wire shape."** P2 (#3)
was M8's exact lesson recurring: integration tests that match only
on `operation_name` accept any wire payload, so a regression in
`columnValues` shape, position enum mapping, or parent-id routing
slips through. M8's fix-up commit wired `match_variables` across
item set / item update fixtures; M9 inherited the testing helpers
but didn't inherit the discipline. The round-2 P2 follow-up (long_text
re-wrap pin) is a sub-case of the same pattern — even with
`match_variables` on most fixtures, missing one column type (long_text)
leaves the most-divergent wire shape unpinned. Lesson: when
`bundleColumnValues` (or any shared shape projection) has a
type-specific branch, every type with a divergence needs its own
fixture pin, not just the common case.

**Sub-pattern — "the envelope source aggregator never lies twice."**
P2 #1 (preflight source aggregation) was the third instance of "the
envelope source must reflect every wire leg that fired" pattern
post-M3. M3's original error was inferring source from warnings
alone (cache hit → live envelope). M5b's bulk path repeated it on
the cache-age slot. M9's twist was a network leg that ran *before*
the dry-run engine (parent lookup) — the engine's `source: 'none'`
short-circuit is honest about the engine's own legs but knows
nothing about pre-planner work. Lesson: every async function that
might fire a wire call in the action path needs to surface its
source contribution to the caller; the action layer folds them.

### Single load-bearing lesson

**"When a fix-up helper exists, the default is wire it through and
argue exceptions, not skip it and argue the gate."**

M5b's R19 lift made `foldResolverWarningsIntoError` +
`maybeRemapValidationFailedToArchived` the contract for every
mutation surface. M9's create skipped both initially under the
reasoning "the resolver gates on archived already" — which was
true but didn't cover stale-cache. The wire-it-through default
costs nothing when the helper is already extracted; the explicit-
skip default costs a P1 finding because the reasoning is always
local while the contract is global.

This is symmetric with M8's "always aggregate before acting"
pattern — both are arguments for keeping the established
discipline by default and reasoning explicitly when departing.

### Fix-up commits

Round 1 → round 2 (`1a005d8`):

- F4 remap wired into the create catch arm (mirrors item set's
  shape verbatim — every translated column id, the active
  `resolveBoardId`, the aggregated source).
- `resolveCreateMode` now returns `{mode, preflightSource,
  preflightCacheAgeSeconds}` and the action body folds these into
  both dry-run and live envelopes via two new merge helpers
  (`mergeSourceWithPreflight`, `mergeCacheAgeWithPreflight`).
- Four fixtures gain `match_variables` (top-level multi-set, both
  `--position` enum maps, subitem `--set` with `parentItemId` +
  subitems-board column ids, subitem no-`--set` with
  `columnValues: null`, `--group` pass-through).
- `executeCreateSubitem` always sets `parent_id` from argv;
  test that previously blessed omission now asserts presence.
- `agent-flow` E2E timeout bumped 5s → 10s (4 spawns × ~1.2s
  + `item create` registration nudge tipped it past the default).

Round 2 → round 3 (`9f5364e`):

- New live test pins `--set <long_text>=value` bundling as
  `{text: "value"}` inside `create_item.column_values` (the rule
  `bundleColumnValues` inherits from M5b's update path).
- F4 remap test rewritten in the cache-seeding pattern item-set
  uses (seed cache via separate read, cache hit on resolution,
  fail mutation as `validation_failed`, refresh confirms archived,
  expect `column_archived` with `details.remapped_from`).

Coverage stayed above the 95 / 94 / 95 / 95 floor across both
rounds. Final: 97.89 / 94.03 / 97.89 / 97.96 (1647 tests passing).

### Spec-gap backfill

Two spec gaps surfaced and remain non-blocking — flagged in this
post-mortem rather than blocking the M9 close:

1. **`hierarchy_type` enum literal**. Code uses `"multi_level"`
   (cli-design §2.8 + §6.4 prose); the actual Monday API enum
   value isn't pinned in cli-design. The implementation gates on
   the exact string `"multi_level"`; if Monday's enum is named
   differently (`"multilevel"`, `"hierarchy"`), classic boards
   would still pass through without misrouting (the rejection
   would not fire, and create_subitem would behave per Monday's
   server-side rules). Adding a fixture pin against a real
   Monday response is M10+ work.

2. **Subitems-board ID derivation**. `deriveSubitemsBoardId`
   parses `subtasks.settings_str.boardIds[0]` from the parent's
   board metadata. The parse is defensive (handles missing
   column / null settings_str / malformed JSON / empty
   boardIds), but if a future Monday API adds a new shape (e.g.
   `subitems.boardIds` or a single `boardId`), the helper
   would surface a usage_error. Pre-flight pin via real
   Monday cassette also pending M10+.

Both gaps are documented in the source comments + cli-design
M9 carve-in and unblock at the v0.3 multi-level work or M16
(board columns), whichever lands first.

## 12. Refactor backlog — R20–R26 (post-M9 audit)

> **All seven landed in 2026-05 ahead of M10.** See "Shipped"
> annotations on each entry below; per-commit messages capture
> the why/how. Net diff: ~1900 LOC of duplication collapsed into
> seven new shared modules totalling ~660 LOC; coverage stayed
> at 98.82 / 95.17 / 99 / 98.91, well above the 95/94/95/95
> floor. M10 picks up the helpers cleanly.

Same R-class shape v0.1's §15 + §17 used: each entry is a
duplication-trigger that's already fired (≥3 consumers, the
established threshold) and would land cleanly before the next
mutation milestone (M10) so the new `archive` / `delete` /
`duplicate` commands inherit shared helpers rather than copy
the patterns. Audit was triggered post-M9 ship; lifts shipped
in the M9→M10 cleanup window.

The R-class threshold is "lift on the third consumer" per
`v0.1-plan.md` §17 R-timing rule. Every item below has at
least three consumers today; the M5b post-mortem (`v0.1-plan
§19`) and the M8 post-mortem (`§10` here) both flagged that
delaying R-lifts past the trigger costs review-finding time
disproportionate to the lift itself.

### R20 — Three-pass column resolution helper

**Shipped: 3239110.** New `src/api/resolution-pass.ts`
exports `resolveAndTranslate`. All five call sites now use the
helper. Two consistency normalisations landed alongside: archived-
column message wording unified ("Monday rejects writes against
archived columns"); translate-time catch unified to `instanceof
MondayCliError` (the M5b R19 contract — pre-fix the dry-run
paths only caught `ApiError`, missing translator `UsageError`s
that should fold). Net –1188 / +134 LOC across the four touched
files.

**Sites (5):**
- `src/api/dry-run.ts planChanges` (M5a — single-item dry-run)
- `src/api/dry-run.ts planCreate` (M9 — create dry-run)
- `src/commands/item/update.ts` action (M5b single-item live)
- `src/commands/item/update.ts runBulk` (M5b bulk live)
- `src/commands/item/create.ts` action (M9 live)

Each implements the same ~80-90 LOC three-pass discipline:
resolve every `--set` token → resolve every `--set-raw` token →
cross-token duplicate-resolved-id check → translate each, with
cumulative resolver-warning fold across every typed throw. M8's
"resolve-all-then-translate" invariant (§10 finding) required
fixing 4 of these in one commit; M9's F4-remap fix needed all 5.

**Proposed shape:** `src/api/resolution-pass.ts` exporting
`resolveAndTranslate({client, boardId, setEntries, rawEntries,
dateResolution, peopleResolution, env, noCache}) → {resolved,
translated, resolvedIds, warnings, source, cacheAgeSeconds}`.
Each consumer keeps its specific work (item read for
planChanges, no item read for planCreate, mutation dispatch
for live paths).

**Risk:** highest of the seven (largest LOC churn). Land
behaviour-preservingly — every existing test should pass
byte-for-byte. Best landed before M10 starts.

### R21 — `mergeSource` / `mergeCacheAge` lift

**Shipped: 4fa856f.** New `src/api/source-aggregator.ts`
exports `mergeSource` + `mergeSourceWithPreflight` +
`mergeCacheAge`. dry-run.ts swaps imports for its private
helpers; update.ts renames `mergeSourceForRemap` → `mergeSource`
and replaces five inline cache-age `if (cacheAge !== null && (agg
=== null || cacheAge > agg)) agg = cacheAge` blocks with one-line
`agg = mergeCacheAge(agg, cacheAge)`; create.ts drops three
local helpers. `mergeSourceWithPreflight` carries the 4-state
'none' collapse rule create.ts needed; lifted ready for the next
preflight-then-planner consumer.

**Sites (4):**
- `src/api/dry-run.ts` `mergeSource` + `mergeCacheAge`
  (private)
- `src/commands/item/update.ts` `mergeSourceForRemap` +
  inline `Math.max` cache-age
- `src/commands/item/create.ts` `mergeSourceLeg` +
  `mergeSourceWithPreflight` + `mergeCacheAgeWithPreflight`
- Inline `Math.max` patterns in update single + bulk

`update.ts:925` carries the explicit comment **"If a third
consumer arrives, lift to a shared module then."** — the
third arrived with M9 create.

**Proposed shape:** `src/api/source-aggregator.ts` exporting
`mergeSource(current, next)` + `mergeCacheAge(current, next)`,
optionally a `SourceAggregator` class for the running-aggregate
pattern. Re-export from `dry-run.ts` for back-compat.

**Risk:** very low. Same logic across 4 sites; no behavioural
change.

### R22 — `splitSetExpression` lift

**Shipped: c221bba.** New `src/api/set-expression.ts` exports
`splitSetExpression(raw)`. Three identical 12-line copies
collapse into one import. The M8 sibling `parseSetRawExpression`
in `raw-write.ts` stays where it is — its JSON-parsing concerns
aren't duplicated, and consolidating it would either widen this
helper's contract or extract a tiny private split for
parseSetRawExpression to wrap; neither pulls weight today.

**Sites (3):** `set.ts:204`, `update.ts:308`, `create.ts:378` —
identical 12-line first-`=` split with `usage_error` on
malformed input.

**Proposed shape:** `src/api/set-expression.ts` exporting
`splitSetExpression(raw) → {token, value}`. The
`parseSetRawExpression` parser in `raw-write.ts` (M8) shares
the same first-`=` rule and could either move into the same
module or import from it.

**Risk:** trivial. Preempts M10 / M11 / M12 fan-out.

### R23 — Item-board lookup helper

**Shipped: a5c1480.** New `src/api/item-board-lookup.ts`
exports `ITEM_BOARD_LOOKUP_QUERY` + `ITEM_PARENT_LOOKUP_QUERY` +
`boardLookupResponseSchema` + `parentLookupResponseSchema` +
`lookupItemBoard({client, itemId, label?, detailKey?})` +
`lookupItemBoardWithHierarchy(...)` + `resolveBoardId({client,
itemId, explicit})`. set / clear / update / create.ts each shed
~70 LOC of duplicated GraphQL + schema + resolver. Error labels
caller-supplied (`Item` / `Parent item` / `--relative-to item`)
preserve the disambiguating phrasing in the consumer's voice.
Null-board message wording unified across consumers ("its board"
+ "the board is deleted"); no test exercises the wording.

**Sites (4):** `set.ts:93/151/226`, `clear.ts:81/126`,
`update.ts:106/173/324`, `create.ts` (`ItemBoardLookup` for
relative-to + `ItemParentLookup` for parent's
`hierarchy_type`).

Each has the same triple: GraphQL string + `boardLookupResponseSchema`
zod parse boundary + `resolveBoardId(client, itemId, explicit)`
helper with the `not_found` / null-board defensive logic.

**Proposed shape:** `src/api/item-board-lookup.ts` exporting:
- `lookupItemBoard(client, itemId): Promise<{itemId, boardId}>`
  (M5b shape)
- `lookupItemBoardWithHierarchy(client, itemId): Promise<{itemId,
  boardId, hierarchyType}>` (M9 shape — extra field)
- Shared zod schemas

**Risk:** low. M10 archive / delete / duplicate will need this
again — preempt the 5th + 6th copy.

### R24 — Resolution-context builder

**Shipped: d413df9.** New `src/api/resolution-context.ts`
exports `buildResolutionContexts({client, ctx, globalFlags})`.
Each of the four sites collapses 12 lines of `dateResolution` +
`peopleResolution` construction into one helper call. The helper
is the natural seam for v0.3's `me`-token caching.

**Sites (4):** `set.ts:352`, `update.ts:465` (single) +
`update.ts:1290` (bulk), `create.ts:931`. Each builds the same
12-line `dateResolution` + `peopleResolution` block closing over
`ctx.clock` + `ctx.env.MONDAY_TIMEZONE` + `client` +
`globalFlags.noCache`.

**Proposed shape:** `src/api/resolution-context.ts` exporting
`buildResolutionContexts({client, ctx, globalFlags}):
{dateResolution, peopleResolution}`. One-line replacement at
each call site.

**Risk:** trivial. Bonus: opens a clean spot for v0.3's
`me`-token caching (memoize once per call rather than per-token)
without touching consumers.

### R25 — Use shared `CreateMode` from `dry-run.ts`

**Shipped: ddb1671.** Local `CreateModeFromCommand` in
`commands/item/create.ts` dropped; one-line import of `CreateMode`
from `dry-run.ts` replaces it. The "import dance" comment that
justified the parallel declaration is no longer load-bearing.

**Sites (1+1):** `commands/item/create.ts` defines
`CreateModeFromCommand` locally (line ~810) that's
structurally identical to `CreateMode` exported from
`dry-run.ts`. Comment in code says "Locally typed so the
helper signature doesn't need an import dance" — but the
import dance is one line, and the parallel declaration is
drift waiting to happen.

**Proposed fix:** drop `CreateModeFromCommand`, import
`CreateMode` from `dry-run.ts`. Update `resolveCreateMode`
+ `ResolveCreateModeResult.mode` to use the shared type.

**Risk:** trivial. M9 self-introduced drift; one-line fix
prevents it.

### R26 — Mutation catch-arm helper (`foldAndRemap`)

**Shipped: 1770c23.** `src/api/resolver-error-fold.ts` extends
with `foldAndRemap({err, warnings, client, boardId, columnIds,
env, noCache, resolutionSource})`. Five mutation surfaces (set
/ clear / update single + bulk / create) each shed ~25 LOC of
fold + F4 remap. Bulk's per-item-progress decoration stays
bulk-specific: the bulk path calls `foldAndRemap` first, then
attaches `applied_count` / `applied_to` / `failed_at_item` /
`matched_count` before re-throwing.

**Sites (5):** `set.ts`, `clear.ts`, `update.ts` single +
bulk, `create.ts`. Each ~25 LOC of:

```ts
catch (err) {
  if (err instanceof MondayCliError) {
    const folded = foldResolverWarningsIntoError(err, warnings);
    const columnIds = translated.map((t) => t.columnId);
    if (columnIds.length === 0) throw folded;
    throw await maybeRemapValidationFailedToArchived(folded, {
      client, boardId, columnIds, env, noCache,
      resolutionSource: aggregateSource ?? 'live',
    });
  }
  throw err;
}
```

The Codex M9 P1 finding (cache-stale archived columns
surfacing as `validation_failed`) was caused by `item create`
**skipping this exact catch arm**. Lifting reduces the surface
where a future command can forget to wire it.

**Proposed shape:** extend `src/api/resolver-error-fold.ts`
with `foldAndRemap({err, warnings, client, boardId, columnIds,
env, noCache, resolutionSource})`. Bulk's per-item-progress
decoration stays a bulk-specific concern (call `foldAndRemap`
first, then decorate).

**Risk:** low. Pins the M5b R19 contract as a single function
call per mutation surface.

### Recommended ordering when scheduled

The trivial cluster (R22 + R24 + R25) is essentially free and
could land as one tidy-up commit. R21 + R23 are mechanical but
larger. R20 is the biggest payoff but also the largest churn —
best landed **before M10 starts** so M10's three new mutation
commands inherit the helper rather than duplicate the pattern.
R26 is the Codex-finding-prevention pattern; nice companion to
R20 but lands cleanly on its own.

**What actually shipped (2026-05).** R22 → R25 → R24 → R23 → R21
→ R26 → R20, one atomic commit per R. Each with quality gates
(typecheck + lint + 1647 tests) green between commits. The
ordering above held with minor variation: R22 + R24 + R25 went as
three separate commits rather than one bundled cleanup, which
made the per-R commit-message rationale + diff easier to review.
R20 + R26 went after the smaller lifts so the biggest-churn diff
landed on top of the now-clean foundation. The pattern from
v0.1's M2.5 refactor pass holds: lifts that pre-empt the next
milestone's duplication pay back disproportionately.

## 13. M10 post-mortem — what Codex caught after implementation

M10 shipped across two sessions (the post-M9.5 working pattern of
"one tight cluster per session, atomic commit per verb, Codex
round-trip per session"). **Session A** (commits `67c9825`
archive + `53b5e77` delete + `59ece13` docs sweep + `3a19a15`
round-1 P2 fix) landed the two destructive lifecycle siblings.
**Session B** (commit `36ae3cf` duplicate + this post-mortem +
final docs sweep) landed the third sibling and closed M10.

### Findings table

| # | Severity | Session | Finding | File |
|---|----------|---------|---------|------|
| 1 | P2 | A round 1 | Confirmation gate ran AFTER `resolveClient()`; missing-token + missing-`--yes` masked `confirmation_required` (exit 1) as `config_error` (exit 3) | `src/commands/item/archive.ts` + `src/commands/item/delete.ts` |
| 2 | spec-gap | A pre-impl | cli-design §4.3 line 532 missing `--yes` on `monday item archive` (delete already had it) | `docs/cli-design.md` |
| 3 | spec-gap | B docs | cli-design §6.4 missing dry-run shapes for archive + delete + duplicate (carried over from Session A's docs sweep) | `docs/cli-design.md` |
| 4 | P2 | B round 2 | §3 M10 deliverables list claimed "all three reuse `runByIdLookup` ... cache + live for the lookup leg" — drift from a pre-implementation draft; none of the three actually uses `runByIdLookup` and no leg consults the cache | `docs/v0.2-plan.md` line 196 |
| — | Y / zero findings | B round 1 + B round 3 | duplicate.ts cleared round 1 (Codex confirmed `data.duplicated_from_id` is the right extension, `with_updates` is pinned in live and dry-run paths, `idempotent: false` matches create semantics); round 3 confirmed the round-2 P2 fix closed the §3 M10 prose drift | — |

### What pattern explains the findings

**Pattern 1 — "the gate's contract is unconditional; ordering
matters."** Session A's round-1 P2 came from a natural ordering
mistake: every M5b mutation surface (`item set` / `clear` /
`update` single + bulk) reads `globalFlags` off the
`resolveClient` return value. Copying that idiom into archive +
delete worked for the `--yes` + valid-token case, but
`cli-design §3.1 #7`'s gate is **unconditional** — a destructive
op without `--yes` must exit 1 with `confirmation_required`
regardless of the environment. With the gate ordered after
`resolveClient`, a missing token short-circuited to `config_error`
before the gate could fire. The fix moved the gate ahead of the
client resolution (mirroring `commands/raw/index.ts:435`'s
pre-network ordering precedent for `--dry-run` + raw mutations).
The integration tests didn't catch it because `useItemTestEnv()`
always injects `MONDAY_API_TOKEN`; the round-1 fix added a
dedicated regression test that drops the token from `env` so the
ordering trap can't recur.

The pattern is recognisable: "the established idiom is for one
context (token-validated mutations), the new context has different
contract preconditions (gate is unconditional), copying the idiom
loses the new context's invariant." Same shape as M9's Pattern 1
("the argument that the gate is unnecessary") — both are about
copying a familiar pattern without re-checking whether its
implicit assumptions still hold.

**Pattern 2 — "pre-implementation spec gaps deserve their own
backfill commit."** Session A's spec-gap (#2) was the cli-design
§4.3 missing `--yes` on archive. Caught pre-implementation,
backfilled in commit `a3c45aa` (a docs-only commit before the
feature commits) so the contract was up to date before the code
landed. Session B carried forward a §6.4 gap (#3) — none of
archive / delete / duplicate's dry-run shapes were pinned in §6.4
even though all three landed during M10. Backfilled in Session B's
docs sweep (this post-mortem's accompanying docs commit). The
takeaway: **whenever a milestone implements a spec-gap-flagged
shape, the docs sweep at milestone close gets the §6.4 update —
not at the end of the v0.2 release**. M10 surfaced the rule;
M11+ inherit it.

**Sub-pattern — "Session A's round-1 fix anticipated Session B
correctly."** The Session A round-1 P2 fix (gate before
resolveClient) doesn't apply to duplicate because duplicate has
no gate to mask — it's creative, not destructive. The CLAUDE.md
session-handoff prompt explicitly called out the pattern as
applicable "if you find yourself adding any pre-network gating",
and Session B's implementation correctly identified it doesn't
apply. The pattern still served as a positive signal: the
session-handoff captured the most recent near-miss so the
next session could check "does this still apply?" rather than
re-discover the trap.

### Single load-bearing lesson

**"A new verb's argv shape does not imply a new verb's contract
preconditions."**

archive + delete + duplicate share `<iid>` + envelope projection
+ idempotency-knob + parseRawItem boundary — the obvious
similarity. They diverge on the gate condition (destructive →
`--yes` required; creative → no gate), the wire-leg count
(archive + delete: 1 leg; duplicate: 2 legs live, 1 leg dry-run),
and the envelope `data` shape (archive + delete: pure
projection; duplicate: projection + `duplicated_from_id`). When
implementing a verb that **looks** like a sibling, the work is
identifying which preconditions the sibling's implementation
encodes implicitly and re-checking each one against the new
verb's contract.

Session A's round-1 P2 was the case where one such precondition
("gate fires unconditionally") got dropped on copy. Session B's
clean round 1 was the case where the same audit caught the
inverse — duplicate's lack of gate means it doesn't need the
ordering fix Session A introduced.

Session B's round-2 P2 was the **doc-side mirror**: the §3
deliverables list was a pre-implementation draft (written before
Session A) that promised "all three reuse `runByIdLookup` ...
cache + live for the lookup leg". Reality after Session A: none
of the three uses `runByIdLookup` (the helper emits a get-shaped
envelope; §6.4 dry-runs need `data: null` + `planned_changes`),
and no leg ever consults the cache. The drift sat through Session
A's docs sweep + Session B's docs sweep before round 2 caught it.
**Generalisable rule: a milestone's §13 post-mortem and §3
deliverables list describe the same contract from different
angles — the docs sweep audits one against the other.**

This is symmetric with M9's lesson ("when a fix-up helper exists,
default to wire it through and argue exceptions"). M9's pattern
is about **shared helpers**; M10's pattern is about **shared
shapes**. Both warn against the same failure mode: assuming the
new context inherits the old context's implicit invariants —
Session B's round 2 generalised the lesson to documents (assuming
the §3 promise still describes what shipped after the
implementation lands).

### Fix-up commits

**Session A round 1 → close-out** (`3a19a15`):

- archive.ts + delete.ts: `parseGlobalFlags(program.opts(),
  ctx.env)` ahead of `resolveClient()`; gate condition
  (`!dryRun && !yes`) fires immediately, then `resolveClient()`
  only on the live + dry-run paths.
- New regression test per verb: drives the action with
  `MONDAY_API_TOKEN` deliberately removed from env. Pre-fix
  exits 3 / `config_error`; post-fix exits 1 /
  `confirmation_required`.
- The "rejects without --yes" tests now also assert
  `meta.source === 'none'` — the gate-error envelope's
  provenance reflects that `resolveClient()` (which would have
  committed `'live'` via `ctx.meta.setSource('live')`) never
  ran. Pinned in both files.

**Session A docs sweep** (`59ece13`):

- output-shapes.md gained archive + delete sections (live +
  dry-run shapes documented).
- architecture.md gained `commands/item/archive.ts` +
  `commands/item/delete.ts` entries.
- CLAUDE.md gained the M10 (Session A) milestones-table row +
  status paragraph.

**Session A pre-implementation backfill** (`a3c45aa`):

- cli-design §4.3 line 532: `monday item archive <iid>` →
  `monday item archive <iid> --yes` plus the eight-line comment
  block (destructive context, `--dry-run` carve-out, idempotency).
  Mirrors the existing `--yes` annotation on `delete`.

**Session B implementation + docs sweep** (`36ae3cf` + this
commit):

- `src/commands/item/duplicate.ts` (~280 LOC) — third lifecycle
  sibling. Two-leg live, single-leg dry-run, `data.duplicated_
  from_id` extension, `with_updates` plumbed through to the
  mutation variables (pinned via `match_variables` in two
  fixtures).
- 14 integration cases mirroring archive's coverage plus
  duplicate-specific assertions (lookup-leg vs mutation-leg
  not_found, `with_updates` echo in dry-run, parseRawItem drift
  on both legs, `idempotent: false` knob pin against
  `itemCreateCommand.idempotent` for the parallel-false
  semantics).
- 2 new envelope snapshots (live with `duplicated_from_id`,
  dry-run with `with_updates`); command_count snapshot 39 → 40.
- E2e timeout bumps: `agent-flow.test.ts` 10s → 15s +
  `schema.test.ts` defaults → 15s. CLAUDE.md predicted both
  ("bump to 15s if it flakes"); the schema test was on the
  default 5s, which had no headroom for any Session B growth.
- cli-design §4.3 line 531 narrative comment for duplicate
  (creative-not-destructive, two-leg live, single-leg dry-run,
  `idempotent: false`, `data.duplicated_from_id` echo).
- cli-design §6.4 backfill for **all three** M10 dry-run shapes
  (archive + delete + duplicate). The §6.4 gap predates Session
  A; landing all three together avoids a future inconsistency
  where one verb's shape is documented and the others aren't.
- output-shapes.md gained an `item duplicate` section parallel
  to archive's + delete's, plus the live envelope's
  `duplicated_from_id` shape.
- architecture.md gained a `commands/item/duplicate.ts` entry
  capturing the two-leg live / single-leg dry-run divergence
  from archive + delete.
- CLAUDE.md milestones table consolidated `M10 (Session A)` →
  `M10` (full row covering both halves); status paragraph +
  "Remaining v0.2 milestones" updated to start at M11.

**Session B round 2 → round 3** (`a6a2e14`):

- §3 M10 deliverables list bullet 4 ("All three reuse
  `runByIdLookup` ... cache + live for the lookup leg") was
  drift from a pre-implementation draft — none of the three
  shipped verbs actually uses `runByIdLookup`, and no leg
  consults the cache. Replaced with the actual shared contract
  (`ITEM_FIELDS_FRAGMENT` + `parseRawItem` + `projectItem`) plus
  per-path wire-leg counts and the always-`"live"` source rule.
- The §13 post-mortem already described the actual contract
  correctly; the round-2 fix closed the inconsistency between
  §3's "what we'll build" prose and §13's "what we built" prose.
- Pattern note: the §3 draft was written **before** Session A
  shipped; M10 didn't update it during either session's docs
  sweep. Generalisable rule for M11+: when a milestone's §13
  post-mortem describes the contract, audit the §3 deliverables
  list against the post-mortem prose **as part of the docs
  sweep**, not as a separate later task. Otherwise the §3
  promise drifts from §13's reality.

Coverage stayed above the 95 / 95 / 95 / 95 floor across all
three rounds. Final: 98.95 / 95.47 / 99.34 / 99.05 (1697 tests
passing). archive.ts + delete.ts + duplicate.ts each at 100%
lines + 100% branches.

### Spec-gap backfill

One spec gap surfaced during Session B and was backfilled in the
docs sweep:

1. **cli-design §6.4 dry-run shapes for archive + delete +
   duplicate**. Pre-fix, §6.4 documented column-mutation,
   comment-create, raw-GraphQL, and item-create shapes — but
   not the M10 lifecycle verbs. Codex round-1 (Session B)
   flagged this as the only docs follow-up. Backfilled the
   shapes for all three verbs alongside the duplicate-specific
   `data.duplicated_from_id` extension prose. The fix unblocks
   M11's `item move` shape (which will diverge from M10's
   simple `{operation, item_id, item}` triple by carrying
   target group / board IDs).

No spec gaps from Session A surfaced after its close, beyond the
pre-implementation `--yes` backfill that landed before the feature
commits.

## 14. Refactor backlog — surfaced post-M10

Three R-class lift candidates surfaced auditing the three M10
lifecycle commands (`archive.ts` + `delete.ts` + `duplicate.ts`)
post-Codex round 3. Each had reached the "lift on third consumer"
threshold; M11 (`item move`) and M14 / M15 / M17 (board +
workspace + group lifecycle) add 2–5 more consumers each. **R27 +
R28 shipped in the M10 → M11 cleanup window** (commits `0248b9d` +
`a5c2a6f`); R29 stays deferred to M15 (the third-consumer trigger
fires when board archive / delete lands).

Same R-class shape v0.1 §15 + §17 + v0.2 §12 used: each entry is
a duplication-trigger with at least three consumers today (the
established threshold per `v0.1-plan.md` §17), would land cleanly
before the next mutation milestone (M11), and has been
risk-assessed against the bar of behaviour-preserving refactor
(every existing test passes byte-for-byte).

The R-timing rule v0.1-plan §17 codified: **two copies is
acceptable (shape isn't proven yet); three copies is the lift
trigger; four copies is overdue**. R27 and R28 fire on three
consumers each (M5b's set/clear/update count separately for R28
because they share the M5b helper layer). M5b's R19 (resolver-
warning fold) and M9.5's R20–R26 are precedents for "lift before
the next mutation milestone" — both pre-empted M9 / M10's verbs
inheriting helpers cleanly rather than copying patterns.

### R27 — Source-item dry-run reader

**Shipped: 0248b9d.** New `src/api/item-source-read.ts` exporting
`readSourceItemForDryRun({client, itemId, operationName})` →
`Promise<ProjectedItem>`. The helper builds the GraphQL query string
internally (body byte-identical across the three M10 verbs; only the
operation name differs); the three M10 dry-run paths each collapse
from ~12 LOC to one helper call. The `ITEM_*_READ_QUERY` constants
and `Item*ReadResponse` interfaces are deleted from each file —
owned by the helper now. M11's `ItemMoveRead` will inherit the seam
on day one. Net –108 / +41 LOC across three command files plus 84
LOC of new module; new helper at 100% lines + 100% branches via the
existing per-verb integration tests. Zero behaviour change — every
existing test passes byte-for-byte.

**Sites (3 today, 4 by M11):**
- `src/commands/item/archive.ts` action — dry-run path
- `src/commands/item/delete.ts` action — dry-run path
- `src/commands/item/duplicate.ts` action — dry-run path
- `src/commands/item/move.ts` (M11, projected) — dry-run path
  will share the shape

Each implements the same ~12 LOC sequence:

```ts
const readResponse = await client.raw<ItemXReadResponse>(
  ITEM_X_READ_QUERY,
  { ids: [parsed.itemId] },
  { operationName: 'ItemXRead' },
);
const items = readResponse.data.items;
const first: unknown = Array.isArray(items) ? items[0] : undefined;
if (first === undefined || first === null) {
  throw new ApiError(
    'not_found',
    `Monday returned no item for id ${parsed.itemId}`,
    { details: { item_id: parsed.itemId } },
  );
}
const projected = projectItem({
  raw: parseRawItem(first, { item_id: parsed.itemId }),
});
```

The query body is byte-identical across all three M10 verbs (just
`items(ids:) { ${ITEM_FIELDS_FRAGMENT} }`); only the operation
name differs (`ItemArchiveRead` / `ItemDeleteRead` /
`ItemDuplicateRead`). The op-name divergence exists for fixture
cassettes + Monday's request-log telemetry (so wire calls are
distinguishable per verb), so the helper takes it as a parameter
rather than collapsing it.

**Proposed shape:** new `src/api/item-source-read.ts` exporting
`readSourceItemForDryRun({client, itemId, operationName})`
→ `Promise<ProjectedItem>`. The helper builds the query string
internally (parameterised by `operationName`), runs the read,
applies the `Array.isArray + first` shape check + `not_found`
throw, and returns the projected snapshot. Callers go from ~12
lines to one line:

```ts
const projected = await readSourceItemForDryRun({
  client,
  itemId: parsed.itemId,
  operationName: 'ItemArchiveRead',
});
```

The op-name parameter doubles as documentation for the wire-log
telemetry; making it a required argument prevents copy-paste
where one verb accidentally uses another's op name.

**Risk:** low. Pure refactor — three call sites, identical
behaviour, all covered by the M10 integration tests (parse
boundary cases, null-result cases, malformed-item cases). No
test changes needed; the existing tests pass byte-for-byte
through the helper.

**Net diff estimate:** –~30 LOC across three files +
~20 LOC of new module = **net –10 LOC** with the
duplication eliminated. Smaller than R20–R26's typical net
reduction (~270 LOC each) because the duplicated block is
small per-site; the value is consistency + the M11 free-
inheritance, not LOC-savings.

### R28 — Live-mutation projection with null-not_found

**Shipped: a5c2a6f.** New `src/api/item-mutation-result.ts` exporting
`projectMutationItem({raw, itemId, errorCode, errorMessage})` →
`ProjectedItem`. The helper centralises the null check + the
`details: { item_id }` envelope shape + the `parseRawItem +
projectItem` chain; each call site supplies its own typed error
parts. **Why parameterised on `errorCode` + `errorMessage` rather
than the originally proposed `mutationName`-only signature.** M5b
chose `internal_error` + "Monday returned no item payload from the
mutation for id ..." semantics for an empty-payload mutation success
(rare server-side glitch, abnormal); M10 chose `not_found` +
"Monday returned no item from <op_name> for id ..." for Monday's
idiomatic null-for-missing-or-no-access response (typed agent-
recovery story). Both are correct for their semantics, both are
pinned by integration tests, and both must survive the lift
byte-for-byte. The helper preserves both shapes; each call site's
error-code + message stays identical to pre-lift. M5b sites use a
local `(raw, itemId) → ProjectedItem` wrapper so the existing
`executeMutation` arms + bulk per-item dispatch stay untouched
(post-projection error decoration — fold-and-remap, validation_failed
remap, applied_count attachment — continues to operate exactly as
before). Net –100 / +74 LOC across six command files plus 73 LOC of
new module; new helper at 100% lines + 100% branches. Zero
behaviour change — every existing test passes byte-for-byte (1697
tests, including the per-cohort null-payload assertions in
`item-{set,clear,archive,delete,duplicate}.test.ts` which continue
to pin internal_error / not_found respectively).

**Sites (6 today across M5b + M10, 7+ by M11+M14+M15):**
- `src/commands/item/archive.ts` action — live path (line 228)
- `src/commands/item/delete.ts` action — live path (line 222)
- `src/commands/item/duplicate.ts` action — live path (line 264)
- `src/commands/item/set.ts` (M5b — line 470)
- `src/commands/item/clear.ts` (M5b — line 327)
- `src/commands/item/update.ts` (M5b — line 581)
- `src/commands/item/move.ts` (M11, projected)
- `src/commands/board/archive.ts` / `delete.ts` (M15, projected —
  same shape against `Board` not `Item`)

Each implements the same ~10-LOC sequence after a successful
mutation `client.raw(...)`:

```ts
const raw = response.data.<mutation_output_key>;
if (raw === null || raw === undefined) {
  throw new ApiError(
    'not_found',
    `Monday returned no item from <mutation_name> for id ${itemId}`,
    { details: { item_id: itemId } },
  );
}
const projected = projectItem({
  raw: parseRawItem(raw, { item_id: itemId }),
});
```

Six consumers today is **double the lift threshold**. The M5b
sites pre-date the M10 lifecycle cluster but share the exact
shape, so the helper picks them up retroactively when it lands.

**Proposed shape:** new `src/api/item-mutation-result.ts`
exporting `projectMutationItem<T>({raw, mutationName, itemId})`
→ `ProjectedItem`. Caller passes the wire result + the mutation
name (for the error message — agents reading the not_found error
key off `details.item_id`, but the human-readable message names
the mutation that failed) + the item id (for both the error
details and the parseRawItem fallback hint). Callers collapse
from ~10 lines to one:

```ts
const projected = projectMutationItem({
  raw: response.data.archive_item,
  mutationName: 'archive_item',
  itemId: parsed.itemId,
});
```

**Risk:** low-medium. The M5b call sites have richer
error-handling context (some live inside the resolver-error-fold
catch; some preserve `details.applied_to` for bulk paths); the
helper must be careful not to swallow that context. **Mitigation:**
the helper handles only the `null → not_found` shape;
post-projection error decoration (resolver-warning fold,
applied_count attachment) stays at the call site. The helper's
return type is `ProjectedItem`, so the caller chains its own
fold-and-remap if needed.

**Net diff estimate:** –~50 LOC across six sites + ~25 LOC of new
module = **net –25 LOC** plus a uniform error message format
across every `Item`-shaped mutation. M11 + M15 inherit the
helper rather than copy.

### R29 — Destructive-verb confirmation gate

**Sites (2 today, 5+ by M15 / M17):**
- `src/commands/item/archive.ts` action (line 147-163)
- `src/commands/item/delete.ts` action (line 141-158)
- `src/commands/board/archive.ts` (M15, projected)
- `src/commands/board/delete.ts` (M15, projected)
- `src/commands/board/group-archive.ts` (M17, projected)
- `src/commands/workspace/delete.ts` (M14, projected — also
  destructive)

Each implements the same `parseGlobalFlags` + `if !dryRun &&
!yes` + `ConfirmationRequiredError` block, diverging only in:

1. The verb name in the message (`archive` / `delete`).
2. The hint text (Monday's 30-day window phrasing differs
   between archive's "no `unarchive` mutation" and delete's
   "trash + no `restore` mutation").
3. The detail key (always `item_id` for now, will be
   `board_id` / `group_id` / `workspace_id` for M14 / M15 /
   M17).

**Two consumers today** is one short of the lift trigger, so
this is a "schedule for after M14 / M15 lands the third site"
candidate rather than an immediate lift. The Codex round-1 P2
ordering trap (gate must fire before `resolveClient`) is the
pattern's load-bearing invariant; whatever helper lifts the
gate must enforce that ordering or push it onto the caller via
an unmistakable signature.

**Proposed shape:** new `src/api/destructive-gate.ts`
exporting `enforceDestructiveGate({globalFlags, verb, target,
detailKey, hint})`. Caller invokes it before `resolveClient`;
helper either throws `ConfirmationRequiredError` or returns
silently. Pseudocode:

```ts
enforceDestructiveGate({
  globalFlags,                  // already parsed by caller
  verb: 'archive',              // → "monday item archive ..."
  target: parsed.itemId,        // → "...12345 would archive..."
  detailKey: 'item_id',         // → details.item_id: '12345'
  hint: 'archive is destructive — Monday retains archived ...',
});
```

The caller still parses `globalFlags` before invoking the helper
(forced by the function signature taking the parsed flags
rather than re-parsing them — this preserves the round-1 P2
ordering: the parse happens before any `resolveClient`).

**Risk:** medium — the round-1 P2 ordering trap is exactly the
kind of invariant a helper can hide. **Mitigation:** the helper
takes `globalFlags` as already-parsed (caller can't accidentally
order the parse after `resolveClient`) and is colocated with a
documentation block citing Codex M10 round-1 P2 so the next
contributor reads the rationale before changing the helper's
shape.

**When to schedule:** **defer until M15 board archive / delete
lands**, at which point the third consumer arrives and the lift
trigger fires. Until then, two copies of a 17-LOC block is
acceptable per the v0.1-plan §17 R-timing rule. Drop a note in
M14 / M15's deliverables that the lift happens in their
docs-sweep arc.

### Ordering — as shipped

R27 and R28 landed together in the M10 → M11 cleanup window
(commits `0248b9d` + `a5c2a6f`), one atomic commit per R, mirroring
M9.5's R20–R26 cadence. Both are pure refactors — every existing
test passed byte-for-byte through both lifts. R29 defers to M15
(third-consumer trigger fires when board archive / delete lands).

The "lift before the next mutation milestone" pattern v0.2's
M9.5 R20–R26 cleanup pioneered carried forward: M11's `item move`
now inherits both helpers rather than copying the patterns (both
single-board and cross-board paths get the dry-run read + live-
mutation projection seams from day one).

---

## 15. M11 post-mortem — SDK shape discovery + scope adjustments

M11 shipped on `main` in commit `daa9d22` (feat) — `monday item move`
across two transports: same-board group move via `move_item_to_group`
and cross-board move via `move_item_to_board`. Net new: `src/api/
column-mapping.ts` (the JSON parse boundary) + `src/commands/item/
move.ts` (the verb). 40 new tests across three layers (13 mapping
unit + 8 planner unit + 18 integration + 1 e2e). Coverage 98.97 /
95.35 / 99.36 / 99.07 above the 95/95/95/95 floor.

Two scope adjustments versus the §3 M11 deliverables — both surfaced
during pre-implementation reading of Monday's SDK 14.0.0 typings
(`node_modules/@mondaydotcomorg/api/dist/esm/index.d.ts:551` +
`:2156`), captured here as design diffs that landed in the M11
implementation commit + this post-mortem rather than as round-trip
Codex review findings.

### Discovery 1 — `ColumnMappingInput` is strictly ID-to-ID

**SDK shape:** `type ColumnMappingInput = { source: ID!, target?: ID }`.
Two slots, both ID-shaped. **No value slot.**

**§3 M11 deliverable:** Accept `--columns-mapping <json>` whose values
are either a target ID string OR `{id, value?}` records, where
`value` re-runs through M5a/M8's column translator on target
metadata. Implies a CLI-side feature (the SDK doesn't surface it
directly).

**Implication:** Supporting value-overrides requires a non-atomic
post-move `change_multiple_column_values` mutation against the
target board. That introduces partial-failure semantics — move
succeeded but the value-override leg failed — with no precedent in
the v0.1+v0.2 envelope shapes. Bulk `item update --where`'s
`applied_count` / `applied_to` decoration is per-item; this would
be cross-leg, a different shape.

**Decision:** Defer value-overrides to v0.3. Ship the simple
`{<src>: <target>}` form (string-to-string) which maps directly to
Monday's `columns_mapping` parameter. The parser rejects the rich
`{id, value?}` form loud with a `usage_error` carrying
`details.hint: "deferred to v0.3 — fire monday item set <iid>
<target>=<value> post-move when you need them"`. Agents
hand-crafting the rich shape see a typed error pointing at the
workaround.

**Rationale.** Atomic single-mutation flow is simpler, matches
Monday's wire shape exactly, and avoids a cross-leg partial-failure
envelope shape. The simple form is enough for the cross-board move
contract — agents who need value translation chain `move` +
`item set` themselves until v0.3 ships the atomic primitive.
v0.3 candidate: a `monday item move --columns-mapping` +
`change_multiple_column_values` two-leg flow with explicit
"two-mutation, partial-success" envelope shape OR Monday adding a
value slot to `ColumnMappingInput` (not signalled).

### Discovery 2 — `move_item_to_board(group_id: ID!)` is mandatory

**SDK shape:** `type MutationMove_Item_To_BoardArgs = { board_id:
ID!, group_id: ID!, item_id: ID!, columns_mapping?: ..., ...}`.
`group_id` is required (not nullable, not InputMaybe).

**cli-design §4.3 line 530 pre-M11:** `monday item move <iid>
--to-group <gid> | --to-board <bid> [--columns-mapping <json>]`
(the `|` indicates mutual exclusion).

**Implication:** The pre-M11 surface form `monday item move <iid>
--to-board <bid>` (no `--to-group`) had no destination on the
target board. Either the CLI had to auto-pick a default group on
the target board (implicit + surprising) or `--to-group` had to be
mandatory for both forms.

**Decision:** Make `--to-group <gid>` required for both same-board
AND cross-board. cli-design §4.3 line 530 updated during the M11
docs sweep: `monday item move <iid> --to-group <gid> [--to-board
<bid>] [--columns-mapping <json>]`. Same-board = `--to-group`
alone; cross-board = `--to-group --to-board`; `--to-board` alone
(no `--to-group`) → `usage_error` ("Monday requires a destination
group").

**The mutex now lives between `--columns-mapping` and `--to-board`'s
absence** — passing `--columns-mapping` without `--to-board` is a
user mistake (mapping is cross-board-only) so the action throws
`usage_error` rather than silently dropping the flag.

**Rationale.** Required-`--to-group` matches Monday's wire shape
without surprising agents with an implicit default. Forces the
agent to think about destination explicitly — same-board moves
need a target group anyway, cross-board moves need both.

### Implementation notes

**Strict default per §8 decision 5.** The unmatched-column check
fires pre-mutation (and pre-emit on dry-run). Every source column
**with actual content** — populated cells per `cellHasData`'s
recursive emptiness check (non-null `value` whose object/array
leaves aren't all empty, OR non-empty `text`) — must either have a
verbatim ID match on target OR be bridged by `--columns-mapping`.
Empty cells (no value, cleared rich shapes like `{}` /
`{personsAndTeams: []}`, or empty `text`) drop out of the check
because Monday wouldn't carry their values across the move anyway.
Unmatched → `usage_error` carrying
`details.unmatched: [{source_col_id, source_title, source_type}]`
+ `details.example_mapping: {<src>: "<target_col_id>"}`. The
example mapping seeds the agent's next call — copy, fill in
target IDs, retry. `--columns-mapping {}` (empty object) is the
explicit "drop everything (Monday's permissive default)" opt-in
that bypasses the check.

**Verbatim ID matches surface explicitly in `column_mappings`.**
Even when source + target IDs match without an agent-supplied
mapping, the planner emits `{source: X, target: X}` in the wire
payload + dry-run echo. Reasoning: the dry-run tells the agent
exactly what Monday will receive on the wire, so a comprehensive
echo is more useful than a partial one. Mapping-overrides win over
verbatim matches (agents can deliberately rename a same-ID
column — e.g., if the target board's `status_4` carries a different
semantics, the agent maps source's `status_4` to target's
`archived_status_4`).

**Inherited helpers (R23, R27, R28, R21).** Both transports use
`projectMutationItem` (R28 — live-mutation null-handling, errorCode:
'not_found'). Same-board dry-run uses `readSourceItemForDryRun`
(R27, operationName: 'ItemMoveRead'). Cross-board uses both R27
+ `lookupItemBoard` (R23) as a board-id fallback for the rare
projection-board-id-null path. `mergeSource` + `mergeCacheAge`
(R21) fold the cross-board path's mixed cache + live legs into the
final envelope source. Net new code stayed small (~530 LOC for
the verb, ~140 for the JSON parser); the inherited helpers paid off.

**Coverage gap.** `move.ts` line 526 (the `lookupItemBoard`
fallback for `sourceItem.board_id === null`) sits at 90% branch
coverage on the file because the fallback fires only when Monday
returns a malformed item with a null board reference — the
projection schema allows it (`board_id: z.string().nullable()`)
but in practice every fixture-driven path returns a non-null
board_id. The branch is genuinely defensive; the file's branch
coverage stays above the 95/94 floor at 90 because the file's
overall surface is small and the one defensive branch dominates
the percentage. Considered + rejected: forcing the test via a
fixture cassette that returns `board: null` on the source-item
read. Rejected because the test would only exercise the fallback
in a synthetic scenario; the production path is the projection's
primary ID with the lookup fallback as a belt-and-braces guard.

### Codex review — round 1

**Verdict: N (1 P1 + 2 P3).** Caught one correctness bug + two
documentation drift items. Fix commit `4385aba`.

| Code | Severity | Area | Finding |
|------|----------|------|---------|
| F1 | P1 | strict-default check | `collectSourceColumnIds()` returned `Object.keys(source.columns)` and assumed `projectItem()` filters empty cells. It doesn't — every wire `column_values` entry survives. Strict-default unmatched check fired for empty unmatched source columns even though Monday wouldn't carry their values across the move. Real consequence: agents on boards with any empty columns hit `usage_error` and had to either bridge or `--columns-mapping {}` columns they never touched. |
| F2 | P3 | doc consistency | v0.2-plan §3 M11 deliverables / tests / exit bullets still showed `--to-group | --to-board` mutually exclusive + the rich `{id, value?}` mapping form, contradicting the §15 status block + the shipped surface. |
| F3 | P3 | doc consistency | output-shapes.md said cross-board `meta.source` could be `'live' \| 'cache' \| 'mixed'`, but the source-item read leg + the mutation leg are always live, so pure `'cache'` is impossible. |

Round-1 close: F1 fix added `cellHasData(col)` predicate +
filter; F2 rewrote the §3 deliverables/tests/exit bullets with
a "pre/post-implementation diff" preface; F3 trimmed `'cache'`
from the cross-board source possibilities.

### Codex review — round 2

**Verdict: N (1 P1 + 2 P2 + 1 P3).** Round-1's F1 fix was too
narrow; round-2 caught the rich-clear escalation, plus two
correctness bugs round-1 didn't reach, plus residual doc drift.
Fix commit `747f300`.

| Code | Severity | Area | Finding |
|------|----------|------|---------|
| F1 | P1 | strict-default check | Round-1's `cellHasData` only treated `value === null \| undefined` as empty, missing the "rich clear" shapes Monday + the M5b clear translator both produce: `{}`, `{label: null, index: null}`, `{date: null, time: null}`, `{personsAndTeams: []}`. Cleared status / date / people cells parsed via `parseColumnValue` to non-null objects, so the round-1 filter wrongly counted them as "has data" — re-introducing the F1 bug for the rich-clear case. |
| F2 | P2 | strict-default check | `--columns-mapping` targets weren't validated against `targetColumnIds`. The parser checked JSON shape only (non-empty string). A typo'd target like `--columns-mapping '{"status_4":"typo"}'` reached Monday's `columns_mapping` parameter and was silently dropped server-side, defeating the strict-default's "reject before silent drop" guarantee. |
| F3 | P2 | dry-run/live parity | Pre-fix: when `--columns-mapping` was undefined, the planner built verbatim mappings (echoed in the dry-run `column_mappings` slot) but the live mutation sent `columnsMapping: null`. Agents previewing via dry-run saw `[{source: date4, target: date4}]` but the live call sent something different. Docs claimed the echo "fully describes what Monday receives". |
| F4 | P3 | doc drift | Round-1's scoped fixes left three lingering doc references: v0.2-plan §1 overview line 78 mentioned "optional value-translation overrides" for `--columns-mapping`; §15 implementation notes equated "with data" to "every key in the projected `columns: {}` map" (contradicting the post-F1 semantic-empty rule); cli-design.md cross-board dry-run source still listed pure `'cache'` (already trimmed in output-shapes.md round-1). |

Round-2 close: F1 fix added the recursive `valueHasContent`
predicate (empty when null/undefined, empty-string, all-empty
arrays/objects) + `cellHasData` falls back to text-non-empty so
read-only-shape cells with populated `text` but empty `value`
still count as data. F2 added target-existence validation in
`planColumnMappings` with `details.invalid_mappings`
decoration. F3 made the live mutation always send
`plan.columnsMapping` so dry-run + live byte-match. F4 swept
the three lingering doc drift sites.

### Test gap-closer pass

Audit after round-2 surfaced three coverage gaps. Fix commit
`19d5a1e` (3 new tests):

- `valueHasContent` primitive-string-in-value branch
  (`value: '42'` for numeric columns) — was untested.
- `invalid_mappings` + `unmatched` precedence — when both apply,
  `invalid_mappings` wins; `unmatched` isn't surfaced this round.
- Empty source item live wire shape — confirms the planner emits
  `[]` and the live wire sends `[]` (matching dry-run's
  empty-array echo).

**Coverage stable** at 98.98 / 95.35 / 99.37 / 99.07 above the
95/95/95/95 floor; `move.ts` 91.93 branches (the one defensive
`lookupItemBoard` projection-board-id-null fallback remains
deliberately uncovered as documented above).

### Lessons for M12+

1. **Read the SDK typings before the design pass.** Both M11
   discoveries surfaced from `node_modules/@mondaydotcomorg/api`
   reading; either could have been caught during v0.2-plan §3 M11
   drafting. The pattern: when a milestone names a Monday
   mutation, read the SDK's args type before committing to a
   surface shape. M9 did this for `create_item` / `create_subitem`;
   M11 missed it.

2. **Cross-leg atomicity is a contract surface, not an
   implementation detail.** When a CLI verb requires two
   mutations to deliver one user-visible action, the partial-
   failure envelope shape becomes part of the §6.4 contract.
   Defer rather than improvise.

3. **`--dry-run` raises real errors, not preview-of-failure.**
   v0.2-plan §3 M11 already pinned this (unmatched columns raise
   `usage_error` even in dry-run); the principle generalises.
   Agents don't have to interpret a "would-fail" dry-run shape
   when the real shape is the same `usage_error` they'd hit
   live.

---

## 16. Refactor backlog — surfaced post-M11

Three R-class candidates surfaced during M11 implementation +
review. Same shape as §12 (post-M9 R20–R26) and §14 (post-M10
R27–R29) — each entry names the duplication, the shape proposed,
the sites that'd be touched, and a defer-or-ship recommendation.
**Documented only — no implementation work this session.** Pick
up before M12 (`item upsert`) starts so the new mutation surface
inherits the lifted helpers rather than copying the patterns.

### R30 — `SourceAggregator` accumulator helper

**Shipped: 1454116.** New `src/api/source-aggregator.ts` exporting a
stateful `SourceAggregator` class — constructor seeds (for callers
holding a first leg's `{source, cacheAgeSeconds}`), `.record(source,
cacheAgeSeconds)` per leg, `.result(fallback?)` snapshots at emit
time. The audit found a **fifth** consumer at lift time (the M11
v0.2-plan entry counted four; `update.ts` bulk's dry-run aggregation
was the missing site) — `runCrossBoardMove`, `update.ts` single-item,
`update.ts` bulk dry-run, `update.ts` bulk live, plus `create.ts`
live all collapse to `new SourceAggregator(...)` + per-leg
`.record(...)` + `...sourceAgg.result()` spread, replacing ~10–15
LOC of accumulator boilerplate per site. The standalone
`mergeSource` / `mergeCacheAge` / `mergeSourceWithPreflight` exports
stay — `create.ts` dry-run uses `mergeSourceWithPreflight` (folds a
`'none'`-claiming planner source against an `EnvelopeSource |
undefined` preflight) which doesn't fit the class's
`EnvelopeSource`-only shape, so dry-run keeps the standalone helper
per the entry's "post-planner aggregation only" scope. Pure refactor
— every existing test passed byte-identically; new focused unit
suite (22 tests) covers constructor seeding, per-leg merge
mirroring, fallback-default, multi-leg sequencing across all source
types. Coverage stayed above 95/95/95/95 (98.98 / 95.48 / 99.37 /
99.07). M12's `item upsert` + bulk `item clear --where` will inherit
the helper rather than copying the accumulator pattern again.

**Sites with the same shape (4 named in this entry; 5 at lift):**

- `src/commands/item/move.ts` `runCrossBoardMove` (M11) — explicit
  `let aggregateSource; let aggregateCacheAge; const recordLeg
  = ...` closure with per-leg `recordLeg(source, cacheAge)` calls.
  Cleanest expression of the pattern (the lift-candidate seed).
- `src/commands/item/create.ts` (M9) — dry-run path uses
  `mergeSourceWithPreflight` + `mergeCacheAge` inline, live path
  declares `aggregateSource` / `aggregateCacheAge` from
  `resolutionResult` then folds 'live' for the mutation leg.
- `src/commands/item/update.ts` single-item path (M5b) — assigns
  `aggregateSource` from `resolutionResult.source`, then folds
  via `mergeSource(aggregateSource, 'live')` post-mutation.
- `src/commands/item/update.ts` bulk path (M5b) — loops with
  `aggregatedSource = mergeSource(aggregatedSource, result.source)`
  + `aggregatedCacheAge = mergeCacheAge(aggregatedCacheAge,
  result.cacheAgeSeconds)` per item.

**Why now (post-M11).** Three sites pre-M11; M11's
`runCrossBoardMove` was the fourth — it kept the local `let +
recordLeg` closure rather than introducing the lift inline because
the M11 work was already large. The same accumulator pattern keeps
duplicating; M12 (`item upsert`) will make it a fifth consumer
(match-by lookup + create-or-update branch + mutation = 2-3 legs to
aggregate).

**Shape proposed:**

```ts
// src/api/source-aggregator.ts (extend existing module)
export class SourceAggregator {
  private source: EnvelopeSource | undefined;
  private cacheAge: number | null = null;

  constructor(
    seed?: { readonly source: EnvelopeSource; readonly cacheAgeSeconds: number | null },
  ) {
    if (seed !== undefined) {
      this.source = seed.source;
      this.cacheAge = seed.cacheAgeSeconds;
    }
  }
  record(source: EnvelopeSource, cacheAgeSeconds: number | null): void {
    this.source = mergeSource(this.source, source);
    this.cacheAge = mergeCacheAge(this.cacheAge, cacheAgeSeconds);
  }
  result(fallback: EnvelopeSource = 'live'): {
    readonly source: EnvelopeSource;
    readonly cacheAgeSeconds: number | null;
  } {
    return { source: this.source ?? fallback, cacheAgeSeconds: this.cacheAge };
  }
}
```

Each call site replaces ~10–15 lines of accumulator boilerplate
with a 2-line constructor + `.record()` per leg + `.result()` at
emit time. The standalone `mergeSource` / `mergeCacheAge` exports
stay (the `mergeSourceWithPreflight` shape in `create.ts` doesn't
collapse cleanly into the class — leave it as a separate helper).

**Edge cases the lift must preserve:**

- `aggregateSource ?? 'live'` fallback when no leg has fired
  yet — needed for the dry-run-only paths where every leg
  technically didn't fire.
- The `mergeSourceWithPreflight` pre-planner shape used by
  `create.ts` doesn't fit the class — keep that helper module-
  level. R30 only consolidates the post-planner aggregation.
- `update.ts` bulk path's per-iteration warning aggregation lives
  alongside source aggregation; R30 doesn't touch that (warnings
  are a separate aggregation; one R per concern).

**Recommendation: ship before M12.** Same "lift before the next
mutation milestone" pattern v0.2's M9.5 R20–R26 cleanup pioneered.
M11's `runCrossBoardMove` will inherit the helper rather than
keeping the local closure. **Shipped before M12 opened**, see the
"Shipped: 1454116" annotation at the top of the entry.

### R31 — `cellHasData` / `valueHasContent` semantic-emptiness predicate

**Sites today (1):** `src/commands/item/move.ts` (the M11 round-2
F1 fix). Determines whether a projected column carries actual
data (recursive non-empty check on `value` + non-empty `text`).

**Why a candidate.** The check is genuinely a property of the
projected column, not of the move command. The natural home is
`src/api/item-projection.ts` alongside `projectItem` /
`projectColumnValue`. M12+ commands that filter "items where this
column has data" or "drop empty cells before sending to Monday"
would reuse the predicate.

**Recommendation: defer until second consumer.** Same shape as
R29 (the destructive-verb confirmation gate lift). The
single-consumer rule keeps the API surface narrow; lifting now
would be premature abstraction. **When to schedule:** if M12
(`item upsert`) or M14/M15 (workspace/board lifecycle) needs the
same "is this projected cell empty" check, lift then. Trigger
phrase: "I want to count populated cells on a projected item" or
"I want to filter empty cells before X".

### R32 — JSON-object argv parse helper

**Sites with the same shape (2):**

- `src/api/raw-write.ts parseSetRawExpression` (M8) — splits
  `<col>=<json>` on first `=`, parses the JSON segment, validates
  the result is a JSON object, throws `UsageError` on every
  malformed branch with detail decoration.
- `src/api/column-mapping.ts parseColumnMappingJson` (M11) —
  parses raw JSON string, validates the result is an object whose
  keys + values are non-empty strings, throws `UsageError` on
  every malformed branch.

**Shared shape:**
1. Reject non-string / empty raw input.
2. `JSON.parse` with try/catch → `UsageError` on `SyntaxError`.
3. Reject non-object root (`null`, array, primitive).
4. Validate the inner shape via zod.

**Why a candidate.** Two consumers today; M12+ verbs that accept
JSON-object argv flags (e.g. `--filter-json`, `--settings`) might
make this a fourth or fifth. The per-site error-decoration flavour
(specific hints, specific token-bearing details) varies, so a
naive lift would have to accept callback-shaped error builders or
template strings — not a clean win.

**Recommendation: defer until third consumer.** The two-site
duplication is real but the per-site flavour is also real;
lifting now risks a leaky abstraction where each call site has
to override the error messages anyway. **When to schedule:** when
M12 / M16's `--settings <json>` / a future v0.3 surface adds the
third consumer of the parse-then-validate-then-decorate pattern.
Trigger phrase: "I'm writing yet another JSON-object argv parser
with the same `JSON.parse → object root → zod validate → typed
UsageError` skeleton."

### Ordering — as shipped

R30 landed in the M11 → M12 cleanup window (commit `1454116`),
same pattern as R27 + R28 in the M10 → M11 window. One atomic
commit, **five** call sites migrated (the entry's pre-flight count
of four undershot by one — see the "Shipped" annotation above),
integration coverage stayed byte-identical. R31 + R32 stay deferred
to their respective third-consumer triggers (R31 watch-for in M12;
R32 watch-for in M16).

---

## 17. M12 post-mortem — five-round review on a wire-shape contract

M12 shipped on `main` across nine atomic commits (see the §3 M12
status block for the full chain). Net new: `src/commands/item/
upsert.ts` (1345 LOC, the verb) + bulk `--where` extension to
`src/commands/item/clear.ts` (+804 / -54 LOC). Inherited helpers
from R20–R30 paid off — `SourceAggregator` (R30) collapsed the
lookup + create-or-update aggregation to one constructor +
per-leg `.record()` + `.result()` at emit time;
`mergeSourceWithPreflight` (R21) covered the dry-run create
branch; `splitSetExpression` (R22) parsed `--match-by`'s
column-token entries with the same parser the rest of the
v0.1+v0.2 column-write surfaces use; `projectMutationItem`
(R28) handled both branches' live-mutation projection. **39 new
integration tests** (26 upsert + 13 bulk clear) plus 4 new
envelope snapshots (`envelope-snapshots.test.ts.snap` +395
lines). Project coverage: 98.87 / 99.11 / 98.98 / 95.01 above
the 95 / 95 / 95 / 94 floor. Per-file: `upsert.ts` 96.59 / 78.12
/ 96.51 — the branch number is notably lower than other v0.2
files (`create.ts` 98.93, `move.ts` 94.82) because upsert's
state-machine has the largest fan-out of any v0.1+v0.2 verb
(branch decision × dry-run / live × per-leg empty-cell
filtering × resolver-warning fold paths). The 95.01 project
branch number stays above the floor because sibling files
absorb the gap; tightening upsert's per-file branch coverage to
95+ is a §16/§18 candidate flagged at M13's review.

The implementation itself was small. The five review rounds
(more than M11's two, M9's four, M8's four) were almost all
about the **`--match-by` round-trip contract** — what
combinations of column kind + value form are guaranteed to
match the just-written item on the next call. Each round
narrowed the contract further as a new wire-shape mismatch
came to light.

### Round trajectory

**Round 1 — 5 findings (1 P1, 4 P2). Fix commit `1f081a4`.**

| Code | Severity | Area | Finding |
|------|----------|------|---------|
| F1 | P1 | match-by lookup | `lookupMatches` built filter rules by hand and skipped `buildQueryParams`'s `me`-resolution leg. `--match-by Owner --set Owner=me` resolved to `me` on the create / update leg but queried the items_page filter with the literal `"me"` string → 0 matches → duplicate created on every rerun. Fix: route column-token entries through `buildQueryParams` so `me` resolution + cache-miss-refresh + collision warnings inherit automatically. |
| F2 | P2 | dry-run source | The dry-run create branch reported `source: "cache"` even when the SourceAggregator's lookup leg had recorded a live items_page leg. Fix: fold the SourceAggregator into the dry-run via `mergeSourceWithPreflight` (create branch) / `mergeSource` (update branch) so the source carries the recorded legs. |
| F3 | P2 | branch decision | Empty page with non-null cursor was treated as zero matches → branched to create. Monday's items_page contract returns `cursor: null` when no more pages exist, so empty-with-cursor is an API anomaly, not a "no matches" signal. Fix: `decideBranch` raises `internal_error` on the anomaly with `details.board_id` + `details.match_by` decoration. |
| F4 | P2 | clear schema | Bulk `item clear --where`'s envelope now has two distinct shapes (single-item `ProjectedItem` for the per-item leg + `{summary, items}` for bulk), but `itemClearOutputSchema` was the per-item shape only. `monday schema item.clear --json` advertised half the contract. Fix: union schema. |
| F5 | P2 | surface drift | §4.3 surface line for `item upsert` omitted `[--dry-run]` and `[--create-labels-if-missing]`. Fix: add both. |

**Round 2 — 1 finding (1 P2). Fix commit `59e6d09`.**

F1 (P2): the upsert help text + cli-design §5.8 + output-shapes
had an example `--match-by name,owner --set owner=alice@example.com`.
The help advertised it as the canonical "match by people" pattern,
but it doesn't round-trip — the lookup pipeline (via
`buildQueryParams`) resolves `me` for people columns and passes
everything else verbatim, while the mutation translator on the
create / update legs DOES resolve emails to user IDs. So the
naive call creates an item storing `<user_id>` but the next
lookup queries for `"alice@example.com"` (string compare) → 0
matches → duplicate. Fix: replaced the email example with
numeric-user-ID + `--set owner=me` examples; added a
"Match-value caveat" block to help / §5.8 / output-shapes
framing the v0.2 workaround as "pass already-resolved forms."

**Round 3 — 1 finding (1 P2). Fix commit `db18fb7`.**

F1 (P2): the round-2 framing — "pass already-resolved forms;
numeric IDs for people, ISO dates for date" — was symmetric, but
the symmetry isn't real. `parsePeopleInput` (`src/api/people.
ts:222`) rejects digit-only people tokens with
`numericPeopleTokenError` per cli-design §5.3 step 3 (M5b
deferral — the people `--set` grammar lists only emails + `me`;
raw numeric user IDs are v0.3-deferred). The round-2 example
`--set owner=12345678` would fail 100% of the time on the
create / update leg with `usage_error`. Fix: dropped the broken
numeric-people-ID example, restricted people columns in
`--match-by` to `me`-only.

**Round 4 — 4 findings (2 P2, 2 P3). Fix commit `f45d0e7`.**

| Code | Severity | Area | Finding |
|------|----------|------|---------|
| F1 | P2 | date round-trip | The round-3 fix kept "ISO dates round-trip" as a v0.2-safe claim. False — Monday's items_page filter requires `compare_value: ["EXACT", "YYYY-MM-DD"]` for date-equals (per Monday's date-filter changelog), but `buildQueryParams` emits bare `["YYYY-MM-DD"]`. Date columns are NOT v0.2-safe in `--match-by`. The EXACT-marker lift is a cross-surface v0.3 candidate. |
| F2 | P2 | "other kinds" claim | The round-3 caveat said "other column kinds (text / numbers / status / dropdown / link / email / phone / external_id-shaped text) round-trip cleanly." Too broad. `link=<url>\|<text>` writes `{url, text}` but the lookup leg sends the literal pipe string; same for `email` / `phone`. Status / dropdown ARE safe via label-text (Monday's filter compares against the stored label, which both translators agree on). Fix: per-kind safe-list (always-safe / via-label-text / restricted / not-v0.2-safe). |
| F3 | P3 | comment drift | `lookupMatches` docstring still said agents should pass "already-resolved IDs (numeric user IDs, ISO dates)" — round-3 bug in comment form. |
| F4 | P3 | help wrap | Round-3's help text wrapped "numeric-user-ID" across two joined lines and used `**...**` markdown inside Commander plain-text help. Rewrote as plain-bullet hierarchy. |

**Round 5 — Y on P1/P2. 2 P3 findings. Fix commit `89d5cf3`.**

| Code | Severity | Area | Finding |
|------|----------|------|---------|
| F1 | P3 | help wrap | Round-4's wrap fix moved the markdown / orphan-bullet issue but left `["EXACT", "YYYY-MM-DD"]` literal that still wrapped inside the date token, rendering `YYYY-MM-` / `DD`. Reworded to "EXACT marker plus YYYY-MM-DD" so the token doesn't break. |
| F2 | P3 | shared-pipeline accuracy | The lookupMatches docstring + cli-design §5.8 said upsert "shares buildQueryParams with item search and item update --where". Half right — `item search` parses identical syntax via `buildColumnQueries` but targets `items_page_by_column_values`, a different filter shape. The `items_page(query_params)` proof for status / dropdown label-text round-trip comes from `item list --where` + bulk `item update --where`. |

### What pattern explains the findings

The whole chain — round-2 through round-5 — was the same shape:
**a claim about "X round-trips" backed by the wrong evidence.**

- Round 2's claim ("pass already-resolved forms") backed by the
  reasoning "lookup translator accepts the resolved form;
  mutation translator emits the resolved form."
- Round 3's narrowing ("numeric user IDs work") same shape —
  the mutation translator's actual grammar (cli-design §5.3
  step 3) was checked against the help text ONLY at round-3,
  after the help had already shipped one round and made the
  claim.
- Round 4's narrowing ("ISO dates work, other kinds work")
  same shape — the items_page wire shape (the EXACT marker,
  the bare-pipe-string compare for rich grammars) was only
  consulted at round-4, after Codex web-searched Monday's
  changelog.

The reasoning never asked: **is this round-trip pinned by an
existing test, or am I reasoning from the translator's input
shape?** Translator-shape reasoning is one step removed from
wire-shape reality. The wire shape is what Monday's filter
actually compares against, and the only source of truth for
that is either (a) an existing integration test on a sibling
surface (`item list --where`, `item update --where`) that
shipped the same wire shape and pinned the round-trip, or (b)
a new integration test that pins the round-trip explicitly.

### Single load-bearing lesson

> **Wire-shape claims need empirical proof.** When a CLI verb
> sells idempotency / round-trip / "this matches what you just
> wrote," every column-kind + value-form combination on the
> safe list either (a) is pinned by an integration test on this
> verb, or (b) inherits from a sibling surface whose existing
> tests pin the same wire shape. Translator-input-shape
> reasoning ("the lookup translator accepts X; the mutation
> translator emits X") is not enough — the mutation translator
> emits a rich payload, the lookup translator emits a filter
> compare, and Monday's filter compares against the stored rich
> shape. The two paths diverge at the wire layer and only
> empirical evidence proves they reconverge.

Operational rule for M13+: when a milestone makes a "this
round-trips" claim in help / contract, the corresponding
integration test that pins the round-trip MUST be either a new
test in this milestone or an explicitly-cited test from a
sibling surface (with the test path in the post-mortem). No
test cited → the claim doesn't ship in the contract.

### Implementation notes

**Branch decision is a discrete state machine.** `decideBranch`
takes the lookup result's `(matchCount, cursor)` and returns
one of `{ kind: 'create' } | { kind: 'update', itemId } |
{ kind: 'ambiguous', error: ApiError }`, throwing
`ApiError('internal_error', ...)` directly on the empty-with-
cursor anomaly. The three returned states cover the valid
Monday responses (zero matches with null cursor → create, one
match → update, 2+ matches → ambiguous_match wrapped as
`error`); the throw covers the contract anomaly Codex round-1
F3 caught (zero matches with non-null cursor — Monday's
items_page returns `cursor: null` when no more pages exist, so
non-null cursor with empty items is API-side malformation).
Throwing rather than returning a fourth state keeps the union
narrow at the call sites — the create / update / ambiguous
branches are exhaustive over the valid responses.

**The "candidates" in ambiguous_match.** When the lookup
returns 2+ matches, the error envelope's `details.candidates`
carries up to 10 items' `{id, name}` projection so an agent
debugging sees representative items without paying for a
second round-trip. The lookup uses `limit: 11` — 10 items + 1
lookahead lets the planner distinguish "exactly 2..10 matches"
from "11+ matches" in a single page. `details.match_by` /
`details.match_values` / `details.matched_count` round out the
debug surface so an agent can replay the predicate or tighten
it without re-deriving the input shape from argv.

**Bulk clear `--where` reuses bulk `item update --where` almost
verbatim.** The cursor walk, `confirmation_required` gate,
per-item failure decoration, and dry-run shape are inherited
from M5b's bulk update. The only M12-specific code is the
"clear" mutation per-item (one column → null) vs update's
"set" mutation. This is why M12 deliberately bundled both verbs
— the clear extension is mostly free given the upsert
infrastructure was already exercised in test.

**§16 R31 / R32 audit.** R31 (`cellHasData` lift to
`item-projection.ts`) — M12 didn't surface a second consumer.
The M12 lookup leg projects items via the existing
`projectMutationItem` (R28) which doesn't use semantic
emptiness. R31 stays deferred. R32 (JSON-object argv parse
helper) — M12 didn't add a third consumer. The bulk
`--filter-json` parser inherits from `parseFilterJson`
(filters.ts, M4); the `--match-by` parser uses
`splitSetExpression` (R22). R32 stays deferred. Both R31 and
R32 reach their re-evaluation triggers at M14/M15/M16.

### Coverage trajectory

| File | Branch coverage | Notes |
|------|-----------------|-------|
| `commands/item/upsert.ts` | 95.62 | Round-1 F1 + round-1 F3 fixes added two branches each, both fully covered. |
| `commands/item/clear.ts` | 96.83 | Bulk extension preserved per-item path coverage. |
| Project total | 95.01 | Above the 95/95/95/94 floor. |

---

## 18. Refactor backlog — surfaced post-M12

One R-class candidate surfaced during M12 implementation. Same
shape as §12 (post-M9), §14 (post-M10), §16 (post-M11) — names
the duplication, the shape proposed, the sites that'd be
touched, and a defer-or-ship recommendation.

### R33 — GraphQL mutation strings + per-command operation names

**Sites with the same shape (4):**

- `src/commands/item/create.ts` (M9) — `CREATE_ITEM_MUTATION`,
  `CREATE_SUBITEM_MUTATION` (operation names `ItemCreateTopLevel`
  / `ItemCreateSubitem`).
- `src/commands/item/update.ts` (M5b) —
  `CHANGE_SIMPLE_COLUMN_VALUE_MUTATION`,
  `CHANGE_COLUMN_VALUE_MUTATION`,
  `CHANGE_MULTIPLE_COLUMN_VALUES_MUTATION` (operation names
  `ItemUpdateSetSimple` / `ItemUpdateSet` / `ItemUpdateBulk`).
- `src/commands/item/clear.ts` (M5b + M12) —
  `CHANGE_SIMPLE_COLUMN_VALUE_MUTATION`,
  `CHANGE_COLUMN_VALUE_MUTATION` (operation names
  `ItemClearSimple` / `ItemClear`).
- `src/commands/item/upsert.ts` (M12) — `CREATE_ITEM_MUTATION`,
  `CHANGE_SIMPLE_COLUMN_VALUE_MUTATION`,
  `CHANGE_COLUMN_VALUE_MUTATION`,
  `CHANGE_MULTIPLE_COLUMN_VALUES_MUTATION` (operation names
  `ItemUpsertCreate` / `ItemUpsertSetSimple` / `ItemUpsertSet`
  / `ItemUpsertBulk`).

`CHANGE_SIMPLE_COLUMN_VALUE_MUTATION` and
`CHANGE_COLUMN_VALUE_MUTATION` are now 3-consumer sites
(update + clear + upsert), and `CREATE_ITEM_MUTATION` is a
2-consumer site (create + upsert).
`CHANGE_MULTIPLE_COLUMN_VALUES_MUTATION` is a 2-consumer site
(update + upsert). The mutation strings themselves are short
(~15 LOC each) and identical across sites, but the
**operation_name** differs per command (the SDK's
`request<T>(query, vars, headers)` flow uses the operation
name for telemetry / error attribution).

**Why a candidate.** §16 R30's "lift on third consumer" rule
fires on `CHANGE_SIMPLE_COLUMN_VALUE_MUTATION` and
`CHANGE_COLUMN_VALUE_MUTATION` post-M12. The natural home is
`src/api/item-mutations.ts` exporting the constants + a thin
factory like `createItemMutation(operationName)` that returns
the GraphQL string with the operation name slotted in.

**Why defer.** A clean lift needs a parameterized factory
because each call site uses a different operation name —
either pass the operation name through every call or ship a
factory function per mutation. The factory shape is a small
abstraction tax (one extra import + one factory call per
site) for a savings of ~15 LOC × 4 sites = ~60 LOC — a thin
margin. The duplicate strings also stay byte-identical
because the SDK's `gql` template tag would let any string
drift surface as a GraphQL parse error during integration
tests, so the existing tests already pin the consistency.

**Recommendation: defer until fifth consumer.** M13 is the
update / comment mutation cluster (eight new mutation verbs
mostly thin wrappers around existing `change_*` mutations
the SDK already provides typed calls for); the new verbs
mostly land via the SDK's typed mutations rather than raw
GraphQL strings, so M13 likely won't add a fifth consumer of
the raw `CHANGE_SIMPLE_COLUMN_VALUE_MUTATION` /
`CHANGE_COLUMN_VALUE_MUTATION` constants. M14 / M15
(workspace + board lifecycle) introduce different mutations
entirely. **Trigger phrase:** "I'm copying yet another
`mutation ChangeSimpleColumnValue($itemId: ID! ...)` into a
new commands/* file." Audit at the M14 → M15 boundary.

### R34 — `items_page` walker + GraphQL queries

**Shipped: 1813c3d.** New `src/api/items-page-walker.ts` exporting
`fetchItemsPage<T>` (single-page query — used directly by upsert's
`limit: 11` lookup and indirectly by every `paginate.ts`-driven walker
as the initial leg) + `fetchNextItemsPage<T>` (`next_items_page(cursor)`
continuation). Both return `MondayResponse<{items, cursor}>` with `data`
re-typed to the parsed shape; the wider response (complexity, stats)
passes through unchanged so callers spread it into `toEmit(response)`.
Operation name + item-fields fragment + optional `groupId` stay
parameterized so Monday's telemetry attribution + the
`boards.groups.items_page` group-variant query shape carry over per-
site. Each migrated site replaces ~30–80 LOC of GraphQL + parse
boundary with one closure (or one direct `fetchItemsPage` for upsert);
the cursor walker stays in `pagination.ts` for every consumer so the
§3.1 #8 per-page ascending-by-ID sort + §5.6 `stale_cursor` enrichment
ride byte-identical (Codex round-1 F1+F2 caught the variant where a
thin async generator dropped both — fix kept the helper single-page and
routed bulk paths back through `paginate.ts` with the helper closures,
mirroring `item list`'s pattern). Net diff: -301 LOC across the four
migrated files. Pure refactor — every existing integration test passes
byte-identical; new focused unit suite (15 tests) covers happy /
malformed / groupId / queryParams / itemFields / operationName /
paginate-composition wiring. Coverage moves up: branches 95.53 → 95.60,
lines 98.87 → 99.08, helper file 100/100 lines/branches.

**Sites with the same shape (5):**

- `src/commands/item/list.ts` (M4) — `ITEMS_PAGE_QUERY` (id +
  full item fields), `ITEMS_PAGE_BY_GROUP_QUERY` (group-
  filtered variant), `NEXT_ITEMS_PAGE_QUERY`. Operation names:
  `ItemsPage` / `ItemsPageByGroup` / `NextItemsPage`.
- `src/commands/item/update.ts` (M5b) — `ITEMS_PAGE_QUERY`
  (id-only projection), `NEXT_ITEMS_PAGE_QUERY`. Operation
  names: `ItemsPage` / `NextItemsPage`. Bulk `--where` walker.
- `src/commands/item/clear.ts` (M5b + M12) —
  `ITEMS_PAGE_QUERY` (id-only), `NEXT_ITEMS_PAGE_QUERY`.
  Operation names: `ItemsPage` / `NextItemsPage`. Bulk
  `--where` walker (M12).
- `src/commands/item/upsert.ts` (M12) — `UPSERT_LOOKUP_QUERY`
  (id + name projection, single-page `limit: 11` lookup, no
  `next_items_page` continuation because the planner caps at
  10 candidates). Operation name: `ItemUpsertLookup`.

**Shared shape:**
- Variables: `{boardId: ID!, limit: Int!, queryParams:
  ItemsQuery}` (or `+ groupId: String!` for the by-group
  variant).
- Response: `data.boards[0].items_page.{cursor, items[]}`
  with malformed-response handling raising
  `internal_error` carrying `Monday returned a malformed
  ItemsPage response for board ${boardId}`.
- Pagination loop: while `cursor !== null`, call
  `next_items_page(cursor, limit)` until exhausted.
- Operation-name attribution: each consumer passes a unique
  `operationName` so SDK telemetry / error attribution
  distinguishes `item list` walks from `item update --where`
  walks from `item clear --where` walks etc.

**Why a candidate.** §16 R30's "lift on third consumer" rule
fires — five sites today, four of which use the id-only
projection with the identical wire shape. The natural home
is `src/api/items-page-walker.ts` exporting:

```ts
// Single-page query (used by upsert's limit-bounded lookup +
// bulk's first page).
export interface ItemsPageInputs<T> {
  readonly client: MondayClient;
  readonly operationName: string;       // e.g. 'ItemsPage', 'ItemUpsertLookup'
  readonly boardId: string;
  readonly groupId?: string;            // by-group variant
  readonly limit: number;
  readonly queryParams: Readonly<Record<string, unknown>> | undefined;
  readonly itemFields: string;          // GraphQL fragment — id only / id+name / full
}
export const fetchItemsPage = async <T>(inputs: ItemsPageInputs<T>): Promise<{
  readonly items: readonly T[];
  readonly cursor: string | null;
}> => { ... };

// Cursor walker (used by bulk paths only — upsert exits after one page).
export const walkItemsPages = async function* <T>(
  initial: ItemsPageInputs<T>,
): AsyncIterable<readonly T[]> { ... };
```

Each call site replaces ~30–50 LOC of GraphQL string +
malformed-response handling + cursor loop with one factory
call + one `for await` (or one direct `fetchItemsPage`).

**Edge cases the lift must preserve:**

- Operation name as a parameter (R33's reasoning — the SDK
  uses it for telemetry; lifting without parameterization
  collapses operations under one name and breaks attribution).
- Item-field projection as a string parameter (id-only vs
  `${ITEM_FIELDS_FRAGMENT}` vs `id\n          name`) — the
  factory accepts the inner-fragment string.
- Group-filter variant (`item list --group-id`) needs an
  optional `$groupId: String!` variable + an extra `groups`
  layer in the response. Either the factory takes
  `groupId?: string` and emits a different query string, or
  it stays separate and only the unfiltered walker is lifted.
- Malformed-response error message includes the boardId in
  the context string — keep it parameterizable so individual
  sites can tweak the wording.

**Recommendation: ship before M14.** Five consumers
post-M12 → R30's third-consumer rule fires comfortably. M13
(update mutations) doesn't add new walkers — eight comment-
verb wrappers all single-shot. M14 (workspace lifecycle)
might add walkers for `boards` / `workspaces` pagination,
but those use `walkPages` (R-pre — page-based, not
cursor-based) and don't touch `items_page`. So the M13 →
M14 cleanup window is the right slot for this lift, before
M14 introduces a sixth `items_page` consumer through any
bulk-board-touching verb.

### R35 — bulk-mutation envelope shape + per-item failure decoration

**Sites with the same shape (2):**

- `src/commands/item/update.ts` bulk `--where` path (M5b) —
  `bulkLiveDataSchema = {summary: {matched_count: number,
  applied_count: number, board_id: string}, items: [...]}`
  on success; per-item failure decorates with
  `details.applied_count`, `details.applied_to`,
  `details.failed_at_item`, `details.matched_count` so an
  agent retrying after a partial failure knows where to
  resume.
- `src/commands/item/clear.ts` bulk `--where` path (M12) —
  identical envelope. Same `summary` shape, same per-item
  failure decoration, same dry-run aggregation pattern
  (`{summary, planned_changes}` with deduplicated resolver
  warnings via `code+message+token` keying).

**Shared shape:**
- Live envelope: `{summary: {matched_count, applied_count,
  board_id}, items: ProjectedItem[]}` — `summary.matched_count`
  is the lookup result count, `summary.applied_count` is how
  many items the loop completed before stopping or finishing,
  `items` is the per-item live projection.
- Dry-run envelope: `{summary: {matched_count, board_id},
  planned_changes: PlannedChange[]}` — N-element array, one
  per matched item.
- Failure decoration: `details.applied_count` /
  `details.applied_to: ItemId[]` /
  `details.failed_at_item: ItemId` /
  `details.matched_count` decorate every error from the
  live loop (resolver warnings, mutation failures,
  validation failures with `column_archived` remap, the
  per-leg cache-stale fold from `foldAndRemap`).
- Resolver-warning aggregation: dry-run dedupes per-item
  warnings by `code+message+token`, hoists into the
  envelope's `warnings` array.

**Why a candidate.** Two consumers today; M14 / M15 will add
a third if Monday surfaces bulk archive / bulk delete /
bulk move (none have shipped yet but aren't ruled out for
v0.3+). The shape is mature — both M5b's bulk update and
M12's bulk clear use it identically — but lifting now would
be premature: the third consumer's exact shape would
discriminate "what stays generic" from "what's per-verb." For
example, bulk archive's `applied_to` might want
`archived_at: timestamp` per item; bulk delete might want
`recovery_window_seconds`. The lift's value is a shared
schema + a shared failure-decorator helper, not the loop
itself (which is already factored into the per-verb walker).

**Recommendation: defer until third consumer.** Same
pattern as R29 (destructive-verb confirmation gate, also
two-site post-M11). The shape is correct; the abstraction
opportunity isn't compelling without a third consumer
discriminating the generic from the per-verb. **When to
schedule:** if v0.3 adds `item archive --where` /
`item delete --where` / similar, lift then. **Trigger
phrase:** "I'm hand-rolling a third
`{summary, items}`-shaped bulk envelope with the same
`applied_count` / `applied_to` / `failed_at_item` failure
decoration."

### Ordering — as shipped

R34 (`items_page` walker + GraphQL queries) landed in the M12 → M13
cleanup window (commit `1813c3d`), same pattern as R30 + R27 + R28
in earlier cleanup windows. One consolidated refactor commit, **four**
call sites migrated (upsert lookup, clear bulk, update bulk, list);
upsert calls `fetchItemsPage` directly while the other three thread
both fetchers through `paginate.ts`. Two-round Codex review (round 1:
F1+F2 P2 — bulk paths lost per-page sort + stale_cursor enrichment
when routed through a thin async generator; round 2: zero findings
post-fix). Integration coverage stayed byte-identical; helper unit
suite added 15 tests at 100/100 file coverage.

R33 (mutation strings) and R35 (bulk envelope shape) stay deferred
to their respective trigger conditions (R33 fifth consumer; R35
third consumer). R31 and R32 (post-M11 §16) remain deferred to
M14/M15/M16 triggers.

---

## 19. M13 post-mortem — eight verbs, one breaking change, two Codex rounds

M13 shipped on `main` across twelve atomic commits — one docs pre-flight
(`bb7ee17` — cli-design §4.3 + §6.4 partial-success envelope shape),
seven feat commits (one per verb / verb-pair), the breaking-change
audit on `update list` (`f84a7b3`), one docs close (`10a50da`), and
two fix-up commits closing the Codex review rounds (`9c16d43` for
round 1; round 2's fix commit lands alongside this post-mortem
update). **Codex round 1 returned 2 P2; round 2 returned 1 P2 + 2 P3
(the P2 was a "same fix, second route" gap on round 1's F2 — board
variant of `update list` had no `match_query` pin where the per-item
variant did).** Project coverage: 98.94 / 95.39 / 99.43 / 99.05 —
branches up from 95.01 (M12 close) because the new helpers (toggle,
body-source, partial-success-mutation) all hit 90+% and the new
commands all hit 95+% per file. Tests 1897 (initial close) → 1899
(post-round-1) → 1900 (post-round-2; +1 new `--board --with-replies`
case plus an in-place rework of the existing `--board` default test
to add the populated-replies fixture + `match_query` pin).

The five-round arc M12's wire-shape claims required (§17) didn't recur
here; the load-bearing reason is that M13's wire-shape claims are all
captured in `match_variables` fixture pins (the variable-name
divergence between `like_update`/`unlike_update`'s `update_id` vs
`pin_to_top`/`unpin_from_top`'s `id`; the `ids` rename on `update list`
during the breaking-change audit; the `parent_id` vs `item_id` on
`update reply`). M12's lesson — "wire-shape claims need empirical
proof" — was applied prophylactically in M13's tests rather than
chased through review.

### Codex round 1 — 2 P2, both correct

| Code | Severity | Area | Finding |
|------|----------|------|---------|
| F1 | P2 | clear-all pagination | `walkPages(... maxPages: DEFAULT_MAX_PAGES)` dropped `result.hasMore`. An item with more than 50 × 100 = 5000 updates would silently truncate — agent sees `ok: true` and a results array for the prefix, assumes "all cleared," older updates linger. Bypasses walkPages's caller contract for `pagination_cap_reached`. |
| F2 | P2 | update list query-shape | The breaking-change tests pinned variables and the projection's `replies: []` default but not the actual GraphQL query text. A future regression that always requested replies (silently reintroducing the v0.1 complexity charge) would pass because the projection's `normaliseReplies` empties them regardless of what the wire response carried. |

Both fixed in `9c16d43`. F1: `collectUpdateIds` returns `{ids,
hasMore, pagesFetched}`; the action layer surfaces
`pagination_cap_reached` as a warning on both live + dry-run paths
+ adds `--limit-pages <n>` to extend the cap (mirroring `update
list` / `item list`). Two new integration cases drive a 2-page-
capped 200-update thread through both paths and assert the warning
code lands. F2: `match_query` regex assertions added on both paths
(default asserts `replies {` ABSENT; `--with-replies` asserts
PRESENT); the default test's fixture also returns populated
replies to prove the projection's `normaliseReplies` empties them.
Defense-in-depth covering both wire-query shape AND client-side
projection.

**Pattern explaining both findings.** Same shape: "what's the
contract, and is it pinned at every layer it should be?" F1 was
about a contract (`walkPages`'s `hasMore` → `pagination_cap_reached`
warning) that lived in two siblings (`item list` / `update list`)
but didn't get inherited by the new clear-all consumer. F2 was
about a contract (the wire-query SHAPE, not just variables) that
the projection layer obscured — passing the projection-level test
proved the projection worked but said nothing about the wire query.

The lesson generalises: **when a feature has multiple layers
(wire query / wire variables / projection / envelope), and the
breaking-change premise rests on one specific layer's behaviour
(here: the wire query OMITS replies for complexity savings),
pin every layer with its own assertion**. Otherwise a regression
in the load-bearing layer can pass tests that only check
downstream layers. Symmetric to M9's "wire-shape regressions are
match_variables territory" lesson — applies one layer up to the
query string itself, not just the variables it carries.

### Codex round 2 — 1 P2, 2 P3 (all correct)

| Code | Severity | Area | Finding |
|------|----------|------|---------|
| F1 | P2 | update list query-shape pin (board variant) | Round-1 F2's fix added `match_query` regex assertions on the per-item `update list` paths but missed the parallel per-board paths. `buildBoardQuery()` is a separate GraphQL string with the same opt-in / default branching; without `match_query` on `--board` cases, a board-only regression could re-introduce `replies {` and burn complexity on large board scans while still passing tests (the projection forces `replies: []` regardless). |
| F2 | P3 | post-mortem self-contradiction | The §19 intro paragraph claimed "round 2 clean" pre-emptively while a later subsection said "outcome documented post-run". |
| F3 | P3 | cli-design surface drift | Round-1's `--limit-pages` flag added at runtime but cli-design §4.3 line 693 still listed only `--yes [--dry-run]` for `update clear-all`. |

All three fixed in the round-2 close commit. F1: same `match_query`
pins on `--board` (default + `--with-replies` opt-in), with the
default fixture returning populated replies to prove the projection
empties them on the board route too. F2: §19 rewritten honestly —
two rounds, the P2 fix on board-variant pinning, the P3 doc fixes.
F3: cli-design §4.3 `update clear-all` line gained `[--limit-pages
<n>]` plus a paragraph explaining `pagination_cap_reached` semantics
and the per-call idempotency guarantee that lets agents re-run
after the prefix clears.

**The single load-bearing lesson — generalised, then qualified.**
Round-1's F2 lesson was "pin every layer the breaking-change premise
rests on." Round-2's F1 surfaced a refinement, and Codex round 3
qualified it further:

- **When parallel routes can drift independently** — separate
  GraphQL strings, separate translators, separate wire shapes that
  the agent observes — pin each route at every layer the
  breaking-change premise rests on. That's M13's `update list`
  shape: `buildItemQuery()` and `buildBoardQuery()` are independent
  strings; round-1 pinned the per-item but round-2 caught the
  parallel per-board route exposed.
- **When parallel routes share a helper** — one translator function
  consumed by N callers, one query factory taking parameters — pin
  the helper invariant once + add representative coverage for the
  routes that consume it. That's M13's toggle helper: four verbs
  (like / unlike / pin / unpin) all consume `buildUpdateToggle
  Command` with different `idVariable` values. The wire-shape
  invariant is "the helper sends the variable named in
  `idVariable`"; pinning that invariant via `match_variables` on
  one verb per `idVariable` value (one `update_id` verb, one `id`
  verb) is sufficient. Pinning all four would be duplicate
  integration work for the same invariant.

The discriminator: **"if a future agent rewrites this part of the
code, can they break route B without breaking route A?"** If yes
(separate strings / translators), pin both. If no (helper drift
breaks all consumers simultaneously), pin one.

The round-1 → round-2 ladder still illustrates the
**fix-then-audit-laterally** pattern: when a finding fires on
route A, the first question is "do parallel routes B / C / …
exist, and could the same regression hit them?" — but the answer
is verb-by-verb. M14 / M15's add-users / remove-users will share
the partial-success-mutation helper (one shape across workspace /
board / future team); pinning the helper invariant via the M14
add-users tests is sufficient unless the per-noun routes diverge
(e.g. workspace add-users sends `workspace_id` but board add-users
sends `board_id` to a different mutation entirely — at which point
they're independent routes again).

### Two SDK discoveries the pre-flight surfaced

**Discovery 1 — `clear_item_updates(item_id)` exists as an atomic
alternative.** Monday's SDK exposes a one-shot mutation that deletes
every update on an item server-side. The v0.2-plan §3 M13 + Codex P2
round of the planning review prescribed page-walk + sequential
`delete_update` per id; neither caught the atomic alternative. The
discovery surfaced during pre-flight reading of the SDK's
`MutationClear_Item_UpdatesArgs` shape.

**Decision: keep the page-walk + per-update fan-out**. Reasons:
1. **Per-update visibility** is more useful to agents on partial-
   permission failures. The atomic `clear_item_updates` returns
   `Item` (the cleared item), giving zero per-update outcome
   visibility — agents can't tell which updates were skipped (e.g.
   if the token lacks delete permission on one specific update).
2. **The partial-success envelope shape M14 / M15 inherit needs a
   real first consumer**. Decision 3 in §8 closed this pre-M13: the
   shape is `data.results: [{<id_field>, ok, error?}]` with the
   universal "ok: true when dispatch ran" rule. Without a real
   first consumer in M13, M14's `workspace add-users` would have to
   land both the helper AND the contract test, doubling the design
   risk for a milestone that's already permission-sensitive.
3. **Empirical proof of `clear_item_updates`'s atomic semantics is
   missing**. M11 logged: "Read the SDK typings before the design
   pass, then test against real Monday before committing to the
   wire shape." The atomic primitive's failure semantics (does it
   roll back if one update can't be deleted? does it skip? does it
   surface a partial-success in some unprinted way?) aren't
   documented in the SDK or Monday's API reference.

`clear_item_updates` is logged as a v0.3 optimisation candidate. The
follow-up needs (a) empirical testing against a real Monday account
with mixed-permission updates to characterise the atomic-failure
mode, and (b) a partial-success-vs-atomic decision recorded in
cli-design §6.4. Neither blocks v0.2 release.

**Discovery 2 — Per-mutation SDK variable-name divergence.** The four
toggle verbs split on the input variable name: `like_update` /
`unlike_update` use `update_id`; `pin_to_top` / `unpin_from_top` use
`id`. This isn't documented in Monday's API reference — the
divergence is only visible in the SDK's `MutationLike_UpdateArgs` /
`MutationPin_To_TopArgs` shapes.

**Decision: capture the divergence at the dispatch site.** The shared
toggle helper (`src/commands/update/toggle.ts`) takes `idVariable: 'id'
| 'update_id'` as a config field. Each verb's CommandModule carries
the right value; the helper sends the matching wire key. The
`match_variables` fixture pins (per-verb integration tests) catch any
future regression where someone collapses the divergence on the
helper side without updating the per-verb config.

### R-class lifts in M13

M13 surfaced one mid-milestone R-class candidate that fired on the
third consumer per the v0.1-plan §17 R-timing rule:

**R36 — `readUpdateBody` body-source helper** (shipped inline in
`7837d0c feat: add update reply`). M5b's `update create` carried the
~90 LOC body-source plumbing (`--body` / `--body-file` /
`--body-file -` for stdin) inline. With M13's `update reply` (second
consumer) and `update edit` (third consumer) both needing the same
plumbing, the lift fired on the second consumer arrival rather than
waiting until edit. Net: `src/commands/update/body-source.ts` exports
`readUpdateBody({inlineBody, bodyFile, stdin, verbHint})`. Three
consumers post-M13 (create / reply / edit); zero behaviour change on
the create side (every existing test passed byte-identical).

**One additional shape lift inside M13** (not strictly R-class but
worth naming): `src/commands/update/toggle.ts` exports
`buildUpdateToggleCommand({...})` — a CommandModule factory the four
toggle verbs (like / unlike / pin / unpin) consume. Without it each
verb would have ~150 LOC of GraphQL + zod + projection + action body;
with it each verb is ~20 LOC of config. The factory shape is unusual
(returns a CommandModule rather than executing logic), so it's not a
canonical R-class — it's a "verb-cluster shape" lift specific to
M13's four-toggle pattern. M14 / M15 don't reuse this factory (their
verbs aren't toggles); M14's add-users / remove-users will reuse the
partial-success-mutation helper instead.

### Partial-success envelope shape — first consumer pinned

`src/api/partial-success-mutation.ts` exports `dispatchSequential` +
`PartialSuccessResult` + `PartialSuccessEnvelopeData`. The id-field
key is parameterised at the dispatch site so the JSON shape stays
self-documenting per verb (`update_id` for `update clear-all`;
`user_id` for M14/M15's add-users / remove-users). The "ok: true when
dispatch ran" universal rule (§1) is enforced by the helper — the per-
target catch arm captures `MondayCliError` failures into the result
record's `error: {code, message}` slot rather than throwing, so the
loop completes regardless of per-target outcomes.

Five test cases pin the envelope shape from clear-all's perspective:
all-ok, all-fail, mid-loop-fail-with-recovery, empty-thread-zero-
results (idempotency proof), and whole-call not_found (the §6.1
top-level error escape). M14's add-users tests will reuse the same
shape with `user_id` substituted; the helper makes that substitution
a config change.

### The breaking change — `update list --with-replies` default flip

The one v0.2 output-shape breaking change shipped clean. v0.1
silently populated each update's `replies: [...]` array on every
call (Monday's nested selection ran inside the GraphQL query); v0.2
makes the second leg opt-in via `--with-replies`. Without the flag,
the GraphQL query OMITS the `replies` selection entirely (linear
complexity charge), and the projection forces `replies: []` on the
output side.

The wire-side change is non-trivial — two query strings (with and
without replies), two extraction-time replies-normalisation paths
(default empty / opt-in populated). The projection layer's
`normaliseReplies` function shoulders the "always emit
`replies: []` regardless of GraphQL's response" contract. v0.1's
fixtures already had `replies: []` populated, so most existing tests
needed no change; one test (`returns the projected updates for an
item`) updated to use the new wire variable name (`ids:` vs
`itemIds:`) and the post-flip `replies: []` projection assertion.

CHANGELOG.md gets the breaking-change line in M18's release-prep
commit (per v0.2-plan §3 M18 deliverables); M13's docs sweep updates
output-shapes.md §updates and cli-design §4.3 (already pinned
pre-flight).

### Single load-bearing lesson

**Pre-flight contracts pay back in review-round savings.** M12's five
review rounds were almost entirely about wire-shape claims that
weren't pinned in any test or contract before implementation began.
M13 inverted the pattern: the docs(m13) pre-flight commit
(`bb7ee17`) locked the cli-design §4.3 + §6.4 contracts before any
code shipped, including the partial-success envelope shape and the
per-mutation operation slots. Then the implementation rounds were
"does the code match the pinned contract?" — a much narrower
question than "does the wire shape work the way I think it does?"

This is the symmetric pattern to M12's "wire-shape claims need
empirical proof": empirical proof works retroactively (test fixtures
catching drift); pre-flight contracts work prophylactically (locking
the shape before code touches it). Both have a place; M13's
pre-flight worked because the SDK's mutation shapes were small enough
to characterise upfront and the partial-success envelope shape was
already closed via §8 decision 3.

For M14+: the SDK-discovery rituals from M11's lessons (read SDK
typings before the design pass) AND the wire-shape empirical-proof
rituals from M12's lessons (every claim pinned via integration test
or sibling-surface citation) AND the pre-flight contract-locking
ritual M13 ratified (cli-design §4.3 + §6.4 pinned before
implementation) are now the standard milestone workflow. Each
addresses a different failure mode; using all three keeps review
rounds at zero.

### Coverage trajectory

| File | Branch coverage | Notes |
|------|-----------------|-------|
| `commands/update/list.ts` | 88.46 | The board / item routing branches are both covered by integration tests; the remaining gaps are defensive runtime checks (mutual-exclusion fallback after schema refinement, etc.) — genuinely hard to drive without contriving a malformed inputSchema parse. |
| `commands/update/clear-all.ts` | 90.00 | Defensive page-walk null-handling branches; same shape as the v0.1 `update list` branches. |
| `commands/update/body-source.ts` | 86.36 | Inherited M5b coverage gap on the `--body-file -` programmer-bug branch (`stdin === undefined` defensive). |
| `commands/update/toggle.ts` | 95+ | Helper exercised through both `like` / `unlike` / `pin` / `unpin` integration suites; the variable-name divergence is pinned. |
| `api/partial-success-mutation.ts` | 50 (nominal) | One defensive branch (non-`MondayCliError` re-throw) is genuinely unreachable from the normal call path — a programmer bug somewhere in `dispatch()` would surface as `internal_error` via the runner. The 50% branch number sits on a 4-branch surface (one ok arm + one MondayCliError arm + the defensive throw + the loop continue), so the ratio reads worse than the surface area implies. |
| Project total | 95.39 | Above the 94/95/95/95 floor. |

The clear-all + toggle branch numbers each sit a few points below
the 95/95/95/94 floor at the per-file level but the project total
holds well above (sibling files with 99+% absorb the gap). Tightening
those per-file numbers is a §16/§18 candidate to flag at M14's review.

---

## 20. Refactor backlog — surfaced post-M13

Three R-class candidates surfaced auditing M13's eight new mutation
verbs against the v0.1-plan §17 R-timing rule ("lift on the third
consumer; four is overdue"). Two are net-new lifts; one is the
**reactivation** of R29 (the destructive-verb confirmation gate
deferred at M10 close, whose trigger has now fired hard).

Same R-class shape v0.1 §15 + §17 + v0.2 §12 + §14 + §16 + §18 used:
each entry names the duplication, the shape proposed, the sites
that'd be touched, the trigger state, and a defer-or-ship
recommendation. **Documented only — no implementation work this
session.** Pick up the two firm lifts (R37 + R38) before M14
opens so the new workspace cluster inherits clean helpers rather
than copying patterns; R29 reactivation slots into M14's docs sweep
when workspace delete arrives as the 5th consumer.

### R37 — `projectMutationUpdate` helper (R28 sibling for `Update`)

**Shipped: 2182ded** (one consolidated commit alongside R38; mirrors
R30's "Shipped: 1454116" / R34's "Shipped: 1813c3d" annotation
pattern). New `src/api/update-mutation-result.ts` exports
`projectMutationUpdate({raw, updateId, mutationName, idKey?})` (full
null-check + strict parse, used by reply / edit / delete / toggle)
and `assertUpdateMutationPresent({raw, updateId, mutationName,
idKey?})` (null-check only, used by clear-all whose per-target
`DELETE_UPDATE_MUTATION` selects only `{ id }` — strict parse would
break the existing `{ delete_update: { id: '77' } }` fixture
asserting `ok: true`). Both helpers share a private
`buildNotFoundError` so error code, message format, and
`details.<idKey>` are byte-identical across all five sites — agents
key off `error.code` + `details.update_id` (or `details.parent_id`
for reply, the only site with the `parent_id` divergence) regardless
of which verb threw.

The lift differed from the original §20 sketch in two ways the
session surfaced:

1. **Two exports (full + assertion-only).** §20 sketched a single
   `projectMutationUpdate` for all five sites. Auditing clear-all's
   wire shape (`DELETE_UPDATE_MUTATION` selects only `{ id }`)
   showed that a strict-schema parse would have flipped existing
   per-target fixtures from `ok: true` to `ok: false`, violating
   "every existing test passes byte-identical." The fix is the
   second export — same `not_found` decoration, no projection.
2. **`idKey` parameter.** §20 sketched all sites keying off
   `update_id`. Reply pins `details.parent_id` in tests
   (update.test.ts:833) because the argv input is the parent, not
   the new update — same way `item duplicate` keys errors off the
   source item's id. The helper's optional `idKey: 'update_id' |
   'parent_id'` parameter (defaults to `'update_id'`) preserves the
   existing decoration without leaking the divergence into the four
   non-reply sites.

Both deviations preserve byte-identical tests. The §20 sketch
under-specified clear-all's wire-shape narrowness and reply's
`details.parent_id` pinning; the load-bearing invariant ("every
existing test passes byte-identical") drove both deviations.

**Coverage:** 100% lines / 100% functions / 100% branches on the
new helper file via the existing per-verb integration suites
(reply / edit / delete / four toggle verbs / clear-all all exercise
the helper without dedicated unit tests). Project-wide branches
stayed at 95.43% (above the 94/95/95/95 floor); no per-verb
coverage regressed.

**Net diff (lift):** -175 LOC across 7 migrated files + 186 LOC of
new helper module = **+11 LOC** vs the §20 estimate of **-30 LOC**.
Delta is pure documentation weight: the helper carries ~80 lines of
docblocks documenting the dual-export seam (full vs assertion-only)
and the `parent_id` / `update_id` `idKey` divergence, both of which
the next reader will need. Code-only delta is closer to -75 LOC.

**Sites with the same shape (5):**

- `src/commands/update/reply.ts` (M13) — `projectReply`. Adds
  `parent_id` echo to the base shape (mirrors `item duplicate`'s
  `duplicated_from_id` extension pattern).
- `src/commands/update/edit.ts` (M13) — `projectEdited`.
- `src/commands/update/delete.ts` (M13) — `projectDeleted`.
- `src/commands/update/toggle.ts` (M13) — `projectToggle`.
  Consumed by four verbs (like / unlike / pin / unpin), but the
  projection itself is one site inside the helper.
- `src/commands/update/clear-all.ts` (M13) — inline projection
  inside `dispatchSequential`'s per-target dispatch.

**Shared shape:**

```ts
const projectXxx = (raw: unknown, updateId: string): UpdateXxxOutput => {
  if (raw === null || raw === undefined) {
    throw new ApiError(
      'not_found',
      `Monday returned no update from <mutation_name> for id ${updateId}`,
      { details: { update_id: updateId } },
    );
  }
  return unwrapOrThrow(
    updateXxxOutputSchema.safeParse(raw),
    {
      context: `Monday returned a malformed update payload for id ${updateId}`,
      details: { update_id: updateId },
    },
  );
};
```

Plus the `responseSchema` boilerplate (`z.object({ <mutation_name>:
z.unknown() }).loose()`) which appears alongside in reply / edit /
delete / toggle / clear-all. Both pieces collapse into the helper.

**Why a candidate.** Five consumers post-M13; the v0.1-plan §17 R-
timing rule fires comfortably (3 = trigger, 4 = overdue, 5 = past
due). The natural home is `src/api/update-mutation-result.ts`
exporting `projectMutationUpdate({raw, updateId, mutationName})` —
mirrors R28's `projectMutationItem` exactly with `Update` substituted
for `Item` and `update_id` for `item_id` in the error decoration.

**Proposed shape:**

```ts
// src/api/update-mutation-result.ts (new)
export const updateProjectionSchema = z.object({
  id: UpdateIdSchema,
  body: z.string(),
  text_body: z.string().nullable(),
  creator_id: z.string().nullable(),
  creator: creatorSchema.nullable(),
  item_id: ItemIdSchema.nullable(),
  created_at: z.string().nullable(),
  updated_at: z.string().nullable(),
}).strict();

export type UpdateProjection = z.infer<typeof updateProjectionSchema>;

export interface ProjectMutationUpdateInputs {
  readonly raw: unknown;
  readonly updateId: string;
  /** Mutation root-field name for the not_found message
   *  (`create_update` / `edit_update` / `delete_update` /
   *  `like_update` / `unlike_update` / `pin_to_top` /
   *  `unpin_from_top`). */
  readonly mutationName: string;
}

export const projectMutationUpdate = (
  inputs: ProjectMutationUpdateInputs,
): UpdateProjection => { ... };
```

Reply's `parent_id` extension stays at the call site (the helper
returns the base shape; reply's wrapper adds `parent_id` from
argv before emit), mirroring how `item duplicate` extends the
projected item with `duplicated_from_id` post-helper.

**Edge cases the lift must preserve:**

- Reply's `parent_id` echo from argv — extension stays at the
  caller, helper returns the base shape unchanged. R28's
  `projectMutationItem` followed the same rule for duplicate's
  `duplicated_from_id`.
- Toggle's projection runs inside the shared helper
  (`buildUpdateToggleCommand`); the lift collapses one inline
  projection in toggle.ts plus one inline schema declaration.
- Clear-all's per-target dispatch projects inside
  `dispatchSequential`'s callback; the lift replaces the
  inline null-check + zod parse with one helper call.

**Risk:** low. Pure refactor — every existing test passes
byte-identical. The Update projection schema is shared by `update
get` (M3) too; the lift could optionally consolidate that site as
well (would make 6 consumers), but `update get` carries `replies`
+ `edited_at` slots the mutations don't return, so the helper's
schema would need either a base + extension split or a separate
projection entirely. Recommend leaving `update get` outside R37's
scope — its shape is a read-projection, not a mutation-projection,
and conflating the two would force the helper to grow optional
slots that mutations never carry.

**Net diff estimate:** –~80 LOC across five files + ~50 LOC of
new module = **net –30 LOC**, plus a single source of truth for
the Update projection schema and the not_found error decoration.

**Recommendation: ship before M14 opens.** Same "lift before the
next mutation milestone" pattern v0.2's M9.5 R20–R26 cleanup
pioneered (reaffirmed by R30 → M12, R34 → M13). M14 / M15 don't
add update mutations directly, but M13's surface is now permanent
and any future `update`-cluster work (v0.3+ thread tooling) starts
from the helper. Trigger phrase: "I'm typing yet another
`if (raw === null) throw new ApiError('not_found', ..., {details:
{update_id}})`."

### R38 — `UPDATE_FIELDS_FRAGMENT` (R-pre sibling for `ITEM_FIELDS_FRAGMENT`)

**Shipped: 2182ded** (one consolidated commit alongside R37). New
`UPDATE_FIELDS_FRAGMENT` exported from
`src/api/update-mutation-result.ts` carries the shared GraphQL
selection (id / body / text_body / creator_id / creator { id name
email } / item_id / created_at / updated_at) at 6-space continuation
indent — the column every consumer interpolates `${UPDATE_FIELDS_
FRAGMENT}` at. Rendered query bytes are unchanged post-lift; tests
mock at operation_name level (no cassette text matching), so the
substitution is non-observable.

Six sites adopted: `update create` / `reply` / `edit` / `delete` /
`toggle` (buildMutationBody) / `get` (composes `edited_at` +
`replies { ... }` on top — same pattern `item subitems` uses against
`ITEM_FIELDS_FRAGMENT`). All six render byte-identical GraphQL
strings post-lift.

**Net diff (lift):** rolled into R37's consolidated commit (R38's
contribution: -42 LOC across the 6 GraphQL strings + 9 LOC of new
fragment export = -33 LOC). Single source of truth for the Update
selection; a future cli-design §6.4 wire-shape addition is a
one-touch fragment edit instead of a six-touch sweep.

**Sites with the same shape (6):**

- `src/commands/update/create.ts` (M5b) —
  `CREATE_UPDATE_MUTATION` body.
- `src/commands/update/reply.ts` (M13) —
  `CREATE_REPLY_MUTATION` body.
- `src/commands/update/edit.ts` (M13) —
  `EDIT_UPDATE_MUTATION` body.
- `src/commands/update/delete.ts` (M13) —
  `DELETE_UPDATE_MUTATION` body.
- `src/commands/update/toggle.ts` (M13) — `buildMutationBody`
  template-literal selection (consumed by 4 toggle verbs).
- `src/commands/update/get.ts` (M3) — partial overlap (also
  selects `edited_at` + `replies` for the read shape).

**Shared selection:**

```graphql
id
body
text_body
creator_id
creator { id name email }
item_id
created_at
updated_at
```

Six GraphQL strings carry this exact selection (five mutations +
one read). The mutations also share the `ID!`+`String!` argument
shapes, but those vary per-mutation enough that lifting the
arguments doesn't pay; the **selection set** is the genuine
duplicate.

**Why a candidate.** Mirror of `src/api/item-helpers.ts`'s
`ITEM_FIELDS_FRAGMENT` (M5b lift). Six consumers comfortably
above the 3-consumer trigger. Naturally bundles with R37 since
both touch the same five M13 files; landing them as one cleanup
commit keeps the per-R diff readable.

**Proposed shape:**

```ts
// src/api/update-mutation-result.ts (alongside R37)
export const UPDATE_FIELDS_FRAGMENT = `
  id
  body
  text_body
  creator_id
  creator { id name email }
  item_id
  created_at
  updated_at
`;
```

Each consumer's GraphQL string interpolates `${UPDATE_FIELDS_
FRAGMENT}` where the literal selection set was. `update get`
extends with `${UPDATE_FIELDS_FRAGMENT}\n  edited_at\n  replies
{...}` — same composition pattern `item subitems` uses on top of
`ITEM_FIELDS_FRAGMENT`.

**Edge cases the lift must preserve:**

- The toggle helper's `buildMutationBody` builds the GraphQL
  string dynamically per-verb (operation name + variable name
  vary). The fragment lift is a one-line replacement inside the
  template — the dynamic parts stay dynamic.
- `update get` (M3) carries extra slots (`edited_at`, `replies`)
  that the mutations don't. The fragment is the **shared** subset;
  consumers compose it with their extras. Same shape `item
  subitems` (M3) uses against `ITEM_FIELDS_FRAGMENT`.

**Risk:** trivial. Pure string consolidation. The SDK's `gql`
discipline isn't in play (we use raw template literals); a typo
in the fragment surfaces as a GraphQL parse error during
integration tests, so existing tests pin the consistency.

**Net diff estimate:** –~30 LOC across six files + ~10 LOC of
new export = **net –20 LOC**.

**Recommendation: ship alongside R37 in one consolidated cleanup
commit.** Two R-class lifts touching the same five files pay
back disproportionately when bundled — same pattern R27 + R28
followed in the M10 → M11 window.

### R29 — destructive-verb confirmation gate

**Shipped: 2b74321** — at M14 close per the recommendation below.
Mirrors R30's "Shipped: 1454116" / R34's "Shipped: 1813c3d" /
R37+R38's "Shipped: 2182ded" annotation pattern.
`src/api/destructive-gate.ts` exports `enforceDestructiveGate
({globalFlags, verb, target, detailKey, action, hint, previewSuffix?})`.
All 5 sites migrate (item archive + item delete + update delete +
update clear-all + workspace delete); message templates reproduced
verbatim — every existing test passes byte-identical pre-lift vs
post-lift. 1956 → 1957 tests (+1 net new — added a parallel
empty-entries rejection test on remove-users to mirror the add-
users coverage gap surfaced by the cross-verb audit).

**Sites pre-M13 (2):** item archive + item delete (M10).
**Sites post-M13 (4):** + update delete + update clear-all (M13).
**Sites at lift (5):** + workspace delete (M14 — this lift).
**Sites at M15 close (7):** + board archive + board delete (M15).
**Sites at M16 close (8):** + column-delete (M16 — first
two-tuple consumer; introduced the `extraDetails` slot on
`enforceDestructiveGate({...})` so the confirmation envelope
can echo `{board_id, column_id, hint}` per cli-design §6.5
single-target shape; the canonical `[detailKey]: target` + `hint`
always win on key collision so the existing seven single-id
consumers stay byte-identical post-extension; shipped in M16's
`565d5d3 feat(m16): add board column-delete`).
**Sites projected by M17 close (10):** + group archive +
group delete (M17 — both two-tuple consumers via the
`extraDetails` slot, mirroring M16's column-delete pattern).

The 2-consumer-pre-M13 state was below trigger; v0.2-plan §14
deferred to "schedule for after M14 / M15 lands the third
site" with the caveat that the lift's load-bearing invariant
(the gate-before-`resolveClient` ordering from M10 round-1 P2)
is the reason the helper is worth shipping at all. **M13 added
two consumers; trigger has fired hard.** With 4 consumers today
and 6+ by M15 close, R29 is now overdue, not deferred.

**Updated recommendation: schedule alongside M14's docs sweep.**
M14 introduces the 5th consumer (workspace delete); landing R29
at M14 close means workspace delete adopts the helper from day
one rather than copying the 17-LOC block. Alternative: ship R29
in the M13 → M14 cleanup window alongside R37 + R38 (one big
cleanup commit). The alternative gets the helper into the tree
before M14 starts; the M14-close timing matches the v0.2-plan
§14 original recommendation and lets workspace delete stress-test
the helper's shape during M14's review rounds.

**Pick the M14-close timing.** The cleanup window is already
big enough (R37 + R38 are firm); adding R29 risks bundling too
much into one cleanup pass. M14's docs sweep is the canonical
moment for R29 — same pattern v0.2's M11 → M12 cleanup window
shipped R30, M12 → M13 shipped R34. R29 ships in M14 → M15
window if M14 doesn't surface a blocker; otherwise drops into
M14's close-pass diff.

The §14 entry's "Proposed shape" + "Risk" + "Net diff" + "When
to schedule" content stays load-bearing — it's reproduced here
only for the trigger-state update, not the helper design (which
is unchanged).

### Recommended ordering

The clear path:

1. **M13 → M14 cleanup window — ship R37 + R38** as one
   consolidated commit (mirrors R27+R28 in the M10 → M11 window).
   Both touch the same five M13 files; bundling reads cleanly.
2. **M14 docs sweep — ship R29** (or M14 → M15 window if
   workspace delete surfaces an unforeseen wrinkle). Workspace
   delete is the 5th consumer; the lift fires on its arrival.

The trivial-cluster pattern from M9.5 (R22 + R24 + R25 as three
separate commits) doesn't apply here because R37 and R38 share
a target file (`src/api/update-mutation-result.ts` would house
both); one commit for both is cleaner than two.

### Preconditions (before R37 + R38 land)

Before the M13 → M14 cleanup commit ships:

- [x] R37 + R38 ship as one consolidated commit (per the ordering
      above). Do not split unless the diff exceeds review-comfort
      size (~500 LOC). **Shipped: 2182ded** — 8 files changed, 284
      insertions / 273 deletions; well under the split threshold.
- [x] All five M13 update-mutation files migrate; coverage stays
      above the 95/95/95/94 floor on the helper file (target
      100/100 via existing per-verb integration tests). **All five
      migrated** (reply / edit / delete / toggle / clear-all);
      helper hits 100/100/100 (lines / functions / branches);
      project branches at 95.43% (above floor).
- [x] No behaviour change: every existing test passes byte-
      identical pre-fix vs post-fix. The lift is pure
      consolidation. **All 1900 tests pass byte-identical.** No
      fixture edits. No envelope-shape changes.
- [x] Helper's signature accommodates reply's `parent_id` echo as
      a caller-side extension (helper returns base shape; caller
      wraps). **Verified** — `projectMutationUpdate` returns
      `UpdateProjection` (no `parent_id`); reply's call site
      spreads `{...base, parent_id: UpdateIdSchema.parse(parsed.
      parentId)}`. Plus `idKey: 'parent_id'` parameter on the
      helper preserves reply's `details.parent_id` not_found
      decoration (update.test.ts:833 still passes).

### Sites NOT lifting yet

- **Page-walk first-page-not_found** pattern (`update list` +
  `update clear-all` both implement "first-page items: [] →
  not_found"). Two consumers today, below trigger. Defer until
  third consumer arrives. Trigger phrase: "I'm copying the
  first-page-empty → not_found page-walker idiom into a third
  surface."
- **`details: { <id_field>: <id> }` error decoration on update
  mutations** — every M13 verb's catch arm carries this. Trivial
  inline string; the lift would be a 3-line helper saving 0 LOC
  per consumer. Not worth the abstraction tax.

---

## 21. M14 post-mortem — five verbs, two contracts, four Codex rounds

M14 shipped on `main` across **eleven** atomic commits — one docs
pre-flight (`3fe1d58` — cli-design §4.3 + §6.4 workspace verb
annotations + five new operation shapes), three pre-flight Codex
rounds (`57fa844` / `a1904f8` / `2086953` — 6 + 3 + 1 findings,
all P1/P2 contract bugs caught before any code shipped), five feat
commits (one per verb, in order: create / update / delete / add-
users / remove-users), three implementation Codex rounds (`cbd0ab4`
/ `d37eae2` / `4c6b807` — 2 + 2 + 1 findings), one R29 lift
(`2b74321` — destructive-verb confirmation gate), and this docs
close. **Codex rounds total: seven (three pre-flight + four
implementation)** — more than M13's three but every round caught
real contract drift, not phrasing nits. Project coverage: 98.92 /
95.04 / 99.46 / 99.08 — branches dropped from 95.44 (M13 close) to
95.04 because M14's new partial-success surface area introduced
defensive branches the test suite legitimately can't drive (e.g.
`internal_error` on a malformed `userByEmail` response). The five
workspace files sit between 78-92% per-file branches; sibling files
at 99+% absorb the gap so the project total holds above the
94/95/95/95 floor. Tests 1900 (M13 close) → 1957 (M14 close).

### Pre-flight Codex rounds — three rounds, ten findings (3 P1, 6 P2, 1 P3)

The pre-flight pattern bb7ee17 ratified in M13 paid back hard in
M14 — the contract drift caught BEFORE implementation began would
have been three implementation-round fixes had it slipped past
pre-flight. Three rounds:

| Round | Findings | Theme |
|-------|----------|-------|
| 1 | 6 (3 P1, 2 P2, 1 P3) — all correct | Live partial-success envelope missed `data.operation`; numeric `--users` IDs claimed to flow through `userByEmail`; whole-call boundary used wrong error code; `kind?` SDK slot unspecified; partial-success-envelope text contradicted M14's resolution; symbol drift (`buildPartialSuccessMutation` doesn't exist). |
| 2 | 3 (2 P1, 1 P2) | Whole-call boundary too aggressive (mixed numeric+ghost should stay partial-success); `meta.source` aggregation conflated dry-run vs live; §6.5 missed `user_not_found` per-code details. |
| 3 | 1 (P3) | v0.2-plan §3 M14 + §8 decision 3 still used "ALL --users tokens failed" shorthand instead of the round-2 refined "no dispatchable user_id remains" boundary. |

**Pattern:** every pre-flight finding was a contract-precision
issue. Round 1 caught the major shape bugs; round 2 narrowed the
quantifier phrasing ("ALL emails failed" → "no dispatchable id
remains"); round 3 propagated the round-2 refinement back to the
plan document. The ladder shows the cli-design contract benefited
from three independent passes — each round saw issues the
previous missed. Lesson: **multi-round pre-flight review is more
efficient than multi-round implementation review** when the
contract surface is non-trivial.

### Implementation Codex rounds — four rounds, six findings (2 P1, 4 P2, 0 P3)

| Round | Findings | Theme |
|-------|----------|-------|
| 1 | 2 (2 P1) | Failed dispatch legs skipped `meta.source` aggregation (recorded AFTER `await`); null per-target payload (no `errors[]`) treated as illusory success. |
| 2 | 2 (2 P2) | Missing mutation root key conflated with null payload (zod `.loose()` accepts missing key under `z.unknown()`); round-1 F1 regression test didn't exercise the bug. |
| 3 | 1 (1 P2) | Missing-root-key fix from round-2 only landed on partial-success verbs (add-users + remove-users); single-target verbs (update + delete) still conflated. |
| 4 | 0 — closure confirmed | All four mutation verbs distinguish missing-root-key from null-payload. |

**Pattern explaining the implementation findings: the
`fix-then-audit-laterally` lesson M13 round-2 logged generalised
to a four-round arc.** Each round caught the same class of bug
applied to a route the previous round didn't propagate to:

- Round 1: dispatch leg recording in add-users → also fixed
  remove-users (same commit).
- Round 2: missing-root-key fix in add-users + remove-users.
  Round 3 caught update + delete.
- Round 3 closed the partial route audit; round 4 confirmed.

The round count is 4, not 3 (M13's pattern), because M14
introduced TWO new contract surfaces (partial-success-envelope
fan-out + missing-root-key vs null-payload distinction) where M13
introduced one. Each new contract takes one round to surface +
one round to propagate to parallel routes; M14's two surfaces
needed 2+2 = 4 rounds. The discipline scales: don't cap rounds
arbitrarily, just keep going until P1/P2 returns zero.

### The single load-bearing lesson — `internal_error` is whole-call, always

Round-2's F1 surfaced the most subtle bug: the partial-success
contract is "ok: true when dispatch ran; per-target failures
land in `results[i].error`" — but the `internal_error` code is a
special case that should NEVER land per-record. `internal_error`
signals CLI bugs or Monday-side schema drift; both are whole-call
surfaces that an agent needs to know about distinctly from "this
specific user wasn't found." The fix:

1. `dispatchSequential` re-throws `internal_error` MondayCliErrors,
   propagating them to the runner's catch-all.
2. The per-verb `assertDispatchPayloadPresent` distinguishes
   missing-root-key (`internal_error`) from null-value (`not_found`).

Both layers needed to change for the contract to hold. M13
shipped without the layer-1 fix because `update clear-all`'s
`assertUpdateMutationPresent` only throws `not_found` — the
re-throw path doesn't activate at the M13 site. The M13 site
would have the same conflation bug if a future change rewrote
`assertUpdateMutationPresent` to throw `internal_error` on
missing-key; the M14 fix makes `dispatchSequential` defend
against that retroactively.

This generalises a M11 lesson — "wire-shape claims need empirical
proof" — to "envelope-shape claims need cross-layer proof". When
a verb's contract spans multiple layers (resolver / dispatch /
projection / envelope), each layer can fail independently. The
test suite needs to exercise each layer's contract, not just the
one closest to the user. M14's cache-mixed regression test (round-2
F2 fix) is the canonical example: a test that pre-fix passes for
the wrong reason (SourceAggregator's fallback default emitting
`'live'`) doesn't actually pin the contract.

### Two-phase wire-shape pinning

M14's verbs split into two contract clusters:

- **Single-target full-projection**: create + update + delete.
  Each returns the full Workspace shape via
  `workspaceGetOutputSchema` (the schema `workspace get` already
  pins). Three sites share the projection; M14 close logs the
  consolidation candidate (`src/api/workspace-projection.ts`) but
  doesn't lift — the lift is best done when M15's board cluster
  arrives with a parallel split (board create / update / delete
  all return the Board shape). At that point, the consolidation
  pattern repeats: 3 workspace sites + 3 board sites = 6 sites,
  past-trigger by a wide margin.
- **Per-target fan-out partial-success**: add-users + remove-users.
  Each fires one `add_users_to_workspace` / `delete_users_from_workspace`
  call per user via `dispatchSequential`. The two verbs share
  ~90% of their action body (parser, resolution, dispatch,
  envelope assembly). M14 close logs the consolidation candidate
  (a workspace-users-mutation helper) but doesn't lift — M15's
  `board add-users` is the third consumer that triggers it.

The "lift on third consumer" rule (v0.1-plan §17) holds: don't
lift before the trigger. R29's lift (5 consumers) was past-due;
the workspace projection + workspace-users helpers (3 consumers
each, projected) lift in M15-close or M14 → M15 cleanup window.

### R-class candidates surfaced post-M14

Three lifts logged for the M14 → M15 cleanup window OR M15 close:

**R39 — `workspace-projection` schema lift.** Three consumers
(workspace get / create / update / delete = 4 actually). The shape
duplicates verbatim across all four sites. Trigger: 4 consumers
exceeds the threshold. Recommendation: lift in M14 → M15 cleanup
window OR alongside M15's board cluster (which adds 3-4 more
consumers and creates the parallel symmetry the lift wants).

**R40 — partial-success `--users` resolver-fronted-fan-out
helper.** Two consumers in M14 (add-users + remove-users) sharing
~90% of their action body: the `parseUsersArg` token parser, the
`resolveTokens` resolver loop, the dispatch loop, the envelope
assembly. Trigger fires on the third consumer — M15's
`board add-users` (cli-design §4.3 line 465). Recommendation:
lift at M15 close once the third consumer's parameter shape is
known (e.g. workspace_id vs board_id; whether `kind?` parameter
becomes meaningful at the surface level).

**R41 — `assertDispatchPayloadPresent` null-payload guard.**
Two M14 sites (add-users + remove-users) duplicate the missing-
root-key + null-value logic verbatim modulo the mutation root
key name. Combined with M13's `assertUpdateMutationPresent` (one
site, but uses similar shape), this could lift to a parameterised
helper. Trigger: 2 sites → below trigger. Plus the M13 helper
uses `not_found` on null while M14's uses both `internal_error`
(missing key) and `not_found` (null value); a unified helper
would either retrofit M13 or stay split. Recommendation: defer
until M15's add-users adds the third consumer; revisit then.

### Coverage trajectory

| File | Branch coverage | Notes |
|------|-----------------|-------|
| `commands/workspace/create.ts` | 100 | Trivial mutation; every branch covered. |
| `commands/workspace/update.ts` | 79.31 | Three flag-presence branches × diff-builder + missing-root-key + zero-attributes-defensive (c8-ignored). The defensive branches dominate the per-file ratio. |
| `commands/workspace/delete.ts` | 100 | Inline gate + projection + null-mapping all covered by integration tests. |
| `commands/workspace/add-users.ts` | 92.5 | Per-token-resolution branches × per-target-dispatch branches × meta.source aggregation; the genuinely-defensive branches (non-`user_not_found` re-throw; empty-tokens-array fallback) are c8-ignored. |
| `commands/workspace/remove-users.ts` | 82.5 | Same shape as add-users; one less branch covered because the partial-success forbidden test doesn't repeat for remove-users (verb symmetry; add-users covers the shape). Could rise to 92+ with two more parallel tests; deferred to M14 → M15 cleanup if branch coverage tightens. |
| `api/destructive-gate.ts` | 100 | Helper exercised through all five destructive-verb sites' integration tests; `previewSuffix` default + override both covered. |
| `api/partial-success-mutation.ts` | 70 (nominal) | The new `internal_error` re-throw branch is exercised; the non-`MondayCliError` re-throw stays defensive (programmer-bug-only). The 70% number sits on a small surface; the genuinely-impossible branches dominate. |
| Project total | 95.04 | Above the 94/95/95/95 floor. |

The per-file workspace dip (78-92%) is the M14 close item to
audit at M15 review. The cluster's branch coverage is dominated
by defensive branches the test suite can't drive without
contriving malformed inputs; the helper consolidation candidates
(R40 + R41) would compress these into a single helper file's
coverage report and likely boost the workspace verbs' per-file
numbers above 95.

### Post-flight rituals — pre-flight, empirical proof, contract-locking, fix-then-audit-laterally

M14 ratified four standard rituals every post-M14 milestone
inherits:

1. **Pre-flight contract diff** (M13's bb7ee17 pattern): cli-
   design §4.3 + §6.4 land BEFORE any implementation. Each
   pre-flight gets ≥1 Codex round. Round count scales with
   contract complexity (M13 = 1 round; M14 = 3 rounds).
2. **Empirical proof rituals** (M11/M12): every wire-shape claim
   pinned via integration test fixture's `match_variables` +
   `match_query` regex. M13 round-2 added the `match_query`
   discipline; M14's add-users / remove-users + workspace update
   inherit it.
3. **Pre-flight contract-locking ritual** (M13): the docs(mNN)
   pre-flight commit ratifies the contract before code touches
   it. Reduces implementation rounds from "is the contract
   right?" (open-ended) to "does the code match the contract?"
   (narrow).
4. **Fix-then-audit-laterally** (M13 round-2; M14 generalised):
   when a Codex finding fires on route A, the first question is
   "do parallel routes B / C / … exist, and could the same bug
   hit them?" M14's four-round arc was the worked example of this
   discipline applied at scale — round 1 fixed two routes; round 2
   caught a third; round 3 caught a fourth; round 4 confirmed
   closure.

Each ritual addresses a different failure mode; using all four
keeps review rounds bounded (M14 = 7 rounds total but every
round caught real contract drift; M12 = 5 rounds where most were
about contract claims that should have been pre-flight'd).

---

## 22. Refactor backlog — surfaced post-M14

Four R-class candidates surfaced auditing M14's five new mutation
verbs against the v0.1-plan §17 R-timing rule ("lift on the third
consumer; four is overdue"). Same shape v0.1 §15 + §17 + v0.2 §12 +
§14 + §16 + §18 + §20 used: each entry names the duplication, the
shape proposed, the sites that'd be touched, the trigger state, and
a defer-or-ship recommendation. **Documented only — no
implementation work this session.**

### R39 — `WORKSPACE_FIELDS_FRAGMENT` + `workspaceProjectionSchema` lift

**Shipped: ec1398b.**

**Sites (5):** the workspace field selection is duplicated across
five GraphQL strings (one per verb on the with-settings path:
`workspace get` / `workspace create` / `workspace update`'s
preflight read + mutation / `workspace delete`). The matching zod
projection schema (`workspaceGetOutputSchema` defined at
`src/commands/workspace/get.ts:50`) is already imported by all four
mutation verbs, so the schema-side duplication is partially closed.
The GraphQL-fragment side isn't.

`workspace list` has its own narrower schema (no `settings.icon`)
and a parallel narrower GraphQL string. R39's minimal scope keeps
list distinct — lifting the with-settings fragment + the with-
settings schema serves the four mutation verbs, leaving list as-is.

Mirror: R28 (`ITEM_FIELDS_FRAGMENT` + `parseRawItem` +
`projectMutationItem` at `src/api/item-helpers.ts` +
`src/api/item-mutation-result.ts`) and R38
(`UPDATE_FIELDS_FRAGMENT` at `src/api/update-mutation-result.ts`).
Both followed the same shape — one shared fragment + one shared
schema + a thin assertion helper.

**Proposed shape:** new `src/api/workspace-projection.ts` exporting
`WORKSPACE_FIELDS_FRAGMENT` (the GraphQL field list with
`settings.icon`) + `workspaceProjectionSchema` (the zod schema with
`settings.icon`) + `WorkspaceProjection` type. Migrate get + create
+ update + delete to import both. The five GraphQL strings
collapse to four interpolations (`${WORKSPACE_FIELDS_FRAGMENT}`)
plus the surrounding mutation declarations; the schema definition
moves out of `workspace/get.ts`.

**Trigger:** 4 schema-side consumers + 5 fragment-side consumers
exists today. Past trigger by a wide margin. The lift fired
silently when M14 shipped — every mutation verb ALREADY imported
the schema from `workspace/get.ts`, but the file colocation was
incidental rather than intentional.

**Risk:** low. Pure consolidation; every existing test passes
byte-identical post-lift. The one design decision is whether to
extend `workspace list` to share the fragment too (would add
`settings.icon` to its output shape — a SemVer-minor additive
change). Recommendation: keep list distinct (no surface change);
lift only the with-settings fragment + schema for the four mutation
verbs.

**When to schedule:** **M14 → M15 cleanup window** OR alongside
M15's board cluster (which adds 3-4 more workspace-shaped
projections via `board create` / `update` / `delete` /
`duplicate`). Bundling with M15 is cleaner if R39 + the parallel
board lift land as one consolidated commit; shipping standalone is
fine too. Either timing fits the v0.1-plan §17 R-timing rule.

### R40 — partial-success `--users` resolver-fronted-fan-out helper — **Shipped: `541e5e7`**

**Sites (2):** `workspace add-users` / `workspace remove-users`
share ~90% of their action body — the `parseUsersArg` token parser
(numeric vs email validation), the `resolveTokens` resolver loop
(per-token `userByEmail` for emails; numeric IDs argv-derived),
the dispatch loop (per-target `dispatchSequential` call), and the
envelope-assembly logic (merging resolution outcomes with dispatch
outcomes via `dispatchableIndices`).

Per-verb the divergence is: the GraphQL mutation string + the
operation-name literal in `data.operation` + the operation-name
parameter to `client.raw`. Three string parameters; everything
else is identical.

**Proposed shape:** new `src/api/users-fan-out-mutation.ts`
exporting `dispatchUsersFanOut({client, env, workspaceTarget,
mutationQuery, operationName, dataOperation, noCache, dryRun,
ctx, programOpts, command})` returning the partial-success
envelope. Each call site collapses from ~200 LOC of action body
to a single function call. The envelope shape stays per-verb
(distinct `data.operation` literals via `dataOperation`
parameter); the per-record `user_id` + `ok` + `error?` shape
stays uniform.

**Trigger:** 2 consumers today (workspace add-users + remove-users).
M15 `board add-users` (cli-design §4.3 line 465) is the third
consumer; v0.2-plan §3 M15 deliverables pin it as a copy of
M14's add-users shape with `board_id` substituted for
`workspace_id`. Lift fires when M15 ships its third consumer.

**Risk:** medium. The factory's parameter surface needs to
accommodate per-noun divergence (workspace vs board target id;
the `kind?: WorkspaceSubscriberKind` slot M14 omits but M15's
board add-users may surface). Pre-lift the workspace verbs work;
post-lift M15's board add-users joins as a third config value —
but if M15's surface diverges (e.g. takes a `kind` flag for
subscriber-vs-owner), the factory's parameter shape needs that
slot from day one. **Mitigation:** wait for M15 implementation to
stabilise before lifting — the factory's parameters become known
at the moment of M15's add-users implementation.

**Shipped at M15 → M16 cleanup window** (`541e5e7 refactor(r40):
lift partial-success --users fan-out helper into api/users-fan-
out-mutation`). All three consumers (workspace add-users +
workspace remove-users + board add-users) migrate to
`dispatchUsersFanOut` from `src/api/users-fan-out-mutation.ts`;
the helper exposes `parseUsersArg` separately so each call site
preserves the `parseArgv → parseUsersArg → resolveClient` order
that pins `usage_error` (malformed `--users`) ahead of
`config_error` (missing token). Net –766 LOC across the three
call sites + ~+360 LOC in the new helper file. 2023 tests pass
byte-identical post-lift; project coverage 98.97 / 95.31 / 99.47
/ 99.11 (above the 94/95/95/95 floor).

The factory signature pins six per-verb parameters (mutation
query + operationName + rootKey, dataOperation literal, scope
key + id + variableKey, verbDescription) — every divergence the
three pre-lift call sites carried. R44's generic-over-nouns lift
stays deferred (see R44).

### R41 — `assertDispatchPayloadPresent` null-payload guard — **Shipped: `4756269`**

**Sites (3 at lift):** `workspace add-users` / `workspace remove-users`
each define a verbatim `assertDispatchPayloadPresent` function
(differing only in the mutation root key name). The function
distinguishes "key absent" (`internal_error`, whole-call) from
"value null" (`not_found`, per-record) — the contract M14 round-2
F1 + round-3 F1 pinned.

M13's `assertUpdateMutationPresent` (at
`src/api/update-mutation-result.ts:155`) is adjacent: it handles
null/undefined uniformly with `not_found`, NOT distinguishing
missing-root-key from null-value. R41's lift would
(a) consolidate the M14 inline duplication and (b) optionally
retrofit M13's helper to match M14's contract semantics (or leave
M13 as-is — see R42 for the cross-cutting fix).

**Proposed shape:** new `src/api/response-root.ts` exporting
`assertResponseFieldPresent(data, key, internalErrorContext,
notFoundDetails)` that throws `internal_error` on key absent,
`not_found` on value null. Each M14 partial-success verb's
inline `assertDispatchPayloadPresent` collapses to a one-liner.
The helper composes with the existing per-verb projection: the
dispatch callback runs the assertion BEFORE returning, so
`dispatchSequential` decorates per-target failures via the same
catch arm. The `internal_error` re-throw path (M14 round-2 F1)
stays in `dispatchSequential`; R41 doesn't touch that.

**Trigger:** 2 consumers today (workspace add-users + remove-users).
M15 `board add-users` is the third — same trigger as R40.

**Risk:** low. Pure consolidation. Tests pass byte-identical
post-lift.

**Shipped at M15 close** (`4756269 refactor(r41): lift partial-
success null-payload guard into api/response-root`). All three
consumers migrate to `assertResponseFieldPresent` from
`src/api/response-root.ts`; the helper signature pins the three
parameter divergences (mutation root key, operation label, scope-
id key/value). 2023 tests pass byte-identical post-lift.

**R40 stays deferred** to the M15 → M16 cleanup window — its
surface is larger (~250 LOC consolidation across three files
including the parseUsersArg + resolveTokens loops + dispatch loop
+ envelope assembly) and benefits from a focused session rather
than bundling here. R40's parameter shape IS now empirically
known after `board add-users` shipped (workspace+board variants
share everything except 4 string parameters), so the lift's
factory signature is fully characterised — the "wait for the
third consumer" guard cleared.

### R42 — retroactive missing-root-key distinction across pre-M14 mutation verbs

**Sites (~27 post-M16):** every single-target mutation
projection call across M5b/M9/M10/M11/M12/M13 currently
conflates "missing mutation root key" (schema drift) with
"value null" (idiomatic Monday-side missing/no-access). The fix
M14 round-2 F1 + round-3 F1 pinned for workspace verbs hasn't
propagated to these older sites. M15's six new mutation sites
+ M16's four new column-mutation sites land the distinction
inline at write-time (matching the M14 round-2 / round-3 pin
proactively); R42 consolidates the inline check across the
~17 pre-M14 sites + the ~6 M15 sites + the ~4 M16 sites onto
R41's `assertResponseFieldPresent` helper for one uniform
shape. Sites:

- M5b `item set` (`change_simple_column_value` / `change_column_
  value` — 2 paths) at `src/commands/item/set.ts:426,447`.
- M5b `item clear` (3 paths — simple / rich / multiple-clear) at
  `src/commands/item/clear.ts:474,503`.
- M5b `item update` (3 paths — simple / rich / multiple) at
  `src/commands/item/update.ts:545,562,578`.
- M9 `item create` / `item create-subitem` (`create_item` /
  `create_subitem`) at `src/commands/item/create.ts`.
- M10 `item archive` / `item delete` / `item duplicate` at
  `src/commands/item/{archive,delete,duplicate}.ts`.
- M11 `item move` (group + cross-board paths) at
  `src/commands/item/move.ts:552,689`.
- M12 `item upsert` (4 projection sites — create / update /
  bulk-clear paths) at `src/commands/item/upsert.ts:630,666,697,
  719`.
- M13 `update create` / `update reply` / `update edit` /
  `update delete` / `update toggle` (×4 verbs) at
  `src/commands/update/{create,reply,edit,delete,toggle}.ts`.
- M13 `update clear-all` per-target dispatch at
  `src/commands/update/clear-all.ts:341` (uses
  `assertUpdateMutationPresent` which throws `not_found`).

Total pre-M14: ~17 single-target sites + 1 partial-success
site. **+6 M15 sites** (`board create` / `board update` /
`board archive` / `board delete` / `board duplicate` /
`board add-users`). **+4 M16 sites** (`column-create` /
`column-update` ×2 wire surfaces — `change_column_title` +
`change_column_metadata` / `column-delete`). Grand total
post-M16: **~27 sites**, all currently inline missing-root
checks that R42 would consolidate via `assertResponseFieldPresent`.

**Why it's worth fixing:** Codex M14 round-4 explicitly noted this
as "pre-existing and matches your stated close-out scope, so not a
round-4 finding" — but the contract gap is real. An agent driving
`monday item delete <iid>` against a Monday tenant where the
GraphQL schema drops `delete_item` from the response (rare; theoretical
but possible during an API version transition) sees `not_found` and
treats it as "this item was already deleted" rather than the actual
"contract drift" signal. The semantic distinction matters for
agents writing recovery loops keyed off `error.code`.

**Proposed shape:** the lift R41 introduces (`assertResponseFieldPresent`
at `src/api/response-root.ts`) gets used at every pre-M14 mutation
projection site. Two patterns:

1. **Inline assertion before existing helper call:**
   ```ts
   assertResponseFieldPresent(data, 'archive_item', {
     mutationName: 'archive_item',
     details: { item_id: itemId },
   });
   const projected = projectMutationItem({
     raw: data.archive_item,
     itemId,
     errorCode: 'not_found',
     errorMessage: '...',
   });
   ```
   Each site adds ~3 lines.

2. **Extend `projectMutationItem` / `projectMutationUpdate` to take
   the parsed response root + key name** (optional new parameters):
   ```ts
   const projected = projectMutationItem({
     raw: data.archive_item,
     itemId,
     errorCode: 'not_found',
     errorMessage: '...',
     responseRoot: data,         // optional
     fieldName: 'archive_item',  // optional
   });
   ```
   When both optional params provided, the helper distinguishes
   missing-key (internal_error) from null-value
   (errorCode = caller's code).

   Lift fewer call-site lines, more helper-side complexity.

Pattern 1 is cleaner — single-responsibility helpers, explicit
ordering at each site. Pattern 2 is compact — one helper handles
both checks. Recommendation: go with pattern 1.

**Trigger:** N/A — this is a retroactive consistency fix, not a
duplication-trigger lift. Ships when prioritised.

**Risk:** low for individual sites (each fix is mechanical and
additive); medium aggregate (touching ~17 files at once is a real
review surface). **Mitigation:** ship as a dedicated session with
per-noun batching (e.g. one commit for all M5b sites, one for M10,
etc.) so each commit's diff is reviewable. Or one big atomic
commit if the diff stays under ~500 LOC.

**When to schedule:** **dedicated post-M16 cleanup session.**
R42 is purely additive and doesn't block M17. M15 + M16 sites
land the inline missing-root distinction proactively at write-
time (matching the M14 round-2 / round-3 pin), so the contract
consistency is held — R42 just consolidates the boilerplate
onto R41's helper. **Recommended:** post-M16 dedicated session
absorbing all ~27 sites in one focused review pass. Per-noun
batching (one commit for M5b sites, one for M10 / M11 / M12,
one for M13, one for M15, one for M16) keeps each commit's
diff reviewable; bundling into one big commit is also viable
if the diff stays under ~500 LOC.

### R43 — `projectMutationBoard` helper — **Shipped: `6b45abf`**

**Sites (5):** Each M15 mutation verb defines a near-verbatim
projection function (or inline `unwrapOrThrow` parse) that
null-checks the wire payload and parses through
`boardProjectionSchema`:

- `src/commands/board/create.ts:213` — `projectCreatedBoard`
  (errorCode: `internal_error` on null; create's contract is
  "every successful call returns a Board").
- `src/commands/board/archive.ts:201` — `projectArchivedBoard`
  (errorCode: `not_found` on null; archive's null-payload is
  "board missing").
- `src/commands/board/delete.ts:191` — `projectDeletedBoard`
  (errorCode: `not_found` on null; delete's null-payload is
  "board missing / already deleted").
- `src/commands/board/update.ts` — inline final-read parse via
  `unwrapOrThrow(boardUpdateOutputSchema.safeParse(first), ...)`
  with a defensive `internal_error` on null `first`.
- `src/commands/board/duplicate.ts` — inline wrapped-board parse
  via `unwrapOrThrow(boardProjectionSchema.safeParse(wrapper.board),
  ...)` with `internal_error` on null inner board.

Per-verb the divergence is two strings + one enum value: the
errorCode (`'not_found'` | `'internal_error'`), the errorMessage
prefix, and the resourceId/Name detail key (`board_id` /
`board_name`). Everything else (null-check, parse, throw shape)
is verbatim.

Mirrors **R28** (`projectMutationItem` lifted at 4 consumers in
`src/api/item-mutation-result.ts`) and **R37**
(`projectMutationUpdate` lifted at 5 consumers in
`src/api/update-mutation-result.ts`) — both per-noun helpers
landed at the same 4–5 consumer trigger. R43 ships the third
per-noun helper at the same threshold.

**Proposed shape:** new `src/api/board-mutation-result.ts`
exporting:

```ts
projectMutationBoard({
  raw,
  errorCode,        // 'not_found' | 'internal_error'
  errorMessage,     // full message (caller-formatted with the id/name)
  detailKey,        // 'board_id' | 'board_name'
  detailValue,      // the actual id or name
}): BoardProjection
```

Each call site collapses from a 7–10 LOC named function (or
inline `unwrapOrThrow` block) to a single object-literal call.

**Risk:** low. Pure consolidation. Tests pass byte-identical
post-lift (same discipline as R28 / R37 / R39 / R41).

**Shipped at M15 → M16 cleanup window** (`6b45abf refactor(r43):
lift board mutation projection into api/board-mutation-result`).
Five consumers (board create / archive / delete / update / duplicate)
migrate to `projectMutationBoard` from
`src/api/board-mutation-result.ts`; the helper takes the
per-verb `errorCode` + caller-formatted `errorMessage` +
`detailKey` (`'board_id'` or `'board_name'`) + `detailValue`.
The outer wrapper-null guard in `board duplicate` (the §6.4
`BoardDuplication` envelope's null-`duplicate_board` path) stays
at the call site since R43 only covers the inner-board
projection. Net –29 LOC across the five board verbs + ~+50 LOC
in the new helper. 2023 tests pass byte-identical post-lift;
project coverage stays above the 94/95/95/95 floor. Shipped as
a separate commit from R40 (different verb families, no shared
helper target).

### R44 — generic `projectMutationResource` over R28/R37/R43/R45

**Sites (4 helpers, ~17 consumers post-R45):** Post-R45 the
per-noun projection helper count is 4: `projectMutationItem`
(R28) / `projectMutationUpdate` (R37) / `projectMutationBoard`
(R43) / `projectMutationColumn` (R45) — plus the 3 inline-only
workspace single-target mutation verbs that never got their own
helper. The four helpers diverge on:

- `idKey` (e.g. `'item_id'` / `'update_id'` / `'board_id'` /
  `'column_id'` / `'workspace_id'`).
- `errorMessage` prefix (boilerplate per noun — "Monday returned
  no item from …" / "no update from …" / "no column from …" /
  etc.).
- `errorCode` (per-call-site: `'not_found'` vs
  `'internal_error'`).
- `schema` (the noun's projection schema).

Everything else (the `if (raw === null || raw === undefined)`
check + the `throw new ApiError(errorCode, errorMessage,
{ details: { [idKey]: idValue } })` shape + the
`unwrapOrThrow(schema.safeParse(raw), { context, details })`
parse) is identical.

**Proposed shape:** new `src/api/mutation-result.ts` exporting
`projectMutationResource({ raw, schema, idKey, idValue,
errorCode, errorMessage, context })`. The four per-noun
helpers (R28 / R37 / R43 / R45 + a new
`projectMutationWorkspace`) collapse to thin wrappers that fill
in the schema + idKey + a default context message.

**Trigger:** post-R45 the count is 17+ consumers across 4
nouns. Past §17 R-timing by a wide margin. But the per-noun
wrappers add a stylistic indirection layer — not every project
wants a generic-over-per-noun.

**Risk:** medium. Touches 4 noun families simultaneously and
the cassette assertions on each. The byte-identical-tests
discipline catches behavioural regressions, but the design
question ("is the per-noun wrapper layer worth keeping?") is
aesthetic and can split a maintainer one way or the other.

**When to schedule:** **defer to v0.3 OR a fifth-noun trigger**.
M16 (board columns) shipped R45 (`projectMutationColumn`) at
implementation start, taking the per-noun count from 3 to 4
without firing R44 — touching R44 alongside R45 would have
re-touched three sibling helpers and scope-crept into a
five-noun refactor. The fifth-noun trigger now becomes M17's
`projectMutationGroup` (board groups also expose a per-mutation
`Maybe<Group>` shape that R44's helper would consolidate); if
M17 adds the fifth per-noun helper, R44's trigger fires
cleanly at the M17 → M18 cleanup window. Until then, R28 /
R37 / R43 / R45 stay as parallel per-noun helpers and the
duplication-cost is acceptable.

### R45 — `projectMutationColumn` helper (R28/R37/R43 sibling for `Column`) — **Shipped: `26aa821`**

**Surfaced by M16 pre-flight.** The three M16 column verbs
(`column-create` / `column-update` / `column-delete`) all
return Monday's `Maybe<Column>` post-mutation. Each verb's
success-envelope `data` projects the wire response through a
zod schema and surfaces a missing-root-key vs null-payload
distinction (per R41 / R42's contract pin). With three
consumers landing in one milestone, lifting `projectMutation
Column` alongside the first M16 commit (mirroring R39's
"ship the projection helper alongside the first new mutation
rather than as a follow-up R-class" precedent) avoids three
parallel inline implementations of the same shape.

**Sites at M16 ship (3):** column-create + column-update +
column-delete.

**Proposed shape:** new `src/api/column-mutation-result.ts`
exporting `projectMutationColumn({ raw, idValue, errorCode,
errorMessage })` per the R28/R37/R43 wrapper shape.
`COLUMN_FIELDS_FRAGMENT` + `columnProjectionSchema` lift
alongside (mirroring R39's `WORKSPACE_FIELDS_FRAGMENT` +
`workspaceProjectionSchema` co-ship). The schema mirrors
the `columnSchema` already in `src/api/board-metadata.ts`
line 98 (`{id, title, type, description, archived,
settings_str, width}`), but as an exported strict projection
schema rather than a private board-metadata field.

**Trigger:** 3-consumer pre-shipping count + R39 precedent
("ship alongside the first new mutation"). M16's three verbs
arrive together rather than spread across milestones, which
is exactly the case R39's pattern was written for.

**Risk:** low. Tracks R28/R37/R43 verbatim with the per-noun
divergences (idKey: `column_id` paired with `board_id` —
the two-tuple wire signature); the helper file is ~50 LOC
plus a strict schema. Byte-identical-tests discipline catches
behavioural regressions across the three consumers. The
fourth-noun lift is a setup for R44's "fifth-noun trigger"
without itself triggering R44 (touching three per-noun
helpers + a fourth at the same time would scope-creep to a
4-noun refactor, exactly the cost R44's deferral wording
calls out).

**When to schedule:** **ship at M16 implementation start, not
post-cleanup.** Mirrors R39's M15-implementation timing — the
first M16 column verb's commit lifts the helper + adopts it
in one go. Every subsequent column-verb commit consumes the
helper rather than cloning the inline shape.

### R46 — `withBoardInvalidation` post-success projection wrapper — **Shipped: `5b06d53`**

**Surfaced by M16 pre-flight §8 contract.** The cli-design §8
eager-invalidation contract pins a non-trivial timing rule:
single-leg verbs invalidate AFTER success-envelope `data`
projection (skip on error path); fan-out verbs invalidate
ONCE after the per-attribute loop settles, conditional on at
least one per-call leg succeeding. The rule MUST land
identically across every consumer or the contract leaks at
implementation time (the cache could end up either over-
invalidated, under-invalidated, or invalidated at the wrong
time relative to data projection). A wrapper that takes the
projection function + the `invalidateBoard(boardId)` call as
parameters and enforces the timing in the helper signature
is the natural lift.

**Sites projected by M16 close (4):** column-create +
column-delete (single-leg) + the M15 `board archive` + `board
delete` retrofits (single-leg). M15 retrofit's `board update`
fan-out + M16's `column-update` fan-out are also consumers
but use the fan-out-conditional variant (different timing
rule).
**Sites projected by M17 close (9):** + group-create +
group-update (fan-out variant) + group-archive + group-
duplicate + group-delete.

**Proposed shape:** new `src/api/board-mutation-invalidation.ts`
exporting two helpers covering the §8 timing-rule split:

- `withBoardInvalidationSingleLeg({boardId, env, runMutation,
  projectData})` — runs `runMutation`, projects `data`,
  invalidates AFTER projection on success, skips on error.
- `withBoardInvalidationFanOut({boardId, env, runFanOut,
  projectData})` — runs the per-attribute loop; invalidates
  ONCE after the loop settles iff `runFanOut`'s
  high-water-mark counter > 0; on whole-call success the
  invalidation lands after the trailing call's data
  projection; on partial-application failure invalidation
  fires before the error envelope returns.

The helper signature pins the timing rule in the type system
— callers can't accidentally invalidate before the wire call
or skip invalidation on partial-application error.

**Trigger:** 4 consumers at M16 close + 5 more at M17 close
(9 total before v0.3). Above the typical 3-consumer trigger
threshold; the §8 contract's timing-invariant load-bearing-
ness pushes it harder than usual (a per-verb implementation
mistake silently breaks cache freshness rather than failing
loudly).

**Risk:** medium-low. The fan-out variant's high-water-mark
counter is the load-bearing detail; getting it wrong inverts
the §8 contract. Byte-identical-tests catch behavioural
regression across consumers; integration cassettes assert
the §8 "no `stale_cache_refreshed` warning on the eager-
invalidation happy path" rule (per the M16 milestone test
plan), which catches both timing variants.

**When to schedule:** **post-M16 cleanup window** (mirrors
R40 + R43's M15 → M16 cleanup-window timing). M16's three
column verbs + the M15 retrofit cluster ship FIRST as inline
implementations; the lift consolidates them into a single
helper after all four call-site shapes have stress-tested
the contract. Pre-shipping the helper would risk locking the
signature before the consumers have validated it. M17's
group verbs adopt the helper from day one, mirroring R29's
M14 close-then-M15-adopts pattern.

### Sites NOT lifting yet

- **`ensureSubcommand('workspace', 'Workspace commands')`
  repetition** across the eight workspace command files (3 reads
  + 5 mutations). Each call is one line; `ensureSubcommand`
  already absorbs the find-or-create work. Not worth lifting
  further. Same for the other nouns (`item`, `board`, `update`,
  `user`).
- **Single-target `confirmation_required` details builder.**
  R29's `enforceDestructiveGate({detailKey, hint, ...})` already
  centralises the gate-firing + the per-call detail decoration;
  the §6.5 single-target shape pinned in the M16 pre-flight is
  what R29 already constructs. No further lift needed — the
  contract pin is documentation of what R29 already does.
- **`--users` token validation regex** (`NUMERIC_TOKEN_PATTERN` +
  `EMAIL_TOKEN_PATTERN`) duplicated between add-users and
  remove-users. Two consumers; below trigger. Falls inside R40's
  lift scope when it fires.
- **`parseGlobalFlags` re-parse** in destructive-verb sites. Each
  site calls `parseGlobalFlags(program.opts(), ctx.env)` for the
  gate, then `resolveClient` re-parses internally. The double-parse
  is intentional (preserves the gate-before-`resolveClient`
  ordering invariant in the type signature) and microsecond-cheap.
  Not worth lifting.
- **`details: { <id_field>: <id>, hint: ... }` error decoration**
  on per-verb destructive-gate calls. R29 already lifted the gate
  + the `details` shape; the per-verb hint text varies enough that
  further consolidation would be aesthetic rather than value-
  adding.
- **The pre-flight ritual itself** (M12's bb7ee17 → M13's bb7ee17
  → M14's 3fe1d58 — all ship `docs(mNN): pre-flight cli-design
  contract for ...` commits). Documented as one of the four
  standard rituals in §21; not a code refactor.

### Recommended ordering

The clear path:

1. **M14 → M15 cleanup window — ship R39** as a single
   `refactor(r39):` commit (mirrors R28/R38's shape). 4 sites
   migrate + new helper file; pure consolidation.
2. **M15 implementation — ship `board add-users`** as part of
   M15's verb cluster. This arrives as the third consumer of the
   partial-success-fan-out pattern.
3. **M15 close — ship R40 + R41 together** as a single
   `refactor(r40+r41):` commit (mirrors R37+R38's shape).
   `dispatchUsersFanOut` + `assertResponseFieldPresent` lift, three
   call sites adopt.
4. **Post-M16 dedicated cleanup session — ship R42** as a
   focused session touching the ~27 retroactive sites (~17
   pre-M14 + 6 M15 mutation sites + 4 M16 column-mutation
   sites). One bigger commit OR per-noun batched commits.
5. **M16 implementation start — ship R45** alongside the first
   M16 column verb commit. `projectMutationColumn` + `COLUMN_
   FIELDS_FRAGMENT` + `columnProjectionSchema` lift; mirrors
   R39's "ship the projection helper alongside the first new
   mutation" precedent. The three M16 column verbs adopt the
   helper from day one rather than each landing inline shapes.
6. **Post-M16 cleanup window — ship R46**. The four M16-shipped
   call sites (column-create + column-delete single-leg; column-
   update + M15 retrofit `board update` fan-out) plus the three
   M15 retrofit cluster sites stress-test the timing contract
   first, then lift consolidates them. M17 group verbs adopt
   the helper from day one (mirrors R29's M14-close-then-M15-
   adopts pattern).

The trivial-cluster pattern from M9.5 (R22 + R24 + R25 as three
separate commits) doesn't apply to R40 + R41 because they share
the same lift target (the new fan-out helper + the new response-
root assertion are coupled). One commit for both is cleaner.
R45 and R46 don't bundle either — R45 lands at M16 implementation
start; R46 lands at M16 cleanup window after the four call sites
have stress-tested the timing contract.

---

## 23. M15 post-mortem — six verbs, wrapped envelope, two Codex round-clusters

M15 shipped on `main` across **fifteen** atomic commits — one
docs pre-flight (`a3b03c3` — cli-design §4.3 + §6.4 board verb
annotations + six new operation shapes), four pre-flight Codex
rounds (`62934e3` / `27c66f3` / `b222d23` / `080946b` — 4 + 4 +
6 + 1 findings = 15 total, 1 P1 + 9 P2 + 5 P3, all caught before
any code shipped), six feat commits (one per verb, in order:
create / update / archive / delete / duplicate / add-users), two
implementation Codex rounds (`ba88ea8` / `28e44bd` — 3 + 3
findings, 0 P1 + 2 P2 + 4 P3), one R41 lift (`4756269` —
partial-success null-payload guard), and this docs close.
**Codex rounds total: six (four pre-flight + two
implementation)** — one fewer than M14's seven; the pre-flight
took one extra round but the implementation closed in two,
suggesting the heavier pre-flight contract investment is paying
off in shorter implementation review cycles. Project coverage:
98.96 / 95.04 / 99.48 / 99.14 — branches held at the M14-close
baseline despite M15's larger surface (six verbs + wrapped
envelope shape + per-attribute fan-out wire). Tests 1957 (M14
close) → 2023 (M15 close).

### Pre-flight Codex rounds — four rounds, fifteen findings (1 P1, 9 P2, 5 P3)

The pre-flight pattern bb7ee17 ratified in M13 + 3fe1d58
ratified in M14 paid back further in M15 — the pre-flight
investment caught 15 contract bugs before any code shipped, more
than M14's 10 because M15's surface was broader (six verbs vs
five, plus a wrapped envelope shape and §6.1 cross-cutting
amendment). Four rounds:

| Round | Findings | Theme |
|-------|----------|-------|
| 1 | 4 (1 P1, 3 P2) | Major shape bugs — `--template` wrongly required `board_kind: template` (BoardKind enum has no template value); §4.3 board-update implied atomicity Monday doesn't guarantee; §9.1 `delete_*` row claimed wire-idempotent yes while per-verb prose said no; `duplicate_board` live envelope shape unpinned (BoardDuplication carries `is_async`). |
| 2 | 4 (3 P2, 1 P3) | Cross-section drift — v0.2-plan §3 M15 still cited the wrong `--template` mechanism; §6.4 board-update didn't pin force-live final read; §6.1 generic envelope didn't acknowledge wrappers; cache-staleness caveat missing on archive + duplicate dry-run. |
| 3 | 6 (3 P2, 3 P3) | Round-2 fix propagation — §6.4 mutation-result block at line 2234 still said `data: <resource>`; §6.4 board-update didn't restate force-live constraint; v0.2-plan §3 M15 missed `description?`; cache-staleness caveat missed board-update dry-run; §9.2 list missed cross-cutting amendments; §3 M16 missed `invalidateBoard` retrofit note. |
| 4 | 1 (1 P3) | Closure check — §9.2 cross-cutting list lead-in said "round-1 + round-2 fixes" but list now also included round-3 fixes; board-update force-live bullet named only §4.3 even though round 3 also added the same constraint to §6.4. |

**Pattern:** every pre-flight finding was a contract-precision
issue. Round 1 caught the major shape bugs; round 2 narrowed
cross-section consistency; round 3 propagated round-2's
refinements to parallel sections (the M14 round-3 lesson
generalised — fixing one section's issue makes parallel sections
that need the same fix surface as new findings); round 4
confirmed closure with a tiny labelling drift. The ladder shows
the cli-design contract benefited from four independent passes.
Lesson: **the pre-flight round count scales with surface
breadth** — M13 ran one round (single `update clear-all`
contract), M14 ran three (five workspace verbs + one
contract-cluster), M15 ran four (six board verbs + wrapped
envelope shape + cross-cutting §6.1 amendment).

### Implementation Codex rounds — two rounds, six findings (0 P1, 2 P2, 4 P3)

| Round | Findings | Theme |
|-------|----------|-------|
| 1 | 3 (2 P2, 1 P3) | `board update` missed null-payload guard on per-field response (M14 lateral-propagation: the M14 round-2 / round-3 missing-root-key vs null-payload distinction needed to extend to `update_board`'s per-attribute response); archive + duplicate dry-run snapshots missing `items_count` + `permissions` (cli-design pinned full §6.2 projection but implementation used BoardMetadata-overlap subset); partial-application test asserted ok:false but didn't pin the stable error code. |
| 2 | 3 (3 P3) — closure | `board create` didn't distinguish missing-root-key from null payload (lateral propagation continued from round-1 F1 lesson); `board add-users` per-target dispatch failure test didn't pin error code (round-1 F3 sibling); archive + duplicate dry-run tests didn't pin items_count + permissions (round-1 F2 regression-test weakness). |

**Pattern explaining the implementation findings: the
`fix-then-audit-laterally` lesson M13 round-2 logged + M14
generalised across four rounds reduced to two rounds in M15.**
Each round caught the same class of bug applied to a route the
previous round didn't propagate to:

- Round 1 F1 (board update null-payload) → Round 2 F1 (board
  create missing-root-key, the lateral pattern).
- Round 1 F3 (partial-application stable error code) → Round 2
  F2 (board add-users per-target stable error code, the lateral
  pattern).
- Round 1 F2 (snapshot completeness) → Round 2 F3 (regression
  test pin for the snapshot, the lateral pattern).

Two rounds vs M14's four rounds because M15's contract surface
was already pinned harder by the four-round pre-flight — the
implementation only had to extend the M14 contracts (missing-
root-key distinction, force-live final read, partial-success
shape) to the new surface, not invent them. **The shorter
implementation review confirms the M14 thesis: "multi-round
pre-flight is more efficient than multi-round implementation
review."** Pre-flight round count went up by one (3 → 4) but
implementation round count went down by two (4 → 2), net saving
of one round and considerably less code-side review surface.

### The single load-bearing lesson — wire-shape divergence forces per-verb implementation rules

`board update`'s wire shape diverges from `update_workspace`:
Monday's `update_board(board_id, board_attribute, new_value)` is
**per-attribute**, while `update_workspace(id, attributes:
UpdateWorkspaceAttributesInput)` is multi-attribute. The CLI
preserves the same envelope shape across both verbs (single
ok:true on whole-call success, single ok:false on any per-field
failure) — but the IMPLEMENTATION rules differ:

1. **`update_workspace`**: single wire call; success-envelope
   `data` is the wire response's projection.
2. **`update_board`**: N wire calls fan-out + a separate force-
   live final `boards(ids:)` read for the success envelope's
   `data`. The post-mutation read MUST bypass cache (Codex pre-
   flight round-2 F2) so the success envelope reflects post-
   update state, not stale cached metadata. The CLI fires the
   final read via `client.raw` directly rather than
   `loadBoardMetadata` — this keeps it always-live.

This generalises: **identical envelope contracts can require
distinct implementation rules when wire shapes diverge.** The
contract (envelope shape) is what agents key off; the
implementation (force-live, fan-out, etc.) is invisible to them
but invisible-to-agent doesn't mean invisible-to-implementer —
the implementation rules are part of the contract's *internal*
structure and need explicit pinning.

This generalises a M11 lesson — "wire-shape claims need
empirical proof" — to "implementation-rule claims need explicit
contract pinning when the wire shape forces them". M15's pre-
flight Codex round-2 F2 caught the force-live pin gap before
implementation; without that round, the implementation would
have naturally reused `loadBoardMetadata` and emitted stale
post-update envelopes.

### Wrapped envelope precedent — `BoardDuplication`

`board duplicate` is the only M15 verb whose `data` wraps
because Monday's `duplicate_board` returns `BoardDuplication {
board, is_async }` (SDK 14.0.0 type) — both fields are load-
bearing for agents:

- `board`: the new board's projection (same shape as
  `boardProjectionSchema` — what `board create` / `update` /
  `archive` / `delete` return as bare `data`).
- `is_async`: a readiness signal; when true, Monday has queued
  the duplication and the new board may not be fully populated
  by envelope time. Agents needing to operate on the duplicated
  items / updates poll `boards(ids: [<new_id>]) { state }`
  until terminal state.

Wrapping the envelope `data` rather than flattening to a
projection-with-`is_async` field is the right call: the
projection schema is `boardProjectionSchema` shared across read
and four mutation verbs; bolting `is_async` onto every shape
would surface a meaningless slot on five other verbs.

This required widening the cli-design §6.1 universal envelope
sketch from `data: <resource | array>` to `data: <resource |
array | verb-specific JSON>` (Codex pre-flight round-2 F3) — a
cross-cutting amendment beyond M15's local board-duplicate
scope. The amendment is documented in §9.2 "Before M15 cross-
cutting contract amendments" + integrated into the §6.1 +
§6.4 generic prose.

### Coverage trajectory

| File | Branch coverage | Notes |
|------|-----------------|-------|
| `commands/board/create.ts` | 100 (projected) | Trivial mutation; every branch covered including missing-root-key and null-payload distinctions. |
| `commands/board/update.ts` | 90+ (projected) | Per-attribute fan-out + force-live final read + null-payload guard add a few defensive branches. |
| `commands/board/archive.ts` | 100 (projected) | Mirrors item-archive's coverage shape; preflight read + null-payload + missing-root-key + cache-source aggregation all exercised. |
| `commands/board/delete.ts` | 100 (projected) | Mirrors workspace-delete's coverage shape; minimal dry-run + null-payload + missing-root-key all exercised. |
| `commands/board/duplicate.ts` | 90+ (projected) | Wrapped envelope adds inner-board null-check branch; both DuplicateBoardType enum branches exercised. |
| `commands/board/add-users.ts` | 90+ (projected) | Per-token resolution × per-target dispatch × meta.source aggregation; defensive branches mirror workspace-add-users. |
| `api/board-projection.ts` | 100 | Schema declarations only; no executable branches. |
| `api/response-root.ts` | 100 (projected) | Helper exercised by all three partial-success consumers' tests. |
| Project total | 95.04 | Held at M14-close baseline despite the larger surface; above the 94/95/95/95 floor. |

### Post-flight rituals — pre-flight, empirical proof, contract-locking, fix-then-audit-laterally

M15 inherits and reinforces the four standard rituals M14
ratified:

1. **Pre-flight contract diff** (M13's bb7ee17 pattern): cli-
   design §4.3 + §6.4 land BEFORE any implementation. Each
   pre-flight gets ≥1 Codex round. Round count scales with
   contract complexity (M13 = 1; M14 = 3; M15 = 4).
2. **Empirical proof rituals** (M11/M12): every wire-shape
   claim pinned via integration test fixture's `match_query`
   regex + `match_variables` shape pin.
3. **Contract-locking via §22 R-class entries** — projected
   future lifts get logged at M15 close so the next session
   inherits the trigger conditions without re-discovering them.
4. **Fix-then-audit-laterally** — round N's fix surfaces
   round N+1's parallel route. M15's two implementation rounds
   followed this pattern verbatim (round-1 F1 → round-2 F1
   lateral; round-1 F3 → round-2 F2 lateral).

### Looking ahead — M15 → M16 cleanup window

Two R-class candidates remain post the cleanup window
(R40 + R43 shipped together as the cleanup-window pair):

- **R42** — retroactive missing-root-key sweep across pre-M14
  mutation verbs (~23 sites including the 6 new M15 sites).
  Schedule: post-M15 dedicated session per §22 R42 timing
  recommendation. R42 is the only remaining backlog item
  carrying into M16; the right call is to bundle with M16 if
  calendar pressure dictates OR ship as a dedicated post-M15
  session (recommended) so the retrofit lands the contract
  uniformly across all sites at once.
- **R44** — generic `projectMutationResource` over R28 / R37 /
  R43. Stays deferred to v0.3 OR a fifth-noun trigger per §22
  R44's entry — touching R44 now would re-touch the three
  per-noun helpers and scope-creep into a 4-noun refactor.
- (R40 shipped at the M15 → M16 cleanup window — see §22 R40
  "Shipped" annotation. R41 shipped at M15 close. R43 shipped
  at the M15 → M16 cleanup window — see §22 R43 "Shipped"
  annotation.)

M16 (board columns + cache-invalidation contract) opens cleanly
per §9.2 "Before M16" preconditions. Note the §3 M16 retrofit
clause: M16's `invalidateBoard(boardId)` work also lands the
M15 `board update` invalidation post-success (the M15 force-
live final read protects the immediate envelope, but later
commands reading the same board hit the stale cache until
M16 lands).

---

## 24. M16 post-mortem — three column verbs, three retrofits, contract-locked first try

M16 shipped on `main` across **ten** atomic commits — two docs
prelude commits (`ea74d7a` SHA backfill + `c42b751` §22 R45/R46
candidate-log) sitting alongside the seven-round pre-flight
landmark (`c0efab5`, referenced from §3 M16 status), six feat
commits (three column verbs + three M15 retrofits, in order:
column-create / column-update / column-delete / board update /
board archive / board delete), and two implementation Codex round
fix-up commits (`f94c2fa` round-1 P2 fix-ups + `95009ae` round-2
P3 diagnostic fix-up). **Codex rounds total: nine (seven pre-
flight + two implementation)** — the heaviest pre-flight to date
(M15: four; M14: three; M13: one), but the implementation review
closed in the projected 1-2 rounds. Project coverage: 99.01 /
95.43 / 99.49 / 99.14 — branches HELD at the M15-close baseline
(95.04) and TICKED UP slightly (95.43) despite M16's larger
surface (three new verbs + three retrofits + helper + categoriser
+ per-type schemas). Tests 2023 (M15 close) → 2127 (M16 close).

### Pre-flight Codex rounds — seven rounds, twenty-two findings (0 P1, 16 P2, 6 P3)

The pre-flight pattern bb7ee17 ratified in M13 + 3fe1d58 ratified
in M14 + a3b03c3 ratified in M15 paid back further in M16 — the
pre-flight investment caught 22 contract bugs before any code
shipped, more than M15's 15 because M16's surface was broader
(three column verbs + the §8 cache-invalidation contract with
its non-trivial timing rules + the M15 retrofit cluster + the
`noncanonical_column_type` warning shape + the per-type
`--settings` schema family + the two-tuple destructive-gate
shape). Seven rounds. The full per-round findings tally lives in
the seven `.review-output*` artifacts (gitignored); summary:

| Round | Findings | Theme |
|-------|----------|-------|
| 1 | 7 (5 P2, 2 P3) | Major shape bugs — `change_column_metadata` arg optionality wire-shape miss; `column-update` data-projection source unpinned; §8 fan-out invalidation ordering contradicted itself; §8 backstop-vs-eager signal blurring; column-create `noncanonical_column_type` warning shape unpinned + per-type `--settings` schema unpinned; column-delete `--dry-run` carve-out missing; §8 cross-process race-model mis-framed. |
| 2 | 4 (3 P2, 1 P3) | Cross-section drift — round-1 fix propagation to v0.2-plan §3 M16, the M15 retrofit cluster widening to three verbs (round-1 F2 lateral propagation), the `extraDetails` slot for two-tuple destructive verbs, the §8 caching layout including groups (M17 forward-pin). |
| 3 | 4 (3 P2, 1 P3) | Round-2 fix propagation — §6.4 column-update wire-shape detail, §8 backstop layer per-source-of-staleness call-out, §6.5 single-target destructive-gate shape pin, the `column_property: 'description'` (string literal) vs ColumnProperty enum entry hint. |
| 4 | 3 (2 P2, 1 P3) | Round-3 fix propagation — §6.4 column-delete dry-run shape pin, §6.4 column-update partial-application caveat, dry-run cache-staleness caveat for column-update preflight read. |
| 5 | 2 (2 P2) | Late-surface lateral-propagation — §6.4 column-create `noncanonical_column_type` warning fires on dry-run too (so the live call's behaviour is predictable); §3 M16 deliverables list missed `--type` argv validation against the full ColumnType enum. |
| 6 | 1 (1 P2) | §6.5 `confirmation_required` `details` schema was bulk-only; widened to a per-producer union (bulk filter-gated shape + single-target destructive-gate shape with verb-specific id-field naming) so column-delete + M17 group-archive / group-delete fit cleanly. |
| 7 | 1 (1 P3) | Closure check — labelling drift in the §9.2 cross-cutting amendments list lead-in. |

**Pattern:** every pre-flight finding was a contract-precision
issue — the M14 + M15 pattern repeated. Round 1 caught major
shape bugs; rounds 2 + 3 narrowed cross-section consistency;
rounds 4 + 5 propagated round-3's refinements to parallel
sections (the M14 round-3 lesson generalised — fixing one
section's issue makes parallel sections that need the same fix
surface as new findings); rounds 6 + 7 closed the cross-cutting
union extension + labelling drift. **Lesson generalised: the
pre-flight round count scales with surface breadth + the
load-bearing-ness of the cross-cutting contract.** M13 ran one
round (single `update clear-all` contract); M14 three (five
workspace verbs + one contract-cluster); M15 four (six board
verbs + wrapped envelope + §6.1 amendment); M16 seven (three
column verbs + §8 cache-invalidation contract + retrofit cluster
+ warning-code addition + two-tuple destructive-gate union
extension). The §8 contract was the load-bearing newcomer —
five of the seven rounds touched §8 at some level.

### Implementation Codex rounds — two rounds, four findings (0 P1, 3 P2, 1 P3)

| Round | Findings | Theme |
|-------|----------|-------|
| 1 | 3 (3 P2) | Board-update double-invalidate on success-path failure (success-path `invalidateBoard` + `emitMutation` lived inside the per-attribute loop's try/catch — a throw from emit would fall back into the catch and re-invalidate); board-update partial-application invalidation not round-trip asserted (existing tests asserted error envelope but didn't seed cache + prove invalidation); board-delete round-trip asserted request count rather than the public envelope shape (`meta.source: 'live'` + no `stale_cache_refreshed`). |
| 2 | 1 (1 P3) — closure | Diagnostic test-name fix-up (board-delete cache-invalidation round-trip name said "sees not_found" but assertion drives a `state: 'deleted'` success envelope, chosen during F3 fix to assert §8 envelope pins on a success-path describe rather than an error-path one). |

**Pattern explaining the implementation findings: the heavier
pre-flight investment (seven rounds vs M15's four) closed the
implementation review faster (two rounds, three findings) than
M15's two-round / six-finding implementation review.** Codex
caught the three M16 round-1 findings as exactly the kind of
cross-shape consistency drift the M15 implementation review
would have caught: board-update's try/catch boundary diverged
from column-update's "correct shape" structure; board-update's
test surface was less complete than column-update's; board-
delete's round-trip assertion was less rigorous than column-
delete's. The fix in F1 was to make board-update structurally
identical to column-update; in F2 to add the missing round-trip
cassettes; in F3 to assert the public envelope shape rather than
the request-count proxy.

**The shorter implementation review reinforces the M14/M15
thesis:** "multi-round pre-flight is more efficient than multi-
round implementation review." M16's pre-flight round count went
up by three (4 → 7) but implementation round count stayed flat
(2). That feels like a net cost, but the pre-flight rounds
caught **22** contract bugs that would otherwise have shipped as
implementation findings (and the M15 implementation rounds
already showed a 3:1 ratio of pre-flight finding to surfaced-in-
implementation finding); a 22-finding implementation review
would have run 5+ rounds. **Net saving: ~3 implementation
rounds + considerably less code-side review surface.** The
heavier pre-flight investment is paying off as the cli-design
contract grows.

### The single load-bearing lesson — non-trivial timing rules need explicit contract pinning AND cross-shape lateral verification

The §8 eager-invalidation contract has a non-trivial timing rule
that splits by leg-count:
- Single-leg verbs (column-create, column-delete, board archive,
  board delete): invalidate AFTER `data` projection on success;
  skip on error path.
- Fan-out verbs (column-update, board update): invalidate ONCE
  after the per-attribute loop settles iff `succeededLegs > 0`
  (high-water-mark counter); whole-call success path AND partial-
  application failure path both invalidate; zero-legs-succeeded
  skips.

The contract pinned both rules in pre-flight round 1 + 3 + 4
across cli-design §8. The implementation lateral-propagation
issue — caught in implementation round 1 F1 — was that
board-update's try/catch boundary diverged from column-update's,
allowing a double-invalidate on success-path failure. **Both
verbs implement the same contract; the structural divergence
was invisible to the contract pin.**

This generalises the M15 lesson — "implementation-rule claims
need explicit contract pinning when the wire shape forces them"
— to "implementation-rule claims need cross-shape lateral
verification when multiple verbs implement the same rule." The
contract pins the rule (`succeededLegs > 0` invalidates ONCE
after loop settles); the implementation pins the structural
shape (try/catch boundary). Both need parallel review.

The fix-then-audit-laterally lesson M13 round-2 logged + M14
generalised across four rounds + M15 confirmed across two rounds
extended further in M16: pre-flight round 1 → 7 logs the fix-
then-audit pattern across CLI-design sections; implementation
round 1 logs the same pattern across call-site shapes (board-
update was the lateral propagation of column-update's correct
structure). **Same lesson, two different review passes, each
catching the propagation the other missed.**

### Cross-shape lateral verification matrix — the six call sites

| Verb | Leg-count | Try/catch boundary | Invalidate-relative-to-emit | Cassette pins |
|------|-----------|--------------------|-----------------------------|--------------|
| column-create | single | n/a (no try/catch) | BEFORE emit | round-trip + warning shape + missing-root-key + null-payload |
| column-update | fan-out | wraps loop (no inner emit) | success: outside catch BEFORE emit; partial: inside catch BEFORE rethrow | round-trip + partial-application + zero-legs-succeeded + per-attribute schema-drift |
| column-delete | single | n/a (no try/catch) | BEFORE emit | round-trip + error-path-skips-invalidation + two-tuple confirmation envelope |
| board update (M16 retrofit) | fan-out | wraps loop + final read + projection (no inner emit) | success: outside catch BEFORE emit; partial: inside catch BEFORE rethrow | round-trip + partial-application + final-read-failure + zero-legs-succeeded |
| board archive (M16 retrofit) | single | n/a | BEFORE emit | round-trip + error-path-skips-invalidation |
| board delete (M16 retrofit) | single | n/a | BEFORE emit | round-trip + error-path-skips-invalidation |

The matrix is what pre-flight + Codex round 1 surfaced as the
implementation contract — no verb diverges from its leg-count's
structural shape. M17's five group verbs (single-leg + fan-out
mix) will adopt the same matrix from day one through R46
(`withBoardInvalidation`).

### Post-flight rituals — four standard rituals applied

The four standard post-flight rituals from M14/M15 carry forward:

1. **Pre-flight contract diff** — `c0efab5` shipped before any
   code shipped (seven Codex rounds folded into the single docs
   commit; cleaner working-tree review for the implementation
   milestone that follows, at the cost of fewer git landmarks
   for the pre-flight arc).
2. **Empirical proof for wire shapes** — every M16 wire mutation
   (`create_column`, `change_column_title`,
   `change_column_metadata`, `delete_column`) verified against
   `node_modules/@mondaydotcomorg/api/dist/esm/lib/generated/
   sdk.d.ts:1868-2041` (`MutationCreate_ColumnArgs`,
   `MutationChange_Column_TitleArgs`,
   `MutationChange_Column_MetadataArgs`,
   `MutationDelete_ColumnArgs`). The integration cassettes
   pin the wire shape via `match_query` regex + `match_variables`
   shape pin (M11/M12 pattern). The two load-bearing wire-shape
   pins — `defaults: JSON` (NOT `settings_str`) for create_column,
   `column_property: 'description'` (NOT 'title') for
   change_column_metadata — are pinned by both the source code
   AND a regex match in the integration cassette.
3. **Contract-locking via §22 R-class** — R45 shipped at first
   M16 commit (R39 precedent — ship the projection helper
   alongside the first new mutation rather than as a follow-up
   R-class); R46 stays deferred to the M16 → M17 cleanup window
   (post-M16 cleanup-window timing per R40 + R43's M15 → M16
   cleanup-window precedent).
4. **Fix-then-audit-laterally** — exercised across the
   pre-flight rounds (rounds 2 + 3 + 4 + 5 propagated round-1's
   fixes to parallel sections) AND the implementation round
   (round 1 F1 → board-update's structural propagation of
   column-update's correct shape).

### Coverage trajectory — branches ticked up despite larger surface

Project coverage: 98.99 / 95.04 / 99.48 / 99.14 (M15 close) →
99.01 / 95.43 / 99.49 / 99.14 (M16 close). Branches ticked up
**+0.39pp** despite three new verbs + three retrofits + the R45
helper + the per-type settings schemas + the categoriser. Per-
file targets met or exceeded for every new file:
- `src/api/column-mutation-result.ts`: 100/100/100/100
- `src/commands/board/column-create.ts`: 100/100/100/100
- `src/commands/board/column-update.ts`: 100/100/100/100
- `src/commands/board/column-delete.ts`: 100/100/100/100

The `cache.ts` `invalidateBoard` extension picked up 4 new
branch-coverage entries (the new function + its invocation paths
across the six call sites) without dropping the underlying file's
coverage.

### Looking ahead — M16 → M17 cleanup window

R-class state at the M16 → M17 cleanup window:

- **R46** — `withBoardInvalidation` post-success projection
  wrapper. **Shipped: `5b06d53`** at the M16 → M17 cleanup
  window per §22 R46 timing recommendation (mirrors R40 + R43's
  M15 → M16 cleanup-window timing). The four single-leg + two
  fan-out call sites stress-tested the §8 timing contract
  end-to-end through the implementation review; the lift
  consolidates them via two wrappers
  (`withBoardInvalidationSingleLeg` / `withBoardInvalidation
  FanOut`) that pin the leg-count split in the type system.
  M17's five group verbs (single-leg + fan-out mix) adopt the
  helpers from day one (R29's M14-close-then-M15-adopts
  pattern).
- **R42** — retroactive missing-root-key sweep across pre-M14
  mutation verbs (~27 sites including the 6 M15 mutation sites
  + 4 M16 column-mutation sites). Stays deferred per §22 R42's
  "post-M16 dedicated cleanup session" recommendation — the
  right call is to ship as a focused post-M16 session
  consolidating all sites onto R41's
  `assertResponseFieldPresent` helper in one review pass.
- **R44** — generic `projectMutationResource` over R28 / R37 /
  R43 / R45. Stays deferred to v0.3 OR a fifth-noun trigger per
  §22 R44's entry — touching R44 now would re-touch four per-
  noun helpers and scope-creep into a five-noun refactor.

M17 (board groups) opens cleanly per §9.2 "Before M17"
preconditions — the §8 invalidation contract M16 introduced
covers M17's five group verbs verbatim (the contract is
leg-count-shaped, not noun-shaped); M17 pre-flight should run
1-2 Codex rounds given the narrower surface (no new contract
introduction; just five verb annotations + the existing §8
contract reused).
