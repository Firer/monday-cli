# v0.5 implementation plan

> **Status:** plan, not contract. The binding contract is
> [`cli-design.md`](./cli-design.md). This document sequences the
> work needed to ship v0.5 of that contract; it doesn't redefine it.
> If a conflict arises, `cli-design.md` wins and this plan is updated.

## 1. Scope summary

v0.5 is "**writers + workdocs CRUD** — agents *create* and *edit*
people-structure and document content." v0.4 closed the
**reactivity** (`item watch`), **bulk throughput** (`--concurrency`),
**asset upload** (multipart wire), **read-only docs** (`doc list/get`),
and **shell completion** gaps. v0.5 picks up the remaining cli-design
§13 v0.5 candidates: the deferred-from-v0.4-M34 **team writer**
surface (create / update / delete / add-users / remove-users) and the
**full workdocs CRUD** mutation backlog Monday exposes on the wire
(9 doc-mutation surfaces deferred from v0.4-M32's read-only landing).
Bundled together at the post-v0.4-M33 candidate-selection session
(R-NEW-75 graduation) for a coherent "team + docs CRUD" v0.5 frame
rather than splitting them across releases.

**In scope (from `cli-design.md` §13 v0.5 + Appendix B verb-noun
matrix):**

| Group | Commands |
|-------|----------|
| **team writers** | `monday user team-list`, `monday user team-get <tid>`, `monday user team-create --name <n> [--users <id>,...] [--guest-team] [--parent <ptid>] [--allow-empty]`, `monday user team-delete <tid> --yes`, `monday user team-add-members <tid> --users <id>,...`, `monday user team-remove-members <tid> --users <id>,...`. Monday's mutation surface (v0.5 kickoff probe `scripts/probe/v0.5-team-mutations.ts` 2026-05-15, API `2026-01`): `create_team(input: CreateTeamAttributesInput!, options: CreateTeamOptionsInput) → Team` + `delete_team(team_id: ID!) → Team` + `add_users_to_team(team_id, user_ids: [ID!]!) → ChangeTeamMembershipsResult` + `remove_users_from_team(team_id, user_ids: [ID!]!) → ChangeTeamMembershipsResult`. **No `update_team` mutation exists** — Monday provides no rename / re-describe surface for teams (drop the `--description` flag the v0.4 cli-design row pencilled — `Team` object carries no description field on the wire). **Out-of-scope tangential verbs probe-surfaced**: `assign_team_owners` / `remove_team_owners` (owner-vs-member distinction); `add_teams_to_board` / `delete_teams_from_board` (board subscription, BoardSubscriberKind); `add_teams_to_workspace` / `delete_teams_from_workspace` (workspace subscription, WorkspaceSubscriberKind). M34 scopes to the 4 core mutations + 2 reads; the 6 tangential mutations defer to a v0.5.x candidate-selection round per R-NEW-75 framework. |
| **workdocs CRUD** | 9 mutation surfaces deferred from v0.4-M32 D8 closure — `create_doc` / `update_doc_name` / `delete_doc` / `duplicate_doc` / `import_doc_from_html` / `add_content_to_doc_from_markdown` / `create_doc_block` / `update_doc_block` / `delete_doc_block`. Each substantial enough to warrant its own milestone cluster; v0.5 likely sequences them across 2-3 milestone clusters (per CLAUDE.md "Next session" estimate). |
| **polish** | Refreshed envelope-snapshot suite for v0.5 surfaces; `output-shapes.md` ToC audit; README quickstart updates; CHANGELOG `0.5.0` entry; version bump. |

**Universal partial-success rule (cli-design §6.1 compliant — same
shape as v0.2/v0.3/v0.4).** Multi-target verbs in v0.5 (the
`team-add-members` / `team-remove-members` verbs emit per-target
results when multiple users are passed) reuse the shared per-target
dispatch helper (`dispatchSequential` in
`src/api/partial-success-mutation.ts`, lifted in v0.2). v0.5
introduces no contract revision to §6.1; the team-membership verbs
follow the same envelope shape v0.3-M19 `monday workspace add-users`
pinned.

**Out of scope (deferred per `cli-design.md` §13 + carried over from
v0.4 deferrals):**

- Files-shaped friendly `--set` translator + `--set-raw` form for
  file columns — slipped from v0.4 release-prep; would need a
  separate dispatch from the translator boundary into the multipart
  wire. Re-evaluate at v0.5 mid-cluster or slip to v0.5.x.
- Cross-board `item move` value-overrides — slipped from v0.3-M28
  audit, then again from v0.4 release-prep. Monday's
  `ColumnMappingInput` carries no value slot at API `2026-01`; the
  richer `{id, value?}` form requires a non-atomic post-move
  `change_multiple_column_values` with cross-leg partial-failure
  envelope shapes. Out of v0.5 scope unless Monday's API surfaces
  the slot at a newer version.
- Cross-board search resumable cursor — slipped from v0.4
  release-prep; per-board cursor-lifetime under aggregation remains
  the load-bearing design issue. Not a v0.5 candidate.
- Multi-level subitem creation — slipped from v0.4 release-prep;
  conditional on Monday's data model surfacing `subtasks` on
  `sub_items_board` at API `2026-01`+. Re-probe empirically before
  any v0.5 reopening; otherwise slips to v0.6.
- OAuth app registration (`monday auth login`) — v0.4.0 still ships
  with the M28 placeholder guard. Reopens only on clear user demand
  for browser-based login over `MONDAY_API_TOKEN`; not a v0.5
  deliverable by default.
- Boolean filter DSL — kept the v0.1/v0.2 `--where` operator
  allowlist + `--filter-json` through v0.4. Pencilled at v0.5
  stretch; only ships when fixture-driven adoption metrics show
  concrete demand.

**Permanent non-goals (per cli-design §13 — reminded here so plan
readers don't propose them as v0.5 candidates):**

- Hosting webhooks (CLIs can't expose public HTTP — out of model).
- App framework / installable monday apps (different surface area).
- Real-time GraphQL subscriptions (Monday's endpoint doesn't
  support them).
- Telemetry, update-notifier, analytics — ever.
- Forms (Monday's public-submission feature) — same hosted-
  endpoint shape as webhooks.
- Saved queries / aliases — would silently change behavior across
  machines; shell aliases / shell functions are the established
  UNIX answer.
- `monday undo` (replay-based reversal of recent mutations) —
  Monday's state model is authoritative and concurrent; honest
  substitutes are `--dry-run` (v0.1) and `item history` (v0.3-M24).

## 2. Pre-flight: what's already in place

Working at HEAD (`main`, post-v0.4.0 release). v0.4.0 published to
npm 2026-05-14T22:46:28Z. v0.4 closed M29–M33 + release-prep across
the year, ending with **3634 + 1 skipped tests across 148 files**;
coverage **99.26 / 96.31 / 99.34 / 99.53** at floor **95 / 95.45 /
95 / 95** (branches margin **0.86pp**); ERROR_CODES count **29**;
command count **101**. `package.json` version: **0.4.0** (stays
through v0.5 milestones; bumps to `0.5.0` at v0.5 release-prep).

**Foundations carrying forward into v0.5:**

- **Item history projector** (v0.3-M24, extended at v0.4-M29) — the
  per-event projector + chronological merge already covers the
  reactivity primitive. v0.5 doc-CRUD verbs don't re-touch the
  projector but may extend the doc-mutation activity-log shape if
  Monday surfaces doc edits via the activity stream.
- **Partial-success bulk + `--concurrency`** (v0.3-M25 + v0.4-M30)
  — the per-target dispatch helper + parallel/sequential routing.
  v0.5 team-membership verbs (`team-add-members` /
  `team-remove-members`) reuse this shape verbatim when multiple
  users are passed.
- **Multipart transport** (v0.4-M31) — `src/api/multipart-transport.ts`
  sibling module. v0.5 doc-CRUD mutations may need it for
  `import_doc_from_html` / `add_content_to_doc_from_markdown` (TBD
  empirically — those mutations may take inline content strings or
  may take file uploads; pre-flight probe pins the shape).
- **Documents read fetchers** (v0.4-M32) — `src/api/documents.ts`
  with `listDocuments` + `getDocument` + the wrapping/unwrapping
  schemas. v0.5 doc-mutation fetchers land in the same module
  alongside `listDocuments` / `getDocument`.
- **Shell completion** (v0.4-M33) — `monday completion <bash|zsh|fish>`.
  v0.5 new verbs auto-flow into the completion tree via the
  hand-rolled `buildCompletionTree` + `flattenPaths` walkers.
- **`docs/architecture.md` "Wire-vs-CLI semantics documentation
  conventions"** (v0.4-M31 R-NEW-41 graduation) — every v0.5
  surface adds an entry if it introduces an asymmetry (e.g., a
  team-mutation input slot that takes wire IDs vs the
  CLI's email-or-id shorthand).

## 3. Milestone sequencing

v0.5 opens with the **empirical-probe kickoff session** (this
doc's first commit). Subsequent milestones are sequenced
incrementally as each pre-flight runs:

- **Probe kickoff** (this session) — `scripts/probe/v0.5-team-mutations.ts`
  + `scripts/probe/v0.5-doc-mutations.ts` pin Monday's team writer
  + workdocs CRUD wire shapes at API `2026-01`. Output: probe
  findings file at §3 per-milestone entries + §8 D-decisions.
- **M34 — team writers** (next session, opens with pre-flight
  contract diff). 4-6 new verbs under `monday user team-*`;
  Monday's `create_team` / `delete_team` / `add_users_to_team` /
  `remove_users_from_team` mutations + (probe-pinned) any
  `update_team` mutation. Pre-flight + IMPL clusters estimated at
  3-4 sessions total per CLAUDE.md "Next session" estimate.
- **M35–M40 (TBD per candidate-selection session)** — 9 doc-CRUD
  mutation surfaces sequenced across 2-3 milestone clusters per
  R-NEW-75 5-dimension framework. Likely groupings:
  - M35 — doc-level CRUD (`create_doc` + `delete_doc` +
    `update_doc_name` + `duplicate_doc`) — 4 mutations bundled
    because they share the doc-identity surface + return shapes.
  - M36 — doc-block CRUD (`create_doc_block` + `update_doc_block`
    + `delete_doc_block`) — 3 mutations bundled because they
    share the block-identity surface + the rich-content payload.
  - M37 — doc-content import (`import_doc_from_html` +
    `add_content_from_markdown`) — 2 mutations bundled because
    they both materialize external content into Monday's doc
    body shape.
- **M41 (or later) — v0.5 release-prep** — envelope-snapshot
  refresh + ToC audit + README quickstart + version bump +
  CHANGELOG. Mirrors v0.4 release-prep cadence (6 commits).

Order at a glance (subject to refinement as probes land):

```
       v0.5 kickoff probes                 — team writers + workdocs CRUD empirical probes
       M34                                 — team writers (create / delete / add-users / remove-users + read complements)
       M35                                 — doc-level CRUD (create / delete / update_name / duplicate)
       M36                                 — doc-block CRUD (create_block / update_block / delete_block)
       M37                                 — doc-content import (import_from_html / add_from_markdown)
       M38 ─ ...                           — TBD (v0.5 release-prep lands last)
```

Each milestone is a coherent commit cluster (3–10 commits) ending on
a green tree with the listed exit criteria met. Earlier milestones
unblock later ones; do not start M(N) until M(N-1)'s exit criteria
are satisfied.

### v0.5 kickoff — empirical probes (this session)

**Status: probes landed.** Three probe rounds against a live
Monday workspace at API `2026-01` pinned the wire shapes for the
v0.5 backlog. Per-mutation findings file below + in §8 D-decisions.
**NO pre-flight contract diff this session** — the pre-flight
opens NEXT session per the v0.4 cadence (R-NEW-77 + R-NEW-75
framework).

**Probe scripts:**

- `scripts/probe/v0.5-team-mutations.ts` — team writer + reader
  surface introspection (`Mutation` enumeration, `Team` field
  shape, `Query.teams`, scoped read surfaces, user-identity
  shape).
- `scripts/probe/v0.5-doc-mutations.ts` — 9 doc-mutation arg +
  return shapes, `DocumentBlock` + `Document` re-confirm,
  async-job surface scan (none — all sync).
- `scripts/probe/v0.5-inputs-and-results.ts` — round 2.
  Input-object inputFields + result-object fields for types
  rounds 1+2 referenced opaquely (round-1 `introspectType`
  selects `fields { ... }` but NOT `inputFields { ... }` —
  see R-class candidate below).
- `scripts/probe/v0.5-nested-inputs.ts` — round 3. Nested
  `CreateDocBoardInput` / `CreateDocWorkspaceInput` slot
  shapes + team-mutation error-shape OBJECTs.

Reports stay gitignored at `scripts/probe/v0.5-*.report.txt`
(local-only per the standing `scripts/probe/` convention).

**Probe-lib gap surfaced.** `_lib.ts:introspectType` selects
`fields { ... }` but NOT `inputFields { ... }`, so `INPUT_OBJECT`
types come back with `fields: null` and stay opaque. Rounds 2+3
worked around this with an inline `introspectRich` helper that
adds `inputFields` to the query. **R-class candidate (filed at
§22 R-v0.5-NEW-1 below):** lift `inputFields` enumeration into
`_lib.ts:introspectType` (or ship a sibling `introspectRich`) at
the next probe consumer. 2 consumers today (round 2 + round 3);
R-NEW-58 lift-at-2 cadence candidate evaluated at M34 pre-flight
kickoff.

**Findings summary — load-bearing surprises:**

1. **`Mutation.update_team` DOES NOT EXIST.** cli-design §4.3
   pencilled an `--description <d>` flag on team-create; the
   wire surface offers neither rename nor re-describe. `Team`
   object also carries NO `description` field (6 fields only:
   `id` / `name` / `owners` / `picture_url` / `users` /
   `is_guest`). M34 contract diff drops the `--description`
   flag (no wire-side persistence) and no v0.5 `team-update`
   verb (no wire path).
2. **`create_team` takes two inputs:** `input:
   CreateTeamAttributesInput!` (`name!`, `is_guest_team`,
   `parent_team_id`, `subscriber_ids: [ID!]`) + `options:
   CreateTeamOptionsInput` (`allow_empty_team`). Hierarchical
   teams exist via `parent_team_id`; v0.5-M34 surfaces the
   slot but defers UX shaping to documentation only (no
   `--parent` flag at M34 — flag at v0.5.x if user demand
   surfaces).
3. **`add_users_to_team` / `remove_users_from_team`** return
   `ChangeTeamMembershipsResult { failed_users: [User!],
   successful_users: [User!] }` — a partial-success envelope at
   the WIRE level. This is the 2nd consumer of R-NEW-31's
   discriminated-union per-status-detail-schema pattern. The
   CLI's `team-add-members` / `team-remove-members` envelope
   wraps this into the §6.1 universal partial-success shape
   (`data.results: [{ ok, user_id, ... }]`) at the action
   boundary.
4. **`Query.teams(ids: [ID!])`** — NO `limit:`, NO pagination,
   NO cursor. Returns all teams in one shot when no `ids`
   passed. M34 contract diff documents the deferred pagination
   slot (Monday surface doesn't expose one) + the natural
   account-size cap on team lists.
5. **`User.teams` exists** (tangential read); `Account.teams`
   does NOT. M34 deferral: a future `monday user teams <uid>`
   verb has a wire path; pencilled at v0.5.x if user demand
   surfaces (not on M34's scope).
6. **9 doc mutations exist; names match handoff verbatim;
   return-type heterogeneity (5 distinct shapes):**
   - Full `Document`: `create_doc(location: CreateDocInput!)`
     — `CreateDocInput` is EITHER `board: CreateDocBoardInput
     { column_id!, item_id! }` (doc-on-column) OR `workspace:
     CreateDocWorkspaceInput { workspace_id!, name!,
     kind?: BoardKind, folder_id? }` (workspace-level doc).
     M35 contract diff branches the CLI argv into the two
     placement modes per D7 closure — two CLI verbs:
     `monday doc create-in-workspace --workspace <wid>
     --name <n> [--folder <fid>] [--kind <k>]` vs
     `monday doc create-on-column --item <iid> --column <cid>`.
   - Opaque `JSON` scalar: `update_doc_name(docId!, name!)`,
     `delete_doc(docId!)`, `duplicate_doc(docId!,
     duplicateType?: DuplicateType)`. CLI projects the JSON
     scalar into a flat `{ doc_id: string, success: true }`
     envelope at the fetcher boundary per D9 closure (`success`
     pinned literal-`true` — Monday surfaces failure via
     GraphQL `errors[]`, NOT via a wire-side success flag).
     Empirical IMPL cassette pins how Monday surfaces the
     opaque JSON payload (`duplicate_doc` carries the new
     doc's id; `rename` / `delete` likely surface `null` /
     empty record).
   - `OBJECT/DocumentBlock`: `create_doc_block(doc_id, type:
     DocBlockContentType!, content: JSON!, after_block_id?,
     parent_block_id?) → DocumentBlock` +
     `update_doc_block(block_id!, content: JSON!) →
     DocumentBlock`. Block returned in full.
   - `OBJECT/DocumentBlockIdOnly`: `delete_doc_block(block_id!)
     → { id }` — single-field return.
   - Custom `success/error` result: `import_doc_from_html(html!,
     workspaceId!, kind?: DocKind, folderId?, title?) →
     ImportDocFromHtmlResult { success!, doc_id?, error? }` +
     `add_content_to_doc_from_markdown(docId!, markdown!,
     afterBlockId?) → DocBlocksFromMarkdownResult { success!,
     block_ids?, error? }`. CLI maps `success: false` to
     `validation_failed` or `internal_error` per the wire
     `error` field's content.
7. **camelCase vs snake_case arg-name asymmetry across the 9
   doc mutations:** `update_doc_name` / `delete_doc` /
   `duplicate_doc` / `import_doc_from_html` /
   `add_content_to_doc_from_markdown` use **camelCase** args
   (`docId`, `afterBlockId`, `workspaceId`, `folderId`);
   `create_doc_block` / `update_doc_block` / `delete_doc_block`
   use **snake_case** (`doc_id`, `block_id`, `after_block_id`,
   `parent_block_id`). The wire-side inconsistency stays at the
   fetcher boundary; CLI argv lands in kebab-case throughout
   (snake_case in error details). **R-NEW-41 4th-consumer
   trigger fires** — see §22 R-class register.
8. **`DocBlockContentType` is a 16-value closed enum** for
   `create_doc_block.type`: `bulleted_list` / `check_list` /
   `code` / `divider` / `image` / `large_title` / `layout` /
   `medium_title` / `normal_text` / `notice_box` /
   `numbered_list` / `page_break` / `quote` / `small_title` /
   `table` / `video`. M36 contract diff pins the closed enum
   at the parse boundary. **Asymmetric typing note:** M32
   read-side `DocumentBlock.type` surfaces as plain `String`
   (not the enum) — write-side constrains, read-side relaxes.
   Another wire-vs-CLI semantics asymmetry (5th supporting
   site for R-NEW-41).
9. **`DuplicateType` 2-value enum** for `duplicate_doc`:
   `duplicate_doc_with_content` (content-only, default) /
   `duplicate_doc_with_content_and_updates` (full backup with
   comments + update history).
10. **NO async-job surface** for any doc mutation — all 9 are
    synchronous. **R-v0.4-W1 (`dispatchPollingLoop` helper)
    does NOT fire here.** Synchronous import behaviour at API
    `2026-01` may differ from older versions Monday's reference
    docs describe; the contract diff cites the empirical pin.
11. **`AssignTeamOwnersError` / `RemoveTeamOwnersError`** shape:
    `{ message, code: AssignTeamOwnersErrorCode, user_id }`.
    Per-user error discrimination on `assign_team_owners` /
    `remove_team_owners` — out of M34 scope (those mutations
    deferred per finding 1 above), but the error-code-enum-per-
    mutation pattern is a watch-item.
12. **6 tangential team-shaped mutations beyond the 4 core
    ones** (`assign_team_owners` / `remove_team_owners` /
    `add_teams_to_board` / `delete_teams_from_board` /
    `add_teams_to_workspace` / `delete_teams_from_workspace`).
    M34 explicitly scopes to the 4 core team-CRUD mutations
    + 2 reads; the 6 tangential mutations defer to a v0.5.x
    candidate-selection round.

### M34 — team writers (IMPL closed)

**Status: IMPL closed at `afdba15..02f1b1a`.** IMPL cluster:
`afdba15` feat (6 runtime fetcher bodies + new wrapping
response schemas + `projectTeam` wire-vs-output filter helper
+ shared `_team-membership.ts:projectMembershipResults`
partial-success projection + 6 action bodies wired with
dry-run/live splits + destructive gate preserved on team-
delete + 40 new integration tests across 6 files + 12
envelope snapshots) + 3 Codex IMPL fix-up rounds (`99bbd01`
round-1: 0 P1 + 1 P2 + 2 P3 — widen
`changeTeamMembershipsResultSchema` bucket containers to
nullable + normalise null→[] at fetcher; pin team-create
missing-key + same-user-in-both-buckets regression tests;
`72470b3` round-2: 0 P1 + 0 P2 + 2 P3 — team-delete no-token
destructive-gate regression test for the M10 P2 invariant +
remove-members null-bucket cassettes parallel to add-members;
`02f1b1a` round-3: 0 P1 + 0 P2 + 1 P3 — dry-run docstring
prose drift on team-add/remove-members, "per supplied user_id"
flipped to single-shot bulk shape). Round-4 ratified clean
across W1-W11. **Cumulative IMPL findings: 0 P1 + 1 P2 + 4 P3
across 3 fix-up rounds** — within the M22 / M27 / M30 / M31 /
M32 IMPL read-or-write precedent.

**Pre-flight cluster** (prior session) closed at
`17c1a54..df4f036`: 4 Codex rounds (3 fix-ups + 1
ratification); 0 P1 + 3 P2 + 3 P3 cumulative.

**Scope refined post-probe:**

- **6 verbs** under `monday user team-*` namespace (cli-design
  §4.3 already pencils them in at the v0.4 column; v0.4-M34
  deferral slipped them to v0.5):
  - `monday user team-list` — wire: `Query.teams { id name
    picture_url is_guest users { id name email } owners { id
    name email } }`. NO pagination at the wire (per Finding 4).
  - `monday user team-get <tid>` — wire: `Query.teams(ids:
    [<tid>])`; null/empty array → `not_found`.
  - `monday user team-create --name <n> [--users <id,...>]
    [--guest-team] [--allow-empty]` — wire: `create_team(input:
    { name: <n>, is_guest_team: <bool>, subscriber_ids:
    [<id...>] }, options: { allow_empty_team: <bool> })`. **NO
    `--description` flag** (Finding 1). `--parent <ptid>`
    deferred to v0.5.x (Finding 2).
  - `monday user team-delete <tid> --yes` — wire:
    `delete_team(team_id: <tid>) → Team`. Destructive gate per
    cli-design §3.1.
  - `monday user team-add-members <tid> --users <id,...>` —
    wire: `add_users_to_team(team_id: <tid>, user_ids: [<id>])
    → ChangeTeamMembershipsResult`. Per-user partial-success
    envelope wrapping the wire's
    `successful_users`/`failed_users` split into §6.1
    `data.results: [{ ok, user_id, ... }]`.
  - `monday user team-remove-members <tid> --users <id,...>`
    — wire: `remove_users_from_team(team_id, user_ids) →
    ChangeTeamMembershipsResult`. Same envelope shape as
    team-add-members.
- **New module `src/api/teams.ts`** — Monday has no existing
  teams writer surface in the CLI; new fetchers
  (`listTeams` / `getTeam` / `createTeam` / `deleteTeam` /
  `addUsersToTeam` / `removeUsersFromTeam`) + schemas.
- **New `TeamId` numeric-string brand** in `src/types/ids.ts`
  (10th brand; v0.4-M32 added `DocId` as the 9th).
- **Destructive gate** on `team-delete` per cli-design §3.1 —
  `--yes` required outside CI; `confirmation_required` on
  missing.
- **No new ERROR_CODE** at M34 — existing `not_found` /
  `forbidden` / `validation_failed` / `usage_error` cover the
  envelope.

**Out of M34 scope** (probe-surfaced but deferred):

- `assign_team_owners` / `remove_team_owners` — owner-vs-member
  surface; tangential to the v0.5 "team CRUD" frame, deferred
  to v0.5.x candidate-selection.
- `add_teams_to_board` / `delete_teams_from_board` — board
  subscription surface; tangential.
- `add_teams_to_workspace` / `delete_teams_from_workspace` —
  workspace subscription surface; tangential.
- `--parent <ptid>` flag on team-create — Monday surfaces the
  slot but agent-UX hierarchical-team usage is unclear today.
- `monday user teams <uid>` per-user team-list verb — wire
  path exists (`User.teams`); deferred to v0.5.x.

### M35 — doc-level CRUD (IMPL closed)

**Status: IMPL closed at `153458a..e7c5e50`.** IMPL cluster:
`153458a` feat (5 runtime fetcher bodies in `src/api/documents.ts`
swap for live `client.raw` round-trips against the pinned
operationNames; new `extractDuplicateDocId` helper for the
opaque-JSON new-doc-id extraction across bare-string / number /
record-with-`id`/`doc_id`/`new_doc_id` shapes; 5 runtime action
bodies wired via `resolveClient` + dry-run via `emitDryRun` +
live via `emitMutation`; `doc delete`'s destructive gate
preserved BEFORE `resolveClient` per M10 round-1 P2 invariant;
40 new integration tests across 5 new files + 13 in the
duplicate suite covering the JSON-shape variation matrix + 11
new envelope snapshots) + 6 Codex IMPL fix-up rounds + 1
ratification: round-1 `930cbc8` (0 P1 + 1 P2 + 2 P3 — rename
null-payload contract decision per the per-fetcher probe-
description asymmetry, module-header prose flip, detail-slot
test assertions across all 5 verbs), round-2 `465bfd3` (0 P1 +
0 P2 + 1 P3 — 4 downstream JSDoc "cassette pins" sites), round-3
`a1e7a46` (0 P1 + 0 P2 + 1 P3 — 1 remaining "duplicate-cassette
pin" hint in `extractDuplicateDocId`), round-4 `a671e01` (0 P1
+ 0 P2 + 1 P3 — 6 forward-looking "cassette" mentions uniformly
removed), round-5 `c26fd31` (0 P1 + 0 P2 + 1 P3 — 5 module
headers' stale `parseGlobalFlags`-vs-`resolveClient` ordering
prose; the IMPL collapsed `parseGlobalFlags` into `resolveClient`
for 4 non-delete verbs), round-6 `e7c5e50` (0 P1 + 0 P2 + 2 P3 —
null-payload test `details.name` assertion + `duplicateDoc`
JSDoc proximity to its function declaration). Round-7 ratified
clean across all 16 audit points (W1-W14 + W15 + W16).
**Cumulative IMPL findings: 0 P1 + 1 P2 + 8 P3 across 6 fix-up
rounds** — 1-2 rounds above the M22 / M27 / M30 / M31 / M32 /
M34 write-surface IMPL precedent, driven by the cassette-prose
sweep that surfaced incrementally as the post-fix-up grep
pattern broadened (rounds 2-4) + the round-5 stub-era
`parseGlobalFlags` ordering prose that only surfaced after the
W11 prose surface had quieted.

**Pre-flight cluster** (prior session) closed at
`f911fbd..a4763a2`: 5 Codex fix-up rounds + 1 ratification; 0
P1 + 1 P2 + 10 P3 cumulative. Lands the contract surface for 5
new `monday doc *` mutation verbs backed by 4 Monday GraphQL
mutations (5 stub fetchers + 5 command stubs + 4 wrapping
response schemas — both create variants share
`createDocResponseSchema` because the wire returns the same
Document shape regardless of placement — + the
`DUPLICATE_TYPE_VALUES` 2-value enum + `DocFolderIdSchema`
brand at `src/types/ids.ts` as 11th numeric ID kind + cli-design
§4.3 DOC section extension + cli-design §13 v0.5 entry update +
output-shapes.md M35 entries + 54 argv-parser unit tests at
`tests/unit/commands/doc/m35-argv.test.ts`).

**Scope refined post-probe (D7 / D8 / D9 closures ratified at
pre-flight):**

- **5 verbs** under the existing `monday doc` namespace (M32
  shipped `list` + `get`; M35 adds 5 mutation verbs):
  - `monday doc create-in-workspace --workspace <wid> --name <n>
    [--folder <fid>] [--kind public|private|share] [--dry-run]`
    — wire: `create_doc(location: { workspace: { workspace_id:
    <wid>, name: <n>, folder_id?: <fid>, kind?: <k> } }) →
    Document`. operationName `CreateDocInWorkspace`. Envelope
    `data: <Document>` — base 13-field Document projection
    OMITTING the wire's `blocks` slot (Monday returns
    `blocks: null` on a fresh create; surfacing a constant-null
    slot on every create envelope would add agent-noise). Agents
    needing block hydration call `monday doc get <new-id>` per
    the M32 read cadence.
  - `monday doc create-on-column --item <iid> --column <cid>
    [--dry-run]` — wire: `create_doc(location: { board: {
    column_id: <cid>, item_id: <iid> } }) → Document`.
    operationName `CreateDocOnColumn`. Same envelope as
    `create-in-workspace`. **D7 closure: two CLI verbs, not one
    with `--workspace`/`--board` choosers** — mutually-exclusive
    wire shape lives at the argv boundary, mirroring M31's
    `monday item upload` / `monday update upload` split for the
    same multipart wire path.
  - `monday doc rename <did> --name <n> [--dry-run]` — wire:
    `update_doc_name(docId: ID!, name: String!) → JSON`
    (opaque). operationName `UpdateDocName`. Projects opaque
    JSON return to flat `{ doc_id: <echoed>, success: true }`
    envelope per D9. **camelCase wire-arg note** — Monday's
    `update_doc_name` uses camelCase `docId` on the wire per
    Finding 7 (vs snake_case `doc_id` Monday uses for
    `Document` field names elsewhere); fetcher boundary mirrors
    wire; CLI argv stays kebab-case; error envelope
    `details.*` keys stay snake_case (`details.doc_id`). 4th
    supporting site for R-NEW-41.
  - `monday doc delete <did> --yes [--dry-run]` — wire:
    `delete_doc(docId: ID!) → JSON` (opaque). operationName
    `DeleteDoc`. Destructive gate per cli-design §3.1; fires
    BEFORE `resolveClient` per M10 round-1 P2 invariant. Null
    wire payload surfaces `not_found` (mirrors M14 workspace-
    delete + M34 team-delete cadence). Same `{ doc_id, success:
    true }` projection per D9.
  - `monday doc duplicate <did> [--with-updates] [--dry-run]` —
    wire: `duplicate_doc(docId: ID!, duplicateType?:
    DuplicateType) → JSON` (opaque). operationName
    `DuplicateDoc`. `--with-updates` boolean opt-in maps to
    Monday's 2-value `DuplicateType` enum (absent → wire-side
    default `duplicate_doc_with_content`; present →
    `duplicate_doc_with_content_and_updates`). **D8 closure: no
    `--name <n>` slot** — Monday's `duplicate_doc` carries no
    rename-on-duplicate arg; the duplicate inherits Monday's
    auto-generated copy name. Agents pair with
    `monday doc rename <new-id> --name <n>` for renaming.
    Envelope `data: { doc_id: <NEW>, success: true }` — the
    `doc_id` slot carries the **NEWLY-CREATED** duplicate's id
    per D9 (IMPL cassette pins the new-id extraction from the
    wire's opaque JSON).
- **New 11th brand `DocFolderIdSchema`** at `src/types/ids.ts` —
  keeps `--folder <fid>` distinct from `WorkspaceId` /
  `ItemId` / etc. at compile time. Mirrors the M34 TeamId +
  M32 DocId brand-discipline cadence.
- **No new ERROR_CODE at M35** — existing codes route doc-
  mutation failures: `not_found` / `usage_error` /
  `validation_failed` / `forbidden` / `unauthorized` /
  `confirmation_required`.
- **D9 closure: opaque-JSON projection.** 3 of 4 M35 mutations
  return Monday's `JSON` scalar (rename / delete / duplicate);
  the CLI projects to flat `{ doc_id: string, success: true }`
  at the fetcher boundary so agents read a uniform shape.
  `success` is pinned to literal `true` because Monday surfaces
  failure via GraphQL `errors[]` (mapped to typed `ApiError`s
  upstream), not via a wire-side success flag. Empirical IMPL
  cassette pins the actual JSON shape Monday returns (current
  expectation per the probe sample: `null` or empty record;
  `duplicate_doc` payload carries the new doc's id).
- **R-NEW-37 W2** safely-by-construction across all 5 fetchers
  — operationName slots NOT caller-overridable; GraphQL
  documents pin matching `mutation Foo` declarations
  literally.
- **R-NEW-76 discipline preserved** across all 5 command stubs
  — `parseArgv` (+ `enforceDestructiveGate` /
  `parseGlobalFlags`) fires BEFORE the `c8 ignore start`
  block-wrap on every verb so invalid argv surfaces
  `usage_error` from the parse boundary, NOT `internal_error`
  from the c8-ignored stub throw.

**Out of M35 scope** (deferred to M36 / M37 / v0.5.x):

- Per-block CRUD (`create_doc_block` / `update_doc_block` /
  `delete_doc_block`) — picked as M36 at the post-M35-IMPL
  candidate-selection session (`175d129`); M36 pre-flight
  opens next session.
- Doc-content import (`import_doc_from_html` /
  `add_content_to_doc_from_markdown`) — M37 candidate-selection
  runs post-M36 close.

### M36 — doc-block CRUD

**Status: pre-flight opened at this commit (stubs landed; runtime
bodies + integration tests deferred to M36 IMPL).** Picked as M36
at the post-M35-IMPL candidate-selection session (`175d129`) per
the R-NEW-75 5-dimension framework — empirical probes already ran
at v0.5 kickoff (no fresh probe needed), JSON `client.raw`
transport seam verbatim, single destructive verb on `block-delete
--yes` per the standard `enforceDestructiveGate` cadence.

**Scope (pre-flight contract surface):**

- **3 new verbs under the existing `doc` namespace.**
  - `monday doc block-create <doc-id> --type <DocBlockContentType>
    --content <json> [--after <bid>] [--parent <bid>] [--dry-run]`
    — wire: `create_doc_block(doc_id: ID!, type:
    DocBlockContentType!, content: JSON!, after_block_id: String,
    parent_block_id: String) → DocumentBlock`. 16-value closed
    enum on `--type` per Finding 8 + D10 closure.
  - `monday doc block-update <block-id> --content <json>
    [--dry-run]` — wire: `update_doc_block(block_id: String!,
    content: JSON!) → DocumentBlock`. No `--type` slot on the
    wire — content type is fixed at block creation; agents
    needing to switch type use `block-delete` + `block-create`
    (lossy: new id, new position).
  - `monday doc block-delete <block-id> --yes [--dry-run]` —
    wire: `delete_doc_block(block_id: String!) →
    DocumentBlockIdOnly` (single-field OBJECT with `id:
    String!`). Destructive gate per §3.1 + M10 round-1 P2
    invariant (`enforceDestructiveGate` BEFORE
    `resolveClient`); idempotent: no (re-deletes surface
    `not_found`).
- **OBJECT-return cadence.** All three M36 mutations return
  typed OBJECT shapes — `create_doc_block` + `update_doc_block`
  return the full 9-field `DocumentBlock` (id / type / content
  / position / parent_block_id / doc_id / created_at /
  created_by / updated_at); `delete_doc_block` returns
  `DocumentBlockIdOnly` (`{id: String!}`). Distinct from M35's
  opaque-JSON projection cadence (no `extractDuplicateDocId`-
  like helper needed at M36).
- **Snake_case wire arg names.** M36 mutations use snake_case
  arg names (`doc_id`, `block_id`, `after_block_id`,
  `parent_block_id`) — back to Monday's standard cadence after
  the M35 camelCase asymmetry (`docId` / `duplicateType` on
  `update_doc_name` / `delete_doc` / `duplicate_doc`). Not a
  new R-NEW-41 supporting site — M36 is the symmetric path,
  M35 was the asymmetric one. Cross-link to
  `src/api/documents.ts` module header.
- **`DocBlockIdSchema` brand (12th).** Added at `src/types/
  ids.ts` for the opaque-string block ID surface. Distinct
  from `DocId`'s numeric brand: Monday's `DocumentBlock.id`
  is `String!` on the wire (NOT `ID!`); the brand uses
  `slugIdSchema` (non-empty string base) like `ColumnId` /
  `GroupId`, NOT the numeric `^\d+$` regex. Keeps `DocBlockId`
  distinct from `DocId` at the type system so a `--parent
  <did>` slip at `block-create <docId>` becomes a compile
  error.
- **16-value `DocBlockContentType` closed enum.** Pinned at
  `src/api/documents.ts:DOC_BLOCK_CONTENT_TYPE_VALUES` per the
  v0.5 empirical probe (Finding 8). Values:
  `bulleted_list` / `check_list` / `code` / `divider` /
  `image` / `large_title` / `layout` / `medium_title` /
  `normal_text` / `notice_box` / `numbered_list` /
  `page_break` / `quote` / `small_title` / `table` / `video`.
  Unknown values reject at the argv-parse boundary per D10.
- **Per-block `content` shape (D11 closure).** Wire
  `content: JSON!` — opaque per-`DocBlockContentType` shape
  varying across the 16 enum values. Pre-flight accepts
  user-supplied `--content <json>` strings unmodified (parsed
  at argv boundary via `parseJsonArg` — R-NEW-42's 4th + 5th
  consumers); per-type content payload structure documentation
  (the 16 distinct shapes Monday accepts) defers to M36 IMPL
  cassettes. Today's pre-flight pins only "required JSON value
  at argv parse".
- **`parseJsonArg` consumer-4 + consumer-5.** M36 `block-create
  --content` + `block-update --content` consume the R-NEW-42-
  lifted helper (already shipped at v0.3-M27; 3-consumer
  threshold + lift complete). No new R-class candidate fires
  here — the helper handles the per-call context / details /
  hint customisations parametrically.
- **R-NEW-37 W2 safely-by-construction** across all 3 fetchers
  — `operationName` is NOT a caller-overridable input slot
  (closed at M27 IMPL round-1 P2-1). Literal names pinned
  at the fetcher boundary: `CreateDocBlock` /
  `UpdateDocBlock` / `DeleteDocBlock`. Audit-point W2
  verifies parity at every fix-up round.
- **R-NEW-76 graduated discipline preserved** across all 3
  M36 command action bodies — `parseArgv` (+ `parseJsonArg`
  for `--content` consumers + `enforceDestructiveGate` on
  `block-delete --yes`) fires BEFORE the `c8 ignore start`
  block-wrap so invalid argv surfaces `usage_error` from the
  parse boundary, NOT `internal_error` from the c8-ignored
  stub throw.
- **No new ERROR_CODES.** M36 reuses existing codes per D10/
  D11 closures: `usage_error` (argv-parse rejections — bad
  DocId/DocBlockId, unknown `--type` enum, malformed
  `--content` JSON), `not_found` (block-update / block-delete
  against a non-existent or inaccessible block ID; rewrap of
  Monday's null payload at the fetcher boundary),
  `internal_error` (create-block null payload — a successful
  `create_doc_block` must return the created block per
  Monday's contract; or response-shape regression),
  `validation_failed` (Monday-side rejection — wrong-shape
  `content` for the chosen `--type`, or token lacking write
  scope mapped to PERMISSION_DENIED),
  `confirmation_required` (destructive gate on
  `block-delete` missing `--yes`).
- **Doc-block mutations are live-only.** Pure-mutation
  surfaces don't cache by definition (cli-design §8);
  `meta.source: 'live'`. Dry-run paths emit `meta.source:
  'none'` per §6.4 mutation-dry-run discipline.

**Exit criteria (pre-flight close — this session):**

- `src/api/documents.ts` extended with 3 stub fetcher
  functions (`createDocBlock` / `updateDocBlock` /
  `deleteDocBlock`) + 3 GraphQL mutation documents
  (`CREATE_DOC_BLOCK_MUTATION` / `UPDATE_DOC_BLOCK_MUTATION`
  / `DELETE_DOC_BLOCK_MUTATION`) + wrapping response schemas
  + `DOC_BLOCK_CONTENT_TYPE_VALUES` 16-value enum +
  `documentBlockIdOnlySchema`. All 3 fetchers ship as
  `c8 ignore`-wrapped stubs that throw `internal_error` so a
  premature invocation surfaces "not yet implemented".
- `src/types/ids.ts` extended with `DocBlockIdSchema` brand
  (12th brand; string-shape via `slugIdSchema`).
- `src/commands/doc/block-create.ts` + `block-update.ts` +
  `block-delete.ts` ship complete argv parsing + schema +
  commander wiring + destructive gate on `block-delete`. The
  action bodies stop after `parseArgv` (+ `parseJsonArg` /
  `enforceDestructiveGate` where applicable) — the dry-run /
  live-dispatch / emit branches land at IMPL behind the
  c8-ignored stub throw.
- `src/commands/index.ts` registry extended.
- `docs/cli-design.md` §4.3 DOC extended with 3 new verb
  rows; §13 v0.5 backlog extended with M36 pre-flight
  framing.
- `docs/output-shapes.md` extended with the M36 envelope
  section (stub-status banner; per-type content payload
  structure deferred to IMPL cassettes per D11).
- `tests/unit/commands/doc/m36-argv.test.ts` covers the per-
  verb input-schema parse-boundary surface — required-flag
  absence, optional-flag presence/absence, brand-validation
  (DocId / DocBlockId), 16-value `DocBlockContentType` closed-
  enum rejection at parse, JSON-content parse via
  `parseJsonArg` (success + invalid JSON). The destructive-
  gate path is exercised at the integration layer (see
  next bullet), not in the unit tests — the gate lives in
  the action body's pre-c8 prelude, not in the input schema.
- `tests/integration/envelope-snapshots.test.ts` extended
  with 6 snapshots covering the pre-flight stub surfaces
  shipped: parse-boundary `usage_error` rejections
  (unknown `--type`, malformed `--content` JSON), the
  post-parse stub `internal_error` envelope on each verb,
  and the destructive-gate `confirmation_required` on
  `block-delete`. Dry-run + live-mutation snapshots land at
  M36 IMPL when the runtime body dispatches the wire call
  (the c8-ignored stub blocks throw before the dry-run /
  live emit can fire at pre-flight).
- Gates green: `npm run typecheck && npm run lint && npm
  test`. Coverage stays ≥ floor 95 / 95.45 / 95 / 95;
  branches margin may shrink slightly from the 3 new c8-
  ignored stub fetcher bodies — IMPL recovers it.
- Codex pre-flight review converged (1-3 rounds typical per
  the M22 / M27 / M30 / M31 / M32 / M34 / M35 pre-flight
  precedent).

**Exit criteria (IMPL close — next session):**

- 3 runtime fetcher bodies swap c8-ignored stubs for live
  `client.raw` round-trips with literal-pinned operationNames.
- 3 runtime action bodies wire `resolveClient` + fetcher +
  `emitDryRun` / `emitMutation` per the M35 cadence.
- Per-block content payload structure documented in
  `docs/output-shapes.md` from live IMPL cassettes (16 shapes
  — one per `DocBlockContentType` value per D11 closure).
- Integration tests per verb against fixture cassettes
  (`tests/integration/commands/doc-block-{create,update,
  delete}.test.ts`); 30-45 new tests expected per the M22 /
  M27 / M32 / M34 / M35 IMPL cadence.
- Envelope snapshots refreshed for the 3 new live-mutation
  envelopes.
- Coverage recovers branches margin to ≥ 0.5pp baseline; all
  metrics improve at IMPL close vs pre-flight close (M32 /
  M34 / M35 cadence).

### M37 — doc-content import (TBD — opens after M36 closes)

**Status: probe complete, sequencing TBD.**

**Scope:**

- 2 verbs:
  - `monday doc import-html --workspace <wid> --html <file|-> /
    --html-string <s> [--folder <fid>] [--kind <k>]
    [--title <t>]` — wire: `import_doc_from_html(html,
    workspaceId, kind?: DocKind, folderId?, title?) →
    ImportDocFromHtmlResult { success!, doc_id?, error? }`.
    Reads from file or stdin per cli-design §3.1 stdin
    discipline.
  - `monday doc append-markdown <doc-id> --markdown <file|-> /
    --markdown-string <s> [--after <bid>]` — wire:
    `add_content_to_doc_from_markdown(docId, markdown,
    afterBlockId?) → DocBlocksFromMarkdownResult { success!,
    block_ids?, error? }`.
- **`success: false` error mapping.** Both mutations return
  custom `{ success, error? }` envelopes; the fetcher maps
  `success: false` + populated `error` to
  `validation_failed` (or `internal_error` if `error` is
  empty/wire-regression-shaped).
- **Sync semantics confirmed at probe (Finding 10).** No
  polling helper needed; the v0.4-W1 `dispatchPollingLoop`
  watch-item doesn't fire here.
- **No max-size limit surfaced via introspection** — empirical
  IMPL cassette verifies whether Monday rejects oversized
  HTML/markdown payloads with a typed error code.

### M37 — doc-content import (TBD)

(Filled in at M37 pre-flight kickoff session, after M36 closes.)

## 4. Cross-cutting "definition of done" per command

Mirrors v0.4-plan §4. Per-command: stub-with-c8-ignore at
pre-flight + runtime body at IMPL + cli-design §4.3 row + Appendix
B verb-noun matrix tick + envelope snapshot + integration tests
+ output-shapes.md entry + (if applicable) ERROR_CODES widening
+ per-target partial-success envelope shape.

(Mostly empty at planning kickoff — fills in as v0.5 milestones
land per-command exit criteria.)

## 5. Testing strategy

Mirrors v0.4-plan §5 verbatim. Tests cover the pyramid: unit
(parse boundary, brand validators, shared helpers), integration
(per-verb end-to-end with `MondayClient` seam injection),
envelope snapshots (per-verb data shape pinned to a byte-stable
output), e2e (process spawn against fixture server) where
applicable.

## 6. Risk register

(Empty at planning kickoff — fills in as v0.5 milestones surface
risks. Carries over R-class candidates that fire in v0.5 from
v0.4-plan §22.)

## 7. v0.5 release exit checklist

(Skeleton — filled in at v0.5 release-prep milestone.)

- [ ] All v0.5 milestone exit criteria met (M34 + M35-M37 +
  release-prep).
- [ ] Coverage at floor; no per-file 100%-residual gap > 0.5pp.
- [ ] `npm run typecheck && npm run lint && npm test` clean.
- [ ] CHANGELOG `0.5.0` entry written; semver bump to `0.5.0`;
  annotated `v0.5.0` git tag; **push to `origin/main`** +
  **GitHub release** + **npm publish** (mirroring v0.3-M28 +
  v0.4 release-prep cadences).
- [ ] Envelope snapshots refreshed for all v0.5 surfaces.
- [ ] `output-shapes.md` ToC audit clean; stale `deferred_to:
  "v0.5"` slots audited per R-NEW-82 process discipline.
- [ ] README quickstart updated for v0.5 surfaces.

## 8. Decisions (open questions to close before each milestone)

D-decisions surfaced at the v0.5 kickoff probes (this session).
Each closes at its named milestone's pre-flight contract diff
session; entries below carry the probe-time framing + tentative
closure recommendation. Per-milestone D-decisions accumulate here
as pre-flight clusters open. Mirrors v0.4-plan §8.

### M34 — team writers

- **D1 (M34) — Drop `--description` flag from `team-create`.**
  Probe Finding 1: `Team` object carries NO `description` field;
  no wire-side persistence. cli-design §4.3 row carries the flag
  speculatively; the M34 pre-flight contract diff drops it.
  Tentative closure: **drop**. Affects cli-design §4.3 row +
  test pin.
- **D2 (M34) — Drop a `team-update` verb from v0.5 scope.**
  Probe Finding 1: `Mutation.update_team` does not exist at API
  `2026-01`. No rename / re-describe surface. Tentative closure:
  **out of v0.5 scope; no verb shipped**. Affects cli-design
  §4.3 + Appendix B verb-noun matrix.
- **D3 (M34) — Defer hierarchical-team `--parent` flag to
  v0.5.x.** Probe Finding 2: `CreateTeamAttributesInput.parent_
  team_id` exists, but agent-UX hierarchical-team semantics
  unclear today. Tentative closure: **slot exists on wire; CLI
  defers**. Re-evaluate at v0.5.x if user demand surfaces.
- **D4 (M34) — Defer the 6 tangential team-shaped mutations
  (`assign_team_owners` / `remove_team_owners` /
  `add_teams_to_board` / `delete_teams_from_board` /
  `add_teams_to_workspace` / `delete_teams_from_workspace`).**
  Probe Finding 12: tangential to the "team CRUD" frame.
  Tentative closure: **out of M34 scope; v0.5.x candidate-
  selection round**. Affects v0.5 scope summary.
- **D5 (M34) — Map `ChangeTeamMembershipsResult` to §6.1
  partial-success envelope.** Probe Finding 3: wire returns
  `{ successful_users, failed_users }`. CLI's
  `team-add-members` / `team-remove-members` envelope wraps
  this into §6.1 `data.results: [{ ok, user_id, ... }]` shape
  at the action boundary. Tentative closure: **wrap at action
  boundary; per-target id is `user_id`; the wire's failure
  reason currently has no `error.code` slot — verify at IMPL
  cassette whether Monday surfaces a reason per failed-user
  or just a presence-in-list signal**. Affects
  `src/api/teams.ts` + per-verb envelope projection.
- **D6 (M34) — No `--limit` / `--page` on `team-list`.** Probe
  Finding 4: `Query.teams` exposes neither slot. Tentative
  closure: **document the wire constraint; rely on natural
  account-size cap on team count**. Affects cli-design §4.3
  + output-shapes.md.

### M35 — doc-level CRUD

- **D7 (M35) — Two verbs vs one with placement choosers for
  `create_doc`.** Probe Finding 6: `CreateDocInput` is
  mutually-exclusive `board` vs `workspace`. **Closed at M35
  pre-flight: two verbs** — `monday doc create-in-workspace` /
  `monday doc create-on-column`. Mutually-exclusive placement
  modes at the wire are clearer as two CLI verbs than one verb
  with `--workspace`/`--board` choosers per agent-UX preference;
  the split also mirrors v0.4-M31's `monday item upload` /
  `monday update upload` cadence for the same multipart wire
  path. Affects cli-design §4.3 + `src/commands/doc/create-in-
  workspace.ts` + `src/commands/doc/create-on-column.ts` + the
  M35 unit-test wrong-slot guards in
  `tests/unit/commands/doc/m35-argv.test.ts`.
- **D8 (M35) — Drop `--name <n>` rename slot from
  `doc-duplicate`.** Wire: `duplicate_doc(docId,
  duplicateType?)` — no name override. CLI would need a post-
  duplicate `update_doc_name` follow-up which adds non-atomic
  partial-failure complexity. **Closed at M35 pre-flight: drop
  the flag; document the deferral**. Agents needing a renamed
  duplicate pair with `monday doc rename <new-id> --name <n>`.
  Affects cli-design §4.3 + `src/commands/doc/duplicate.ts`
  argv schema (no `name` slot).
- **D9 (M35) — Project opaque `JSON` scalar returns into a flat
  record at the fetcher boundary.** Probe Finding 6: 3 of 4
  M35 mutations return `JSON` scalar. **Closed at M35 pre-flight:
  CLI surfaces `{ doc_id: string, success: true }` envelope per
  the §6.1 single-record shape;** `success` pinned literal-`true`
  at `docMutationResultSchema` because Monday surfaces failure
  via GraphQL `errors[]` (mapped to typed `ApiError`s upstream),
  NOT via a wire-side success flag — a JSON return that reaches
  the projection is by construction the success path. IMPL
  cassette pins the actual JSON payload Monday returns
  (`duplicate_doc` carries the new doc id; rename / delete
  likely return `null` or empty record). Affects
  `src/api/documents.ts` + envelope-snapshots.

### M36 — doc-block CRUD

- **D10 (M36) — Reject unknown `--type` values at the argv-
  parse boundary.** Probe Finding 8: `DocBlockContentType` is
  a 16-value closed enum. **Closed at M36 pre-flight: closed-
  enum positional/flag rejection at parse.** Unknown `--type`
  values surface `usage_error` with
  `details.issues[{path: 'type', message: '...'}]` per
  `parseArgv`'s `SummarisedIssue` shape (zod
  `z.enum(DOC_BLOCK_CONTENT_TYPE_VALUES)` at the input schema
  layer; `parseArgv` joins zod's `path[]` into a dotted string
  and forwards `message` only — no `code` field on the
  emitted issue). Affects
  `src/api/documents.ts:DOC_BLOCK_CONTENT_TYPE_VALUES` +
  `src/commands/doc/block-create.ts` argv schema +
  `tests/unit/commands/doc/m36-argv.test.ts` enum-rejection
  test matrix.
- **D11 (M36) — Block-content payload structure per type
  variant.** Wire uses `JSON` scalar (`content: JSON!`).
  **Closed at M36 pre-flight: per-type content shape
  documented in output-shapes.md from Monday read-side
  cassettes at IMPL (16 shapes); CLI parses `--content
  <json>` via `parseJsonArg` (R-NEW-42's 4th + 5th
  consumers).** Pre-flight pins only "required JSON value at
  argv parse" — the inner shape isn't shape-checked by the
  CLI; Monday's wire is the source of truth and rejects
  shape-incompatible payloads with `validation_failed` at
  the live path. The 16-shape breadth documentation surface
  lives in `docs/output-shapes.md` M36 section; today's
  pre-flight ships the section with a stub-status banner +
  the breadth-deferral note, and IMPL fills in the per-type
  shapes from live cassettes. Affects
  `src/commands/doc/block-create.ts` + `block-update.ts`
  parsers + `docs/output-shapes.md` M36 section + M36 IMPL
  cassette breadth.

### M37 — doc-content import

- **D12 (M37) — Map `success: false` + populated `error` to
  `validation_failed`.** Probe Finding 6: both M37 mutations
  return `{ success, error? }`. Tentative closure: **fetcher
  maps `success: false` + populated `error` →
  `validation_failed`; empty `error` →
  `internal_error` (wire-regression hint)**. Affects
  `src/api/documents.ts` error mapping.
- **D13 (M37) — Empirical size-limit probe at M37 pre-flight.**
  Probe Finding 10: no max-size limit surfaced via
  introspection. Tentative closure: **M37 pre-flight runs a
  size-boundary probe (e.g. 1MB / 10MB HTML payload) to find
  the wire-side rejection threshold + map to a typed error
  code**. Affects M37 pre-flight preconditions §9.

## 9. Preconditions

Mirrors v0.3-plan §9 / v0.4-plan §9 — checklist of preconditions
per milestone + status tick at IMPL close. See
[v0.3-plan §9](./v0.3-plan.md#9-preconditions) for the canonical
shape.

### v0.5 kickoff — empirical probes

Pre-flight preconditions for the kickoff probe session:

- [x] **Clean working tree at session start.** `git status`
  was clean after Task #0 landed (`4c5ec77` audit-close
  commit) before probe-script work began.
- [x] **Gates green at session start.** `npm test --reporter=
  dot` returned 3634 passed + 1 skipped (3635) at v0.4.0 HEAD.
- [x] **Round-1 probe scripts written with `_lib.ts`-backed
  code** (`v0.5-team-mutations.ts` + `v0.5-doc-mutations.ts`).
  Round-2 + round-3 probes (`v0.5-inputs-and-results.ts` +
  `v0.5-nested-inputs.ts`) extended introspection with a
  local `introspectRich` helper because `_lib.ts:introspectType`
  doesn't enumerate `inputFields` (probe-lib gap — R-v0.5-NEW-1
  filed at §22).
- [x] **API `2026-01` pin in probe scripts.** Inherited from
  `_lib.ts:MONDAY_API_VERSION`.
- [x] **MONDAY_API_TOKEN loaded via `dotenv/config`** from
  `.env.probe.local` (gitignored). Token not echoed in probe
  reports.
- [x] **Probe findings filed in v0.5-plan §3 per-milestone
  entries + §8 D-decisions.**
- [x] **NO pre-flight contract diff.** No stub modules, no
  ERROR_CODES widening, no cli-design §13 v0.5 expansion. All
  open at NEXT session per v0.4 cadence.
- [x] **NO Codex review.** R-NEW-84 4th supporting instance —
  mechanical / process-only / outside-`src/**/*.ts` cluster;
  gates carry verification (probe scripts live in
  `scripts/probe/`, outside the coverage scope).

### M34 — team writers pre-flight

- [x] **Clean working tree at session start.** Verified at
  `4cac65a` HEAD before lift commit.
- [x] **Gates green at session start.** `npm test --reporter=
  dot` returned 3634 passed + 1 skipped (3635) at v0.4.0 HEAD.
- [x] **R-NEW-70 lift ahead-of-feat.** Single refactor commit
  `17c1a54` lands `parseBrandedListArg` helper at
  `src/utils/parse-brand-list.ts` + migrates the M32
  `parseWorkspaceListArg` inline site + adds 18 paired unit
  tests. Gates green post-lift.
- [x] **M34 pre-flight feat commit lands argv schema + wire
  query/mutation documents + stub fetchers + stub action
  bodies.** Commit `e0eb7f9`. New `src/api/teams.ts` module
  (6 stub fetchers + GraphQL documents + schemas), 6 command
  stubs under `src/commands/user/team-*.ts`, `TeamIdSchema`
  (10th brand) at `src/types/ids.ts`, cli-design §4.3 USER
  section + §13 v0.5 entry updated, 44 argv-parser unit
  tests, envelope-snapshot updated 101 → 107.
- [x] **R-NEW-76 discipline preserved across all 6 command
  stubs.** `parseArgv` (+ `parseBrandedListArg` /
  `enforceDestructiveGate` / `parseGlobalFlags`) fires
  BEFORE the `c8 ignore start` block-wrap on every team-*
  verb. 6th forcing supporting instance — graduates the
  discipline to a permanent CLAUDE.md "Workflow rules"
  entry at close-docs.
- [x] **R-NEW-37 W2 audit-point satisfied safely-by-
  construction.** All 6 fetchers in `src/api/teams.ts` ship
  literal-pinned operationNames (`ListTeams` / `GetTeam` /
  `CreateTeam` / `DeleteTeam` / `AddUsersToTeam` /
  `RemoveUsersFromTeam`); operation slots NOT caller-
  overridable. GraphQL documents pin matching `query Foo` /
  `mutation Foo` declarations.
- [x] **Destructive gate ordering preserved for team-delete.**
  `enforceDestructiveGate` fires BEFORE `resolveClient` per
  M10 round-1 P2 invariant. The c8-ignored stub body
  contains only the post-gate `internal_error` throw.
- [x] **Codex pre-flight review converged across 4 rounds.**
  Round 1 (`dc1b931`): 0 P1 + 2 P2 + 1 P3 (team-create dry-
  run contract + Team.users widening + architecture.md
  cross-link). Round 2 (`359a667`): 0 P1 + 1 P2 + 1 P3
  (wire-vs-output schema split + asymmetry prose
  canonicalisation). Round 3 (`df4f036`): 0 P1 + 0 P2 + 1 P3
  (cli-design pointer drift only). Round 4: 0 P1 + 0 P2 + 0
  P3 — all W1-W14 watch-items clean. Cumulative: 0 P1 + 3 P2
  + 3 P3 across 3 fix-up rounds.
- [x] **R-NEW-70 graduation.** Lift shipped at `17c1a54`;
  4 consumers post-lift (doc/list migrated + 3 new team
  sites). Full entry at v0.4-plan §22 R-NEW-70 marked
  "shipped at v0.5-M34 pre-flight close".
- [x] **R-NEW-84 graduation via negative-case validation.**
  M34 pre-flight cluster added production code → Codex
  review applied per the standard cadence (4 rounds) → rule
  correctly did NOT fire the skip. 4th supporting instance;
  promotes to permanent CLAUDE.md "Workflow rules" entry.

IMPL preconditions (all ticked at M34 IMPL close-docs commit;
runtime cluster `afdba15..02f1b1a` + close-docs commit):

- [x] **6 runtime fetcher bodies landed at `afdba15`.** All
  six fetchers in `src/api/teams.ts` shipped real
  `client.raw<unknown>(...)` round-trips with literal-pinned
  operationNames (`ListTeams` / `GetTeam` / `CreateTeam` /
  `DeleteTeam` / `AddUsersToTeam` / `RemoveUsersFromTeam`).
  Response-parse boundary via `unwrapOrThrow` on wrapping
  `*ResponseSchema` shapes (`.loose()` mode mirrors M27 /
  M32). Null roots → `internal_error` with drift hint;
  `getTeam` empty array → `not_found` (D8 collapse cadence);
  `createTeam` / `deleteTeam` route missing root key through
  `assertResponseFieldPresent` (R42 helper).
- [x] **`projectTeam` wire-vs-output filter helper landed.**
  Wire-vs-output split per round-2 P2-1 + R-v0.5-NEW-4
  sub-field discipline. Filters `null` entries out of wire
  `Team.users` before the agent-facing `teamSchema` sees the
  value; `users: null` (whole list) container passes through
  unchanged; `owners` passes through verbatim (wire pin is
  `[User!]!`, no inner null to filter).
- [x] **6 runtime action bodies wired with `resolveClient` +
  fetcher + emit.** `team-list` / `team-get` use `emitSuccess`
  (read verbs); `team-create` / `team-delete` use
  `emitMutation` with dry-run paths via `emitDryRun` per
  cli-design §6.4 mutation-dry-run; `team-add-members` /
  `team-remove-members` use `emitMutation` + new shared
  `_team-membership.ts:projectMembershipResults` helper for
  the universal §6.1 partial-success envelope projection.
- [x] **`_team-membership.ts:projectMembershipResults` lifted
  at IMPL kickoff.** 2-consumer inline lift mirroring M26b
  `_shared.ts:requireDevBoard` cadence. Shared partial-
  success projection takes wire
  `ChangeTeamMembershipsResult` + input user_id argv order,
  projects to universal §6.1 `results: [{user_id, ok, ...}]`
  shape with input-order preserved + failed-bucket-priority
  discipline + generic `membership_failed` code asymmetry.
- [x] **Destructive-gate ordering on `team-delete` preserved.**
  `enforceDestructiveGate` fires BEFORE `resolveClient` per
  the M10 round-1 P2 invariant. Round-2 P3-1 added an
  explicit no-token regression test pinning the
  `confirmation_required` over `config_error` precedence.
- [x] **6 integration test files landed.**
  `tests/integration/commands/team-*.test.ts` (6 NEW files; 40
  tests post-round-3) covering happy + error + dry-run +
  destructive-gate + partial-success outcomes. Tests use the
  standard `drive(argv, cassette)` helper; no cache isolation
  needed (teams are live-only per cli-design §8).
- [x] **12 envelope-shape snapshots landed.**
  `tests/integration/envelope-snapshots.test.ts` describes
  team list/get/create/delete/add-members/remove-members
  surfaces covering happy + dry-run + partial-success +
  confirmation_required + not_found.
- [x] **Codex IMPL review ran 4 rounds to convergence**
  (commits `99bbd01` round 1 + `72470b3` round 2 + `02f1b1a`
  round 3 + close-docs commit ratifying round 4). **Cumulative
  finding tally: 0 P1 + 1 P2 + 4 P3 across the 3 fix-up
  rounds.** The 1 P2 was a real nullable-bucket schema
  inconsistency surfaced only at IMPL (pre-flight rounds
  reviewed schema + docstring in isolation but didn't catch
  the prose-vs-schema drift). Round 4 ratified convergence
  (0 P1 + 0 P2 + 0 P3).
- [x] **Test count: 3693 + 1 skipped → 3752 + 1 skipped (+59
  net).** 40 integration tests + 12 envelope snapshots + 7
  across the 3 Codex IMPL fix-up rounds.
- [x] **Coverage at floor.** 99.26 / 96.29 / 99.43 / 99.53 at
  the 95 / 95.45 / 95 / 95 floor. Branches margin 0.84pp
  (+0.12pp recovery from pre-flight's 0.72pp). All metrics
  improved at IMPL close — mirrors M32 IMPL precedent.
- [x] **ERROR_CODES count: 29 (unchanged per D1-D6 closures).**
- [x] **Command count: 107 (unchanged — IMPL adds no verbs).**
- [x] **`package.json` version: 0.4.0 (unchanged; stays through
  v0.5 milestones; bumps to `0.5.0` at v0.5 release-prep).**
- [x] **Pushed to `origin/main` at close-docs commit.**

### M35 — doc-level CRUD pre-flight

Pre-flight preconditions (all ticked at M35 pre-flight close-docs;
cluster `f911fbd..a4763a2`):

- [x] **Clean working tree at session start.** Verified at
  `c3a1836` HEAD before pre-flight feat commit landed.
- [x] **Gates green at session start.** `npm test --reporter=dot`
  returned 3752 passed + 1 skipped (3753) at M34 IMPL close HEAD.
- [x] **R-v0.5-NEW-1 lift evaluation.** All M35 input-object
  shapes (`CreateDocInput` / `CreateDocBoardInput` /
  `CreateDocWorkspaceInput` / `DuplicateType`) already pinned at
  v0.5 kickoff rounds 2+3 probes via inline `introspectRich`
  workaround; M35 pre-flight needs no re-probe. **Lift DEFERRED**
  to next probe consumer (M36 doc-block CRUD if its input-shape
  pre-flight needs a fresh probe). R-NEW-58 ahead-of-feat cadence
  correctly does NOT fire here — the 2-consumer threshold stays
  at the v0.5 kickoff count.
- [x] **M35 pre-flight feat commit lands argv schema + wire
  mutation documents + stub fetchers + stub action bodies.**
  Commit `f911fbd`. Extends `src/api/documents.ts` with M35
  surface (5 stub fetchers + 5 GraphQL mutation documents + 4
  wrapping response schemas + `DUPLICATE_TYPE_VALUES` enum +
  `docMutationResultSchema` opaque-JSON projection); 5 new
  command stubs under `src/commands/doc/` (create-in-workspace +
  create-on-column + rename + delete + duplicate); new
  `DocFolderIdSchema` brand (11th numeric ID kind) at
  `src/types/ids.ts`; cli-design §4.3 DOC section extended; §13
  v0.5 entry updated; 52 argv-parser unit tests at
  `tests/unit/commands/doc/m35-argv.test.ts`; envelope-snapshot
  command_count bump 107 → 112.
- [x] **R-NEW-76 discipline preserved across all 5 command
  stubs.** `parseArgv` (+ `parseGlobalFlags` on all 5 +
  `enforceDestructiveGate` on `doc delete`) fires BEFORE the
  `c8 ignore start` block-wrap. 7th consumer post-graduation at
  M34 close-docs; rule stable. `parseBrandedListArg` deliberately
  NOT applied — M35 verbs don't consume comma-separated brand
  lists (that helper is M34 team-writer / M32 `doc list
  --workspace` territory).
- [x] **R-NEW-37 W2 audit-point satisfied safely-by-
  construction.** All 5 fetchers in `src/api/documents.ts` ship
  literal-pinned operationNames (`CreateDocInWorkspace` /
  `CreateDocOnColumn` / `UpdateDocName` / `DeleteDoc` /
  `DuplicateDoc`); operation slots NOT caller-overridable.
  GraphQL documents pin matching `mutation Foo` declarations.
- [x] **Destructive gate ordering preserved for `doc delete`.**
  `enforceDestructiveGate` fires BEFORE `resolveClient` per M10
  round-1 P2 invariant. The c8-ignored stub body contains only
  the post-gate `internal_error` throw.
- [x] **D7 mutual-exclusion guard at the parse boundary.**
  Workspace verb's strict schema rejects board-variant slots
  (`item` / `column`); column verb rejects workspace-variant
  slots (`workspace` / `folder` / `kind` / `name`). Pinned by 2
  targeted unit tests at round-1 P3-4.
- [x] **D8 `--name <n>` slot dropped from duplicate.** No
  rename-on-duplicate wire arg on Monday's `duplicate_doc`;
  agents pair with `monday doc rename <new-id> --name <n>` for
  renamed duplicates. Verified at round-3 P3-2 (D7 / D8
  "Tentative closure" → "Closed at M35 pre-flight" flip).
- [x] **D9 opaque-JSON projection pinned to literal-`true`.**
  `docMutationResultSchema` shape `{ doc_id: string, success:
  z.literal(true) }`. `success` pinned literal because Monday
  surfaces failure via GraphQL `errors[]` (mapped to typed
  `ApiError`s upstream), not via a wire-side success flag.
  Verified at round-1 P3-2 + round-3 P3-1 (literal-`true` prose
  + `blocks` omit framing).
- [x] **Codex pre-flight review converged across 6 rounds.**
  Round 1 `c99cf95`: 0 P1 + 1 P2 + 5 P3
  (`parseGlobalFlags`-before-c8 on 4 non-delete stubs + stale
  one-verb create example + literal-`true` prose + permission-
  sensitive note on create-on-column + wrong-slot tests +
  rename.ts camelCase pointer collapse). Round 2 `43962f5`: 0
  P1 + 0 P2 + 1 P3 (camelCase pointer collapse extended to
  delete + duplicate sibling docstrings). Round 3 `1e7ea83`: 0
  P1 + 0 P2 + 2 P3 (`blocks` omit framing harmonised + v0.5-plan
  §3 dup placeholder removed + D7/D8 closure flip). Round 4
  `8d43ee3`: 0 P1 + 0 P2 + 1 P3 (M35 surface-count prose
  precision). Round 5 `a4763a2`: 0 P1 + 0 P2 + 1 P3 (R-NEW-76
  paragraph dropped speculative `parseBrandedListArg` mention).
  Round 6 ratified clean across W1-W14 + W15. **Cumulative pre-
  flight findings: 0 P1 + 1 P2 + 10 P3 across 5 fix-up rounds**
  — within the M22 / M27 / M30 / M31 / M32 / M34 write-surface
  pre-flight precedent.
- [x] **R-NEW-72 (post-fix-up cross-doc grep) ratified for the
  4th time.** Discipline triggered after every contract-flipping
  fix-up across rounds 1-4; round 5 was a single-paragraph
  precision tweak that didn't fan out (no grep needed).
- [x] **Test count: 3806 + 1 skipped (+54 vs M34 IMPL close).**
  52 new argv tests at `f911fbd` + 2 wrong-slot strict-mode
  tests added at round-1 P3-4 fix `c99cf95`.
- [x] **Coverage at floor.** 99.18 / 96.29 / 99.04 / 99.45 at
  the 95 / 95.45 / 95 / 95 floor. Branches margin 0.84pp
  (unchanged from M34 IMPL close — new stub modules + new
  schemas are c8-ignored or covered by the new argv-parser
  unit tests; M35 happened to land byte-identical at the
  branches metric).
- [x] **ERROR_CODES count: 29 (unchanged per D-decisions; M35
  reuses existing codes — `not_found` / `usage_error` /
  `validation_failed` / `forbidden` / `unauthorized` /
  `confirmation_required`).**
- [x] **Command count: 112 (was 107 at M34 close; +5 new
  doc-mutation verbs).**
- [x] **`package.json` version: 0.4.0 (unchanged; stays through
  v0.5 milestones; bumps to `0.5.0` at v0.5 release-prep).**
- [x] **Pushed to `origin/main` at close-docs commit.**
  Pushed `c3a1836..36fb9ba` post-close-docs.

### M35 — doc-level CRUD IMPL

IMPL preconditions (all ticked at M35 IMPL close-docs; cluster
`153458a..e7c5e50`):

- [x] **Clean working tree at session start.** Verified at
  `f2eab40` HEAD (M35 pre-flight close-docs commit) before IMPL
  feat commit `153458a` landed.
- [x] **Gates green at session start.** `npm test --reporter=dot`
  returned 3806 + 1 skipped (3807) at M35 pre-flight close HEAD.
- [x] **Pre-IMPL cross-doc grep (R-NEW-72 / R-NEW-56 discipline).**
  Enumerated 10 stub→runtime flip sites across `src/api/documents.ts`
  + 5 `src/commands/doc/*.ts` modules + module-header prose blocks.
  All sites swept at IMPL feat commit `153458a`.
- [x] **R-class 2-consumer scan at IMPL kickoff (R-NEW-58).**
  Negative result — no 2-consumer trigger crystallised at IMPL
  kickoff. The `extractDuplicateDocId` helper is a single-
  consumer site (NOT promoted across other fetchers); the
  per-fetcher null-payload semantics asymmetry stays inline per
  fetcher because each fetcher's probe-description-derived
  decision differs.
- [x] **M35 IMPL feat commit lands 5 runtime fetcher bodies +
  5 runtime action bodies + 5 integration test files + 11
  envelope snapshots.** Commit `153458a`. Drops 5 `c8 ignore
  start/stop` block-wraps on stub fetchers + 5 on stub action
  bodies; module-header status prose flipped from "STUB FETCHERS
  shipped" to "Runtime bodies landed". 42 new integration tests
  across `tests/integration/commands/doc-{create-in-workspace,
  create-on-column,rename,delete,duplicate}.test.ts` + 11
  envelope-shape snapshots in
  `tests/integration/envelope-snapshots.test.ts`.
- [x] **R-NEW-76 discipline preserved post-IMPL.** `parseArgv`
  runs BEFORE `resolveClient` on all 5 verbs; `doc delete`
  additionally calls `parseGlobalFlags` + `enforceDestructiveGate`
  BEFORE `resolveClient` (M10 round-1 P2 invariant). The 4
  non-delete verbs let `resolveClient` parse global flags
  internally before `loadConfig`. Round-5 P3-1 flipped the
  stub-era prose to describe the actual IMPL ordering.
- [x] **R-NEW-37 W2 audit-point satisfied safely-by-
  construction.** All 5 fetchers thread literal-pinned
  operationNames; operation slots NOT caller-overridable.
  Verified clean at IMPL rounds 1+7.
- [x] **Destructive gate ordering preserved for `doc delete`.**
  `enforceDestructiveGate` fires BEFORE `resolveClient` per the
  M10 invariant; integration test
  `tests/integration/commands/doc-delete.test.ts` pins the
  no-token regression case (gate-before-resolveClient).
- [x] **D9 opaque-JSON projection per-fetcher null-payload
  asymmetry pinned.** Round-1 P2-1 closure: `renameDoc`
  present-but-null → success envelope (Monday's `update_doc_name`
  probe description makes no return-shape promise; null is
  plausibly empty-success); `deleteDoc` + `duplicateDoc`
  present-but-null → `not_found` (each promises a non-null
  payload on success per probe description). Missing root key
  uniformly `internal_error` via `assertResponseFieldPresent`.
- [x] **`extractDuplicateDocId` helper.** Defensive across
  bare-string / number / record-with-`id` / `doc_id` /
  `new_doc_id` shapes (string or number leaves); unrecognised
  shape surfaces `internal_error` with a re-probe hint pointing
  at `scripts/probe/`. Integration test suite covers all 5
  happy shapes + the unrecognised-shape rejection + the nested-
  numeric-id branch.
- [x] **Codex IMPL review converged across 7 rounds.** Round 1
  `930cbc8`: 0 P1 + 1 P2 + 2 P3 (rename null contract +
  module-header prose + detail-slot test assertions). Round 2
  `465bfd3`: 0 P1 + 0 P2 + 1 P3 (4 downstream cassette JSDoc
  sites). Round 3 `a1e7a46`: 0 P1 + 0 P2 + 1 P3 (1 remaining
  cassette hint — literal-substring grep miss). Round 4
  `a671e01`: 0 P1 + 0 P2 + 1 P3 (6 forward-looking cassette
  mentions; uniform stem removal). Round 5 `c26fd31`: 0 P1 + 0
  P2 + 1 P3 (5 stub-era `parseGlobalFlags` ordering prose
  sites). Round 6 `e7c5e50`: 0 P1 + 0 P2 + 2 P3 (null-payload
  test `details.name` assertion + `duplicateDoc` JSDoc
  proximity). Round 7 ratified clean across all 16 audit
  points. **Cumulative IMPL findings: 0 P1 + 1 P2 + 8 P3
  across 6 fix-up rounds.**
- [x] **R-NEW-72 (post-fix-up cross-doc grep) ratified for the
  5th time.** Discipline fired after every contract-flipping
  fix-up. **Lesson surfaced at round 3:** grep patterns benefit
  from matching noun stems (`cassette`, `cassette pin`,
  `cassette pins`, `cassette pinned`) not just the literal
  substring — the round-2 grep with the narrower pattern missed
  the singular "cassette pin" stem, which round 3 caught.
- [x] **Test count: 3858 + 1 skipped (+52 vs M35 pre-flight
  close).** 42 new integration tests across 5 new files (9 +
  6 + 7 + 7 + 13) + 11 new envelope snapshots − 1 wrong-slot
  stub test collapsed at IMPL (the pre-flight stub's
  `parseGlobalFlags`-before-c8 test became redundant once the
  c8 block was dropped). One round-1 fix flipped a test's
  expected outcome (rename null → success vs not_found); two
  round-6 fixes added an assertion to an existing test +
  reordered a JSDoc block (test count unchanged).
- [x] **Coverage at floor.** 99.29 / 96.36 / 99.44 / 99.55 at
  the 95 / 95.45 / 95 / 95 floor. **Branches margin recovered
  0.84pp → 0.91pp (+0.07pp)** from the runtime-body branches
  covered by integration tests vs the 5 c8-ignore stub drops.
  All four metrics improved vs pre-flight close (stmts +0.11pp,
  branches +0.07pp, fns +0.40pp, lines +0.10pp). Same cadence
  as M32 + M34 IMPL — every metric improves at IMPL close vs
  pre-flight.
- [x] **ERROR_CODES count: 29 (unchanged per D-decisions; M35
  reuses existing codes).**
- [x] **Command count: 112 (unchanged from pre-flight; IMPL
  adds no verbs).**
- [x] **`package.json` version: 0.4.0 (unchanged).**
- [x] **Pushed to `origin/main` at close-docs commit `93d7f28`.**
  Pushed `f2eab40..93d7f28` (the full IMPL cluster + close-docs)
  to `origin/main`.

### M36 — doc-block CRUD pre-flight

Pre-flight preconditions (ticked progressively as the cluster
lands; final ticks at M36 pre-flight close-docs):

- [ ] **Clean working tree at session start.** Verified at
  `8ce52ef` HEAD (post-M36-prep-audit close commit) before the
  pre-flight feat commit lands.
- [ ] **Gates green at session start.** `npm test --reporter=dot`
  returned 3858 + 1 skipped (3859) at M35 IMPL close-docs HEAD.
- [ ] **No fresh empirical probe.** The v0.5 kickoff probes
  (`scripts/probe/v0.5-doc-mutations.ts` +
  `v0.5-inputs-and-results.ts`, 2026-05-15, API `2026-01`)
  pinned the M36 wire surface (3 mutation signatures + return
  types + `DocBlockContentType` 16-value enum +
  `DocumentBlockIdOnly` shape). R-NEW-75 framework: M36 picked
  on neutral trade-offs (probes already settled at v0.5 kickoff;
  no fresh probe needed).
- [ ] **R-class 2-consumer scan at pre-flight kickoff
  (R-NEW-58).** Negative result — `parseJsonArg` already
  3-consumer-lifted at v0.3-M27 (M36 consumes the helper at
  consumers 4 + 5 for `block-create --content` +
  `block-update --content`; no new R-class trigger here).
  `documentBlockSchema` already exists at M32 (M36 fetchers
  reuse it for `create_doc_block` + `update_doc_block` return
  parsing). The 3-fetcher structural pattern (wrapping schema +
  `assertResponseFieldPresent` + null-handling + projection) is
  M22 / M27 / M32 / M34 / M35's standard cadence — not novel
  enough to warrant a 2-consumer lift. No R-class lift fires
  AHEAD of feat.
- [ ] **M36 pre-flight feat commit lands argv schema + wire
  mutation documents + stub fetchers + stub action bodies.**
  Extends `src/api/documents.ts` with M36 surface (3 stub
  fetchers + 3 GraphQL mutation documents + 3 wrapping response
  schemas + `DOC_BLOCK_CONTENT_TYPE_VALUES` 16-value enum +
  `documentBlockIdOnlySchema`); 3 new command stubs under
  `src/commands/doc/` (`block-create` / `block-update` /
  `block-delete`); new `DocBlockIdSchema` brand (12th brand —
  string-shape via `slugIdSchema`, NOT numeric — distinct from
  `DocId`) at `src/types/ids.ts`; cli-design §4.3 DOC section
  extended; §13 v0.5 entry updated to fold M36 alongside M35;
  argv-parser unit tests at
  `tests/unit/commands/doc/m36-argv.test.ts`; envelope-snapshot
  command_count bump 112 → 115.
- [ ] **R-NEW-76 discipline preserved across all 3 command
  stubs.** `parseArgv` (+ `parseJsonArg` for `--content`
  consumers + `parseGlobalFlags` / `enforceDestructiveGate` on
  `block-delete --yes`) fires BEFORE the `c8 ignore start`
  block-wrap. 8th consumer post-graduation at M34 close-docs;
  rule stable.
- [ ] **R-NEW-37 W2 audit-point satisfied safely-by-
  construction.** All 3 fetchers in `src/api/documents.ts`
  ship literal-pinned operationNames (`CreateDocBlock` /
  `UpdateDocBlock` / `DeleteDocBlock`); operation slots NOT
  caller-overridable. GraphQL documents pin matching
  `mutation Foo` declarations.
- [ ] **Destructive gate ordering preserved for
  `doc block-delete`.** `enforceDestructiveGate` fires BEFORE
  the c8-ignored stub body per M10 round-1 P2 invariant. The
  c8-ignored stub body contains only the post-gate
  `internal_error` throw.
- [ ] **D10 closure landed at this commit.** Unknown `--type`
  values reject at the argv-parse boundary with
  `usage_error.details.issues[]` via the 16-value
  `z.enum(DOC_BLOCK_CONTENT_TYPE_VALUES)` schema. Verified by
  the M36 argv-parser unit test matrix.
- [ ] **D11 closure landed at this commit.** Pre-flight pins
  only "required JSON value at argv parse" for `--content`;
  the per-type content payload structure (16 distinct shapes
  Monday accepts) defers to M36 IMPL cassettes. The
  `output-shapes.md` M36 section ships with a stub-status
  banner + breadth-deferral note at this commit.
- [ ] **Codex pre-flight review converged.** Pre-budget 1-3
  rounds per the M22 / M27 / M30 / M31 / M32 / M34 / M35
  pre-flight precedent. Cumulative findings logged at §12 M36
  post-mortem (filled in at close-docs).
- [ ] **R-NEW-72 (post-fix-up cross-doc grep) discipline
  applied** after every contract-flipping fix-up. Lesson from
  M35 IMPL: use noun-stem `\b<noun>\b` regex matching, not
  literal substring, to catch all inflections in a single
  pass.
- [ ] **Test count post-pre-flight close.** 55 argv-parser
  tests at `tests/unit/commands/doc/m36-argv.test.ts` covering
  argv / schema / JSON-content parse + 6 integration envelope
  snapshots in `tests/integration/envelope-snapshots.test.ts`
  covering parse-boundary `usage_error` rejections (unknown
  `--type`, malformed `--content` JSON) + post-parse stub
  `internal_error` envelopes (one per verb) +
  `confirmation_required` on `block-delete`. Dry-run +
  live-mutation snapshots land at M36 IMPL (the c8-ignored
  stub blocks throw before the dry-run / live emit can fire
  at pre-flight). +61 net tests over the 3858 + 1 skipped
  M35 IMPL close baseline.
- [ ] **Coverage stays ≥ floor.** 95 / 95.45 / 95 / 95.
  Branches margin may shrink slightly from the 3 new
  c8-ignored stub fetcher bodies — IMPL recovers it.
- [ ] **ERROR_CODES count: 29 (unchanged per D10/D11
  closures).**
- [ ] **Command count: 115 (was 112 at M35 close; +3 new
  doc-block-mutation verbs).**
- [ ] **`package.json` version: 0.4.0 (unchanged; stays through
  v0.5 milestones; bumps to `0.5.0` at v0.5 release-prep).**
- [ ] **Pushed to `origin/main` at close-docs commit.**

(Per-milestone preconditions fill in at each pre-flight kickoff.)

## 10. Post-mortem template

Mirrors v0.4-plan §10 verbatim. Per-milestone: round-by-round
Codex review summary + contract drift findings + lessons learned
+ R-class candidate filings.

## 11. M34 post-mortem — team writers (6 new verbs)

M34 closed end-to-end. v0.5's first feature milestone; first
team-writer surface on Monday's wire from the CLI. No new
transport seam (JSON `client.raw` round-trip per verb, same as
M22 / M27 / M30 / M32 read-or-write surfaces); no destructive
gate at the cluster level (only `team-delete` ships a gate, per
the existing M10 / M14 cadence). No new ERROR_CODE (29 stays
per D1-D6 closures); no command count change (107 stays —
M34 pre-flight had already added the 6 verbs).

### Codex round summary

**Pre-flight** (commit cluster `17c1a54..df4f036` + post-close
`3383341`; closed prior session): 4 rounds. Cumulative
pre-flight findings: **0 P1 + 3 P2 + 3 P3 across 3 fix-up
rounds**. One round above the M22 / M27 / M30 read-surface
precedent — driven by the wire-vs-output schema split (round-2
P2-1) being a novel cadence at sub-field granularity.

**IMPL** (commit cluster `afdba15..02f1b1a` + close-docs
commit): 3 fix-up rounds + 1 ratification.

- **Round 1** (feat `afdba15` reviewed; fix at `99bbd01`):
  0 P1 + 1 P2 + 2 P3.
  - **P2-1 (W4/W8)**: `changeTeamMembershipsResultSchema`
    parsed `failed_users` + `successful_users` as non-nullable
    arrays despite the empirical-probe note in the same module
    saying both lists are nullable wire containers. Pre-fix an
    all-success response (`failed_users: null`) or all-failed
    response (`successful_users: null`) would reject as
    `internal_error` at the parse boundary BEFORE the partial-
    success projection could run. Fix widened both slots to
    `z.array(teamUserSchema).nullable()`; both fetchers
    normalise `null → []` at the projection boundary via `??
    []`. Two new integration tests pin the null-bucket cases.
  - **P3-1 (W8)**: `team-create.test.ts` covered the null-
    payload branch but not the missing-`create_team`-key
    branch the R42 `assertResponseFieldPresent` helper guards.
    Fix added a `CreateTeam` cassette with
    `response: { data: { other_root: 'unexpected' } }`
    asserting exit 2 / `internal_error`. Symmetric with the
    `team-delete` `other_root` test already in place.
  - **P3-2 (W4)**: partial-success projection's failed-bucket-
    priority discipline (a user_id appearing in BOTH failed +
    successful lands as `ok: false`) was implemented but not
    regression-tested. Pre-fix tests used disjoint buckets, so
    a future refactor flipping the helper's check order to
    successful-first would have passed silently. Fix added a
    same-user-in-both-buckets cassette to
    `team-add-members.test.ts`.
- **Round 2** (cluster `afdba15..99bbd01` reviewed; fix at
  `72470b3`): 0 P1 + 0 P2 + 2 P3.
  - **P3-1 (W6)**: `team-delete` regression coverage for the
    M10 round-1 P2 invariant (destructive gate fires BEFORE
    `resolveClient` so `confirmation_required` beats
    `config_error`) ran only with the fixture token, so a
    future reorder to `resolveClient()` before
    `enforceDestructiveGate()` would still pass. Fix added a
    parallel no-token regression test mirroring the workspace
    delete / item delete / webhook delete / board delete
    convention.
  - **P3-2 (W4/W8)**: round-1's null-bucket normalisation fix
    lived independently in `removeUsersFromTeam` (parallel
    `?? []` site) but the round-1 test fix only pinned
    `team-add-members`. Fix added two mirror cassettes to
    `team-remove-members.test.ts` covering `failed_users:
    null` (all-success) + `successful_users: null` (all-
    failed).
- **Round 3** (cluster `afdba15..72470b3` reviewed; fix at
  `02f1b1a`): 0 P1 + 0 P2 + 1 P3.
  - **P3-1 (W5)**: dry-run docstring prose drift on
    `team-add-members.ts` + `team-remove-members.ts` said the
    planned envelope lists the operation "per supplied
    user_id", echoing the `workspace add-users` per-user fan-
    out cadence. The team-membership wire is a single-shot
    bulk call; implementation + tests correctly emit one
    planned-change entry. The prose left a footgun for a
    future agent who'd "fix" the correct bulk shape into a
    per-user shape. Fix rewrote both dry-run paragraphs to
    spell out the SINGLE planned operation with `team_id` +
    argv-order `user_ids`, explicitly contrasting against the
    workspace add/remove-users per-user fan-out cadence.
- **Round 4** (cluster `afdba15..02f1b1a` reviewed): 0 P1 + 0
  P2 + 0 P3. Convergence ratified. All audit points returned
  clean (W1-W11).

**Cumulative IMPL findings: 0 P1 + 1 P2 + 4 P3 across 3 fix-up
rounds.** Within the M22 / M27 / M30 / M31 / M32 IMPL read-or-
write precedent (1-3 fix-up rounds typical). The 1 P2 was a
real runtime correctness issue — the pre-flight schema
documented the bucket containers as nullable in prose but
encoded them as non-null in the schema; only IMPL surfaced the
inconsistency because the runtime body parses the wire shape
directly (pre-flight rounds had only the stub fetcher's
docstring + signature, not the actual `safeParse` predicate).
Mirrors the M32 IMPL "null-vs-empty branch split" cadence —
pre-flight Codex catches contract drift in the schema /
docstring; IMPL Codex catches contract drift in the runtime
body's predicate logic.

### Contract drift findings

- **Nullable wire containers documented in prose, encoded as
  non-null in schema.** The pre-flight `change
  TeamMembershipsResult` schema parsed `failed_users` +
  `successful_users` as `z.array(teamUserSchema)` (non-
  nullable) despite the same module's empirical-probe
  docstring saying both lists are nullable wire containers per
  the round-2 introspection (`LIST/<wrapped>` outer wrapper
  with no `NON_NULL` marker). The pre-flight Codex rounds
  reviewed the schema + docstring in isolation; the
  inconsistency only surfaced at IMPL when the runtime body
  parsed a wire-valid all-success / all-failed response and
  the schema rejected it as `internal_error`. **Lesson for
  future pre-flight Codex prompts:** add a W audit-point
  asking the reviewer to cross-check every empirical-probe
  prose claim against the corresponding zod schema slot
  (nullable / required / inner non-null) — the M34 pre-flight
  reviewer had both surfaces in scope but didn't enumerate the
  cross-check explicitly.
- **Per-fetcher null-handling parity needs explicit test
  coverage.** Round-1 fixed both `addUsersToTeam` +
  `removeUsersFromTeam` fetchers with parallel `?? []` sites,
  but only `team-add-members.test.ts` got the null-bucket
  test coverage initially. A future refactor that regressed
  only one fetcher's normalisation would have passed silently.
  **Lesson:** when round-N fixes a shared concern across N
  fetchers, the round-N test fix must mirror the fetcher
  change in parallel — not just one consumer.
- **Dry-run prose drift across sibling verbs.** The dry-run
  paragraphs in `team-add-members.ts` + `team-remove-members.
  ts` shared an inaccurate "per supplied user_id" phrase
  describing the planned envelope. Round 3 caught it as P3;
  the prose left a footgun for a future agent. **Lesson:**
  when two sibling verbs share dry-run prose, the pre-flight
  Codex prompt should flag prose-vs-implementation parity
  explicitly — the W5 "dry-run shape per cli-design §6.4"
  audit-point caught the gap but only at round 3.

### Exit ratchets

- **Test count: 3693 + 1 skipped → 3752 + 1 skipped (+59
  net).** 40 new integration tests across 6 new
  `tests/integration/commands/team-*.test.ts` files + 12 new
  envelope snapshots in
  `tests/integration/envelope-snapshots.test.ts` + 4 round-1
  fix-ups + 3 round-2 fix-ups + 0 round-3 fix-ups (doc-prose
  only).
- **Coverage: 99.11 / 96.17 / 98.86 / 99.37 → 99.26 / 96.29 /
  99.43 / 99.53.** All metrics improved at IMPL close.
  **Branches margin recovered 0.72pp → 0.84pp (+0.12pp)** from
  the runtime-body branches covered by integration tests vs
  the 6 c8-ignore stub drops. **Functions recovered 0.57pp**
  from the 12 stub functions becoming covered by runtime
  bodies. Same cadence as M32 IMPL (every metric improved vs
  pre-flight close); confirms the M32 precedent (read-or-write
  new-wire-surface milestones recover branches margin at
  IMPL).
- **ERROR_CODES count: 29 (unchanged per D1-D6 closures).**
- **Command count: 107 (unchanged from pre-flight; IMPL adds
  no verbs).**
- **`package.json` version: 0.4.0 (unchanged; stays through
  v0.5 milestones; bumps to `0.5.0` at v0.5 release-prep).**

### R-class movement at M34 IMPL close

See §22 for the full R-class register. Headlines:

- **`_team-membership.ts:projectMembershipResults` lifted at
  IMPL kickoff** — 2-consumer inline lift mirroring M26b
  `_shared.ts:requireDevBoard` cadence. Two-verb byte-
  identical projection + the wire-vs-CLI asymmetry's
  documentation cross-link made a shared helper the cleaner
  expression than fold-in-place. Filed as a new R-shipped
  entry at §22.
- **R-NEW-73 stays UNFILED at 6 candidate consumers post-IMPL.**
  IMPL surfaced the runtime divergence (per-fetcher error
  code + details shape varies enough that a parametrised
  helper signature would carry more surface than the 4-6
  inline lines it would replace). Re-evaluate at v0.5.x
  consumer-7+ if a future verb's null-payload semantics
  happen to align with an existing fetcher's exact shape.
- **R-v0.5-NEW-4** (wire-vs-output schema split at sub-field
  granularity) gains a **2nd supporting instance** at IMPL:
  the `projectTeam` helper filters wire `null` entries out
  of `Team.users` at the fetcher boundary, then the agent-
  facing `teamSchema` enforces non-null entries via
  `outputSchema.parse(...)` at `emitSuccess`. Two supporting
  instances + the v0.3-M22 full-schema precedent (3 cumulative
  signals); watch-item, fires at 3rd sub-field supporting
  instance.
- **Lesson R-NEW filed as R-v0.5-NEW-6** (new): post-IMPL
  Codex prompt should enumerate "pre-flight prose-vs-schema
  cross-check" as a template-stable audit-point. Surfaced at
  M34 IMPL round-1 P2-1; 1 supporting instance. Watch-item,
  fires at 2nd consumer.

### Lessons for future IMPL sessions

- **Cross-check empirical-probe prose against schema
  nullability** (M34 IMPL round-1 P2-1 lesson). Future IMPL
  sessions for new-wire-surface milestones should run a
  per-field cross-check between probe-pinned wire nullability
  + zod schema encoding before declaring contract surface
  frozen. Filed as **R-v0.5-NEW-6** at §22.
- **Mirror test fixes across sibling fetchers** (M34 IMPL
  round-2 P3-2 lesson). When a round-N fix updates parallel
  sites in N fetchers, the round-N test fix must cover all
  N consumers — a one-consumer test fix lets future refactors
  regress N-1 consumers silently.
- **Sibling-verb prose audits** (M34 IMPL round-3 P3-1
  lesson). When two sibling verbs share dry-run / partial-
  success / error-shape prose, run a paired audit at IMPL
  kickoff via `grep -A 5 '<shared-phrase>'` so prose drift
  surfaces ahead of Codex round-N. Partially overlaps with
  R-v0.5-NEW-5 (paired-verb cli-design row consistency); the
  sibling-prose audit extends to module-docstring prose,
  not just cli-design row prose.

(Mostly empty at planning kickoff — fills in as v0.5 milestones
close.)

## 12. M35 post-mortem — doc-level CRUD (5 new verbs)

M35 closed end-to-end. v0.5's second feature milestone; first
doc-mutation surface on Monday's wire from the CLI. No new
transport seam (JSON `client.raw` round-trip per verb, same as
M22 / M27 / M32 / M34); one destructive verb (`doc delete`)
follows the existing M10 / M14 / M34 destructive-gate cadence.
No new ERROR_CODE (29 stays per D-closures); no command count
change (112 stays — M35 pre-flight had already added the 5
verbs). First M35-specific complexity: 3 of 4 wire mutations
return Monday's opaque `JSON` scalar (read-only probe couldn't
capture a live cassette — Monday's wire requires real write
scope against a sandbox account that wasn't available); the
fetcher boundary projects each opaque JSON to the flat
`{doc_id, success: true}` envelope per D9.

### Codex round summary

**Pre-flight** (commit cluster `f911fbd..a4763a2` + close-docs
commit `36fb9ba..f2eab40`; closed prior session): 5 rounds.
Cumulative pre-flight findings: **0 P1 + 1 P2 + 10 P3 across
5 fix-up rounds**. Within the M22 / M27 / M30 / M31 / M32 /
M34 write-surface pre-flight precedent (cumulative 0-3 P2 +
3-10 P3 typical for new write-mutation surfaces).

**IMPL** (commit cluster `153458a..e7c5e50` + close-docs
commit): 6 fix-up rounds + 1 ratification.

- **Round 1** (feat `153458a` reviewed; fix at `930cbc8`):
  0 P1 + 1 P2 + 2 P3.
  - **P2-1 (W5)**: `renameDoc` treated present-but-null
    `update_doc_name` payload as `not_found`, contradicting D9's
    "success on any present payload" projection contract. The
    pre-flight JSDoc had pinned this null → not_found cadence
    by analogy with M14 workspace-delete + M34 team-delete, but
    IMPL surfaced a per-fetcher asymmetry that the pre-flight
    review didn't catch: each mutation's probe description
    distinguishes whether Monday promises a non-null payload on
    success. `delete_doc` description says "Returns success
    status and the deleted document ID" (non-null promise);
    `duplicate_doc` says "Returns the new document's ID on
    success" (non-null promise); `update_doc_name` description
    carries no return-shape prose at all — null is plausibly an
    empty-success indicator, and Monday would surface a typed
    error for missing doc via GraphQL `errors[]` (mapped to
    typed `ApiError` upstream). Fix dropped the per-fetcher
    null → not_found branch in `renameDoc`; kept it for
    `deleteDoc` + `duplicateDoc`. Module-header prose at
    `src/api/documents.ts:184-216` enumerates the asymmetry
    explicitly. Integration test `doc-rename.test.ts` flipped
    from "null → exit 2 / not_found" to "null → exit 0 /
    success".
  - **P3-1 (W11)**: Module-header prose at
    `src/api/documents.ts:216-235` claimed "IMPL cassette pins
    the JSON shape" + "fetcher stub today" — stale from
    pre-flight tense. Fix rewrote to reflect read-only-probe
    reality: defensive helper accepts plausible-defensive
    shapes today, future live wire response would narrow the
    helper.
  - **P3-2 (W14)**: Schema-drift integration tests for all 5
    verbs only asserted `error.code`, not the scoped
    `error.details.*` slots agents read to scope retries.
    Added explicit assertions on `workspace_id` / `item_id` /
    `column_id` / `doc_id` / `name` details across the
    missing-key + malformed-document tests in all 5 doc-
    mutation integration test files.
- **Round 2** (cluster `153458a..930cbc8` reviewed; fix at
  `465bfd3`): 0 P1 + 0 P2 + 1 P3.
  - **P3-1 (W11′)**: Round-1 flipped the module-header
    "cassette pins" prose at the main module header but left
    the same stale claim at 4 downstream JSDoc sites —
    `docMutationResultSchema`, `docDuplicateOutputSchema`,
    `duplicateDocResponseSchema`, and the
    `src/commands/doc/duplicate.ts` module header. Fix
    rewrote all 4 sites to match the round-1 module-header
    reality. R-NEW-72 cross-doc grep post-fix-up confirmed
    zero remaining "IMPL cassette pins" sites under `src/`.
- **Round 3** (cluster `153458a..465bfd3` reviewed; fix at
  `a1e7a46`): 0 P1 + 0 P2 + 1 P3.
  - **P3-1 (W11″)**: 1 remaining "duplicate-cassette pin"
    hint in `extractDuplicateDocId`'s JSDoc. Round-2 R-NEW-72
    grep matched the literal pattern `cassette pins` only,
    missing the singular `cassette pin` stem. Fix rewrote the
    hint to forward-looking "re-probing `duplicate_doc` live
    so a future commit can narrow the helper to Monday's
    actual wire shape".
- **Round 4** (cluster `153458a..a1e7a46` reviewed; fix at
  `a671e01`): 0 P1 + 0 P2 + 1 P3.
  - **P3-1 (W11‴)**: 6 remaining forward-looking "future
    live cassette" mentions across M35-touched sources.
    Audit-point W11‴ as phrased asked for zero `cassette`
    stem matches under `src/`; rather than narrow the
    audit-point, uniformly replace the noun with "live wire
    response" / "live mutation response" in all 6 sites. The
    forward-looking framing is preserved without any
    `cassette` stem.
- **Round 5** (cluster `153458a..a671e01` reviewed; fix at
  `c26fd31`): 0 P1 + 0 P2 + 1 P3.
  - **P3-1 (out-of-band)**: 5 module-header prose blocks
    still claimed the pre-flight ordering "`parseGlobalFlags`
    runs BEFORE `resolveClient`" across all 5 M35 action
    bodies. The IMPL collapsed the standalone
    `parseGlobalFlags` call into `resolveClient` for the 4
    non-delete verbs (`resolveClient` parses global flags
    internally before `loadConfig`); only `doc delete` keeps
    the standalone call because the destructive gate needs
    the parsed flags ahead of the resolver. Behaviour fine;
    the prose preserved stub-era wording. Sweep at `c26fd31`
    rephrased all 5 sites to describe the actual IMPL shape;
    `doc delete`'s module header was already accurate (it
    DOES call `parseGlobalFlags` separately) so left
    untouched.
- **Round 6** (cluster `153458a..c26fd31` reviewed; fix at
  `e7c5e50`): 0 P1 + 0 P2 + 2 P3.
  - **P3-1 (W14⁵)**: The `internal_error when create_doc
    payload is null` test in
    `doc-create-in-workspace.test.ts` asserted
    `details.workspace_id` but not `details.name`, leaving
    one W14 detail-slot path unpinned even though the fetcher
    emits both on the null-payload branch.
  - **P3-2 (out-of-band)**: The IMPL feat commit landed the
    `duplicateDoc` JSDoc immediately above
    `extractDuplicateDocId`, with the helper's own JSDoc below
    it — two JSDoc blocks back-to-back, the upper one orphaned
    33 lines above its target `export const duplicateDoc`.
    Fix moved the `duplicateDoc` contract prose down to
    immediately precede its export.
- **Round 7** (cluster `153458a..e7c5e50` reviewed): 0 P1 +
  0 P2 + 0 P3. Convergence ratified. All 16 audit points
  returned clean (W1-W14 + W15 + W16).

**Cumulative IMPL findings: 0 P1 + 1 P2 + 8 P3 across 6 fix-up
rounds.** 1-2 rounds above the M22 / M27 / M30 / M31 / M32 /
M34 write-surface IMPL precedent (cumulative 0-3 P2 + 2-5 P3
typical). The extra rounds were driven by:

1. The cassette-prose sweep surfacing incrementally as the
   post-fix-up R-NEW-72 grep pattern broadened (rounds 2 →
   3 → 4: literal "IMPL cassette pins" → singular "cassette
   pin" → all-stems "cassette").
2. The round-5 stub-era `parseGlobalFlags` ordering prose
   only surfacing AFTER the W11 prose surface had quieted —
   Codex's attention had bandwidth to scan a different
   audit-point dimension once the high-frequency cassette
   sites stopped firing.

The 1 P2 was a real runtime correctness issue — `renameDoc`'s
pre-flight-pinned null → not_found cadence (analogous to M14 +
M34 cadence) didn't account for the per-fetcher probe-
description asymmetry: `update_doc_name`'s probe description
promises no return-shape, distinct from `delete_doc` +
`duplicate_doc` which both promise a non-null payload on
success. Mirrors the M32 IMPL "null-vs-empty branch split"
cadence — pre-flight Codex catches contract drift in the schema
/ docstring; IMPL Codex catches contract drift in the runtime
body's predicate logic AND the per-fetcher contract decisions
that require live-wire-shape reasoning.

### Contract drift findings

- **Per-fetcher null-payload semantics MUST be derived from
  probe-description return-shape promises, not by analogy to
  prior milestones.** The pre-flight JSDoc pinned `renameDoc`'s
  null payload as `not_found` by analogy with M14 / M34 +
  `deleteDoc` / `duplicateDoc`, but `update_doc_name`'s probe
  description carries no return-shape prose at all. Round-1
  P2-1 caught this; the per-fetcher contract is now explicitly
  asymmetric and the module-header prose enumerates the three
  fetchers' different semantics. **Lesson for future pre-flight
  Codex prompts:** when multiple sibling fetchers share an
  envelope projection shape but their underlying wire surfaces
  return opaque JSON, the pre-flight contract decision MUST
  derive each fetcher's null-payload semantics from its OWN
  probe description, not by analogy to the cluster's other
  fetchers. Filed as **R-v0.5-NEW-11** at §22.
- **R-NEW-72 cross-doc grep patterns benefit from matching
  noun stems, not literal substrings.** Round 3 surfaced that
  the round-2 grep pattern `cassette pins` missed the singular
  `cassette pin` stem; round 4 surfaced that the round-3
  pattern `cassette pin|cassette pins` missed the bare noun
  `cassette` (forward-looking framing). When R-NEW-72 fires
  after a contract-flipping fix, the grep pattern should
  enumerate the noun + its inflections (`cassette` /
  `cassette pin` / `cassette pins` / `cassette pinned`), not
  just the most-recent stale substring. **Lesson for future
  IMPL sessions:** when running R-NEW-72 post-fix-up grep,
  use `\b<noun>\b` regex matching rather than literal
  substring matching to catch all inflections in a single
  pass. Filed as a refinement to R-NEW-72 — surfaces at
  the next 2nd consumer if a future IMPL session's
  post-fix-up grep encounters the same multi-round narrowing
  cadence.
- **Stub-era module-header prose persists beyond the surface-
  level "STUB FETCHERS → Runtime bodies landed" flip.** The
  IMPL kickoff cross-doc grep caught the "stub" / "PRE-FLIGHT
  STUB" / "deferred_to: v0.5-M35 IMPL" sites but missed the
  collapse of `parseGlobalFlags` calls into `resolveClient`
  for the 4 non-delete verbs (the IMPL flipped the call
  structure but the module headers still described the
  pre-flight ordering). Round-5 caught this — a 5th-round
  finding for what was effectively pre-flight prose preserved
  past IMPL. **Lesson for future IMPL sessions:** the
  pre-IMPL cross-doc grep should ALSO enumerate every call-
  ordering claim in module-header prose ("`parseArgv` before
  X", "X before resolveClient", etc.) and verify each
  ordering claim against the actual IMPL call structure
  post-feat. Filed as **R-v0.5-NEW-12** at §22.

### Exit ratchets

- **Test count: 3806 + 1 skipped → 3858 + 1 skipped (+52
  net).** 42 new integration tests across 5 new
  `tests/integration/commands/doc-*.test.ts` files (9 + 6 +
  7 + 7 + 13) + 11 new envelope snapshots in
  `tests/integration/envelope-snapshots.test.ts` − 1 wrong-
  slot stub test collapsed at IMPL (the pre-flight stub's
  `parseGlobalFlags`-before-c8 test became redundant once the
  c8 block was dropped). One round-1 fix flipped a test's
  expected outcome (rename null → success vs not_found); two
  round-6 fixes added an assertion to an existing test +
  reordered a JSDoc block (test count unchanged on rounds
  2-6).
- **Coverage: 99.18 / 96.29 / 99.04 / 99.45 → 99.29 / 96.36
  / 99.44 / 99.55.** All metrics improved at IMPL close.
  **Branches margin recovered 0.84pp → 0.91pp (+0.07pp)**
  from the runtime-body branches covered by integration tests
  vs the 5 c8-ignore stub drops. **Functions recovered
  0.40pp** from the 10 stub functions (5 fetchers + 5 action
  bodies) becoming covered by runtime bodies + integration
  tests. Same cadence as M32 + M34 IMPL — every metric
  improves at IMPL close vs pre-flight.
- **ERROR_CODES count: 29 (unchanged per D-closures).**
- **Command count: 112 (unchanged from pre-flight; IMPL adds
  no verbs).**
- **`package.json` version: 0.4.0 (unchanged; stays through
  v0.5 milestones; bumps to `0.5.0` at v0.5 release-prep).**

### R-class movement at M35 IMPL close

See §22 for the full R-class register. Headlines:

- **No code-lift R-class movement at M35 IMPL.** The 6-round
  Codex IMPL cluster's findings (1 P2 + 8 P3) were a single
  runtime correctness issue (per-fetcher null-payload
  asymmetry derived from probe descriptions) + prose-precision
  fix-ups + one test detail-slot assertion + one JSDoc
  proximity fix. No 3-consumer threshold crystallised.
- **Two new R-class candidates filed (R-v0.5-NEW-11 +
  R-v0.5-NEW-12).** R-v0.5-NEW-11: per-fetcher null-payload
  contract decision discipline at pre-flight (derive each
  fetcher's null semantics from its own probe description,
  not by analogy). R-v0.5-NEW-12: pre-IMPL cross-doc grep
  extension for call-ordering claims in module-header prose.
  Both LOW priority watch-items at 1-supporting-instance
  threshold; graduate at 3-consumer / 3-instance cadence.
- **R-v0.5-NEW-1 lift evaluated and DEFERRED.** No fresh
  probe ran at M35 IMPL (all input shapes pinned at v0.5
  kickoff + pre-flight); the introspect-helper widening
  watch-item defers to next probe consumer (M36 doc-block
  CRUD likely).
- **R-NEW-72 (post-fix-up cross-doc grep) ratified for the
  5th time** + lesson surfaced for noun-stem grep patterns
  (see Contract drift findings above).
- **R-NEW-37 W2** (operationName parity) safely-by-
  construction across all 5 fetchers — verified clean at
  IMPL rounds 1+7.
- **R-NEW-76** (parseArgv-before-c8 / parseArgv-before-
  resolveClient) consumer-7 applied across all 5 M35 action
  bodies post-IMPL; rule stable post-graduation at M34
  close-docs.

### Lessons for future IMPL sessions

- **Per-fetcher null-payload contract decisions require
  per-fetcher probe-description reasoning** (M35 IMPL round-1
  P2-1 lesson). When a cluster ships multiple opaque-JSON
  return-type fetchers, the pre-flight contract MUST decide
  each fetcher's null-payload semantics individually based on
  that fetcher's own probe description, not by analogy across
  the cluster. The `update_doc_name` vs `delete_doc` /
  `duplicate_doc` asymmetry surfaced only at IMPL because the
  pre-flight schema treated all 3 fetchers symmetrically.
- **R-NEW-72 post-fix-up grep patterns benefit from noun-stem
  matching** (rounds 3-4 lesson). Use `\b<noun>\b` regex
  rather than literal-substring matching to catch all
  inflections in one pass (`cassette` / `cassette pin` /
  `cassette pins` / `cassette pinned`) rather than discovering
  inflections one-at-a-time across consecutive rounds.
- **Pre-IMPL cross-doc grep should enumerate call-ordering
  claims in module-header prose** (round-5 lesson). When the
  IMPL collapses a pre-flight call structure (e.g.
  `parseGlobalFlags` → `resolveClient`), the module-header
  prose describing the OLD ordering must be flipped in
  parallel. The kickoff cross-doc grep should grep for "before
  resolveClient" / "before X" patterns and verify each ordering
  claim against the actual IMPL call structure post-feat.
- **JSDoc proximity matters when inserting helpers between
  interface blocks and their consuming functions** (round-6
  P3-2 lesson). If a helper is added between a `interface
  XInputs` / `interface XResult` declaration block and the
  `export const X = ...` function it supports, the function's
  JSDoc gets orphaned above the helper. Either place the
  helper AFTER the function it supports, OR move the function's
  JSDoc to immediately precede the function (M35 round-6
  P3-2 took the latter approach).
- **Codex IMPL round-count calibration for opaque-JSON
  mutation surfaces.** M35 took 7 rounds (6 fix-ups + 1
  ratification) vs the M22/M27/M30/M31/M32/M34 write-surface
  precedent of 2-4 rounds. Factors that lifted the count: (a)
  the read-only probe couldn't capture live mutation cassettes
  — module-header prose carried defensive-tense framing that
  needed iterative scrubbing; (b) the per-fetcher null-payload
  asymmetry surfaced as a P2 contract decision at round 1,
  not at pre-flight. **Lesson:** opaque-JSON return-shape
  milestones should pre-budget 5-7 Codex IMPL rounds vs the
  3-4 typical for OBJECT-return mutation surfaces; the
  cassette-prose iterative scrubbing alone is worth 2-3 extra
  rounds.

## 22. Refactor backlog — v0.5 R-class register

R-class movements at v0.5-M34 IMPL close (`afdba15..02f1b1a`):

- **`_team-membership.ts:projectMembershipResults` lifted at
  IMPL kickoff (`afdba15`).** 2-consumer inline lift mirroring
  M26b `_shared.ts:requireDevBoard` cadence — both `team-add-
  members` + `team-remove-members` ship a byte-identical
  partial-success projection logic differing only in the
  `operation` literal. Helper takes wire failed/successful
  bucket lists + input user_id argv order, projects to
  universal §6.1 `results: [{user_id, ok, ...}]` with input-
  order preserved + failed-bucket-priority discipline +
  generic `membership_failed`-code asymmetry. Fold-in-place
  would have left two copies of the same generic-message
  constant + two copies of the same `Map`-build pattern.
  Lift fired at the 2-consumer threshold because the shared
  helper sits next to the canonical wire-vs-CLI asymmetry
  documentation note + reads as one clear unit. **Status:
  shipped at `afdba15`.**
- **R-NEW-73 stays UNFILED at 6 candidate consumers post-M34
  IMPL.** IMPL surfaced the runtime divergence between the 6
  null-payload sites: per-fetcher error code splits
  (`internal_error` for listTeams / getTeam null root +
  create-team missing-key + add/remove-members null payload;
  `not_found` for delete-team null payload + get-team empty
  array) + per-fetcher details shape (`team_id` / `team_name`
  / contextual hint). A parametrised helper signature would
  carry 4-5 args + a message closure, exceeding the 4-6 inline
  lines it would replace. Re-evaluate at v0.5.x consumer-7+
  if a future verb's null-payload semantics align exactly
  with an existing fetcher's shape.
- **R-v0.5-NEW-4 gains 2nd sub-field supporting instance**
  (wire-vs-output schema split at sub-field granularity).
  The `projectTeam` helper at `src/api/teams.ts` filters wire
  `null` entries out of `Team.users` at the fetcher boundary
  before the agent-facing `teamSchema` parses at `emitSuccess`.
  Two sub-field supporting instances (M34 wire-vs-output split
  at the pre-flight schema level AND the IMPL projection level)
  + the v0.3-M22 full-schema precedent (3 cumulative signals).
  Watch-item, fires at 3rd sub-field supporting instance.
  Pattern: "wire-parse accepts permissive nullability;
  projection layer narrows to the agent-facing contract before
  outputSchema validates."
- **R-v0.5-NEW-6 filed (new at M34 IMPL)** — pre-flight Codex
  prompt should enumerate "empirical-probe prose vs zod schema
  nullability cross-check" as a template-stable audit-point.
  Surfaced at M34 IMPL round-1 P2-1 where the pre-flight schema
  documented `ChangeTeamMembershipsResult` bucket containers as
  nullable in prose but encoded them as non-null in the schema.
  1 supporting instance; LOW priority watch-item, fires at 2nd
  consumer (any future pre-flight schema where the empirical-
  probe note and the zod encoding drift). Fold into
  `.claude/templates/codex-pre-flight-review.md` alongside W1 /
  W2 if the 2nd consumer surfaces the same pattern.
- **R-NEW-41 4th consumer ratified at M34 IMPL.** The
  asymmetric wire-vs-CLI semantics note at
  `teamMembershipResultSchema` JSDoc + the cross-link from
  team-add-members.ts / team-remove-members.ts to
  `docs/architecture.md`'s "Wire-vs-CLI semantics documentation
  conventions" section both hold post-IMPL — the IMPL runtime
  body wired the projection helper that emits the generic
  `membership_failed` code without any per-user wire reason.
  Section is stable at 4 documented asymmetries (M27
  Webhook.config + M27 NotificationTargetType + M31 multipart-
  vs-JSON transport + M34 ChangeTeamMembershipsResult).

R-class movements at v0.5-M34 pre-flight close
(`17c1a54..df4f036`):

- **R-NEW-70 shipped at `17c1a54`** (ahead-of-feat per R-NEW-29's
  M25 cadence). `parseBrandedListArg<T>(raw, brandSchema,
  options)` lifted to `src/utils/parse-brand-list.ts`. 4
  consumers post-lift: 1 migrated (doc/list `--workspace`) + 3
  new (team-create / team-add-members / team-remove-members
  `--users`). 18 paired unit tests at
  `tests/unit/utils/parse-brand-list.test.ts`. Behaviour-
  preserving for the workspace site (user-facing error envelope
  shape pinned via the lifted helper's options); per-entry
  brand rejection now threads the underlying `ZodError` as
  `cause` (additive — agent-contract neutral, better
  `--debug` diagnostic). **Status: shipped.** Full entry at
  v0.4-plan §22 R-NEW-70.
- **R-NEW-76 graduated** (pre-flight stub argv-before-deferred-
  feature-throw discipline) at v0.5-M34 pre-flight close. 6
  forcing supporting instances pre-graduation (M31a, M31b,
  M32a, M32b, M33, M34's 6 command stubs). Promoted to
  permanent CLAUDE.md "Workflow rules" entry under the Two-AI
  review rule. **Status: shipped.** Full entry at v0.4-plan
  §22 R-NEW-76.
- **R-NEW-84 graduated via negative-case validation** at v0.5-
  M34 pre-flight close. 4th supporting instance (the v0.5
  kickoff probe session was the 3rd supporting instance; the
  M34 pre-flight cluster validates the rule's scope by
  correctly NOT firing the skip when production code lands).
  Promoted to permanent CLAUDE.md "Workflow rules" entry.
  **Status: shipped.** Full entry at v0.4-plan §22 R-NEW-84.

R-class state on the watch-items v0.4 carried forward
(post-M34 pre-flight close):

- **R-NEW-41 4th-consumer trigger fired.** The
  `ChangeTeamMembershipsResult.failed_users[]` carries-User-
  object-but-no-reason asymmetry is the 4th supporting site
  for the docs/architecture.md "Wire-vs-CLI semantics
  documentation conventions" section graduated at v0.4-M31.
  Canonical asymmetry note lives at
  `teamMembershipResultSchema` JSDoc in `src/api/teams.ts`;
  cli-design §4.3 + team-add-members.ts + team-remove-
  members.ts carry short one-line pointers (Codex round-2
  P3-1 + round-3 P3-1 pinned the cadence). The architecture-
  section is unchanged at M34 pre-flight; M34 IMPL will add
  the new asymmetry entry to the section per the graduation
  cadence.
- **R-NEW-73** stays UNFILED at 6 candidate consumers
  post-M34 IMPL. The IMPL session confirmed the per-consumer
  divergence stays high enough to keep the helper deferred —
  per-fetcher error code splits (`internal_error` for listTeams
  / getTeam null root + create-team missing-key + add/remove-
  members null payload; `not_found` for delete-team null
  payload + get-team empty array) + per-fetcher details shape
  (`team_id` / `team_name` / contextual hint) mean a
  parametrised signature would carry more surface than the
  4-6 inline lines it would replace. Re-evaluate at v0.5.x
  consumer-7+ if a future verb's null-payload semantics align
  exactly with an existing fetcher's shape.
- **R-NEW-31** stays at 1 consumer post-M34 pre-flight. The
  team-add-members / team-remove-members partial-success
  envelope ships flat per-record `ok: boolean` discriminators,
  NOT per-status detail unions.
- **R-NEW-58** ratified for the 3rd time at M34 pre-flight
  kickoff via the R-NEW-70 lift-ahead-of-feat at `17c1a54`.
- **R-NEW-72** ratified for the 3rd time at M34 pre-flight
  round-2 fix-up. The wire-vs-output schema split flipped a
  contract surface; the post-fix-up grep across 4 consumer
  output schemas found no drift.

v0.4 R-class entries (R-NEW-44 through R-NEW-84) stay tracked in
v0.4-plan §22 for historical context. Open watch-item candidates
carried forward from v0.4 that v0.5 may trigger:

- **R-NEW-31** (discriminated-union per-status detail schema
  pattern) — 1 consumer at v0.4 close; **M34 negative-case
  validation: did NOT fire** — team-add-members /
  team-remove-members partial-success records use a flat
  `ok: boolean` discriminator with optional `user` / `error`
  slots, NOT a per-status detail union (no status enum that
  varies the detail shape). Trigger remains open for M35-M37
  doc-mutation per-block-type detail variability.
- **R-NEW-43** (deferred-feature surface pattern — placeholder
  guard + sentinel constant + production-mode-no-seam
  integration test) — 1 consumer at v0.4 close (M28 OAuth
  placeholder). v0.5 unlikely to fire unless a doc-mutation is
  gated on external registration (e.g., HTML import requires
  account-owner consent).
- **R-NEW-68** (`parseStrictDecimal` strict-decimal-integer
  parser) — 1 consumer at v0.4-M32 close; **M34 negative-case
  validation: did NOT fire** — team verbs ship no numeric-flag
  argv slot (team-list is argv-empty per D6; team-create's
  `--guest-team` / `--allow-empty` are boolean flags;
  team-add-members / team-remove-members's `--users` is a
  brand-list parsed via the lifted R-NEW-70 helper). Trigger
  remains open for M35-M37 doc-block `--position <n>` flag or
  any future numeric flag.
- **R-NEW-69** (`requiredJsonValueSchema` zod helper) — 1
  consumer at v0.4-M32 close. v0.5 likely brings a 2nd via
  `update_doc_block.content` payloads + a 3rd via
  `import_doc_from_html` payloads.
- **R-NEW-70 shipped at v0.5-M34 pre-flight close** (4
  consumers post-lift: doc/list migrated + 3 new team sites).
  See above for full close-docs note; full historical entry at
  v0.4-plan §22 R-NEW-70.
- **R-NEW-71** (Pagination-invariant `.superRefine` with
  dirty-input early-return guard) — 1 consumer at v0.4-M32
  close; **M34 negative-case validation: did NOT fire** — D6
  closure rules out team-list pagination (`Query.teams`
  exposes no `limit:` / `page:` / cursor slots; the
  `teamListOutputSchema` `.superRefine` enforces only
  `returned_count === teams.length`, not the pagination-
  invariant `has_more === (returned_count === limit)` shape
  R-NEW-71 captures). Trigger remains open for M35-M37 if
  doc-mutation surfaces ship paginated reads (`Query.docs`
  already paginated at M32; future doc-mutation result
  envelopes might too).
- **R-NEW-73** (`assertNonNullArrayPayload` helper for fetcher
  response-parse boundaries) — 3 consumers at v0.4 close
  (`listWebhooks` M27 + `listDocuments` M32 + `getDocument`
  M32); **M34 pre-flight raised the candidate count to 6**
  (3 new fetcher response-parse boundaries pre-shipped as
  stubs: `listTeams` + `addUsersToTeam` +
  `removeUsersFromTeam`); **M34 IMPL confirmed the lift cost
  stays too high** — runtime bodies surfaced the per-fetcher
  divergence empirically (mixed `internal_error` / `not_found`
  rewrap codes + per-fetcher details shape). A parametrised
  signature would carry 4-5 args + a message closure,
  exceeding the 4-6 inline lines it would replace. Stays
  UNFILED; re-evaluate at v0.5.x consumer-7+ if a future
  verb's null-payload semantics align exactly with an
  existing fetcher's shape.
- **R-NEW-74** (`kind: 'record'` for wrapped-paginated-record
  `emitSuccess` shape) — 2 consumers at v0.4 close (M22 usage
  + M32 doc list); **M34 negative-case validation: did NOT
  fire** — team-list ships as a wrapped record (`teamListOutput
  Schema` with `teams` + `returned_count` slots) but Monday's
  `Query.teams` exposes no pagination (D6 closure: no `limit:`
  / `page:` / cursor on the wire), so the wrapped-PAGINATED-
  record pattern doesn't apply. The team-list envelope uses
  `kind: 'single'` correctly. Trigger remains open for any
  future v0.5+ paginated-record verb where table-UX
  observability surfaces the conflation.
- **R-NEW-76** (Pre-flight stub argv-before-deferred-feature-
  throw discipline) — 5 forcing supporting instances at v0.4
  close. v0.5 pre-flights likely fire the 6th forcing
  instance, graduating to a permanent CLAUDE.md "Workflow
  rules" entry.
- **R-NEW-77** (CLI-internal milestone empirical-probe-slot
  equivalent — framework / SDK capability check) — 1
  supporting instance at v0.4-M33 close. v0.5 unlikely to
  fire (no CLI-internal milestones in scope).
- **R-NEW-78** (Codex template W{N} audit-point for output-
  format-flag enumeration correctness) — 1 supporting
  instance at v0.4-M33 close. v0.5 may fire at any new write
  verb's parse-boundary contract diff.
- **R-NEW-79 / R-NEW-80 / R-NEW-81** (M33-IMPL-surfaced
  retroactive candidates) — all 1-consumer/instance LOW-
  priority watch-items at v0.4 close; v0.5 unlikely to fire
  (CLI-internal shell-completion specifics).
- **R-NEW-82** (release-prep `deferred_to` audit step) — 2
  supporting instances at v0.4 close. v0.5 release-prep likely
  fires the 3rd consecutive instance, graduating to a
  permanent CLAUDE.md "Workflow rules" entry.
- **R-NEW-84 shipped at v0.5-M34 pre-flight close**
  (graduated to permanent CLAUDE.md "Workflow rules" entry
  under the Two-AI review rule). 4th supporting instance
  fired at the v0.5 kickoff probe session (probe scripts
  outside `src/**/*.ts` coverage; gates carried verification
  without a Codex round). The M34 pre-flight cluster
  validated the rule's scope via NEGATIVE-CASE — production
  `src/api/teams.ts` + 6 new `src/commands/user/team-*.ts`
  files landed → Codex review applied per the standard
  cadence (4 rounds) → rule correctly did NOT fire the skip.
  Full historical entry at v0.4-plan §22 R-NEW-84.

v0.5-specific R-class candidates seeded at the kickoff probe
session:

- **R-v0.5-NEW-1 — Extend `_lib.ts:introspectType` to enumerate
  `inputFields` alongside `fields` + `enumValues`** (2
  consumers at v0.5 kickoff: `v0.5-inputs-and-results.ts`
  round 2 + `v0.5-nested-inputs.ts` round 3; MEDIUM priority
  watch-item, code lift candidate at 3rd consumer per R7/R8
  threshold OR ahead-of-feat at M34 pre-flight per R-NEW-58
  cadence).

  **Pattern.** `_lib.ts:introspectType`'s GraphQL query selects
  `fields { ... }` + `enumValues(includeDeprecated: true) {
  ... }` + `possibleTypes { ... }` but NOT `inputFields {
  ... }`. Input-object types (`INPUT_OBJECT` kind) come back
  with `fields: null` — the helper renders them as
  `kind: INPUT_OBJECT` with no per-field detail. Probe scripts
  that need to pin Monday's input shapes (any mutation
  introspection) must work around this.

  **Workaround at v0.5 kickoff (rounds 2 + 3).** Both probe
  scripts inlined a sibling `introspectRich` helper at the
  top of the file with the missing `inputFields` selection
  added:

  ```graphql
  inputFields { name description defaultValue type { name kind ofType { name kind ofType { name kind } } } }
  ```

  Plus a per-script render function that walks both `fields`
  and `inputFields` in `printRich`. Net per-script duplication:
  ~50 LOC (interface definitions + introspect + render +
  renderTypeRef).

  **Cost of staying with the workaround.** Every v0.5 probe
  that touches an INPUT_OBJECT re-duplicates the ~50 LOC. M34
  pre-flight probes the team-mutation cassettes will hit the
  same wall; M35/M36/M37 likewise.

  **Lift options:**

  1. **Extend `introspectType`** to also select `inputFields`.
     `IntrospectedType` widens with `inputFields:
     ReadonlyArray<IntrospectedInputField> | null`. Existing
     consumers (`printIntrospected`) extend to render
     `inputFields` when non-null. Trade-off: backward-compatible
     widening; existing callers continue to work; the
     `printIntrospected` rendering must learn the new section.
  2. **Ship a sibling `introspectRich`** that's a clean union
     of fields + inputFields + enumValues. Existing
     `introspectType` callers stay narrow.

  **Recommended trigger.** Lift at the next probe consumer.
  M34 pre-flight + M35 pre-flight both DID NOT re-probe (M34
  used the v0.5-kickoff round-2 + round-3 findings verbatim;
  M35 likewise re-used those findings for `CreateDocInput` /
  `CreateDocBoardInput` / `CreateDocWorkspaceInput` /
  `DuplicateType` shapes). The R-NEW-58 ahead-of-feat lift
  was evaluated at both pre-flights and DEFERRED — no fresh
  probe means no new consumer crystallized. **M36 doc-block
  CRUD is the next likely trigger** — if M36's pre-flight
  introspects `CreateDocBlockInput` / `UpdateDocBlockInput`
  shapes from a fresh probe, the lift fires as the 3rd
  consumer. If M36 also reuses kickoff findings, lift defers
  to a future v0.5.x / v0.6 probe consumer. Lift target file:
  `scripts/probe/_lib.ts`; lift naming: prefer option (1) —
  extend `introspectType` — to keep one canonical helper.

  **Out-of-coverage-scope note.** `scripts/probe/_lib.ts`
  lives outside the `src/**/*.ts` coverage scope, so the
  lift won't move branches margin. The helper widening
  doesn't affect any production module.

  **Status at M35 pre-flight close: stays at 2 consumers;
  lift DEFERRED.**

- **R-v0.5-NEW-2 — `import_doc_from_html` / `add_content_to_
  doc_from_markdown` `success: false` envelope-mapping pattern**
  (1 supporting instance at v0.5 kickoff; LOW priority
  watch-item, NOT a code lift yet).

  **Pattern.** Both M37 mutations return a custom result
  OBJECT with `{ success: Boolean!, error: String?, ... }`
  rather than a typed `Document` / `DocumentBlock` shape OR
  the opaque `JSON` scalar M35 uses. The fetcher boundary
  must map `success: false` to a typed CLI error code —
  `validation_failed` if `error` is populated with a wire-
  reason string; `internal_error` if `error` is empty
  (wire-regression). At 2 consumers today (`ImportDocFromHtml
  Result` + `DocBlocksFromMarkdownResult`); fires at a 3rd
  consumer in v0.5.x / v0.6 if Monday surfaces another
  `success/error` result OBJECT.

  **Lift target at trigger:** `mapSuccessErrorResult(result:
  { success: boolean; error?: string | null }, { wirePath:
  string }) → ApiError | null` helper in
  `src/api/documents.ts` (or `src/utils/api-result.ts` for
  cross-module sharing if a non-doc consumer fires).

  **Status:** 1 supporting instance (2 wire surfaces of the
  same pattern); LOW priority watch-item.

- **R-v0.5-NEW-3 — Wire-vs-CLI semantics documentation 4th
  consumer trigger — camelCase vs snake_case arg-name
  asymmetry across the 9 doc mutations** (R-NEW-41 4th
  consumer — graduation candidate to extend the
  `docs/architecture.md` "Wire-vs-CLI semantics documentation
  conventions" section).

  **Pattern (R-NEW-41 verbatim, 4th supporting site).** v0.4-
  M31 graduated R-NEW-41 at the 3rd consumer (multipart-vs-
  JSON transport asymmetry). The 9-doc-mutation arg-name
  surface adds a 4th instance:

  - **5 mutations use camelCase args**: `update_doc_name`
    / `delete_doc` / `duplicate_doc` / `import_doc_from_html`
    / `add_content_to_doc_from_markdown` (args: `docId`,
    `afterBlockId`, `workspaceId`, `folderId`,
    `duplicateType`).
  - **3 mutations use snake_case args**: `create_doc_block`
    / `update_doc_block` / `delete_doc_block` (args:
    `doc_id`, `block_id`, `after_block_id`,
    `parent_block_id`).
  - **1 mutation uses neither** (`create_doc` takes a single
    `location: CreateDocInput!` input).

  Plus a 5th supporting site in the same M36 / M32 surface:
  `create_doc_block.type` accepts a 16-value
  `DocBlockContentType` closed ENUM, while M32 read-side
  `DocumentBlock.type` surfaces as plain `String` — wire
  write-side constrains, read-side relaxes.

  **CLI projection at fetcher boundary.** All 9 mutations
  surface to the CLI as kebab-case argv flags (e.g.
  `--doc-id` regardless of wire camelCase vs snake_case);
  the snake_case form appears in error-details payload
  (`details.issues[].path` etc.) where the wire path is
  load-bearing for debugging. Architecture-section entry
  documents the wire-side inconsistency + the CLI's
  uniform-kebab-case-at-argv convention so future doc-
  mutation extensions don't drift.

  **Status:** 4th supporting site (M27 + M31 + M32 read-side
  + M35-M37 mutation args + M36 block-type enum read/write
  asymmetry); graduation candidate at M34/M35 pre-flight
  per the architecture-section extension cadence. The
  architecture section already exists (v0.4-M31 graduation);
  the v0.5 work extends it with the new asymmetry rather
  than creating new lift surface.

v0.5-M34 pre-flight R-class candidates (filed at the
post-cluster refactor-audit `a2d5ae7..HEAD`):

- **R-v0.5-NEW-4 — Wire-vs-output schema split at sub-field
  granularity** (2 supporting instances at v0.5-M34 pre-flight
  close; LOW priority watch-item, code-discipline lift
  candidate at 3rd consumer per R7/R8 threshold).

  **Pattern.** Surfaced at v0.5-M34 Codex round-2 P2-1. When a
  Monday wire object has a field whose nullability (or shape
  permissiveness) is broader on the wire than the agent-facing
  contract should expose, the schema split is:

  - `<X>WireSchema` (fetcher-internal; pins the wire's actual
    permissive shape — e.g. accepts `null` entries in a list).
  - `<X>Schema` (exported; the agent-facing strict shape that
    flows through `outputSchema.parse(...)` at `emitSuccess`
    AND `monday schema <verb>` JSON-Schema introspection).
  - Fetcher parses via `<X>WireSchema` → filters / projects to
    the strict shape → constructs the output that flows through
    `emitSuccess`. The projection filter at the action body is
    the wire-vs-output translation layer.

  Splitting wire-parse from output-projection keeps the agent
  contract narrow (`monday schema` exports the strict shape;
  agents reading the JSON-Schema see the clean shape that
  `emitSuccess`'s `outputSchema.parse(...)` defence-in-depth
  guards) while the wire-parse boundary stays permissive enough
  to absorb wire-shape variations (Monday surfaces null entries
  when a record was tombstoned post-creation; the strict
  schema would fault the response parse otherwise).

  **Supporting instances (2 at v0.5-M34 close):**

  1. **v0.3-M22 `monday usage` (full-schema granularity).** The
     `platform_api.daily_*` projection lands a loose wire
     shape parsed by an internal-ish schema, projected to the
     strict output schema. M22 was the original precedent —
     loose-wire / strict-output cadence operating at the
     whole-schema level (the wire payload's per-field
     nullability surface was looser than the projected
     envelope).
  2. **v0.5-M34 `teamWireSchema` vs `teamSchema` (sub-field
     granularity).** The wire schema accepts `users:
     [user1, null, user3]` (sparse entries when a member's User
     record was tombstoned); the output schema rejects null
     entries because the projection layer filters them out
     before the envelope emit. Codex round-2 P2-1 surfaced the
     drift when the round-1 fix widened `teamSchema` directly
     (polluting the agent-facing contract); the round-2 fix
     introduced the split.

  **Distinction from R-NEW-69** (`requiredJsonValueSchema` zod
  helper for required-but-any-JSON-shape slots): R-NEW-69 is a
  reusable zod-shape primitive for ONE specific permissive
  case (required key, any JSON value). R-v0.5-NEW-4 is a
  cross-schema DISCIPLINE for splitting wire-parse from
  output-projection at any granularity. They're complementary
  — a future schema might use `requiredJsonValueSchema` at the
  wire boundary AND split the schema into wire/output forms.

  **Trigger.** Fires at 3rd consumer. Likely sites: v0.5-M36
  `DocumentBlock.parent_block_id` or `Document.settings` JSON-
  payload variability (wire returns a JSON scalar with shape
  variants the agent contract shouldn't expose verbatim); OR
  a v0.5.x team-owners read surface (`Team.owners` inner
  nullability per the round-1 probe truncation — the M34
  pre-flight docstring flags this as the next candidate site
  if IMPL cassette surfaces null entries).

  **Status: 2 supporting instances; LOW priority watch-item;
  fires at 3rd consumer.** Not a code lift today — the
  wire-vs-output split is a 2-schema discipline, not a
  reusable helper. If a 4th-or-5th instance shipps with
  consistent shape, consider a `splitWireOutput<W, O>(
  wireSchema, outputSchema, project: (w: W) => O)` helper —
  but the per-consumer projection-filter logic likely
  diverges enough that the helper would over-fit.

- **R-v0.5-NEW-5 — Codex template W{N} audit-point for
  paired-verb cli-design row consistency** (1 supporting
  instance at v0.5-M34 close; LOW priority watch-item,
  template-stable candidate; fires at 2nd consumer + folds
  into `.claude/templates/codex-pre-flight-review.md`
  alongside W1 redactor + W2 operationName-parity + W (future
  output-format-flag enumeration from R-NEW-78)).

  **Pattern.** Surfaced at v0.5-M34 Codex round-3 P3-1. When
  paired verbs (add-X / remove-X, create-X / delete-X) share
  a wire-vs-CLI asymmetry, BOTH rows in cli-design §4.3
  should carry the same short pointer to the canonical note.
  M34 caught this: team-add-members's row pinned the
  `failed_users[]` asymmetry inline (Monday's wire carries
  User object but no per-user reason; canonical note at
  `teamMembershipResultSchema` JSDoc + docs/architecture.md);
  team-remove-members's row only said "same envelope shape"
  without the equivalent pointer. The drift is hard to spot
  from the cli-design row side-by-side until a paired-verb
  scan runs — the round-3 fix-up landed the short
  continuation line.

  **Lift target at trigger.**
  `.claude/templates/codex-pre-flight-review.md` adds a new
  W{N} audit-point asking the reviewer to verify paired-verb
  cli-design row consistency at every new milestone
  introducing paired verbs (add/remove, create/delete, etc.):

  > W{N}: paired-verb cli-design row consistency audit — when
  > a milestone introduces paired verbs (add-X / remove-X,
  > create-X / delete-X, start-X / stop-X) that share a
  > wire-vs-CLI asymmetry OR a canonical-note cross-link,
  > both cli-design §4.3 rows MUST carry the same short
  > pointer to the canonical note. Verify by side-by-side
  > comparison: identify the paired verbs in the diff,
  > extract the canonical-note cross-reference from one row,
  > confirm the other row carries an equivalent pointer.

  Fold-in cadence mirrors R-NEW-78's (output-format-flag
  enumeration) folding into the template at the 2nd
  consumer.

  **Distinction from R-NEW-72** (cross-doc grep after every
  contract-flipping Codex fix-up): R-NEW-72 covers re-running
  the cross-doc grep AFTER a contract-flipping fix-up to
  catch new drift the fix-up introduced. R-v0.5-NEW-5 covers
  side-by-side paired-verb cli-design row consistency BEFORE
  the first Codex round — an audit-point that would catch
  the drift at round 1 rather than waiting for the round-N
  fix-up cascade to surface it. Complementary: R-NEW-72
  catches drift AFTER fix-ups land; R-v0.5-NEW-5 catches it
  AT pre-flight contract diff time.

  **Trigger.** Fires at 2nd consumer + folds into the codex
  template. Likely 2nd sites: v0.5.x team-owners
  assign/remove pair (if the v0.5.x candidate-selection
  round picks them up); v0.5-M36 doc-block create/delete
  pair; any future v0.6+ paired-verb cluster.

  **Status: 1 supporting instance; LOW priority watch-item;
  template-stable candidate.** Cross-references R-NEW-25
  (findings up front directive) + R-NEW-17 (W1 redactor) +
  R-NEW-37 (W2 operationName parity) + R-NEW-78 (output-
  format-flag W audit-point candidate) — template-extension
  cadence at the codex-pre-flight-review.md surface.

- **R-v0.5-NEW-6 — Pre-flight Codex prompt template W{N}
  audit-point for empirical-probe-prose vs zod-schema
  nullability cross-check** (1 supporting instance at v0.5-
  M34 IMPL round-1 P2-1; LOW priority watch-item, template-
  stable candidate; NOT a code lift).

  **Pattern.** Pre-flight Codex rounds review the schema +
  the empirical-probe prose in the same module but don't
  always catch when the two disagree on nullability. M34
  pre-flight's `changeTeamMembershipsResultSchema` documented
  both `failed_users` + `successful_users` outer containers
  as nullable in prose (per the probe's outer-LIST
  introspection without `NON_NULL` wrapper) but encoded them
  as `z.array(teamUserSchema)` (non-nullable) in the schema.
  The inconsistency only surfaced at M34 IMPL round-1 when
  the runtime body parsed a wire-valid all-success / all-
  failed response and the schema rejected it as
  `internal_error`.

  **Why pre-flight Codex misses this.** Pre-flight reviews
  read the schema + docstring as parallel surfaces but don't
  cross-reference them field-by-field. The reviewer can
  ratify the schema's shape OR the docstring's empirical
  claim independently without noticing the mismatch.

  **Lift target at trigger.**
  `.claude/templates/codex-pre-flight-review.md` adds a new
  W{N} audit-point alongside W1 / W2:

  > W{N}: empirical-probe prose vs zod-schema nullability
  > cross-check — for every new `*Schema` defined in this
  > diff whose JSDoc cites empirical-probe introspection,
  > verify each field's zod nullability encoding
  > (`z.X.nullable()` / `z.X.optional()` / no modifier)
  > matches what the prose claims about wire-side
  > nullability. The check is field-by-field: take each
  > prose claim ("`failed_users: [User!]` — nullable
  > container per probe"), find the corresponding zod slot
  > in the same module, confirm the encoding agrees.

  Fold-in cadence mirrors R-NEW-78 / R-v0.5-NEW-5 — fires
  at 2nd consumer (any future pre-flight schema where the
  probe-prose-vs-schema drift surfaces at IMPL).

  **Trigger.** Fires at 2nd supporting instance + folds
  into the codex template. Cross-references R-NEW-25
  (findings up front directive) + R-NEW-17 (W1 redactor) +
  R-NEW-37 (W2 operationName parity) + R-NEW-78 (output-
  format-flag W audit-point candidate) + R-v0.5-NEW-5
  (paired-verb cli-design row consistency) — template-
  extension cadence at the codex-pre-flight-review.md
  surface.

  **Distinction from R-NEW-72** (cross-doc grep after every
  contract-flipping Codex fix-up): R-NEW-72 covers
  re-running the cross-doc grep AFTER a contract-flipping
  fix-up to catch new drift the fix-up introduced.
  R-v0.5-NEW-6 covers BEFORE-first-round cross-checking of
  the schema-vs-prose contract surface itself — an audit-
  point that would catch the M34 IMPL P2-1 drift at
  pre-flight round 1 rather than waiting for the IMPL
  runtime body to surface it.

  **Status: 1 supporting instance; LOW priority watch-item;
  template-stable candidate.** Likely 2nd-consumer sites:
  v0.5-M35 doc-mutation input-object schemas (`create_doc
  (location: { workspace: {...} })` nested input types are
  introspection-rich); any future probe-driven pre-flight
  with mixed nullable / non-nullable list slots.

- **R-v0.5-NEW-7 — Read-one-by-id wrapper-extract helper**
  (2 consumers at v0.5-M34 IMPL close: `getDocument` (M32) +
  `getTeam` (M34); LOW priority watch-item, code lift
  candidate at 3rd consumer per R7/R8 threshold).

  **Pattern.** Both `getDocument` + `getTeam` parse a
  wrapping response schema, then run a near-byte-identical
  predicate fan-out for the single-id wire shape Monday
  uses across reads:

  ```ts
  if (parsed.<root> === null) { throw ApiError('internal_error', ..., {drift hint}); }
  if (parsed.<root>.length === 0) { throw ApiError('not_found', ..., {id detail}); }
  if (parsed.<root>.length > 1) { throw ApiError('internal_error', ..., {wire regression hint}); }
  const [<entity>] = parsed.<root>;
  /* c8 ignore next 6 */
  if (<entity> === undefined) { throw ApiError('internal_error', ..., {sparse array hint}); }
  // ...projection + return
  ```

  Per-consumer divergence: the `<root>` key + entity name +
  detail-key string + projection function differ, but the 4
  predicate branches + their error codes are byte-identical.
  Pre-fix per-consumer copy: ~35 lines per fetcher (8 lines
  × 4 predicates + return shape).

  **Lift target at trigger.** `src/api/single-id-extract.ts`
  exporting `extractSingleByIdOrThrow<TWire, TOutput>(parsed:
  { <root>: TWire[] | null }, opts: { rootKey, entityName,
  detailKey, id, project: (wire: TWire) => TOutput })`.
  Signature is tractable — 4 args including the projection
  function. Net savings: ~25 lines per consumer; ~75 lines
  at the 3-consumer threshold.

  **Cost of staying inline today.** 2-consumer duplication
  is below the rule-of-three threshold. Lifting now would
  parameterise on a one-off shape — the 3rd consumer's exact
  surface might force a signature widening. Wait.

  **Trigger.** Fires at the 3rd consumer (likely v0.5-M35
  `getDoc` variant if the doc-CRUD cluster adds a new
  read-one-by-id verb; OR v0.5.x team-by-name lookup). At
  trigger: lift to `src/api/single-id-extract.ts` + migrate
  all 3 sites + add unit tests pinning each branch.

  **Status: 2 consumers at v0.5-M34 IMPL; LOW priority
  watch-item.** Cross-references R-NEW-73 (parallel
  null-payload helper that stays unfiled at high consumer
  count due to per-consumer divergence — R-v0.5-NEW-7's
  branches are more uniform across consumers, so the lift
  cost / benefit ratio differs).

- **R-v0.5-NEW-8 — Test-fixture `wireUser` helper
  consolidation** (7 consumers at v0.5-M34 IMPL close: 6
  team integration test files + 1 envelope-snapshot describe
  block; LOW priority watch-item, code lift candidate at
  next test-refactor session that touches the team test
  files).

  **Pattern.** Every team integration test file (`team-list`
  / `team-get` / `team-create` / `team-delete` / `team-add-
  members` / `team-remove-members`) + the team describe
  block in `envelope-snapshots.test.ts` defines an
  identical 5-line `wireUser` factory:

  ```ts
  const wireUser = (id: string) => ({
    id,
    name: `User ${id}`,
    email: `user${id}@example.test`,
  });
  ```

  Net duplication: 5 lines × 7 consumers = 35 lines. The
  variant `wireTeam(overrides)` factory varies across files
  but `wireUser` is byte-identical.

  **Lift target at trigger.**
  `tests/integration/commands/_team-fixtures.ts` exporting
  `wireTeamUser(id)` (matches the M19 / M31 / M32 slim-User
  fixture cadence) + optionally `wireTeam(overrides)` if
  the team-shape factory converges enough. Test files
  swap `const wireUser = ...` for `import { wireTeamUser as
  wireUser } from '../_team-fixtures.js'`.

  **Cost of staying inline today.** Pattern miss is small
  (35 lines across the project); each test file remains
  self-contained which has a separate readability value.
  The cadence mirrors R-NEW-14/15/16 (errorMessage / asError
  / errorCode lifts at v0.3 — those went 5-12 days unnoticed
  at 17 sites before retroactive consolidation). 7 sites is
  well below the 17-site retroactive bar.

  **Trigger.** Fires when (a) the test-fixture footprint
  grows beyond 10 consumers (next 2-3 team-related sessions
  likely), OR (b) a test-side R-class refactor session
  touches the team test files for a related reason. NOT a
  forcing trigger today.

  **Status: 7 consumers; LOW priority watch-item.** Mirrors
  the M2.5 R-class cadence — file for visibility, lift when
  the surface grows or a related refactor lands.

- **R-v0.5-NEW-9 — Round-N parallel-fetcher fix-up test
  parity discipline** (1 supporting instance at v0.5-M34
  IMPL round-2 P3-2; LOW priority watch-item — process
  discipline, NOT a code lift; folds into the IMPL-review
  cadence at 2nd supporting instance).

  **Pattern.** When a Codex IMPL round-N fix-up updates
  parallel sites across N sibling fetchers (e.g. adding the
  same `?? []` normalisation to both `addUsersToTeam` AND
  `removeUsersFromTeam`), the round-N test fix MUST cover
  all N consumers — not just one. A one-consumer test fix
  lets future refactors regress N-1 consumers silently.

  M34 IMPL round-1 (`99bbd01`) widened
  `changeTeamMembershipsResultSchema` + added `?? []` to
  both `addUsersToTeam` + `removeUsersFromTeam`, but the
  round-1 test fix only pinned `team-add-members.test.ts`.
  Round-2 P3-2 caught the test-coverage parity gap +
  required adding mirror cassettes to
  `team-remove-members.test.ts`.

  **Lift target at trigger.** Not a code lift. The
  discipline folds into the IMPL-review prompt template:
  a W audit-point asking the reviewer to verify "every
  round-N fix that touches N parallel sites pins
  test-coverage for all N consumers". Could co-locate with
  R-NEW-72 (cross-doc grep after every contract-flipping
  fix-up).

  **Distinction from R-NEW-72.** R-NEW-72 covers prose drift
  introduced by a fix-up across docs. R-v0.5-NEW-9 covers
  test-coverage parity across sibling code sites the fix-up
  touches. Related but distinct surfaces (docs vs tests).

  **Trigger.** Fires at 2nd supporting instance (any future
  Codex IMPL round-N fix-up that touches N parallel fetcher
  sites + the round-N+1 reviewer catches the test-parity
  gap). Likely v0.5-M35 doc-CRUD cluster if the IMPL session
  touches paired mutation fetchers (e.g. create_doc-in-
  workspace + create_doc-in-board sharing wire-response-
  parse shape).

  **Status: 1 supporting instance; LOW priority watch-item;
  process discipline.** Cross-references R-NEW-56 (cross-doc
  grep at IMPL kickoff) + R-NEW-72 (post-fix-up cross-doc
  grep) — round-over-round IMPL-review discipline cadence.

- **R-v0.5-NEW-10 — Sibling-verb module-docstring prose
  audit at IMPL kickoff** (1 supporting instance at v0.5-
  M34 IMPL round-3 P3-1; LOW priority watch-item — process
  discipline, NOT a code lift; folds into the cross-doc
  grep cadence at 2nd supporting instance).

  **Pattern.** When two sibling verbs share module-docstring
  prose (dry-run shape, envelope shape, error-mapping
  contract, wire-vs-CLI asymmetry note), the prose can
  diverge from the implementation across rounds. M34 IMPL
  round-3 P3-1 caught dry-run docstring prose on
  `team-add-members.ts` + `team-remove-members.ts` that
  said "per supplied user_id" (echoing the workspace add-
  users per-user fan-out cadence) when the team-membership
  wire is a single-shot bulk call. The implementation +
  tests were correct; only the prose drifted.

  **Lift target at trigger.** Not a code lift. The
  discipline extends R-NEW-56 (cross-doc grep at IMPL
  kickoff): in addition to the pre-flight → runtime
  transition prose grep, run a paired-verb prose grep
  comparing the dry-run / envelope / asymmetry sections of
  sibling-verb module docstrings to flag divergence
  pre-Codex-review.

  Example shape:

  ```bash
  grep -A 8 '\*\*Dry-run shape' src/commands/user/team-add-members.ts
  grep -A 8 '\*\*Dry-run shape' src/commands/user/team-remove-members.ts
  diff <(...) <(...)
  ```

  **Distinction from R-v0.5-NEW-5** (paired-verb cli-design
  row consistency at pre-flight). R-v0.5-NEW-5 catches drift
  at the cli-design §4.3 row level BEFORE the first Codex
  round. R-v0.5-NEW-10 catches drift at the module-docstring
  level AT IMPL kickoff — different surface, different
  cadence, both about paired-verb consistency.

  **Distinction from R-NEW-72.** R-NEW-72 covers post-fix-up
  cross-doc grep across all docs. R-v0.5-NEW-10 covers
  AT-IMPL-kickoff sibling-verb module-docstring parity
  specifically — a narrower scope that benefits from a
  paired-verb diff rather than a single-doc grep.

  **Trigger.** Fires at 2nd supporting instance (any future
  IMPL session where sibling-verb module-docstring prose
  drift surfaces at Codex round-N — likely v0.5-M35+ if
  doc-CRUD ships paired verbs like `create_doc_in_workspace`
  / `create_doc_on_column`). At trigger: fold the paired-
  verb prose-diff step into R-NEW-56's IMPL-kickoff
  checklist.

  **Status: 1 supporting instance; LOW priority watch-item;
  process discipline.** Cross-references R-NEW-56 (cross-
  doc grep at IMPL kickoff) + R-v0.5-NEW-5 (paired-verb
  cli-design row consistency at pre-flight) + R-NEW-72
  (post-fix-up cross-doc grep) — paired-verb consistency
  audit cadence across pre-flight + IMPL surfaces.

- **R-v0.5-NEW-11 — Per-fetcher null-payload contract decision
  discipline at pre-flight, derived from probe-description
  return-shape promises** (1 supporting instance at v0.5-M35
  IMPL round-1 P2-1; LOW priority watch-item — process
  discipline, NOT a code lift; graduates to a pre-flight Codex
  template audit-point at 3rd supporting instance).

  **Pattern.** When a milestone cluster ships multiple fetchers
  that all project Monday's opaque `JSON` scalar to the same
  envelope shape, each fetcher's null-payload semantics MUST
  be decided independently based on its OWN probe-description
  return-shape promise — NOT by analogy across the cluster's
  fetchers. M35 pre-flight pinned all 3 opaque-JSON fetchers
  (`renameDoc` / `deleteDoc` / `duplicateDoc`) as `null →
  not_found` by analogy with M14 workspace-delete + M34 team-
  delete cadence. M35 IMPL round 1 surfaced the actual per-
  fetcher asymmetry: `update_doc_name`'s probe description
  carries NO return-shape prose ("Update a document's
  name/title. Changes are applied immediately and visible to
  all users with access to the document.") — null is plausibly
  an empty-success indicator. `delete_doc` description says
  "Returns success status and the deleted document ID";
  `duplicate_doc` description says "Returns the new document's
  ID on success" — both promise non-null payloads on success,
  so null IS a missing-record signal for those two.

  **Decision rule per fetcher.** Read the probe description for
  each opaque-JSON fetcher's return-shape promise:

  - **No return-shape prose** (e.g. `update_doc_name`) → null
    payload is plausibly empty-success → project to success
    envelope. Typed errors for missing record bubble via
    GraphQL `errors[]` → typed `ApiError` upstream.
  - **"Returns ... ID" or "Returns success status"** (e.g.
    `delete_doc`, `duplicate_doc`) → null payload is a wire-
    side missing-record signal → throw `not_found`.

  **Lift target at trigger.** Not a code lift. The discipline
  extends the pre-flight Codex prompt template (alongside W1
  redactor + W2 operationName parity + R-v0.5-NEW-6's
  empirical-probe ↔ zod schema cross-check audit-points).
  Fires at 2nd supporting instance — likely v0.5-M36 doc-block
  CRUD if its fetchers ship opaque-JSON returns OR v0.5.x
  webhook-delivery if it adds JSON-scalar mutation surfaces.
  At trigger: add a W audit-point asking the reviewer to
  enumerate each opaque-JSON fetcher's probe description +
  derive its null-payload semantics from that description, NOT
  by analogy across the cluster.

  **Distinction from R-NEW-73.** R-NEW-73 (assertNonNullArray
  Payload helper) tracks a code-lift candidate for null-payload
  GUARDS — different from R-v0.5-NEW-11 which tracks the
  pre-flight contract DECISION discipline. R-NEW-73 stays
  UNFILED at 6 consumers; R-v0.5-NEW-11 is a separate process
  watch-item.

  **Status: 1 supporting instance; LOW priority watch-item;
  process discipline.** Full M35 IMPL round-1 P2-1 context at
  §12 M35 post-mortem Contract drift findings.

- **R-v0.5-NEW-12 — Pre-IMPL cross-doc grep extension for
  call-ordering claims in module-header prose** (1 supporting
  instance at v0.5-M35 IMPL round-5 P3-1; LOW priority watch-
  item — process discipline, NOT a code lift; extends R-NEW-56
  IMPL-kickoff grep cadence at 2nd supporting instance).

  **Pattern.** Pre-flight module-header prose often describes
  the call structure of stub action bodies ("`parseGlobalFlags`
  runs BEFORE the `c8 ignore start` block-wrap so invalid argv
  surfaces `usage_error`"). At IMPL the c8 block disappears
  and the call structure may collapse (e.g.
  `parseGlobalFlags` collapses into `resolveClient` for verbs
  that don't need a separate gate). The IMPL kickoff cross-
  doc grep (R-NEW-56) catches "stub" / "PRE-FLIGHT STUB" /
  "deferred_to" sites but DOESN'T catch call-ordering claims
  that the IMPL flipped. M35 IMPL round 5 surfaced this when
  Codex flagged 5 module headers (1 in `src/api/documents.ts`
  + 4 in `src/commands/doc/*.ts` non-delete verbs) still
  claiming the pre-flight `parseGlobalFlags`-vs-`resolveClient`
  ordering after the IMPL had collapsed it.

  **Extension to R-NEW-56.** In addition to the pre-flight →
  runtime transition prose grep, run a paired call-ordering
  grep at IMPL kickoff:

  ```bash
  # Identify ordering claims that the IMPL might have flipped:
  grep -E "BEFORE (resolveClient|c8 ignore|loadConfig|enforceDestructiveGate)" src/api/<module>.ts src/commands/<noun>/*.ts
  # Cross-check each claim against the actual IMPL call structure:
  grep -A 30 "\.action(async" src/commands/<noun>/*.ts | grep -E "(parseArgv|parseGlobalFlags|resolveClient|enforceDestructiveGate)"
  ```

  **Trigger.** Fires at 2nd supporting instance (any future
  IMPL session where Codex catches a stub-era call-ordering
  prose claim after the IMPL has collapsed the call structure).
  At trigger: fold the call-ordering grep step into R-NEW-56's
  IMPL-kickoff checklist + extend R-NEW-72's post-fix-up grep
  pattern to include call-ordering language.

  **Distinction from R-NEW-72.** R-NEW-72 catches drift
  introduced BY a contract-flipping fix-up (the fix-up's
  diff fans out new prose drift). R-v0.5-NEW-12 catches
  stub-era prose drift PRESERVED past IMPL feat — the prose
  was correct at pre-flight, the IMPL collapsed the call
  structure, and the module-header prose wasn't updated in
  parallel.

  **Distinction from R-NEW-56.** R-NEW-56 covers pre-flight →
  runtime status prose ("STUB FETCHERS" → "Runtime bodies
  landed"). R-v0.5-NEW-12 covers call-ordering prose
  specifically — a narrower scope that the M35 IMPL kickoff
  grep missed because the "BEFORE X" framing doesn't match
  the "stub" / "PRE-FLIGHT" / "deferred" patterns the
  R-NEW-56 grep was designed for.

  **Status: 1 supporting instance; LOW priority watch-item;
  process discipline.** Full M35 IMPL round-5 P3-1 context at
  §12 M35 post-mortem Contract drift findings.

- **R-v0.5-NEW-13 — Post-IMPL close-docs sweep MUST include
  `docs/output-shapes.md`** (2 supporting instances:
  v0.4-M32 IMPL close + v0.5-M35 IMPL close; MEDIUM priority
  watch-item — process discipline, NOT a code lift;
  graduates to a permanent close-docs checklist item at 3rd
  supporting instance).

  **Pattern.** The close-docs commit for each milestone IMPL
  consistently flips status banners + lesson-derived prose
  across `CLAUDE.md` + `docs/<version>-plan.md` §3 / §9 / §12
  / §22 entries, but **misses `docs/output-shapes.md`** —
  even though that file carries the canonical per-verb live /
  dry-run envelope shapes + pre-flight stub-status banners
  + IMPL-pinned-cassette claims that all need to flip post-
  IMPL.

  **2 supporting instances pinning the trigger:**

  - **M32 IMPL** (v0.4) — Close-docs at `a7d6771` swept
    CLAUDE.md + v0.4-plan §3 / §9 / §14, but `output-
    shapes.md` retained stale "v0.4-M32 pre-flight stub at
    this commit" banners on the new `monday doc list/get`
    sections. The post-M33 IMPL audit `9e56da2` swept those
    forward retroactively ("framing drift across cli-design
    / output-shapes / v0.4-plan §1+§2").
  - **M35 IMPL** (v0.5) — Close-docs at `93d7f28` swept
    CLAUDE.md + v0.5-plan §3 / §9 / §12 / §22, but
    `output-shapes.md` retained 5 stale "v0.5-M35 pre-flight
    stub at this commit" banners + 4 stale "IMPL cassette
    pins" / "IMPL cassette will pin" prose sites + 2
    dry-run shape sites with the wrong (nested
    `location.workspace.*` / `location.board.*`) shape vs
    the actual IMPL flat shape. Caught at the post-M35-IMPL
    refactor-audit sweep `<this commit>`.

  **Lift target at trigger.** Not a code lift. Fold into the
  per-milestone close-docs checklist (whichever CLAUDE.md /
  AGENTS.md surface enumerates close-docs steps):

  ```bash
  # Post-IMPL close-docs checklist additions:
  # 1. Flip M{N} section banners from "pre-flight stub at this
  #    commit" → "Runtime body landed at v<X>-M{N} IMPL
  #    (cluster <SHA1>..<SHA2>)".
  # 2. Flip "IMPL cassette pins" / "IMPL cassette will pin"
  #    prose to match the IMPL's empirical reality (read-only
  #    probe + defensive helper vs live-cassette-validated).
  # 3. Verify dry-run example shapes in output-shapes.md match
  #    the actual IMPL emitDryRun output (snapshot is the
  #    canonical source of truth).
  ```

  **Distinction from R-NEW-56.** R-NEW-56 covers IMPL-kickoff
  cross-doc grep for pre-flight → runtime status flip prose
  (catches drift BEFORE feat commit). R-v0.5-NEW-13 covers
  the post-IMPL close-docs SCOPE problem — even when R-NEW-56
  / R-NEW-72 grep correctly catch drift in the M35-touched
  files, output-shapes.md is consistently missed because the
  close-docs commit's file-list focuses on CLAUDE.md +
  v<X>-plan.md.

  **Distinction from R-NEW-72.** R-NEW-72 covers post-fix-up
  cross-doc grep AFTER every contract-flipping fix-up. R-v0.5-
  NEW-13 covers the systematic file-list gap in the close-docs
  commit itself — a different cadence.

  **Trigger.** Fires at 3rd supporting instance — if the v0.5-
  M36 (or whichever next IMPL milestone) close-docs ALSO
  misses `output-shapes.md`, graduate to a permanent
  `CLAUDE.md "Workflow rules"` entry under the "Auto-document"
  rule + extend the close-docs commit-message convention to
  enumerate `output-shapes.md` in the file-list. Cross-
  references R-NEW-56 (IMPL-kickoff grep) + R-NEW-72 (post-
  fix-up grep).

- **R-v0.5-NEW-14 — Candidate-selection session MUST flip the
  v<X>-plan.md §3 next-candidate planning entry in parallel
  with the CLAUDE.md "Next session" block** (1 supporting
  instance: post-M35-IMPL candidate-selection session
  `175d129`; LOW priority watch-item — process discipline,
  NOT a code lift; graduates to a sub-bullet under the
  R-NEW-75 "Workflow rules" entry at 3rd supporting instance).

  **Pattern.** The graduated R-NEW-75 5-dimension candidate-
  selection framework specifies the close-out as flipping the
  CLAUDE.md "Next session" block + committing
  `docs(<m-n>-prep): annotate Next session block — M<N> =
  <candidate>`. But the v<X>-plan.md §3 entry for the
  picked candidate carries parallel "(TBD — opens after M{N-1}
  closes)" + "Status: probe complete, sequencing TBD" prose
  that becomes stale the moment candidate-selection closes —
  and the R-NEW-75 graduated prose doesn't enumerate that
  flip as part of the session shape.

  **1 supporting instance pinning the trigger:**

  - **Post-M35-IMPL candidate-selection session** (`175d129`)
    — CLAUDE.md "Next session" block flipped from
    candidate-enumeration to "Candidate-selection closed:
    M36 = doc-block CRUD" + "Next session — M36 doc-block
    CRUD pre-flight" in one commit. The v0.5-plan.md §3 M36
    entry retained 3 stale sites: heading `### M36 — doc-
    block CRUD (TBD — opens after M35 closes)` + status
    `**Status: probe complete, sequencing TBD.**` + §3
    placeholder block "(Filled in at M36 pre-flight kickoff
    session, after M35 closes.)" + 1 stale "Out of M35 scope"
    deferral language ("M36 candidate-selection runs post-M35
    close.") — all 4 sites flipped at the post-candidate-
    selection refactor-audit sweep `<this commit>`.

  **Lift target at trigger.** Not a code lift. Fold into the
  R-NEW-75 graduated "Workflow rules" entry as a sub-bullet
  on the close-out step (currently step 6: "annotate
  `CLAUDE.md` "Next session" block + commit"). Extend to:
  "annotate `CLAUDE.md` "Next session" block + parallel-flip
  the v<X>-plan.md §3 entry for the picked candidate (heading
  TBD-marker + status banner + close-of-§3 placeholder + any
  "Out of M{N-1} scope" deferral language pointing at the
  candidate) + commit".

  **Distinction from R-v0.5-NEW-13.** R-v0.5-NEW-13 covers
  the post-IMPL close-docs file-list gap (output-shapes.md
  missed). R-v0.5-NEW-14 covers the post-candidate-selection
  file-list gap (v<X>-plan.md §3 next-candidate entry
  missed). Sister rules; both extend the R-NEW-75 graduated
  cadence.

  **Distinction from R-NEW-56 + R-NEW-72.** R-NEW-56 covers
  IMPL-kickoff cross-doc grep; R-NEW-72 covers post-fix-up
  grep. R-v0.5-NEW-14 covers the candidate-selection
  close-out — a different cadence + a different file-set
  (v<X>-plan.md §3 entry, not module-header prose).

  **Trigger.** Fires at 3rd supporting instance — if the
  M37-pick candidate-selection session AND a v0.5.x
  cleanup-bundle pick BOTH miss the §3 next-candidate flip,
  graduate. Cross-references R-NEW-75 (graduated
  candidate-selection framework) + R-v0.5-NEW-13 (sister
  rule for post-IMPL close-docs).

  **Status: 2 supporting instances; MEDIUM priority watch-
  item; process discipline.** Full M35 IMPL audit context at
  the post-M35-IMPL refactor-audit commit.

(Further v0.5-specific R-class candidates accumulate here as
M34+ milestones surface them.)
