# v0.3 implementation plan

> **Status:** plan, not contract. The binding contract is
> [`cli-design.md`](./cli-design.md). This document sequences the
> work needed to ship v0.3 of that contract; it doesn't redefine it.
> If a conflict arises, `cli-design.md` wins and this plan is updated.

## 1. Scope summary

v0.3 is "**Monday Dev + multi-profile** — agents bring their own
identity, narrate their work against sprint/epic/release/task
artefacts, and stop iterating boards by hand to find the things
they own." v0.2 gave agents the full mutating CRUD surface (item
lifecycle, board structure, comment threads); v0.3 layers four
distinct surfaces on top: (1) the slipped tentative-row writer
expansion (`tags` / `board_relation` / `dependency`) plus the
verb-shaped `time_tracking` extension that closes the
column-type roadmap for v0.3-target slots, (2) multi-profile
credentials cache (`monday auth login` + `~/.monday-cli/config.toml`)
plus the diagnostics cluster (`monday status` + `monday usage`)
that probes its state, (3) the cross-board read upgrades
(cross-board `item search`, `board favorites`, `item history`,
`item update --continue-on-error`) that lift the v0.1 single-board
read assumption, and (4) the `monday dev` namespace + outbound
write surface (notifications, webhooks). v0.3 is the first version
where an agent can fully self-onboard: log in, probe its
configuration, discover what's available, plan against a sprint,
and narrate progress without ever touching a Monday UI tab.

**In scope (from `cli-design.md` §13 v0.3 + §5.3 writer-expansion
roadmap):**

| Group | Commands |
|-------|----------|
| **column writers (slipped from v0.2)** | Friendly translators for `tags`, `board_relation`, `dependency` (the v0.2 tentative row that didn't converge in v0.2's window). `--set-raw` accepts these today; M19 lifts them into the friendly translator. |
| **column verbs (`time_tracking`)** | `item time-track start <iid>` / `item time-track stop <iid>` (verb-shaped, distinct from value writes — `time_tracking` is start/stop, not a settable JSON value). |
| **auth + profiles** | `monday auth login` (OAuth flow + credentials cache at `~/.monday-cli/credentials`, mode `0600`); `~/.monday-cli/config.toml` (multi-profile config — per-profile `api_token_env` reference + workspace + timezone overrides; tokens are NEVER stored in the config file per cli-design §7.2); `--profile <name>` global flag (selection order per cli-design §7.2: `--profile` flag > `MONDAY_PROFILE` env > `default_profile` in config > `MONDAY_API_TOKEN` env). |
| **diagnostics** | `monday status` (DNS / TCP / TLS / 401 probe + cache writability + redaction self-test + env-var pickup summary); `monday usage` (rolling 24h API complexity budget remaining). |
| **cross-board reads** | `item search` cross-board (omit `--board`); `board favorites` (current user's starred boards); `item history <iid>` (per-item activity log — new §6 envelope shape). |
| **partial-success bulk** | `item update --continue-on-error` (new §6.4 sub-section — per-item `{ok, error?}` records vs the v0.2 fail-fast bulk path). |
| **`dev` namespace** | `dev sprint/epic/release/task` workflow shortcuts; `dev discover/configure/doctor` (the three-level command depth carve-out from cli-design §5.2). |
| **outbound writes** | `notification send --user <uid> --target <iid|bid> --target-type item|board --text <t>` (single-recipient per cli-design §4.3); `webhook list/create/delete` (board webhooks; CLI never *receives*). |
| **subitem expansion** | Multi-level subitem creation (slipped from v0.2 — Monday's `create_subitem` accepts a parent `subitem`, but the friendly translator only handles single-level today). **Contract-extension item** — not in cli-design §13 v0.3 list today; ships only after a pre-M28 cli-design §13 v0.3 amendment PR adds it (Decision 11 below). If the amendment is rejected, M28 drops the subitem work and ships polish + release prep only. |
| **polish** | Refreshed envelope-snapshot suite for v0.3 surfaces; `output-shapes.md` ToC audit; README quickstart updates; CHANGELOG `0.3.0` entry; version bump. |

**Universal partial-success rule (cli-design §6.1 compliant — same
shape as v0.2).** Multi-target verbs in v0.3 (M25 `item update
--continue-on-error` is the only v0.3 consumer; M27 `notification
send` stays single-recipient per cli-design §4.3) emit one
**success envelope** (`ok: true`) with per-target results in
`data.results: [...]`. Each record carries its own
`{<target_id_field>, ok: true | false, error?}`. The shared
envelope-builder (`buildPartialSuccessMutation`, lifted in v0.2)
takes the field name as a parameter. The envelope's top-level
`error` slot is reserved for whole-call failure; all-failed-but-
each-call-attempted is still `ok: true` and the agent reads
`data.results` to determine outcomes. v0.3 introduces no contract
revision to §6.1.

**Out of scope (deferred per `cli-design.md` §13 "Explicitly
deferred from v0.1's stable contract"):**

- `item watch <iid>` (v0.4 — polling cadence question still open
  per cli-design §14.4).
- Bulk operations with `--concurrency` (v0.4 — probed against
  Monday's per-account concurrency cap).
- Asset upload (`add_file_to_column`, `add_file_to_update`) — the
  `files`-shaped column write path (v0.4).
- Shell completion (bash / zsh / fish) via commander (v0.4).
- `doc list/get` (v0.4 read-only; full docs CRUD deferred further).
- `team` create/manage (v0.4).
- Boolean filter DSL — v0.3 keeps the v0.1 / v0.2 `--where`
  operator allowlist + `--filter-json`. Pencilled at v0.3 stretch
  / v0.4 firm; only ships when fixture-driven adoption metrics
  show concrete demand.
- `monday item recreate-from-archive` — still no misleading
  `restore` command (cli-design §5.4 stands as a permanent
  non-goal-adjacent decision).

**Permanent non-goals (per cli-design §13 — reminded here so plan
readers don't propose them as v0.3 candidates):**

- Hosting webhooks (CLIs can't expose public HTTP — out of model);
  v0.3's `webhook create` is the *registration* shape against
  Monday, not a receive endpoint.
- App framework / installable monday apps (different surface
  area).
- Real-time GraphQL subscriptions (Monday's endpoint doesn't
  support them).
- Telemetry, update-notifier, analytics — ever.
- Forms (Monday's public-submission feature) — same hosted-
  endpoint shape as webhooks.
- Saved queries / aliases — would silently change behavior across
  machines; shell aliases / shell functions are the established
  UNIX answer.
- `monday undo` (replay-based reversal of recent mutations) —
  Monday's state model is authoritative and concurrent; honest
  substitutes are `--dry-run` (v0.1) and `item history` (v0.3 —
  M24 below).

## 2. Pre-flight: what's already in place

Working at HEAD (`main`, post-`6d93f65`). The post-v0.2 → v0.3
cleanup window closed; **2296 tests passing**; coverage 99.05 /
95.47 / 99.51 / 99.18 (statements / branches / functions / lines)
above the 95/95.45/95/95 floor. `monday-cli@0.2.0` published to
npm 2026-05-08.

What v0.3 inherits from M0–M18:

- **Foundations (M0–M2.5)** — runner / signal handling / typed
  errors / redaction / envelope builders / commander wiring /
  transport seam / retry layer / `MondayClient` / `account`
  commands all live and stable. v0.3 inherits unchanged.
- **Read surface (M3–M4)** — `workspace`, `board`, `user`,
  `update`, `item` reads (incl. filter DSL, cursor pagination,
  NDJSON streaming, projection layer). v0.3's M23 cross-board
  search + M24 history + M27 webhook list lean on these for
  resolution: `--board <bid>` lookup, column resolution, `me`
  token, board metadata cache.
- **Write surface (M5a–M5b + M8–M18)** — every mutation verb
  v0.2 ships, plus the §5.3 writable allowlist (10 friendly
  types: `text`/`long_text`/`numbers`/`status`/`dropdown`/`date`/
  `people` from M5a + `link`/`email`/`phone` from M8) and the
  `--set-raw` escape hatch. **M19's tentative-row work plugs
  into this** — three new translator slots in the same
  dispatcher, one new account-tag directory module, one
  per-board-relation-allowed-boards check.
- **R-class generalised helpers** — `assertResponseFieldPresent`
  (R42, accepts `details: Record<string, unknown>` map +
  `nullHandling: 'caller_handles' | 'throw_not_found'`
  discriminant), `withBoardInvalidation*` (R46, single-leg + fan-
  out wrappers), `findBoardChildOrThrow` (R51), `startNdjsonStream`
  (R52), `buildStreamingTrailerMeta` (R53), per-noun projection
  helpers `projectMutationItem` / `projectMutationUpdate` /
  `projectMutationBoard` / `projectMutationColumn` /
  `projectMutationGroup` (R28 / R37 / R43 / R45 / R48).
  v0.3 mutation surfaces consume these on day one.
- **Cache invalidation discipline (cli-design §8)** —
  `invalidateBoard(boardId)` plus the §8 leg-count split
  (single-leg vs fan-out). v0.3's diagnostics cluster (M22)
  reports cache state through this. **M27 webhook mutations
  are live-only** — webhooks aren't part of §8's cache scope
  (which covers boards / users / schema only) and adding them
  is out-of-scope for v0.3 per the §6 risk register entry.
- **Partial-success envelope** (cli-design §6.1 with v0.2's
  `data.results: [...]` per-target shape) — M25 `item update
  --continue-on-error` is the first v0.3 consumer.
- **Streaming helpers (R52 + R53)** —
  `startNdjsonStream` / `walkPages.onItem` / `paginate.onItem`
  with proper `'drain'` backpressure. M23 cross-board `item
  search` is the first v0.3 consumer (one walker per board, fan
  out across favorites or workspace scope).
- **Dry-run engine** (M5a, cli-design §9.2) — v0.3's M19
  tentative-row friendly translators emit dry-run shapes
  through this engine on day one.
- **Output-shapes contract** (`docs/output-shapes.md`) —
  92 envelope snapshots after M18; v0.3's M28 release prep adds
  ~30+ new snapshots covering cross-board search, history,
  diagnostics, dev verbs, notifications, webhooks.
- **Test infrastructure** — fixture transport, fixture server
  (E2E), redaction-hardening suite, agent-flow E2E, NDJSON
  streaming integration tests. **2296 tests passing**.
- **Tooling** — ESM, NodeNext, strictest TS, ESLint
  `strictTypeChecked + stylisticTypeChecked`, vitest 4. CI runs
  typecheck + lint + test:coverage + build smoke on Node 22 and
  24. Two-AI review (Codex `gpt-5.5`, read-only) per design
  change and per implementation milestone.

What's **missing** that v0.3 needs (each filled by the named
milestone):

- An **account-tag directory** module (M19) mirroring the
  `userByEmail` user-directory pattern in `src/api/resolvers.ts`
  (M5a — there is no standalone `user-directory.ts` module; the
  user-directory cache / lookup / refresh-on-miss machinery lives
  inside `resolvers.ts` lines 192–402). Monday's tags are account-
  scoped (`account_tags` returns the directory; `tags` column
  values reference tag IDs). The friendly translator needs to
  resolve `<tag1>,<tag2>` to tag IDs at boundary; misses surface
  the `tag_not_found` error code (registered pre-M19 at `4c652d5`
  per §8 decision 1).
- A **linked-board enumeration + complexity-budget** path (M19)
  for `board_relation` and `dependency`. Monday's wire shape is
  `{ item_ids: [N1, N2] }` referencing items in the column's
  `settings.boardIds ?? [settings.boardId]`. The friendly
  translator validates input items belong to those allowed
  boards before constructing the wire payload — a per-call
  Monday lookup with complexity-budget interaction (see §6
  risk register).
- A **multi-profile config loader** (M21) — v0.1/v0.2 read
  `MONDAY_API_TOKEN` from env or `.env`. v0.3 adds
  `~/.monday-cli/config.toml` with `[profiles.<name>]` blocks +
  `--profile <name>` global flag. **Selection order is fixed by
  cli-design §7.2:** `--profile` flag > `MONDAY_PROFILE` env >
  `default_profile` in config > `MONDAY_API_TOKEN` env (falls
  back to v1 mode if no config file exists). **Tokens are never
  stored in the config file** — profile entries reference an env
  var name via `api_token_env = "MONDAY_API_TOKEN_<NAME>"` per
  cli-design §7.2.
- A **credentials cache primitive** (M21) —
  `~/.monday-cli/credentials` (mode `0600`) for `monday auth
  login`'s OAuth flow result per cli-design §7.3. Format
  question is open (cli-design §14.5); default leans toward our
  own JSON.
- A **self-test harness** (M22) for the diagnostics cluster —
  mockable transport seam for `monday status`'s probes (so
  status itself is testable without real network), plus a
  rolling-24h-budget query path for `monday usage`.
- A **per-item event projection layer** (M24) — `item history`
  surfaces Monday's `activity_logs` entries as event objects with
  `created_at`, `actor_id`, `kind`, `before` / `after`. Distinct
  from existing item / update projections; a new §6 envelope
  shape (cli-design §13 v0.3 entry).
- A **workflow-shortcut layer** (M26) over the v0.1/v0.2
  CRUD surface — `dev sprint`, `dev epic`, `dev release`,
  `dev task` are convention-driven helpers (per cli-design
  §2.7 — Monday Dev is convention, not API). They translate
  workflow vocabulary to standard board / item operations.
- An **outbound notification + webhook surface** (M27) —
  `notification send` (Monday's `create_notification` mutation;
  per-user-id targeting), `webhook list/create/delete`
  (`webhooks` query + `create_webhook` / `delete_webhook`
  mutations). New nouns. **Webhooks are live-only for v0.3** —
  not part of cli-design §8's cache scope (boards / users /
  schema only). If webhook caching becomes load-bearing later,
  it lands as a v0.3.x or v0.4 §8 contract-extension PR.
- A **multi-level subitem creation** path (M28) — Monday allows
  `create_subitem` with a parent that is itself a subitem; the
  current friendly translator caps at single-level. Shipping
  multi-level requires hierarchy validation (depth limit per
  Monday's policy — typically 3 levels).

## 3. Milestone sequencing

Ten milestones (M19–M28). Each milestone is a coherent commit
cluster (3–10 commits) ending on a green tree with the listed
exit criteria met. Earlier milestones unblock later ones; do not
start M(N) until M(N-1)'s exit criteria are satisfied. Each
milestone updates `docs/output-shapes.md` for any new command
shapes it lands and adds envelope snapshots for any new emitted
shape.

Order at a glance: **writer-expansion close → time_tracking →
auth foundations → diagnostics → cross-board reads → history →
partial-success bulk → dev namespace → outbound writes → subitem
expansion + release prep**.

```
       M19                                — writer expansion close (slipped tentative row)
       M20                                — time_tracking start/stop verbs
       M21 ─ M22                          — auth foundations (login + profiles) → diagnostics (status + usage)
       M23 ─ M24 ─ M25                    — cross-board reads (search + favorites) → history → partial-success bulk
       M26                                — dev namespace (workflow shortcuts)
       M27                                — outbound writes (notifications + webhooks)
       M28                                — subitem expansion + polish + 0.3.0 release prep
```

### M19 — Writer expansion close (slipped tentative row)

**The opening v0.3 milestone.** Closes the column-type
roadmap's v0.2 carryover: the three tentative-row types
(`tags`, `board_relation`, `dependency`) that slipped from v0.2
at M18 close (per cli-design §5.3 writer-expansion roadmap +
§13 v0.3 entry) get their friendly translators. Mirrors M8's
foundational role for v0.2 — the slipped row was always
intended as the first thing v0.3 picked up, since later v0.3
milestones don't depend on it. The runtime
`unsupported_column_type` errors for these three types
already carry `deferred_to: "v0.3"` (per the M18 round-2 P2
fix); landing the friendly translators flips them to
writable.

**Status: M19 closed.** Six implementation commits + one mid-
milestone-pause docs commit shipped across two sessions; the
v0.2 writer-expansion roadmap's tentative row (`tags` /
`board_relation` / `dependency`) graduated into the friendly
`--set` translator allowlist; `monday account tags` read verb
shipped (closes the §6.5 `tag_not_found.details.hint` forward-
reference). The full M19 post-mortem lands in §11 below.

**Shipped commits (in order):**
- `d6c8651 refactor(m19-fold)` — Commit 1 (last session):
  collapse `unsupportedColumnTypeError` into 5-way
  category-table (M19-fold of v0.2-plan §22 / v0.3-plan §22
  quality refactor; behaviour-preserving).
- `19801f8 feat(m19) tags translator` — Commit 2 (last
  session): tags friendly translator + `tag-directory.ts`
  body + `accountTags` cache key + cross-cutting widenings
  shared by Commits 3–4. Pre-flight contract amendments to
  `d822982`: `ResolveTagsResult` adds `cacheAgeSeconds`,
  `LoadAccountTagsResult` adds `complexity`, cache key
  named `accountTags` (camelCase) instead of placeholder
  `account-tags`.
- `b9d9213 docs(m19-mid)` — Commits 1+2 mid-milestone
  Codex review (last session): P1-clean returned with 3 P2
  + 1 P3 findings, all addressed inline as part of the
  pause-and-review cycle.
- `a569590 feat(m19)` — Commit 3 (this session):
  `board_relation` friendly translator +
  `validateBoardRelationItems` runtime body (replacing the
  pre-flight stub) + `parseRelationItemIds` shared parser
  (Codex round-1 P2-11). Success branch widened to
  `{ ok: true, items: ValidatedRelationItem[] }` per Codex
  round-2 P1-3 so the dry-run echo can render per-item
  home boards alongside the wire payload. Cross-cutting
  widenings: `RelationResolution` echo slot,
  `RelationResolutionContext.validateItems` callback (shared
  with `dependency`), `assertEchoExclusivity` N=4
  widening, `buildResolutionContexts` `relationResolution`
  context, `column-create.ts` settings-maps growth,
  `describe.ts exampleSetForColumn` widening.
- `53a76ea feat(m19)` — Commit 4 (this session):
  `dependency` sibling translator. Identical wire shape +
  validator path; reads `column.settings.dependencyBoards`
  instead of `boardIds`. `WRITABLE_COLUMN_TYPES` reaches 13;
  `V0_2_WRITER_EXPANSION_TYPES` becomes empty `[]`. The
  `v0_2_writer_expansion` category branch in the
  `unsupportedColumnTypeError` classifier is now unreachable
  through the runtime; retained as documented dead code per
  the M8-era stability comment for future tentative-row
  revival.
- `969dc7e feat(m19)` — Commit 5 (this session): `monday
  account tags` read verb. M19-fold mandatory per Codex
  round-1 P2-9 — closes the §6.5
  `tag_not_found.details.hint` forward-reference. Output
  schema `{tags: [{id, name}], total}`. Cache-aware via the
  existing `loadAccountTags` helper.
- `7707085 docs(m19)` — Commit 6 (this session): the M19
  close itself — bundles the post-Commit-5 Codex
  implementation review feedback (2 P1 + 3 P2 + 1 P3
  findings, all addressed inline) with the close docs
  sweep (CLAUDE.md status flip, this v0.3-plan §3 +
  §11 update, cli-design §4.3 + §8 entries, README
  writer-allowlist update + v0.3 roadmap rewrite,
  output-shapes ToC + `account tags` entry, api-reference
  M19 row + tentative-mention removal, architecture.md
  raw-writable comment refresh). Combined to one commit
  because the Codex fixes touch the same docs the close
  sweep touches; splitting would have created two near-
  identical doc-sweep commits.

**Why this goes first.** Three reasons: (1) Continuity —
v0.2's writer-expansion narrative finishes here; agents
reading the changelog see "v0.2 firm row → v0.3 tentative
row" as one connected story. (2) Pre-trodden ground — the
M5a translator dispatcher + the M8 `--set-raw` resolution
path + the R-class generalised helpers (R42's
`assertResponseFieldPresent`, R28's `projectMutationItem`)
all give M19 day-one infrastructure. The risk vector is
the per-type design clearance, not the implementation
shape. (3) No dependency on later v0.3 surfaces — M19 can
land without auth profiles (M21), without the diagnostics
cluster (M22), without `dev` namespace (M26). Any other
v0.3 milestone going first would either depend on an open
design question (auth caching format, complexity-budget
interaction with cross-board search) or strand the v0.2
carryover indefinitely.

**Pre-flight design clearance** (must close before any feat
commit lands; mirrors M16's seven-round pre-flight cadence
that pinned 22 contract bugs before code shipped):

1. **`tags` translator shape.** Input form: `<tag1>,<tag2>`
   (comma-split, trimmed). Output wire shape:
   `{ tag_ids: [N1, N2] }`. Resolution: per-account tag
   directory lookup. New `src/api/tag-directory.ts`
   module mirrors the `userByEmail` user-directory
   pattern in `src/api/resolvers.ts` (M5a precedent —
   account-scoped lookup, cache-aware via §8
   invalidation, exposes
   `resolveTags({ client, input, env?, noCache? }):
   Promise<{ ids, misses, source }>` and
   `loadAccountTags({ client, env?, noCache? }):
   Promise<{ tags, source, cacheAgeSeconds }>` — the
   M19 pre-flight contract diff at `d822982` pinned
   the surface as object-form inputs with a required
   `MondayClient`, mirroring `UserByEmailInputs`
   verbatim). Cache key shape:
   `{ kind: 'account-tags' }` (single-account scope; no
   per-board fan-out). Misses surface as a single
   `tag_not_found` error code with `details: { tags:
   <misses[]>, hint: 'Run \`monday account tags\` to
   list available tags.' }` — array shape per §8
   Decision 1 (multi-miss `--set tags=foo,bar,baz`
   surfaces all missing tags in one envelope, NOT one
   error per miss). The hint hard-pins `monday account
   tags` as a v0.3 read-verb deliverable per the
   §6.5 PR's forward-looking commitment (cli-design.md
   §6.5 entry landed at `4c652d5`).
2. **`board_relation` translator shape.** Input form:
   `<iid1>,<iid2>` (comma-split item IDs). Output wire
   shape: `{ item_ids: [N1, N2] }`. Resolution: per-board
   `column.settings.boardIds ?? [column.settings.boardId]`
   list (already in board metadata via M3); validate that
   each input item ID belongs to one of those allowed
   boards. Mismatched item IDs surface as
   `usage_error` with `details: { item_id: <id>,
   allowed_boards: [...], hint: 'item belongs to board X
   but column accepts only [...]' }`. **Complexity-budget
   interaction:** validating each input item via
   `items(ids: [N])` query would burn complexity per item;
   batch via `items(ids: [N1, N2, ...])` to amortise.
   Decision: batch with a hard cap of 25 items per
   `--set` call (matching Monday's documented
   relation-column item cap); >25 surfaces `usage_error`
   pre-network.
3. **`dependency` translator shape.** Identical wire shape
   to `board_relation` (`{ item_ids: [N1, N2] }`); the
   distinction is that Monday's `dependency` column has
   its own settings and its own per-call quirks. Validate
   against `column.settings.dependencyBoards ?? [...]`
   (Monday's separate `dependency` settings field).
   Otherwise treat as a sibling of `board_relation` —
   same resolution path, same complexity-budget
   interaction, same per-call cap.
4. **Clear payloads** (cli-design §5.3 clear table). All
   three types clear to `{}` via `change_column_value`
   (matching v0.2's expansion of the per-type clear
   table; see cli-design §5.3 "Clearing column values").
   Bulk `item clear --where ... <col>` (already shipped
   in M12) inherits this on day one.
5. **Dry-run shape.** Per cli-design §5.3 dry-run example
   block (lines 2416–2434) — `data: null`,
   `meta.dry_run: true`, `planned_changes: [...]` array.
   Each tentative-row type emits `resolved_ids` (column
   ID resolution) + `diff` (`{from: previous, to: new}`)
   + `details` (resolution-source hint: tag-directory
   match for `tags`; allowed-boards-validated for
   `board_relation` / `dependency`).
6. **`example_set` per writable column** (cli-design §3.2
   `board describe`). All three new types need
   `example_set` strings — `"tag1,tag2"` for `tags`,
   `"<iid1>,<iid2>"` for `board_relation` /
   `dependency`. Update `commands/board/describe.ts`
   `example_set` builder to cover the new types so
   `board describe` is self-sufficient for agents
   constructing M19-class `--set` calls.

**Deliverables**

- `src/api/column-values.ts` — three new tentative-row
  translator slots:
  - `tags` — `<tag1>,<tag2>` (comma-split, trimmed,
    deduplicated) → `{ tag_ids: [N1, N2] }` after
    `resolveTags()` lookup. Empty input rejected with
    `usage_error`.
  - `board_relation` — `<iid1>,<iid2>` (comma-split item
    IDs, trimmed) → `{ item_ids: [N1, N2] }` after the
    allowed-boards check. Empty input clears (consistent
    with v0.2 expansion clear semantics: `{}` via
    `change_column_value`).
  - `dependency` — same shape as `board_relation` against
    Monday's `dependency` settings field.
- `src/api/tag-directory.ts` — new module exporting
  `resolveTags({ client, input, env?, noCache? }):
  Promise<ResolveTagsResult>` and
  `loadAccountTags({ client, env?, noCache? }):
  Promise<LoadAccountTagsResult>` (object-form inputs
  with a required `MondayClient` per the M19 pre-flight
  contract diff at `d822982`).
  `ResolveTagsResult = { ids: readonly number[],
  misses: readonly string[], source: 'cache' | 'live'
  | 'mixed', cacheAgeSeconds: number | null }`
  (Commit 2 amendment to the pre-flight diff —
  `cacheAgeSeconds` added so the translator-source
  aggregation pathway can populate
  `meta.cache_age_seconds` per §6.1; `cacheHitAge`
  preserved for `'mixed'` source to surface worst-case
  staleness alongside the cache + live merge).
  `LoadAccountTagsResult = { tags: readonly AccountTag
  [], source: 'cache' | 'live', cacheAgeSeconds:
  number | null, complexity: Complexity | null }`
  (Commit 2 amendment — `complexity` slot mirrors
  `BoardMetadataLoadResult` so `monday account tags
  --verbose` can report budget like other cache-backed
  reads). Mirrors the `userByEmail` user-directory
  pattern in `src/api/resolvers.ts` (the existing
  precedent — there is no standalone
  `user-directory.ts` module; the user directory cache
  / lookup / refresh-on-miss machinery lives inside
  `resolvers.ts` lines 192–402). New cache-key kind
  `{ kind: 'accountTags' }` (camelCase TS literal,
  matching `schemaVersion`'s convention; on-disk path
  `account_tags/index.json` snake-case mirrors the dir
  name; `CacheEntryInfo.kind: 'account_tags'` snake-
  case matches the dir name + the `cache list`
  per-kind label — Commit 2 amendment to the pre-flight
  diff which used kebab-case `'account-tags'` as a
  placeholder).
  **Misses surface as a single `tag_not_found` error
  with `details: { tags: misses, hint: <string> }`** —
  array shape (NOT one error per miss) so a multi-miss
  `--set tags=foo,bar,baz` surfaces all three missing
  tags in one envelope (per Decision 1 below; cli-
  design §6.5 entry landed at `4c652d5`).
- `src/api/board-relation-validation.ts` — new helper
  `validateBoardRelationItems({ client, itemIds,
  allowedBoards, context, env?, noCache? })` that
  batches a single live `items(ids: [...])` query and
  returns `{ ok: true } | { ok: false, mismatches:
  readonly BoardRelationMismatch[] }`. The M19 pre-flight
  contract diff at `d822982` pinned the
  surface — `MondayClient` is required (no cache
  fallback since item board membership can change
  between calls), and the per-call cap
  (`BOARD_RELATION_MAX_ITEMS = 25` per Monday's
  documented relation-column item ceiling) lives
  alongside the validator. Used by both
  `board_relation` and `dependency` translator slots.
- `src/api/column-types.ts` — `WRITABLE_COLUMN_TYPES`
  grows from 10 to 13. Update `unsupportedColumnTypeError`
  to drop `tags`, `board_relation`, `dependency` from the
  `V0_2_WRITER_EXPANSION_TYPES` set (the runtime
  constant name retains its M8-era spelling per the
  stability comment at `column-types.ts` lines 209–211 —
  renaming would churn every consumer with no wire-shape
  change; the M18 round-2 P2 fix already documents that
  the category-name `'v0_2_writer_expansion'` covers the
  v0.3-deferred tentative row). Those errors will no
  longer fire — the friendly translator handles them
  now. Tentative-row reclassification: the `deferred_to:
  "v0.3"` value-write row in `unsupportedColumnTypeError`'s
  category table loses its three members. **`time_tracking`
  stays in a v0.3 verb-shaped category** (per cli-design §5.3
  + §13 v0.3 — verb-shaped extension, NOT v0.4 / files-
  shaped) — its `unsupported_column_type` error keeps
  `deferred_to: "v0.3"` and grows a `hint:
  'time_tracking is verb-shaped; use monday item time-
  track start/stop'`. M20 lands the verb surface and the
  hint becomes self-fulfilling; M19 pins the verb-shaped
  category boundary in the meantime so `time_tracking`
  never accidentally enters the v0.4 / files-shaped
  partition.
- `src/utils/errors.ts` — `tag_not_found` widens
  `ERROR_CODES` (28 total post-M19; cli-design §6.5
  entry landed at `4c652d5`). Pre-flight contract diff
  lands the union widening + corresponding entries in
  `CODE_RETRYABLE_DEFAULT` / `CODE_TYPICAL_HTTP_STATUS`
  / `exitCodeForError` switch ahead of any throw site,
  so M19 implementation feat commits land throws into
  a stable typed surface.
- **`monday account tags` read verb** — required by
  the §6.5 `tag_not_found.details.hint` forward-
  reference. Lands inside M19 implementation OR as a
  fast-follow v0.3.x sidecar; the call is made at M19
  implementation kickoff. NOT a contract change once
  it ships (the verb just resolves the hint's existing
  commitment); just a deliverable scope question. If
  the call is "M19-fold", it ships alongside the
  `tags` translator + `tag-directory.ts`; if it's
  "v0.3.x sidecar", M19 ships without it and a v0.3.x
  patch lands the verb when the cli-design §4.3
  `account` namespace gets its second verb (currently
  only `whoami` lives there).
- `src/commands/item/set.ts` + `src/commands/item/
  update.ts` — wire the new translator slots through the
  existing dispatcher. Bulk `item update --where` and
  bulk `item clear --where ... <col>` inherit
  automatically (the dispatcher is the seam; no per-
  command edits needed).
- `src/commands/item/create.ts` — `create_item`'s
  `column_values` accepts the new types. Existing test
  cassettes need extension to cover M19-class
  create-with-tags / create-with-relation cases.
- `src/commands/board/describe.ts` — `example_set`
  builder gets the three new types. Snapshot refresh
  follows.
- Output-shapes.md — three new entries (tags-set / tags-
  clear, board_relation-set / -clear, dependency-set /
  -clear). ~6 new envelope snapshots.

**Exit criteria**

- All three friendly translators ship with full per-type
  unit + integration test coverage (happy path + every
  error path + dry-run path + bulk path).
- `WRITABLE_COLUMN_TYPES` is 13.
- `tag_not_found` registered in cli-design §6.5 (planning
  doc edit lands as a separate pre-M19 contract-change
  PR with its own Codex review per CLAUDE.md workflow
  rules — NOT in M19's feat commits).
- Coverage holds at or above 95.45% branches; M19 is a
  good candidate for the focused coverage-push that
  recovers toward 95.5% (see §7 release exit checklist).
- 2296 → ~2330 tests passing (M5a expanded ~30 tests per
  new translator slot; expect similar magnitude here).
- Codex pre-flight pass before first feat commit
  (mirrors M16's `c0efab5`); Codex implementation pass
  before M19 close.
- `docs/output-shapes.md` ToC updated; envelope snapshots
  refreshed.
- This plan doc's §11 M19 post-mortem section gets
  written at M19 close (mirrors v0.2-plan §10 M8 post-
  mortem).

**Refactor backlog candidates surfaced** (will resolve at
M19 close per the §22 R-class register):

- Generic `resolveDirectoryReferences` over the `userByEmail`
  user-directory machinery in `src/api/resolvers.ts` +
  `tag-directory.ts` — both share the account-scoped lookup +
  miss-set collection shape; if a third directory-style
  resolver lands later (groups by name? boards by slug?) the
  lift is natural. Tracked at §22 as a v0.3 candidate.
- `validateBoardRelationItems` may share enough shape with
  a future `validateLinkedItems` helper that a generic
  lift surfaces. Tracked at §22.

### M20 — `time_tracking` start/stop verbs

**Verb-shaped column-type extension.** `time_tracking` is
distinct from M19's value-write surface — it's start/stop
semantics, not a settable JSON value. New surface:
`monday item time-track start <iid> [--column <col>]` /
`monday item time-track stop <iid> [--column <col>]`.
Three-level command depth (`item time-track <verb>`) per
cli-design §5.2 carve-out (the `dev` namespace already
established three levels for v0.3).

**Scope size:** S–M (one new noun-verb pair, no new error
codes likely, no cache invalidation fan-out — `time_
tracking` columns don't affect board structure).

**Decisions to close before kickoff:**
- Does `start` while running re-start, no-op, or error?
  Recommended: `usage_error` with `hint: 'time_tracking
  on column X is already running; stop it first or use
  --restart'`. Keeps the contract explicit.
- Does `stop` while not running error or no-op?
  Recommended: error-shaped — `usage_error` with `hint:
  'time_tracking on column X is not running'`. Mirrors
  `start`'s explicit-state convention.
- Idempotency: `start` is non-idempotent (creates a new
  time entry); `stop` is idempotent if already stopped
  (per the no-op-on-stopped decision above; flipping to
  no-op is also defensible).

**Stub deliverables (filled at M20 kickoff):** `src/api/
time-tracking.ts`, `src/commands/item/time-track/
start.ts`, `src/commands/item/time-track/stop.ts`. Codex
pre-flight + implementation review per workflow.

### M21 — `monday auth login` + multi-profile config

**Foundational v0.3 milestone for everything that follows.**
Multi-profile credentials cache via OAuth flow + new
`~/.monday-cli/config.toml` format with per-profile token,
workspace, timezone, and API-version overrides. New
`--profile <name>` global flag.

**Scope size:** L (new boundary surface — config loader,
OAuth flow, credentials cache primitive, per-profile env
resolution, mode `0600` file primitive, redaction
extension). The biggest contract-touching milestone of
v0.3.

**Decisions to close before kickoff** (cli-design §14.5
explicitly flags the auth-caching-format question; the rest
are pinned by cli-design §7.2 / §7.3 already):
- **Auth caching format** (open per §14.5).
  `gh`/`aws`-style credentials helpers vs our own JSON.
  Recommended: our own JSON
  (`~/.monday-cli/credentials`) with mode `0600` for
  simplicity. Profile config (`config.toml`) stays
  human-editable; credentials stay machine-managed.
- **Source priority** (pinned by §7.2). `--profile` flag
  > `MONDAY_PROFILE` env > `default_profile` in config >
  `MONDAY_API_TOKEN` env. M21 implements this exactly.
- **Token storage** (pinned by §7.2). Tokens are NEVER
  stored in `config.toml`. Profile entries reference an
  env var name (`api_token_env = "MONDAY_API_TOKEN_X"`)
  or rely on the credentials cache file. M21 enforces
  this at parse boundary — config-time tokens reject
  with `config_error`.
- **Multi-account vs multi-token-same-account.** Profiles
  are account-scoped — each profile authenticates
  against a (possibly different) Monday account. Tested
  via fixture transport switch.
- **OAuth flow** (pinned by §7.3 — `monday auth login` is
  OAuth + credentials cache, not paste-in). M21 ships
  the full OAuth dance: browser open + localhost
  callback + code exchange + credentials write. **If the
  OAuth dance proves out of session-budget at M21
  kickoff, paste-in becomes a contract-extension PR
  (cli-design §7.3 amendment) BEFORE M21 first feat
  commit, NOT an M21 implementation choice.** No silent
  divergence from §7.3.

**Stub deliverables (filled at M21 kickoff):** `src/config/
profiles.ts` (TOML loader), `src/config/credentials.ts`
(mode-`0600` cache), `src/commands/auth/login.ts`,
`src/commands/auth/logout.ts`, `src/types/global-flags.ts`
(extend with `--profile`), redaction extension to scrub
credentials cache content from outputs.

### M22 — Diagnostics cluster (`monday status` + `monday usage`)

**Self-test surface that probes M21's foundation.**
`monday status` runs DNS / TCP / TLS / 401 probes,
reports cache writability, runs the redaction self-test
against a known-token fixture, summarises env-var pickup
order, and prints the active profile's resolved values.
`monday usage` queries Monday's complexity-budget surface
**(rolling 24h remaining only — per cli-design §13 v0.3
entry, "minimum-viable shape (24h rolling) lands at v0.3;
per-minute rate-limit headroom + concurrency-cap headroom
can grow into the same envelope at v0.4 alongside
`--concurrency`")**. M22 ships the additive-only
envelope shape so v0.4 can extend without breaking.

**Scope size:** M (no new mutations; one new read shape
per command; new probe-mocking infrastructure).

**Decisions to close before kickoff:**
- Should `monday status` probe Monday's API on every run
  or only on `--probe` opt-in? Recommended: probe by
  default (the whole point is "is everything working?");
  `--no-probe` opts out for offline sanity checks.
- `monday usage` — Monday's GraphQL complexity surface is
  per-call (`account.complexity`). **v0.3 ships
  `complexity_remaining_24h` only** (per cli-design §13
  v0.3 entry — per-minute headroom + concurrency-cap
  headroom defer to v0.4 alongside `--concurrency`). The
  envelope is additive-only so v0.4 can extend without a
  breaking change.

**Stub deliverables (filled at M22 kickoff):** `src/api/
probes.ts` (per-probe transport with mockable seam),
`src/api/usage.ts` (rolling 24h query only for v0.3 —
per-minute + concurrency growth deferred to v0.4),
`src/commands/status.ts`, `src/commands/usage.ts`.

### M23 — Cross-board `item search` + `board favorites`

**Paired per cli-design §13 — neither ships alone.**
Cross-board `item search` (omit `--board`) lets agents
"find my open tasks anywhere I have access" without
iterating boards by hand. `board favorites` returns the
current user's starred boards as a natural scoping lever
(`item search --favorites`). Shipping favorites in
isolation buys little agent value; pairing them ships the
full discovery surface.

**Scope size:** M–L (cross-board fan-out via streaming
helpers; complexity-budget design pass; per-board limit
caps).

**Decisions to close before kickoff** (cli-design §13
v0.3 entry flags the budget question):
- **Per-call complexity budget.** Cross-board search
  charges complexity per board scanned. Without a cap,
  an account with 200 boards eats the budget on one
  call. Recommended: default `--max-boards 25` (Monday's
  per-call complexity budget allows ~25 board scans
  comfortably); overridable up to a hard cap per call.
- **Workspace scoping.** `--workspace <wid>` lets the
  agent narrow before fanning out. Recommended: ship
  `--workspace` + `--favorites` as the two scoping
  levers; both default to off.
- **Streaming.** Reuse `startNdjsonStream` (R52) +
  `walkPages.onItem` (M18 hook). Each board's walker is
  a child stream; the parent merges into one NDJSON
  output stream.

**Stub deliverables (filled at M23 kickoff):** `src/api/
cross-board-search.ts` (fan-out walker), `src/commands/
item/search.ts` (extend with `--workspace` /
`--favorites` flags + cross-board path), `src/commands/
board/favorites.ts`. New error code candidate:
`complexity_budget_exhausted` (already in registry as
`complexity_exceeded`; M23 may extend the hint).

### M24 — `item history <iid>`

**New §6 envelope shape.** Per-item activity log surfaces
**status changes, column edits, assignments, AND comments
interleaved chronologically** (per cli-design §13 v0.3
entry — the comments-interleaved requirement is part of
the contract surface). Two source streams merge in the
projector: Monday's `activity_logs` (status / column /
assignment events) + Monday's `updates` (comment thread).
Output event objects carry `created_at`, `actor_id`,
`kind`, `before` / `after`; the `kind` taxonomy includes
an `update_posted` (or similar) variant for comment
events alongside the activity-log kinds. Distinct from
existing item / update projections (item = current state;
update = comment thread; history = chronological merged
event log).

**Scope size:** M–L (one new noun-verb shape; new
two-source merge projection layer; per-source pagination
both consumed; per-event ordering by `created_at`).

**Decisions to close before kickoff:**
- **Pagination shape.** Page-numbered (Monday's native
  for `activity_logs`) vs cursor-style. Recommended:
  page-numbered surfaced via `--page <n> --limit <n>`
  for activity-log slice; updates pagination uses the
  v0.1 update-list cursor walker. The merge projection
  walks both sources to a unified time horizon per
  page.
- **Merge semantics.** Recommended: drain both sources
  to a wall-clock cap (`--since <iso>` / `--until
  <iso>`) and emit merged events ordered by
  `created_at`. Stream output preserves order across
  the merge.
- **`before` / `after` shape.** Monday's `activity_logs`
  entries have a `data` field that's a JSON string. The
  projector parses it and surfaces typed before/after
  pairs per `kind`; unknown kinds surface as raw `data`
  with a warning. The kind taxonomy is a fresh contract
  surface (cli-design §6 needs an extension entry — see
  §8 decision 2). Comment-event `before` / `after` is
  always `null` (comments are append-only events; the
  body lives in the event payload).
- **Streaming.** Reuse `startNdjsonStream` (R52) for
  large per-item histories. Trailer meta carries
  `total_pages` for the activity-log slice and
  `next_cursor` for the updates slice (both surfaced
  per cli-design §6).

**Stub deliverables (filled at M24 kickoff):** `src/api/
item-history-projection.ts`, `src/commands/item/
history.ts`. Output-shapes.md gets a new top-level entry
for the event-object shape.

### M25 — `item update --continue-on-error` (partial-success bulk)

**New §6.4 sub-section.** Today's bulk `item update
--where` fails fast on the first per-item error (matched
items before the failure surface in `details.applied_to`
per §6.5). The flag attempts every matched item
regardless and emits a partial-success envelope with per-
item `{ok, error?}` records — that's a §6.4 sub-section,
not just a flag. Cli-design §13 v0.3 entry.

**Scope size:** S–M (one new flag; one new envelope
sub-section; reuses v0.2's `data.results: [...]` per-
target shape from §1 above).

**Decisions to close before kickoff:**
- **`--continue-on-error` vs `--no-fail-fast`.** Naming
  question. Recommended: `--continue-on-error`
  (positive form; the v0.2 default fails fast).
- **Per-item error envelope.** `{ ok: false, item_id, error:
  { code, message } }` per entry. Inherits v0.2's shared
  `buildPartialSuccessMutation` envelope-builder.
- **Top-level `ok` field.** All-failed-but-each-attempted
  is still `ok: true` per §1's universal partial-success
  rule. The agent reads `data.results` to determine per-
  item outcomes.

**Stub deliverables (filled at M25 kickoff):**
`src/commands/item/update.ts` extension (new flag +
partial-success path), `src/api/partial-success-bulk.ts`
(thin wrapper around the existing fail-fast bulk path).

### M26 — `dev` namespace (workflow shortcuts)

**Three-level command depth carve-out.** `dev sprint` /
`dev epic` / `dev release` / `dev task` are convention-
driven workflow shortcuts on top of the standard board /
item CRUD. Plus `dev discover` / `dev configure` /
`dev doctor` for setup + diagnostics. Cli-design §2.7 +
§5.2 + §11.3 pin Monday Dev as **convention, not API** —
v0.3's `dev` namespace doesn't introduce new Monday
mutations; it translates workflow vocabulary to standard
operations.

**Scope size:** L (broad surface — cli-design §4.3 lists
~13 commands across the namespace: `dev discover` /
`dev configure` / `dev doctor` + `dev sprint
current/list/items` + `dev epic list/items` +
`dev release list` + `dev task list/start/done/block`).
**This exceeds the §3 sequencing intro's 3–10 commit
cluster invariant** (v0.2-plan §3, line 85). M26 is
flagged as an **intentional milestone-size exception**:
the surface is convention-driven (Monday Dev is
convention, not API per cli-design §2.7), so each verb
is a thin wrapper over CRUD plumbing already shipped.
Pre-M26 kickoff requires enumerating the exact commit
slices (mirror v0.2-plan §3 M13's eight-commit cluster
shape — feat per noun-verb pair, then implementation
review fix-ups) to prove the surface fits.
Alternatively, split M26 into M26a (`dev sprint/epic/
release`) + M26b (`dev task` + diagnostics) at the
M25 → M26 transition if pre-flight surfaces a clean
cleavage point.

**Decisions to close before kickoff:**
- **Discovery shape** (`dev discover`). Recommended: scan
  user's accessible boards for ones tagged "Sprint" /
  "Epic" / "Release" by Monday Dev convention; surface
  the inferred mapping. `dev configure` lets the agent
  override the mapping in the active profile's config.
- **Doctor diagnostics** (`dev doctor`). Recommended:
  validate the active profile's `dev` mapping against
  current board shape; surface drift (missing column,
  renamed status, etc.) as warnings.
- **Three-level naming.** `dev sprint` / `dev sprint
  current` / `dev sprint create` — verb on the noun, not
  the namespace. Cli-design §5.2 carve-out covers this.

**Stub deliverables (filled at M26 kickoff):** `src/api/
dev-conventions.ts`, `src/commands/dev/sprint/...`,
`src/commands/dev/epic/...`, `src/commands/dev/release/
...`, `src/commands/dev/task/...`, `src/commands/dev/
discover.ts`, `src/commands/dev/configure.ts`,
`src/commands/dev/doctor.ts`.

### M27 — `notification send` + `webhook list/create/delete`

**Outbound writes — bundled because both are write-only,
low surface.** `notification send` uses Monday's
`create_notification` mutation (per-user-id targeting).
`webhook list/create/delete` uses `webhooks` query +
`create_webhook` / `delete_webhook` mutations (board
webhooks; CLI never *receives* — see §1 permanent non-
goals). **Webhooks are live-only for v0.3** — outside
cli-design §8's cache scope; any caching extension is
a v0.3.x / v0.4 contract-extension PR.

**Scope size:** M (two new nouns, four new verbs total).

**Decisions to close before kickoff:**
- **Notification recipients.** **Single-recipient** per
  cli-design §4.3 (`--user <uid>` singular). Multi-
  recipient is a contract extension (cli-design §4.3 +
  §6 partial-success shape) and is OUT of scope for
  M27; agents who need fan-out call `notification send`
  N times. If multi-recipient becomes load-bearing, it
  ships as a v0.3.x or v0.4 contract-extension PR.
- **Webhook event types.** Monday's documented event
  taxonomy. Recommended: validate against the documented
  list at parse boundary (zod enum); surface `usage_
  error` for unknown events with `hint: 'see Monday
  webhook event docs at ...'`.
- **Cache invalidation.** **Webhook reads are live-only
  for v0.3** — cli-design §8 defines cache entries for
  `boards/<id>.json`, `users/index.json`, and
  `schema/version.json` only; webhooks are not in the
  cache scope and `invalidateBoard()` clears board
  metadata, not webhook listings. Adding webhooks to the
  §8 cache scope would be a contract extension. M27
  ships webhook list/create/delete with `meta.source:
  "live"` always; if cache support proves valuable
  later, it lands as a v0.3.x or v0.4 §8 extension PR.

**Stub deliverables (filled at M27 kickoff):** `src/api/
notifications.ts`, `src/api/webhooks.ts`, `src/commands/
notification/send.ts`, `src/commands/webhook/list.ts`,
`src/commands/webhook/create.ts`, `src/commands/webhook/
delete.ts`.

### M28 — Multi-level subitems + polish + 0.3.0 release prep

**Mirrors M18.** Multi-level subitem creation
**(contract-extension item — not in cli-design §13 v0.3
today; requires Decision 11 amendment PR before kickoff)**
— slipped from v0.2; per v0.2-plan §26 looking-ahead —
Monday allows `create_subitem` with a parent that is
itself a subitem; current friendly translator caps at
single-level. Plus the release-prep cluster: refreshed
envelope-snapshot suite for v0.3 surfaces (~30+ new),
output-shapes.md ToC audit, README quickstart updates,
CHANGELOG `0.3.0` entry, version bump. **If Decision 11
rejects the §13 amendment**, M28 drops the subitem work
and ships polish + release prep only.

**Scope size:** M (subitem hierarchy validation + release
prep; mirrors M18's "no new contract surface" pattern,
but with one bona-fide new feature in subitems).

**Decisions to close before kickoff:**
- **Hierarchy depth limit.** Monday's documented limit
  (typically 3 levels). Recommended: validate at parse
  boundary; surface `usage_error` with `hint: 'monday
  caps subitem nesting at 3 levels'` if exceeded.
- **Snapshot count target.** ~120 snapshots post-v0.3
  (92 at v0.2.0 close + ~30 v0.3 additions).
- **Coverage target.** ≥95.5% branches (recovering the
  R42 dip + clearing the M18-deferred 96% target if
  load-bearing). M19's tentative-row work + M22's
  diagnostics cluster naturally lift coverage as test
  surface grows.

**Stub deliverables (filled at M28 kickoff):**
`src/api/subitem-hierarchy.ts` extension, full release-
prep cluster (snapshot refresh, README, CHANGELOG,
version bump, agent-flow E2E refresh).

## 4. Cross-cutting "definition of done" per command

Every v0.3 command, regardless of milestone, satisfies the
following before its containing milestone closes:

- **One file per subcommand** in `src/commands/...`,
  exported as `CommandModule`, registered in
  `cli/index.ts`.
- **zod schema for argv at the boundary** — never trust
  commander's runtime shape (per `.claude/rules/cli.md`
  "Commander's runtime option shape" trap). All flags
  parse through `globalFlagsRawSchema` + per-command
  schema; commander's coerced types (string for `<n>`,
  string for `<list>`) get coerced/split inside the
  schema, never inside the command body.
- **One Monday GraphQL operation per file** — top-of-
  file comment names the operation(s) the command calls;
  agents grep for this when planning changes.
- **Universal envelope on every success and failure
  path.** Success: `{ok: true, data, meta, warnings}`.
  Failure: `{ok: false, error: {code, message, http_
  status, monday_code, request_id, retryable, retry_
  after_seconds}, meta}`. Agents key off `error.code`,
  never English.
- **Stable error code from the §6.5 registry.** v0.3 may
  extend the registry (M19's `tag_not_found`; M24's
  history-event-kind taxonomy if it lands as new error
  codes). Each extension lands as a separate pre-
  milestone contract-change PR with its own Codex review
  per CLAUDE.md workflow.
- **NO interactive prompts.** Destructive ops without
  `--yes` return `confirmation_required`. M19's
  tentative-row writers inherit this from M5b. M27's
  webhook deletes are destructive — `--yes` required.
- **Exit codes per §7 standard:** 0 success / 1 usage or
  `confirmation_required` / 2 API or network / 3 config
  / 130 SIGINT.
- **Dry-run support** for any destructive or high-impact
  verb. M19's tentative-row writers ship dry-run from
  day one; M25's partial-success bulk ships
  `--dry-run` (per-item planned-changes preview).
- **Cache invalidation eager + correct.** Mutations
  follow cli-design §8. M27's webhook lifecycle is
  live-only — outside §8's cache scope and does not
  extend the §8 contract.
- **Stdin support** for any "operate on this thing" verb
  per cli-design §10.1. M19's tentative-row writers
  inherit the stdin convention from M5b.
- **NDJSON streaming** for any verb with unbounded
  collection output. M23 cross-board search + M24
  history are the v0.3 streaming consumers.
- **`example_set` per writable column** (M19 only).
  `monday board describe` self-sufficient for agents
  constructing M19-class `--set` calls.
- **Tests cover every branch.** Coverage floor 95 / 95.45
  / 95 / 95 (statements / branches / functions / lines)
  per `vitest.config.ts`. M19's focused coverage push
  should target ≥95.5% branches.
- **Envelope snapshots** for every emitted shape (success
  + failure variants).
- **`docs/output-shapes.md`** ToC entry for every new
  command shape.
- **`docs/cli-design.md`** updates for any binding
  decisions (separate pre-milestone PR with Codex
  review).
- **Codex pre-flight review** before first feat commit;
  Codex implementation review before milestone close.

## 5. Testing strategy — the pyramid, made explicit

v0.3 inherits v0.2's pyramid (unit / integration / E2E).
New cassette categories v0.3 introduces:

- **Account-tag directory cassettes** (M19) — `account_
  tags` query responses (happy / empty / many tags /
  paginated). Mirrors `users(...)` cassettes from M5a.
- **Board-relation allowed-boards validation cassettes**
  (M19) — `items(ids: [...])` batch-lookup responses
  with each item's `board_id` field.
- **OAuth-flow + credentials cache fixtures** (M21) —
  full OAuth dance per cli-design §7.3 (browser-open
  intent, localhost callback listener, code exchange,
  credentials write to `~/.monday-cli/credentials` mode
  `0600`). Mock OAuth seam injected via
  `__test_oauth_helper` env var so the browser-open +
  localhost callback steps run as test stubs (returning
  a `tok-fixture-xxxx` token); cache file mode `0600`
  verified via fstat in the test.
- **Multi-profile config fixtures** (M21) — tmp-dir
  `~/.monday-cli/config.toml` with N profiles, each
  switching transport seam to its own fixture cassette.
- **Probe transport fixtures** (M22) — DNS / TCP / TLS
  / 401 each with happy + failure variants. Mock
  transport seam runs probes without real network.
- **Complexity-budget fixtures** (M22) — `account.
  complexity` + `webhooks.complexity` responses with
  near-budget + over-budget variants.
- **Cross-board fan-out cassettes** (M23) — N-board
  fan-out via fixture transport; per-board complexity
  budget.
- **Item history cassettes** (M24) — TWO-source merge
  coverage per cli-design §13 v0.3 ("activity log
  + comments interleaved chronologically"):
  `activity_logs(item_id)` responses with each
  documented `kind` covered; `updates(item_id)` comment-
  thread responses; unknown-kind fallback path; merge-
  order cases where activity events and comment events
  interleave by `created_at` (assert chronological
  emission order across the merge); pagination
  exhaustion on both sources to a unified time horizon.
- **Webhook cassettes** (M27) — `webhooks(board_id)`
  query, `create_webhook` mutation, `delete_webhook`
  mutation. Event-type enum pinned at parse boundary.

**Adversarial test discipline carries over from M0/M2:**
every redaction-affected output path (M21's credentials
cache, M22's `monday status` env-var pickup summary)
must assert that the literal token is absent from every
emitted byte across the suite. The token-leak detection
suite extends with M21's credentials-cache scrubbing.

## 6. Risk register

Known design risks for v0.3, by milestone:

- **M19 `tags` translator — account-tag directory cache
  invalidation.** Tags can be created / deleted via
  Monday's UI between CLI invocations. Stale cache could
  surface `tag_not_found` for a tag that's been re-
  created with a different ID, OR map to a deleted tag's
  ID and produce a wire-shape mismatch. Mitigation:
  conservative TTL (5 min cache lifetime via §8 leg-
  count split), `--refresh-tags` flag for force-refresh,
  surfaced cache-age in `meta.cache_age_seconds`.
- **M19 `board_relation` / `dependency` — complexity-
  budget interaction.** Per-call validation of N input
  items burns complexity. Mitigation: hard cap at 25
  items per `--set` call; documented in cli-design §5.3.
- **M21 OAuth flow — secret in transit.** OAuth dance
  passes a code through HTTP redirects. Mitigation:
  full OAuth dance per cli-design §7.3 ships with a
  randomised localhost callback port + state-parameter
  CSRF check + short-lived code (Monday's OAuth issues
  codes valid for ~10 min); the callback listener
  binds to `127.0.0.1` only (never `0.0.0.0`) and
  validates the `state` parameter before any code
  exchange. If the dance proves out of session-budget,
  paste-in is a separate cli-design §7.3 amendment PR
  before M21 first feat commit, NOT a silent
  implementation choice.
- **M21 credentials-cache scrubbing.** New file path
  (`~/.monday-cli/credentials`) needs to be scrubbed
  from any debug / status / config-show output.
  Mitigation: extend `redact()` to recognise the
  credentials path; redaction self-test (M22's `monday
  status`) asserts the scrubbing.
- **M22 `monday status` — false negatives.** Probe at
  one moment may say "everything works" while a
  transient issue resolves between probe and real call.
  Mitigation: surface probe timestamp in output; agents
  retry on transient.
- **M22 `monday usage` — Monday's complexity surface
  drift.** If Monday extends the complexity surface
  (per-minute, concurrency-cap), `monday usage`'s
  envelope needs to grow without breaking. Mitigation:
  envelope is additive-only; new fields land as v0.3.x
  minor bumps.
- **M23 cross-board fan-out — pagination consistency.**
  Item state can change during a multi-board walk.
  Mitigation: surface `meta.snapshot_drift: true`
  warning if any per-board cursor returns `stale_cursor`
  mid-walk.
- **M24 history — `kind` taxonomy drift.** Monday adds
  new `activity_logs.kind` values over time. Mitigation:
  unknown-kind fallback path surfaces raw `data` with a
  warning; cli-design §6 envelope additive-only.
- **M27 webhook events — endpoint validation.** Monday
  validates webhook URLs server-side. Mitigation:
  surface Monday's wire `validation_failed` mapped to
  `usage_error`; document the required URL shape (HTTPS
  + reachable from Monday's IPs).
- **M28 multi-level subitem hierarchy — depth limit
  drift.** Monday's documented 3-level limit could
  change. Mitigation: validate against documented limit
  at parse boundary; surface Monday's wire validation
  error if Monday's actual limit shifts.

## 7. v0.3 release exit checklist

Mirrors v0.2-plan §7. Each box ticks at M28 close:

- [ ] All 10 milestones M19–M28 closed (each with green
      tree + Codex pre-flight + implementation review +
      §11+ post-mortem).
- [ ] **2296 → ~2500+ tests passing** (rough estimate:
      M19 ~30, M20 ~15, M21 ~50, M22 ~30, M23 ~25, M24
      ~25, M25 ~15, M26 ~50, M27 ~25, M28 ~25).
- [ ] **Coverage at or above 95.5% branches** (recovering
      the R42 dip + clearing the M18-deferred 96% target
      if load-bearing). Statements / functions / lines
      stay at or above current 99.05 / 99.51 / 99.18.
- [ ] **Snapshot count ~120** (92 at v0.2.0 close + ~30
      v0.3 additions).
- [ ] **`output-shapes.md` ToC audited** — every new
      v0.3 command shape entered; existing entries re-
      verified against current envelope output.
- [ ] **README quickstart** includes one M19 example
      (tentative-row friendly write), one M21 example
      (multi-profile login + selection), one M23 example
      (cross-board search), one M26 example (`dev
      sprint current`).
- [ ] **CHANGELOG `0.3.0` entry** lists every new
      surface, every new flag, every new error code,
      every breaking change (none expected — additive
      only).
- [ ] **`package.json` version `0.3.0`**.
- [ ] **`docs/cli-design.md` §13 v0.3** ticked as
      shipped (mirrors the v0.2 entry's "shipped" tag).
- [ ] **CI green** on Node 22 + 24.
- [ ] **Published to npm** (`monday-cli@0.3.0`).
- [ ] **GitHub release tag `v0.3.0`**.
- [ ] **CLAUDE.md status block flipped** to "v0.3.0
      published; v0.4 plan opens next session" pattern
      mirroring the v0.2 → v0.3 flip.
- [ ] **v0.4 plan opens next session** in fresh
      `docs/v0.4-plan.md`.

## 8. Decisions (open questions to close before M19)

The contract-touching decisions below land as separate
pre-milestone PRs with Codex review per CLAUDE.md
workflow. Listed in the milestone they block:

1. **`tag_not_found` registry entry** (M19). **Closed:
   `4c652d5`** — cli-design §6.5 extension PR landed
   ahead of the M19 contract diff per CLAUDE.md
   workflow rules. `details` shape pinned as
   `{ tags: string[], hint: string }` (array form, NOT
   singular `{ tag }`) — `resolveTags()` returns
   `misses: string[]` in one directory pass, so a
   single `--set tags=foo,bar,baz` call surfaces every
   missing tag in one error envelope rather than N
   per-tag errors. **Hint default hard-pinned to
   reference `monday account tags`** (forward-looking
   commitment per this session's decision — the
   `monday raw` fallback wording was rejected for
   agent-UX reasons). The hint pins `monday account
   tags` as a v0.3 read-verb deliverable; whether it
   lands inside M19 implementation or as a v0.3.x
   fast-follow is decided at M19 implementation
   kickoff (NOT a re-opened v0.3-plan question — the
   §6.5 PR's hint wording lets either path resolve
   the commitment without a contract change).
2. **Item-history `kind` taxonomy** (M24). Monday's
   `activity_logs.kind` values are documented but not
   exhaustive. Decision: ship the documented set as a
   zod enum with a fallback `unknown` variant + warning;
   surface unknown kinds as raw `data` per §6 envelope
   additive-only.
3. **Auth caching format** (M21; cli-design §14.5).
   Decision: our own JSON at `~/.monday-cli/credentials`
   (mode `0600`); not `gh`/`aws`-style. Document the
   format in cli-design §7.
4. **Three-level command depth carve-outs** (M20, M26).
   Cli-design §5.2 already permits `dev` namespace at
   three levels. M20's `item time-track <verb>`
   introduces a second carve-out. Decision: extend §5.2
   to permit `<noun> <subnoun> <verb>` for time-tracking
   shape (mirrors `dev` namespace); document the rule
   so future carve-outs aren't ad-hoc.
5. **Cross-board search default cap** (M23). Decision:
   `--max-boards 25` default; overridable up to 100;
   above-cap surfaces `usage_error` with hint.
6. **`--continue-on-error` naming** (M25). Decision:
   `--continue-on-error` (positive form); document in
   cli-design §6.4.
7. **`monday status` probe-by-default** (M22). Decision:
   probe by default; `--no-probe` opts out.
8. **`monday usage` envelope shape** (M22). Decision:
   surface `complexity_remaining_24h` ONLY for v0.3 (per
   cli-design §13 v0.3 entry); envelope additive-only so
   v0.4 can extend with per-minute + concurrency-cap
   fields without breaking.
9. **Webhook event-type validation** (M27). Decision:
   zod enum at parse boundary against Monday's
   documented event list; surface `usage_error` for
   unknown events.
10. **Subitem hierarchy depth limit** (M28). Decision:
    validate against Monday's documented 3-level limit
    at parse boundary; rely on Monday's server-side
    validation as the authoritative cap.
11. **Multi-level subitem creation as v0.3 scope** (M28).
    Currently NOT in cli-design §13 v0.3 list; M28
    treats it as a contract-extension item. Decision:
    pre-M28 cli-design §13 v0.3 amendment PR adds it,
    OR M28 drops the subitem work and ships polish +
    release prep only. Lands as a separate Codex-
    reviewed PR before M28 kickoff.

Decisions NOT closing before M19 (kept open until their
respective milestone kickoff so M19 doesn't drag on
unrelated design questions): 2 (M24), 3 (M21), 4 (M20
+ M26), 5–11. Decision 1 must close before M19 first
feat commit.

## 9. Preconditions

Before M19 starts:

- [x] Decision 1 (`tag_not_found` registry entry) closed
      via cli-design §6.5 extension PR with Codex
      review (`4c652d5`).
- [x] M19 pre-flight contract diff committed (mirrors
      `c0efab5` / `bed75c6` / `14fcd55` cadence). The
      diff lands the tag-directory module signatures +
      the board-relation validation helper signatures +
      `src/utils/errors.ts` ERROR_CODES widening for
      `tag_not_found` WITHOUT implementation
      (`column-values.ts` dispatcher widening lands at
      M19's first feat commit alongside the actual
      translator bodies, to avoid the half-state where
      a type is "writable" but the body throws); Codex
      pre-flight reviewed the diff before any feat
      commit.

Before M20 starts:

- [ ] M19 closed (exit criteria met).
- [ ] Decision 4 (three-level carve-out) closed for
      `item time-track` shape.

Before M21 starts:

- [ ] M20 closed.
- [ ] Decision 3 (auth caching format) closed.
- [ ] OAuth-flow callback design pinned (per cli-design
      §7.3 — randomised `127.0.0.1` port + state-CSRF
      check + short-lived code) with a mock OAuth helper
      env var (`__test_oauth_helper`) for tests.

Before M22 starts:

- [ ] M21 closed (M22 probes M21's auth state).
- [ ] Decisions 7 + 8 closed.

Before M23 starts:

- [ ] M22 closed (M23 leans on M22's complexity-budget
      surface for self-throttling guidance).
- [ ] Decision 5 (cross-board cap) closed.

Before M24 starts:

- [ ] M23 closed.
- [ ] Decision 2 (history kind taxonomy) closed.

Before M25 starts:

- [ ] M24 closed.
- [ ] Decision 6 (--continue-on-error naming) closed.

Before M26 starts:

- [ ] M25 closed.
- [ ] Decision 4 (three-level carve-out) extended to
      `dev` namespace verbs (already permitted in
      cli-design §5.2; just verify no new shape
      questions surfaced).

Before M27 starts:

- [ ] M26 closed.
- [ ] Decision 9 (webhook event-type validation) closed.

Before M28 starts:

- [ ] M27 closed.
- [ ] Decision 10 (subitem hierarchy depth limit)
      closed.
- [ ] Decision 11 (multi-level subitem creation as v0.3
      scope) closed via cli-design §13 v0.3 amendment PR
      OR resolved as "drop from M28; M28 ships polish +
      release prep only".

## 10. Post-mortem template

Each milestone's post-mortem (§11+ below) follows the
v0.2-plan §10–§26 template:

- **Codex round summary** — number of pre-flight rounds
  + implementation rounds; per-round finding count by
  priority (P1 / P2 / P3); links to the .review-output.md
  artifacts (gitignored locally).
- **Contract drift findings** — any cli-design §X
  decision that drifted during implementation; the fix
  + the lesson learned.
- **Exit ratchets** — coverage floor changes (raised
  only; never lowered without documented rationale per
  the M18 → R42 precedent), test-count delta, snapshot
  delta, surface count delta.
- **Refactor backlog candidates surfaced** — entries
  added to §22 R-class register at milestone close.
- **Lessons for the next milestone** — what to do
  differently; what worked well; what to retain.

Per-milestone post-mortems land at §11 (M19), §13 (M20),
§15 (M21), §17 (M22), §19 (M23), §21 (M24), §23 (M25),
§25 (M26), §27 (M27), §29 (M28). The odd-numbered
sections between (§12, §14, §16, ...) are the
milestone-specific R-class refactor backlogs surfaced
post-implementation (mirrors v0.2-plan §11–§22 structure).

## 11.–§29. Per-milestone post-mortems

**To be written at each milestone's close.** Each
post-mortem follows the §10 template; each adjacent R-
class section captures the milestone's surfaced
refactor candidates (mirrors v0.2-plan §11→§12 cadence,
§13→§14, ... through §29→§30 final-release). The
shipped R-class entries land in this plan doc's §22
register (below) at their respective shipping
moments.

## 11. M19 post-mortem — writer expansion close

**Status:** closed.

### Codex round summary

Six total Codex passes across the milestone's lifetime:

- **Pre-flight rounds 1–3** on the implementation plan
  (last session, before any code shipped). 18 findings
  across rounds 1–2; round 3 returned P1-clean. Lifted
  the `details.tags` array shape (Decision 1 round-1
  P2.8), the forward-looking hint default (`monday
  account tags` rather than the conservative `monday raw`
  fallback), the `MondayClient`-required input pattern,
  and the `ResolveTagsResult.cacheAgeSeconds` /
  `LoadAccountTagsResult.complexity` amendments to the
  pre-flight contract diff.
- **Mid-milestone Codex review** of Commits 1+2 (last
  session, pause-and-review cycle): P1-clean returned
  with 3 P2 + 1 P3. P2-1 plan-doc drift on the new
  contract amendments (fixed in §3 M19 deliverables
  paragraph); P2-3 `cacheAgeSeconds` interface comment
  contradicting the runtime (fixed + assertion added);
  P3-1 over-promising "M19 close" wording in inline
  comments (tightened to "Commit 2"). P2-2 (no
  integration happy-path cassette for `item set
  tags=launch`) deferred to Commit 3 — addressed there.
- **Post-Commit-5 Codex implementation review** (this
  session, run on Commits 3+4+5 diff): findings
  applied at Commit 6.

### Contract drift findings

Three contract-amendment moments to record for the
v0.3-plan §X / cli-design.md §X cross-references:

- **`BoardRelationValidationResult.ok: true` widening**
  (Codex round-2 P1-3, applied at Commit 3). The
  pre-flight diff at `d822982` pinned the success
  branch as `{ ok: true }`; the dry-run echo needed
  per-item home boards to render
  `details.resolved_from.items: [{input,
  resolved_board_id}]`. Widened to `{ ok: true, items:
  ValidatedRelationItem[] }`. Additive — existing code
  that branches on `result.ok` narrows correctly via
  TS.
- **`ResolveTagsResult.cacheAgeSeconds` addition**
  (Commit 2 amendment, Codex pre-flight). The
  pre-flight diff didn't include the slot; the
  translator-source aggregation pathway needs it for
  `meta.cache_age_seconds` per §6.1. The `'mixed'`
  source case carries the cache leg's age as
  worst-case staleness per the existing `mergeCacheAge`
  contract.
- **`LoadAccountTagsResult.complexity` addition**
  (Commit 2 amendment, Codex pre-flight). Mirrors
  `BoardMetadataLoadResult.complexity` so `monday
  account tags --verbose` reports budget like other
  cache-backed reads.

### Exit ratchets

- **Test count:** 2308 (M19 entry, post-pre-flight) →
  2466 (M19 close). +158 net across Commits 3+4+5+
  Codex fix-ups. The growth is concentrated in the
  three new translator surfaces (~25 unit tests +
  3-5 integration cassettes per translator) plus the
  `monday account tags` integration suite (5 tests).
- **Coverage:** 99.05 / 95.47 / 99.51 / 99.18 (M19
  entry) → 99.03 / 95.47 / 99.32 / 99.18 (M19 close).
  The branch floor held at 95.45% (above 95.45%
  required). The functions percentage dropped 0.19pp
  — driven by the new validator + translator surface
  growing the denominator faster than the per-file
  100% coverage grew the numerator. Both percentages
  stay above the 95% floor; the focused coverage push
  toward 95.5% branches per §7 release exit checklist
  is deferred to a future milestone.
- **Coverage floor:** unchanged at
  95/95.45/95/95. M19 doesn't ratchet up because the
  measured branches percentage didn't clear 95.5% with
  margin (95.47% — 0.03pp above the floor).
- **Surface count:** `WRITABLE_COLUMN_TYPES` 10 → 13
  (tags / board_relation / dependency); ERROR_CODES
  27 → 28 (`tag_not_found`, landed at `4c652d5` ahead
  of the milestone); command_count 69 → 70
  (`account.tags`); cache key kinds extended with
  `accountTags`.
- **Snapshot delta:** envelope-snapshot file's
  `command_count` slot bumped 69 → 70; the per-command
  envelope snapshot suite is expected to grow at the
  next focused output-shapes refresh (deferred — output-
  shapes.md sweep at Commit 7 didn't refresh per-command
  envelopes for the M19 verbs since the friendly
  translators emit through the existing
  `change_column_value` envelope shape and the deferral
  doesn't load-bear).

### Refactor backlog candidates surfaced

- **`peopleResolution.resolveEmail` source-aggregation
  parity gap** — pre-existing M5a / M8 behaviour where
  `--set Owner=alice@example.com` against a cache-hit
  user-directory lookup doesn't contribute to the
  envelope-level `meta.source`. M19 introduces the
  same pattern for `tags` / `board_relation` /
  `dependency` (the latter two are always-live, so no
  parity gap; `tags` correctly surfaces source via
  `translatorResolution`). Fixing `people` requires a
  similar `peopleResolution: PeopleResolutionInfo`
  echo slot or a `userByEmail`-shaped wrapper that
  records source + cacheAge. Logged for the M19 → M20
  cleanup window or M20-fold.
- **Generic `resolveDirectoryReferences` lift** —
  `userByEmail` (in `resolvers.ts`) and the new
  `tag-directory.ts` share a similar account-scoped
  cache-then-live-refresh-on-miss shape. If a third
  directory resolver lands later (groups by name?
  workspaces by slug?), the lift is natural. Below the
  3-consumer threshold today; tracked at §22 as a v0.3+
  candidate.

### Lessons for the next milestone (M20)

- **The mid-milestone-pause-and-review pattern works.**
  Splitting M19 into two sessions (Commits 1+2 → pause
  → mid-milestone Codex → Commits 3+4+5+6+7) caught
  cross-cutting infrastructure issues (the
  `cacheAgeSeconds` interface-comment-vs-runtime drift,
  the missing integration cassette for `item set
  tags=launch`) BEFORE Commits 3+4 absorbed them. The
  alternative — landing all 7 commits then running
  Codex once at the end — would have folded those
  findings deeper into the diff and made the fix-up
  pass larger.
- **R45 / R48 cadence held.** "Ship the helper
  alongside the first new consumer" worked verbatim
  for `parseRelationItemIds` (Commit 3) — the parser
  shipped alongside `board_relation`'s translator
  arm; `dependency` (Commit 4) inherited it for free.
- **Cross-cutting widenings stay one-time-per-
  milestone.** The N-way `assertEchoExclusivity`
  widened from N=3 to N=4 at Commit 3 (adds
  `relationResolution`); Commit 4 didn't widen further
  because `dependency` reuses the same slot. Net cross-
  cutting churn = 1 N-way step per milestone, not per
  translator. M20's `time_tracking` verb shape is
  distinct from value-writes so the
  `assertEchoExclusivity` won't widen there; M20's
  cross-cutting load is concentrated in the three-
  level command-depth carve-out and `start_time_tracking`
  / `stop_time_tracking` mutation wiring.
- **Test retargeting cascade is real.** Each
  graduating type fired a 5-test retarget across the
  integration suite (item-set / item-clear /
  item-update / item-update-bulk / board-doctor); the
  Commit 4 cascade was the larger one because
  `dependency` was the last tentative-row member —
  the v0_2_writer_expansion category branch went from
  partially-live to fully-dead in the same commit.
  Plan for M20: time_tracking is currently in its own
  category branch (`time_tracking` literal in the
  classifier). When M20 graduates the verbs,
  `time_tracking` doesn't graduate from the
  unsupported-types row in the same way (it routes
  through new verbs, not through the friendly
  translator); the integration test cascade should be
  smaller.

## 22. Refactor backlog — R44 / R49 / R50 migrated + non-R-class

Migrated from `docs/v0.2-plan.md` §22 at v0.3 plan
opening. The shipped v0.2 R-class entries (R28 / R29 /
R37 / R38 / R40–R53) stay in v0.2-plan §22 unchanged;
their `**Shipped: <sha>**` annotations are the canonical
history. v0.3 plan's §22 owns the candidates that
weren't shipped at v0.2 close — plus the non-R-class
quality refactor.

### R44 candidate — generic `projectMutationResource` over R28/R37/R43/R45/R48

**Migrated from `docs/v0.2-plan.md` §22 R44 entry**
(see v0.2-plan lines 4457–4517 for the full migration
context). Five per-noun projection helpers post-v0.2:
`projectMutationItem` (R28) / `projectMutationUpdate`
(R37) / `projectMutationBoard` (R43) /
`projectMutationColumn` (R45) / `projectMutationGroup`
(R48). Plus the inline-only workspace single-target
mutation verbs (M14) that never got their own helper —
post the R42 + R42-followup folding (`6d93f65`),
workspace's per-verb projectors call into the
generalised `assertResponseFieldPresent` directly so the
"sixth-noun candidate" status is firmer than at v0.2
close.

The five per-noun helpers diverge on `idKey` /
`errorMessage` prefix / `errorCode` / `schema` (the
noun's projection schema). Everything else — the
`if (raw === null || raw === undefined)` check + the
`throw new ApiError(errorCode, errorMessage,
{ details: { [idKey]: idValue } })` shape + the
`unwrapOrThrow(schema.safeParse(raw), { context, details
})` parse — is identical.

**Sites at v0.3 plan opening (5 active + 1 implicit):**
item / update / board / column / group + workspace
(post-R42 R42-followup, workspace single-target
mutations call `assertResponseFieldPresent` directly
through the generalised contract; the per-verb
projector pattern itself is the candidate sixth noun if
a future refactor unifies the inline-only path with the
named-helper path).

**Trigger:** **6 nouns post-v0.3 if the workspace
projector unification fires**, OR a fresh v0.3 noun
(M19's tag-directory? — unlikely, tags are an account-
scope read, not a mutation noun; M27's webhook
mutations? — possible if `create_webhook` /
`delete_webhook` projectors land as a sibling helper).

**Risk:** medium. Touches 5 noun families simultaneously
+ the cassette assertions on each. The byte-identical-
tests discipline catches behavioural regressions, but
the design question ("is the per-noun wrapper layer
worth keeping?") is aesthetic and can split a maintainer
one way or the other.

**When to schedule:** **v0.3 cleanup window candidate.**
Earliest non-scope-creep moment is the M19 → M20
cleanup window (R51 cadence between M17 and M18 is the
precedent; R53 cadence between M18 and v0.3-opening is
the second precedent). If a sixth-noun trigger fires
during M27 (webhook mutations), R44 lands then; if
not, it stays deferred until v0.4 or until a sixth-
noun lift naturally surfaces.

### R49 candidate — snapshot-bearing destructive-archive dry-run helper

**Migrated from `docs/v0.2-plan.md` §22 R49** (the
candidate evaluation at M17 close, see v0.2-plan
"R49 candidate" wording in the §22 looking-ahead block
+ the M17 → M18 cleanup-window discussion).

The three archive verbs (`item-archive` / `board-
archive` / `group-archive`) load a pre-archive snapshot
and surface it in the dry-run output. The
snapshot-loading semantics diverge per noun (item =
state + columns + group_id; board = full board metadata;
group = group columns + items count), and the
unified helper signature exceeds the >4-parameter
heuristic that v0.2-plan §22 R-timing rule uses to
gate refactor candidates.

**Sites at v0.3 plan opening (3):** item-archive +
board-archive + group-archive (all M15–M17, all
shipped).

**Trigger:** **only if a fourth archive-shaped verb
lands AND the snapshot semantics align.** v0.3 doesn't
introduce new archive verbs (M27 webhook delete is
delete-only; no snapshot-bearing archive). Stays
deferred.

**When to schedule:** **defer further to v0.4 OR fold
into a parameter-count-permissive variant if the
archive-snapshot inline divergence becomes a
maintenance burden.** If M28's release prep surfaces
inconsistent snapshot output across the three verbs,
the consolidation pressure rises; otherwise leave
parallel.

### R50 candidate — 1-surface attribute fan-out helper

**Migrated from `docs/v0.2-plan.md` §22 R50** (the
candidate evaluation at M17 close).

The `withBoardInvalidationFanOut` wrapper (R46) covers
the post-success projection + cache invalidation timing
for fan-out verbs. R50's candidate lift is one level
higher: a helper that wraps the entire attribute-fan-
out loop (per-attribute mutation, per-result
projection, per-error decoration, partial-success
envelope assembly).

**Sites at v0.3 plan opening (2):** board-update +
group-update (the two M15–M17 fan-out attribute
verbs).

**Trigger:** **3-consumer (R-timing rule).** v0.3
introduces no new fan-out attribute verbs in M19–M27;
M28 has none. Stays below the trigger.

**When to schedule:** **defer to v0.4 OR a third fan-
out verb landing.** If a v0.4 verb introduces a new
fan-out shape (e.g. `dev sprint update` if it lands
multi-attribute), R50 fires then; otherwise stays
parallel.

### Non-R-class — `unsupportedColumnTypeError` category-table-driven refactor

**Migrated from `docs/v0.2-plan.md` §26 looking-ahead**
(the post-v0.2 → v0.3 cleanup-window deferral —
"R42's coverage-recovery work pushed this session past
the typical refactor budget; the quality refactor was
bonus polish per the handoff and ships at the v0.3 →
first-cleanup-window or when the next deferral target
lands").

**Current shape.** `src/api/column-values.ts
unsupportedColumnTypeError` is a 5-branch if/else over
read_only_forever / v0_2_writer_expansion / files-
shaped / time_tracking / future categories. Each branch
hand-builds its `details` slot.

**Proposed shape.** A category table (one
`Record<Category, { deferred_to?: string, read_only?:
boolean, hint: string }>`) that the function indexes
into; the resulting helper is ~40 lines vs the current
~80. M19's tentative-row reclassification (the v0_2
row loses its three members; the v0_3 row picks them up
+ time_tracking) is the natural trigger to do this
refactor — touching the function anyway means the
category-table consolidation costs nothing extra.

**Sites at v0.3 plan opening (1 function, 1 caller):**
single function with a single caller; doesn't fit the
R-class 3-consumer trigger. Tracked as **non-R-class
quality refactor**, not as R-N.

**Risk:** low. Pure consolidation; byte-identical tests
catch any per-category drift.

**When to schedule:** **fold into M19** (the
tentative-row reclassification touches the function;
the consolidation costs nothing extra at that point).
Alternatively defer to the M19 → M20 cleanup window if
M19's pre-flight surfaces this as scope-creep risk.

### Looking ahead — M19 → M20 cleanup window

Mirrors v0.2-plan's M(N-1) → M(N) cleanup-window
cadence (R51 between M17-M18; R53 between M18-v0.3-
opening). Candidates for M19's cleanup window:

- R44 sixth-noun lift if M19 surfaces one (unlikely;
  tag-directory is read-scoped, not a mutation noun).
- The non-R-class `unsupportedColumnTypeError`
  category-table refactor (folded into M19 if not
  shipped earlier).
- Any new R-class candidates surfaced by M19's
  implementation review (e.g. `resolveDirectoryReferences`
  generic over `user-directory` + `tag-directory` if M19
  surfaces the shared lookup shape with strong evidence).

The cleanup window is **not** a hard requirement — if
M19 ships clean and no R-class candidates surface, the
window is empty and M20 starts directly.
