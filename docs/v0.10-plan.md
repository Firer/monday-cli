# v0.10 implementation plan

> **KICKOFF 2026-05-22 — v0.10 = the `NOUN_DESCRIPTIONS` single-
> source-of-truth lift, on the `2026-01` pin.** Picked at v0.10
> candidate-selection per R-NEW-75 after v0.9.0 shipped (npm
> `latest`, 2026-05-22T16:38:40Z; annotated tag `v0.9.0` at
> `ee96681`). SDK probe at candidate-selection:
> `@mondaydotcomorg/api` still at 14.0.0 baking `2026-01`; no 15.x
> (`2026-04`) or 16.x (`2026-07`) release. **4th consecutive pivot**
> (v0.7, v0.8, v0.9, v0.10) — the API-version-bump milestones
> DEFER again and v0.10 ships the next actionable code lift: the
> graduated R-v0.8-NEW-22 candidate that closes a *demonstrated*
> shipped-correctness bug class (sibling-string drift across the
> noun-description registration surface).
>
> **Why now:** the v0.8 post-publish audit filed R-v0.8-NEW-22 after
> M50 surfaced one drift (`doc` "read-only at v0.4" — order-dependent
> sibling literal won by attach order). The v0.10 pre-flight
> *immediately surfaced a second active drift* (`update/upload.ts`'s
> `'Update (comment) commands (cli-design §4.3 UPDATE)'` — an
> internal `cli-design §` ref leaking into help via `feedback_
> public_docs_clean` violation, order-dependent on which `update/*`
> file attaches first). Two demonstrated drifts in two consecutive
> milestones validates the MEDIUM-HIGH priority — this is not
> hypothetical, the bug class is live. The lift collapses ~100
> duplicate `ensureSubcommand(program, '<noun>', '<desc>')` literals
> across ~80 verb files into one ~18-entry map; divergence becomes
> structurally impossible (typos throw `internal_error` /
> `unknown_noun` at registration time, not silently at `--help`).
>
> **DEFERRED (retain numbers; reopen when the gating SDK publishes):**
> M39 (API `2026-04` pin) + M40 (`item set-description` — proven to
> work on `multi_level` boards but the mutation is `2026-04`-only) +
> M41 (`doc block-create-bulk`) need SDK 15.x; M44 (user-entity
> migration) + M45 (`user activity`) need SDK 16.x. v0.9 dev-board
> probe sweep confirmed M40 is feature-real on multi-level boards
> (the "Monday bug" / "paid gating" reads were SUPERSEDED); only
> the SDK gate remains.

## 1. Milestones

> **Scope: single milestone + release-prep.** Bounded blast radius —
> description strings only, no behaviour change, no new wire surface,
> no new transport seam, no new ERROR_CODE, no new commands. The
> alternative candidates (C cross-board `item move` value-overrides;
> E profile-scoped argument defaults; G `Item.description` read-side
> paired with v0.7-M40) stay filed for v0.10.x / v0.11 candidate-
> selection per the 2026-05-22 user binding.

| M | Surface | Wire op | Status |
|---|---------|---------|--------|
| **M53** | **`NOUN_DESCRIPTIONS` single-source-of-truth lift.** Collapse ~100 duplicate `ensureSubcommand(program, '<noun>', '<desc>')` literals across ~80 verb files into one ~18-entry `NOUN_DESCRIPTIONS` map (`src/commands/noun-descriptions.ts`). `ensureSubcommand(program, name, summary?)` looks the description up when `summary` is omitted; passing an explicit `summary` overrides the map (preserved across the pre-flight → IMPL migration window so all ~120 existing call sites compile unchanged at pre-flight). Strict-mode invariant: a name with no map entry AND no explicit summary throws `InternalError` with `details.reason: 'unknown_noun'` — catches a typo or missing entry at registration time, not silently at first `--help`. Closes the demonstrated-shipped bug class: v0.5/v0.8/v0.9 each surfaced a drift (the `doc` "read-only at v0.4" + the `update` `cli-design §4.3 UPDATE` ref leaking via `feedback_public_docs_clean` violation). | (n/a) — pure code lift; no wire surface | **PRE-FLIGHT CLOSED 2026-05-22** (`c48510f`); IMPL pending |
| ~~M39 / M40 / M41~~ | API `2026-04` pin + `item set-description` + `doc block-create-bulk` | — | DEFERRED — SDK 15.x not published |
| ~~M44 / M45~~ | user-entity migration + `user activity` | — | DEFERRED — SDK 16.x not published |
| **release-prep** | Version bump 0.9.0 → 0.10.0; CHANGELOG `[0.10.0]` headlining the lift (one user-impact line — "internal cleanup; no behaviour change"); ToC audit; deferral-slot slip cross-doc grep (R-NEW-82); README refresh per R-v0.9-NEW-15 across the three surfaces — quickstart, per-version Scope blocks, and the `**v<next> (next):**` backlog block; help-text hygiene grep (R-v0.8-NEW-21 — likely landing CLEAN since the lift IS the hygiene fix); envelope-snapshot refresh probe; close-docs sweep. Process-only — Codex skipped per R-NEW-84 if zero production `src/**/*.ts` semantic changes outside the lift. | (n/a) | — pending IMPL |

**Rough build order:** M53 pre-flight (this commit, additive signature stub + map skeleton; ~120 call sites unchanged) → M53 IMPL (drop 3rd arg from each of the ~120 call sites; remove the `update/upload.ts` drift in the mass migration; Codex IMPL review) → release-prep (version bump + CHANGELOG + close-docs).

## 2. Empirical probe basis (M53 — none required)

M53 is a pure code lift. **No empirical probe is needed.** The lift
touches no wire surface, no transport, no Monday API. The pre-flight
*observation* (analogous to a probe) is the call-site enumeration:

- **~120 `ensureSubcommand(...)` call sites** across `src/commands/**`
  (`grep -rn 'ensureSubcommand(' src/`).
- **18 unique noun keys** in the canonical map (one per top-level
  noun + one per group-tier noun); **19 currently-active literal
  pairs in source** — the 19th is the R-v0.10-NEW-1 `update` drift
  (`'Update (comment) commands (cli-design §4.3 UPDATE)'`) which
  resolves to the same `'update'` map key as the canonical pair:
  - **Top-level (13):** `account`, `auth`, `board`, `cache`,
    `config`, `dev`, `doc`, `item`, `notification`, `update`, `user`,
    `webhook`, `workspace`.
  - **Group-tier under `dev` (4):** `sprint`, `epic`, `release`,
    `task`.
  - **Group-tier under `item` (1):** `time-track`.
  - All 18 disjoint — no name collision between top-level + group
    tiers today.
- **Two demonstrated drifts at pre-flight enumeration:**
  1. `'doc'` — v0.5/v0.8/v0.9 sessions caught the "read-only at v0.4"
     staleness; M50 close cleaned the source.
  2. `'update'` — `src/commands/update/upload.ts:110` carries
     `'Update (comment) commands (cli-design §4.3 UPDATE)'` instead of
     the canonical `'Update (comment) commands'`. The parenthetical
     leaks an internal `cli-design §` reference into the user-facing
     help surface (violates `feedback_public_docs_clean`). NOT fixed
     at pre-flight (per handoff §7 — file the drift, don't fix; the
     IMPL migration drops it structurally). Filed §22 R-v0.10-NEW-1.

## 3. Per-milestone decisions

> Land at each milestone's pre-flight contract diff. M53 dimensions
> (D1) group-tier keying — flat noun-stem vs `noun.group`; (D2)
> signature shape — additive (3rd arg optional with map fallback)
> vs replace (3rd arg drops, all 120 sites migrate at pre-flight);
> (D3) map location — extend `src/commands/types.ts` vs new
> `src/commands/noun-descriptions.ts`; (D4) `tests/unit/commands/
> types.test.ts` contract.

### M53 — NOUN_DESCRIPTIONS single-source-of-truth lift (pre-flight CLOSED 2026-05-22, `c48510f`)

**Probe basis:** none required (§2). Pre-flight observation is the
call-site enumeration: 122 call sites collapsing to 18 canonical
noun keys (19 currently-active literal pairs in source — the 19th
is the R-v0.10-NEW-1 `update` drift that the IMPL migration drops).

**Headline finding (drove the scope).** Pre-flight enumeration
surfaced a SECOND active drift in `update/upload.ts:110` that pre-
dates M53 by months. The previous-known drift (`doc` "read-only at
v0.4") was the cited motivation when R-v0.8-NEW-22 was filed; the
second instance crystallises the rule-of-three (one bug from v0.5,
one from v0.8, one from v0.10's own pre-flight enumeration). The
lift addresses the bug *class*, not the individual instances —
once the map is the source of truth, divergence is structurally
impossible.

**Decision: ADDITIVE PRE-FLIGHT** (the 4-D choices below; no user
escalation needed — the constraints are unambiguous given the
existing call-site shape + the handoff's pre-flight scope).

- **D1 — Group-tier keying: FLAT noun-stem.** The map keys are
  plain noun strings (`'sprint'`, `'epic'`, `'time-track'`, …) —
  not `'dev.sprint'` / `'item.time-track'`. Trade-offs:
  - **Flat noun-stem (chosen).** No collisions today across the 18
    nouns. Simplest lookup (`NOUN_DESCRIPTIONS[name]`). Project
    rule "don't design for hypothetical future requirements" applies.
    A future collision (if a group-tier noun ever shared a name with
    a top-level noun, or two group-tier nouns under different
    parents collided) would NOT surface automatically: the lookup
    returns the same string for both registrations, and commander's
    find-or-create works against separate parent scopes, so both
    parents would render the same description silently. Mitigation
    is to **revisit the keying** when a collision actually appears
    — at that point the migration cost is small (the map already
    centralises the strings), so the optionality is preserved.
  - **Path-keyed (`noun.group`).** Future-proof against name
    collisions. Costs a context-aware lookup signature
    (`ensureSubcommand` would need to derive the parent-chain key
    from the `program` arg) — surface complexity for a problem
    nobody has today. Defer until a collision actually surfaces.
- **D2 — Signature shape: ADDITIVE** —
  `ensureSubcommand(program, name, summary?)` with `summary` made
  optional + falling back to `lookupNounDescription(name)`. All ~120
  existing call sites compile unchanged at pre-flight; M53 IMPL drops
  the 3rd arg from each call. Trade-offs:
  - **Additive (chosen).** Matches the pre-flight contract diff
    discipline ("pre-flight ships stubs additively; IMPL flips the
    runtime"). Single function — easy to test, easy to grep for
    migration progress (the IMPL hand-off section below pins the
    deterministic `rg` commands; the count starts at 122 and
    drops to 0 as IMPL migrates). Explicit summary override wins
    (matches commander.js precedent — most-specific source wins).
  - **Replace (3rd arg drops).** Forces all ~120 sites to migrate
    at pre-flight — bigger blast radius, no migration window, the
    pre-flight commit becomes the IMPL commit. Defeats the
    pre-flight discipline's separation of contract surface from
    runtime migration.
- **D3 — Map location: NEW `src/commands/noun-descriptions.ts`.**
  Trade-offs:
  - **New module (chosen).** SRP — the map is *concrete data*,
    `types.ts` is *pure interface + helper*. New module is small
    (~80 lines incl. the lookup helper + JSDoc), focused, easy to
    grep. ~80 future verb files will import the helper indirectly
    via `ensureSubcommand` (no direct import surge from this lift).
  - **Extend `types.ts`.** Adds ~80 lines of concrete data to
    what's currently 80 lines of types + a single helper. Mixes
    purposes; the file's name (`types`) no longer matches its
    content. Marginal save (one fewer file) doesn't justify the
    SRP loss.
- **D4 — Test contract** (`tests/unit/commands/types.test.ts`).
  The new test surface pins:
  1. **Lookup happy path** — `ensureSubcommand(program, 'config')`
     finds `NOUN_DESCRIPTIONS.config` and uses it as
     `description()`.
  2. **Find-or-create preserved** — second call with the same name
     returns the same Command instance (not re-created).
  3. **Multi-noun isolation** — `'config'` + `'cache'` create
     distinct commands matched by name only.
  4. **Explicit override wins** — passing a 3rd arg uses that
     string verbatim instead of the map (M53 IMPL-migration window
     contract).
  5. **Override-on-existing is ignored** — find-or-create wins; a
     2nd call with override on an existing noun returns the
     existing Command unchanged (matches the silent-drift bug
     class — second drift literals never overrode the first; the
     map removes the drift, the find-or-create semantics stay).
  6. **Strict-mode rejection** — `ensureSubcommand(program,
     'nonexistent-noun')` throws `InternalError` with
     `details.reason: 'unknown_noun'` + `noun` + `known_nouns[]`.
     This catches typos / missing-map-entries at registration time
     (commander's startup walk of the command registry fires the
     throw before the user sees `--help`).
  7. **Override bypasses lookup** — `ensureSubcommand(program,
     'unknown-noun', 'Explicit')` succeeds; the map IS the source
     of truth ONLY when the override is absent.
  8. **`NOUN_DESCRIPTIONS` shape invariants** — 18 entries (the
     pre-flight enumerated set); every entry is a non-empty
     string; no entry contains internal-doc references
     (`cli-design §`, `v0.x-M`, `deferred_to`, `TODO`, `FIXME`) —
     `feedback_public_docs_clean` enforced at the test boundary.
  9. **`lookupNounDescription` direct contract** — returns the
     mapped string; throws `InternalError` with structured
     `details` on miss.

  Tests drive the real production `NOUN_DESCRIPTIONS` map + real
  `Command` (not hand-shaped per `testing.md`). 12 tests total;
  all pass on the pre-flight commit.

- **D5 — ERROR_CODES delta.** ZERO. Registry stays 29. The
  `lookupNounDescription` rejection throws `InternalError` with a
  NEW `details.reason: 'unknown_noun'` discriminator — adds to the
  existing `internal_error` code's discriminator family, no new
  top-level code. 118 commands unchanged (no new verb).

**Pre-flight stub: NOT APPLICABLE** (R-v0.9-NEW-2 — the rule-
clarification graduated at v0.9 close). M53 is a pure code lift —
no new deferred wire leg, no new transport seam, no new mutation.
The R-NEW-76 parseArgv-before-c8-ignore stub convention anchors
NEW deferred wire legs; M53 adds none. The `unknown_noun` rejection
is a live runtime invariant — fires at registration walk on a typo,
not a deferred-leg stub — so it ships unconditional, no
`m53_preflight_stub` literal, no PIN test. **2nd instance recorded
for R-v0.9-NEW-2** (after M50): the "no stub for rejection-lift /
pure-refactor pre-flights" pattern. Trigger ratified — fold the
clarification into `.claude/rules/workflow.md`'s pre-flight stub
rule at M53 IMPL close (graduation criterion met at 2 instances per
R-NEW-58's consumer-count gating; cross-refs R-NEW-76 + R-v0.9-NEW-1).

**IMPL hand-off — contract-term checklist (R-v0.7-NEW-4).** The IMPL
session runs a cross-doc grep + flips every site below.

*Should DISAPPEAR from current (non-historical, non-regression-guard)
prose post-IMPL:*
  - All ~100 `ensureSubcommand(program, '<noun>', '<desc>')`
    3-arg call sites — collapsed to `ensureSubcommand(program,
    '<noun>')` (the 3-arg form stays VALID for one milestone window;
    IMPL drops the override at every site).
  - `'Update (comment) commands (cli-design §4.3 UPDATE)'` in
    `src/commands/update/upload.ts:110` — the `cli-design §` ref
    drops with the 3rd arg.
  - Migration-window prose in `src/commands/types.ts`'s
    `ensureSubcommand` docstring (the "preserved across the v0.10-M53
    → IMPL migration window so the existing ~120 call sites compile
    unchanged" sentence) — IMPL flips this to "single source is
    `NOUN_DESCRIPTIONS`; override path retained only for ad-hoc /
    test injections".
  - Migration-window prose in `docs/architecture.md`'s
    `commands/types.ts` bullet (same parenthetical) — flip similarly.

*Source sites needing the flip (enumerated)* — the mechanical surface
is enumerated deterministically at IMPL kickoff by two ripgrep
multiline patterns:

```bash
# Single-line: all four args on one line.
rg -nP "ensureSubcommand\(\s*\w+,\s*'[\w-]+',\s*'[^']+'\s*\)" \
  src/commands/

# Multi-line: 3-arg call wrapped across 3+ lines (the only other
# call shape in the current codebase). Uses `-U` (multiline) + `-P`
# (PCRE2) so `\n` matches across lines; counts each `ensureSubcommand(`
# block whose third literal arg is a quoted string.
rg -nUP "ensureSubcommand\(\s*\n\s*\w+,\s*\n\s*'[\w-]+',\s*\n\s*'[^']+'" \
  src/commands/
```

Empirically verified at this pre-flight: **122 call sites** total
across `src/commands/**` (87 single-line + 35 multi-line; counts
via `rg -c` and `rg --count-matches -U`). Excludes the re-export in
`src/commands/index.ts:461` and import-only sites. Each edit is
mechanical — delete the third argument (and any trailing
comma/whitespace) — no semantic changes.

*Should APPEAR post-IMPL:* `v0.10-M53`, "single source of truth
for noun descriptions", "`NOUN_DESCRIPTIONS`", "lookupNounDescription".

**Codex pre-flight review (gpt-5.5, read-only):** **CONVERGED R4 —
0 P1 across R1/R2/R3/R4; 3 P2 (R1) → 3 P2 (R2) → 2 P2 (R3) → 0 P2
(R4); 3 P3 (R1) → 2 P3 (R2) → 2 P3 (R3) → 2 P3 (R4).** All findings
folded inline. Notable R3 P2-1 catch — strict-mode `InternalError`
from `lookupNounDescription` was thrown OUTSIDE the runner catch
boundary (`src/cli/run.ts:191` built the program before the `try`),
so a future M53 IMPL typo would have escaped the envelope machinery
as a raw stack trace. Fix: moved `buildProgram` inside the `try`
block + added a runner-level test pinning the attach-time-throw →
envelope path. R3 P2-2 caught a stale 4-arg grep example
contradicting the locked-in `rg` commands; replaced with a pointer
to the §3 IMPL hand-off block. R3 P3-1 + R4 P3-1 caught "18 unique
pairs" wording imprecision (active `update` drift means 19 literal
pairs, 18 canonical keys); fixed across §1/§2/§9. R4 P3-2 caught
CLAUDE.md handoff D1 rationale contradicting v0.10-plan's corrected
"would NOT surface automatically" wording; synced.

### M53 — IMPL close (pending)

— pending IMPL session. Expected scope: drop the 3rd arg from every
`ensureSubcommand(program, '<noun>', '<desc>')` call site (~120
sites); confirm `update/upload.ts` drift drops in the migration;
gates stay green; ERROR_CODES + command count unchanged.

## 7. v0.10 release exit checklist

Mirrors v0.9-plan §7. v0.10 stays on API `2026-01` (no SDK 15.x or
16.x publication in the window — 4th consecutive deferral of the
M39/M40/M41 cluster, 5th of the v0.8-skeleton M44/M45), so the
SKELETON's API-pin flip + M44/M45 steps DO NOT apply.

Exit checklist (ticked at v0.10 release-prep close; publish steps
pending the user):

- [ ] Envelope-snapshot refresh probe ran clean.
- [ ] R-NEW-82 cross-doc grep ran at the ToC-audit step:
  `rg 'deferred_to: "v0.10"' src/ tests/` expected to return ZERO
  hits (the grep is scoped to `src/` + `tests/`; the plan-doc
  itself is excluded by scope so this checklist line citing the
  literal is not a false-positive). M53 IMPL is a pure code lift,
  so no new deferred wire leg ships a slot; the gate is a
  defensive sweep matching the v0.5/v0.6/v0.7/v0.8/v0.9 precedent.
- [ ] README refreshed across the three surfaces R-v0.9-NEW-15
  graduated at v0.9 close: (a) quickstart (likely held — M53
  doesn't change any user-visible verb), (b) per-version Scope
  blocks (the `## Scope` section per shipped version — check the
  v0.10 entry is added when the version bumps), (c) the
  `**v<next> (next):**` backlog block at the bottom of Scope
  (pointer flips from "v0.10 (next): NOUN_DESCRIPTIONS lift" →
  "v0.10.x / v0.11 (next): TBD per candidate-selection").
- [ ] Version bump `0.9.0` → `0.10.0`. `npm audit` clean (no
  audit-fix folded unless dependencies update).
- [ ] CHANGELOG `[0.10.0]` entry — one user-impact line: "internal
  cleanup; no behaviour change. Removes a silent drift class in
  command help text." Per `feedback_public_docs_clean`, no
  M-number references in the public CHANGELOG body.
- [ ] Help-text hygiene grep CLEAN (R-v0.8-NEW-21 trigger) —
  expected CLEAN by construction (the lift IS the hygiene fix; the
  `update` drift drops at IMPL).
- [ ] Close-docs sweep: CLAUDE.md status flip + §1 row + this
  §3/§7 fill + §22 R-class updates. R-NEW-82 ratification count
  ticks up; R-NEW-84 applied (Codex skipped — mechanical/process-
  only cluster, zero production `src/**/*.ts` semantic changes at
  release-prep).
- [ ] Gates green at close: `typecheck` + `lint` clean; tests pass
  (count stable + the ~12 new types.test.ts tests); branches ≥
  95.45 floor; 29 ERROR_CODES; **118 commands** (unchanged from
  v0.9.0); `npm audit` 0 vulns.
- [ ] **Published.** `git push origin main` → annotated `v0.10.0`
  tag → GitHub release → `npm publish` placed `monday-cli@0.10.0`
  on `latest` → post-publish flip commit (mirrors v0.9's
  precedent: CLAUDE.md "Published: `monday-cli@0.10.0` … release
  complete").

## 9. Preconditions

- ✅ `@mondaydotcomorg/api@14.0.0` baking API `2026-01` — unchanged
  since v0.6 (4th-consecutive SDK stall).
- ✅ v0.9.0 published + release-complete (`ee96681`,
  2026-05-22T16:38:40Z; npm `latest`).
- ✅ R-v0.8-NEW-22 candidate filing reviewed at v0.10 candidate-
  selection (`5d89754`) — picked the lift over carry-forward C/E/G
  alternatives per R-NEW-75 5-dimension scoping.
- ✅ Pre-flight call-site enumeration complete — 18 canonical noun
  keys identified (19 currently-active literal pairs in source);
  122 call sites total (87 single-line + 35 multi-line, empirically
  verified); 2 demonstrated drifts catalogued (the `doc` fix
  already shipped at M50 close; the `update` drift filed §22
  R-v0.10-NEW-1 + structurally fixed at M53 IMPL via the mass
  migration).

## 22. R-class register

> **Initial population at the M53 pre-flight refactor-audit
> (2026-05-22).** Carries forward the still-open v0.8 §22
> watch-items + the v0.9 §22 watch-items not yet graduated, with
> current status. Files new `R-v0.10-NEW-*` candidates surfaced at
> M53 pre-flight. Extended at each v0.10 milestone close-docs.
> Full prose for carried-forward entries stays in their original
> plan-doc §22 — the entries below carry the status delta + pointer.

### Carried forward from v0.8 §22 (still open)

- **R-v0.8-NEW-19 — create-time `settings`-wrap registry fold**
  (code lift). Still 2 wrap-types; trigger UNMET (M53 doesn't touch
  `column-create.ts`). **Status: OPEN.** Full entry: v0.8-plan §22.
- **R-v0.8-NEW-20 — suspected `dependency` settings live-drift**.
  NEEDS A PROBE. M53 doesn't touch `column-values.ts`; trigger
  UNMET. **Status: OPEN.** Full entry: v0.8-plan §22; user-memory
  `project_dependency_settings_drift`.
- **R-v0.8-NEW-21 — help-text hygiene release-prep grep** (process,
  MEDIUM). **Status: PARTIALLY-RESOLVED** at v0.9 release-prep
  (CLEAN). Trigger meets at v0.10 release-prep — the lift IS the
  hygiene fix; expected to stay CLEAN. Full entry: v0.8-plan §22.

### Promoted to v0.10 milestones

- **R-v0.8-NEW-22 → M53** (`NOUN_DESCRIPTIONS` single-source-of-
  truth map). **Status: PROMOTED — pre-flight closed 2026-05-22.**
  M53 IMPL completes the lift (drops the 3rd arg from ~120 call
  sites; remove the `update` drift in the migration). The
  candidate's facets all land in M53: the map collapses ~100
  duplicate literals; the signature change preserves find-or-create;
  the strict-mode rejection catches typos at registration time.

### Carried forward from v0.9 §22 (still open, not graduated)

- **R-v0.9-NEW-3 — Contract-term checklist must enumerate ALL doc
  surfaces + match line-wrapped phrases** (process, MEDIUM). Trigger
  at next milestone with multi-doc-surface contract-term grep — M53
  IMPL is mechanical (no contract-term flips beyond the one-line
  `update` drift); trigger UNMET at pre-flight. **Status: OPEN.**
  Full entry: v0.9-plan §22.
- **R-v0.9-NEW-5 — Board-projection schema fragmentation across 4
  schemas** (LOW). Trigger UNMET (M53 doesn't touch board schemas).
  **Status: OPEN.** Full entry: v0.9-plan §22.
- **R-v0.9-NEW-8 — `Item.description` read-side, paired with
  v0.7-M40 reopen** (MEDIUM, user-directed at v0.9-M52 close).
  Trigger UNMET (M53 is the noun-descriptions lift, NOT `Item.
  description`); stays paired with M40 reopen. **Status: OPEN.**
  Full entry: v0.9-plan §22.
- **R-v0.9-NEW-9 / 10 / 11 / 12** — fixture-factory / helper-lift /
  fixture-leftover-detection / Codex `-xhigh` behaviour. All
  watch-items, all UNMET at M53. Full entries: v0.9-plan §22.
- **R-v0.9-NEW-13 — Post-publish flip pattern** (2nd consecutive
  instance at v0.9 post-publish; graduates into workflow.md at 3rd).
  Trigger fires at v0.10 post-publish (if pattern recurs); WATCH.
  Full entry: v0.9-plan §22.
- **R-v0.9-NEW-14 — Deletion-led IMPLs preempt R-NEW-82 release-
  prep slip work** (1st instance, M50). Trigger fires at the
  2nd deletion-led IMPL; M53 is additive-then-deletion (pre-flight
  additive + IMPL deletion of 3rd args), so this could partially
  apply at M53 IMPL close — WATCH at IMPL.
- **R-v0.9-NEW-15 — README Scope-section drift at release-prep**
  (2nd-instance trigger fired at v0.9 post-publish; checklist
  needs to widen). M53 IMPL is mechanical, so the Scope section
  likely doesn't drift — but the v0.10 release-prep checklist
  should still include the per-version Scope blocks per the
  v0.9 graduation. **Status: PARTIALLY-RESOLVED** (the checklist
  widen action lives in v0.9-plan §22; M53 release-prep applies
  it).
- **R-v0.9-NEW-16 — No User-Agent header on outbound requests**
  (filed `a572d36`). Trigger UNMET at M53 (no transport touches).
  Full entry: v0.9-plan §22.

### New at M53 pre-flight (2026-05-22)

- **R-v0.10-NEW-1 — Active `'update'` description drift in
  `update/upload.ts:110`** (data point + structurally fixed at M53
  IMPL). The pre-flight call-site enumeration surfaced
  `src/commands/update/upload.ts:110` carrying `'Update (comment)
  commands (cli-design §4.3 UPDATE)'` — the parenthetical leaks an
  internal `cli-design §` reference into user-facing `--help`
  output, violating `feedback_public_docs_clean`. Order-dependent:
  if `update/upload.ts`'s `attach()` runs before any other
  `update/*.ts`, the leaked string renders (commander's find-or-
  create semantics); otherwise the canonical
  `'Update (comment) commands'` from another sibling wins. Both the
  v0.5 `doc` drift (cleaned at M50 close) and this v0.10 `update`
  drift confirm the bug *class* R-v0.8-NEW-22 cited. **Priority:**
  LOW (single instance; M53 IMPL structurally drops it). **Action:**
  NO inline fix at pre-flight (per handoff §7); the IMPL migration
  removes the override when dropping the 3rd arg from every
  `ensureSubcommand` call site. **Trigger:** verify at M53 IMPL
  close-docs — the `update/upload.ts:110` literal must be gone +
  the regression-guard test (D4 #8) must reject it.
- **R-v0.10-NEW-2 — Rule-of-three demonstrated for the noun-
  description drift bug class** (1st instance: `doc`, v0.5/v0.8;
  2nd instance: `update`, v0.10 pre-flight; 3rd consumer would
  graduate the lift's value-justification into a permanent
  contract-surface rule). M53 closes the bug class
  *structurally*; no future drift instance should occur (typos
  throw `internal_error` at registration time). **Status:**
  WATCH — if a future drift surfaces despite the map (e.g., via
  the explicit-override escape hatch that M53 IMPL drops), file
  the post-IMPL regression + tighten further (e.g., delete the
  override path entirely).
