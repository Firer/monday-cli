# v0.7 implementation plan

> **Status:** SKELETON ratified at v0.7 kickoff candidate-selection
> session 2026-05-18 per R-NEW-75 framework. **M39 D1 closed:**
> sequence-shuffle v0.7 milestone ship order to **M42 → M43 →
> (re-probe SDK 15.x) → M39 → M40 → M41**. M42 + M43 are pure
> v0.6.x carve-out folds (`add_file_to_column` per-item +
> create-time two-leg) reusing v0.4-M31's multipart wire surface
> — independent of the API pin bump — so they ship under the
> current 2026-01 / SDK-14.0.0 pin while SDK 15.x has 4-6 more
> weeks to publish (latest publish at kickoff: `14.0.0`,
> 2026-03-18, bakes `CURRENT_VERSION = "2026-01"`; no 15.x release
> exists yet).
>
> **M42 IMPL shipped** (R0 `22df2fa` + R1 fix-up `968b154` +
> R2 fix-up `a41c5d0` + R3 fix-up `e905a33`). Pre-flight contract
> diff at `160330b` closed D1-D4 (see §3 M42 entry below): D1
> reuses v0.4-M30's `dispatchParallel` over a shared
> `MultipartTransport`; D2 mirrors M25's partial-success shape
> with `asset` slot replacing `item`; D3 single upfront
> `precheckLocalFile` before the dispatch loop per cli-design §5.8
> atomicity discipline; D4 zero net change to ERROR_CODES (registry
> stays at 29). IMPL replaced the c8-ignored stub body with the
> full per-item multipart fan-out: `executeFileColumnSet` per
> matched item under the `--concurrency` selector +
> `foldAndRemap` for stable-code remap on stale-cache archived
> columns (R1 P1-1) + `SourceAggregator` over metadata + M38
> pre-check + items_page walk + dispatch legs (R1 P2-1) +
> partial-success invalidate on fail-fast with prior applies
> (R1 P2-2). The `'m42_preflight_stub'` discriminator literal is
> RESERVED post-IMPL (regression-guarded by an integration test
> that asserts the literal does NOT appear in stdout/stderr).
> Existing v0.6-M38 integration tests asserting
> `'file_set_on_bulk_unsupported'` flipped to assert the new
> contract envelope shape; mutex tests asserting
> `'multi_file_set_unsupported'` / `'mixed_file_and_value_sets'`
> on bulk callShape stay green (those rules are universal). Full
> post-mortem (Codex round-by-round summary, R-class candidates,
> branches-margin recovery target) lands at the close-docs commit.
>
> Closure rationale: zero override commitment until M43 close +
> preserves optionality across all three fallback paths (SDK 15.x
> lift / string-literal override / continued wait). The override
> path itself is cheaper than initially framed at candidate-
> selection scrutiny: `src/api/transport.ts:99`'s per-request
> `API-Version` header is already parameterised + overridable, and
> the `.claude/rules/monday-api.md` "Boundary-typing trap" pattern
> already routinely hand-rolls zod schemas for SDK-untyped fields
> (`BatteryValue` / `hierarchy_type` / `is_leaf` / `capabilities`).
> So the M43-close re-decision is robust whether SDK 15.x ships
> or not. The bounded-fallback enumeration documented at §3 M39
> D1 carries forward unchanged — sequence-shuffle was fallback
> (iii) selected at kickoff candidate-selection rather than after
> 2-4 sessions of waiting. Pattern noted in [[R-v0.7-NEW-1]].
>
> Per-milestone decisions (§8) + empirical-probe results (§9)
> populate at each milestone's pre-flight kickoff.
>
> The binding contract is [`cli-design.md`](./cli-design.md). This
> document sequences the work needed to ship v0.7 of that contract;
> it doesn't redefine it. If a conflict arises, `cli-design.md` wins
> and this plan is updated.

## 1. Scope summary

v0.7 is "**Monday API 2026-04 + carry-forward enrichments**" — the
first of two API-version-bump releases pinning the CLI to a newer
Monday API surface. v0.6 closed the files-shaped friendly `--set`
writer path (the longest-running v0.4 → v0.5 → v0.6 carry-over).
v0.7 picks up the next class of work: pinning to API `2026-04` (the
first GA Monday API version newer than `2026-01`) and exposing the
high-value additive surfaces that version introduces, while folding
in the v0.6.x bulk + create file-`--set` carve-outs deferred at M38.

**In scope (v0.7 milestones — full per-milestone scope filled at
each milestone's pre-flight kickoff):**

| M | Surface | Wire op | Status |
|---|---------|---------|--------|
| **M39** | API pin bump `2026-01` → `2026-04` + mass-flip of `2026-01` literals across docs/probes/cassettes + envelope-snapshot refresh + SDK upgrade to 15.x (or string-literal override on SDK 14.0.0 per pre-flight D1 closure). Mechanical/process cluster; no new CLI verb. | (n/a — pin flip only) | SKELETON |
| **M40** | `monday item set-description <iid> --content <md\|html>` (+ `--dry-run`) — set the item's description block via `set_item_description_content`. Single-call mutation; mirrors M37's `monday doc append-markdown` payload-source pattern (`--content <file\|-> \| --content-string <s>` mutex). | `set_item_description_content` | SKELETON |
| **M41** | `monday doc block-create-bulk <did> --blocks <json>` (+ `--dry-run`) — bulk-create up to 25 doc blocks per call. Supersedes M36's per-block round-trip pattern for batch use cases (M36 `block-create` stays for single-block writes). | `create_doc_blocks` | SKELETON |
| **M42** | v0.6.x **bulk file `--set` carve-out fold** — `monday item update --where ... --set <file-col>=<path>` per-item file dispatch + partial-success envelope + `--concurrency` shared-transport semantics. Closes D5 deferral from v0.6-M38. | `add_file_to_column` (v0.4-M31 wire, per-item) | SHIPPED (R0 `22df2fa` + R1 `968b154` + R2 `a41c5d0` + R3 `e905a33`; close-docs pending) |
| **M43** | v0.6.x **create-time file `--set` carve-out fold** — `monday item create --set <file-col>=<path>` via post-create `add_file_to_column` two-leg dispatch. Closes D6 deferral from v0.6-M38. Atomicity-envelope shape per cli-design §5.8 pinned at pre-flight. | `create_item` + `add_file_to_column` (two-leg) | SKELETON |
| **polish** | Refreshed envelope-snapshot suite for v0.7 surfaces; `output-shapes.md` ToC audit; README quickstart updates; CHANGELOG `0.7.0` entry; version bump 0.6.0 → 0.7.0; close-docs sweep. Mirrors v0.6 release-prep cluster shape verbatim. | (n/a) | SKELETON |

**Optional v0.7.x candidates (filed but NOT in v0.7.0 scope by default
— elevated to in-scope only if pre-flight empirical probes surface
unexpected friction OR strong agent demand):**

- `monday board describe` + `monday board get` projections extended
  with `Board.folder` + `Board.created_from_board_id` (2026-04
  additive Board fields). Small, additive; could fold into M39's
  envelope-snapshot refresh if zero-cost; otherwise defers to
  v0.7.x. Decision lands at M39 pre-flight.

**Out of scope (deferred to v0.7.x / v0.8 / permanently — full
rationale per item):**

### 2026-04 wire surfaces NOT exposed at v0.7

- **Knowledge-base reads + AI surfaces** — `articles` / `article_blocks`
  queries; `create_article` / `publish_article` / `delete_article`
  mutations; `knowledge_base_search` (AI-answered); `ask_developer_docs`
  query. **Rationale:** AI-flavoured surface area; questionable fit
  for the CLI's agent-first primary audience (agents would route AI
  queries through their own LLM, not Monday's). Revisits at v0.7.x
  on demonstrated user demand only.
- **Notetaker meetings** — `notetaker.meetings` query (recordings,
  summaries, transcripts, participants). **Rationale:** Niche
  enterprise-grade surface; out-of-scope for the agent-first CLI's
  read/write loop until user demand surfaces.
- **Object-relations API** — `object_relations` query +
  `create_object_relations` / `delete_object_relation` mutations +
  `create_object.relations` arg. **Rationale:** Niche; revisit at
  v0.7.x on demand.
- **Email sequences** — `allowed_sequences_to_enroll` query +
  `enroll_items_to_sequence` mutation. **Rationale:** Niche
  marketing-automation surface; out-of-scope until user demand.
- **Doc version history** — `doc_version_history` +
  `doc_version_diff` queries. **Rationale:** Useful but niche;
  defers to v0.7.x as a doc-namespace expansion if user demand
  surfaces.
- **Webhook subscription management** —
  `get_app_lifecycle_subscriptions` / `update_app_lifecycle_subscription`
  / `delete_app_lifecycle_subscription` mutations. **Rationale:**
  Adjacent to v0.3-M27's existing `monday webhook list/create/delete`
  surface; lifecycle-subscription semantics differ from regular
  webhooks (app-installation events vs board-data events). Revisits
  at v0.7.x with a clean scoping decision distinguishing the two
  surfaces.
- **Enterprise-gated additions:** `create_project` (Enterprise-plan
  only, project boards) + departments API (`departments` query +
  `department` field on users + 7 CRUD mutations). **Rationale:**
  Enterprise-plan-gated — most users can't exercise the surface;
  exposing it without a plan-detection gate would surface
  `unauthorized` to non-Enterprise users on every invocation.
  Revisits at v0.7.x with a plan-gate Decision OR drops permanently
  if the surface stays Enterprise-only.
- **Reaction attribution + Kind.agent enum + ExternalWidget enum
  expansion** (`Reaction.attribution_entity_ref`,
  `Reaction.attribution_entity_type`, `Kind.agent`,
  `ExternalWidget.APP_FEATURE` + `LISTVIEW`). **Rationale:**
  Wire-shape detail useful for `--verbose` debugging but not
  surfaceable through a clean CLI verb. Folds into envelope
  projections if a future read verb surfaces a Reaction; otherwise
  carries forward as transparent SDK-typed pass-through.
- **`BlockEvent.iterator_id` / `current_iteration` / `max_iterations`
  (automation loops).** **Rationale:** Webhook-payload-only surface;
  the CLI doesn't host webhook receivers (cli-design §13 permanent
  non-goal). N/A.
- **`update_board(item_nickname:)` attribute extension.**
  **Rationale:** Folds into existing M15 `monday board update`
  per-attribute fan-out if the surface is zero-cost; decision lands
  at M39 pre-flight.
- **`workspaces.query_params(:)` filter (account product kind).**
  **Rationale:** Folds into existing `monday workspace list`
  filtering if zero-cost; decision at M39 pre-flight.
- **`create_marketplace_app_discount`** (deprecates
  `grant_marketplace_app_discount`). **Rationale:** Marketplace-app-
  developer surface, not Monday-data surface. N/A — out of the
  CLI's scope permanently.

### Carry-forward backlog (continues from v0.6)

- **Multi-file `--set` per call** (v0.6-M38 D2 deferral —
  `multi_file_set_unsupported`). **Status:** v0.7.x candidate. Carries
  M42's bulk-file-set design dimensions per item rather than per
  call; revisit once M42 pins the per-item file-dispatch envelope.
- **`<file-col>=-` stdin support** (v0.6-M38 D7 deferral). **Status:**
  v0.7.x candidate. Needs clean `--filename` companion shape pinned
  first (`monday item upload` carries the same deferral).
- **Multi-level subitem creation** — slipped v0.3 → v0.4 → v0.5 → v0.6
  → v0.7. **Status:** STAYS deferred. Monday's `sub_items_board` still
  carries no `subtasks` column at API `2026-04` (per agent's 2026-05-18
  changelog research — no entry surfacing subtasks on multi-level
  boards). Re-probe at v0.7 kickoff to confirm; if data-model gap
  unchanged, slips to v0.8 → v0.9 → ... until Monday surfaces the
  capability.
- **Cross-board `item move` value-overrides** — slipped v0.3 → v0.4
  → v0.5 → v0.6 → v0.7. **Status:** STAYS deferred. Monday's
  `ColumnMappingInput` still carries no value slot at API `2026-04`.
- **Resumable cross-board cursor** — per-board cursor-lifetime under
  aggregation design issue unchanged. **Status:** STAYS deferred to
  v0.7.x or v0.8.
- **Profile-scoped argument defaults** — filed at v0.6 kickoff.
  **Status:** STAYS at v0.7.x candidate-selection backlog; carries
  its §13 carve-out Decision prerequisite forward.
- **OAuth app registration (`monday auth login`)** — placeholder
  guard from v0.3-M28. **Status:** STAYS deferred. Reopens only on
  clear user demand.

**Permanent non-goals (per cli-design §13 — unchanged from v0.6):**

- Hosting webhooks / app framework / GraphQL subscriptions /
  telemetry / forms / saved queries-as-aliases / `monday undo`.
- See [`docs/v0.6-plan.md`](./v0.6-plan.md) §1 "Permanent non-goals"
  for the full list. v0.7 inherits unchanged.

## 2. Pre-flight: what's already in place

> **Filled at v0.7 kickoff candidate-selection session 2026-05-18
> (this commit).**

**Working HEAD pin:** `a7a890d` (the SKELETON-filing commit at the
post-v0.6.0 publish session — `docs(future-plans): file v0.7 + v0.8
skeleton plans — Monday API 2026-04 + 2026-07 version bumps`). v0.6
release-prep cluster closed at `98185d4..51ba1a4`; v0.6.0 published
to npm on 2026-05-18T16:30:21Z.

**Post-v0.6.0 live numbers carried into v0.7 kickoff:**

- **Tests:** 4100 passing + 1 skipped across 172 files.
- **Coverage:** 99.26 / 96.46 / 99.31 / 99.52 (stmts / branches /
  fns / lines) at the 95 / 95.45 / 95 / 95 floor; **branches margin
  1.01pp** (up from 1.00pp at v0.5.0 — held the new floor across
  v0.6-M38 + release-prep without re-slipping).
- **29 stable ERROR_CODES.**
- **117 commands.**
- **`npm audit`:** 0 vulnerabilities (audit-fix `fast-uri 3.1.0 →
  3.1.2` already folded into the v0.6 version-bump commit).
- **`package.json` version:** `0.6.0`.

**SDK 15.x publication probe (kickoff session 2026-05-18):**

- `npm view @mondaydotcomorg/api versions --json | tail`: latest
  publish is `14.0.0` (2026-03-18T15:46:19.143Z). No `15.0.0`
  release; no `15.0.0-beta.*` pre-release either.
- SDK 14.0.0 bakes `AvailableVersions.CURRENT_VERSION = "2026-01"`
  (verified in `node_modules/@mondaydotcomorg/api/dist/cjs/lib/
  constants/index.d.ts`); this matches the CLI's current API pin
  at `src/api/client.ts:38`'s `PINNED_API_VERSION` re-export.
- **Monday SDK cadence reference points:**
  - `13.0.0` → `14.0.0`: 2026-01-20 → 2026-03-18 (≈2 months).
  - `12.0.0` → `13.0.0`: 2025-12-03 → 2026-01-20 (≈7 weeks).
  - Today (2026-05-18) sits **exactly 2 months** past `14.0.0`'s
    publish — SDK 15.x baking `2026-04` is expected within weeks,
    not months, on historical cadence.
- **M39 D1 closure (this session):** WAIT for SDK 15.x publication;
  defer M39 pre-flight until 15.x ships. See §3 M39 entry for the
  closure rationale + bounded-wait fallback structure.

**Foundations carrying forward into v0.7:**

- **SDK + API version pin.** Unchanged from v0.6 close — pinned at
  SDK 14.0.0 / API `2026-01`. M39 lifts both in lockstep once SDK
  15.x publishes. Override fallback documented at §3 M39 D1 entry +
  §6 R1.
- **Multipart transport seam (v0.4-M31).** Reused at v0.6-M38
  + carries forward to v0.7-M42 (bulk file `--set`) + v0.7-M43
  (create-time file `--set`). Per-item file dispatch + shared-
  transport semantics for `--concurrency` are the new design
  dimensions M42 introduces.
- **`file-source.ts` helper (R-v0.6-NEW-1).** 3 consumers post-
  v0.6-M38 lift (M31 `item upload` + M31 `update upload` + M38
  `executeFileColumnSet`); M42 + M43 likely become consumers 4 +
  5 — re-evaluates R-NEW-58 ahead-of-feat trigger threshold.
- **`details.reason` discriminator pattern (R-v0.6-NEW-2).** 4
  supporting instances at v0.6-M38; M42 + M43 likely add the 5th
  + 6th (per-item file-set failure reasons in bulk envelope +
  atomicity-envelope reasons in create two-leg). R-v0.6-NEW-2
  graduates at the 5th-consumer threshold.

## 3. Milestone sequencing

> **Per-milestone detail filled in at each milestone's pre-flight
> kickoff.** Skeleton entries below sketch the wire surface +
> known design dimensions; D1-D{N} decisions land at pre-flight
> contract diff commits.

**Sequencing decision (v0.7 kickoff refactor-audit 2026-05-18):**
ship order is **M42 → M43 → (re-probe SDK 15.x) → M39 → M40 → M41**.
Milestone IDs stay stable (M39-M43 numbering unchanged); only the
session-to-session ordering flips. Per-milestone entries below
remain in M39-M43 ID order for stable cross-referencing — read
the §1 status block for the active ship order. M42 + M43 ship
first because they're API-pin-independent v0.6.x carve-out folds
reusing v0.4-M31's multipart wire surface; M39 (API pin bump
2026-01 → 2026-04) gates on SDK 15.x publication OR an explicit
override decision at M43 close.

### M39 — API pin bump 2026-01 → 2026-04

**Wire surface:** No new wire ops (pin flip only). 475 `2026-01`
literal sites across `src/` + `tests/` + `docs/` flip to `2026-04`
in lockstep (mostly comments / probes / cassettes / prose; the
runtime pin lives in one place — `src/api/client.ts:38`'s
`PINNED_API_VERSION = AvailableVersions.CURRENT_VERSION` re-export).

**Open decisions at pre-flight:**

- **D1 — SDK upgrade vs string-literal override.** ✅ **Closed at
  v0.7 kickoff candidate-selection session 2026-05-18 (this
  commit): SEQUENCE-SHUFFLE.** Ship M42 → M43 first (API-pin-
  independent v0.6.x carve-out folds reusing v0.4-M31's multipart
  wire surface). At M43 close, re-probe SDK 15.x publication
  state + decide between (a) SDK 15.x lift (if shipped) / (b)
  string-literal override on SDK 14.0.0 + hand-rolled zod schemas
  for new 2026-04 fields (per `.claude/rules/monday-api.md`
  "Boundary-typing trap" pattern — already routinely applied for
  `BatteryValue` / `hierarchy_type` / `is_leaf` / `capabilities`)
  / (c) continued wait if both paths still look unattractive.
  Probe result at kickoff: latest `@mondaydotcomorg/api` publish
  is `14.0.0` (2026-03-18T15:46:19.143Z, bakes
  `CURRENT_VERSION = "2026-01"`); no 15.x release exists yet.
  **Closure rationale:** (a) v0.6.0 published today — no urgency
  on v0.7 timeline; (b) sequence-shuffle preserves optionality
  across all three fallback paths while delivering 2 milestones
  (M42 + M43) of v0.7 progress immediately under the current
  2026-01 / SDK-14.0.0 pin; (c) the override "hack" is cheaper
  than initially framed at candidate-selection scrutiny —
  `src/api/transport.ts:99`'s per-request `API-Version` header is
  already parameterised + overridable, and hand-rolled zod schemas
  are routine at every wire boundary per `validation.md` "parse,
  don't validate" (the SDK-generated types are useful build-time
  signal but not load-bearing — runtime always parses through
  zod) — so the M43-close re-decision is robust whether SDK 15.x
  ships or not. The bounded-fallback enumeration (continued wait
  / override / sequence-shuffle) carries forward structurally;
  sequence-shuffle was fallback (iii) selected at kickoff
  candidate-selection rather than after 2-4 sessions of waiting.
  Pattern noted in [[R-v0.7-NEW-1]].
- **D2 — Breaking change defensive sweep.** API 2026-04's one
  breaking change (`AggregateGroupByResult.value_*` collapsed to
  unified `value: JSON`) does NOT affect today's CLI surface
  (verified by `grep -r aggregate_group_by src/` returning empty
  per the agent's 2026-05-18 changelog research; re-verify at
  pre-flight). If a future milestone surfaces aggregate-group
  reads, that milestone owns the projection re-shape.
- **D3 — `Board.folder` + `Board.created_from_board_id` projection
  fold.** Zero-cost additive Board fields. Fold into `monday board
  get` + `monday board describe` projections at M39 if zero-cost;
  defer to v0.7.x optional candidate otherwise.
- **D4 — `update_board(item_nickname:)` + `workspaces.query_params(:)`
  filter folds.** Both zero-or-low-cost extensions to existing
  surfaces. Decision: fold into M39 envelope-snapshot refresh OR
  defer to v0.7.x.

**IMPL Codex review estimate:** 1-2 fix-up rounds (mechanical
cluster — primary risk is the mass-flip mass-noisy step).

**R-class triggers anticipated:** None at M39 by default (no novel
wire shape, no new helper lift).

### M40 — `monday item set-description <iid>`

**Wire surface:** `set_item_description_content` mutation — sets
the item's description block content (single-call). Wire payload
likely accepts an HTML string OR a doc-block-content JSON payload
shape; clarified at pre-flight empirical probe.

**Empirical probe required at pre-flight** (per workflow.md
"Empirical probe for any novel API surface"):

- Wire-shape probe — what payload shapes does
  `set_item_description_content` accept (markdown / HTML / doc-block
  content JSON)? Mirrors v0.5-M37's MAX_DOC_IMPORT_PAYLOAD_BYTES
  probe structure.
- Size-limit probe — does the wire side cap content size? At what
  threshold? (Likely mirrors M37's 250KB-OK / 500KB-rejected
  threshold given doc-content shared infrastructure; verify.)

**Open decisions at pre-flight:**

- **D1 — Argv source pattern.** `--content <file|-> \|
  --content-string <s>` mutex (mirrors M37's pattern + reuses
  `readSourceContent` helper at `src/utils/source-content.ts`).
  Confirmed at pre-flight after probe pins the payload shape.
- **D2 — `--dry-run` envelope shape.** Mirrors M37 `monday doc
  append-markdown --dry-run` verbatim: `planned_changes:
  [{operation: 'set_item_description_content', item_id,
  content_format, content_size_bytes}]`. Standard envelope; no
  new contract surface.
- **D3 — ERROR_CODES delta.** Likely zero (registry stays at 29).
  Rejections route through existing `usage_error` /
  `validation_failed` / `not_found` with `details.reason`
  discriminators. Confirmed at pre-flight.

**IMPL Codex review estimate:** 2-3 fix-up rounds (median IMPL
cadence for single-mutation milestones per v0.5-M34 / v0.6-M38
precedent).

**R-class triggers anticipated:**
`readSourceContent` consumer count goes 5 → 6 (M13 update verbs
+ M37 doc verbs + M40); no graduation impact (helper already
shipped at M37 IMPL kickoff).

### M41 — `monday doc block-create-bulk <did>`

**Wire surface:** `create_doc_blocks` mutation — bulk-create up
to 25 doc blocks per call. Closes the per-call efficiency gap in
M36's per-block CRUD (where N blocks = N round-trips).

**Empirical probe required at pre-flight:**

- Wire-shape probe — payload shape for the 25-block max. Likely
  an array of M36-shaped block records; verify exact shape +
  per-block error semantics (does a single bad block reject the
  whole batch, or does the wire return per-block partial
  success?). Probe drives the §6.1 envelope shape decision.

**Open decisions at pre-flight:**

- **D1 — Per-block partial-success vs all-or-nothing envelope
  shape.** If the wire returns per-block partial success
  (`failed_blocks` / `successful_blocks` similar to M34's team
  mutations), project to the universal §6.1 `data.results:
  [{block_index, ok, ...}]` shape. If all-or-nothing, return a
  flat success envelope with the created `blocks` array. Probe
  pins this.
- **D2 — Mutex with M36 `block-create`.** Single-block create stays
  on the M36 single-block verb; M41 is the bulk variant. Mirrors
  v0.3-M25 `update --where --continue-on-error` pattern where the
  bulk verb is orthogonal to the single-item one.
- **D3 — `--blocks <json>` parse-boundary size cap.** Per the M37
  pattern: pre-empt at parse boundary if the wire's max is fixed
  (25 blocks = some byte threshold). Probe pins the threshold.
- **D4 — ERROR_CODES delta.** Likely zero. Bulk-specific
  rejections route through `usage_error` / `validation_failed`
  with `details.reason: 'block_batch_too_large'` /
  `'block_validation_failed'` discriminators.

**IMPL Codex review estimate:** 3-4 fix-up rounds (one above median
— the partial-success projection design dimension typically
surfaces an extra fix-up round per v0.5-M34 precedent).

**R-class triggers anticipated:**
Per-batch partial-success projection (`projectBlockBatchResults`)
likely 1-2 consumers at M41 close; R-NEW-58 ahead-of-feat trigger
fires only if a 3rd consumer site is anticipated within v0.7 scope.

### M42 — Bulk file `--set` carve-out fold (D5 closure from v0.6-M38)

**Wire surface:** `add_file_to_column` per-item, dispatched across
the `--where`-resolved item-id set with the M25 partial-success
envelope. No new wire op; reuses v0.4-M31's multipart wire +
v0.6-M38's `executeFileColumnSet` runtime body verbatim.

**Empirical probe NOT required at pre-flight** (wire surface is
known + cassette-pinned at v0.4-M31; novelty is in the per-item
file dispatch + `--concurrency` interaction, which is
implementation work, not wire-shape work).

**Open decisions at pre-flight — CLOSED at M42 pre-flight contract
diff (this commit):**

- **D1 — `--concurrency` semantics for file dispatch.** ✅
  **Closed at v0.7-M42 pre-flight contract diff: reuse v0.4-M30's
  `dispatchParallel` over a shared `MultipartTransport`.** Each
  parallel worker constructs its own `MultipartTransportRequest`
  per call; the transport itself is connection-pool-shared
  per-token. Range pinned to `[MIN_CONCURRENCY,
  MAX_CONCURRENCY]` (1..32) — same range as M30's JSON-bulk
  parallel dispatch. **No new probe required:** v0.4-M30's
  concurrency probe (`scripts/probe/m30-concurrency.report.txt`)
  pinned the per-token wire-side rate-limit semantics + M31's
  multipart probe (`scripts/probe/m31-asset-upload.report.txt`)
  pinned the multipart wire shape; the intersection (parallel
  multipart over shared transport) is the cross-product of two
  already-pinned surfaces. Re-probing at M42 IMPL is allowed if
  IMPL surfaces an unexpected interaction, but it's not a
  pre-flight gate.

- **D2 — Per-item asset slot in envelope.** ✅ **Closed at v0.7-
  M42 pre-flight contract diff: per-item
  `data.results[i].asset: { id, name, ... }` echo on success
  (mirrors M31's `itemUploadOutputSchema`'s `asset` slot);
  per-item failure surfaces as
  `data.results[i].error: { code, message }` per M25 partial-
  success.** Aggregate
  `data.summary.{matched_count, applied_count, failed_count,
  board_id, column_id, filename, file_size_bytes}` extends M25's
  `partialSuccessBulkUpdateDataSchema` with file-dispatch slots
  (`column_id` / `filename` / `file_size_bytes`) so an agent
  reading the success envelope sees which file was dispatched +
  where. `operation: 'item_update_bulk_file_set'` literal
  discriminator (distinct from M25's `'item_update'`) so agents
  can branch uniformly on `data.operation`. Schemas defined at
  pre-flight in `src/commands/item/update.ts`
  (`bulkFileSetResultSchema` + `bulkFileSetDataSchema`); runtime
  body shipped at M42 IMPL (`22df2fa` + R1-R3 fix-ups).

- **D3 — Per-item file pre-check timing.** ✅ **Closed at v0.7-
  M42 pre-flight contract diff: single upfront `precheckLocalFile`
  call BEFORE the per-item dispatch loop starts.** The bulk
  shape has ONE file path (the value of the file `--set`) shared
  across N matched items; pre-checking it once upfront is both
  cheaper AND consistent with cli-design §5.8's "pre-checks MUST
  fire BEFORE any wire round-trip" atomicity discipline. A failed
  pre-check surfaces upfront as `usage_error` with
  `details.reason: 'file_not_readable'` / `'file_empty'`
  (mirrors M31 single-item shape) — this is whole-call-abort
  REGARDLESS of `--continue-on-error`. The
  `--continue-on-error` flag partitions ONLY the wire-dispatch
  failures (per-item `add_file_to_column` rejections from
  Monday), never the local file pre-check. No per-item
  pre-check needed because the same path applies to every
  matched item.

- **D4 — ERROR_CODES delta.** ✅ **Closed at v0.7-M42 pre-flight
  contract diff: ZERO net change. Registry stays at 29.** All
  M42-specific rejections route through existing codes:
  - Local file pre-check failures → `usage_error` with
    `details.reason: 'file_not_readable'` / `'file_empty'`
    (existing M31 discriminators reused).
  - Mutex violations → `usage_error` with
    `details.reason: 'multi_file_set_unsupported'` /
    `'mixed_file_and_value_sets'` (existing M38 discriminators,
    now universal across single + bulk).
  - Per-item wire failures under `--continue-on-error` →
    per-record `error: { code, message }` shape with whatever
    code Monday returned (mirrors M25's partial-success shape).
  - Per-item wire failures under fail-fast → top-level error
    with `details.applied_to: [<item_ids>]` decoration (mirrors
    v0.1 fail-fast bulk update).

  The `'file_set_on_bulk_unsupported'` discriminator literal at
  `src/api/file-column-set.ts` no longer surfaces from the
  runtime path post-M42 carve-out fold; the literal stays
  RESERVED across the codebase (do not reuse for a different
  rejection reason; agent scripts that branch on it will fall
  through to the new dispatch path on v0.7-M42 and later).

**Pre-flight stub structure (this commit).** Per R-NEW-76:

- argv parse (commander → `parseArgv` → zod) — shipped contract.
- `validateInputShape` (single vs bulk discriminator) — shipped
  contract.
- `setEntries` / `rawEntries` parse — shipped contract.
- `loadBoardMetadata` (board-metadata fetch) — shipped contract.
- `preCheckM38FileDispatch({callShape: 'item_update_bulk'})` —
  shipped contract; now returns `kind: 'file_bulk'` on the
  clean dispatch path (was throwing `'file_set_on_bulk_unsupported'`
  at v0.6-M38).
- `buildQueryParams` + items_page walk — shipped contract.
- Empty-match short-circuit + confirmation gate — shipped
  contract.
- Per-item dispatch loop + envelope emit — at pre-flight, this
  was a **c8-ignored stub** throwing `internal_error` with
  `details.reason: 'm42_preflight_stub'`. **Lifted at M42 IMPL
  (`22df2fa`)**; the c8-ignore wrapper dropped and the runtime
  body now runs `precheckLocalFile` → per-item
  `executeFileColumnSet` (sequential or `dispatchParallel`) →
  `invalidateBoard` → envelope emit. The
  `'m42_preflight_stub'` discriminator literal stays RESERVED
  across the codebase post-IMPL (regression-guarded by an
  integration test).

The pre-flight stub `internal_error` code (rather than
`usage_error`) was deliberate: the argv was accepted at
pre-flight (parse succeeded), the issue was that the wire
dispatch wasn't shipped yet — that's a server-side / internal
gap, not a user-input problem. Post-IMPL the runtime body
emits the proper success / `--continue-on-error` partial-success
/ fail-fast `applied_to`-decorated envelopes instead.

**IMPL Codex review actual:** 8 fix-up rounds total. R1 closed
the behavioral surface (P1-1 foldAndRemap + P2-1
SourceAggregator + P2-2 fail-fast partial-success invalidate +
P3-1 caller prose × 3 + P3-2 +3 tests). **R2-R8 were
W9-prose-only** (no behavioral findings) — Codex's regex sweep
caught a wider layer of adjacent prose each round, escalating
from 3 sites (R2/R3) → 8+3-proactive (R4) → 5+1-test-strengthen
(R5) → 9 (R6) → 4+workflow-graduation (R7) → 7+workflow-refine
(R8). The asymptotic prose convergence pattern motivated the
R-v0.7-NEW-4 graduation (see §22 below) of a **Pre-IMPL
contract-term checklist for cross-doc grep** discipline at
`.claude/rules/workflow.md`. R8 verdict was "improves the named
R7 sites, but W9 not clean yet" — accepted as residual at user
direction; the workflow.md graduation is the load-bearing
remediation for v0.7-M43 onwards.

**R-class triggers actual:**

- `file-source.ts` `precheckLocalFile` consumer count went 3 → 4
  at M42 IMPL (M31 item upload + M31 update upload + M38 item
  set / item update single + M42 bulk item update).
  R-v0.6-NEW-1 watch-item — already-shipped helper scales
  cleanly to consumer 4. **Graduation expectation unchanged** —
  fires at 5th-consumer threshold (v0.7-M43 create-time fold
  likely tips it).
- Per-item file-failure projection helper —
  `runItemUpdateBulkFileDispatch` inlines the fold rather than
  extracting it. **R-NEW-58 ahead-of-feat 2-consumer trigger
  NOT yet fired** — M43 pre-flight decides whether to lift
  (likely yes if M43's atomicity-envelope shape echoes per-leg
  `asset` records).
- R-v0.6-NEW-2 `details.reason` discriminator pattern: zero new
  reasons added at M42 IMPL (per D4 zero-delta closure —
  existing `file_not_readable` / `file_empty` /
  `multi_file_set_unsupported` / `mixed_file_and_value_sets`
  cover every M42 rejection shape). **Graduation expectation
  unchanged** — fires at 5th-consumer threshold, likely at M43
  atomicity-envelope shape if `create_then_file_upload_partial_
  failure` lands as planned per the §3 M43 D3 closure.
- **NEW R-v0.7-NEW-3** filed at `3328a42` (post-pre-flight
  refactor-audit) — pre-flight stub-body-internal pattern.
  Open question at filing: "is `'m42_preflight_stub'` a 5th
  supporting instance of R-v0.6-NEW-2?" **Resolution at M42
  close-docs:** no, stub-flavoured discriminators count
  separately because they're explicitly transient (disappear
  at IMPL when the stub body lifts). R-v0.6-NEW-2 graduation
  expectation stays unchanged.
- **NEW R-v0.7-NEW-4** filed at R7 (`8f2832b`), refined at R8
  (`08ae263`) — Pre-IMPL contract-term checklist for cross-doc
  grep. Graduated immediately at v0.7-M42 IMPL (rather than
  waiting for a 2nd consumer per R-NEW-58 norms) because the
  7-round W9 escalation made the pattern unambiguously load-
  bearing. v0.7-M43 inherits the checklist. See §22 below for
  the full register entry.

**Coverage residual.** Final coverage 98.86 / 95.82 / 99.16 /
99.15 (stmts / branches / fns / lines) at floor 95 / 95.45 /
95 / 95. **Branches margin 0.37pp** (was 0.58pp at pre-flight;
1.01pp at v0.6.0 baseline). The 0.21pp drop from pre-flight to
IMPL is from new conditional-spread arms on the foldAndRemap-
decorated ApiError ctor (P1-1 fix) plus the SourceAggregator
seed-vs-record branching (P2-1 fix). Pattern matches the JSON-
bulk fail-fast precedent at `update.ts:1334-1361` which has
identical uncovered spread arms unchanged from v0.4-M30 /
v0.3-M25. A lock-step ratchet across both code paths would
recover ~0.5pp but is out of scope for M42 IMPL — flagged at
the M42 helper docstring's "future lift candidate" section
(R-NEW-58 2-consumer trigger; lift fires at the 3rd consumer
which M43 create-time fold may add). Filed as
[[R-v0.7-NEW-5]] at M42 IMPL close-docs (§22 below).

### M43 — Create-time file `--set` carve-out fold (D6 closure from v0.6-M38)

**Wire surface:** Two-leg dispatch — `create_item` (with placeholder
column_values for the file column) followed by
`add_file_to_column` (post-create file dispatch). Non-atomic by
construction; pre-flight Decision pins the atomicity-envelope shape
per cli-design §5.8.

**Empirical probe required at pre-flight:**

- Cleanup-on-failure probe — what happens to the created item if
  the post-create `add_file_to_column` leg fails? Does Monday
  GC the item, or does it leak? Drives the atomicity-envelope
  shape (rollback vs orphan-warn vs leave-as-is per cli-design
  §5.8).

**Open decisions at pre-flight:**

- **D1 — Atomicity envelope shape.** Three candidate shapes:
  (a) Best-effort rollback: failed file leg triggers a `delete_item`
      cleanup leg. Pre-flight Codex review will ask whether
      delete-after-create is safe (Monday's `delete_item` is
      destructive-permanent; agents have no recovery path if rollback
      itself fails).
  (b) Orphan-warn: failed file leg surfaces `internal_error` with
      `details.created_item_id` echo + `details.hint` pointing at
      the orphaned item (agents drive their own cleanup).
  (c) Leave-as-is: failed file leg surfaces `internal_error` with
      details but doesn't echo the orphan; relies on agents to
      diagnose via `monday item list`.
  Probe drives the cleanup-leg viability; if (a) is safe, default
  to (a); otherwise (b).
- **D2 — Dry-run envelope shape.** Mirrors v0.6-M38's `--dry-run`
  envelope verbatim plus a second `planned_changes` entry for the
  post-create file leg (`operation: 'add_file_to_column'` after
  `operation: 'create_item'`).
- **D3 — ERROR_CODES delta.** Likely zero. Atomicity failures route
  through `internal_error` with `details.reason:
  'create_then_file_upload_partial_failure'` discriminator.

**IMPL Codex review estimate:** 4-5 fix-up rounds (similar
profile to M42).

**R-class triggers anticipated:**

- Two-leg atomicity-envelope helper potentially shared across M11's
  `item move` cross-board, M43's create+file, and any future
  non-atomic mutation. R-NEW-58 ahead-of-feat trigger fires only if
  a 3rd consumer site is anticipated within v0.7 scope.

### v0.7 release-prep (placeholder — opens at the next session after M43 IMPL closes)

Mirrors v0.6 release-prep cluster shape verbatim (5 commits with
diff + 1 zero-diff envelope-snapshot probe): ToC audit +
`deferred_to: "v0.7"` slip (R-NEW-82 graduated discipline; 5th-
consecutive consumer expected) + README v0.7 quickstart + scope
refresh + version bump 0.6.0 → 0.7.0 + audit-fix if `npm audit`
flags + CHANGELOG `[0.7.0]` entry + close-docs sweep. R-NEW-84
graduated discipline applies (skip Codex review on the mechanical
cluster).

## 4. Cross-cutting "definition of done" per command

Identical to v0.5-plan §4 / v0.6-plan §4. Each new surface needs:

1. Unit test coverage at the floor (95.45% branches).
2. Integration tests against cassette-pinned wire responses.
3. E2E test against the fixture-server subprocess.
4. Envelope-snapshot pin for every success + failure shape.
5. `output-shapes.md` per-verb section.
6. `cli-design.md` §4.3 row.
7. README quickstart example IF the surface is high-visibility
   (M40 / M41 likely; M42 / M43 fold into M38's existing example).
8. CHANGELOG entry at release-prep.

See [`docs/v0.6-plan.md`](./v0.6-plan.md) §4 for full detail.

## 5. Testing strategy

Identical to v0.6-plan §5. Five test layers: unit, integration
(in-process `FixtureTransport` + `MultipartFixtureTransport`),
E2E (subprocess against fixture server), envelope-shape snapshot
suite, published-tarball E2E.

See [`docs/v0.6-plan.md`](./v0.6-plan.md) §5 for full detail.

## 6. Risk register

> **Per-milestone risks land at each milestone's pre-flight
> kickoff.** Skeleton entries:

- **R1 — SDK 15.x publication timing.** If SDK 15.x baking
  `2026-04` is not published at v0.7 kickoff, M39 D1 decides
  between waiting (release blocked on external timeline) vs
  string-literal override on SDK 14.0.0 (maintenance burden on
  hand-rolled types for new 2026-04 fields). Mitigation:
  re-check npm at every session start; the override path is
  always available.
- **R2 — 2026-04 breaking-change blast radius.** The single
  breaking change (`AggregateGroupByResult.value_*` collapse) is
  unused by current CLI surface per the agent's 2026-05-18
  changelog research; re-verify at M39 pre-flight via
  `grep -r aggregate_group_by src/`. Mitigation: defensive
  sweep at M39 catches any introduced consumer in the v0.6 →
  v0.7 interim.
- **R3 — Empirical-probe surprises on novel wire surfaces.**
  M40 (`set_item_description_content`) + M41 (`create_doc_blocks`)
  + M43 (two-leg atomicity) carry probe-driven design decisions
  at pre-flight. Mitigation: budget 1-2 extra IMPL rounds per
  milestone if probes surface wire-shape surprises (per v0.5-M37
  precedent's custom-OBJECT projection pattern).
- **R4 — Bulk file `--set` `--concurrency` interaction.** M42's
  concurrent multipart over shared MultipartTransport is the
  largest novelty in v0.7 IMPL scope; per-token rate-limit
  semantics at the wire side are pre-flight probe targets.

## 7. v0.7 release exit checklist

> **Filled at v0.7 release-prep close.** Mirrors v0.6-plan §7
> verbatim — every cluster-item ticked + post-publish flip
> commit.

## 8. Decisions (open questions to close before each milestone)

> **Per-milestone D-list lands at each milestone's pre-flight
> contract diff commit.** Skeleton above (§3) sketches the
> expected decision dimensions per milestone.

## 9. Preconditions

> **Per-milestone preconditions lands at each milestone's
> pre-flight kickoff.** Skeleton above (§3) sketches the
> expected empirical-probe targets per milestone.

## 10. Post-mortem template

Mirrors v0.5-plan §10 / v0.6-plan §10 verbatim. Per-milestone:
round-by-round Codex review summary + contract drift findings +
lessons learned + R-class candidate filings. Lands at the
milestone close-docs commit.

See [`docs/v0.6-plan.md`](./v0.6-plan.md) §10 for full detail.

## 22. R-class register

> **Status:** populated at v0.7-M42 IMPL post-close refactor-audit
> (this commit) — close-docs sweep at `8878745` updated §3 + CLAUDE.md
> only; the §22 R-class register fill follows the v0.6-M38 post-close
> audit precedent (`aa10744`). Mirrors v0.6-plan §22 structure.
> Subsequent M40 / M41 / M43 close-docs (+ post-close audits)
> append to the same sections.

### Watch-items carried forward from v0.6

- **R-NEW-58 lift-ahead-of-feat** (graduated v0.4-M31 IMPL). Fires
  at every IMPL kickoff scan. Expected M40-M43 targets: file-source
  consumer count scaling (3 → 4+ at M42 / M43), batch-projection
  helper for partial-success envelopes at M41 / M42, atomicity-
  envelope helper at M43. **M42 IMPL outcome:** the per-item
  projection helper inlines the fail-fast error-decoration block
  rather than lifting to a shared helper (2-consumer threshold —
  R-NEW-58 norms wait for 3rd consumer); filed forward as
  [[R-v0.7-NEW-5]] watch-item with the in-code "Future lift
  candidate" docstring at `src/commands/item/update.ts:1621-1627`
  pointing at it. M43 create-time fold likely tips to 3rd consumer.
- **R-NEW-72 post-fix-up cross-doc grep** (graduated v0.4-M33; 8th
  instance ratified v0.5-M37 IMPL). Search paths per v0.5-NEW-19:
  `src/api/*.ts` + `src/commands/**/*.ts` + `src/commands/index.ts`
  + `docs/*.md`. Noun-stem regex matching. **9th-12th instances
  ratified at v0.7-M42 IMPL R1-R8** — each Codex round triggered a
  fresh post-fix-up grep, and rounds R2-R8 each surfaced a wider
  W9-prose layer the prior round's regex missed (the asymptotic
  convergence pattern that drove [[R-v0.7-NEW-4]]'s graduation).
  The discipline holds; what scaled was the term checklist
  (R-v0.7-NEW-4 extends R-NEW-72 with a pre-IMPL contract-term
  enumeration so each round's grep starts from a richer base
  rather than re-deriving from round-N findings).
- **R-NEW-75 candidate-selection 5-dimension framework**
  (graduated v0.4-M33). **Applied for 5th consecutive cycle at
  v0.7 kickoff session 2026-05-18 (this commit).** The kickoff
  session collapsed to D1 closure only because the v0.7-plan
  SKELETON (`a7a890d`) had pre-selected the M39-M43 candidate
  set forward — the 5-dimension scoring re-verified each
  milestone still scored as predicted at SKELETON-filing time.
  Sub-pattern (SKELETON-pre-selected kickoff collapses to
  Decision-closure-only) carries forward as [[R-v0.7-NEW-2]].
  **Caveat surfaced during candidate-selection scrutiny at this
  kickoff:** the SKELETON-pre-selected collapse can SKIP
  dependency-graph re-examination of the ship-order question
  (M42 + M43 are API-pin-independent and ship first without
  M39's external-dep gate — but the original framing landed on
  M39 first because of milestone-ID order rather than dependency
  order, and only the override-cost-scrutiny reframing surfaced
  the sequencing optimization). When SKELETON pre-selects, the
  candidate-selection collapse still owes the framework's
  *ordering* dimension — separately from the kept-vs-cut
  candidate dimension. Sub-pattern correction: at SKELETON-
  pre-selected kickoffs, score the SEQUENCING dimension across
  selected candidates (dependency graph, external-dep gating,
  carry-forward-fold timing) even when the candidate SET is
  pre-pinned. Recurs as a 2nd-consumer trigger when the next
  SKELETON-pre-selected kickoff (v0.8 kickoff) hits a similar
  sequencing-vs-set-selection ambiguity.
- **R-NEW-76 parseArgv-before-c8** (graduated v0.5-M34). Pre-flight
  stub discipline; applies to every M40/M41/M42/M43 pre-flight stub.
  **Applied at v0.7-M42 pre-flight (`160330b`)** — argv parse +
  `parseBrandedListArg` + `enforceDestructiveGate` + `parseGlobalFlags`
  fired BEFORE the c8-ignored stub body; the three INTERNAL
  conventions inside that body crystallized as [[R-v0.7-NEW-3]]
  (resolved at this close-docs; see below).
- **R-NEW-82 release-prep deferred-to slip audit** (graduated v0.5
  release-prep; ratified 4th-consecutive at v0.6 release-prep).
  Expected 5th-consecutive consumer at v0.7 release-prep —
  `deferred_to: "v0.7"` cross-doc grep.
- **R-NEW-84 skip Codex review on mechanical clusters** (graduated
  v0.5-M34 negative; ratified at v0.5 + v0.6 release-prep).
  **Applied at v0.7 kickoff session 2026-05-18 (`75b1cf9`) —
  docs-only mechanical cluster, gates carried verification, Codex
  skipped.** **Re-applied at v0.7-M42 IMPL close-docs (`8878745`)**
  — `CLAUDE.md` + `docs/v0.7-plan.md` only, zero `src/**/*.ts`
  changes. Expected further application at v0.7 release-prep + at
  M39 (mechanical pin-bump cluster).
- **R-v0.6-NEW-1 file pre-check + Blob-construction helper**
  (shipped v0.6-M38). 3 consumers post-lift; M42 + M43 likely
  become consumers 4 + 5. **M42 IMPL confirmed as 4th consumer**
  (`runItemUpdateBulkFileDispatch` calls `precheckLocalFile` once
  upfront per D3 closure + `buildBlobFromPath` per-item inside
  the dispatch loop). Helper scaled cleanly to consumer-4 with
  zero internal-shape changes. Graduation expected at 5th-consumer
  threshold (v0.7-M43 create-time fold likely tips it).
- **R-v0.6-NEW-2 `details.reason` discriminator pattern.** 4
  supporting instances at v0.6-M38; M42 + M43 likely add 5th + 6th.
  Graduation expected at 5th-consumer threshold. **M42 IMPL added
  zero new reasons** per D4 zero-delta closure — existing
  `file_not_readable` / `file_empty` / `multi_file_set_unsupported`
  / `mixed_file_and_value_sets` reasons cover every M42 rejection
  shape (M38's mutex rules apply uniformly to single + bulk).
  The `'m42_preflight_stub'` literal IS a supporting-instance
  candidate but stub-flavoured discriminators count separately
  (see [[R-v0.7-NEW-3]] resolution below). **Graduation
  expectation unchanged** — still waiting on 5th-consumer; likely
  at M43 atomicity-envelope shape if `create_then_file_upload_
  partial_failure` lands as planned per §3 M43 D3 closure.

### New R-class candidates surfaced at v0.7

Additional R-class candidates surfaced at the v0.7 kickoff
candidate-selection session (this commit):

- **R-v0.7-NEW-1 — External-dependency wait-with-bounded-fallback
  Decision-closure structure.** First explicit instantiation of a
  Decision-closure shape that gates a milestone on an
  unpublished external dependency: enumerate the external blocker
  (SDK 15.x publication) + the fallback paths (continued wait /
  string-literal override / sequence-shuffle to independent
  milestones) + a bounded re-evaluation cadence (re-probe at
  every session start, re-decide at 2-4 sessions if blocker
  persists) + a documented mitigation in the §6 risk register.
  1 supporting instance — v0.7-M39 D1 closure at v0.7 kickoff
  2026-05-18 (this commit). The "Next session" pointer in
  `CLAUDE.md` codifies the re-probe instruction inline
  (`npm view @mondaydotcomorg/api versions --json | tail`) so
  the next session re-evaluates without re-deriving the probe
  shape. **Structure validated by intra-session reconsideration
  during candidate-selection:** an initial WAIT framing was
  reconsidered within the same kickoff session after follow-up
  scrutiny on override costs surfaced that the sequence-shuffle
  fallback was strictly dominant (zero override commitment + 2
  milestones of progress + preserves optionality across all three
  fallback paths). Fallback (iii) [sequence-shuffle to M42 → M43
  first] was selected at re-scoring rather than after 2-4
  sessions of waiting. **Lesson:** the bounded-fallback
  enumeration's value isn't only inter-session timescale survival
  — the pre-enumerated fallback paths also let the closure handle
  intra-session reconsideration cleanly without ripping out the
  structure (the §1 status block's SKELETON-ratification slot
  carries the same closure surface regardless of which fallback
  path is selected). **Priority: LOW** — process
  discipline. Fires at 2nd consumer when another milestone
  Decision gates on an external blocker (next plausible site:
  v0.8-M44 if Monday API `2026-07`'s user-entity migration
  timeline depends on SDK 16.x publication; speculative). At
  3rd consumer, graduate to a `.claude/rules/workflow.md`
  "External-dep wait closure" entry alongside the existing
  milestone-workflow disciplines, with the intra-session-
  reconsideration property captured as a key feature.
  Cross-references R-NEW-75 (candidate-selection framework — the
  external-dep blocker dimension wasn't present at R-NEW-75's
  v0.4-M33 graduation; this entry extends the framework's
  decision-closure side with a new closure-shape; the missed
  sequencing-dimension caveat on R-NEW-75's updated entry is
  what the immediate fallback trigger surfaced) + R-v0.7-NEW-2
  (forward-filed SKELETON enables the bounded-wait fallback —
  the v0.7-plan §1 status block's SKELETON-ratification cleanly
  carries the D1 closure stance forward across sessions, and
  also cleanly carries the closure-flip when fallbacks trigger).
- **R-v0.7-NEW-2 — Forward-filed SKELETON plan-doc dual filing
  at post-release session.** First instance of filing multiple
  version-bump plan-docs forward in SKELETON form at a post-
  release session, before any of those versions enters its own
  kickoff cycle. `a7a890d` filed `docs/v0.7-plan.md` +
  `docs/v0.8-plan.md` together at the post-v0.6.0 publish
  session (2026-05-18), enumerating in-scope milestones +
  explicit out-of-scope rationale + carry-forward backlog +
  per-milestone D-skeleton sections for each — all subject to
  ratification at the corresponding version's kickoff. The
  status-block flow ("SKELETON" → "SKELETON ratified at
  `<sha>` — D1 closed `<stance>`") provides a stable handoff
  shape that survives across multiple sessions: prior plan-docs
  (`docs/v0.6-plan.md` first filed at `0cb8b69` M38 pre-flight,
  not pre-kickoff) didn't use this pattern. 2 supporting
  instances at `a7a890d` (v0.7-plan SKELETON + v0.8-plan
  SKELETON filed together). **Priority: LOW** — process
  discipline; benefit is amortised across multi-version planning
  horizons. Fires at 2nd ratification cycle (i.e., when v0.8
  kickoff ratifies the v0.8-plan SKELETON, expected post-v0.7.0
  publish), which IS the validation event — if the forward-
  filed v0.8 SKELETON survives ratification with minimal
  rework, the pattern earns its keep; if v0.8 kickoff finds
  the SKELETON materially stale, the pattern earns a discount
  factor. At v0.8 ratification, graduate to a
  `.claude/rules/workflow.md` "Forward-filed SKELETON plan-docs"
  entry (with the post-release-session timing + ratification-
  at-kickoff handoff shape) IF the v0.8 SKELETON survives
  cleanly; OR file a learnings sub-entry capturing the
  material-staleness category if it doesn't. Cross-references
  R-NEW-75 (candidate-selection framework — SKELETON-pre-selected
  kickoff collapses the framework's full 6-step session shape
  to D-closure-only as documented in R-NEW-75's updated entry
  above) + R-v0.7-NEW-1 (the bounded-wait Decision-closure
  shape relies on the SKELETON's stable §1 + §3 surface to
  carry D-closure stances across sessions).
- **R-v0.7-NEW-3 — Pre-flight stub-body-internal contract
  pattern.** Sub-discipline of R-NEW-76 (parseArgv-BEFORE-c8).
  R-NEW-76 covers the BOUNDARY around the c8-ignored stub
  body (argv parse + parse-boundary helpers fire BEFORE the
  ignore block); R-v0.7-NEW-3 covers the THREE INTERNAL
  conventions inside the stub body that surfaced at v0.7-M42
  pre-flight (`160330b`):

  1. **Async-signature anchor via `await Promise.resolve()`.**
     Pre-flight stub helpers declared `async` (to anchor the
     IMPL's `async` signature so callers don't have to re-shape)
     need at least one `await` to satisfy
     `@typescript-eslint/require-await`. A single `await
     Promise.resolve()` at the top of the c8-ignored body is
     the minimal-noise workaround — preserves the async
     contract, costs one micro-task tick (irrelevant at the
     stub-throw level), satisfies lint, and self-documents as
     "IMPL replaces this with real `await` calls". Tested at
     `src/commands/item/update.ts:1729-1734` in the M42 stub.
  2. **`details.reason: '<milestone>_preflight_stub'`
     discriminator literal + `details.milestone` companion
     slot.** Pre-flight stub throws should surface
     `internal_error` (not `usage_error` — argv IS accepted at
     pre-flight; the gap is server-side) with a milestone-
     tagged `details.reason` literal (`'m42_preflight_stub'`,
     `'m43_preflight_stub'`, etc.) AND a `details.milestone`
     slot echoing the milestone ID (`'v0.7-M42'`). Agents
     reading the stub-throw envelope branch uniformly on
     `details.reason` AND get the milestone tracker for grep.
     The two-slot convention beats a single multi-purpose slot
     because `details.reason` is the existing discriminator
     pattern (R-v0.6-NEW-2 4 supporting instances) and
     `details.milestone` is a clean ID slot for milestone
     lookup. Tested at `src/commands/item/update.ts:1748-1762`
     in the M42 stub.
  3. **Integration test shape: assert
     `internal_error.details.reason === '<milestone>_preflight_
     stub'` after exercising the FULL shipped argv + parse +
     pre-check + (where applicable) wire-fetcher surface.**
     The test serves as a "reaches-the-stub" probe: if the
     argv shape is wrong, the stub never fires (the parse
     surface rejects first); if the shipped pre-check fires
     incorrectly, the stub never fires (the pre-check rejects
     first); only when the FULL shipped surface exercises
     cleanly does the test see the stub-throw. This shape is
     stricter than the equivalent v0.6-M38 D5 rejection
     assertion (which fired before the items_page walker —
     no wire shape assertion) and forces the test to wire the
     full cassette set (BoardMetadata + ItemsPage + ...).
     Tested at
     `tests/integration/commands/item-update-bulk.test.ts:3480-3563`
     in the M42 stub.

  **1 supporting instance** — v0.7-M42 pre-flight at `160330b`.
  **Priority: LOW** — process discipline; sub-discipline of an
  already-graduated rule (R-NEW-76). Fires at 2nd consumer
  when the next pre-flight stub with a c8-ignored runtime body
  lands (likely v0.7-M43 create-time file `--set` two-leg
  dispatch, immediately after M42 IMPL closes). At 3rd consumer
  (likely v0.7-M40 or v0.7-M41 if either ships pre-flight stubs
  with c8-ignored bodies), graduate to a sub-bullet under
  R-NEW-76's `.claude/rules/workflow.md` entry rather than a
  standalone entry — the three internal conventions are tightly
  coupled to R-NEW-76's boundary discipline and read better
  together than apart. Cross-references R-NEW-76
  (parseArgv-BEFORE-c8 — this entry covers the INTERNALS the
  R-NEW-76 boundary wraps) + R-v0.6-NEW-2 (`details.reason`
  discriminator pattern — extension instance for the stub-throw
  variant). **Question for M42 IMPL session:** the
  `'m42_preflight_stub'` discriminator IS a 5th supporting
  instance of R-v0.6-NEW-2's `details.reason` pattern OR
  should stub-flavoured discriminators count separately
  (because they're explicitly transient — they disappear at
  IMPL when the stub body lifts)? Resolution at M42 IMPL
  close-docs sweep.

  **RESOLUTION (v0.7-M42 IMPL close-docs `8878745`):** stub-
  flavoured discriminators count **SEPARATELY** from production
  `details.reason` literals — they're explicitly transient
  (the M42 IMPL body lifted the `'m42_preflight_stub'` throw
  out of the runtime path; the literal stays RESERVED in
  module docstrings + regression-guard test assertions per the
  R-v0.7-NEW-4 checklist (b)/(c) allowed contexts, but it no
  longer fires from any production code path). The
  R-v0.6-NEW-2 5th-consumer graduation expectation therefore
  stays unchanged — M42 IMPL added zero net production
  `details.reason` literals. M43 atomicity-envelope shape is
  the next plausible site if `create_then_file_upload_partial_
  failure` lands. Cross-references R-v0.6-NEW-2 (above) for
  the parent pattern + R-v0.7-NEW-4 (below) for the checklist
  that codified the (a)/(b)/(c) allowed-context taxonomy for
  reserved-literal carry-overs.

- **R-v0.7-NEW-4 — Pre-IMPL contract-term checklist for cross-
  doc grep.** Sub-discipline of R-NEW-72 (post-fix-up cross-doc
  grep). R-NEW-72 covers the *timing* (re-grep after every
  contract-flipping Codex round); R-v0.7-NEW-4 covers the
  *enumeration* — the IMPL author commits upfront to the full
  set of state-current + pre-IMPL-framing contract-term
  phrases the milestone's shipped surface uses, and the
  post-fix-up grep runs against THAT enumerated checklist
  rather than re-deriving from round-N findings. Without the
  pre-IMPL enumeration, Codex fix-up rounds escalate as each
  round's broader sweep catches a new layer of adjacent prose
  the prior round's narrower regex missed. Graduated to a
  `.claude/rules/workflow.md` permanent rule at v0.7-M42 IMPL
  R7 (`8f2832b`) — refined at R8 (`08ae263`) with the
  (a)/(b)/(c) allowed-contexts list for reserved-literal
  carry-overs.

  **Why it graduated immediately at 1st consumer (rather than
  waiting at R-NEW-58's 2-consumer threshold):** the 7-round
  W9 prose-drift escalation across v0.7-M42 IMPL R2-R8 made
  the pattern unambiguously load-bearing. R2/R3 caught 3 sites
  each. R4 jumped to 8+3-proactive. R5 returned 5+1-test.
  R6 jumped to 9. R7 returned 4+workflow-graduation. R8
  returned 7+workflow-refinement. The asymptotic convergence
  shape (each round's regex sweep catching a wider layer
  than the prior's) is structurally identical to the
  "post-fix-up grep must re-run after every contract-flipping
  fix" pattern R-NEW-72 graduated for — but R-NEW-72 didn't
  pin the *enumeration*, only the *timing*. The pre-IMPL
  checklist extends R-NEW-72 with the missing enumeration
  step.

  **The v0.7-M42 checklist** (codified at workflow.md):
  state-current phrases that should appear post-IMPL
  (`v0.7-M42`, `bulk friendly`, `per-item fan-out`,
  `--continue-on-error`, `--concurrency`,
  `runItemUpdateBulkFileDispatch`, `BulkFileSetData`,
  `item_update_bulk_file_set`); pre-IMPL phrases that should
  NOT appear in current prose post-IMPL with three allowed
  exceptions — (a) historical post-mortem entries in earlier
  `v0.{N-1}-plan.md` section bodies; (b) reserved-literal
  docstrings naming literals that stay RESERVED across the
  codebase; (c) regression-guard test assertions
  (`expect(...).not.toContain(...)` on the literal). All
  OTHER occurrences are contract-term drift bugs.

  **2 supporting instances** at graduation — v0.7-M42 IMPL R7
  filing (`8f2832b`; introduced the section + initial term
  list) + R8 refinement (`08ae263`; added (a)/(b)/(c) allowed-
  contexts list + extended the should-not-appear term list
  with the semantic variants Codex surfaced across R6-R8 rounds:
  `single-item shape only`, `M38 + M31 only`, `M38 ships bulk`,
  `form ships at M38`, `bulk file dispatch rejects per D5`).
  **Priority: HIGH** — graduates the load-bearing remediation
  for the W9-prose-escalation pattern; v0.7-M43's pre-flight
  inherits the checklist + extends it for M43-specific
  contract terms.

  **Next-milestone inheritance path:** at v0.7-M43 pre-flight
  (next session), enumerate the M43 contract-term checklist
  upfront (M43-specific state-current + pre-IMPL-framing
  terms); commit it inline at the pre-flight commit; use as
  the post-Codex-fix-up grep checklist throughout M43 IMPL.
  Goal: collapse M43 IMPL's W9 cycle from M42's 8 rounds to
  ≤3 rounds. If M43 IMPL achieves ≤3 W9 rounds, the
  graduation earns its keep; if M43 IMPL still escalates to
  5+ rounds, file a learnings sub-entry capturing what the
  checklist missed.

  Cross-references R-NEW-72 (parent — pre-IMPL checklist
  extends the post-fix-up grep with the enumeration step) +
  R-v0.6-NEW-2 (related — the (a)/(b)/(c) allowed-context
  taxonomy formalised the reserved-literal carry-over
  pattern that R-v0.6-NEW-2's discriminator literals also
  follow).

- **R-v0.7-NEW-5 — Fail-fast error-decoration block lift
  candidate.** Sub-discipline of R-NEW-58 (lift-ahead-of-feat).
  The fail-fast error-decoration block —
  `if (err.code === 'usage_error') { throw new UsageError(...) }
  else { throw new ApiError(...) }` with stale-cache remap +
  partial-success invalidate inside the catch — is byte-
  equivalent across two production sites:

  1. **JSON-bulk action body** at
     `src/commands/item/update.ts:1334-1361` (originated
     v0.3-M25 partial-success + v0.4-M30 `--concurrency`;
     unchanged from those milestones).
  2. **File-bulk per-item dispatch** at
     `runItemUpdateBulkFileDispatch` (`src/commands/item/update.ts`
     ~lines 1916-2010; shipped v0.7-M42 IMPL `22df2fa` + R1
     `968b154`).

  Both sites also have identical uncovered conditional-spread
  arms on the ApiError ctor (foldAndRemap-decorated path)
  that contribute to the 0.64pp branches-margin drop at M42
  IMPL — a lock-step ratchet across both code paths would
  recover ~0.5pp.

  **1-consumer threshold not yet met** for R-NEW-58's
  ahead-of-feat 2-consumer lift trigger (the helper inlines
  rather than extracts at M42 IMPL because the JSON-bulk
  site predates R-NEW-58 graduation and the file-bulk site
  is the first new consumer post-graduation). Documented
  in-code as the "Future lift candidate" docstring section
  at `src/commands/item/update.ts:1621-1627`.

  **Priority: LOW** — code lift, but waiting at R-NEW-58's
  3rd-consumer threshold (M43 create-time fold likely adds
  the 3rd consumer if its rollback / orphan-warn shape
  re-uses the same decoration). Coverage residual is
  acceptable at floor + 0.37pp branches margin per §3 M42
  "Coverage residual" prose. Cross-references R-NEW-58
  (parent rule) + §3 M42 "Coverage residual" (the JSON-bulk
  precedent note that motivated this candidate's filing) +
  the in-code docstring at `update.ts:1621-1627` (the
  authoritative description of the byte-equivalent surfaces).
