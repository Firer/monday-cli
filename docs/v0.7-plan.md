# v0.7 implementation plan

> **PIVOT 2026-05-20 — v0.7 drops all API-`2026-04`-dependent
> milestones.** Per session-end directive: do NOT pursue
> `2026-04`-dependent work until `@mondaydotcomorg/api` SDK 15.x
> ships with `CURRENT_VERSION = '2026-04'` (or later) natively.
> The original v0.7-M39 / M40 / M41 cluster is DEFERRED:
>
> - **M39** (string-literal API-version override) — pre-flight +
>   IMPL + close-docs commits SHIPPED in a prior session
>   (`bb7c2cc..2e501b5`), then `git reset --hard ea153ee`'d at
>   session-end 2026-05-20 because the first downstream surface
>   that the override existed for (M40) turned out to be a
>   premium-feature wire. Re-attempt M39 when SDK 15.x publishes
>   AND there's a strong agent-facing reason to bump (no point
>   overriding ahead of the SDK for a single broken verb).
> - **M40** (`monday item set-description <iid>`) — DEFERRED.
>   Empirical probe 2026-05-20 (preserved in user-memory at
>   `~/.claude/projects/-home-nick-code-monday-cli/memory/project_m40_findings_deferred.md`
>   + recoverable in `git reflog` for ~90 days) revealed
>   Monday's wire returns opaque `INTERNAL_SERVER_ERROR
>   { service: 'docs-api' }` at every payload size against
>   freshly-created sandbox items + every existing item in the
>   visible workspaces. `account.plan: null` (free tier);
>   most-plausible cause is paid-tier feature gating that
>   Monday's wire surfaces as INTERNAL_SERVER_ERROR rather than
>   a typed `forbidden` / `feature_disabled` envelope. The
>   description widget isn't worth the override-maintenance
>   overhead for a single user-blocked verb. Re-attempt only
>   from a paid-tier account where the wire is empirically
>   reachable.
> - **M41** (`monday doc block-create-bulk <did>` via
>   `create_doc_blocks`) — DEFERRED. Same gating as M39 (only
>   exists on API `2026-04`; the bulk variant supersedes
>   v0.5-M36's per-block CRUD which is `2026-01`-compatible).
>
> **What ships in v0.7** (effective scope post-pivot):
> M42 + M43 (the v0.6.x carve-out folds — both SHIPPED already;
> see entries below) + a release-prep cluster (envelope-snapshot
> refresh probe + ToC audit + version bump 0.6.0 → 0.7.0 +
> CHANGELOG + README quickstart + close-docs sweep). The v0.7
> identity collapses from "API `2026-04` + carry-forward
> enrichments" to "v0.6.x carve-out folds only".
>
> **Original M39 D1 closure (now reverted):** sequence-shuffle
> v0.7 milestone ship order to **M42 → M43 → (re-probe SDK 15.x)
> → M39 → M40 → M41**. M42 + M43 (the carry-forward folds)
> shipped first under the current 2026-01 / SDK-14.0.0 pin.
> M39 + M40 + M41 (all 2026-04-dependent) DEFERRED post-M40
> probe finding per the directive above.
>
> **M42 IMPL shipped** (R0 `22df2fa` + R1 fix-up `968b154` +
> R2 fix-up `a41c5d0` + R3 fix-up `e905a33`). Pre-flight contract
> diff at `160330b` closed D1-D4 (see §3 M42 entry below): D1
> reuses v0.4-M30's `dispatchParallel` over a shared
> `MultipartTransport`; D2 mirrors M25's partial-success shape
> with `asset` slot replacing `item`; D3 single upfront
> `precheckLocalFile` before the dispatch loop per cli-design §5.8
> atomicity discipline; D4 zero net change to ERROR_CODES (registry
> stays at 29). IMPL replaced the c8-ignored stub body with the
> full per-item multipart fan-out: `executeFileColumnSet` per
> matched item under the `--concurrency` selector +
> `foldAndRemap` for stable-code remap on stale-cache archived
> columns (R1 P1-1) + `SourceAggregator` over metadata + M38
> pre-check + items_page walk + dispatch legs (R1 P2-1) +
> partial-success invalidate on fail-fast with prior applies
> (R1 P2-2). The `'m42_preflight_stub'` discriminator literal is
> RESERVED post-IMPL (regression-guarded by an integration test
> that asserts the literal does NOT appear in stdout/stderr).
> Existing v0.6-M38 integration tests asserting
> `'file_set_on_bulk_unsupported'` flipped to assert the new
> contract envelope shape; mutex tests asserting
> `'multi_file_set_unsupported'` / `'mixed_file_and_value_sets'`
> on bulk callShape stay green (those rules are universal). Full
> post-mortem (Codex round-by-round summary, R-class candidates,
> branches-margin recovery target) lands at the close-docs commit.
>
> **M43 pre-flight contract diff cluster SHIPPED at
> `28f117c..ea71b55`** (R0 contract diff + R1-R5 Codex fix-ups
> + §22 watch-items annotation close-out; Codex pre-flight
> cycle CONVERGED at R5 with 0 P1 across all 5 rounds — every
> finding was prose-drift on the contract-term surface).
> Closes D1-D3 (see §3 M43 entry below): D1 atomicity-envelope shape defaulted to (b) orphan-warn
> — leg-2 failure surfaces `internal_error` with
> `details.reason: 'create_then_file_upload_partial_failure'` +
> `details.created_item_id` echoing leg-1's orphan + a hint
> directing agents to retry leg-2 alone OR rollback via `monday
> item delete`. Closure rationale: the pre-flight rollback-
> viability probe could not run empirically (token lacked
> `create_item` permission in a token-created sandbox workspace;
> existing-board attempts correctly blocked by harness
> modify-shared-state guards). Defaulting to (b) under
> uncertainty preserves the agent's recovery handle without
> introducing a destructive `delete_item` cleanup leg whose own
> failure mode is unaccounted for. D2 dry-run envelope pins two
> `planned_changes` entries (`create_item` + `add_file_to_column`).
> D3 zero net change to ERROR_CODES (registry stays at 29).
>
> **IMPL R0 SHIPPED at `5cf4365`** (Codex IMPL review in
> progress at the current session; round-by-round summary lands
> at close-docs). `runItemCreateFileDispatch` in
> `src/commands/item/create.ts` mirrors M42's
> `runItemUpdateBulkFileDispatch` shape; the runtime body at R0
> lifted the formerly c8-ignored pre-flight stub for the two-leg
> dispatch under the §5.8 orphan-warn atomicity envelope. The
> `'m43_preflight_stub'` discriminator literal is RESERVED post-
> IMPL (regression-guarded by an integration test asserting the
> literal does NOT appear in stdout/stderr across all 4 emit
> surfaces — success + dry-run + orphan-warn + leg-1 failure).
> Existing v0.6-M38 integration + unit tests asserting
> `'file_set_on_create_unsupported'` re-flipped to assert the
> IMPL envelope shape + regression-guard both literals' absence;
> the mixed-rule SUPPRESSION on `'item_create'` (per D6
> asymmetry — `create_item` natively bundles non-file
> `column_values` atomically) stays unit-tested. Inherited the
> R-v0.7-NEW-4 pre-IMPL contract-term checklist (graduated
> v0.7-M42 IMPL R7); checklist body lives in the §3 M43 entry
> below.
>
> Closure rationale: zero override commitment until M43 close +
> preserves optionality across all three fallback paths (SDK 15.x
> lift / string-literal override / continued wait). The override
> path itself is cheaper than initially framed at candidate-
> selection scrutiny: `src/api/transport.ts:99`'s per-request
> `API-Version` header is already parameterised + overridable, and
> the `.claude/rules/monday-api.md` "Boundary-typing trap" pattern
> already routinely hand-rolls zod schemas for SDK-untyped fields
> (`BatteryValue` / `hierarchy_type` / `is_leaf` / `capabilities`).
> So the M43-close re-decision is robust whether SDK 15.x ships
> or not. The bounded-fallback enumeration documented at §3 M39
> D1 carries forward unchanged — sequence-shuffle was fallback
> (iii) selected at kickoff candidate-selection rather than after
> 2-4 sessions of waiting. Pattern noted in [[R-v0.7-NEW-1]].
>
> Per-milestone decisions (§8) + empirical-probe results (§9)
> populate at each milestone's pre-flight kickoff.
>
> The binding contract is [`cli-design.md`](./cli-design.md). This
> document sequences the work needed to ship v0.7 of that contract;
> it doesn't redefine it. If a conflict arises, `cli-design.md` wins
> and this plan is updated.

## 1. Scope summary

v0.7 was originally framed as "**Monday API 2026-04 + carry-forward
enrichments**" — pinning the CLI to a newer Monday API surface
plus folding in the v0.6.x file-`--set` carve-outs deferred at M38.
**Post-pivot 2026-05-20 (see opening note above), v0.7 collapses to
"v0.6.x carve-out folds only"**: M42 + M43 ship, the
`2026-04`-dependent milestones (M39 / M40 / M41) DEFER until SDK 15.x
publishes natively. v0.6 closed the files-shaped friendly `--set`
writer path (the longest-running v0.4 → v0.5 → v0.6 carry-over).
v0.7 picks up the next class of work: the v0.6.x bulk + create
file-`--set` carve-outs deferred at M38, with the API-pin work
shelved for a future release.

**In scope (v0.7 milestones — full per-milestone scope filled at
each milestone's pre-flight kickoff):**

| M | Surface | Wire op | Status |
|---|---------|---------|--------|
| ~~**M39**~~ | API pin bump `2026-01` → `2026-04` + mass-flip + envelope-snapshot refresh + SDK upgrade to 15.x (or string-literal override on SDK 14.0.0). | (n/a — pin flip only) | **DEFERRED 2026-05-20** — override path abandoned after M40 probe revealed paid-tier gating; re-attempt when SDK 15.x ships with `CURRENT_VERSION = '2026-04'` natively. Earlier override commits `bb7c2cc..2e501b5` recoverable in `git reflog`. |
| ~~**M40**~~ | `monday item set-description <iid> --content <md>` (+ `--dry-run`) via `set_item_description_content`. | `set_item_description_content` | **DEFERRED 2026-05-20** — empirical probe revealed paid-tier gating + opaque `INTERNAL_SERVER_ERROR { service: 'docs-api' }` on free-tier accounts. Findings preserved in user-memory + the M40 pre-flight contract diff was abandoned uncommitted. Re-attempt only from a paid-tier account where the wire is reachable, AND only after M39 (API pin) re-attempt succeeds. |
| ~~**M41**~~ | `monday doc block-create-bulk <did> --blocks <json>` (+ `--dry-run`) via `create_doc_blocks`. | `create_doc_blocks` | **DEFERRED 2026-05-20** — `create_doc_blocks` is new at API `2026-04`. Same gate as M39. Defer until SDK 15.x ships and M39 re-opens. The v0.5-M36 per-block CRUD verbs (`block-create` / `update` / `delete`) stay sufficient for the `2026-01` pin. |
| **M42** | v0.6.x **bulk file `--set` carve-out fold** — `monday item update --where ... --set <file-col>=<path>` per-item file dispatch + partial-success envelope + `--concurrency` shared-transport semantics. Closes D5 deferral from v0.6-M38. | `add_file_to_column` (v0.4-M31 wire, per-item) | SHIPPED (R0 `22df2fa` + R1 `968b154` + R2 `a41c5d0` + R3 `e905a33`; close-docs at `8878745`) |
| **M43** | v0.6.x **create-time file `--set` carve-out fold** — `monday item create --set <file-col>=<path>` via post-create `add_file_to_column` two-leg dispatch. Closes D6 deferral from v0.6-M38. Atomicity-envelope shape: orphan-warn per D1 closure (cli-design §5.8 + v0.7-plan §3 M43). | `create_item` + `add_file_to_column` (two-leg) | SHIPPED (`5cf4365..1b4f423` — R0 IMPL + 3 Codex IMPL fix-up commits + close-docs `1b4f423`; R4 CONVERGED zero findings) |
| **release-prep** | Refreshed envelope-snapshot suite for the shipped v0.7 surfaces (M42 + M43); `output-shapes.md` ToC audit + deferral slip (`v0.7` → `v0.8` across 3 sites — multi-level subitem `--parent` rejection literal at runtime + integration test pin + output-shapes prose); README quickstart updates (extended step 13 with M42 bulk + M43 create-time forms) + scope refresh (cascaded labels: v0.7.0 current / v0.6.0 previous / v0.5.0 prior / v0.4.0 earlier / v0.3.0 even-earlier) + new "What v0.7 added (M42 + M43)" block + reframed "v0.8 (next)" block; CHANGELOG `0.7.0` entry; version bump 0.6.0 → 0.7.0 + audit-fix `brace-expansion 5.0.2 → 5.0.5+` folded inline per v0.6 precedent; close-docs sweep. Mirrors v0.6 release-prep cluster shape verbatim (5 commits with diff + 1 zero-diff envelope-snapshot probe folded into close-docs prose). | (n/a) | **SHIPPED** at `9b7e9ad..3e46f59` — ToC audit + slip `9b7e9ad` / README refresh `3ca51ea` / version bump + audit-fix `75c8831` / CHANGELOG `9ebaa81` / close-docs sweep `3e46f59`. Published to npm at 2026-05-20T15:48:07Z; annotated `v0.7.0` tag at `3e46f59`; GitHub release at https://github.com/Firer/monday-cli/releases/tag/v0.7.0. R-NEW-82 ratified at 5th-consecutive consumer; R-NEW-84 ratified at 6th supporting instance (v0.7 release-prep IS the symmetric positive-case application — zero production semantic changes modulo the `'v0.7'` → `'v0.8'` literal flip; Codex review skipped; gates alone verified). |

**Optional v0.7.x candidates (filed but NOT in v0.7.0 scope by default
— elevated to in-scope only if pre-flight empirical probes surface
unexpected friction OR strong agent demand):**

- `monday board describe` + `monday board get` projections extended
  with `Board.folder` + `Board.created_from_board_id` (2026-04
  additive Board fields). **DEFERRED 2026-05-20 — needs the same
  2026-04 pin as M39.** Revisits when SDK 15.x ships with `2026-04`
  as `CURRENT_VERSION`.

**Out of scope (deferred to v0.7.x / v0.8 / permanently — full
rationale per item):**

### 2026-04 wire surfaces NOT exposed at v0.7

- **Knowledge-base reads + AI surfaces** — `articles` / `article_blocks`
  queries; `create_article` / `publish_article` / `delete_article`
  mutations; `knowledge_base_search` (AI-answered); `ask_developer_docs`
  query. **Rationale:** AI-flavoured surface area; questionable fit
  for the CLI's agent-first primary audience (agents would route AI
  queries through their own LLM, not Monday's). Revisits at v0.7.x
  on demonstrated user demand only.
- **Notetaker meetings** — `notetaker.meetings` query (recordings,
  summaries, transcripts, participants). **Rationale:** Niche
  enterprise-grade surface; out-of-scope for the agent-first CLI's
  read/write loop until user demand surfaces.
- **Object-relations API** — `object_relations` query +
  `create_object_relations` / `delete_object_relation` mutations +
  `create_object.relations` arg. **Rationale:** Niche; revisit at
  v0.7.x on demand.
- **Email sequences** — `allowed_sequences_to_enroll` query +
  `enroll_items_to_sequence` mutation. **Rationale:** Niche
  marketing-automation surface; out-of-scope until user demand.
- **Doc version history** — `doc_version_history` +
  `doc_version_diff` queries. **Rationale:** Useful but niche;
  defers to v0.7.x as a doc-namespace expansion if user demand
  surfaces.
- **Webhook subscription management** —
  `get_app_lifecycle_subscriptions` / `update_app_lifecycle_subscription`
  / `delete_app_lifecycle_subscription` mutations. **Rationale:**
  Adjacent to v0.3-M27's existing `monday webhook list/create/delete`
  surface; lifecycle-subscription semantics differ from regular
  webhooks (app-installation events vs board-data events). Revisits
  at v0.7.x with a clean scoping decision distinguishing the two
  surfaces.
- **Enterprise-gated additions:** `create_project` (Enterprise-plan
  only, project boards) + departments API (`departments` query +
  `department` field on users + 7 CRUD mutations). **Rationale:**
  Enterprise-plan-gated — most users can't exercise the surface;
  exposing it without a plan-detection gate would surface
  `unauthorized` to non-Enterprise users on every invocation.
  Revisits at v0.7.x with a plan-gate Decision OR drops permanently
  if the surface stays Enterprise-only.
- **Reaction attribution + Kind.agent enum + ExternalWidget enum
  expansion** (`Reaction.attribution_entity_ref`,
  `Reaction.attribution_entity_type`, `Kind.agent`,
  `ExternalWidget.APP_FEATURE` + `LISTVIEW`). **Rationale:**
  Wire-shape detail useful for `--verbose` debugging but not
  surfaceable through a clean CLI verb. Folds into envelope
  projections if a future read verb surfaces a Reaction; otherwise
  carries forward as transparent SDK-typed pass-through.
- **`BlockEvent.iterator_id` / `current_iteration` / `max_iterations`
  (automation loops).** **Rationale:** Webhook-payload-only surface;
  the CLI doesn't host webhook receivers (cli-design §13 permanent
  non-goal). N/A.
- **`update_board(item_nickname:)` attribute extension.**
  **Rationale:** Folds into existing M15 `monday board update`
  per-attribute fan-out if the surface is zero-cost; decision lands
  at M39 pre-flight.
- **`workspaces.query_params(:)` filter (account product kind).**
  **Rationale:** Folds into existing `monday workspace list`
  filtering if zero-cost; decision at M39 pre-flight.
- **`create_marketplace_app_discount`** (deprecates
  `grant_marketplace_app_discount`). **Rationale:** Marketplace-app-
  developer surface, not Monday-data surface. N/A — out of the
  CLI's scope permanently.

### Carry-forward backlog (continues from v0.6)

- **Multi-file `--set` per call** (v0.6-M38 D2 deferral —
  `multi_file_set_unsupported`). **Status:** v0.7.x candidate. Carries
  M42's bulk-file-set design dimensions per item rather than per
  call; revisit once M42 pins the per-item file-dispatch envelope.
- **`<file-col>=-` stdin support** (v0.6-M38 D7 deferral). **Status:**
  v0.7.x candidate. Needs clean `--filename` companion shape pinned
  first (`monday item upload` carries the same deferral).
- **Multi-level subitem creation** — slipped v0.3 → v0.4 → v0.5 → v0.6
  → v0.7. **Status:** STAYS deferred. Monday's `sub_items_board` still
  carries no `subtasks` column at API `2026-04` (per agent's 2026-05-18
  changelog research — no entry surfacing subtasks on multi-level
  boards). Re-probe at v0.7 kickoff to confirm; if data-model gap
  unchanged, slips to v0.8 → v0.9 → ... until Monday surfaces the
  capability.
- **Cross-board `item move` value-overrides** — slipped v0.3 → v0.4
  → v0.5 → v0.6 → v0.7. **Status:** STAYS deferred. Monday's
  `ColumnMappingInput` still carries no value slot at API `2026-04`.
- **Resumable cross-board cursor** — per-board cursor-lifetime under
  aggregation design issue unchanged. **Status:** STAYS deferred to
  v0.7.x or v0.8.
- **Profile-scoped argument defaults** — filed at v0.6 kickoff.
  **Status:** STAYS at v0.7.x candidate-selection backlog; carries
  its §13 carve-out Decision prerequisite forward.
- **OAuth app registration (`monday auth login`)** — placeholder
  guard from v0.3-M28. **Status:** STAYS deferred. Reopens only on
  clear user demand.

**Permanent non-goals (per cli-design §13 — unchanged from v0.6):**

- Hosting webhooks / app framework / GraphQL subscriptions /
  telemetry / forms / saved queries-as-aliases / `monday undo`.
- See [`docs/v0.6-plan.md`](./v0.6-plan.md) §1 "Permanent non-goals"
  for the full list. v0.7 inherits unchanged.

## 2. Pre-flight: what's already in place

> **Filled at v0.7 kickoff candidate-selection session 2026-05-18
> (this commit).**

**Working HEAD pin:** `a7a890d` (the SKELETON-filing commit at the
post-v0.6.0 publish session — `docs(future-plans): file v0.7 + v0.8
skeleton plans — Monday API 2026-04 + 2026-07 version bumps`). v0.6
release-prep cluster closed at `98185d4..51ba1a4`; v0.6.0 published
to npm on 2026-05-18T16:30:21Z.

**Post-v0.6.0 live numbers carried into v0.7 kickoff:**

- **Tests:** 4100 passing + 1 skipped across 172 files.
- **Coverage:** 99.26 / 96.46 / 99.31 / 99.52 (stmts / branches /
  fns / lines) at the 95 / 95.45 / 95 / 95 floor; **branches margin
  1.01pp** (up from 1.00pp at v0.5.0 — held the new floor across
  v0.6-M38 + release-prep without re-slipping).
- **29 stable ERROR_CODES.**
- **117 commands.**
- **`npm audit`:** 0 vulnerabilities (audit-fix `fast-uri 3.1.0 →
  3.1.2` already folded into the v0.6 version-bump commit).
- **`package.json` version:** `0.6.0`.

**SDK 15.x publication probe (kickoff session 2026-05-18):**

- `npm view @mondaydotcomorg/api versions --json | tail`: latest
  publish is `14.0.0` (2026-03-18T15:46:19.143Z). No `15.0.0`
  release; no `15.0.0-beta.*` pre-release either.
- SDK 14.0.0 bakes `AvailableVersions.CURRENT_VERSION = "2026-01"`
  (verified in `node_modules/@mondaydotcomorg/api/dist/cjs/lib/
  constants/index.d.ts`); this matches the CLI's current API pin
  at `src/api/client.ts:38`'s `PINNED_API_VERSION` re-export.
- **Monday SDK cadence reference points:**
  - `13.0.0` → `14.0.0`: 2026-01-20 → 2026-03-18 (≈2 months).
  - `12.0.0` → `13.0.0`: 2025-12-03 → 2026-01-20 (≈7 weeks).
  - Today (2026-05-18) sits **exactly 2 months** past `14.0.0`'s
    publish — SDK 15.x baking `2026-04` is expected within weeks,
    not months, on historical cadence.
- **M39 D1 closure (this session):** WAIT for SDK 15.x publication;
  defer M39 pre-flight until 15.x ships. See §3 M39 entry for the
  closure rationale + bounded-wait fallback structure.

**Foundations carrying forward into v0.7:**

- **SDK + API version pin.** Unchanged from v0.6 close — pinned at
  SDK 14.0.0 / API `2026-01`. M39 lifts both in lockstep once SDK
  15.x publishes. Override fallback documented at §3 M39 D1 entry +
  §6 R1.
- **Multipart transport seam (v0.4-M31).** Reused at v0.6-M38
  + carries forward to v0.7-M42 (bulk file `--set`) + v0.7-M43
  (create-time file `--set`). Per-item file dispatch + shared-
  transport semantics for `--concurrency` are the new design
  dimensions M42 introduces.
- **`file-source.ts` helper (R-v0.6-NEW-1).** 3 consumers post-
  v0.6-M38 lift (M31 `item upload` + M31 `update upload` + M38
  `executeFileColumnSet`); M42 + M43 likely become consumers 4 +
  5 — re-evaluates R-NEW-58 ahead-of-feat trigger threshold.
- **`details.reason` discriminator pattern (R-v0.6-NEW-2).** 4
  supporting instances at v0.6-M38; M42 + M43 likely add the 5th
  + 6th (per-item file-set failure reasons in bulk envelope +
  atomicity-envelope reasons in create two-leg). R-v0.6-NEW-2
  graduates at the 5th-consumer threshold.

## 3. Milestone sequencing

> **Per-milestone detail filled in at each milestone's pre-flight
> kickoff.** Skeleton entries below sketch the wire surface +
> known design dimensions; D1-D{N} decisions land at pre-flight
> contract diff commits.

**Sequencing decision (v0.7 kickoff refactor-audit 2026-05-18):**
ship order is **M42 → M43 → (re-probe SDK 15.x) → M39 → M40 → M41**.
Milestone IDs stay stable (M39-M43 numbering unchanged); only the
session-to-session ordering flips. Per-milestone entries below
remain in M39-M43 ID order for stable cross-referencing — read
the §1 status block for the active ship order. M42 + M43 ship
first because they're API-pin-independent v0.6.x carve-out folds
reusing v0.4-M31's multipart wire surface; M39 (API pin bump
2026-01 → 2026-04) gates on SDK 15.x publication OR an explicit
override decision at M43 close.

### M39 — API pin bump 2026-01 → 2026-04

**Wire surface:** No new wire ops (pin flip only). 475 `2026-01`
literal sites across `src/` + `tests/` + `docs/` flip to `2026-04`
in lockstep (mostly comments / probes / cassettes / prose; the
runtime pin lives in one place — `src/api/client.ts:38`'s
`PINNED_API_VERSION = AvailableVersions.CURRENT_VERSION` re-export).

**Open decisions at pre-flight:**

- **D1 — SDK upgrade vs string-literal override.** ✅ **Closed at
  v0.7 kickoff candidate-selection session 2026-05-18 (this
  commit): SEQUENCE-SHUFFLE.** Ship M42 → M43 first (API-pin-
  independent v0.6.x carve-out folds reusing v0.4-M31's multipart
  wire surface). At M43 close, re-probe SDK 15.x publication
  state + decide between (a) SDK 15.x lift (if shipped) / (b)
  string-literal override on SDK 14.0.0 + hand-rolled zod schemas
  for new 2026-04 fields (per `.claude/rules/monday-api.md`
  "Boundary-typing trap" pattern — already routinely applied for
  `BatteryValue` / `hierarchy_type` / `is_leaf` / `capabilities`)
  / (c) continued wait if both paths still look unattractive.
  Probe result at kickoff: latest `@mondaydotcomorg/api` publish
  is `14.0.0` (2026-03-18T15:46:19.143Z, bakes
  `CURRENT_VERSION = "2026-01"`); no 15.x release exists yet.
  **Closure rationale:** (a) v0.6.0 published today — no urgency
  on v0.7 timeline; (b) sequence-shuffle preserves optionality
  across all three fallback paths while delivering 2 milestones
  (M42 + M43) of v0.7 progress immediately under the current
  2026-01 / SDK-14.0.0 pin; (c) the override "hack" is cheaper
  than initially framed at candidate-selection scrutiny —
  `src/api/transport.ts:99`'s per-request `API-Version` header is
  already parameterised + overridable, and hand-rolled zod schemas
  are routine at every wire boundary per `validation.md` "parse,
  don't validate" (the SDK-generated types are useful build-time
  signal but not load-bearing — runtime always parses through
  zod) — so the M43-close re-decision is robust whether SDK 15.x
  ships or not. The bounded-fallback enumeration (continued wait
  / override / sequence-shuffle) carries forward structurally;
  sequence-shuffle was fallback (iii) selected at kickoff
  candidate-selection rather than after 2-4 sessions of waiting.
  Pattern noted in [[R-v0.7-NEW-1]].
- **D2 — Breaking change defensive sweep.** API 2026-04's one
  breaking change (`AggregateGroupByResult.value_*` collapsed to
  unified `value: JSON`) does NOT affect today's CLI surface
  (verified by `grep -r aggregate_group_by src/` returning empty
  per the agent's 2026-05-18 changelog research; re-verify at
  pre-flight). If a future milestone surfaces aggregate-group
  reads, that milestone owns the projection re-shape.
- **D3 — `Board.folder` + `Board.created_from_board_id` projection
  fold.** Zero-cost additive Board fields. Fold into `monday board
  get` + `monday board describe` projections at M39 if zero-cost;
  defer to v0.7.x optional candidate otherwise.
- **D4 — `update_board(item_nickname:)` + `workspaces.query_params(:)`
  filter folds.** Both zero-or-low-cost extensions to existing
  surfaces. Decision: fold into M39 envelope-snapshot refresh OR
  defer to v0.7.x.

**IMPL Codex review estimate:** 1-2 fix-up rounds (mechanical
cluster — primary risk is the mass-flip mass-noisy step).

**R-class triggers anticipated:** None at M39 by default (no novel
wire shape, no new helper lift).

### M40 — `monday item set-description <iid>`

**Wire surface:** `set_item_description_content` mutation — sets
the item's description block content (single-call). Wire payload
likely accepts an HTML string OR a doc-block-content JSON payload
shape; clarified at pre-flight empirical probe.

**Empirical probe required at pre-flight** (per workflow.md
"Empirical probe for any novel API surface"):

- Wire-shape probe — what payload shapes does
  `set_item_description_content` accept (markdown / HTML / doc-block
  content JSON)? Mirrors v0.5-M37's MAX_DOC_IMPORT_PAYLOAD_BYTES
  probe structure.
- Size-limit probe — does the wire side cap content size? At what
  threshold? (Likely mirrors M37's 250KB-OK / 500KB-rejected
  threshold given doc-content shared infrastructure; verify.)

**Open decisions at pre-flight:**

- **D1 — Argv source pattern.** `--content <file|-> \|
  --content-string <s>` mutex (mirrors M37's pattern + reuses
  `readSourceContent` helper at `src/utils/source-content.ts`).
  Confirmed at pre-flight after probe pins the payload shape.
- **D2 — `--dry-run` envelope shape.** Mirrors M37 `monday doc
  append-markdown --dry-run` verbatim: `planned_changes:
  [{operation: 'set_item_description_content', item_id,
  content_format, content_size_bytes}]`. Standard envelope; no
  new contract surface.
- **D3 — ERROR_CODES delta.** Likely zero (registry stays at 29).
  Rejections route through existing `usage_error` /
  `validation_failed` / `not_found` with `details.reason`
  discriminators. Confirmed at pre-flight.

**IMPL Codex review estimate:** 2-3 fix-up rounds (median IMPL
cadence for single-mutation milestones per v0.5-M34 / v0.6-M38
precedent).

**R-class triggers anticipated:**
`readSourceContent` consumer count goes 5 → 6 (M13 update verbs
+ M37 doc verbs + M40); no graduation impact (helper already
shipped at M37 IMPL kickoff).

### M41 — `monday doc block-create-bulk <did>`

**Wire surface:** `create_doc_blocks` mutation — bulk-create up
to 25 doc blocks per call. Closes the per-call efficiency gap in
M36's per-block CRUD (where N blocks = N round-trips).

**Empirical probe required at pre-flight:**

- Wire-shape probe — payload shape for the 25-block max. Likely
  an array of M36-shaped block records; verify exact shape +
  per-block error semantics (does a single bad block reject the
  whole batch, or does the wire return per-block partial
  success?). Probe drives the §6.1 envelope shape decision.

**Open decisions at pre-flight:**

- **D1 — Per-block partial-success vs all-or-nothing envelope
  shape.** If the wire returns per-block partial success
  (`failed_blocks` / `successful_blocks` similar to M34's team
  mutations), project to the universal §6.1 `data.results:
  [{block_index, ok, ...}]` shape. If all-or-nothing, return a
  flat success envelope with the created `blocks` array. Probe
  pins this.
- **D2 — Mutex with M36 `block-create`.** Single-block create stays
  on the M36 single-block verb; M41 is the bulk variant. Mirrors
  v0.3-M25 `update --where --continue-on-error` pattern where the
  bulk verb is orthogonal to the single-item one.
- **D3 — `--blocks <json>` parse-boundary size cap.** Per the M37
  pattern: pre-empt at parse boundary if the wire's max is fixed
  (25 blocks = some byte threshold). Probe pins the threshold.
- **D4 — ERROR_CODES delta.** Likely zero. Bulk-specific
  rejections route through `usage_error` / `validation_failed`
  with `details.reason: 'block_batch_too_large'` /
  `'block_validation_failed'` discriminators.

**IMPL Codex review estimate:** 3-4 fix-up rounds (one above median
— the partial-success projection design dimension typically
surfaces an extra fix-up round per v0.5-M34 precedent).

**R-class triggers anticipated:**
Per-batch partial-success projection (`projectBlockBatchResults`)
likely 1-2 consumers at M41 close; R-NEW-58 ahead-of-feat trigger
fires only if a 3rd consumer site is anticipated within v0.7 scope.

### M42 — Bulk file `--set` carve-out fold (D5 closure from v0.6-M38)

**Wire surface:** `add_file_to_column` per-item, dispatched across
the `--where`-resolved item-id set with the M25 partial-success
envelope. No new wire op; reuses v0.4-M31's multipart wire +
v0.6-M38's `executeFileColumnSet` runtime body verbatim.

**Empirical probe NOT required at pre-flight** (wire surface is
known + cassette-pinned at v0.4-M31; novelty is in the per-item
file dispatch + `--concurrency` interaction, which is
implementation work, not wire-shape work).

**Open decisions at pre-flight — CLOSED at M42 pre-flight contract
diff (this commit):**

- **D1 — `--concurrency` semantics for file dispatch.** ✅
  **Closed at v0.7-M42 pre-flight contract diff: reuse v0.4-M30's
  `dispatchParallel` over a shared `MultipartTransport`.** Each
  parallel worker constructs its own `MultipartTransportRequest`
  per call; the transport itself is connection-pool-shared
  per-token. Range pinned to `[MIN_CONCURRENCY,
  MAX_CONCURRENCY]` (1..32) — same range as M30's JSON-bulk
  parallel dispatch. **No new probe required:** v0.4-M30's
  concurrency probe (`scripts/probe/m30-concurrency.report.txt`)
  pinned the per-token wire-side rate-limit semantics + M31's
  multipart probe (`scripts/probe/m31-asset-upload.report.txt`)
  pinned the multipart wire shape; the intersection (parallel
  multipart over shared transport) is the cross-product of two
  already-pinned surfaces. Re-probing at M42 IMPL is allowed if
  IMPL surfaces an unexpected interaction, but it's not a
  pre-flight gate.

- **D2 — Per-item asset slot in envelope.** ✅ **Closed at v0.7-
  M42 pre-flight contract diff: per-item
  `data.results[i].asset: { id, name, ... }` echo on success
  (mirrors M31's `itemUploadOutputSchema`'s `asset` slot);
  per-item failure surfaces as
  `data.results[i].error: { code, message }` per M25 partial-
  success.** Aggregate
  `data.summary.{matched_count, applied_count, failed_count,
  board_id, column_id, filename, file_size_bytes}` extends M25's
  `partialSuccessBulkUpdateDataSchema` with file-dispatch slots
  (`column_id` / `filename` / `file_size_bytes`) so an agent
  reading the success envelope sees which file was dispatched +
  where. `operation: 'item_update_bulk_file_set'` literal
  discriminator (distinct from M25's `'item_update'`) so agents
  can branch uniformly on `data.operation`. Schemas defined at
  pre-flight in `src/commands/item/update.ts`
  (`bulkFileSetResultSchema` + `bulkFileSetDataSchema`); runtime
  body shipped at M42 IMPL (`22df2fa` + R1-R3 fix-ups).

- **D3 — Per-item file pre-check timing.** ✅ **Closed at v0.7-
  M42 pre-flight contract diff: single upfront `precheckLocalFile`
  call BEFORE the per-item dispatch loop starts.** The bulk
  shape has ONE file path (the value of the file `--set`) shared
  across N matched items; pre-checking it once upfront is both
  cheaper AND consistent with cli-design §5.8's "pre-checks MUST
  fire BEFORE any wire round-trip" atomicity discipline. A failed
  pre-check surfaces upfront as `usage_error` with
  `details.reason: 'file_not_readable'` / `'file_empty'`
  (mirrors M31 single-item shape) — this is whole-call-abort
  REGARDLESS of `--continue-on-error`. The
  `--continue-on-error` flag partitions ONLY the wire-dispatch
  failures (per-item `add_file_to_column` rejections from
  Monday), never the local file pre-check. No per-item
  pre-check needed because the same path applies to every
  matched item.

- **D4 — ERROR_CODES delta.** ✅ **Closed at v0.7-M42 pre-flight
  contract diff: ZERO net change. Registry stays at 29.** All
  M42-specific rejections route through existing codes:
  - Local file pre-check failures → `usage_error` with
    `details.reason: 'file_not_readable'` / `'file_empty'`
    (existing M31 discriminators reused).
  - Mutex violations → `usage_error` with
    `details.reason: 'multi_file_set_unsupported'` /
    `'mixed_file_and_value_sets'` (existing M38 discriminators,
    now universal across single + bulk).
  - Per-item wire failures under `--continue-on-error` →
    per-record `error: { code, message }` shape with whatever
    code Monday returned (mirrors M25's partial-success shape).
  - Per-item wire failures under fail-fast → top-level error
    with `details.applied_to: [<item_ids>]` decoration (mirrors
    v0.1 fail-fast bulk update).

  The `'file_set_on_bulk_unsupported'` discriminator literal at
  `src/api/file-column-set.ts` no longer surfaces from the
  runtime path post-M42 carve-out fold; the literal stays
  RESERVED across the codebase (do not reuse for a different
  rejection reason; agent scripts that branch on it will fall
  through to the new dispatch path on v0.7-M42 and later).

**Pre-flight stub structure (this commit).** Per R-NEW-76:

- argv parse (commander → `parseArgv` → zod) — shipped contract.
- `validateInputShape` (single vs bulk discriminator) — shipped
  contract.
- `setEntries` / `rawEntries` parse — shipped contract.
- `loadBoardMetadata` (board-metadata fetch) — shipped contract.
- `preCheckM38FileDispatch({callShape: 'item_update_bulk'})` —
  shipped contract; now returns `kind: 'file_bulk'` on the
  clean dispatch path (was throwing `'file_set_on_bulk_unsupported'`
  at v0.6-M38).
- `buildQueryParams` + items_page walk — shipped contract.
- Empty-match short-circuit + confirmation gate — shipped
  contract.
- Per-item dispatch loop + envelope emit — at pre-flight, this
  was a **c8-ignored stub** throwing `internal_error` with
  `details.reason: 'm42_preflight_stub'`. **Lifted at M42 IMPL
  (`22df2fa`)**; the c8-ignore wrapper dropped and the runtime
  body now runs `precheckLocalFile` → per-item
  `executeFileColumnSet` (sequential or `dispatchParallel`) →
  `invalidateBoard` → envelope emit. The
  `'m42_preflight_stub'` discriminator literal stays RESERVED
  across the codebase post-IMPL (regression-guarded by an
  integration test).

The pre-flight stub `internal_error` code (rather than
`usage_error`) was deliberate: the argv was accepted at
pre-flight (parse succeeded), the issue was that the wire
dispatch wasn't shipped yet — that's a server-side / internal
gap, not a user-input problem. Post-IMPL the runtime body
emits the proper success / `--continue-on-error` partial-success
/ fail-fast `applied_to`-decorated envelopes instead.

**IMPL Codex review actual:** 8 fix-up rounds total. R1 closed
the behavioral surface (P1-1 foldAndRemap + P2-1
SourceAggregator + P2-2 fail-fast partial-success invalidate +
P3-1 caller prose × 3 + P3-2 +3 tests). **R2-R8 were
W9-prose-only** (no behavioral findings) — Codex's regex sweep
caught a wider layer of adjacent prose each round, escalating
from 3 sites (R2/R3) → 8+3-proactive (R4) → 5+1-test-strengthen
(R5) → 9 (R6) → 4+workflow-graduation (R7) → 7+workflow-refine
(R8). The asymptotic prose convergence pattern motivated the
R-v0.7-NEW-4 graduation (see §22 below) of a **Pre-IMPL
contract-term checklist for cross-doc grep** discipline at
`.claude/rules/workflow.md`. R8 verdict was "improves the named
R7 sites, but W9 not clean yet" — accepted as residual at user
direction; the workflow.md graduation is the load-bearing
remediation for v0.7-M43 onwards.

**R-class triggers actual:**

- `file-source.ts` `precheckLocalFile` consumer count went 3 → 4
  at M42 IMPL (M31 item upload + M31 update upload + M38 item
  set / item update single + M42 bulk item update).
  R-v0.6-NEW-1 watch-item — already-shipped helper scales
  cleanly to consumer 4. **Graduation expectation unchanged** —
  fires at 5th-consumer threshold (v0.7-M43 create-time fold
  likely tips it).
- Per-item file-failure projection helper —
  `runItemUpdateBulkFileDispatch` inlines the fold rather than
  extracting it. **R-NEW-58 ahead-of-feat 2-consumer trigger
  NOT yet fired** — M43 pre-flight decides whether to lift
  (likely yes if M43's atomicity-envelope shape echoes per-leg
  `asset` records).
- R-v0.6-NEW-2 `details.reason` discriminator pattern: zero new
  reasons added at M42 IMPL (per D4 zero-delta closure —
  existing `file_not_readable` / `file_empty` /
  `multi_file_set_unsupported` / `mixed_file_and_value_sets`
  cover every M42 rejection shape). **Graduation expectation
  unchanged** — fires at 5th-consumer threshold, likely at M43
  atomicity-envelope shape if `create_then_file_upload_partial_
  failure` lands as planned per the §3 M43 D3 closure.
- **NEW R-v0.7-NEW-3** filed at `3328a42` (post-pre-flight
  refactor-audit) — pre-flight stub-body-internal pattern.
  Open question at filing: "is `'m42_preflight_stub'` a 5th
  supporting instance of R-v0.6-NEW-2?" **Resolution at M42
  close-docs:** no, stub-flavoured discriminators count
  separately because they're explicitly transient (disappear
  at IMPL when the stub body lifts). R-v0.6-NEW-2 graduation
  expectation stays unchanged.
- **NEW R-v0.7-NEW-4** filed at R7 (`8f2832b`), refined at R8
  (`08ae263`) — Pre-IMPL contract-term checklist for cross-doc
  grep. Graduated immediately at v0.7-M42 IMPL (rather than
  waiting for a 2nd consumer per R-NEW-58 norms) because the
  7-round W9 escalation made the pattern unambiguously load-
  bearing. v0.7-M43 inherits the checklist. See §22 below for
  the full register entry.

**Coverage residual.** Final coverage 98.86 / 95.82 / 99.16 /
99.15 (stmts / branches / fns / lines) at floor 95 / 95.45 /
95 / 95. **Branches margin 0.37pp** (was 0.58pp at pre-flight;
1.01pp at v0.6.0 baseline). The 0.21pp drop from pre-flight to
IMPL is from new conditional-spread arms on the foldAndRemap-
decorated ApiError ctor (P1-1 fix) plus the SourceAggregator
seed-vs-record branching (P2-1 fix). Pattern matches the JSON-
bulk fail-fast precedent at `update.ts:1334-1361` which has
identical uncovered spread arms unchanged from v0.4-M30 /
v0.3-M25. A lock-step ratchet across both code paths would
recover ~0.5pp but is out of scope for M42 IMPL — flagged at
the M42 helper docstring's "future lift candidate" section
(R-NEW-58 2-consumer trigger; lift fires at the 3rd consumer
which M43 create-time fold may add). Filed as
[[R-v0.7-NEW-5]] at M42 IMPL close-docs (§22 below).

### M43 — Create-time file `--set` carve-out fold (v0.6-M38 → v0.7-M43 D6 fold)

**Status:** SHIPPED. Pre-flight contract diff cluster at
`28f117c..ea71b55` (R0 + 5 Codex pre-flight rounds, CONVERGED at
R5). IMPL + close-docs cluster at `5cf4365..1b4f423` — R0 IMPL
`5cf4365` + R1 fix-up `3539124` + R2 fix-up `31313cb` +
R2-trailing `7265fe0` + R3 fix-up `c217011` + close-docs
`1b4f423`; Codex IMPL review CONVERGED at R4 with zero findings
(W1-W9 all clean). D1-D3 closed; the IMPL R0 commit lifted the
formerly c8-ignored stub at
`src/commands/item/create.ts:runItemCreateFileDispatch` under
the §5.8 orphan-warn atomicity envelope.

**IMPL Codex review actual:** 4 rounds total to convergence
(estimate was 4-5; R-v0.7-NEW-4 inherited contract-term checklist
worked as intended — short-circuited the W9 prose-drift
escalation M42 absorbed across 8 rounds down to 4 at M43). Round-
by-round summary:
- **R1** (`3539124`): 0 P1; 1 P2 + 1 P3 — both W9. P2-1 status
  prose drift at v0.7-plan.md:117 + :664 + CLAUDE.md:39
  ("PRE-FLIGHT (stubs landed; IMPL next)" / "Next session:
  v0.7-M43 IMPL"). P3-1 regression-guard narrow at item-create.test.ts:3055
  (4-surface claim, 1-surface coverage).
- **R2** (`31313cb` + `7265fe0`): 0 P1; 1 P2 + 1 P3 — both W9.
  P2-1 wider doc-drift sweep at v0.7-plan.md:61-73 (§0 summary)
  + :812-829 (M43 entry "Pre-flight stub anchoring" subsection)
  + cli-design.md:8915-8920 (canonical contract §13 deferral
  list). P3-1 translation-reject regression-guard missing
  `'file_set_on_create_unsupported'` absence. Trailing site at
  v0.7-plan.md:846-856 (Test surface bullet — one-step pre-flight
  framing for what's a two-step post-IMPL transition) caught at
  `7265fe0`.
- **R3** (`c217011`): 0 P1; 1 P2 + 1 P3 — both W9. P2-1
  round-counter staleness across review rounds (v0.7-plan.md:125
  + :671 + :1442 + CLAUDE.md:51 still framed as "Codex R1 in
  progress" / "IMPL pending"). P3-1 present-tense c8-ignored
  framing at v0.7-plan.md:65-66 + :674-676 + :844 + CLAUDE.md:84.
  R3 fix flipped both to round-agnostic past-tense framing —
  round-counter sites use "Codex IMPL review in progress
  (round-by-round summary lands at close-docs)" to avoid the
  per-round-update treadmill.
- **R4** (this close-docs commit): CONVERGED. Zero findings.
  W1-W9 all clean.

**Sites surfaced per round (W9 distribution).** R1: 3 + 1 = 4
sites (3 status-prose + 1 narrow guard). R2 + trailing: 3 + 1 + 1
= 5 sites (3 wider doc-drift + 1 missing literal + 1 trailing
test-surface). R3: 4 + 4 = 8 sites (4 round-counter + 4 c8-
ignored present-tense). R4: 0 sites. Total: 17 W9 sites across 4
rounds. Pattern: each round's broader grep caught what the prior
round's narrower regex missed (asymptotic widening converged at
R4).

**Coverage residual.** Branches margin 0.37pp → 0.20pp from
the floor 95.45 (delta -0.17pp). The drop traces to four
sources: (a) leg-1 catch arm's F4 remap with `translated.map(t
=> t.columnId)` — covered by the mixed-set leg-1 failure test
introduced at R0; (b) leg-2 catch arm's `MondayCliError`
discriminator + orphan-warn envelope decoration — covered by
the orphan-warn (D1) test; (c) resolveAndTranslate / planCreate
catch arms — partially covered (instanceof-MondayCliError true-
arm covered by the translation-reject test; the
`mergeResolverWarningsIntoError` happy path with non-empty
warnings is structurally hard to exercise, so the helper's `&&
length > 0` guard was collapsed at R0 — `mergeResolverWarningsIntoError`
is a no-op on empty warnings, removing the AND branch surface
relative to the JSON path's pattern at create.ts:1010-1022);
(d) defensive non-`MondayCliError` re-throws + the
`metaSource !== undefined` unreachable arm + the
`remapped.details !== undefined` defensive guard — c8-ignored
per testing.md preferred form (single-line `c8 ignore next`).
The 0.20pp margin remains above floor; the v0.7-M42 IMPL R7
graduated discipline (collapse redundant `&& length > 0`
guards where the downstream helper is a no-op on the
sentinel value) shaved the absolute drop relative to a
mechanical port of the JSON path's catch pattern.

**Wire surface:** Two-leg dispatch — `create_item` (or
`create_subitem` for subitem path) with bundled non-file
`column_values` followed by `add_file_to_column` for the file
entry. Non-atomic by construction; cli-design §5.8 "v0.7-M43
create-time file `--set` two-leg atomicity" pins the
atomicity-envelope shape (orphan-warn per D1 closure).

**Pre-IMPL contract-term checklist (R-v0.7-NEW-4 inherited from
v0.7-M42 IMPL).**

State-current terms (should appear post-IMPL):

- `v0.7-M43`
- `file_create` (the `enforceSingleFileColumnSet` /
  `preCheckM38FileDispatch` enforcement-kind variant)
- `runItemCreateFileDispatch` (the two-leg helper)
- `create_then_file_upload_partial_failure` (atomicity-failure
  `details.reason` discriminator)
- `details.created_item_id` (orphan-warn echo)
- `create+file two-leg` / `two-leg dispatch`
- `orphan-warn` (atomicity-envelope label per D1)
- `m43_preflight_stub` (transient — disappears at IMPL; the
  literal stays RESERVED post-IMPL like M42's
  `m42_preflight_stub`)

Pre-IMPL framing terms (should NOT appear post-IMPL except in
allowed contexts):

- `file_set_on_create_unsupported` runtime throw. Allowed
  contexts post-fold:
  - (a) historical: `docs/v0.6-plan.md` M38 D6 prose entries.
  - (b) reserved-literal: docstrings explicitly noting the
    literal is RESERVED (the M38 `file-column-set.ts` docstring +
    the M43 `runItemCreateFileDispatch` helper prose both name
    it as such).
  - (c) regression-guard tests asserting the literal does NOT
    appear in runtime output post-IMPL (unit test in
    `tests/unit/api/file-column-set.test.ts` + integration test
    in `tests/integration/commands/item-create.test.ts`).
- `deferred_to: 'v0.7-M43'` — disappears post-fold. Allowed
  (a) historical only.
- `D6 closure` / `D6 carve-out` framing as forward-looking —
  becomes historical post-fold; post-IMPL prose should say
  "v0.6-M38 → v0.7-M43 D6 fold" (past tense).
- `--set <file-col>=<path>` is not supported on `monday item
  create` (M38 error message prose) — gone post-fold.
- `non-atomic post-create wire shape breaks §5.8 state safety
  without the M43 atomicity-envelope shape` (pre-M43 framing
  in cli-design / file-column-set.ts) — flipped to "the
  two-leg shape's orphan-warn envelope makes the divergence
  visible".
- `defer to v0.7-M43` / `defers to v0.7-M43` / `pending at
  v0.7-M43`.

**Empirical probe outcome (D1 driver):**

- Phase-1 introspection: confirmed
  `Mutation.delete_item(item_id: ID) → OBJECT/Item` wire shape
  — rollback IS expressible.
- Phase-2 rollback-viability: **could not run empirically**.
  Token lacked `create_item` permission in a fresh sandbox
  workspace/board (Monday "User unauthorized to perform action"
  rejection on a token-created workspace+board). Attempting
  against shared/existing boards was correctly blocked by the
  harness auto-classifier as modifying-shared-state without
  authorization for that specific board.
- M43 IMPL revisits if a user-authorized probe sandbox surfaces
  concrete rollback-reliability data; default decision below
  preserves optionality.

**Closed decisions (D1-D3):**

- **D1 — Atomicity envelope shape: (b) orphan-warn.** Failed
  leg-2 surfaces `internal_error` with
  `details.reason: 'create_then_file_upload_partial_failure'` +
  `details.created_item_id` echoing leg-1's new item ID +
  `details.column_id` + `details.cause` carrying leg-2's
  underlying error shape (`column_archived` / `validation_failed`
  / `not_found` / `file_too_large` per M31's pinned surface) +
  `details.hint` directing agents to `monday item set <iid>
  <file-col>=<path>` (retry leg-2 only) OR `monday item delete
  <iid>` (rollback). Closure rationale: default to (b) under
  uncertainty about rollback-leg reliability (probe couldn't
  run empirically; option (a) would introduce a destructive
  `delete_item` cleanup leg whose own failure mode is
  unaccounted for). (b) gives agents the orphan handle to drive
  their own cleanup AND preserves the option to retry leg-2
  alone (less wasteful than rolling back + re-creating the
  whole two-leg sequence). M43 IMPL can lift to (a) if a
  user-authorized probe later surfaces concrete rollback-
  reliability data.
- **D2 — Dry-run envelope shape:** two `planned_changes`
  entries per call — (1) `operation: 'create_item'` (or
  `'create_subitem'`) carrying `name` + bundled non-file
  `column_values` from the resolveAndTranslate leg + the
  create-mode dispatch; (2) `operation: 'add_file_to_column'`
  carrying `column_id` + `file_path` + `filename` +
  `file_size_bytes` from the local pre-check. `source`
  aggregates the pre-planner network legs (parent lookup +
  parent-board metadata for subitems + `--relative-to`
  verification) + the M38 pre-check leg + the
  resolveAndTranslate leg — mirrors the existing JSON-path
  dry-run aggregation verbatim. No multipart wire round-trip
  fires on dry-run.
- **D3 — ERROR_CODES delta: zero net change.** Registry stays
  at 29 codes. Atomicity failures route through `internal_
  error` (existing) with the `create_then_file_upload_partial_
  failure` discriminator at `details.reason`. Leg-2 column
  resolution / validation failures route through existing
  `column_archived` / `validation_failed` / `not_found` /
  `file_too_large` per M31's pinned surface (M43 reuses M31's
  `addFileToColumn` fetcher verbatim).

**IMPL Codex review estimate:** 4-5 fix-up rounds (similar
profile to M42 — non-atomic two-leg). Apply the R-v0.7-NEW-4
checklist above as the post-fix-up grep checklist from R1
onwards to short-circuit the W9 prose-drift escalation
pattern M42 absorbed across 8 rounds.

**R-class triggers anticipated at IMPL:**

- R-v0.6-NEW-1 (file pre-check + Blob helper) tips to 4th
  consumer at IMPL (M31 item upload + M31 update upload +
  M38 single-item + M43 create-time = 4); R-NEW-58 5-consumer
  graduation threshold not yet hit.
- R-v0.7-NEW-5 (fail-fast envelope decoration lift candidate)
  may tip to 3rd consumer at IMPL if the M43 orphan-warn
  envelope's `details.{created_item_id, column_id, cause,
  hint}` decoration block lands byte-equivalent to the
  fail-fast pattern at `update.ts:1334-1361` +
  `runItemUpdateBulkFileDispatch`. Lift fires AHEAD of the M43
  IMPL feat commit per R-NEW-58 norms if the 3rd consumer
  crystallises at IMPL kickoff.
- Two-leg atomicity-envelope helper potentially shared across
  M11's `item move` cross-board + M43's create+file + any
  future non-atomic mutation. R-NEW-58 ahead-of-feat trigger
  fires only if a 3rd consumer site is anticipated within v0.7
  scope (not currently — M11 is shipped; M43 is the second
  consumer pattern but lifting a 2-consumer helper is below
  the threshold).

**Pre-flight anchoring (shipped at `28f117c..ea71b55`; runtime
body lifted at IMPL R0 `5cf4365`):**

- `src/api/file-column-set.ts:enforceSingleFileColumnSet` +
  `preCheckM38FileDispatch` extended with `kind: 'file_create'`
  variant on the clean path. The v0.6-M38 `'item_create'`
  callShape gate (`'file_set_on_create_unsupported'` throw)
  folded; the universal multi-file mutex still rejects 2+ file
  `--set` entries; the mixed mutex SUPPRESSED on `'item_create'`
  per D6 asymmetry (`create_item` natively bundles non-file
  `column_values` atomically + `--name` is required).
- `src/commands/item/create.ts:runItemCreateFileDispatch` —
  helper at the bottom of the file mirroring
  `runItemUpdateBulkFileDispatch`'s shape. At pre-flight the
  body was c8-ignored and threw `internal_error` with
  `details.reason: 'm43_preflight_stub'` + `details.column_id`
  + `details.milestone: 'v0.7-M43'` + `details.hint` directing
  agents to the M38 fallback; at IMPL R0 (`5cf4365`) the
  runtime body lifted to drive the two-leg dispatch
  (`executeCreateItem` / `executeCreateSubitem` then
  `executeFileColumnSet` under the D1 orphan-warn envelope).
  The `'m43_preflight_stub'` literal stays RESERVED post-IMPL
  (regression-guarded across all 4 emit surfaces). All
  upstream argv parse + shape validation + duplicate-token
  check + create-mode resolution + M38 pre-check fire BEFORE
  the `runItemCreateFileDispatch` two-leg runtime body per
  R-NEW-76 (parseArgv-BEFORE-c8 discipline; the c8-ignore
  boundary that anchored that discipline pre-IMPL is gone
  post-`5cf4365`, but the parseArgv-first ordering itself
  stays load-bearing).
- Test surface: unit tests in
  `tests/unit/api/file-column-set.test.ts` flipped at pre-flight
  from asserting `'file_set_on_create_unsupported'` throw to
  asserting `kind: 'file_create'` return + new regression-guard
  asserting the v0.6-M38 literal never appears in the
  enforcement throws (stays valid post-IMPL). Integration tests
  in `tests/integration/commands/item-create.test.ts` re-flipped
  at IMPL R0 — pre-flight flipped from
  `'file_set_on_create_unsupported'` envelope to
  `'m43_preflight_stub'` envelope assertions; IMPL R0 replaced
  the 3 pre-flight stub assertions with 12 IMPL tests covering
  the live two-leg happy path (top-level + subitem), the D6
  mixed-set asymmetry, dry-run two-`planned_changes`, the D1
  orphan-warn envelope, leg-1 failure w/ + w/o F4 remap, the
  resolveAndTranslate translation-reject catch arm, and the
  upfront `precheckLocalFile` ENOENT abort. Both reserved
  literals (`'m43_preflight_stub'` + `'file_set_on_create_unsupported'`)
  are regression-guarded across all 4 emit surfaces by a
  dedicated `assertLiteralAbsent`-helper test + inline at each
  failure-surface test.

### v0.7 release-prep (closed at `9b7e9ad..3e46f59`; published to npm at 2026-05-20T15:48:07Z; post-publish drift swept at `<post-publish-audit-commit>`)

5 commits with diff + 1 zero-diff envelope-snapshot probe folded
into this close-docs prose (the v0.5 / v0.6 release-prep precedent
— zero-diff probes don't warrant their own commit). Mirrors the
v0.4 / v0.5 / v0.6 release-prep cadence verbatim modulo the
v0.7-specific findings:

- **`9b7e9ad`** — `docs(v0.7-release-prep): slip stale v0.7
  deferral to v0.8 + ToC v0.7-M42/M43 friendly file --set sync`.
  R-NEW-82 graduated cross-doc grep ran + caught one stale
  `deferred_to: "v0.7"` slot (multi-level subitem `--parent`
  rejection — slipped from `"v0.7"` → `"v0.8"` across 3 sites:
  runtime literal at `src/commands/item/create.ts:653` + matching
  integration test pin at `tests/integration/commands/item-
  create.test.ts:1149` + doc prose narrative at `docs/output-
  shapes.md:2586`) + one ToC drift (v0.7-M42 + v0.7-M43 friendly
  file `--set` annotations missing from `docs/output-shapes.md:61`
  `item (mutations)` row — M42 / M43 close-docs added substantive
  output-shape sections at lines 2399 + 2524 but didn't propagate
  the milestone tags to the ToC verb-row; sibling drift mirrors
  v0.6-M38's ToC gap that v0.6 release-prep ToC audit caught —
  R-NEW-82 5th-consecutive consumer ratified). Slip target was
  v0.8 (not v0.7.x) — Monday's `sub_items_board` carries no
  `subtasks` column at API `2026-01`, v0.7 pivoted away from
  API `2026-04` at 2026-05-20, so the data-model probe gate
  moves to v0.8's planned `2026-07` pin. Slip-history counts
  bumped 3 → 4 consecutive release-preps. 3 files / 11 changed
  / 9 deletions.
- **`3ca51ea`** — `docs(readme): v0.7 quickstart + scope refresh`.
  Quickstart step 13 extended to enumerate all four CLI shapes
  reaching Monday's `add_file_to_column` multipart wire (M38
  single-item + M42 bulk + M43 create-time + M31 verb-shaped
  alternative) per v0.7-plan §4's "M42 / M43 fold into M38's
  existing example slot" directive. Scope section reshaped:
  v0.7.0 (current) / v0.6.0 (previous) / v0.5.0 (prior) /
  v0.4.0 (earlier) / v0.3.0 (an even-earlier) / v0.2.0
  (foundation-of-mutations) / v0.1.0 (foundation milestone). New
  "What v0.7 added (M42 + M43)" per-milestone bullet block with
  load-bearing facts (M42 `dispatchParallel`-over-shared-
  `MultipartTransport` + new `data.summary` slots; M43 two-leg
  dispatch + orphan-warn `create_then_file_upload_partial_
  failure` discriminator + D6 mixed-set mutex SUPPRESSED on
  `'item_create'`; pre-v0.7 literals stay RESERVED + regression-
  guarded). "v0.7 (next)" block reframed as "v0.8 (next)" with
  M39 / M40 / M41 explicit-deferred narrative. OAuth deferral
  version reference bumped v0.6.x / v0.7 → v0.7.x / v0.8;
  `cli_version` sample bumped 0.6.0 → 0.7.0. 1 file / 144
  insertions / 46 deletions.
- **`75c8831`** — `chore(v0.7-release-prep): bump version 0.6.0
  → 0.7.0 + audit-fix brace-expansion`. package.json field flip
  + matching package-lock.json regeneration. Audit-fix folded
  in per the v0.6 release-prep precedent: `npm audit` flagged
  `brace-expansion@5.0.2` (moderate severity — GHSA-jxxr-4gwj-
  5jf2 large-numeric-range DoS-protection defeat); `npm audit
  fix` cleanly resolved via the lockfile to `5.0.5+` (non-
  breaking transitive through eslint's minimatch chain). `npm
  audit` reports `0 vulnerabilities` post-fix. Moderate severity
  is below security.md's "high = merge blocker" threshold; the
  inline-fix-at-version-bump precedent established at v0.6
  release-prep applies symmetrically. 2 files / 6 changed.
- **`9ebaa81`** — `docs(v0.7-release-prep): CHANGELOG [0.7.0]
  entry — bulk + create-time file --set carve-out folds (M42 +
  M43)`. Mirrors the `[0.6.0]` entry shape verbatim (Breaking
  changes / Surface / Output contract additions / Upgrade notes
  / Internals / Tests + quality gates / Documentation). Two-
  milestone v0.7 scope (M42 + M43) reads as paired-milestone
  narrative vs v0.6's single-milestone form (M38). Captures the
  v0.7 pivot note (M39 / M40 / M41 deferred pending SDK 15.x
  native `2026-04`), the universal callShape mutex collapse
  post-M42 + M43, the M43 orphan-warn envelope, the R-class
  graduation events (R-v0.6-NEW-1 / R-v0.6-NEW-2 / R-NEW-82 /
  R-NEW-84 / R-v0.7-NEW-4 / R-NEW-76). 1 file / 388 insertions.
- **`3e46f59`** — `docs(v0.7-release-prep-close):
  close-docs sweep — status flip + §1 SHIPPED row + §3 release-prep
  entry + R-NEW-82/84 ratification`. CLAUDE.md Status block
  flipped to "v0.7.0 ready for publish — pending user push
  authorization + npm publish". Live numbers refreshed
  (4124 + 1 skipped tests unchanged across release-prep; coverage
  98.79 / 95.65 / 99.16 / 99.08 held at 0.20pp branches margin;
  package.json 0.7.0; npm audit 0 vulnerabilities post-fix).
  Next-session pointer flipped to "v0.7.x candidate-selection
  (externally blocked on v0.7.0 push + npm publish first)" per
  R-NEW-75. §1 release-prep row flipped from SKELETON to
  SHIPPED with SHA range (with `<this-commit>` placeholder for
  the close-docs SHA itself; backfilled post-publish per
  R-v0.5-NEW-24). §3 release-prep close subsection added (this
  block). §22 R-NEW-82 entry flipped to past-tense 5th-
  consecutive ratification + R-NEW-84 entry flipped to 6th
  supporting instance. 2 files / 222 insertions / 81 deletions.

**Post-publish flip + post-cluster refactor-audit at
`<post-publish-audit-commit>`** (single combined commit per R-v0.5-NEW-
24's graduated parallel-sweep recipe; not part of the 5-commit
release-prep cluster but narrated here for traceability):

- npm publish landed at 2026-05-20T15:48:07Z; annotated
  `v0.7.0` tag pushed pointing at `3e46f59`; GitHub release
  live at https://github.com/Firer/monday-cli/releases/tag/v0.7.0.
- CLAUDE.md "Published" block flipped from "ready for publish"
  → "published — release complete" with the npm timestamp +
  tag URL + GitHub release URL. CLAUDE.md "Next session"
  pointer dropped the "EXTERNALLY BLOCKED on publish/tag" gate.
- v0.7-plan.md `<this-commit>` placeholders backfilled
  to `3e46f59` across §1 SHIPPED row + §3 release-prep heading
  + §3 release-prep close commit bullet (above). The new
  `<post-publish-audit-commit>` placeholder slot in the heading
  gets backfilled at the next post-publish audit OR stays as a
  placeholder pending v0.5-plan.md's convention.
- §7 release exit checklist filled (this audit).
- §22 R-class register updated: R-v0.5-NEW-24 ratified at 3rd
  supporting instance (1st successful parallel-sweep
  application; previously v0.5 + v0.6 post-publish flips
  touched only CLAUDE.md); R-v0.7-NEW-8 filed (test-flake
  observed at `3e46f59` pre-commit gate run — 1/4124 transient
  failure cleared on next two consecutive runs; low-priority
  watch-item).

**v0.7 release-prep findings:** Contract drift findings = 1
stale `deferred_to: "v0.7"` slot (3 sites — runtime + test +
output-shapes) + 1 ToC drift (M42 + M43 missing from `item
(mutations)` row), all caught at the cluster's R-NEW-82 audit
step (mirrors v0.6 release-prep's 1 stale slot + 1 ToC drift —
v0.7 count matches v0.6's small-scope cluster pattern; both
release-preps shipped 1-2 feature milestones). Decisions closed
= none (zero new contract surface at release-prep). R-class
state = no code-lift movement at release-prep; R-NEW-82
ratifies at 5th-consecutive consumer (v0.3-M28 1st + v0.4 2nd
+ v0.5 3rd + v0.6 4th + v0.7 5th); R-NEW-84 ratifies at 6th
supporting instance (v0.7 release-prep IS the symmetric
positive-case application — zero production `src/**/*.ts`
semantic changes modulo the literal `'v0.7'` → `'v0.8'` flip
in the multi-level subitem rejection slot, which is mechanical
not semantic; Codex review skipped; gates alone verified).

**Lessons folded forward:**

1. **R-NEW-82's 5th-consecutive ratification** confirms the
   graduated discipline holds firm across the post-pivot
   release-prep too — even a release-prep cluster covering
   feature milestones that landed in a different session
   surface still catches ≥1 stale `deferred_to` slot AND ≥1
   ToC drift sub-class finding. The cross-doc grep recipe +
   the three per-site decisions (slip / drop slot / drop
   rejection) + the ToC row drift sub-class audit-step remain
   stable.
2. **R-NEW-84's 6th symmetric application** — single-feature
   (v0.6), four-feature (v0.5), and two-feature (v0.7)
   release-prep clusters all fire the rule cleanly; the rule
   is scope-invariant. Pivot-rooted release-preps (v0.7
   shipping a reduced-scope set after a mid-cycle pivot)
   apply the rule identically to non-pivot release-preps.
3. **Audit-fix folded into version-bump per the v0.6
   release-prep precedent** — establishes a 2nd-consecutive
   consumer (v0.6 + v0.7). v0.5 release-prep did NOT surface
   an audit-fix (no high-severity advisories in the lockfile
   during that cluster); v0.6 + v0.7 release-preps DID + both
   handled inline. Future release-preps inherit the inline-
   audit-fix-at-version-bump cadence on any moderate-or-above
   transitive flag.
4. **Slip-history descriptor pattern.** v0.7 release-prep
   reused v0.6's cascade pattern with one new descriptor
   ("an even-earlier release") coined for the v0.3 slot —
   future release-preps continue the recipe by coining one
   new descriptor per cascade (v0.8 would coin a fresh label
   for v0.4's new slot).

## 4. Cross-cutting "definition of done" per command

Identical to v0.5-plan §4 / v0.6-plan §4. Each new surface needs:

1. Unit test coverage at the floor (95.45% branches).
2. Integration tests against cassette-pinned wire responses.
3. E2E test against the fixture-server subprocess.
4. Envelope-snapshot pin for every success + failure shape.
5. `output-shapes.md` per-verb section.
6. `cli-design.md` §4.3 row.
7. README quickstart example IF the surface is high-visibility
   (M40 / M41 likely; M42 / M43 fold into M38's existing example).
8. CHANGELOG entry at release-prep.

See [`docs/v0.6-plan.md`](./v0.6-plan.md) §4 for full detail.

## 5. Testing strategy

Identical to v0.6-plan §5. Five test layers: unit, integration
(in-process `FixtureTransport` + `MultipartFixtureTransport`),
E2E (subprocess against fixture server), envelope-shape snapshot
suite, published-tarball E2E.

See [`docs/v0.6-plan.md`](./v0.6-plan.md) §5 for full detail.

## 6. Risk register

> **Per-milestone risks land at each milestone's pre-flight
> kickoff.** Skeleton entries:

- **R1 — SDK 15.x publication timing.** If SDK 15.x baking
  `2026-04` is not published at v0.7 kickoff, M39 D1 decides
  between waiting (release blocked on external timeline) vs
  string-literal override on SDK 14.0.0 (maintenance burden on
  hand-rolled types for new 2026-04 fields). Mitigation:
  re-check npm at every session start; the override path is
  always available.
- **R2 — 2026-04 breaking-change blast radius.** The single
  breaking change (`AggregateGroupByResult.value_*` collapse) is
  unused by current CLI surface per the agent's 2026-05-18
  changelog research; re-verify at M39 pre-flight via
  `grep -r aggregate_group_by src/`. Mitigation: defensive
  sweep at M39 catches any introduced consumer in the v0.6 →
  v0.7 interim.
- **R3 — Empirical-probe surprises on novel wire surfaces.**
  M40 (`set_item_description_content`) + M41 (`create_doc_blocks`)
  + M43 (two-leg atomicity) carry probe-driven design decisions
  at pre-flight. Mitigation: budget 1-2 extra IMPL rounds per
  milestone if probes surface wire-shape surprises (per v0.5-M37
  precedent's custom-OBJECT projection pattern).
- **R4 — Bulk file `--set` `--concurrency` interaction.** M42's
  concurrent multipart over shared MultipartTransport is the
  largest novelty in v0.7 IMPL scope; per-token rate-limit
  semantics at the wire side are pre-flight probe targets.

## 7. v0.7 release exit checklist

Mirrors v0.5-plan §7 / v0.6-plan §7 verbatim. At v0.7 release-prep,
the cluster followed the v0.4 / v0.5 / v0.6 release-prep cadence
(5 commits with diff + 1 zero-diff envelope-snapshot probe folded
into close-docs prose per the v0.5 precedent: ToC audit /
`deferred_to: "v0.7"` slip + README v0.7 quickstart + version
bump + audit-fix + CHANGELOG `[0.7.0]` entry + close-docs sweep).
R-NEW-82 graduated discipline applied: release-prep cross-doc grep
for stale `deferred_to: "v0.7"` slots ran at the ToC-audit step.

Exit checklist (every line ticked at v0.7 release-prep close +
post-publish audit):

- [x] Envelope-snapshot refresh probe ran clean (zero diff vs M43
  IMPL close) — folded into the close-docs prose per the v0.5 /
  v0.6 precedent rather than warranting a separate commit.
- [x] R-NEW-82 cross-doc grep ran — caught one stale
  `deferred_to: "v0.7"` slot (multi-level subitem `--parent`
  rejection at `src/commands/item/create.ts:653` + matching
  integration test pin at
  `tests/integration/commands/item-create.test.ts:1149` +
  `docs/output-shapes.md:2586` prose narrative) + one ToC
  drift (v0.7-M42 + v0.7-M43 friendly file `--set` annotations
  missing from `docs/output-shapes.md:61` `item (mutations)`
  row). Slipped to `"v0.8"` + ToC annotated at commit `9b7e9ad`.
  5th-consecutive consumer ratification of R-NEW-82's graduated
  discipline (v0.3-M28 + v0.4 + v0.5 + v0.6 + v0.7).
- [x] README v0.7 quickstart + scope refresh shipped at
  `3ca51ea`. Step 13 extended with the M42 bulk + M43 create-
  time forms reaching the M31 `add_file_to_column` multipart
  wire alongside the existing M38 single-item form. Scope
  section reshaped around v0.7.0 (current) / v0.6.0 (previous)
  / v0.5.0 (prior) / v0.4.0 (earlier) / v0.3.0 (an even-earlier)
  / v0.2.0 / v0.1.0 per-version layout. v0.8 (next) block
  enumerates the carry-forward backlog + the v0.7-deferred M39
  / M40 / M41 cluster pending SDK 15.x native `2026-04`.
- [x] Version bump `0.6.0` → `0.7.0` shipped at `75c8831`
  (package.json + package-lock.json two-site flip). `npm audit`
  flagged a transitive `brace-expansion@5.0.2` (moderate
  severity — GHSA-jxxr-4gwj-5jf2 large-numeric-range DoS-
  protection defeat); `npm audit fix` cleanly resolved to
  `5.0.5+` (non-breaking transitive through eslint's minimatch
  chain). Audit-fix folded into the version-bump commit per
  the v0.6 release-prep precedent (2nd-consecutive consumer;
  watch-item per §22 R-NEW-82 sibling). `npm audit` reports
  `0 vulnerabilities` post-fix.
- [x] CHANGELOG `[0.7.0]` entry shipped at `9ebaa81` (388
  insertions; mirrors `[0.6.0]` shape verbatim — Breaking
  changes / Surface / Output contract additions / Upgrade
  notes / Internals / Tests + quality gates / Documentation
  sections). Two-milestone v0.7 scope (M42 + M43) reads as
  paired-milestone narrative vs v0.6's single-milestone form
  (M38). Captures the v0.7 pivot note (M39 / M40 / M41
  deferred pending SDK 15.x native `2026-04`), the universal
  callShape mutex collapse, the M43 orphan-warn envelope, and
  the R-class graduation events.
- [x] CLAUDE.md Status block flipped to "v0.7.0 ready for
  publish — release-prep closed end-to-end" at the close-docs
  sweep commit `3e46f59`. v0.7-plan §3 release-prep subsection
  added at the end of §3 (commit `3e46f59`). §22 R-NEW-82 +
  R-NEW-84 entries flipped to past-tense ratification (commit
  `3e46f59`). Next-session pointer flipped from "v0.7 release-
  prep" to "v0.7.x candidate-selection".
- [x] **Live numbers** at v0.7.0 release-prep close: 4124 + 1
  skipped tests across 172 files (unchanged from M43 IMPL close
  — release-prep added zero new tests; one test-description
  string reworded at the deferral-slip commit); coverage
  98.79 / 95.65 / 99.16 / 99.08 at floor 95 / 95.45 / 95 / 95
  (branches margin **0.20pp**, held flat across release-prep);
  29 ERROR_CODES; 117 commands; `package.json` 0.7.0; `npm
  audit` 0 vulnerabilities post-fix.
- [x] Annotated `v0.7.0` tag created locally + pushed to
  `origin/main` (tag points at `3e46f59`, the close-docs sweep
  commit — released at the cluster's final step). Tag object
  SHA `550e2dc`.
- [x] GitHub release `v0.7.0` created from the CHANGELOG body
  (live at https://github.com/Firer/monday-cli/releases/tag/v0.7.0).
  Published at 2026-05-20T15:52:47Z.
- [x] `npm publish` shipped to npm `latest` dist-tag —
  `monday-cli@0.7.0` published 2026-05-20T15:48:07Z (per
  `npm view monday-cli time` records). Post-publish CLAUDE.md
  status-flip + npm timestamp record + R-v0.5-NEW-24 parallel-
  sweep + R-v0.7-NEW-8 filing all landed at the post-publish
  refactor-audit commit `<post-publish-audit-commit>`.

## 8. Decisions (open questions to close before each milestone)

> **Per-milestone D-list lands at each milestone's pre-flight
> contract diff commit.** Skeleton above (§3) sketches the
> expected decision dimensions per milestone.

## 9. Preconditions

> **Per-milestone preconditions lands at each milestone's
> pre-flight kickoff.** Skeleton above (§3) sketches the
> expected empirical-probe targets per milestone.

## 10. Post-mortem template

Mirrors v0.5-plan §10 / v0.6-plan §10 verbatim. Per-milestone:
round-by-round Codex review summary + contract drift findings +
lessons learned + R-class candidate filings. Lands at the
milestone close-docs commit.

See [`docs/v0.6-plan.md`](./v0.6-plan.md) §10 for full detail.

## 22. R-class register

> **Status:** populated at v0.7-M42 IMPL post-close refactor-audit
> (this commit) — close-docs sweep at `8878745` updated §3 + CLAUDE.md
> only; the §22 R-class register fill follows the v0.6-M38 post-close
> audit precedent (`aa10744`). Mirrors v0.6-plan §22 structure.
> Subsequent M40 / M41 / M43 close-docs (+ post-close audits)
> append to the same sections.

### Watch-items carried forward from v0.6

- **R-NEW-58 lift-ahead-of-feat** (graduated v0.4-M31 IMPL). Fires
  at every IMPL kickoff scan. Expected M40-M43 targets: file-source
  consumer count scaling (3 → 4+ at M42 / M43), batch-projection
  helper for partial-success envelopes at M41 / M42, atomicity-
  envelope helper at M43. **M42 IMPL outcome:** the per-item
  projection helper inlines the fail-fast error-decoration block
  rather than lifting to a shared helper (2-consumer threshold —
  R-NEW-58 norms wait for 3rd consumer); filed forward as
  [[R-v0.7-NEW-5]] watch-item with the in-code "Future lift
  candidate" docstring at `src/commands/item/update.ts:1621-1627`
  pointing at it. M43 create-time fold likely tips to 3rd consumer.
- **R-NEW-72 post-fix-up cross-doc grep** (graduated v0.4-M33; 8th
  instance ratified v0.5-M37 IMPL). Search paths per v0.5-NEW-19:
  `src/api/*.ts` + `src/commands/**/*.ts` + `src/commands/index.ts`
  + `docs/*.md`. Noun-stem regex matching. **9th-12th instances
  ratified at v0.7-M42 IMPL R1-R8** — each Codex round triggered a
  fresh post-fix-up grep, and rounds R2-R8 each surfaced a wider
  W9-prose layer the prior round's regex missed (the asymptotic
  convergence pattern that drove [[R-v0.7-NEW-4]]'s graduation).
  **13th-17th instances ratified at v0.7-M43 pre-flight R1-R5**
  — same asymptotic pattern: R1 5 findings, R2 2, R3 1, R4 2,
  R5 1 (CONVERGED). Cumulative `4950330..ab1d6dd`. The
  R-v0.7-NEW-4 checklist short-circuited some drift but the
  M43 pre-flight surfaced a **search-path widening lesson**:
  the inherited checklist's enumeration covered `src/api/file-
  column-set.ts` + `docs/cli-design.md` + `docs/v0.7-plan.md`
  (the M42 epicenter sites) but missed runtime hint surfaces
  (`src/api/raw-write.ts` + `src/api/column-types.ts` +
  `src/commands/board/column-create.ts`) and architecture /
  README prose. Each pre-flight round's grep widened the
  search-path set; round 1 found 4 sites, round 2 found 6
  more, round 3 found 6 more, round 4 found 2 more, round 5
  found 1 trailing site. **Lesson for R-v0.7-NEW-4 checklist
  extension at M43 close-docs**: the inherited-checklist
  prose-term enumeration needs broader file-set coverage
  including every runtime hint surface (raw-write, column-
  types, board column-create) + architecture / README docs in
  addition to the M42-original sites. The discipline holds;
  what scaled was the term checklist (R-v0.7-NEW-4 extends
  R-NEW-72 with a pre-IMPL contract-term enumeration so each
  round's grep starts from a richer base rather than
  re-deriving from round-N findings); M43 pre-flight extends
  that further with a broader file-set coverage requirement.
- **R-NEW-75 candidate-selection 5-dimension framework**
  (graduated v0.4-M33). **Applied for 5th consecutive cycle at
  v0.7 kickoff session 2026-05-18 (this commit).** The kickoff
  session collapsed to D1 closure only because the v0.7-plan
  SKELETON (`a7a890d`) had pre-selected the M39-M43 candidate
  set forward — the 5-dimension scoring re-verified each
  milestone still scored as predicted at SKELETON-filing time.
  Sub-pattern (SKELETON-pre-selected kickoff collapses to
  Decision-closure-only) carries forward as [[R-v0.7-NEW-2]].
  **Caveat surfaced during candidate-selection scrutiny at this
  kickoff:** the SKELETON-pre-selected collapse can SKIP
  dependency-graph re-examination of the ship-order question
  (M42 + M43 are API-pin-independent and ship first without
  M39's external-dep gate — but the original framing landed on
  M39 first because of milestone-ID order rather than dependency
  order, and only the override-cost-scrutiny reframing surfaced
  the sequencing optimization). When SKELETON pre-selects, the
  candidate-selection collapse still owes the framework's
  *ordering* dimension — separately from the kept-vs-cut
  candidate dimension. Sub-pattern correction: at SKELETON-
  pre-selected kickoffs, score the SEQUENCING dimension across
  selected candidates (dependency graph, external-dep gating,
  carry-forward-fold timing) even when the candidate SET is
  pre-pinned. Recurs as a 2nd-consumer trigger when the next
  SKELETON-pre-selected kickoff (v0.8 kickoff) hits a similar
  sequencing-vs-set-selection ambiguity.
- **R-NEW-76 parseArgv-before-c8** (graduated v0.5-M34). Pre-flight
  stub discipline; applies to every M40/M41/M42/M43 pre-flight stub.
  **Applied at v0.7-M42 pre-flight (`160330b`)** — argv parse +
  `parseBrandedListArg` + `enforceDestructiveGate` + `parseGlobalFlags`
  fired BEFORE the c8-ignored stub body; the three INTERNAL
  conventions inside that body crystallized as [[R-v0.7-NEW-3]]
  (resolved at this close-docs; see below). **Re-applied at
  v0.7-M43 pre-flight (`28f117c`)** — `parseArgv` +
  `splitSetExpression` + `parseSetRawExpression` +
  `checkDuplicateTokens` + `validateInputShape` +
  `resolveCreateMode` + `preCheckM38FileDispatch` ALL fire
  BEFORE the c8-block-wrap around `runItemCreateFileDispatch`'s
  body. Codex pre-flight R1-R5 verified the ordering held
  across every fix-up round (W4 cleared all 5 rounds).
  **Survived M43 IMPL R0 (`5cf4365`)** — the c8-ignore
  boundary that anchored the discipline pre-IMPL is gone
  post-IMPL (runtime body lifted), but the parseArgv-first
  ordering itself stays load-bearing: every upstream parse /
  shape / resolution leg still fires BEFORE the wire
  dispatch helpers inside `runItemCreateFileDispatch`, so
  argv-level failures continue to surface as `usage_error`
  (exit 1) instead of leaking through as `internal_error`
  (exit 2). The discipline graduates from "stub-anchored
  ordering invariant" to "wire-dispatch-anchored ordering
  invariant" without contract changes — useful pattern for
  future post-IMPL re-applications.
- **R-NEW-82 release-prep deferred-to slip audit** (graduated v0.5
  release-prep; ratified 4th-consecutive at v0.6 release-prep).
  Expected 5th-consecutive consumer at v0.7 release-prep —
  `deferred_to: "v0.7"` cross-doc grep.
- **R-NEW-84 skip Codex review on mechanical clusters** (graduated
  v0.5-M34 negative; ratified at v0.5 + v0.6 release-prep).
  **Applied at v0.7 kickoff session 2026-05-18 (`75b1cf9`) —
  docs-only mechanical cluster, gates carried verification, Codex
  skipped.** **Re-applied at v0.7-M42 IMPL close-docs (`8878745`)**
  — `CLAUDE.md` + `docs/v0.7-plan.md` only, zero `src/**/*.ts`
  changes. Expected further application at v0.7 release-prep + at
  M39 (mechanical pin-bump cluster).
- **R-v0.6-NEW-1 file pre-check + Blob-construction helper**
  (shipped v0.6-M38). 3 consumers post-lift; M42 + M43 confirmed
  as consumers 4 + 5. **M42 IMPL confirmed as 4th consumer**
  (`runItemUpdateBulkFileDispatch` calls `precheckLocalFile` once
  upfront per D3 closure + `buildBlobFromPath` per-item inside
  the dispatch loop). **M43 IMPL confirmed as 5th consumer at
  `5cf4365`** (`runItemCreateFileDispatch` calls `precheckLocalFile`
  once upfront per the §5.8 atomicity-before-wire discipline;
  `buildBlobFromPath` is invoked transitively through M38's
  `executeFileColumnSet` on leg-2). Helper scaled cleanly to
  consumer-5 with zero internal-shape changes — the lift's
  single-arg `precheckLocalFile(rawPath)` signature accommodated
  both the per-item-fan-out (M42) and the single-item-leg-2 (M43)
  call shapes without modification. **Graduation criterion met
  (5-consumer threshold reached).** The helper is now structurally
  load-bearing across the entire files-shaped friendly `--set`
  family (single-item `item set` + single-item `item update` +
  bulk `item update --where` + create-time `item create`) plus
  M31's verb-shaped `item upload` / `update upload`. The 5-
  consumer crystallisation also confirms the v0.6-M38 lift
  decision (the pre-lift trace where the same precheck + Blob
  pattern would have been needed in 5 places — the lift saved
  ~150 lines of duplicated code + ~5 inconsistency risks). No
  further work item — the helper is shipped; future consumers
  (multi-file `--set` if it ever ships, stdin file `--set` per
  the v0.6-M38 D7 deferral) inherit the same 5-consumer-stable
  surface.
- **R-v0.6-NEW-2 `details.reason` discriminator pattern.** 4
  supporting instances at v0.6-M38; M42 IMPL added zero (D4
  zero-delta closure); **M43 IMPL added the 5th** via D1
  closure. **M43 IMPL confirmed as 5th supporting instance at
  `5cf4365`** — leg-2 failure surfaces `internal_error` with
  `details.reason: 'create_then_file_upload_partial_failure'`
  (the orphan-warn atomicity-envelope discriminator). The 5
  state-current supporting instances post-M43:
  `'file_not_readable'` + `'file_empty'` (M31 / M38 / M42 /
  M43 local pre-check) + `'multi_file_set_unsupported'` (M38 /
  M42 / M43 universal mutex) + `'mixed_file_and_value_sets'`
  (M38 / M42 single-item + bulk; SUPPRESSED on M43 per D6
  asymmetry) + `'create_then_file_upload_partial_failure'`
  (M43 orphan-warn). The two reserved-literal stub
  discriminators (`'m42_preflight_stub'` + `'m43_preflight_stub'`)
  count separately per the (a)/(b)/(c) allowed-context
  taxonomy ratified at [[R-v0.7-NEW-3]] — they're
  regression-guarded post-IMPL but stay in the codebase as
  RESERVED literals; new discriminators must NOT reuse those
  string values.

  **Graduation criterion met (5-consumer threshold reached).**
  The `details.reason` discriminator pattern is now structurally
  load-bearing across the v0.6 files-shaped friendly write
  family — every error envelope from the entire file-dispatch
  surface (single-item + bulk + create-time + the M31 verb-
  shaped upload) routes through a `details.reason` slot that
  agents can branch on without parsing English messages. Future
  consumers (a hypothetical bulk-create-time path; multi-file
  `--set` if it ships; stdin file `--set` per D7) inherit the
  same pattern; no further work item — the discriminator
  convention is stable at 5 consumers + ready to grow
  additively without re-thinking the shape.

### New R-class candidates surfaced at v0.7

Additional R-class candidates surfaced at the v0.7 kickoff
candidate-selection session (this commit):

- **R-v0.7-NEW-1 — External-dependency wait-with-bounded-fallback
  Decision-closure structure.** First explicit instantiation of a
  Decision-closure shape that gates a milestone on an
  unpublished external dependency: enumerate the external blocker
  (SDK 15.x publication) + the fallback paths (continued wait /
  string-literal override / sequence-shuffle to independent
  milestones) + a bounded re-evaluation cadence (re-probe at
  every session start, re-decide at 2-4 sessions if blocker
  persists) + a documented mitigation in the §6 risk register.
  1 supporting instance — v0.7-M39 D1 closure at v0.7 kickoff
  2026-05-18 (this commit). The "Next session" pointer in
  `CLAUDE.md` codifies the re-probe instruction inline
  (`npm view @mondaydotcomorg/api versions --json | tail`) so
  the next session re-evaluates without re-deriving the probe
  shape. **Structure validated by intra-session reconsideration
  during candidate-selection:** an initial WAIT framing was
  reconsidered within the same kickoff session after follow-up
  scrutiny on override costs surfaced that the sequence-shuffle
  fallback was strictly dominant (zero override commitment + 2
  milestones of progress + preserves optionality across all three
  fallback paths). Fallback (iii) [sequence-shuffle to M42 → M43
  first] was selected at re-scoring rather than after 2-4
  sessions of waiting. **Lesson:** the bounded-fallback
  enumeration's value isn't only inter-session timescale survival
  — the pre-enumerated fallback paths also let the closure handle
  intra-session reconsideration cleanly without ripping out the
  structure (the §1 status block's SKELETON-ratification slot
  carries the same closure surface regardless of which fallback
  path is selected). **Priority: LOW** — process
  discipline. Fires at 2nd consumer when another milestone
  Decision gates on an external blocker (next plausible site:
  v0.8-M44 if Monday API `2026-07`'s user-entity migration
  timeline depends on SDK 16.x publication; speculative). At
  3rd consumer, graduate to a `.claude/rules/workflow.md`
  "External-dep wait closure" entry alongside the existing
  milestone-workflow disciplines, with the intra-session-
  reconsideration property captured as a key feature.
  Cross-references R-NEW-75 (candidate-selection framework — the
  external-dep blocker dimension wasn't present at R-NEW-75's
  v0.4-M33 graduation; this entry extends the framework's
  decision-closure side with a new closure-shape; the missed
  sequencing-dimension caveat on R-NEW-75's updated entry is
  what the immediate fallback trigger surfaced) + R-v0.7-NEW-2
  (forward-filed SKELETON enables the bounded-wait fallback —
  the v0.7-plan §1 status block's SKELETON-ratification cleanly
  carries the D1 closure stance forward across sessions, and
  also cleanly carries the closure-flip when fallbacks trigger).
- **R-v0.7-NEW-2 — Forward-filed SKELETON plan-doc dual filing
  at post-release session.** First instance of filing multiple
  version-bump plan-docs forward in SKELETON form at a post-
  release session, before any of those versions enters its own
  kickoff cycle. `a7a890d` filed `docs/v0.7-plan.md` +
  `docs/v0.8-plan.md` together at the post-v0.6.0 publish
  session (2026-05-18), enumerating in-scope milestones +
  explicit out-of-scope rationale + carry-forward backlog +
  per-milestone D-skeleton sections for each — all subject to
  ratification at the corresponding version's kickoff. The
  status-block flow ("SKELETON" → "SKELETON ratified at
  `<sha>` — D1 closed `<stance>`") provides a stable handoff
  shape that survives across multiple sessions: prior plan-docs
  (`docs/v0.6-plan.md` first filed at `0cb8b69` M38 pre-flight,
  not pre-kickoff) didn't use this pattern. 2 supporting
  instances at `a7a890d` (v0.7-plan SKELETON + v0.8-plan
  SKELETON filed together). **Priority: LOW** — process
  discipline; benefit is amortised across multi-version planning
  horizons. Fires at 2nd ratification cycle (i.e., when v0.8
  kickoff ratifies the v0.8-plan SKELETON, expected post-v0.7.0
  publish), which IS the validation event — if the forward-
  filed v0.8 SKELETON survives ratification with minimal
  rework, the pattern earns its keep; if v0.8 kickoff finds
  the SKELETON materially stale, the pattern earns a discount
  factor. At v0.8 ratification, graduate to a
  `.claude/rules/workflow.md` "Forward-filed SKELETON plan-docs"
  entry (with the post-release-session timing + ratification-
  at-kickoff handoff shape) IF the v0.8 SKELETON survives
  cleanly; OR file a learnings sub-entry capturing the
  material-staleness category if it doesn't. Cross-references
  R-NEW-75 (candidate-selection framework — SKELETON-pre-selected
  kickoff collapses the framework's full 6-step session shape
  to D-closure-only as documented in R-NEW-75's updated entry
  above) + R-v0.7-NEW-1 (the bounded-wait Decision-closure
  shape relies on the SKELETON's stable §1 + §3 surface to
  carry D-closure stances across sessions).
- **R-v0.7-NEW-3 — Pre-flight stub-body-internal contract
  pattern.** Sub-discipline of R-NEW-76 (parseArgv-BEFORE-c8).
  R-NEW-76 covers the BOUNDARY around the c8-ignored stub
  body (argv parse + parse-boundary helpers fire BEFORE the
  ignore block); R-v0.7-NEW-3 covers the THREE INTERNAL
  conventions inside the stub body that surfaced at v0.7-M42
  pre-flight (`160330b`):

  1. **Async-signature anchor via `await Promise.resolve()`.**
     Pre-flight stub helpers declared `async` (to anchor the
     IMPL's `async` signature so callers don't have to re-shape)
     need at least one `await` to satisfy
     `@typescript-eslint/require-await`. A single `await
     Promise.resolve()` at the top of the c8-ignored body is
     the minimal-noise workaround — preserves the async
     contract, costs one micro-task tick (irrelevant at the
     stub-throw level), satisfies lint, and self-documents as
     "IMPL replaces this with real `await` calls". Tested at
     `src/commands/item/update.ts:1729-1734` in the M42 stub.
  2. **`details.reason: '<milestone>_preflight_stub'`
     discriminator literal + `details.milestone` companion
     slot.** Pre-flight stub throws should surface
     `internal_error` (not `usage_error` — argv IS accepted at
     pre-flight; the gap is server-side) with a milestone-
     tagged `details.reason` literal (`'m42_preflight_stub'`,
     `'m43_preflight_stub'`, etc.) AND a `details.milestone`
     slot echoing the milestone ID (`'v0.7-M42'`). Agents
     reading the stub-throw envelope branch uniformly on
     `details.reason` AND get the milestone tracker for grep.
     The two-slot convention beats a single multi-purpose slot
     because `details.reason` is the existing discriminator
     pattern (R-v0.6-NEW-2 4 supporting instances) and
     `details.milestone` is a clean ID slot for milestone
     lookup. Tested at `src/commands/item/update.ts:1748-1762`
     in the M42 stub.
  3. **Integration test shape: assert
     `internal_error.details.reason === '<milestone>_preflight_
     stub'` after exercising the FULL shipped argv + parse +
     pre-check + (where applicable) wire-fetcher surface.**
     The test serves as a "reaches-the-stub" probe: if the
     argv shape is wrong, the stub never fires (the parse
     surface rejects first); if the shipped pre-check fires
     incorrectly, the stub never fires (the pre-check rejects
     first); only when the FULL shipped surface exercises
     cleanly does the test see the stub-throw. This shape is
     stricter than the equivalent v0.6-M38 D5 rejection
     assertion (which fired before the items_page walker —
     no wire shape assertion) and forces the test to wire the
     full cassette set (BoardMetadata + ItemsPage + ...).
     Tested at
     `tests/integration/commands/item-update-bulk.test.ts:3480-3563`
     in the M42 stub.

  **1 supporting instance** — v0.7-M42 pre-flight at `160330b`.
  **Priority: LOW** — process discipline; sub-discipline of an
  already-graduated rule (R-NEW-76). Fires at 2nd consumer
  when the next pre-flight stub with a c8-ignored runtime body
  lands (likely v0.7-M43 create-time file `--set` two-leg
  dispatch, immediately after M42 IMPL closes). At 3rd consumer
  (likely v0.7-M40 or v0.7-M41 if either ships pre-flight stubs
  with c8-ignored bodies), graduate to a sub-bullet under
  R-NEW-76's `.claude/rules/workflow.md` entry rather than a
  standalone entry — the three internal conventions are tightly
  coupled to R-NEW-76's boundary discipline and read better
  together than apart. Cross-references R-NEW-76
  (parseArgv-BEFORE-c8 — this entry covers the INTERNALS the
  R-NEW-76 boundary wraps) + R-v0.6-NEW-2 (`details.reason`
  discriminator pattern — extension instance for the stub-throw
  variant). **Question for M42 IMPL session:** the
  `'m42_preflight_stub'` discriminator IS a 5th supporting
  instance of R-v0.6-NEW-2's `details.reason` pattern OR
  should stub-flavoured discriminators count separately
  (because they're explicitly transient — they disappear at
  IMPL when the stub body lifts)? Resolution at M42 IMPL
  close-docs sweep.

  **RESOLUTION (v0.7-M42 IMPL close-docs `8878745`):** stub-
  flavoured discriminators count **SEPARATELY** from production
  `details.reason` literals — they're explicitly transient
  (the M42 IMPL body lifted the `'m42_preflight_stub'` throw
  out of the runtime path; the literal stays RESERVED in
  module docstrings + regression-guard test assertions per the
  R-v0.7-NEW-4 checklist (b)/(c) allowed contexts, but it no
  longer fires from any production code path). The
  R-v0.6-NEW-2 5th-consumer graduation expectation therefore
  stays unchanged — M42 IMPL added zero net production
  `details.reason` literals. M43 atomicity-envelope shape is
  the next plausible site if `create_then_file_upload_partial_
  failure` lands. Cross-references R-v0.6-NEW-2 (above) for
  the parent pattern + R-v0.7-NEW-4 (below) for the checklist
  that codified the (a)/(b)/(c) allowed-context taxonomy for
  reserved-literal carry-overs.

- **R-v0.7-NEW-4 — Pre-IMPL contract-term checklist for cross-
  doc grep.** Sub-discipline of R-NEW-72 (post-fix-up cross-doc
  grep). R-NEW-72 covers the *timing* (re-grep after every
  contract-flipping Codex round); R-v0.7-NEW-4 covers the
  *enumeration* — the IMPL author commits upfront to the full
  set of state-current + pre-IMPL-framing contract-term
  phrases the milestone's shipped surface uses, and the
  post-fix-up grep runs against THAT enumerated checklist
  rather than re-deriving from round-N findings. Without the
  pre-IMPL enumeration, Codex fix-up rounds escalate as each
  round's broader sweep catches a new layer of adjacent prose
  the prior round's narrower regex missed. Graduated to a
  `.claude/rules/workflow.md` permanent rule at v0.7-M42 IMPL
  R7 (`8f2832b`) — refined at R8 (`08ae263`) with the
  (a)/(b)/(c) allowed-contexts list for reserved-literal
  carry-overs.

  **Why it graduated immediately at 1st consumer (rather than
  waiting at R-NEW-58's 2-consumer threshold):** the 7-round
  W9 prose-drift escalation across v0.7-M42 IMPL R2-R8 made
  the pattern unambiguously load-bearing. R2/R3 caught 3 sites
  each. R4 jumped to 8+3-proactive. R5 returned 5+1-test.
  R6 jumped to 9. R7 returned 4+workflow-graduation. R8
  returned 7+workflow-refinement. The asymptotic convergence
  shape (each round's regex sweep catching a wider layer
  than the prior's) is structurally identical to the
  "post-fix-up grep must re-run after every contract-flipping
  fix" pattern R-NEW-72 graduated for — but R-NEW-72 didn't
  pin the *enumeration*, only the *timing*. The pre-IMPL
  checklist extends R-NEW-72 with the missing enumeration
  step.

  **The v0.7-M42 checklist** (codified at workflow.md):
  state-current phrases that should appear post-IMPL
  (`v0.7-M42`, `bulk friendly`, `per-item fan-out`,
  `--continue-on-error`, `--concurrency`,
  `runItemUpdateBulkFileDispatch`, `BulkFileSetData`,
  `item_update_bulk_file_set`); pre-IMPL phrases that should
  NOT appear in current prose post-IMPL with three allowed
  exceptions — (a) historical post-mortem entries in earlier
  `v0.{N-1}-plan.md` section bodies; (b) reserved-literal
  docstrings naming literals that stay RESERVED across the
  codebase; (c) regression-guard test assertions
  (`expect(...).not.toContain(...)` on the literal). All
  OTHER occurrences are contract-term drift bugs.

  **2 supporting instances** at graduation — v0.7-M42 IMPL R7
  filing (`8f2832b`; introduced the section + initial term
  list) + R8 refinement (`08ae263`; added (a)/(b)/(c) allowed-
  contexts list + extended the should-not-appear term list
  with the semantic variants Codex surfaced across R6-R8 rounds:
  `single-item shape only`, `M38 + M31 only`, `M38 ships bulk`,
  `form ships at M38`, `bulk file dispatch rejects per D5`).
  **Priority: HIGH** — graduates the load-bearing remediation
  for the W9-prose-escalation pattern; v0.7-M43's pre-flight
  inherits the checklist + extends it for M43-specific
  contract terms.

  **Next-milestone inheritance path:** at v0.7-M43 pre-flight
  (next session), enumerate the M43 contract-term checklist
  upfront (M43-specific state-current + pre-IMPL-framing
  terms); commit it inline at the pre-flight commit; use as
  the post-Codex-fix-up grep checklist throughout M43 IMPL.
  Goal: collapse M43 IMPL's W9 cycle from M42's 8 rounds to
  ≤3 rounds. If M43 IMPL achieves ≤3 W9 rounds, the
  graduation earns its keep; if M43 IMPL still escalates to
  5+ rounds, file a learnings sub-entry capturing what the
  checklist missed.

  **First-inheritance outcome at v0.7-M43 pre-flight
  (`28f117c..ab1d6dd`):** the checklist body landed inline at
  the pre-flight commit's v0.7-plan §3 M43 entry (state-
  current terms: `v0.7-M43`, `file_create`,
  `runItemCreateFileDispatch`,
  `create_then_file_upload_partial_failure`,
  `details.created_item_id`, `create+file two-leg`,
  `orphan-warn`, `m43_preflight_stub`; pre-IMPL terms:
  `file_set_on_create_unsupported`, `deferred_to: 'v0.7-M43'`,
  `D6 closure` framed as forward-looking, etc.). The Codex
  pre-flight cycle ran for **5 rounds** (1 above the M42
  workflow.md estimate of "1-2 rounds for pre-flight"; below
  the M42 IMPL 8-round W9 ceiling). Trend: R1 5 findings, R2
  2, R3 1, R4 2, R5 1 (CONVERGED). Zero P1 across all 5
  rounds. **Sub-finding for the checklist (file as
  R-v0.7-NEW-4 extension lesson):** the M43 inherited
  checklist enumerated the load-bearing TERMS correctly, but
  underestimated the **file-set** the post-fix-up grep needed
  to cover. R1+R2+R3 fix-up rounds each surfaced new prose
  drift in runtime hint surfaces (`src/api/raw-write.ts`,
  `src/api/column-types.ts`, `src/commands/board/column-
  create.ts`) + architecture / README docs that the
  M42-original checklist didn't anchor on. M43 close-docs
  should extend the workflow.md rule's "How to apply" section
  with a **file-set enumeration step** ("at the pre-flight
  commit, enumerate ALL files that name the milestone's
  contract terms — runtime error hints, suggested-write-path
  surfaces, module docstrings, top-of-file docstrings,
  README quickstart prose, architecture.md prose, output-
  shapes.md prose — and grep against the full set per round
  rather than re-deriving from round-N findings"). This is a
  meta-pattern: the checklist's PRESENCE short-circuited some
  W9 escalation (R1-R5 vs M42's R2-R8), but the
  checklist's INCOMPLETENESS on file-set coverage left
  residual drift. The graduation still earns its keep (5
  rounds vs M42's 8); the extension request is for future
  consumers (M40/M41/M44+).

  **Second-inheritance outcome at v0.7-M43 IMPL
  (`5cf4365..c217011`):** the inherited checklist short-
  circuited the W9 cycle down to **4 rounds** (target was
  ≤3; M42's IMPL was 8 rounds). Round-by-round W9-site
  count: R1 4 sites (3 status-prose + 1 narrow guard); R2
  + trailing 5 sites (3 wider doc-drift + 1 missing
  literal + 1 trailing test-surface); R3 8 sites (4 round-
  counter + 4 c8-ignored present-tense); R4 0 sites
  (CONVERGED). Total 17 W9 sites across 4 rounds — versus
  M42 IMPL's 5 W9-only rounds + 3 mixed (8 total).
  Convergence at R4 vs M42's R8 = 4 rounds saved, ~half
  the W9 surface caught proactively by the inherited
  checklist. **Graduation criterion met** — the v0.7-M42
  IMPL R7 graduation discipline holds for v0.7-M43 IMPL
  (closer to but not yet at the workflow.md "≤3 W9 rounds"
  target).

  **Sub-finding for the checklist (M43 IMPL extension
  lesson) — round-counter staleness as a category.** The
  M43 IMPL R3 fix-up surfaced a new W9 sub-category the
  M42 + M43-pre-flight checklists hadn't anchored on:
  **round-counter staleness** ("Codex R1 in progress" /
  "IMPL pending" framing). After R1 fix-up landed, the
  R1-specific framing went stale; same for R2. The R3 fix
  flipped to round-agnostic framing ("Codex IMPL review
  in progress (round-by-round summary lands at close-docs)")
  to avoid the per-round-update treadmill. **Workflow.md
  extension request:** add a "round-agnostic framing for
  in-progress review prose" sub-rule to R-v0.7-NEW-4's
  inheritance checklist — when documenting in-progress
  review state in plan docs, use round-agnostic wording so
  the prose doesn't go stale after each round's fix-up.
  Detail belongs at close-docs (this commit landed it).

  Cross-references R-NEW-72 (parent — pre-IMPL checklist
  extends the post-fix-up grep with the enumeration step) +
  R-v0.6-NEW-2 (related — the (a)/(b)/(c) allowed-context
  taxonomy formalised the reserved-literal carry-over
  pattern that R-v0.6-NEW-2's discriminator literals also
  follow).

- **R-v0.7-NEW-5 — Fail-fast error-decoration block lift
  candidate.** Sub-discipline of R-NEW-58 (lift-ahead-of-feat).
  The fail-fast error-decoration block —
  `if (err.code === 'usage_error') { throw new UsageError(...) }
  else { throw new ApiError(...) }` with stale-cache remap +
  partial-success invalidate inside the catch — is byte-
  equivalent across two production sites:

  1. **JSON-bulk action body** at
     `src/commands/item/update.ts:1334-1361` (originated
     v0.3-M25 partial-success + v0.4-M30 `--concurrency`;
     unchanged from those milestones).
  2. **File-bulk per-item dispatch** at
     `runItemUpdateBulkFileDispatch` (`src/commands/item/update.ts`
     ~lines 1916-2010; shipped v0.7-M42 IMPL `22df2fa` + R1
     `968b154`).

  Both sites also have identical uncovered conditional-spread
  arms on the ApiError ctor (foldAndRemap-decorated path)
  that contribute to the 0.64pp branches-margin drop at M42
  IMPL — a lock-step ratchet across both code paths would
  recover ~0.5pp.

  **1-consumer threshold not yet met** for R-NEW-58's
  ahead-of-feat 2-consumer lift trigger (the helper inlines
  rather than extracts at M42 IMPL because the JSON-bulk
  site predates R-NEW-58 graduation and the file-bulk site
  is the first new consumer post-graduation). Documented
  in-code as the "Future lift candidate" docstring section
  at `src/commands/item/update.ts:1621-1627`.

  **Priority: LOW** — code lift, but waiting at R-NEW-58's
  3rd-consumer threshold. Coverage residual is acceptable at
  floor + 0.20pp branches margin per §3 M43 "Coverage residual"
  prose. Cross-references R-NEW-58 (parent rule) + §3 M42 +
  §3 M43 "Coverage residual" (the JSON-bulk + file-bulk
  precedent notes that motivated this candidate's filing) +
  the in-code docstring at `update.ts:1621-1627` (the
  authoritative description of the byte-equivalent surfaces).

  **M43 IMPL outcome (this commit):** INLINE; lift candidate
  does NOT tip to 3rd consumer at v0.7-M43 IMPL. The M43
  helper's leg-2 catch arm at
  `src/commands/item/create.ts:1794-1857` (post-`5cf4365`)
  is structurally distinct from the R-v0.7-NEW-5 fail-fast
  split-and-rebuild pattern:
  - **R-v0.7-NEW-5 pattern** (JSON-bulk + file-bulk):
    catch a `MondayCliError` from per-item dispatch → call
    `foldAndRemap` → preserve the remapped error's typed
    code (`usage_error` vs `column_archived` vs etc.) →
    re-throw with applied_count / applied_to / failed_at_item
    / matched_count decoration via either UsageError or
    ApiError ctor (the typed split is the load-bearing
    invariant; the conditional-spread arms on httpStatus /
    mondayCode / requestId / cause / retryAfterSeconds
    preserve the original error's wire metadata).
  - **M43 leg-2 catch pattern:** catch a `MondayCliError`
    from `executeFileColumnSet` → call `foldAndRemap` to
    surface `column_archived` etc. → wrap as a NEW
    `ApiError('internal_error', ...)` with `details.cause`
    as a JSON projection `{ code, message, details? }` of
    the remapped error; the outer code is ALWAYS
    `internal_error` (D1 closure pin). No typed split, no
    conditional-spread arms, no decoration of the original
    error in-place — the original error embeds as
    `Error.cause` for stack debugging plus a separate
    JSON projection at `details.cause` for agent
    inspection.

  The two patterns share a single shape feature
  (`foldAndRemap` applied to a leg-N catch arm) but
  diverge on every load-bearing detail: outer code stability
  (M43 always `internal_error` vs R-v0.7-NEW-5 preserves
  remapped code), decoration field set (M43 orphan-warn slots
  vs R-v0.7-NEW-5 applied-count slots), error-type re-
  construction (M43 builds a fresh `ApiError` vs
  R-v0.7-NEW-5 reconstructs the typed class). A lifted
  helper accommodating both would need to parameterise (a)
  outer code policy (preserve-vs-pin), (b) decoration shape,
  (c) `cause` projection vs in-place merge — which collapses
  the helper's value proposition to "shared `foldAndRemap`
  call site," already factored separately.

  **Decision:** stays deferred at 2 consumers. Cite v0.7-
  M43 IMPL outcome as the 1st "considered but not tipped"
  event — useful as a sub-pattern record for future R-NEW-58
  consumer-threshold gating (not every superficially similar
  catch arm tips a lift candidate; structural distinctness
  matters more than line-count overlap). Reference the in-
  code helper docstring at
  `src/commands/item/create.ts:1421-1434` (post-`5cf4365`)
  where the INLINE decision is documented inline with
  rationale for future readers.

  **Future-consumer triggers (carry forward):** the 3rd
  consumer would need to be ANOTHER fail-fast bulk per-item
  dispatch that preserves the remapped typed code + adds
  applied-count-style decoration (e.g., a hypothetical
  `monday item move --where` bulk path; or a v0.8-M44+ bulk
  variant that mirrors the JSON-bulk / file-bulk shape).
  Single-item two-leg orphan-warn patterns (M43-shaped)
  do NOT count as consumers — they're structurally a
  different lift candidate (potentially file-able as a
  separate "always-internal-error wrap with cause JSON
  projection" pattern if a 2nd consumer surfaces).

  **v0.8-M46 IMPL outcome — TRIGGER FIRED (filed at v0.8-M46
  post-IMPL refactor-audit).** `runItemUpdateBulkFileMultiDispatch`'s
  fail-fast path (`src/commands/item/update.ts` ~line 2854,
  shipped at the M46 IMPL `3e6bdc1`) is EXACTLY the predicted
  consumer — a fail-fast bulk per-item dispatch that runs
  `foldAndRemap` → preserves the remapped typed code → re-throws
  via the byte-identical `if (remapped.code === 'usage_error')
  { UsageError } else { ApiError }` split-and-rebuild with the
  5 conditional-spread metadata arms, decorated with
  applied-count slots (M46 adds `applied_file_columns_per_item`
  + `file_count` + `file_column_ids` to the existing
  `applied_count` / `applied_to` / `failed_at_item` /
  `matched_count` set). A re-count during the audit also found
  the original 2-consumer tally **UNDERCOUNTED**: `monday item
  clear --where` bulk (`src/commands/item/clear.ts:839`) carries
  the same split-and-rebuild scaffold (its own comment notes it
  is "kept symmetric with bulk update's per-item failure
  handling so the two surfaces stay diff-able"). So the scaffold
  is now at **4 consumers**: JSON-bulk (`update.ts:1437`) +
  item-clear-bulk (`clear.ts:839`) + M42 file-bulk
  (`update.ts:2159`) + M46 file-bulk-multi (`update.ts:2854`).

  **Lift CRYSTALLIZED; deferred to a dedicated session.** The
  tractable signature is a `reThrowDecorated(remapped:
  MondayCliError, details: Record<string, unknown>): never`
  helper (or `src/api/`-level) — each site builds its own
  `details` decoration object then delegates the ~25-line typed
  split-and-rebuild + conditional-spread scaffold. NOT lifted at
  the M46 IMPL cluster because it spans 4 mutation paths
  (clear / JSON-bulk / M42 / M46) — out of M46's "lift the
  multi-file leg only" scope, and the M42 + JSON-bulk + clear
  sites were explicitly fenced off from the M46 IMPL touch. The
  lift wants its own session: high blast radius (4 error-
  construction paths), a Codex review pass, and a lock-step
  coverage ratchet across the conditional-spread c8-ignore arms
  (the lift would consolidate them, recovering the ~0.5pp
  branches margin noted above). **Priority raised LOW → MEDIUM →
  COMMITTED to the v0.8 refactor cluster** (prioritised 2026-05-20
  on user demand; see `docs/v0.8-plan.md` §3 "v0.8 refactor
  cluster" + PIVOT block). Lands as a dedicated `src/api/`
  error-helper lift (`reThrowDecorated`) with its own Codex pass +
  the 4-path coverage ratchet. Cross-references
  the new [[R-v0.8-NEW-6]] (the sibling orphan-warn /
  always-internal-error-wrap candidate that M46 also crystallised
  — the two are distinct lifts: this one preserves the remapped
  code + applied-count decoration, NEW-6 pins `internal_error` +
  a cause JSON projection).

- **R-v0.7-NEW-6 — Pre-flight stub helper boilerplate
  pattern.** Sub-discipline of R-NEW-76 (parseArgv-BEFORE-c8).
  The pre-flight stub helper bodies for milestones that
  introduce a new feat-helper share a near-identical
  c8-ignored-body shape — `/* c8 ignore start */` block-wrap
  + `await Promise.resolve();` to anchor the async signature
  + a `void inputs.X;` reference-once line per input slot to
  suppress unused-input warnings + a `throw new ApiError(
  'internal_error', '<milestone> ... not yet implemented...',
  { details: { reason: '<milestone>_preflight_stub', milestone,
  ... } })` + `/* c8 ignore stop */`. 2 consumers post v0.7-M43
  pre-flight:

  1. **v0.7-M42** at `src/commands/item/update.ts:1786`
     (`runItemUpdateBulkFileDispatch` stub at `160330b`,
     lifted at `22df2fa`).
  2. **v0.7-M43** at `src/commands/item/create.ts:1410`
     (`runItemCreateFileDispatch` stub at `28f117c`; runtime
     body lifted at IMPL R0 `5cf4365`).

  **Why filed as watch-item rather than lifted now:** the
  pattern's per-consumer cost is ~30 lines of boilerplate.
  A meta-helper would need to accept (a) the milestone
  identifier; (b) the human-readable "not yet implemented"
  message body; (c) the `details` slot's milestone-specific
  fields (column_id / matched_count / etc.); (d) the inputs
  object whose slots get `void`'d. Per-milestone divergence
  on (b) + (c) suggests over-fitting risk if lifted at 2
  consumers — each new milestone would want its own
  diagnostic context. R-NEW-58's 3-consumer rule of thumb
  applies cleanly here.

  **Trigger criteria:** lift at 3rd consumer (likely a
  future milestone's pre-flight stub if it introduces a
  new helper). The lift target would be
  `src/utils/preflight-stub.ts:buildPreflightStub(milestone,
  detailsBody, inputs)` returning a throw-only async
  function. Pre-lift cost-benefit check: the c8-ignored
  body is already mechanical; the lift removes ~20 lines
  per consumer but pulls the diagnostic-context fields
  into a parameterised shape that may be harder to read at
  a glance than the inline form.

  **Priority: LOW** — pure boilerplate; no contract-
  correctness risk; lift is reversible. Cross-references
  R-NEW-76 (parent — the parseArgv-BEFORE-c8 ordering
  discipline these stubs implement) + R-v0.7-NEW-3
  (related — the stub-flavoured discriminator literal
  reservation pattern that pairs with this stub-body
  shape).

- **R-v0.7-NEW-7 — Token-permission ceiling on sandbox
  `create_item` for empirical probes that need write
  activity.** Process discipline / pre-flight probe
  planning candidate. Monday's per-token permission model
  doesn't auto-grant `create_item` on boards created by
  the token's own user-of-origin in a token-created
  workspace — `create_item` mutations in fresh sandbox
  boards return `"User unauthorized to perform action"`.
  This blocks empirical probes that need write activity
  on a known-clean board.

  **2 supporting instances** at filing:

  1. **v0.3-M28 multi-level subitem probe** —
     `scripts/probe/m28-multi-level-subitem.ts`. The phase-2
     empirical depth test created a sandbox workspace +
     board via the token, then `create_item` rejected as
     unauthorized. M28 ultimately worked around by
     accepting an optional `M28_PROBE_BOARD_ID` env var
     pointing at a user-provided pre-authorized board.
  2. **v0.7-M43 D1 rollback-viability probe** —
     `scripts/probe/m43-create-then-file-cleanup.ts`. The
     phase-2 empirical rollback test hit the same
     "User unauthorized to perform action" rejection on a
     freshly-created token-owned sandbox workspace+board;
     phase-2 could not run. Defaulting D1 to (b)
     orphan-warn under uncertainty preserved optionality
     but loses the empirical evidence the probe was meant
     to produce.

  **Why filed as watch-item:** the pattern is now confirmed
  across two milestones spanning v0.3 → v0.7. The next
  pre-flight probe needing write activity (M40 `monday item
  set-description` content-write probe + M41 `monday doc
  block-create-bulk` probe are both write-shaped and may
  hit the same ceiling) is the 3rd-consumer trigger.

  **Trigger criteria:** graduate to a permanent rule in
  `.claude/rules/workflow.md` (likely a sub-section under
  "Pre-flight contract diff discipline" step 1
  "Empirical probe") at the 3rd consumer. The rule prose:
  pre-flight probes that need write activity (`create_item`,
  `create_subitem`, `change_column_value`, `change_multiple_
  column_values`, `add_file_to_column`, doc mutations,
  etc.) MUST be planned upfront to use either (a) a
  user-pre-authorized known-writable board (via env var
  like `M<N>_PROBE_BOARD_ID`) or (b) read-only introspection
  + structural-pattern analysis if no write activity is
  truly required. The "token-creates-its-own-sandbox"
  pattern is unreliable for write activity.

  **Priority: MEDIUM** — process discipline; every M40 /
  M41 / future-milestone probe pre-flight session is
  affected. Without the discipline, probe-blocked
  D-decisions default to conservative-under-uncertainty
  closures (M43 D1 → orphan-warn was the right call but
  cost us empirical evidence that would have let us pick
  (a) rollback if safe). Mitigation cost is small — one
  upfront question to the user at pre-flight kickoff
  ("which board can I write to for this probe?") before
  designing the probe.

  **Cross-references:** parent — §22's earlier "Watch-items
  carried forward from v0.6" doesn't include an
  empirical-probe-planning watch-item explicitly; this
  R-v0.7-NEW-7 is the first formal filing of the discipline.
  Related: v0.3-plan §22 "scripts/probe/ reusable probe
  infrastructure" (the `_lib.ts` lift) addressed the probe
  *infrastructure* but not the *permission* problem this
  candidate covers.

- **R-v0.7-NEW-8 — SIGINT-mid-cadence flake on `monday item watch`
  graceful-drain test.** Test-stability watch-item filed + isolated
  in a single audit pass. **6 supporting instances** at the v0.8-M46
  IMPL close-docs update — first two during the post-
  v0.7-publish refactor-audit, third at the v0.8 kickoff candidate-
  selection pre-commit gate, fourth at the v0.8-M46 pre-flight
  contract diff pre-commit gate, fifth at the v0.8-M46 post-cluster
  refactor-audit pre-commit gate, sixth at the v0.8-M46 IMPL final
  gate (this commit). Instances #4-#6 share a NEW failure surface
  (worker-spawn `EAGAIN` on forks pool
  / `--maxWorkers=1` non-spawn variant), distinct from instances
  #1-3's item-watch SIGINT timing-race surface but sharing the
  "tests pass under isolated re-run" diagnostic pattern. The
  category has now widened to "concurrent-load + signal-timing
  worker-pool flake umbrella":

  1. **`3e46f59` close-docs commit pre-commit gate** —
     1/4124 transient failure. The dot reporter showed only
     "Test Files  1 failed | 171 passed (172)" / "Tests  1
     failed | 4123 passed | 1 skipped (4125)" with no `FAIL`
     marker; failing test ID not identifiable from that run.
     Two subsequent re-runs cleared (4124 passed + 1 skipped).
  2. **Post-publish-audit pre-commit gate** — same failure
     count + ratio. Re-run with `--reporter=verbose` isolated
     the failing test:
     `tests/integration/commands/item-watch.test.ts > monday
     item watch — SIGINT graceful drain > emits trailer +
     exits 130 when the signal aborts mid-cadence` (179ms).
     One more re-run cleared (4124 passed + 1 skipped).
  3. **v0.8 kickoff candidate-selection pre-commit gate** —
     5/4124 transient failures across 2 files: the failing
     item-watch test was `tests/integration/commands/item-
     watch.test.ts > monday item watch — codex impl review
     round-1 regression coverage > P1-1: SIGINT mid-once-poll
     emits a signal trailer (not a rethrow)` (70ms — different
     `it` block than instances #1/#2 but same item-watch SIGINT
     timing-race surface). Re-run of the suspected files in
     isolation passed 31/31 (clean). The other 4 failures in
     the same gate run were e2e timeouts on
     `tests/e2e/m14.test.ts > M14 e2e — workspace remove-users
     (live)` (10226ms — likely concurrent-load timeout) +
     `tests/e2e/config.test.ts > e2e: monday config path > --json
     reports the .env file path candidate` (10075ms — likely
     concurrent-load timeout); both passed under isolated re-run
     and are filed as collateral observations (not own
     watch-items at this filing — first-observed instances; open
     a new watch-item if they recur next session).
  4. **v0.8-M46 pre-flight contract diff pre-commit gate** —
     worker-spawn `EAGAIN` flake on the forks pool (system
     resource exhaustion from concurrent worker forks; not a
     test-logic failure). Manifested as `Test Files 1 failed |
     171 passed (172) / Tests 4124 passed | 1 skipped + 1 error`
     — no specific test name surfaced; the failing file (likely
     `tests/integration/commands/doc-create-in-workspace.test.ts`
     per the unhandled-error trace) was a worker-spawn EAGAIN
     before the test file even loaded. Re-run with
     `--maxWorkers=2` cleared (4129 + 1 skipped — every test
     passed; the 2 transient failures on the first `--maxWorkers=2`
     attempt were also flake, cleared on second re-run). This
     instance widens the category from "item-watch SIGINT timing"
     to "concurrent-load worker-pool flake" — different failure
     mode but same "tests pass under isolated re-run" diagnostic
     pattern. Priority stays MEDIUM; deflake work + worker-pool
     concurrency cap configuration are separate sessions.
  5. **v0.8-M46 post-cluster refactor-audit pre-commit gate** —
     worker-pool flake recurred with broader impact: first
     `--maxWorkers=2` run surfaced `Test Files 1 passed (some
     percent skipped) / Tests 4101 passed | 1 skipped (4141) +
     3 errors`; second `--maxWorkers=1` run still hit
     `Test Files 171 passed (172) / Tests 3929 passed | 1 skipped
     (4146) + 1 error`. Isolated re-run of the touched test file
     (`tests/unit/api/file-column-set.test.ts`) PASSED CLEAN
     (37/37). The refactor-audit changes for this commit are
     docs-only + 1 docstring update — they CANNOT affect test
     outcomes; the failures are environmental. Confirms the
     R-v0.7-NEW-8 flake category as load-bearing — gates flake
     under broader concurrent-load conditions than initially
     documented. Priority remains MEDIUM; instance #5 is
     evidence the deflake work should bundle worker-pool
     concurrency-cap configuration alongside the SIGINT-timing
     fix.
  6. **v0.8-M46 IMPL final gate** — worker-pool flake recurred
     under an unusually heavy concurrent-load condition: a SECOND
     Claude Code agent was running the test suite in the same repo
     simultaneously (an unrelated `is_leaf` bug-fix session), so two
     full-suite runs contended for OS process-fork slots. Two
     manifestations in one gate pass: (a) `npm run test:coverage`
     surfaced 14/4156 failures (item-watch abort/backoff timing +
     `schema.test.ts` ajv-validation 15s timeout) at 354s wall —
     coverage instrumentation amplifies the contention; (b)
     `npm test -- --maxWorkers=2` then hit a hard
     `spawn /…/node EAGAIN` (errno -11 — OS refused to fork the
     worker) with `Test Files 169 passed (171)` + 3 errors; the 2
     non-passing files were worker-fork deaths before the test files
     loaded, not test-logic failures. Isolated re-runs at
     `--maxWorkers=1` PASSED CLEAN: the 3 touched integration suites
     (190), `tests/unit/api/file-column-set.test.ts` (37), and a
     combined run incl. `schema.test.ts` (194 — confirming the M46
     `outputSchema` union widening stays ajv-2020-12 valid). The M46
     IMPL is real `src/` change, but the failing files were either
     untouched (`item-watch`) or independently validated clean in
     isolation (`schema`). This instance adds a concrete amplifier
     to the root-cause picture: **the EAGAIN surface is triggered by
     OS fork-slot exhaustion under concurrent full-suite runs**, not
     just by this suite's own worker count — strengthening the case
     that the deflake bundle should set an explicit `maxWorkers` /
     `poolOptions.forks.maxForks` cap in `vitest.config.ts` so the
     pool degrades gracefully rather than EAGAIN-ing. Priority
     remains MEDIUM; not deflaked this session (out of scope for the
     M46 IMPL cluster).

  **Root-cause hypothesis (NOT verified at audit time):** the
  test exercises v0.4-M29's `monday item watch` long-poll
  surface under a synthetic SIGINT delivered mid-cadence + asserts
  that the NDJSON `{"_meta": {...}}` trailer flushes before exit
  130. The race window is between SIGINT-handler invocation,
  cadence-loop abort propagation through the AbortController,
  trailer-write to stdout, and the final `process.exit(130)`.
  Plausible failure modes (speculative pending isolation): (a)
  signal delivered before the first cadence event so the trailer
  flushes without backlog and the test's `events_emitted > 0`
  assertion fails; (b) trailer-write races with `process.exit` so
  the trailer bytes get truncated; (c) the test's polling cadence
  uses a real-time `setTimeout` whose timing varies under
  concurrent test workers.

  **Priority: MEDIUM** (re-prioritised from LOW per the
  watch-item's trigger criteria — flake recurred within the
  same audit session, exceeding the "if it recurs in the next
  3 sessions" gate). Test-stability + isolation candidate; no
  contract drift, no production surface impact at the runtime
  level (the runtime SIGINT handler IS contractually correct;
  the flake is in the test's timing assumptions about when the
  trailer can be observed).

  **Trigger criteria (post-isolation):** the deflake work is a
  separate session — open a deflake task against
  `tests/integration/commands/item-watch.test.ts:<line range
  for the "emits trailer + exits 130 when the signal aborts
  mid-cadence" `it` block>`. Recipe: run the SUSPECT TEST in
  isolation with `npm test -- tests/integration/commands/item-
  watch.test.ts -t "emits trailer" --no-coverage` repeatedly
  (target: 100+ runs) to confirm reproduction rate; if
  reproducible <5% under isolation but >5% under full-suite
  runs, the race is inter-test (likely a shared-state mutation
  from a sibling test); if reproducible at consistent rates
  under both, the race is intra-test (timing-sensitive
  assertion in the SIGINT handler).

  **Why filed despite 1-occurrence:** the project's testing
  discipline (testing.md "every branch covered" + the 95.45pp
  branches floor) treats test reliability as a contract — a
  flaky test is a coverage hole that doesn't always trigger.
  Filing the watch-item preserves the signal across sessions
  even if no agent witnesses the next recurrence; without
  filing, the next agent seeing a "1 failed" line would
  re-derive the audit trail from scratch.

  **Cross-references:** testing.md (project's testing standard
  — every branch covered, every error case asserted on type +
  message). Related: v0.4-plan §22 may have analogous test-
  stability watch-items if any v0.4 milestone surfaced a flake
  (not audited at this filing — speculative). v0.6 + v0.5
  release-prep audits did not surface analogous flakes per
  their §22 registers (cross-checked at this filing).

### Watch-items ratified at v0.7 post-publish refactor-audit

- **R-v0.5-NEW-24 — Post-publish flip commit covers BOTH
  CLAUDE.md AND `v<N>.<M>-plan.md` placeholder / status-marker
  sites in one pass.** Originally filed at v0.5 post-release-
  prep refactor-audit (`docs/v0.5-plan.md` §22). **Ratified at
  3rd supporting instance + 1st successful application** at
  v0.7 post-publish refactor-audit (this commit). Cadence
  trace:

  1. **v0.5 (1st supporting instance, filing event)** — v0.5
     post-release-prep refactor-audit at `78e8100` flipped
     CLAUDE.md Status but discovered v0.5-plan.md parallel
     `<this-commit>` / "ready for publish" / "pending user
     authorization" sites stayed stale. R-v0.5-NEW-24 filed
     as a LOW-priority watch-item with the cross-doc parallel-
     sweep recipe.
  2. **v0.6 (2nd supporting instance, gap-confirmation event)**
     — v0.6 post-release-prep flip at `2a16d91` touched only
     CLAUDE.md; the v0.6-plan.md `<this-commit>` placeholders
     (e.g., line 591 §3 release-prep heading) stayed un-
     backfilled. This confirms the watch-item's premise:
     without explicit parallel-sweep discipline, the post-
     publish flip routinely misses the plan-doc parallels.
  3. **v0.7 (3rd supporting instance, successful-application
     event)** — v0.7 post-publish refactor-audit applies the
     recipe end-to-end: backfilled `<this-commit>` → `3e46f59`
     across §1 SHIPPED row + §3 release-prep heading + §3
     close-docs commit bullet; flipped "ready for publish" →
     "published" with the npm timestamp (2026-05-20T15:48:07Z)
     + annotated tag at `3e46f59` (tag object `550e2dc`) +
     GitHub release URL across CLAUDE.md Published block + §1
     row + §7 exit checklist; filled v0.7-plan §7 release exit
     checklist with the v0.6-plan §7 template + v0.7-specific
     post-publish lines.

  **Graduation status:** **GRADUATED** at 3rd supporting
  instance + clean application. Successor sessions inherit
  the recipe as a standard cadence rule for v0.8+ post-
  publish flips. Future plan docs (v0.8-plan.md and beyond)
  should anticipate the post-publish parallel-sweep slot at
  release-prep close-docs writing time (use explicit
  `<post-publish-audit-commit>` placeholder for the future-
  commit reference rather than the recursive `<this-commit>`
  placeholder that v0.5 / v0.6 / v0.7 used).

  **Workflow.md extension request:** add a "Post-publish
  parallel-sweep at release-prep flip" sub-section under
  the existing "Release-prep cross-doc grep for stale
  `deferred_to` slots" rule (R-NEW-82's home), with the
  recipe: (a) CLAUDE.md "Published" block flip with npm
  timestamp + tag URL + GitHub release URL; (b) CLAUDE.md
  "Next session" pointer EXTERNALLY-BLOCKED gate removal;
  (c) plan-doc §1 SHIPPED row `<this-commit>` backfill +
  same publish-trio additions; (d) plan-doc §3 release-prep
  heading + close-docs commit bullet `<this-commit>` backfill;
  (e) plan-doc §7 release exit checklist fill (mirrors prior-
  release §7 pattern); (f) plan-doc §22 R-v0.5-NEW-24
  ratification update. Detail belongs at the next release-
  prep close-docs (v0.8 release-prep close-docs should
  include this discipline in the close-docs commit's task
  list).

  **Cross-references:** parent — `docs/v0.5-plan.md` §22
  R-v0.5-NEW-24 entry (filing event); v0.6-plan.md §3 line
  591 + v0.6 post-publish commit `2a16d91` (2nd-supporting-
  instance gap evidence); R-NEW-82 (sibling release-prep
  discipline — cross-doc grep for `deferred_to` slips; the
  post-publish parallel-sweep is the "after the release-prep
  closes" counterpart to R-NEW-82's "before the release-prep
  closes" sweep); R-NEW-84 (sibling — Codex review skip on
  mechanical clusters; post-publish parallel-sweep clusters
  are mechanical and skip Codex by R-NEW-84).
