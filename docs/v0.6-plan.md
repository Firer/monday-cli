# v0.6 implementation plan

> **Status:** plan, not contract. The binding contract is
> [`cli-design.md`](./cli-design.md). This document sequences the
> work needed to ship v0.6 of that contract; it doesn't redefine it.
> If a conflict arises, `cli-design.md` wins and this plan is updated.

## 1. Scope summary

v0.6 is "**fill the column-write story**" — agents reach the last
column-type-shape Monday exposes (file-shaped columns) through the
same friendly `--set <col>=<val>` surface they already use for every
other writable column-type. v0.5 closed the team-writers + workdocs
CRUD mutation backlogs. v0.6 picks up the longest-running carry-over
in the §13 backlog: the files-shaped friendly `--set` translator that
slipped from v0.4 → v0.5 → v0.6 across two consecutive release-preps
because neither cycle picked up the translator-to-multipart-wire
dispatch.

**In scope (from `cli-design.md` §13 v0.6 + Appendix B verb-noun
matrix):**

| Group | Commands |
|-------|----------|
| **files-shaped `--set`** | `monday item set <iid> <file-col>=<path>` + `monday item update <iid> --set <file-col>=<path>` — friendly translator extension dispatching from the standard `--set` boundary into v0.4-M31's multipart wire (`add_file_to_column`) rather than the JSON `change_column_value` path the other writable types use. No new wire fetcher (reuses M31's `addFileToColumn` verbatim); no new transport seam (multipart shipped at v0.4-M31); no new ERROR_CODE (29 stays). |
| **polish** | Refreshed envelope-snapshot suite for v0.6 surfaces; `output-shapes.md` ToC audit; README quickstart updates; CHANGELOG `0.6.0` entry; version bump. |

**Universal partial-success rule** (cli-design §6.1 compliant — same
shape as v0.2 / v0.3 / v0.4 / v0.5). M38 ships single-item only —
bulk `item update --where ... --set <file-col>=<path>` REJECTS at
the column-resolution boundary per D5 closure (parse-time can't
know — column types only resolve after metadata loads; defers to
v0.6.x for the per-item file-set partial-success envelope). v0.6
introduces no contract revision to §6.1.

**Out of scope (deferred per `cli-design.md` §13 + carried over from
v0.5 deferrals):**

- Bulk `item update --where ... --set <file-col>=<path>` — defers to
  v0.6.x candidate-selection. Per-item file dispatch + partial-success
  envelope + `--concurrency` interaction each carry design dimensions
  (per-item retry timing for multipart; per-item asset slot in
  envelope; concurrent multipart with shared transport) that warrant
  their own milestone cluster.
- `item create --board <bid> --name <n> --set <file-col>=<path>` —
  REJECTS at column-resolution boundary per D6 closure. Create-time
  file upload would need: (a) `create_item` with placeholder
  `column_values`, (b) post-create `add_file_to_column` — non-atomic,
  opens state-safety holes per cli-design §5.8 "atomicity, state
  safety, and idempotency". Defers to v0.6.x once a clean
  atomicity-envelope shape is pinned.
- `<path>='-'` stdin support — mirrors M31 `monday item upload`'s
  rejection rationale (no clean `--filename` companion shape pinned
  for `--set` syntax). Defers to v0.6.x once an extension shape is
  designed.
- `--set-raw <file-col>=<json>` — STAYS REJECTED per D3 closure.
  Files have no JSON wire shape `change_column_value` accepts; the
  escape-hatch contract is "user supplies the JSON Monday's
  `change_column_value` accepts" which doesn't compose with the
  multipart wire. v0.6.x may re-evaluate if Monday exposes an
  asset-id-reference wire shape for `change_column_value`.
- Cross-board `item move` value-overrides — carried over from v0.4
  + v0.5 release-prep slips. Monday's `ColumnMappingInput` carries
  no value slot at API `2026-01`; the richer `{id, value?}` form
  requires a non-atomic post-move `change_multiple_column_values`
  with cross-leg partial-failure envelope shapes. Out of v0.6
  scope; re-evaluate at v0.6.x or v0.7.
- Cross-board search resumable cursor — carried over from v0.4 +
  v0.5 release-prep; per-board cursor-lifetime under aggregation
  remains the load-bearing design issue. Not a v0.6 candidate by
  default.
- Multi-level subitem creation — carried over from v0.4 + v0.5
  release-prep; conditional on Monday's data model surfacing
  `subtasks` on `sub_items_board` at API `2026-01`+. Re-probe
  empirically before any v0.6 reopening; otherwise slips to v0.7.
- Profile-scoped argument defaults — filed at v0.6 kickoff
  candidate-selection session as a new feature candidate. Picks up
  at v0.6.x candidate-selection if user demand surfaces; carries
  its §13 carve-out Decision prerequisite forward (the "Saved
  queries / aliases" non-goal needs amending to distinguish
  aliases-as-stored-command-strings from defaults-as-stored-flag-
  values).
- OAuth app registration (`monday auth login`) — v0.5.0 still ships
  the M28 placeholder guard. Reopens only on clear user demand for
  browser-based login over `MONDAY_API_TOKEN`.

**Permanent non-goals (per cli-design §13 — reminded here so plan
readers don't propose them as v0.6 candidates):**

- Hosting webhooks (CLIs can't expose public HTTP — out of model).
- App framework / installable monday apps (different surface area).
- Real-time GraphQL subscriptions (Monday's endpoint doesn't
  support them).
- Telemetry, update-notifier, analytics — ever.
- Forms (Monday's public-submission feature) — same hosted-endpoint
  shape as webhooks.
- Saved queries / aliases — would silently change behavior across
  machines; shell aliases / shell functions are the established
  UNIX answer. **Note:** profile-scoped argument defaults (carried
  forward to v0.6.x — see "Out of scope" above) needs a §13
  carve-out Decision distinguishing aliases-as-stored-command-
  strings (still non-goal) from defaults-as-stored-flag-values
  (carve-out). That Decision lands at the milestone that picks the
  candidate.
- `monday undo` (replay-based reversal of recent mutations) —
  Monday's state model is authoritative and concurrent; honest
  substitutes are `--dry-run` (v0.1) and `item history` (v0.3-M24).

## 2. Pre-flight: what's already in place

Working at HEAD (`main`, post-v0.5.0 release). v0.5.0 published to
npm 2026-05-17T20:55:05Z. v0.5 closed M34–M37 + release-prep,
ending with **4054 + 1 skipped tests across 170 files**; coverage
**99.29 / 96.45 / 99.45 / 99.55** at floor **95 / 95.45 / 95 / 95**
(branches margin **1.00pp**); ERROR_CODES count **29**; command
count **117**. `package.json` version: **0.5.0** (stays through
v0.6 milestones; bumps to `0.6.0` at v0.6 release-prep).

**Foundations carrying forward into v0.6:**

- **Multipart transport seam (v0.4-M31).** `src/api/multipart-
  transport.ts` ships the `MultipartTransport` interface +
  `createMultipartTransport(...)` builder. `ResolvedClient.multipart`
  carries the resolved-or-stub transport through the runner's
  context (paired with the JSON `transport` slot). M38 reuses both
  surfaces verbatim — `dispatchMultipart` consumer count goes 1
  → 2; `ResolvedClient.multipart` consumer count goes 1 → 2 (test
  seam pattern). **R-NEW-NEW M31-IMPL-surfaced** lift candidates
  (shared retry-thunk + `dispatchMultipartWithSizeRewrap`) stay
  UNFILED — argument divergence per-fetcher rationale carries
  forward to M38.

- **Friendly `--set` translator (v0.1-M5a + v0.2-M8 + v0.3-M19).**
  `src/api/column-values.ts:translateColumnValueAsync` produces a
  JSON `ColumnValuePayload` for the 13 writable allowlisted column
  types; `selectMutation` picks
  `change_simple_column_value` / `change_column_value` /
  `change_multiple_column_values`. The translator currently
  REJECTS file-shaped types with `unsupported_column_type` via the
  `UNSUPPORTED_TABLE.files_shaped` row (`column-values.ts:1723`).
  M38 extends the dispatch with a SIBLING branch at the command
  action body that detects file-column entries AFTER column
  resolution + routes them to the multipart wire (NOT inside the
  translator itself — the translator stays JSON-shaped output for
  the 13 existing types).

- **M31 file pre-check pattern.** `src/commands/item/upload.ts`
  ships the `fs.stat() + fs.access(R_OK) + non-empty` pre-check
  pattern (round-1 P2-2 fix at M31 IMPL added the R_OK leg); the
  `sniffContentType` helper at `src/utils/mime.ts` (R-NEW-NEW
  M31-IMPL-surfaced lift at 2-consumer threshold) populates the
  Blob's `type` slot. **R-NEW-58 IMPL-kickoff lift target**: extract
  the file-pre-check + Blob-construction pattern into a shared
  helper at `src/utils/file-source.ts` (or fold into M31's
  `assets.ts` as a sibling export) for the M38 IMPL dispatch leg
  consumer (3-consumer trigger fires at IMPL: M31's `item upload`
  + `update upload` + M38's `--set` dispatch). The lift stays
  ahead-of-feat at M38 IMPL kickoff per R-NEW-29's M25 cadence.

- **Column-resolution machinery (M5a/M5b/M8).**
  `src/api/columns.ts:resolveColumnWithRefresh` resolves
  `<col>=<val>` tokens (ID / title / case-fold / cache-miss-
  refresh-once); `src/api/board-metadata.ts` carries the metadata
  cache + invalidation primitives. M38 reuses both verbatim — the
  file-column dispatch leg fires AFTER column resolution and reads
  the resolved `column.type === 'file'` discriminator to route.

- **Pre-flight stub discipline (R-NEW-76 graduated at v0.5-M34).**
  Stub action bodies invoke `parseArgv` + sibling parse-boundary
  helpers BEFORE the `c8 ignore start` block-wrap so invalid argv
  surfaces `usage_error` (exit 1) from the parse boundary, NOT
  `internal_error` (exit 2) from the c8-ignored stub throw. M38
  pre-flight applies the discipline to the new file-column
  dispatch branch in 1 action body (`item set`) — consumer 15+
  post-graduation. The parallel branches in `item update` (single
  + bulk) + `item create` land at M38 IMPL; at pre-flight those
  paths still surface the existing translator's
  `unsupported_column_type` rejection for files-shaped columns.

- **Post-fix-up cross-doc grep (R-NEW-72 graduated at v0.4-M33 +
  re-ratified through v0.5-M37; 8th time validated).** When a
  Codex fix-up flips a contract surface (runtime predicate
  change, error-code semantic change, schema invariant change,
  contract-prose-bearing module rename), re-run the R-NEW-56
  cross-doc grep AFTER the fix-up commit lands. Search paths per
  R-v0.5-NEW-19 graduation: `src/api/*.ts` + `src/commands/**/*.ts`
  + `src/commands/index.ts` (module-import block prose) + `docs/
  *.md`. Noun-stem regex matching per the v0.5-M37 lesson —
  use `\b<noun>\b` patterns, not literal substrings, to catch all
  inflections in a single pass.

- **Candidate-selection 5-dimension scoring framework (R-NEW-75
  graduated at v0.4-M33).** Each candidate scored on wire-shape
  novelty / transport seam / destructive gate / R-class triggers
  / Codex round estimate. Process-only candidates legitimately
  return zero across 4-5 axes; that IS the signal. v0.6 kickoff
  session ratified the framework for the 4th consecutive cycle
  (v0.5-M37 IMPL close session was the 3rd).

## 3. Milestone sequencing

### M38 — files-shaped friendly `--set` writes

**Status:** IMPL shipped end-to-end. Pre-flight contract diff
landed at `0cb8b69..1a92955` (4 fix-up rounds + 1 ratification);
the R-v0.6-NEW-1 lift landed at `3c2a9b0` (ahead-of-feat per
R-NEW-29's M25 cadence); IMPL runtime bodies + action-body
branches shipped at `e749931` (the feat commit); Codex IMPL
review covered the combined `d2e4ee2..e749931` diff. Round-by-
round IMPL detail appended after the pre-flight summary below.

**Wire surface.** Zero net change. M38 reuses Monday's existing
`add_file_to_column(item_id: ID!, column_id: String!, file: File!) →
Asset` mutation (v0.4-M31 probe-pinned). The novelty is the
**dispatch routing decision** at the CLI's translator boundary —
when `--set <col>=<path>` resolves to a `file`-typed column, the
action body branches OFF the standard JSON-translator path INTO
M31's multipart `addFileToColumn` fetcher.

**Two new CLI surfaces** (no new verbs, no new flags — extends
existing `--set` accepted-value grammar):

  - `monday item set <iid> <file-col>=<path> [--board <bid>] [--dry-run]`
    — single-column file write. Positional `<col>=<value>` grammar
    extended: when the resolved column has `type: 'file'`, the
    `<value>` is treated as a local file path (NOT a JSON-wire
    value). Path resolution + pre-check pattern mirrors M31's
    `item upload` (round-1 P2-2 R_OK leg included).
  - `monday item update <iid> --set <file-col>=<path> [--board <bid>] [--dry-run]`
    — single-`--set` file write. M38 requires EXACTLY ONE `--set`
    flag when a file-column is the target; mixing with value-`--set`
    / `--set-raw` / `--name` rejects at the column-resolution
    boundary with `usage_error` carrying
    `'mixed_file_and_value_sets'` at `details.reason`. Multi-file
    `--set` rejects with `usage_error` carrying
    `'multi_file_set_unsupported'` at `details.reason`.

**Eight Decision closures pinned at pre-flight** (`§8 Decisions`
register below).

**Empirical probe — not required.** The wire shape is M31's
`add_file_to_column` mutation verbatim. The translator-boundary
dispatch design is contract diff work (pre-flight scope), not
probe work. The §5.3 translator architecture from M8/M12 + the
M31 multipart wire contract are both already pinned.

**No new ERROR_CODE** (D8 closure; registry stays at 29). All
M38-specific failures route through existing codes with `details.
reason` discrimination:

  - `usage_error.details.reason: 'mixed_file_and_value_sets'` —
    `--set <file-col>=<path>` mixed with any other `--set` /
    `--set-raw` / `--name` flag in same call. Rejects at column-
    resolution boundary (parse-time can't know — column type not
    resolved yet).
  - `usage_error.details.reason: 'multi_file_set_unsupported'` —
    2+ `--set <file-col>=<path>` entries in same call. M38 defers
    multi-file dispatch to v0.6.x.
  - `usage_error.details.reason: 'file_set_on_create_unsupported'`
    — `--set <file-col>=<path>` on `monday item create`. Defers
    create-time file upload to v0.6.x per D6 (non-atomic post-
    create wire shape would break §5.8 state safety).
  - `usage_error.details.reason: 'file_set_on_bulk_unsupported'` —
    `monday item update --board <bid> --where ... --set
    <file-col>=<path>`. Defers bulk file-set to v0.6.x per D5.
  - `usage_error.details.reason: 'file_not_readable'` /
    `'file_empty'` / `'file_too_large'` — inherited from M31's
    `item upload` action body pattern verbatim.
  - `unsupported_column_type` — `--set-raw <file-col>=<json>`
    stays rejected per D3 (existing prose flips to "M38 ships
    `--set <file-col>=<path>` but `--set-raw` for files stays
    rejected because Monday's wire has no JSON-shape for
    `change_column_value` on files").

**Pre-flight Codex review estimate**: 1-3 fix-up rounds + 1
ratification per the M22 / M27 / M30 / M31 / M32 / M34 / M35 /
M36 / M37 write-surface pre-flight precedent (cumulative 0-3 P2
+ 3-10 P3 typical).

**Actual pre-flight Codex review outcome (closed
`0cb8b69..1a92955`)**: 4 fix-up rounds + 1 ratification round
(round-5 with the close-docs R-NEW-72 metadata flip applied
inline). **Cumulative findings: 0 P1 + 6 P2 + 7 P3** — at the
upper bound of the precedent; the P2 count slightly exceeds
the 3-typical-upper-bound (6 vs 3) due to the substantive round
1 dry-run rewrap fix introducing more contract surface than
typical pre-flight stub passes. Per-round detail:

- **Round 1** `b9c5ff4` (0 P1 + 3 P2 + 2 P3):
  - **P2-1 (W3/W14)** — `item set --dry-run <file-col>=<path>`
    was reaching `planChanges` → translator → `UNSUPPORTED_
    TABLE.files_shaped` and surfacing `unsupported_column_type`
    instead of the M38 pre-flight stub; live path had the M38
    dispatch check at `src/commands/item/set.ts:319`, dry-run
    path didn't. First-pass fix moved column resolution upfront
    (BEFORE dry-run / live split), but that double-resolved
    metadata and flipped the dry-run envelope `source` from
    "live" to "mixed" via `planChanges`' second resolution
    hitting cache; caught by the `envelope-snapshots.test.ts`
    snapshot regression. Final fix: catch-and-rewrap inside
    the dry-run branch (catch `planChanges`'s `unsupported_
    column_type` with `details.type === 'file'` on friendly
    `--set` paths and rewrap as `internal_error.details.
    reason: 'pre_flight_stub'`); preserves source-aggregation
    semantics for non-file dry-run paths. Added integration
    test at `tests/integration/commands/item-set.test.ts`
    pinning the dry-run stub fires for
    `attachments=./report.pdf --dry-run`. Collapses at M38
    IMPL when the D4 dry-run envelope shape lands.
  - **P2-2 (W5/W13)** — three stale D3 prose sites flipped:
    `docs/output-shapes.md:2057-2062` (`--set-raw` files-shaped
    rejection from `deferred_to: "v0.6"` + slip narrative →
    D3 permanent-rejection wording naming BOTH M38 friendly +
    M31 verb-shaped); `docs/output-shapes.md:933-939`
    (`noncanonical_column_type` warning category enum prose
    widened); `docs/architecture.md:241`
    (`translateRawColumnValue` module description flipped to
    D3 wording).
  - **P2-3 (W9/W16)** — three v0.6-plan.md drifts: line 31
    "parse boundary" → "column-resolution boundary"; line 365
    D1 closure named nonexistent `parseFileColumnSet` → flipped
    to `enforceSingleFileColumnSet`; lines 167 + 522 overstated
    pre-flight scope as "3 action bodies" → flipped to "1
    action body (item set); other 2 land at M38 IMPL".
  - **P3-1 (W7/W16)** — asymmetry numbering: architecture.md
    "three" → "four"; expansion rule from "the 4th" → "the
    5th"; file-column-set.ts + v0.6-plan.md "5th supporting
    site candidate" → "4th supporting site filed".
  - **P3-2 (W10)** — consumer-count prose at
    `src/api/file-column-set.ts:59` reframed as "Projected
    consumer counts at M38 IMPL" with "Today (pre-flight)
    still N" annotations.
- **Round 2** `5808fd4` (0 P1 + 1 P2 + 4 P3):
  - **P2-1 (W11/W13)** — `noncanonical_column_type` warning
    at `src/commands/board/column-create.ts:467-478` still
    named only M31; flipped to BOTH paths + D3 permanent
    rejection note. Module docstring summary + cli-design.md
    §4.3 column-create row inline comment flipped in
    lockstep. Extended
    `tests/integration/commands/board.test.ts:3186` to assert
    on the MESSAGE prose (6 new tokens: `v0.6-M38`, `v0.4-M31`,
    `monday item set`, `monday item upload`, `--set-raw`,
    `REJECTED`).
  - **P3-1 (W7)** — three more asymmetry-numbering drift sites
    in v0.6-plan.md (lines 338 / 479 / 664) flipped to "4th
    supporting site" / "2 → 3" / '"the 4th" to "the 5th"'.
  - **P3-2 (W3'/W14')** — dry-run test prose contradicted
    round-1 catch-and-rewrap; rewrote title + comment to
    describe the mechanic + source-aggregation rationale.
  - **P3-3 (W16)** — rolled into P3-1 (`sniffContentType`
    2 → 4 → 2 → 3).
  - **P3-4 (out-of-band)** — CLAUDE.md live numbers refreshed
    to post-round-1 values.
- **Round 3** `30204a3` (0 P1 + 0 P2 + 1 P3): provenance
  metadata only (R-NEW-72 instance count 8 → 10 with explicit
  round attribution; `<this-commit>` placeholder filled with
  `b4d4512`; CLAUDE.md R-NEW-72 carry-forward bullet refreshed
  with round-1 + round-2 cadence application notes).
- **Round 4** `1a92955` (0 P1 + 2 P2 + 0 P3):
  - **P2-1 (W4)** — three discriminator strings
    (`'mixed_file_and_value_sets'`,
    `'file_set_on_create_unsupported'`,
    `'file_set_on_bulk_unsupported'`) at cli-design.md
    §5.3 "File-column dispatch leg — mutex rules" were split
    across line boundaries inside inline-code spans; Markdown
    whitespace normalization rendered them with a space inside
    the code span. Flipped prose so each discriminator literal
    lives in a single unbroken backtick pair on one line.
  - **P2-2 (W11/W13/W16)** — three stale single-path
    files-shaped sites while the implementation emits BOTH
    paths joined with ` OR `: cli-design.md:983 inline
    comment; column-types.ts:355-365
    `NoncanonicalColumnTypeDetails` JSDoc; v0.6-plan.md:617
    R-v0.6-NEW-3 stale line reference (flipped to symbol-
    based).
- **Round 5** ratification (0 P1 + 0 P2 + 1 P3): code/contract
  surfaces clean across all 16 audit points (W1-W16). Single
  self-referential R-NEW-72 instance-count metadata flip (10 →
  12) applied at close-docs without a separate fix-up commit
  per the R-NEW-84 spirit — process-only cadence updates skip
  Codex re-review because they don't touch code or contract
  surfaces. No round 6 needed (further self-referential rounds
  would loop indefinitely: every fix-up bumps the count by 1,
  the next round sees it as stale by 1).

**IMPL Codex review estimate**: 3-4 fix-up rounds per the same
precedent (M35's 7-round outlier was opaque-JSON-driven; M38's
dispatch-routing surface mirrors M34 / M36 / M37 cadences more
closely).

**Actual IMPL Codex review outcome (closed
`3c2a9b0..63adb40`)**: 4 fix-up rounds + 1 ratification round
(round-5 with the close-docs out-of-band EOF whitespace P3
applied inline per R-NEW-84 spirit). **Cumulative IMPL findings:
0 P1 + 3 P2 + 11 P3 across 4 fix-up rounds** — within the M22 /
M27 / M30 / M31 / M32 / M34 / M35 / M36 / M37 write-surface
IMPL precedent (0-3 P2 + 3-10 P3 typical); ~1 round above
M36's clean 3-round cadence, driven by the round-1 substantive
refactor (catch-and-route → resolution-boundary pre-check) that
surfaced additional warning-threading + archived-guard gaps
across rounds 2-3. Per-round detail:

- **Pre-feat lift `3c2a9b0`** (refactor, no Codex pass — lift
  is consumer-count-driven, gates verify): R-v0.6-NEW-1 lift
  of `precheckLocalFile` + `buildBlobFromPath` to
  `src/utils/file-source.ts` (169 lines new; ~50-line
  byte-identical pattern consolidated across M31 `item upload`
  + `update upload` action bodies + 10 unit tests pinning the
  branch matrix). 3-consumer trigger satisfied at IMPL kickoff
  per R-NEW-29's M25 cadence; M38 consumes the helper from day
  one via `executeFileColumnSet`.

- **IMPL feat `e749931`**: runtime bodies for
  `executeFileColumnSet` + `enforceSingleFileColumnSet` at
  `src/api/file-column-set.ts`; live + dry-run dispatch wired
  in `src/commands/item/set.ts` (inline `resolution.match.column
  .type === 'file' && !isRaw` check on live path; catch-and-
  rewrap from `planChanges` translator rejection on dry-run);
  catch-and-route action-body branches in `item update`
  single-item live + dry-run + bulk dry-run loop + bulk live
  + `item create` dry-run + live (caught
  `unsupported_column_type/type:file` and rewrapped as M38
  reason discriminators). Schema unions widened
  (`itemSetOutputSchema` + `itemUpdateOutputSchema` extended
  to `z.union([projectedItemSchema, fileColumnSetOutputSchema])`).
  9 new integration tests + 10 enforceSingleFileColumnSet
  runtime unit tests + 2 M38 envelope snapshots.

- **Round 1** `9b9f60d` (0 P1 + 2 P2 + 3 P3):
  - **P2-1 (W11)** — `--set-raw <file-col>=<json>` hijack on
    item update / bulk / create. The catch-and-route pattern
    routed on `unsupported_column_type + details.type === 'file'`,
    which is identical for friendly `--set` and `--set-raw`
    rejections (both go through the same factory). Raw-only
    file writes were rewrapped as
    `file_set_on_bulk_unsupported` / `file_set_on_create_unsupported`
    / `internal_error` instead of staying as the D3 permanent
    `unsupported_column_type` rejection.
  - **P2-2 (W4)** — D2/D5/D6 mutex priority was translator-
    order dependent. `--set bad_date=invalid --set
    attachments=path` surfaced the date error before the M38
    mixed-set mutex check because the translator iterates
    `--set` entries in argv order; the catch only fired if
    translation reached the file entry.

    Both P2s share the same root cause: mutex enforcement
    ran at the translator-rejection boundary instead of the
    column-resolution boundary that cli-design §5.3 step 5
    mandates. Fix introduces `preCheckM38FileDispatch` at
    `src/api/file-column-set.ts` — a synchronous helper that
    resolves `setEntries` column types upfront via
    `resolveColumnWithRefresh` + runs
    `enforceSingleFileColumnSet` BEFORE the translator path.
    The pre-check inspects setEntries only, so `--set-raw`
    rejections route through `translateRawColumnValue`'s D3
    permanent rejection unchanged (P2-1 fixed). Mutex
    priority fires upfront regardless of translator order
    (P2-2 fixed). Bulk + create pre-check short-circuits the
    rejection at the column-resolution boundary BEFORE the
    items_page walker / confirmation gate (bulk) and BEFORE
    planCreate / resolveAndTranslate (create), saving the
    walker round-trip + `--yes` prompt on bulk.

    `runItemUpdateSingleFileDispatchDryRun` +
    `runItemUpdateSingleFileDispatchLive` +
    `buildBulkFileRejection` + `buildCreateFileRejection`
    helpers from the IMPL feat collapsed into a unified
    `runItemUpdateSingleFileDispatch` helper that takes the
    pre-check's `kind: 'file'` result and dispatches per
    `isDryRun`. The bulk + create catch handlers dropped
    entirely (rejection happens at the pre-check now).
  - **P3-1 (out-of-band)** — single-item `item update` file
    dispatch dropped resolver warnings on the live success
    envelope. Fix threads `m38.warnings` into emitMutation
    via the new unified helper.
  - **P3-2 (W10)** — `docs/output-shapes.md` mixed-error
    example used `file_column_id` + `value_column_ids` keys;
    runtime emits `column_id`, `non_file_set_count`,
    `set_raw_count`, `has_name`. Example flipped to runtime
    shape.
  - **P3-3 (W10)** — `docs/v0.6-plan.md` §3 M38 status block
    still described "pre-flight kickoff this session" post-
    IMPL feat. Flipped to "IMPL shipped end-to-end".

  Envelope snapshots refreshed: `item create` top-level live
  + dry-run flipped `source: 'live' → 'mixed'`. The pre-
  check's live BoardMetadata fetch + downstream cache hit
  yields a mixed aggregate per §6.1 source-aggregation rules
  — contractually correct; the pre-IMPL `'live'` claim was
  under-counting wire activity.

- **Round 2** `28d2356` (0 P1 + 1 P2 + 3 P3):
  - **P2-1 (W3/W4)** — archived-column guard bypass.
    `preCheckM38FileDispatch` resolved with
    `includeArchived: true` but didn't check `r.match.column
    .archived === true` before returning `kind: 'file'` or
    throwing M38 mutex errors. `item update --set
    archived_file_col=<path> --dry-run` could emit a successful
    file `planned_change` or reach live multipart dispatch
    instead of the stable `column_archived` contract.

    Fix mirrors `resolveAndTranslate`'s pass-(a) archived
    guard inside the pre-check loop. Each
    `resolveColumnWithRefresh` result aggregates source/cacheAge
    + pushes resolver warnings BEFORE the archived check;
    archived check fires BEFORE the entry is pushed to the
    working set AND BEFORE `enforceSingleFileColumnSet` runs.
    Throw uses `buildColumnArchivedError(...)` (canonical
    M5b R19 shape) folded through
    `foldResolverWarningsIntoError(...)`.

  - **P3-1 (W7/W8)** — M38 pre-check resolver warnings dropped
    on JSON paths. A pre-check `stale_cache_refreshed` would
    be lost because downstream `planChanges` /
    `resolveAndTranslate` re-resolves against the warm cache
    and doesn't re-emit the refresh warning.

    Fix threads `m38.warnings` into every downstream envelope
    emit, deduped via `dedupeWarnings` (existing helper from
    bulk path; new `dedupeCreateWarnings` mirror added for
    item create). 6 sites: update single dry-run + live;
    bulk empty-match + dry-run loop seed + live; create
    dry-run + live.

  - **P3-2 (W5/W6/W11)** — test gaps. Friendly `--set` file
    dispatch was covered but not:
    - `--set-raw <file-col>=<json>` D3 invariant on update
      single / bulk / create (3 new tests at item-update /
      item-update-bulk / item-create).
    - Mutex priority on `--set bad_date=invalid --set
      attachments=path` (1 new test at item-update single).
    - Archived file column on item update (1 new test
      pinning P2-1 regression).

  - **P3-3 (W10)** — stale helper banner at
    `item/update.ts:1260-1273` referenced removed
    `buildBulkFileRejection`. Rewrote.

- **Round 3** `0328823` (0 P1 + 0 P2 + 3 P3):
  - **P3-1 (W4)** — M38 pre-check warnings threaded into
    SUCCESS envelopes at round 2 but NOT into FAILURE
    envelopes. If downstream `planChanges` /
    `resolveAndTranslate` / `planCreate` throws, the thrown
    error only carries downstream resolver warnings (its
    own internal fold); pre-check warnings lost.

    Fix introduces `mergeResolverWarningsIntoError` helper at
    `src/api/resolver-error-fold.ts` that reads the existing
    `details.resolver_warnings`, dedupes against additional
    warnings by code+message+token, and re-folds via the
    existing `foldResolverWarningsIntoError`. 6 catch sites
    in item update + create now use the merge helper in
    `if (err instanceof MondayCliError && m38.warnings.length
    > 0)` guards.

  - **P3-2 (W4)** — bulk live `resolverWarnings` (the
    narrowed list passed into `foldAndRemap` for fail-fast +
    partial-success bulk results) was built from
    `resolutionResult.warnings` only. Fix seeds the slot with
    deduped `m38Warnings + resolutionResult.warnings` so
    per-item failure + partial-success records include the
    pre-check leg.

  - **P3-3 (W6/W10)** — 3 multi-line discriminator-literal
    backtick spans (R-v0.6-NEW-5 rendering discipline)
    reflowed at cli-design.md §5.3 mutex-rules subsection +
    output-shapes.md M38 mutex rules + v0.6-plan.md §8 D2
    closure prose. Each `'<discriminator>'` literal lives in
    a single-line code span; `details.reason` path lives in a
    separate backtick span outside the literal.

- **Round 4** `63adb40` (0 P1 + 0 P2 + 2 P3):
  - **P3-1 (W3)** — `mergeResolverWarningsIntoError` JSDoc
    stale (said pre-check warnings come first but code
    iterates existing warnings first). Reworded.
  - **P3-2 (W10)** — R-v0.6-NEW-5 sweep missed 2 more sites
    at `v0.6-plan.md:233` (§3 M38 rejection prose) +
    `cli-design.md:1710` (§4.3 inventory comment). Both
    reflowed.

- **Round 5** ratification (0 P1 + 0 P2 + 1 P3 out-of-band):
  EOF whitespace at `item/create.ts:1225` +
  `item/update.ts:1453` (`git diff --check` flagged trailing
  blank lines). Applied inline at close-docs without a separate
  fix-up commit per R-NEW-84 spirit (out-of-band whitespace
  is cosmetic, not contract-flipping; the M38 IMPL round-5
  P3 mirrors the M38 pre-flight round-5 P3 cadence).
  Code/contract surfaces: clean across all 16 audit points;
  W1-W16 all "nothing flagged".

### v0.6 release-prep (closed at `98185d4..<this-commit>`; published at `<post-publish-commit>`)

5 commits with diff + 1 zero-diff envelope-snapshot probe folded
into this close-docs prose (the v0.5 release-prep precedent —
zero-diff probes don't warrant their own commit). Mirrors the
v0.4 / v0.5 release-prep cadence verbatim modulo the M38-specific
slips:

- **`98185d4`** — `docs(v0.6-release-prep): slip stale v0.6
  deferral to v0.7 + ToC v0.6-M38 friendly file --set sync`.
  R-NEW-82 graduated cross-doc grep ran + caught one stale
  `deferred_to: "v0.6"` slot (multi-level subitem `--parent`
  rejection — slipped to `"v0.7"` across 3 sites: runtime
  literal at `src/commands/item/create.ts:622` + integration
  test pin at `tests/integration/commands/item-create.test.ts:
  1091`+`:1120` + doc prose narrative at `docs/output-shapes.md:
  2418`) + one ToC drift (v0.6-M38 friendly file `--set`
  annotation missing from `docs/output-shapes.md:61` `item
  (mutations)` row — M38 close-docs added substantive output-
  shape sections at lines 2100/2151/2243 but didn't propagate
  the v0.6-M38 tag to the ToC verb-row; sibling drift mirrors
  v0.5-M34's team-writer ToC gap that v0.5 release-prep ToC
  audit caught — R-NEW-82 graduated lesson). 3 files / 9 lines
  changed.
- **`cd4d2a9`** — `docs(readme): v0.6 quickstart + scope
  refresh`. New step 13 demonstrates M38 friendly file `--set`
  on item set + item update + dry-run (renumbers downstream
  steps 13-16 → 14-17). Scope section reshaped: v0.6.0 (current)
  / v0.5.0 (previous) / v0.4.0 (prior) / v0.3.0 (earlier) /
  v0.2.0 (foundation-of-mutations) / v0.1.0 (foundation
  milestone). New "What v0.6 added (M38)" per-milestone bullet.
  v0.7 (next) block enumerates carry-forward backlog +
  v0.6.x carve-outs. OAuth deferral version reference bumped
  to v0.6.0; `cli_version` sample bumped 0.5.0 → 0.6.0.
  1 file / 92 insertions / 44 deletions.
- **`e41b36d`** — `chore(v0.6-release-prep): bump version
  0.5.0 → 0.6.0 + audit-fix fast-uri`. package.json field flip
  + matching package-lock.json regeneration. Audit-fix folded
  in: `npm audit` flagged `fast-uri@3.1.0` (high severity —
  GHSA-q3j6-qgpj-74h6 path traversal + GHSA-v39h-62p7-jpjc
  host confusion); `npm audit fix` cleanly resolved to
  `fast-uri@3.1.2` (non-breaking — `ajv@8.20.0`'s `^3.0.1`
  constraint satisfies 3.1.x). `npm audit` reports
  `0 vulnerabilities` post-fix. 2 files / 6 changed.
- **`988482e`** — `docs(v0.6-release-prep): CHANGELOG [0.6.0]
  entry — files-shaped friendly --set writes (M38)`. Mirrors
  the `[0.5.0]` entry shape verbatim (Breaking changes / Surface
  / Output contract additions / Upgrade notes / Internals /
  Tests + quality gates / Documentation). Single-milestone v0.6
  scope (M38 only) → "Surface" section reads as a single
  milestone narrative vs v0.5's four-milestone aggregate. Load-
  bearing M38 facts captured: D1 sibling-branch dispatch routing
  + D2 / D3 mutex rules + D3 `--set-raw` permanent rejection
  + D5 / D6 bulk + create rejection reasons + D8 zero new
  ERROR_CODES (registry stays at 29). 1 file / 277 insertions.
- **`<this-commit>`** — `docs(v0.6-release-prep-close):
  close-docs sweep — status flip + §7 ticks + §3 release-prep
  + §22 R-NEW-82 ratification`. CLAUDE.md Status block flipped
  to "v0.6.0 ready for publish — pending user push authorization
  + npm publish". Live numbers refreshed (4100 + 1 skipped tests
  unchanged; 99.26 / 96.46 / 99.31 / 99.52 coverage held at
  1.01pp branches margin; package.json 0.6.0; annotated v0.6.0
  tag pending user authorization). Next-session pointer flipped
  to "v0.7 kickoff candidate-selection (externally blocked on
  v0.6.0 push + npm publish first)". §7 release exit checklist
  every-line tick. §3 release-prep close subsection added (this
  block). §22 R-NEW-82 entry flipped to past-tense 4th-
  consecutive ratification.

**v0.6 release-prep release-prep findings:** Contract drift
findings = 3 stale `deferred_to: "v0.6"` sites + 1 ToC drift,
all caught at the cluster's R-NEW-82 audit step (mirrors v0.5
release-prep's 14 + 1 audit-step findings — lower v0.6 count
reflects the single-milestone scope: only one v0.6 feature
(M38) shipped vs v0.5's four). Decisions closed = none (zero
new contract surface at release-prep). R-class state = no
code-lift movement at release-prep; R-NEW-82 ratifies at
4th-consecutive consumer (M28 + v0.4 + v0.5 + v0.6); R-NEW-84
ratifies at 6th supporting instance (v0.6 release-prep IS
the symmetric positive-case application — zero production
`src/**/*.ts` semantic changes modulo the literal `'v0.6'`
→ `'v0.7'` flip in the multi-level subitem rejection slot,
which is mechanical not semantic; Codex review skipped; gates
alone verified).

**Lessons folded forward:**

1. **R-NEW-82's 4th-consecutive ratification** confirms the
   graduated discipline holds firm — every release-prep cluster
   to date has caught ≥1 stale `deferred_to: "v<currently-
   releasing-version>"` slot AND ≥1 ToC drift sub-class
   finding. The cross-doc grep recipe + the three per-site
   decisions (slip / drop slot / drop rejection) + the ToC
   row drift sub-class audit-step remain stable.
2. **R-NEW-84's release-prep symmetric application** — single-
   feature-milestone release-prep clusters (v0.6) and four-
   feature-milestone release-prep clusters (v0.5) both fire
   the rule cleanly; the rule is scope-invariant. Future
   release-preps with N feature milestones (small or large)
   apply the rule by the same gate (zero production semantic
   changes).
3. **Audit-fix folded into version-bump per security.md
   "high = merge blocker"** — establishes a v0.6 release-prep
   precedent for the version-bump-+-audit-fix combo when
   `npm audit` flags a high-severity transitive issue between
   IMPL close and release-prep. v0.5 release-prep did NOT
   surface an audit-fix (no high-severity advisories in the
   lockfile during that cluster); v0.6 release-prep DID +
   handled inline. Future release-preps inherit the inline-
   audit-fix pattern when applicable.

## 4. Cross-cutting "definition of done" per command

Identical to v0.4-plan §4 / v0.5-plan §4. Each new surface needs:

- Argv schema with zod + parse-boundary `usage_error` wrap.
- Action body wired through `parseArgv` → `resolveClient` → action
  logic → `emitMutation` / `emitDryRun`.
- Unit tests for the argv parse boundary + translator-boundary
  dispatch routing (mutex enforcement, single-vs-multi rejection,
  file-vs-value branching).
- Integration tests covering the dispatch leg's branches (happy /
  reject paths) using fixture cassettes for the multipart wire
  (M31's pattern + `MultipartTransport` test seam).
- cli-design §4.3 row updates for the affected verbs (item set,
  item update).
- output-shapes.md envelope updates for the file-column dispatch
  legs.
- Pre-flight Codex review → IMPL → IMPL Codex review →
  close-docs sweep.

## 5. Testing strategy

Mirrors v0.5-plan §5: unit tests at parse boundary; integration
tests using fixture cassettes for the multipart wire (M31's
existing `MultipartTransport` test seam — `ctx.multipart` slot);
e2e tests left at the v0.4-M31 baseline (no new e2e patterns
needed; the dispatch leg adopts M31's e2e shape).

## 6. Risk register

- **R-NEW-58 lift-ahead-of-feat at IMPL kickoff.** File-pre-check
  + Blob-construction pattern crystallizes a 3-consumer lift
  trigger at M38 IMPL (M31's `item upload` + `update upload` +
  M38's `--set` dispatch). Lift target: `src/utils/file-source.ts`
  (new) or extend `src/api/assets.ts` with a sibling export.
  Mitigation: lift fires at IMPL kickoff per R-NEW-29's M25
  cadence; pre-flight stubs reference the future helper site by
  name so IMPL plumbing stays straightforward.

- **R-NEW-72 cross-doc grep widening to module-prose sites.** M38
  pre-flight contract diff touches ≥10 files across `src/api/`,
  `src/commands/`, `docs/`. Post-fix-up cross-doc grep must extend
  to `src/commands/index.ts` (per R-v0.5-NEW-19 graduated) +
  noun-stem regex matching (per v0.5-M37 lesson). Mitigation:
  enumerate sibling-site noun-stems at pre-flight kickoff +
  re-run grep after every contract-flipping Codex fix-up.

- **R-v0.4-W2 "non-retryable rewrap placement" axis (2nd
  consumer).** M31 IMPL P2-1 surfaced the `file_too_large`
  rewrap-inside-retry-thunk pattern unique to multipart-over-
  retryable-network-error. M38 reuses M31's `addFileToColumn`
  verbatim (rewrap pattern already INSIDE the retry thunk per
  the M31 IMPL fix), so the watch-item fires safely-by-
  construction at M38 — no new substantive code surface. Check
  at pre-flight contract diff: confirm M38's reuse of
  `addFileToColumn` is direct (no rewrap re-implementation).

- **R-NEW-41 "Wire-vs-CLI semantics documentation" 4th
  supporting site (architecture.md).** The `--set` syntax is
  type-uniform from the agent's view (`<col>=<value>`), but for
  file columns the value is a path and the dispatch transitions
  silently from JSON to multipart at the translator boundary.
  This is a documented asymmetry that ought to be cross-linked
  to `docs/architecture.md`'s "Wire-vs-CLI semantics" section.
  Mitigation: extend the section's enumeration at pre-flight +
  add an inline pointer in the new `src/api/file-column-set.ts`
  module docstring.

## 7. v0.6 release exit checklist

Mirrors v0.5-plan §7 verbatim. At v0.6 release-prep, the cluster
followed the v0.4 / v0.5 release-prep cadence (5 commits with
diff + 1 zero-diff envelope-snapshot probe folded into close-docs
prose per the v0.5 precedent: ToC audit / `deferred_to: "v0.6"`
slip + README v0.6 quickstart + version bump + audit-fix +
CHANGELOG `[0.6.0]` entry + close-docs sweep). R-NEW-82 graduated
discipline applied: release-prep cross-doc grep for stale
`deferred_to: "v0.6"` slots ran at the ToC-audit step.

Exit checklist (every line ticked at v0.6 release-prep close):

- [x] Envelope-snapshot refresh probe ran clean (zero diff vs M38
  IMPL close) — folded into this close-docs prose per the v0.5
  precedent rather than warranting a separate commit.
- [x] R-NEW-82 cross-doc grep ran — caught one stale
  `deferred_to: "v0.6"` slot (multi-level subitem `--parent`
  rejection at `src/commands/item/create.ts:622` + matching
  integration test pin + `docs/output-shapes.md:2418` prose +
  narrative) + one ToC drift (v0.6-M38 friendly file `--set`
  annotation missing from `docs/output-shapes.md:61` `item
  (mutations)` row). Slipped to `"v0.7"` + ToC annotated at
  commit `98185d4`. 4th consecutive consumer ratification of
  R-NEW-82's graduated discipline (M28 + v0.4 + v0.5 + v0.6).
- [x] README v0.6 quickstart + scope refresh shipped at
  `cd4d2a9`. New step 13 demonstrates M38 friendly file `--set`
  on item set + item update + dry-run; renumbered downstream
  steps 14-17. Scope section reshaped around v0.6.0 (current)
  / v0.5.0 (previous) / v0.4.0 (prior) / v0.3.0 (earlier) /
  v0.2.0 / v0.1.0 per-version layout. v0.7 (next) block
  enumerates the carry-forward backlog + v0.6.x carve-outs.
- [x] Version bump `0.5.0` → `0.6.0` shipped at `e41b36d`
  (package.json + package-lock.json two-site flip). `npm audit`
  flagged a transitive `fast-uri@3.1.0` (high severity);
  `npm audit fix` cleanly resolved to `3.1.2` (non-breaking).
  Audit-fix folded into the version-bump commit per security.md
  "high = merge blocker" rule. `npm audit` reports
  `0 vulnerabilities` post-fix.
- [x] CHANGELOG `[0.6.0]` entry shipped at `988482e`
  (277 insertions; mirrors `[0.5.0]` shape verbatim — Breaking
  changes / Surface / Output contract additions / Upgrade
  notes / Internals / Tests + quality gates / Documentation
  sections).
- [x] CLAUDE.md Status block flipped to "v0.6.0 ready for
  publish — release-prep closed end-to-end" (this commit).
  v0.6-plan §3 release-prep subsection added at the end of
  the M38 entry (this commit). §22 R-NEW-82 entry flipped
  to past-tense 4th-consecutive ratification (this commit).
  Next-session pointer flipped from "v0.6 release-prep" to
  "v0.7 kickoff candidate-selection (externally blocked on
  v0.6.0 push + npm publish first)".
- [x] **Live numbers** at v0.6.0 release-prep close: 4100 + 1
  skipped tests across 172 files (unchanged from M38 IMPL close
  — release-prep added zero new tests); coverage 99.26 / 96.46 /
  99.31 / 99.52 at floor 95 / 95.45 / 95 / 95 (branches margin
  **1.01pp**, was 1.00pp at v0.5.0); 29 ERROR_CODES; 117
  commands; `package.json` 0.6.0.
- [x] Annotated `v0.6.0` tag created locally + pushed to
  `origin/main` (tag points at `51ba1a4`, the close-docs sweep
  commit — published at the cluster's final step).
- [x] GitHub release `v0.6.0` created from the CHANGELOG body
  (live at https://github.com/Firer/monday-cli/releases/tag/v0.6.0).
- [x] `npm publish` shipped to npm `latest` dist-tag —
  `monday-cli@0.6.0` published 2026-05-18T16:30:21Z (per the
  v0.5.0 publish precedent's `npm whoami` step). Post-publish
  CLAUDE.md status-flip + npm timestamp record landed at
  `2a16d91`.

## 8. Decisions (open questions to close before each milestone)

### M38 — files-shaped friendly `--set` writes

- **D1 — Dispatch routing layer.** Where does `<file-col>=<path>`
  branch off the standard `--set` value-translator path?
  - **Closed at pre-flight contract diff (this session).** SIBLING
    BRANCH at the command action body, AFTER column resolution.
    The translator (`translateColumnValueAsync`) stays JSON-output-
    shaped for the 13 existing writable types; a new sibling
    module at `src/api/file-column-set.ts`
    (`enforceSingleFileColumnSet` mutex-check + `executeFileColumn
    Set` stub fetcher) handles the file-column dispatch leg.
    Routing decision fires at the action body level: after column
    resolution, if `column.type === 'file'` AND single-`--set` AND
    no value-`--set` / `--set-raw` / `--name`, route to
    `executeFileColumnSet`; otherwise route to the standard
    translator + `executeItemMutation` path.

- **D2 — Mutex rules across `--set` / `--set-raw` / `--name`.**
  What mixing combinations does M38 accept?
  - **Closed at pre-flight contract diff.** EXACTLY ONE
    `--set <file-col>=<path>` per call, no other `--set` /
    `--set-raw` / `--name` flags allowed. Mutex enforcement at
    the column-resolution boundary (parse-time can't know — the
    column type only resolves after metadata loads). Rejection
    surfaces:
    - File `--set` + ANY value `--set` / `--set-raw` / `--name`
      → `usage_error` carrying `'mixed_file_and_value_sets'`
      at `details.reason`.
    - 2+ file `--set` entries → `usage_error` carrying
      `'multi_file_set_unsupported'` at `details.reason`.

- **D3 — `--set-raw <file-col>=<json>`.** Does M38 lift the
  existing `--set-raw` rejection for file columns?
  - **Closed at pre-flight contract diff.** NO. Files have no
    JSON wire shape Monday's `change_column_value` accepts —
    `add_file_to_column` is the only file-write wire surface and
    it's multipart-only. The `--set-raw` escape-hatch contract
    ("user supplies the JSON `change_column_value` accepts")
    doesn't compose with multipart. M38 ONLY ships the friendly
    `--set <file-col>=<path>` form; the existing rejection at
    `src/api/raw-write.ts:translateRawColumnValue` stays
    unchanged. Prose flips slightly to note "M38 ships the
    friendly `--set` form but `--set-raw` for files stays
    rejected".

- **D4 — Dry-run envelope shape.** What does `--set <file-col>=
  <path> --dry-run` emit?
  - **Closed at pre-flight contract diff.** Mirrors M31 `item
    upload --dry-run` envelope verbatim:
    `planned_changes: [{operation: 'add_file_to_column',
    item_id, column_id, file_path, filename, file_size_bytes}]`
    (size from `fs.stat()`; no file bytes loaded into memory).
    `meta.source: 'none'`. The `file_path` slot echoes the
    argv-derived (relative or absolute) path per M31 IMPL round-
    2 P3-2 fix. The `column_id` slot echoes the RESOLVED column
    ID (not the argv token).

- **D5 — Bulk `item update --where ... --set <file-col>=<path>`.**
  Allowed at M38?
  - **Closed at pre-flight contract diff.** REJECTED with
    `usage_error.details.reason: 'file_set_on_bulk_unsupported'`
    at the resolution boundary. Defers to v0.6.x candidate-
    selection. Rationale: per-item file dispatch + `--continue-
    on-error` partial-success envelope + `--concurrency` shared-
    transport semantics each carry additional design dimensions
    (per-item retry timing for multipart; per-item asset slot in
    envelope shape; concurrent multipart over the same
    `MultipartTransport`) worth their own milestone cluster. M38
    ships single-item only.

- **D6 — `item create --set <file-col>=<path>`.** Allowed at M38?
  - **Closed at pre-flight contract diff.** REJECTED with
    `usage_error.details.reason: 'file_set_on_create_unsupported'`
    at the column-resolution boundary. Defers to v0.6.x.
    Rationale: file upload at create time would require either
    (a) `create_item` with placeholder `column_values` followed
    by a post-create `add_file_to_column` (non-atomic, breaks
    §5.8 state safety) or (b) a Monday wire mutation that
    accepts multipart file parts inside `create_item.column_
    values` (no such mutation exists at API `2026-01`). Until
    Monday surfaces an atomic create-with-file shape, M38 keeps
    the rejection.

- **D7 — `<path>='-'` stdin support.** Allowed at M38?
  - **Closed at pre-flight contract diff.** OUT OF SCOPE for
    M38. Mirrors M31 `monday item upload`'s rejection rationale
    — no clean `--filename` companion shape pinned for `--set
    <file-col>=-` syntax (stdin reads byte-anonymously; the
    filename is the load-bearing handle for Monday's wire
    `Asset.name` slot). Defers to v0.6.x once a clean extension
    shape is designed (likely as a `--set <file-col>=-`-paired
    `--filename <n>` flag or a `<file-col>=stdin:<filename>`
    embedded form).

- **D8 — ERROR_CODES delta.** New codes at M38?
  - **Closed at pre-flight contract diff.** ZERO. Registry stays
    at 29. All M38-specific rejections route through existing
    `usage_error` / `unsupported_column_type` / `not_found` /
    `validation_failed` codes with `details.reason`
    discrimination (per cli-design §6.5's "details.reason
    discriminator" pattern from M14 / M27 / M31).

## 9. Preconditions

### M38 — files-shaped friendly `--set` writes pre-flight

Gate before the M38 pre-flight feat commit ships:

- [x] Clean `main` at HEAD; tree clean; `git status` empty.
- [x] All gates green (`npm run typecheck && npm run lint &&
  npm test`).
- [x] v0.5.0 published to npm + `v0.5.0` annotated tag pushed to
  `origin/main`.
- [x] CLAUDE.md "Next session" block names M38 pre-flight as the
  next session (annotated at v0.6 kickoff candidate-selection at
  `85a478c`).
- [x] R-NEW-58 2-consumer R-class scan at pre-flight kickoff:
  surface any 2-/3-consumer R-class crystallizations. Known
  candidates flagged in CLAUDE.md "Next session" block (M31's
  `dispatchMultipart` 1 → 2, `ResolvedClient.multipart` 1 → 2,
  `sniffContentType` 2 → 3, R-NEW-41 potential 4th site,
  R-v0.4-W2 "non-retryable rewrap" 2nd consumer). Lift AHEAD of
  feat per R-NEW-29's M25 cadence ONLY when coverage threatens
  to drop below floor OR duplication-cost analysis justifies
  it. None fire at pre-flight (M31 fetcher reuse is direct);
  re-evaluate at M38 IMPL kickoff.
- [x] Empirical probe — NOT REQUIRED. Wire shape is M31's
  `add_file_to_column` verbatim. Translator-boundary dispatch
  design is contract diff work, not probe work.
- [x] Pre-flight Codex review template ready
  (`.claude/templates/codex-pre-flight-review.md`) — template-
  stable W1 (redactor) + W2 (operation-name parity) audit-points
  apply; M38-specific watch-items W3-W{N} land at the pre-flight
  Codex prompt assembly.

## 10. Post-mortem template

Mirrors v0.4-plan §10 verbatim. Per-milestone: round-by-round
Codex review summary + contract drift findings + lessons learned
+ R-class candidate filings. Lands at the milestone close-docs
commit.

## 22. R-class register

> **Status:** placeholder — populated at M38 close-docs commit
> with R-class candidates surfaced during pre-flight + IMPL Codex
> reviews. Mirrors v0.4-plan §22 / v0.5-plan §22 structure.

### Watch-items carried forward from v0.5

- **R-NEW-58 lift-ahead-of-feat** (graduated at v0.4-M31 IMPL).
  Fires at every IMPL kickoff scan. M38 IMPL kickoff scan target:
  file-pre-check + Blob-construction pattern (M31 `item upload` +
  M31 `update upload` + M38 `--set` dispatch = 3 consumers at
  IMPL).

- **R-NEW-72 post-fix-up cross-doc grep** (graduated at v0.4-M33;
  8th instance ratified at v0.5-M37 IMPL). Search paths per
  R-v0.5-NEW-19 graduation: `src/api/*.ts` + `src/commands/**/*.ts`
  + `src/commands/index.ts` + `docs/*.md`. Noun-stem regex
  matching per v0.5-M37 lesson.

- **R-NEW-75 candidate-selection 5-dimension framework**
  (graduated at v0.4-M33). v0.6 kickoff session ratified the
  framework for the 4th consecutive cycle.

- **R-NEW-76 parseArgv-before-c8** (graduated at v0.5-M34).
  M38 pre-flight applies the discipline to 1 new stub branch
  (item set file-dispatch) — consumer 15+ post-graduation. The
  parallel branches in `item update` (single + bulk) + `item
  create` land at M38 IMPL; at pre-flight those paths still
  surface the existing translator's `unsupported_column_type`
  rejection for files-shaped columns.

- **R-NEW-82 release-prep deferred-to slip audit** (graduated at
  v0.5 release-prep). **4th-consecutive consumer ratified at
  v0.6 release-prep close** (M28 + v0.4 + v0.5 + v0.6). The
  cross-doc grep ran at commit `98185d4` (the cluster's first
  commit) and caught one stale `deferred_to: "v0.6"` slot
  (multi-level subitem `--parent` rejection — slipped from
  v0.4 → v0.5 → v0.6 → v0.7 across three prior release-preps;
  Monday's `sub_items_board` still carries no `subtasks` column
  at API `2026-01`, so the slip is data-model-gated, not
  feature-prioritization-gated) + one ToC drift sub-class
  finding (v0.6-M38 friendly file `--set` annotation missing
  from `docs/output-shapes.md:61` `item (mutations)` row;
  M38 close-docs added the substantive output-shape sections
  at lines 2100/2151/2243 but didn't propagate the `v0.6-M38`
  tag to the ToC verb-row). Both findings remediated at the
  same commit. The `deferred_to: "v0.6.x"` slots in
  `src/api/file-column-set.ts:472, 503, 533` (D5 / D6 / D7
  closures) STAY at `"v0.6.x"` — those are forward-looking
  deferrals to within the v0.6 series (bulk + create + multi-
  file + stdin file-set carve-outs), NOT stale slips. Rule
  graduation holds firm at 4 consecutive consumers.

- **R-NEW-84 skip Codex review on mechanical clusters**
  (graduated at v0.5-M34 negative-case; re-ratified at v0.5
  release-prep symmetric application). M38 pre-flight ships
  production `src/**/*.ts` code surface, so Codex review applies
  per the standard cadence — rule correctly does NOT fire the
  skip.

- **R-NEW-41 Wire-vs-CLI semantics documentation conventions**
  (graduated at v0.4-M31). 4th supporting site filed at M38
  pre-flight — the `--set` syntax is type-uniform from the
  agent's view, but for file columns the value is a path and the
  dispatch transitions silently from JSON to multipart at the
  translator boundary. The M38 asymmetry is a DISTINCT 4th
  asymmetry vs the existing M31 multipart-vs-JSON transport
  asymmetry: M38 operates at the AGENT-INPUT boundary, M31's #3
  operates at the wire boundary. The architecture.md section's
  enumeration extended from THREE to FOUR; the expansion rule
  flipped from "the 4th" to "the 5th".

### New R-class candidates surfaced at M38 pre-flight

R-class movements at v0.6-M38 pre-flight close (`0cb8b69`):

- **R-v0.6-NEW-1 — File pre-check + Blob-construction shared
  helper. SHIPPED at v0.6-M38 IMPL kickoff (`3c2a9b0`).**
  Lifted to `src/utils/file-source.ts` (NEW module) as two
  exports: `precheckLocalFile(rawPath)` (fs.stat + fs.access(R_OK)
  + non-empty + size capture; surfaces `usage_error` with
  `details.reason: 'file_not_readable' | 'file_empty'`) +
  `buildBlobFromPath(precheck)` (readFile + sniffContentType +
  Blob construction). 3 consumers post-lift: M31 `monday item
  upload` action body + M31 `monday update upload` action body
  + M38's `executeFileColumnSet` runtime body. 10 direct unit
  tests at `tests/unit/utils/file-source.test.ts` pin the
  branch matrix (happy / not-a-file / ENOENT / EACCES / empty
  / Blob sniff content-type for png/pdf/octet-stream).
  Behaviour-preserving lift — all M31 integration tests pass
  unchanged across the migration. Mirrors R-NEW-29's M25
  ahead-of-feat cadence + R-NEW-70's M34 lift-ahead-of-feat
  cadence. Cross-references R-NEW-29 + R-NEW-58 + R-v0.4-W2
  axis 7.

- **R-v0.6-NEW-2 — `details.reason` discriminator pattern for
  mutex-violation rejection paths.** Four supporting instances
  at M38 pre-flight (`mixed_file_and_value_sets` /
  `multi_file_set_unsupported` / `file_set_on_create_unsupported`
  / `file_set_on_bulk_unsupported` — all defined inline at
  `docs/cli-design.md` §5.3 "File-column dispatch leg" subsection
  + the v0.6-plan §8 D2/D5/D6 closures). At pre-flight the
  discriminators are documented but the action body branches that
  surface them live at M38 IMPL — only one of the four
  (`mixed_file_and_value_sets`) becomes agent-reachable in
  `item update` single-item; the other three fire across
  `item update --where`, `item create`, and within
  `item update` multi-file-set. **Priority: LOW** — process
  discipline, NOT a code lift; graduates to a permanent cli-
  design §6.5 row OR a `docs/architecture.md` "Per-error reason-
  discriminator conventions" section at the 5th supporting site
  (likely a future v0.6.x milestone introducing additional
  reason slots — e.g., bulk file-set lift adding `file_set_with_
  concurrency_unsupported` or similar). Pattern: when a single
  ERROR_CODE serves multiple semantic failure modes within a
  milestone, distinguish via `details.reason` literal-string
  enum — agents key off the discriminator without needing the
  parsed message. Cross-references R-NEW-31 (discriminated-union
  per-status detail schema; orthogonal — R-NEW-31 is per-shape,
  R-v0.6-NEW-2 is per-rejection-cause-within-same-shape) +
  cli-design §6.5 stable-error-code-registry conventions.

- **R-v0.6-NEW-3 — `categorizeNoncanonicalColumnType`
  `suggestedWritePath` multi-path string-concatenation pattern.**
  One supporting instance at M38 pre-flight (`src/api/column-
  types.ts` `categorizeNoncanonicalColumnType` `files_shaped`
  branch — the row now returns a human-readable string joining
  two write paths with " OR ":
  `'monday item set <iid> <file-col>=<path> (v0.6-M38; friendly)
  OR monday item upload <iid> --column <col> <file> (v0.4-M31;
  verb-shaped)'`). The string-concatenation pattern works today
  but doesn't compose cleanly when future column types reach
  N+1 write paths — the consumer (the `noncanonical_column_type`
  warning emitter at `src/commands/board/column-create.ts:442`)
  echoes the string verbatim into the warning envelope, where
  agents have to parse the " OR " separator to enumerate
  alternatives. **Priority: LOW** — process discipline + code
  shape; lift candidate is widening
  `NoncanonicalColumnTypeDetails.suggestedWritePath` from
  `string | null` to `readonly string[] | null` (one entry per
  write path) + updating the warning emitter to project the
  array to `details.suggested_write_paths: string[]` (plural)
  + a back-compat `suggested_write_path: string` slot mirroring
  the first entry for v0.5.0-era agents. Fires at 2nd consumer
  (any future writable column type with N>1 write paths — e.g.,
  if a v0.6.x lift adds a `--set-raw <file-col>=<asset_id_json>`
  form via a new Monday wire mutation, the `files_shaped` row
  grows a 3rd path; or if a future hierarchical column type
  surfaces multiple write paths from the start). Cross-references
  R-NEW-41 (asymmetric wire-vs-CLI documentation conventions —
  the M38 4th-asymmetry filing surfaces the same "multi-path
  agent surface" pattern; R-v0.6-NEW-3 is the structured-data
  shape for representing it).

Additional R-class candidates surfaced during M38 pre-flight
Codex review rounds (`b9c5ff4..adeb431`):

- **R-v0.6-NEW-4 — Catch-and-rewrap pre-flight stub shim for
  dispatch legs targeting previously-rejected wire shapes.
  PARTIALLY COLLAPSED at v0.6-M38 IMPL round-1.** The IMPL
  round-1 refactor (catch-and-route → resolution-boundary
  pre-check via `preCheckM38FileDispatch`) eliminated the
  catch-and-rewrap pattern at item update single + bulk + item
  create paths. **Item set dry-run retains a catch-and-rewrap
  shape at IMPL** because the single-positional argv has no
  mutex to enforce + the existing shim from pre-flight still
  works correctly at IMPL (the `isRaw` discriminator catches
  `--set-raw` rejections cleanly; the dry-run catch rewraps
  the translator's `unsupported_column_type/type:file`
  rejection as the D4 dry-run envelope shape instead of the
  pre-flight-stub `internal_error`). Pattern stays as a
  1-consumer watch-item (item-set-only) post-IMPL. The
  source-aggregation-preserving alternative-to-pre-screening
  rationale documented at the pre-flight P2-1 surfacing event
  carries forward; at IMPL with pre-screening for item update /
  create, the snapshot drift (`source: 'live' → 'mixed'` for
  non-file paths) was accepted as contract-correct (the
  pre-IMPL `'live'` claim was under-counting wire activity per
  §6.1 — the pre-check + downstream cache hit IS a mixed
  aggregate). Cross-references R-NEW-76 (parseArgv-BEFORE-c8
  sibling pre-flight-stub discipline) + R-NEW-72 (post-fix-up
  cross-doc grep) + R-v0.6-NEW-6 (the general dry-run/live
  mirror discipline; partially superseded by pre-check at
  resolution boundary) + R-v0.6-NEW-8 (the merge-resolver-
  warnings helper that the refactor surfaced as a separate
  watch-item).

- **R-v0.6-NEW-5 — Discriminator-literal Markdown-rendering
  preservation discipline. GRADUATED at v0.6-M38 IMPL close.**
  When documenting `details.reason` discriminator strings in
  Markdown prose, the literal MUST live in a single unbroken
  backtick pair on one line. Markdown's whitespace
  normalization renders multi-line backtick-spans with a space
  inside the code span (e.g., `'mixed_file_and_value_sets'`
  rendered as `mixed_file_and_ value_sets`), breaking agents
  who key on the literal string. **5 supporting instances**
  cumulative across pre-flight + IMPL: pre-flight round-4
  (cli-design.md §5.3 — 3 broken literals caught), IMPL
  round-3 (3 doc sites reflowed: cli-design.md / output-
  shapes.md / v0.6-plan.md), IMPL round-4 (2 more sites at
  v0.6-plan.md:233 + cli-design.md:1710 caught by the round-3
  sweep's noun-stem grep extension). Graduation trigger: the
  4th-5th instance crossed the "fires consistently across
  rounds" threshold. Discipline promotes to:
    - A permanent Codex pre-flight + IMPL template W{N}
      audit-point (extension of W6 in the existing template:
      "R-v0.6-NEW-5 — Verify every discriminator-literal
      backtick span lives on a single line; multi-line
      backtick-spans render with whitespace inside the code
      span and break the agent-keyed string contract"). Land
      at the next pre-flight session touching a milestone
      with `details.reason` discriminators.
    - A cross-doc grep target alongside R-NEW-72's noun-stem
      matching: `grep -P 'reason:\s*\n\s*'\\''` and
      `grep -P '\\``[^`\n]+\n\s*[^`\n]+`\\``'` to catch
      multi-line backtick spans.
  Pattern: when writing `` `'<discriminator_literal>'` `` in
  Markdown prose, break the surrounding prose around the
  literal rather than through it; prefer phrasing like
  `usage_error` carrying `'<literal>'` at `details.reason`
  over `usage_error.details.reason: '<literal>'` when the
  full path would wrap. Cross-references R-v0.6-NEW-2 (per-
  rejection-cause discriminator pattern; same literals,
  different typesetting concern) + R-NEW-72 (post-fix-up
  cross-doc grep — now extended with R-v0.6-NEW-5 grep
  targets) + W6 audit-point of `.claude/templates/codex-pre-
  flight-review.md`.

- **R-v0.6-NEW-6 — Dry-run / live-path dispatch-stub mirror
  discipline.** For any new pre-flight stub of a dispatch leg,
  BOTH dry-run and live paths must surface the stub. The
  dry-run path commonly routes through a planner helper that
  invokes the translator; when the planner internally rejects
  the column type / wire shape the dispatch leg also targets,
  the translator's existing rejection fires BEFORE the action
  body's M{N} dispatch check, causing the dry-run to bypass
  the M{N} stub. The discipline: at every pre-flight stub of
  a new dispatch leg, audit the dry-run path explicitly. Two
  implementation alternatives (R-v0.6-NEW-4 captures the
  source-aggregation-preserving variant; an upfront resolution
  + check works for paths whose planner doesn't aggregate
  source across resolution legs). 1 supporting instance at M38
  pre-flight (caught at round-1 P2-1; the live path had the
  stub at `src/commands/item/set.ts:319` but the dry-run path
  through `planChanges` bypassed it). **Priority: LOW** —
  process discipline / Codex pre-flight template W{N} audit-
  point candidate. Fires at 2nd consumer; at 3rd, fold into the
  Codex pre-flight template as a new W{N} audit-point. Pattern
  at template surface: "For every new pre-flight stub of a
  dispatch leg, verify BOTH dry-run and live paths surface the
  stub — the dry-run path commonly routes through a planner
  helper that invokes the translator's pre-existing rejection
  BEFORE the M{N} dispatch check." Cross-references
  R-v0.6-NEW-4 (the catch-and-rewrap shim that implements this
  discipline for M38; sibling — R-v0.6-NEW-4 is the SPECIFIC
  code-side pattern, R-v0.6-NEW-6 is the GENERAL discipline) +
  R-NEW-76 (parseArgv-BEFORE-c8 sibling pre-flight-stub
  discipline; graduated at v0.5-M34).

- **R-v0.6-NEW-7 — Self-referential bookkeeping metadata cycle
  / R-NEW-84 sub-class.** Provenance metadata that updates on
  every fix-up (R-class instance counts, commit-SHA placeholders
  that fill in retroactively, status-marker timestamps) creates
  a self-referential cycle under strict zero-P3 Codex
  ratification: each fix-up bumps the count / fills the
  placeholder by 1, the next Codex round sees the new state as
  stale by 1, surfacing as a P3. R-NEW-84's spirit (skip Codex
  on process-only / mechanical clusters) applies to break the
  loop, but the SPECIFIC sub-pattern wasn't formalized at
  R-NEW-84's graduation at v0.5-M34. 3 supporting instances —
  2 at M38 pre-flight (round-3 P3-1 + round-5 P3-1) + 1 at
  M38 IMPL round-5 (out-of-band EOF whitespace P3 applied
  inline at close-docs `<this-commit>` without a separate
  Codex re-review per R-NEW-84's spirit). Graduates to a
  CLAUDE.md "Workflow rules" carve-out at the 3rd consumer
  trigger — but the v0.6-M38 IMPL close-docs application is
  itself the 3rd consumer's "did the discipline fire correctly
  at this milestone close?" question; the answer is yes (the
  round-5 EOF whitespace fix landed inline + the
  round-5-as-ratification claim holds). Promotion to
  permanent CLAUDE.md "Workflow rules" entry below at v0.6
  release-prep close OR the next IMPL milestone that surfaces
  a 4th supporting instance. **Priority: LOW** — process
  discipline / R-NEW-84 sub-class. Fires at 3rd supporting
  instance — likely a future multi-round Codex review cluster
  that surfaces another self-referential bookkeeping P3. At
  3rd consumer, graduate to a CLAUDE.md "Workflow rules"
  carve-out OR fold into R-NEW-84's permanent entry as a
  sub-bullet: "Self-referential bookkeeping fix-ups
  (provenance metadata that updates as part of the round
  itself — R-class instance counts, commit-SHA placeholders
  filled in retroactively, status timestamps) don't trigger
  another Codex round; apply at close-docs without a separate
  Codex re-review per R-NEW-84's spirit. Without the carve-out,
  the round cadence would loop indefinitely: every fix-up bumps
  the count by 1, the next round sees it as stale by 1." Cross-
  references R-NEW-84 (skip-Codex-on-mechanical, graduated at
  v0.5-M34) + R-NEW-72 (post-fix-up cross-doc grep — the source
  of the self-reference; each fix-up's grep extension is itself
  a R-NEW-72 instance) + R-NEW-82 (release-prep deferred-to
  slip audit — same R-NEW-84 application precedent).

Additional R-class candidates surfaced during M38 IMPL Codex
review rounds (`9b9f60d..63adb40`):

- **R-v0.6-NEW-8 — `mergeResolverWarningsIntoError` helper —
  read existing fold, dedupe, re-fold pattern.** Surfaced at
  v0.6-M38 IMPL round-3 P3-1: `foldResolverWarningsIntoError`'s
  contract is "replace `details.resolver_warnings`" — calling
  it twice with two separate warning sources discards the
  first call's warnings. The IMPL round-3 fix introduced
  `mergeResolverWarningsIntoError` at
  `src/api/resolver-error-fold.ts:79-141` which:
    - Reads existing `details.resolver_warnings` defensively
      (Array.isArray guard for the `unknown`-typed slot).
    - Dedupes by code+message+token; existing warnings
      (downstream) come first so additional warnings dedupe
      against them.
    - Re-folds via `foldResolverWarningsIntoError` with the
      combined list.
  1 consumer today (6 use sites across `item/update.ts` +
  `item/create.ts` catch handlers for the M38 pre-check +
  downstream two-leg warning aggregation pattern). **Priority:
  LOW** — process discipline + code lift; the helper itself
  is shipped + the consumer count is well-bounded (6 sites,
  all in the same milestone's IMPL). Fires at 2nd consumer
  when another action body adopts the pre-check-then-downstream
  warning-aggregation shape (likely v0.6.x bulk file-set lift
  OR v0.7 multi-leg dispatch — both inherit the same shape).
  At 3rd consumer, the helper crosses the R7/R8 graduation
  threshold for promotion to a permanent "Two-fold warning
  aggregation" helper in the standard merger toolkit. Cross-
  references R-NEW-19 (ApiError wrap discipline; orthogonal —
  R-NEW-19 is about wrap-at-parse-boundary, R-v0.6-NEW-8 is
  about merge-existing-fold) + R-v0.6-NEW-4 (the catch-and-
  rewrap shim that the round-1 refactor superseded; the
  merge helper was surfaced by that refactor's downstream
  error-path handling).

Additional R-class candidates surfaced at the post-M38-IMPL
refactor-audit sweep (`b5ec37b` close-docs + this audit commit):

- **R-v0.6-NEW-9 — `sampleFileBoard` test fixture lift to
  `_item-fixtures.ts`.** 6 supporting instances across the M38
  IMPL test surface: `tests/integration/commands/item-set.test.ts`
  (2 inline `fileBoard = {...}` consts — one each in the D3
  `--set-raw` test + the v0.6-M38 describe block), `tests/
  integration/commands/item-update.test.ts` (3 inline `fileBoard`
  consts — the main M38 describe block's fixture + 2 ad-hoc
  variants for archived-column + date-column scenarios in
  round-2 tests), `tests/integration/commands/item-update-bulk
  .test.ts` (1 inline `fileBoard`), `tests/integration/commands/
  item-create.test.ts` (1 inline `fileBoard`), `tests/integration/
  envelope-snapshots.test.ts` (1 inline `fileBoard` for the M38
  envelope-snapshot block). Per-site shape variation:
    - Minimal: 1 file column only (`attachments`).
    - 1 file + 1 status: M38 dispatch path tests use this
      shape to exercise the mixed-set mutex rule.
    - 2 file columns: multi-file_set_unsupported mutex test in
      item-update.
    - file + date: mutex-priority test in item-update (round-2
      `--set due=invalid --set attachments=path`).
    - file (archived: true): archived-column guard test in
      item-update.
  **Priority: MEDIUM** — at the project's 3-consumer lift
  threshold AND the duplicated surface is ~14 lines per site ×
  6 sites = ~84 lines of test-fixture boilerplate that adds no
  test value. Lift target: extend `_item-fixtures.ts` with
  `sampleFileBoard(opts?)` factory taking optional shape hints
  (`numFileColumns`, `archived`, `extraColumns: ['status' |
  'date']`). Per-site variation can flow through the factory
  options rather than parametrising the column type union (the
  union risk is over-fitting; the factory shape stays narrow).
  Cross-references R-NEW-8 (set-delta helpers; orthogonal — that
  was about predicate factoring, this is about fixture factoring)
  + the historical `useItemTestEnv` lift (post-M5b cleanup) as a
  precedent for test-helper consolidation. Fires immediately —
  lift is ready when this candidate fires; deferred to a follow-
  up audit session because the M38 IMPL cluster is already shipped
  and fixture lift would best ship as its own focused commit.

- **R-v0.6-NEW-10 — `dedupeWarnings` cross-site lift.** 2
  supporting instances: `src/commands/item/update.ts:dedupeWarnings`
  + `src/commands/item/create.ts:dedupeCreateWarnings`. The two
  helpers are byte-identical apart from the rename (the
  `dedupeCreateWarnings` body is identical to the
  `dedupeWarnings` body; only the multi-line vs single-line
  parameter-annotation formatting differs cosmetically). Both
  dedupe by `code + message + details.token`. Surfaced at v0.6-
  M38 IMPL round-2 P3-1 when item create's pre-check warning
  threading needed a dedupe helper + the bulk-path helper from
  `item/update.ts` wasn't reachable (same-folder commands but
  not in a shared helper module). **Priority: LOW** — at the
  rule-of-three watch-threshold; lift fires at 3rd consumer.
  Lift target: `src/api/resolver-error-fold.ts` (sibling to
  `foldResolverWarningsIntoError` + `mergeResolverWarningsIntoError`)
  OR `src/utils/output/envelope.ts` (sibling to `Warning` type
  + the dedupe semantics). The resolver-error-fold.ts location
  reads more natural because the helper deduplicates resolver
  warnings specifically (column_token_collision /
  stale_cache_refreshed) rather than generic `Warning`
  shapes. Cross-references R-v0.6-NEW-8
  (`mergeResolverWarningsIntoError` — the lift target's
  natural neighbour) + R-NEW-14/15/16's "missed earlier triggers,
  mass-migrate when surfaced" cadence (the dedupe helper has
  shipped twice now under different names; the next consumer
  triggers the consolidation).

- **R-v0.6-NEW-11 — `withMergeResolverWarnings` async wrapper
  for the merge-helper call pattern.** 6 supporting instances
  across `src/commands/item/update.ts` + `src/commands/item/
  create.ts` for the M38 pre-check-then-downstream error-path
  threading. Each catch site repeats the same 4-line pattern:

  ```ts
  try { result = await downstreamCall(); }
  catch (err) {
    if (err instanceof MondayCliError && m38.warnings.length > 0) {
      throw mergeResolverWarningsIntoError(err, m38.warnings);
    }
    throw err;
  }
  ```

  Lift target: `withMergeResolverWarnings<T>(thunk: () =>
  Promise<T>, additionalWarnings: readonly ResolverWarning[]):
  Promise<T>` async wrapper at `src/api/resolver-error-fold.ts`
  (sibling to `mergeResolverWarningsIntoError`). Saves
  5 lines × 6 sites = 30 lines, replaces the try/catch noise
  with `const result = await withMergeResolverWarnings(() =>
  downstreamCall(), m38.warnings);`. **Priority: LOW** —
  process discipline + code shape; the inline 4-line pattern
  is readable, and the lift would only shave 1 line per site
  net (`try {...} catch {...}` → `withMergeResolverWarnings`)
  while introducing a "withX" helper. Fires at 8th consumer
  OR if v0.6.x adds another pre-check-then-downstream warning
  aggregation site. Cross-references R-v0.6-NEW-8
  (`mergeResolverWarningsIntoError` itself; the wrapper sits
  alongside) + R-NEW-29's M25 cadence (lift-ahead-of-feat;
  doesn't apply here because no upcoming milestone has
  signalled the pattern's next consumer).

R-class state carried forward at M38 IMPL close:

- **R-NEW-58 lift-ahead-of-feat** stays ratified; M38 IMPL
  kickoff scan target = R-v0.6-NEW-1 above (3-consumer trigger
  crystallizing at IMPL).
- **R-NEW-72 post-fix-up cross-doc grep** stays ratified — 12
  instances post-M38 pre-flight Codex rounds (8 instances at
  v0.5-M37 IMPL close + 4 new at v0.6-M38). At M38: round-1
  fix-up `b9c5ff4` was the 9th instance (caught a trailing
  comment in `tests/unit/api/column-values.test.ts`); round-2
  fix-up `5808fd4` was the 10th (swept "5th supporting" /
  "THREE to FIVE" / "2 → 4" stems and identified historical
  v0.5-M37-era sites correctly left untouched); round-3 fix-up
  `30204a3` was the 11th (provenance metadata sweep for stale
  `<this-commit>` placeholders); round-4 fix-up `1a92955` was
  the 12th (broken-discriminator-string sweep + stale-single-
  path files-shaped prose sweep + R-class register provenance
  sweep, all returned clean post-fix). Round-5 ratification
  surfaced no new contract-flipping fix-ups — only this
  self-referential R-NEW-72 instance-count metadata flip.
  Search paths per R-v0.5-NEW-19 graduation extension applied
  through all four rounds (`src/api/*.ts` +
  `src/commands/**/*.ts` + `src/commands/index.ts` +
  `docs/*.md` with noun-stem regex matching).
- **R-NEW-76 parseArgv-BEFORE-c8** consumer 15+ applied at the
  M38 dispatch branch in `src/commands/item/set.ts` (the c8
  ignore wrap fires AFTER `parseArgv` + `splitSetExpression` +
  `resolveBoardId` + `resolveColumnWithRefresh`).
- **R-NEW-41 4th asymmetry filed** at `docs/architecture.md`
  "Wire-vs-CLI semantics documentation conventions" — M38's
  translator-boundary dispatch asymmetry. The architecture.md
  section's enumeration extended from THREE to FOUR. Expansion
  rule flipped from "the 4th" to "the 5th" (the 5th asymmetry,
  when it surfaces, follows the same discipline).
- **R-NEW-84 skip-Codex-on-test-only-clusters** does NOT apply
  to the M38 pre-flight commit `0cb8b69` — production
  `src/**/*.ts` code changes shipped (new `src/api/file-column-
  set.ts` module + new dispatch branch in `src/commands/item/
  set.ts` + prose flips in `src/api/{column-types,column-values,
  raw-write}.ts`). Codex pre-flight review applies per the
  standard cadence; opens at the next session.

Cross-cutting fix-up at the post-pre-flight audit
(`b4d4512`):

- **Pre-flight stub error-class alignment.** The two stubs in
  `src/api/file-column-set.ts` (`executeFileColumnSet` +
  `enforceSingleFileColumnSet`) originally threw `UsageError`;
  the post-pre-flight audit flipped both to
  `ApiError('internal_error', ...)` to match the M35 / M36 /
  M37 pre-flight stub convention (pre-flight stubs are CLI-
  internal "not implemented yet" gaps, not agent usage errors;
  the exit code 2 for `internal_error` is the correct semantic).
  Both stubs are c8-ignored so the change is functionally
  unreachable at runtime; the fix-up aligns the convention so
  IMPL session swap-in is consistent. NOT filed as a new
  R-class candidate — the convention is already established
  across M35/M36/M37 and this was a one-off bug, not a new
  pattern.
